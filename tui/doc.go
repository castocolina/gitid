// Package tui provides the Bubble Tea TUI for gitid. It wires a Bubble Tea
// program to the internal packages for identity management, displaying a
// doctor dashboard on startup and supporting form-based identity creation
// and editing. It imports internal packages but is never imported by them —
// the dependency arrow is strictly one-directional.
//
// Implementation lands in a later phase (Phase 5).
package tui
