---
phase: 04-doctor
plan: 04
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - internal/doctor/checks/signing.go      # OVERWRITES the Plan 01 stub (same path; CheckSigning + CheckAgent signatures)
  - internal/doctor/checks/signing_test.go
autonomous: true
requirements: [DOC-05]
must_haves:
  truths:
    - "Doctor reports ssh-agent unreachable as a warning with a start-agent fix (DOC-05, D-18)"
    - "Doctor warns per gitid-managed identity whose key is not currently loaded in the agent (DOC-05, D-18)"
    - "Doctor warns when git < 2.36 and a managed includeIf uses hasconfig: (DOC-05, D-20)"
    - "Agent classification uses BOTH ssh-add exit code AND output text for portability (RESEARCH Pitfall 1)"
    - "signing.go is OVERWRITTEN in place (the Plan 01 stub file holding CheckSigning + CheckAgent), not added — no redeclaration; `go build ./... && go test ./internal/doctor/...` passes after the overwrite"
  artifacts:
    - path: "internal/doctor/checks/signing.go"
      provides: "CheckSigning() + CheckAgent() — ssh-add probe, fingerprint match, git-version gate (DOC-05); OVERWRITES the Plan 01 stub (both funcs live here)"
      contains: "func CheckAgent"
    - path: "internal/doctor/checks/signing_test.go"
      provides: "fake RunSSHAdd / RunSSHKeygenFingerprint driven agent + version tests"
      contains: "func TestAgentUnreachable"
  key_links:
    - from: "internal/doctor/checks/signing.go"
      to: "doctor.Deps.RunSSHAdd / RunSSHKeygenFingerprint / GitVersionAtLeast"
      via: "injected subprocess seams (no live agent in unit tests)"
      pattern: "deps\\.RunSSHAdd"
    - from: "CheckAgent fingerprint matching"
      to: "account.PubPath"
      via: "ssh-keygen -lf SHA256 token present in ssh-add -l output"
      pattern: "SHA256"
---

<objective>
The Signing/Agent slice (DOC-05): probe ssh-agent reachability via `ssh-add -l` with portable
exit-code+text classification, warn per gitid-managed identity whose key is not loaded (fingerprint
match against `ssh-keygen -lf`), and warn when git < 2.36 with a `hasconfig:` includeIf. This plan
OVERWRITES the single per-family stub file Plan 01 created (`internal/doctor/checks/signing.go`, which
holds BOTH the CheckSigning and CheckAgent stubs) in place — same path, same signatures — so there is no
function redeclaration. After this plan the `=== Signing ===` and `=== Agent ===` sections show real findings.

Purpose: This is the one family with genuinely new subprocess logic (the others compose existing readers).
The ssh-add exit-code semantics and fingerprint matching are isolated, injectable seams so unit tests
need no live agent. Runs in parallel with Plans 02/03 — only its own checks/signing.go stub is overwritten.

Output: OVERWRITTEN internal/doctor/checks/signing.go (real CheckSigning + CheckAgent replacing the stubs)
+ new internal/doctor/checks/signing_test.go.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-doctor/04-CONTEXT.md
@.planning/phases/04-doctor/04-RESEARCH.md
@.planning/phases/04-doctor/04-PATTERNS.md
@.planning/phases/04-doctor/04-UI-SPEC.md
</context>

## Artifacts this plan produces

- `checks.CheckAgent(doctor.Deps) []Finding` and `checks.CheckSigning(doctor.Deps) []Finding` — both OVERWRITE the Plan 01 stub file `internal/doctor/checks/signing.go` in place (the stub file already declares BOTH; this plan replaces both bodies, keeping the exported signatures byte-identical) — DOC-05.
- Internal helpers (planner-discretion names, all pure + testable): `classifyAgentState(output string, exitCode int)`, `extractFingerprint(keygenLine string) string`, `isKeyLoaded(agentOutput, pubPath string, runFp func(string)(string,error)) bool`.
- Note: the gpg.format=ssh and allowed_signers email==user.email locked-value checks (the "signing wiring" value half of DOC-05) are implemented in Plan 03 CheckCoherence (D-17 carve-outs). This plan owns the ssh-agent + git-version + key-loaded half. CheckSigning here renders the agent-reachable/version findings under the Signing/Agent families; if it is cleaner to keep gpg.format here too, coordinate via SUMMARY — but do NOT duplicate the check across both plans.

> Wave-2 redeclaration rule (BLOCKER 1): signing.go already exists as the Plan 01 stub holding both
> CheckSigning and CheckAgent. This plan does NOT create a new file alongside it — it replaces both stub
> bodies. Read the frozen `doctor.Deps` field set from `04-01-SUMMARY.md` first so the RunSSHAdd /
> RunSSHKeygenFingerprint / GitVersionAtLeast wiring matches Plan 01's locked contract (WARNING 4).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: OVERWRITE signing.go stubs — ssh-agent probe + classification + fingerprint matching (RED→GREEN)</name>
  <files>internal/doctor/checks/signing.go, internal/doctor/checks/signing_test.go</files>
  <read_first>
    - .planning/phases/04-doctor/04-01-SUMMARY.md (the FROZEN doctor.Deps field set + per-family file map — signing.go is the stub holding CheckSigning + CheckAgent that this plan overwrites)
    - internal/deps/deps.go (GitVersionAtLeast lines 47-73 exec.Command pattern + G204 annotation — the subprocess template to mirror)
    - internal/doctor/doctor.go (Deps.RunSSHAdd, Deps.RunSSHKeygenFingerprint, Deps.GitVersionAtLeast, Deps.Identities, Finding/Severity from Plan 01)
    - internal/identity/identity.go (Account.PubPath, Account.KeyPath, Account.Matches — Matches carries the includeIf strategy for the hasconfig: D-20 check)
    - .planning/phases/04-doctor/04-RESEARCH.md §"Pattern 3: ssh-agent Probing" (exit-code table 0/1/2, classifyAgentState, extractFingerprint, isKeyLoaded; `ssh-add -L` NOT needed), §"Pitfall 1" (use exit code AND text), §"Pitfall 7" (guard fingerprint on missing pub)
    - .planning/phases/04-doctor/04-CONTEXT.md D-18 (agent reachable + per-managed-key loaded; scoped to gitid keys only), D-20 (git<2.36 + hasconfig: warning via GitVersionAtLeast(2,36)), D-05 (all these are warning severity)
    - .planning/phases/04-doctor/04-UI-SPEC.md §"Copywriting Contract" DOC-05 entries (exact copy for agent-unreachable, key-not-loaded, git<2.36; all warning, none carry [fix] — agent loading is report-only per D-03)
  </read_first>
  <behavior>
    - Test (TestClassifyAgentState): (out, 0)→runningWithKeys; (out, 1) or text "no identities"→runningEmpty; (anything, 2) or exec error→unreachable.
    - Test (TestExtractFingerprint): "256 SHA256:abc comment (ED25519)" → "SHA256:abc"; line without SHA256 → "".
    - Test (TestAgentUnreachable): fake RunSSHAdd returns ("", 2) → one Agent warning "ssh-agent: not reachable", fix mentions `eval "$(ssh-agent -s)"`; no per-key findings emitted when unreachable.
    - Test (TestAgentKeyNotLoaded): agent running, output lacks an identity's fingerprint → per-identity warning "identity \"<name>\": key not loaded in agent", fix "ssh-add ~/.ssh/gitid_<name>".
    - Test (TestAgentKeyLoaded): agent output contains the identity's SHA256 → no finding for that identity.
    - Test (TestAgentMissingPubSkipped): account whose PubPath Stat would fail → skipped (Pitfall 7: coherence already reports it; no false "not loaded").
    - Test (TestGitVersionGate): an identity with a hasconfig: match strategy + GitVersionAtLeast(2,36)==false → warning "git <ver>: hasconfig: not supported"; with GitVersionAtLeast true → no finding.
  </behavior>
  <action>
    RED: write signing_test.go covering the pure helpers (classifyAgentState, extractFingerprint, isKeyLoaded) and CheckAgent/CheckSigning via fake doctor.Deps (fake RunSSHAdd returning crafted (output, exitCode); fake RunSSHKeygenFingerprint returning crafted SHA256 lines; fake GitVersionAtLeast; crafted Identities incl. one with a hasconfig: Match). (signing.go already holds the Plan 01 `_`-param stubs for both funcs; the RED test fails against them.) Commit `test(04-04): ...`.
    GREEN: OVERWRITE internal/doctor/checks/signing.go — replace both stub bodies (keep CheckSigning + CheckAgent exported signatures byte-identical to the Plan 01 stub). classifyAgentState/extractFingerprint/isKeyLoaded as pure helpers per RESEARCH Pattern 3. CheckAgent(deps): call deps.RunSSHAdd(); classify; if unreachable → one warning Agent finding (no per-key findings, no [fix]); if running → for each gitid-managed identity (guard: skip if PubPath absent, Pitfall 7) compute isKeyLoaded via deps.RunSSHKeygenFingerprint; emit a per-identity warning Agent finding when not loaded (no [fix], agent loading report-only D-03). CheckSigning(deps): the git-version gate (D-20) — if any account's Matches uses the hasconfig: strategy AND !deps.GitVersionAtLeast(2,36) → warning "git <ver>: hasconfig: not supported" with the upgrade hint. (The gpg.format=ssh + allowed_signers email carve-outs live in Plan 03 CheckCoherence — do not duplicate here; if SUMMARY coordination moves them here instead, ensure exactly one owner.) Family assignment: agent findings → FamilyAgent; signing/version findings → FamilySigning. Mirror the deps.go exec G204 annotation pattern in the REAL RunSSHAdd/RunSSHKeygenFingerprint implementations that the cmd layer (Plan 01 buildDoctorDeps) injects — but note those real impls are wired in cmd; this file only consumes the injected fns. Run green, commit `feat(04-04): ...`.
  </action>
  <verify>
    <automated>go test ./internal/doctor/checks/... -run 'TestClassifyAgentState|TestExtractFingerprint|TestAgent|TestGitVersionGate' && go build ./... && go test ./internal/doctor/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go test ./internal/doctor/...` passes after the overwrite — CheckSigning and CheckAgent each defined exactly once: `grep -rn 'func CheckSigning\|func CheckAgent' internal/doctor/checks/` returns exactly two matches (both in signing.go).
    - ssh-add exit 2 → Agent warning "not reachable"; exit 1/"no identities" → running-empty (per-key warnings), exit 0 → match per key (RESEARCH Pitfall 1: code AND text).
    - A managed key absent from the agent output yields a per-identity warning with fix `ssh-add ~/.ssh/gitid_<name>`; a loaded key yields none.
    - git<2.36 with a hasconfig: identity yields a warning; git>=2.36 yields none (D-20).
    - No Agent finding carries `[fix]` (agent loading is report-only, D-03).
    - `make lint` passes; `grep -rn 'internal/filewriter' internal/doctor/` returns nothing; unit tests use only fakes (no live agent).
  </acceptance_criteria>
  <done>Signing/Agent families detect agent reachability, per-key load state (fingerprint match), and the git-version hasconfig: gate (signing.go stub overwritten in place), fully fake-tested with no live agent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess → agent check | ssh-add -l, ssh-keygen -lf invoked with fixed arg slices (no shell, no user-interpolated args) |
| ssh-agent socket → doctor | SSH_AUTH_SOCK is read by ssh-add; doctor never sets or mutates it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | exec ssh-add / ssh-keygen (real impls injected by cmd) | mitigate | arg-slice `exec.Command("ssh-add","-l")` / `("ssh-keygen","-lf",pubPath)` form, no shell; pubPath is a gitid-managed Account.PubPath; G204 annotated |
| T-04-13 | Information Disclosure | fingerprint / agent output handling | mitigate | only SHA256 fingerprints + key comments are read (public material); private-key content never read or logged; report prints identity name + path, not key bytes |
| T-04-14 | Denial of Service | ssh-add hanging on a dead socket | accept | ssh-add -l returns promptly with exit 2 on a dead/unset socket (RESEARCH exit-code table); no timeout wrapper needed for v1; low risk |
| T-04-15 | Tampering | doctor core gaining write capability | mitigate | signing.go imports no filewriter; agent findings are report-only (no Fix descriptors); no mutation path |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `go test ./internal/doctor/checks/... -run 'TestClassifyAgentState|TestExtractFingerprint|TestAgent|TestGitVersionGate'` green.
- `go build ./... && go test ./internal/doctor/...` pass after the stub overwrite — CheckSigning and CheckAgent each defined exactly once in package checks (no redeclaration).
- `make lint` passes; `grep -rn 'internal/filewriter' internal/doctor/` returns nothing.
- `gitid doctor` now shows real findings under `=== Signing ===` and `=== Agent ===`.
- RED→GREEN commits present (`git log --grep='^test(04-04)'`, `--grep='^feat(04-04)'`).
- Manual (per VALIDATION.md): `ssh-add -l; gitid doctor` — Agent section matches actual loaded keys.
</verification>

<success_criteria>
- ssh-agent reachability + per-managed-key load state reported as warnings (DOC-05, D-18), scoped to gitid keys only.
- git<2.36 + hasconfig: warning works via GitVersionAtLeast(2,36) (DOC-05, D-20).
- Classification uses exit code AND text (portable across OpenSSH versions, Pitfall 1).
- Agent loading is report-only; write-free core preserved (D-03/D-01); signing.go stub overwritten in place with no redeclaration.
</success_criteria>

<output>
Create `.planning/phases/04-doctor/04-04-SUMMARY.md` when done (RED/GREEN commits, confirm where gpg.format/email carve-outs are owned to avoid duplication with Plan 03, the real RunSSHAdd/RunSSHKeygenFingerprint signatures the cmd layer must inject, confirmation that signing.go was overwritten in place and `go build ./...` is green).
</output>
</output>
