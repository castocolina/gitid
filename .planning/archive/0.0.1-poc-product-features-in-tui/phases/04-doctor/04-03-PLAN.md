---
phase: 04-doctor
plan: 03
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - internal/doctor/checks/coherence.go   # OVERWRITES the Plan 01 stub (same path, same CheckCoherence signature)
  - internal/doctor/checks/coherence_test.go
  - internal/doctor/checks/orphans.go      # OVERWRITES the Plan 01 stub (same path, same CheckOrphans signature)
  - internal/doctor/checks/orphans_test.go
autonomous: true
requirements: [DOC-03, DOC-04]
must_haves:
  truths:
    - "Doctor reports an IdentityFile / includeIf fragment that no longer resolves as an error finding (DOC-03)"
    - "Doctor reports a missing IdentitiesOnly yes and a missing allowed_signers line as error findings (DOC-03)"
    - "Doctor reports orphans (fragment with no includeIf, key with no Host block) under a distinct Orphans family, separate from Coherence (DOC-04, D-09/D-10)"
    - "An unused key is a warning only, with wording admitting gitid cannot confirm it is unused, and no [fix] marker (D-12/D-13/D-03)"
    - "coherence.go and orphans.go are OVERWRITTEN in place (the Plan 01 stub files), not added — no CheckCoherence/CheckOrphans redeclaration; `go build ./... && go test ./internal/doctor/...` passes after the overwrite"
  artifacts:
    - path: "internal/doctor/checks/coherence.go"
      provides: "CheckCoherence() — existence/resolution checks + locked-value carve-outs (DOC-03, D-15/D-17); OVERWRITES the Plan 01 stub"
      contains: "func CheckCoherence"
    - path: "internal/doctor/checks/orphans.go"
      provides: "CheckOrphans() — ListBlocks-vs-disk cross-reference, unused-key check (DOC-04); OVERWRITES the Plan 01 stub"
      contains: "func CheckOrphans"
  key_links:
    - from: "internal/doctor/checks/coherence.go"
      to: "doctor.Deps.Identities (identity.Reconstruct result) + Stat"
      via: "per-account KeyPath/FragmentPath resolution + Incomplete→Coherence mapping"
      pattern: "Incomplete"
    - from: "internal/doctor/checks/orphans.go"
      to: "injected managed-block names + every Host IdentityFile in ~/.ssh/config"
      via: "cross-reference key against ALL Host blocks (D-12) to classify unused-key"
      pattern: "IdentityFile"
---

<objective>
Two report families built on identity.Reconstruct and managed-block enumeration: Coherence (DOC-03) —
existence/resolution of IdentityFile, includeIf fragment, IdentitiesOnly yes, allowed_signers line, plus
the bounded locked-value carve-outs — and Orphans (DOC-04) — artifacts on disk with no owning managed
block, kept strictly distinct from Coherence. This plan OVERWRITES the two per-family stub files Plan 01
created (`internal/doctor/checks/coherence.go` and `checks/orphans.go`) in place — same path, same
`CheckCoherence`/`CheckOrphans` signature — so there is no function redeclaration. After this plan the
`=== Coherence ===` and `=== Orphans ===` sections show real findings.

Purpose: This is the crux of the "incomplete vs orphan" distinction (Pitfall 5). Coherence = managed block
exists but the thing it references is gone (account.Incomplete). Orphan = artifact exists on disk but no
block claims it. Runs in parallel with Plans 02/04 — only its own two stub files are overwritten.

Output: OVERWRITTEN internal/doctor/checks/{coherence.go, orphans.go} (real implementations replacing the
stubs) + new {coherence_test.go, orphans_test.go}.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-doctor/04-CONTEXT.md
@.planning/phases/04-doctor/04-RESEARCH.md
@.planning/phases/04-doctor/04-PATTERNS.md
@.planning/phases/04-doctor/04-UI-SPEC.md
</context>

## Artifacts this plan produces

- `checks.CheckCoherence(doctor.Deps) []Finding` — OVERWRITES the Plan 01 stub file `internal/doctor/checks/coherence.go` in place (DOC-03 + D-15/D-17; same exported signature).
- `checks.CheckOrphans(doctor.Deps) []Finding` — OVERWRITES the Plan 01 stub file `internal/doctor/checks/orphans.go` in place (DOC-04 + D-09..D-13).

> Wave-2 redeclaration rule (BLOCKER 1): both target files already exist as Plan 01 stubs. This plan does
> NOT create a new file alongside them — it replaces the stub body. Read the frozen `doctor.Deps` field set
> from `04-01-SUMMARY.md` before implementing so the block-name injection + all-Host IdentityFile fields
> match Plan 01's locked contract (WARNING 4).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: OVERWRITE the CheckCoherence stub — existence/resolution + locked-value carve-outs (RED→GREEN)</name>
  <files>internal/doctor/checks/coherence.go, internal/doctor/checks/coherence_test.go</files>
  <read_first>
    - .planning/phases/04-doctor/04-01-SUMMARY.md (the FROZEN doctor.Deps field set + per-family file map — coherence.go is the stub this plan overwrites; wire against the locked Identities/Stat/ReadFile/RunGitConfigGet fields)
    - internal/identity/loader.go (Reconstruct lines 17-22, Account field assignment + Incomplete line 75 — the foundation; Incomplete!="" maps to Coherence NOT Orphans)
    - internal/identity/identity.go (Account struct: KeyPath, PubPath, FragmentPath, AllowedSignersPath, GitEmail, Alias, Incomplete)
    - internal/sshconfig/reader.go (ParseManagedHosts → SSHHostInfo.IdentitiesOnly bool — the IdentitiesOnly check)
    - internal/gitconfig/reader.go (ReadFragment → FragmentInfo incl. GPGFormat/GitEmail; RemoveAllowedSignersLine line ~156 email byte-match `fields[0]==identityEmail` + namespaces="git" pattern — reuse the same exact-match parse for the allowed_signers check)
    - internal/doctor/doctor.go (Deps.Identities, Deps.Stat, Deps.ReadFile (allowed_signers), Deps.RunGitConfigGet, Finding/Severity from Plan 01)
    - .planning/phases/04-doctor/04-CONTEXT.md D-03 (every IdentityFile resolves, includeIf→fragment, IdentitiesOnly yes, allowed_signers line), D-15 (existence/resolution only), D-17 (the ONLY value checks: ignorecase=false [Plan 02], gpg.format=ssh, allowed_signers email==user.email), D-19 (no content compare)
    - .planning/phases/04-doctor/04-UI-SPEC.md §"Copywriting Contract" DOC-03 + DOC-05 gpg.format/allowed_signers-mismatch entries (exact copy + severity + which carry [fix]: IdentitiesOnly & allowed_signers-missing & email-mismatch carry [fix]; IdentityFile/fragment-missing do not)
    - .planning/phases/04-doctor/04-RESEARCH.md §"Pattern 6: Coherence ... Mechanics", Pitfall 6 (allowed_signers byte-identical email match — use ==, not EqualFold)
  </read_first>
  <behavior>
    - Test (TestCoherenceIdentityFileGone): an account with KeyPath set, Stat→os.ErrNotExist → error finding "IdentityFile <path> does not exist", names the identity, no [fix].
    - Test (TestCoherenceFragmentGone): account.FragmentPath Stat→ErrNotExist → error "includeIf fragment <path> does not exist".
    - Test (TestCoherenceIdentitiesOnly): a managed Host with IdentitiesOnly==false → error "Host \"<alias>\": IdentitiesOnly yes missing", [fix] (auto-fixable re-add).
    - Test (TestCoherenceSignersLine): a signing identity (gpg.format ssh) with no matching line in allowed_signers → error "allowed_signers: no entry for <email>", [fix].
    - Test (TestCoherenceGPGFormat): fragment gpg.format != "ssh" → error "gpg.format is \"<actual>\" (expected \"ssh\")".
    - Test (TestCoherenceEmailMismatch): allowed_signers line email not byte-equal to user.email → error "email mismatch", [fix].
    - Test (TestCoherenceIncompleteMapsHere): account.Incomplete != "" surfaces under Coherence (not Orphans).
    - Test (TestCoherenceAllPass): all artifacts resolve, IdentitiesOnly yes, gpg.format ssh, matching signers line → no findings.
  </behavior>
  <action>
    RED: write coherence_test.go with fake doctor.Deps providing crafted []identity.Account, a fake Stat (presence map), a fake ReadFile returning allowed_signers bytes, a fake RunGitConfigGet/fragment reader for gpg.format + email. (coherence.go already holds the Plan 01 `_`-param stub; the RED test fails against it.) Commit `test(04-03): ...`.
    GREEN: OVERWRITE internal/doctor/checks/coherence.go — replace the stub body of CheckCoherence(deps) with the real implementation (keep the exported signature byte-identical to the Plan 01 stub). For each account in deps.Identities: (1) if KeyPath set and Stat→ErrNotExist → error IdentityFile finding; (2) if FragmentPath set and Stat→ErrNotExist → error fragment finding; (3) if the managed Host has IdentitiesOnly==false → error + Fix descriptor (missing-wiring re-add class, D-02; mark fixable, actual fixer is Plan 05); (4) for signing identities: read allowed_signers (deps.ReadFile), find a line whose first field == account.GitEmail (exact ==, Pitfall 6) AND contains namespaces="git"; if absent → error + Fix; if present but email not byte-equal → error email-mismatch + Fix; (5) gpg.format carve-out (D-17): if fragment gpg.format != "ssh" → error finding (no [fix]); (6) account.Incomplete != "" → a Coherence finding describing the missing piece(s). Existence/resolution only — no rendering of block bodies, no diff (D-15/D-19). Use exact copy from UI-SPEC. Run green, commit `feat(04-03): ...`.
  </action>
  <verify>
    <automated>go test ./internal/doctor/checks/... -run TestCoherence && go build ./... && go test ./internal/doctor/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go test ./internal/doctor/...` passes after the overwrite — CheckCoherence defined exactly once: `grep -rn 'func CheckCoherence' internal/doctor/checks/` returns exactly one match.
    - A non-resolving IdentityFile and a missing includeIf fragment each produce an error finding naming the identity (DOC-03).
    - Missing IdentitiesOnly yes and missing allowed_signers line each produce an error with [fix] (auto-fixable re-add).
    - allowed_signers email comparison uses byte-exact `==` (Pitfall 6) — a case-differing email is reported as a mismatch.
    - account.Incomplete surfaces under Coherence, never Orphans.
    - A fully-coherent identity set yields zero findings; `make lint` passes.
  </acceptance_criteria>
  <done>Coherence family covers all DOC-03 resolution checks + the gpg.format & email locked-value carve-outs (coherence.go stub overwritten in place), distinct from Orphans, fully fake-tested.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: OVERWRITE the CheckOrphans stub — block-vs-disk cross-reference + unused-key (RED→GREEN)</name>
  <files>internal/doctor/checks/orphans.go, internal/doctor/checks/orphans_test.go</files>
  <read_first>
    - .planning/phases/04-doctor/04-01-SUMMARY.md (the FROZEN doctor.Deps field set — orphans.go is the stub this plan overwrites; wire against the locked injected-block-name + all-Host IdentityFile fields)
    - internal/filewriter/block.go (ListBlocks lines 18-50 NamedBlock{Name,Body}, sentinel prefixes lines 99-108 — orphan enumeration source; remember internal/doctor must NOT import filewriter, so block names arrive via injected deps fields, NOT by importing NamedBlock)
    - internal/sshconfig/reader.go (ParseManagedHosts + how every Host's IdentityFile is parsed — D-12 requires cross-referencing a key against EVERY Host block, gitid-managed AND hand-written)
    - internal/doctor/doctor.go (Deps fields for injected managed-block names, SSHConfigBytes, all-Host IdentityFile listing, Stat; Finding/Severity from Plan 01)
    - .planning/phases/04-doctor/04-CONTEXT.md D-09 (orphan = artifact with no owning block, inverse of incomplete), D-10 (own Orphans family, distinct from Coherence), D-11 (managed-block orphans are the fixable ones), D-12 (unused-key cross-refs ALL Host blocks), D-13 (unused key → warning only, honest wording, NO auto-fix), D-14 (do NOT read known_hosts), D-03 (key deletion report-only)
    - .planning/phases/04-doctor/04-UI-SPEC.md §"Copywriting Contract" DOC-04 entries (orphaned-fragment warning + [fix]; unused-key warning, NO [fix], must say "may be used for direct server SSH or 'ssh -i' — review before deleting")
    - .planning/phases/04-doctor/04-RESEARCH.md §"Orphan detection (DOC-04 + D-09)" algorithm + Pitfall 5 (incomplete vs orphan), Pitfall 7 (guard fingerprint/stat on missing pub)
  </read_first>
  <behavior>
    - Test (TestOrphanFragment): a fragment file exists on disk but no gitconfig includeIf managed block claims its name → Orphans warning, Title "<path>: orphaned gitconfig fragment", [fix] (auto-fixable removal).
    - Test (TestOrphanAliasHostNoInclude): a managed SSH Host block with no matching gitconfig includeIf → orphan, [fix].
    - Test (TestOrphanKey): a gitid key file on disk referenced by NO Host block (managed or hand-written) → warning, Title "<path>: not referenced in ~/.ssh/config", explanation includes the "may be used for direct server SSH / ssh -i — review before deleting" wording, NO [fix] marker.
    - Test (TestOrphanKeyReferencedByHandWrittenHost): the same key IS referenced by a hand-written Host's IdentityFile → NOT flagged (D-12 respects user config).
    - Test (TestOrphanNotIncomplete): an account with Incomplete set (managed block exists, artifact missing) produces NO Orphans finding (that is Coherence's job, Pitfall 5).
    - Test (TestOrphanAllPass): every artifact owned by a block, every key referenced → no Orphans findings.
  </behavior>
  <action>
    RED: write orphans_test.go with fake doctor.Deps providing the set of managed block names (ssh + gitconfig), a fake Stat presence map for fragment/key files, and the list of ALL Host IdentityFile paths (managed + hand-written) for the unused-key cross-reference. (orphans.go already holds the Plan 01 `_`-param stub; the RED test fails against it.) Commit `test(04-03): ...`.
    GREEN: OVERWRITE internal/doctor/checks/orphans.go — replace the stub body of CheckOrphans(deps) with the real implementation (keep the exported signature byte-identical to the Plan 01 stub). Algorithm (RESEARCH §Orphan detection): (a) a fragment file present on disk whose name has no owning gitconfig managed includeIf block → orphan warning + Fix (removal, D-11 fixable; mark fixable, fixer is Plan 05); (b) a managed SSH Host block name with no matching gitconfig includeIf → orphan + Fix; (c) a gitid key file present on disk whose path appears in NO Host block IdentityFile across the ENTIRE ~/.ssh/config (managed + hand-written, D-12) → warning, NO Fix (key deletion report-only, D-03/D-13), with the honest "cannot confirm unused" wording. Never read known_hosts (D-14). Do NOT emit an Orphans finding for an account whose Incomplete is set — that is Coherence (Pitfall 5). Guard against missing pub files (Pitfall 7). All copy exact per UI-SPEC. Run green, commit `feat(04-03): ...`.
  </action>
  <verify>
    <automated>go test ./internal/doctor/checks/... -run TestOrphan && go build ./... && go test ./internal/doctor/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go test ./internal/doctor/...` passes after the overwrite — CheckOrphans defined exactly once: `grep -rn 'func CheckOrphans' internal/doctor/checks/` returns exactly one match.
    - An on-disk fragment with no includeIf is an Orphans warning with `[fix]`; orphans render under Orphans, never Coherence (DOC-04, D-10).
    - An unused key is a warning with NO `[fix]` and wording that admits gitid cannot confirm it is unused (D-13).
    - A key referenced by a hand-written Host block is NOT flagged (D-12).
    - `grep -rn 'known_hosts' internal/doctor/` returns nothing (D-14).
    - `grep -rn 'internal/filewriter' internal/doctor/` returns nothing (block names injected, not imported).
  </acceptance_criteria>
  <done>Orphans family detects managed-block orphans (fixable) and unused keys (report-only warning) (orphans.go stub overwritten in place), strictly distinct from Coherence, fully fake-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → coherence/orphans | reads ~/.ssh/config, ~/.gitconfig, fragment files, ~/.ssh/allowed_signers, key file presence (gitid-managed paths) |
| user SSH config → orphan check | hand-written Host blocks are read-only inputs; gitid never flags a key the user references (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | reading allowed_signers / fragment / config (coherence) | mitigate | gosec G304 on trusted gitid-managed paths; reads only public material (emails, gpg.format, paths) — never private-key content; report prints paths/emails, not secrets |
| T-04-09 | Tampering | mis-classifying a user's in-use key as deletable | mitigate | unused-key cross-references EVERY Host IdentityFile incl. hand-written (D-12); severity capped at warning; NO auto-fix for keys (D-03/D-13); honest wording forces human review |
| T-04-10 | Spoofing | allowed_signers email case-mismatch passing verification | mitigate | byte-exact `==` email comparison (Pitfall 6) flags case-differing principals that git signing would reject |
| T-04-11 | Tampering | doctor core gaining write capability / reading known_hosts dead-end | mitigate | no filewriter import; known_hosts never read (D-14); orphan [fix] descriptors injected, executed only by Plan 05 |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `go test ./internal/doctor/checks/... -run 'TestCoherence|TestOrphan'` green.
- `go build ./... && go test ./internal/doctor/...` pass after both stub overwrites — CheckCoherence and CheckOrphans each defined exactly once in package checks (no redeclaration).
- `grep -rn 'internal/filewriter\|known_hosts' internal/doctor/` returns nothing (D-01, D-14).
- `make lint` passes (gosec G304 on all Stat/ReadFile).
- `gitid doctor` now shows real findings under `=== Coherence ===` and `=== Orphans ===`.
- RED→GREEN commits present for both TDD tasks.
</verification>

<success_criteria>
- Coherence reports non-resolving IdentityFile/fragment, missing IdentitiesOnly/allowed_signers, gpg.format & email carve-outs (DOC-03, D-15/D-17).
- Orphans reports managed-block orphans (fixable) and unused keys (report-only warning) distinctly from Coherence (DOC-04, D-09/D-10).
- Unused-key wording is honest; hand-written-referenced keys are spared (D-12/D-13).
- No content-compare, no known_hosts, write-free core (D-19/D-14/D-01); coherence.go + orphans.go stubs overwritten in place with no redeclaration.
</success_criteria>

<output>
Create `.planning/phases/04-doctor/04-03-SUMMARY.md` when done (RED/GREEN commits, the Deps fields used for block-name injection + all-Host IdentityFile listing so Plan 01's Deps shape is confirmed, which findings carry Fix descriptors for Plan 05, confirmation that coherence.go/orphans.go were overwritten in place and `go build ./...` is green).
</output>
</output>
