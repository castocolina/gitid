---
phase: 04-doctor
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/doctor/doctor.go
  - internal/doctor/doctor_test.go
  - internal/doctor/doc.go
  - internal/doctor/checks/perms.go
  - internal/doctor/checks/perms_test.go
  - internal/doctor/checks/deps.go        # per-family STUB this plan; Plan 02 OVERWRITES same path
  - internal/doctor/checks/baseline.go    # per-family STUB this plan; Plan 02 OVERWRITES same path
  - internal/doctor/checks/coherence.go   # per-family STUB this plan; Plan 03 OVERWRITES same path
  - internal/doctor/checks/orphans.go     # per-family STUB this plan; Plan 03 OVERWRITES same path
  - internal/doctor/checks/signing.go     # per-family STUB this plan; Plan 04 OVERWRITES same path
  - cmd/gitid/doctor.go
  - cmd/gitid/doctor_test.go
  - cmd/gitid/main.go
autonomous: true
requirements: [DOC-02, DOC-06, DOC-07]
must_haves:
  truths:
    - "User can run `gitid doctor` and see a grouped-by-family health report (D-06)"
    - "A private key at mode 0644 is reported as a critical Permissions finding with fix `chmod 0600 <path>` (DOC-02, D-05)"
    - "A clean environment prints `doctor: all checks passed` / `exit code: 0` (D-07)"
    - "Exit code is the highest severity present: 0 clean / 1 warn+info / 2 error / 3 critical (D-07)"
    - "internal/doctor never imports internal/filewriter — fix capabilities are injected function fields on doctor.Deps (D-01)"
    - "Each of the seven check families lives in its OWN checks/<family>.go file; six ship as compiling stubs this plan and are OVERWRITTEN in place (same path, same Check<Family> signature) by Wave 2 — never redeclared in a second file"
    - "The doctor.Deps field set is FROZEN and documented in this plan's SUMMARY before Wave 2 executes (Wave 2 plans wire against it; a late Deps change must not silently break Wave 2)"
  artifacts:
    - path: "internal/doctor/doctor.go"
      provides: "Finding, Severity, Family types; doctor.Deps struct; Run(); ExitCode() aggregation (D-05/D-06/D-07)"
      contains: "func Run"
      min_lines: 80
    - path: "internal/doctor/checks/perms.go"
      provides: "CheckPermissions() — os.Stat mode compare to KEY-02 targets (DOC-02)"
      contains: "func CheckPermissions"
    - path: "internal/doctor/checks/deps.go"
      provides: "STUB CheckDeps() returning nil — overwritten by Plan 02"
      contains: "func CheckDeps"
    - path: "internal/doctor/checks/baseline.go"
      provides: "STUB CheckBaseline() returning nil — overwritten by Plan 02"
      contains: "func CheckBaseline"
    - path: "internal/doctor/checks/coherence.go"
      provides: "STUB CheckCoherence() returning nil — overwritten by Plan 03"
      contains: "func CheckCoherence"
    - path: "internal/doctor/checks/orphans.go"
      provides: "STUB CheckOrphans() returning nil — overwritten by Plan 03"
      contains: "func CheckOrphans"
    - path: "internal/doctor/checks/signing.go"
      provides: "STUB CheckSigning() + CheckAgent() returning nil — overwritten by Plan 04"
      contains: "func CheckSigning"
    - path: "cmd/gitid/doctor.go"
      provides: "newDoctorCmd(), runDoctor(), renderReport(), buildDoctorDeps() wiring real packages"
      contains: "func newDoctorCmd"
    - path: "internal/doctor/doctor_test.go"
      provides: "RED tests: ExitCode tiers, Finding fields, Run aggregation"
      contains: "func TestExitCode"
  key_links:
    - from: "cmd/gitid/main.go newRootCmd"
      to: "newDoctorCmd"
      via: "root.AddCommand(newDoctorCmd())"
      pattern: "AddCommand\\(newDoctorCmd"
    - from: "cmd/gitid/doctor.go buildDoctorDeps"
      to: "doctor.Deps fix fields"
      via: "FixPerm injected as os.Chmod closure (D-01: doctor never imports filewriter)"
      pattern: "FixPerm"
    - from: "internal/doctor/checks/perms.go"
      to: "doctor.Deps.Stat"
      via: "injected stat function, gosec G304 annotated"
      pattern: "deps\\.Stat"
---

<objective>
Foundational vertical slice for `gitid doctor`: establish the read-only Finding/Severity/Family
data model, the `doctor.Deps` injected-function struct, the `Run()` orchestrator (calling all seven
check families — six stubbed nil this plan, Permissions real), the `ExitCode()` severity aggregation,
and a minimal real `gitid doctor` Cobra command that renders the grouped-by-family report end-to-end
with ONE real family (Permissions). After this plan, `gitid doctor` is runnable, prints a grouped
report, detects a 0644 private key as critical, and exits with the tiered code.

Purpose: Lock the UI-agnostic data shape (reused unchanged by Phase 5 TUI) and the write-free core
contract (D-01) before the other six families are added in parallel. Each family lives in its OWN
checks/<family>.go file; this plan ships six compiling stubs (deps/baseline/coherence/orphans/signing —
each `func Check<Family>(deps doctor.Deps) []doctor.Finding { _ = deps; return nil }`), and every Wave 2
plan OVERWRITES its own family file(s) in place — same path, same exported signature — so there is no
function redeclaration, no shared-file edit, and no deletion at any wave boundary. The renderer iterates
all seven Family constants in fixed order, so Wave 2 plans never touch doctor.go or cmd/doctor.go.

Output: internal/doctor/{doctor.go, doctor_test.go}, internal/doctor/checks/{perms.go, perms_test.go,
deps.go, baseline.go, coherence.go, orphans.go, signing.go (the last five are stubs)},
cmd/gitid/{doctor.go, doctor_test.go}, updated doc.go + main.go registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-doctor/04-CONTEXT.md
@.planning/phases/04-doctor/04-RESEARCH.md
@.planning/phases/04-doctor/04-PATTERNS.md
@.planning/phases/04-doctor/04-UI-SPEC.md
@.planning/phases/04-doctor/04-VALIDATION.md
</context>

## Phase Goal (user story)

**As a** gitid user, **I want to** run `gitid doctor` and receive a complete, actionable
health report — each finding with severity and a suggested fix — **so that** I can find and
repair config problems before they break authentication or signing.

## Artifacts this plan produces

- Types: `doctor.Severity` (iota: `SeverityInfo`, `SeverityWarning`, `SeverityError`, `SeverityCritical`) + `String()`; `doctor.Family` (string consts `FamilyDeps`, `FamilyPerms`, `FamilyCoherence`, `FamilyOrphans`, `FamilySigning`, `FamilyAgent`, `FamilyBaseline`); `doctor.Finding{Family, Severity, Title, Explanation, SuggestedFix, Fix *FixDescriptor}`; `doctor.FixDescriptor{Summary string, Fn func() error}`.
- `doctor.Deps` struct (read + injected-fix function fields; see <action>). **The field set is finalized and documented in this plan's SUMMARY before Wave 2 starts — it is the contract Plans 02/03/04/05 wire against (WARNING 4 lock).**
- Functions: `doctor.Run(Deps) []Finding`, `doctor.ExitCode([]Finding) int`, `doctor.Families() []Family` (fixed order), `checks.CheckPermissions(Deps) []Finding` (REAL), and one STUB per remaining family in its own file: `checks.CheckDeps`, `checks.CheckBaseline` (deps.go / baseline.go → Plan 02), `checks.CheckCoherence`, `checks.CheckOrphans` (coherence.go / orphans.go → Plan 03), `checks.CheckSigning` + `checks.CheckAgent` (signing.go → Plan 04). Every stub is `func Check<Family>(deps doctor.Deps) []doctor.Finding { _ = deps; return nil }`.
- cmd: `newDoctorCmd()`, `runDoctor(out io.Writer, fix, yes bool) int`, `renderReport(out, findings, colorEnabled)`, `buildDoctorDeps()`; `--fix`/`--yes` flags declared (gate logic is Plan 05; this plan registers flags + the `--yes requires --fix` guard only).
- New file paths: `internal/doctor/checks/{perms,deps,baseline,coherence,orphans,signing}.go`, `cmd/gitid/doctor.go`.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Finding/Severity/Family model + Deps + Run + ExitCode + seven per-family stub files (RED→GREEN→REFACTOR)</name>
  <files>internal/doctor/doctor.go, internal/doctor/doctor_test.go, internal/doctor/doc.go, internal/doctor/checks/deps.go, internal/doctor/checks/baseline.go, internal/doctor/checks/coherence.go, internal/doctor/checks/orphans.go, internal/doctor/checks/signing.go</files>
  <read_first>
    - internal/doctor/doc.go (current write-free package contract — must be preserved/expanded, D-01)
    - internal/identity/identity.go (Deps injected-function pattern, lines 119-148; Account struct lines 25-49)
    - .planning/phases/04-doctor/04-RESEARCH.md §"Pattern 1: Finding + Severity Data Model" and §"Pattern 2: Injected-Deps" (the [ASSUMED] type shapes — adopt research recommendation approach (b): fix capabilities as Deps fields)
    - .planning/phases/04-doctor/04-CONTEXT.md D-01, D-05, D-06, D-07 (read-only core, 4-level severity, grouped families, tiered exit codes)
    - .planning/phases/04-doctor/04-VALIDATION.md (test infra: go test table-driven, fake doctor.Deps)
    - $HOME/.claude/projects/.../memory: TDD RED-stub-under-strict-lint (`_`-param failing stub so RED commit passes hard-fail lint without --no-verify)
  </read_first>
  <behavior>
    - Test: SeverityCritical.String()=="critical", SeverityError=="error", SeverityWarning=="warning", SeverityInfo=="info".
    - Test (TestExitCodeClean): ExitCode(nil) == 0.
    - Test (TestExitCodeError): ExitCode of findings whose max severity is error == 2; only warning/info == 1; any critical == 3 (TestExitCodeCritical). Highest-wins: a set with one info and one critical returns 3.
    - Test (TestFindingFields): a Finding carries non-empty Family, Severity, Title, Explanation, SuggestedFix; Fix may be nil (report-only) or non-nil (auto-fixable).
    - Test (TestRunCallsAllFamilies): Run with a fake Deps that makes Permissions emit one finding returns exactly that finding; the six stub families contribute nothing.
    - Test (TestFamiliesFixedOrder): Families() returns the 7 family constants in UI-SPEC order: Dependencies, Permissions, Coherence, Orphans, Signing, Agent, Baseline.
  </behavior>
  <action>
    RED: write internal/doctor/doctor_test.go with the table-driven tests above against fake doctor.Deps; add RED stubs in doctor.go using the project `_`-param failing-stub pattern (signatures present, bodies return zero+sentinel so the lint-gated pre-commit hook passes while tests genuinely fail). Commit `test(04-01): ...`.
    GREEN: implement in doctor.go — `Severity int` iota (info<warning<error<critical) with `String()`; `Family string` consts in the seven UI-SPEC names; `Finding` struct {Family, Severity, Title, Explanation, SuggestedFix, Fix *FixDescriptor}; `FixDescriptor{Summary string, Fn func() error}`; `Passed`-equivalent handled by absence-of-finding (renderer derives the ✓ lines). Define `Deps` with READ fields (`ReadFile func(string)([]byte,error)`, `Stat func(string)(os.FileInfo,error)`, `RunSSHAdd func()(string,int)`, `RunSSHKeygenFingerprint func(string)(string,error)`, `RunGitConfigGet func(file,key string)(string,error)`, `Identities []identity.Account`, `GitVersionAtLeast func(int,int) bool`, `CurrentOS func() string`, `InstallHint func(tool, os string) string`, `DetectTools func() deps.Report`, `ListSSHBlocks func([]byte)[]filewriter.NamedBlock`-typed-as-local-shape-or-name-slice, `SSHConfigBytes []byte`, `GitconfigBytes []byte`, `AllowedSigners []byte`, `BaselineState`-fields-or-reader-fn, path fields for the four baseline files) AND injected FIX fields per D-01 approach (b): `FixPerm func(path string, mode os.FileMode) error`, `RemoveBlock func(path, name string) error`, `AddWiring func(...) error`. Keep internal/doctor free of any `internal/filewriter` import — represent block names as a local `[]string` or a doctor-owned `BlockName` slice produced by injected functions, NOT by importing filewriter types. `Run(deps) []Finding` appends `checks.CheckDeps`, `CheckPermissions`, `CheckCoherence`, `CheckOrphans`, `CheckSigning`, `CheckAgent`, `CheckBaseline` — referencing these STABLE function names so Run() compiles unchanged at every wave boundary (six are nil-returning stubs this plan). `ExitCode([]Finding) int` per D-07 highest-wins. `Families() []Family` fixed order. Update doc.go to keep the "never writes — returns structured findings only" sentence and note fix capabilities are injected (D-01). Use error format `doctor: <context>: %w`.
    PER-FAMILY STUB FILES: create one file per remaining family, each containing ONLY its stub so Wave 2 overwrites it without redeclaration — `internal/doctor/checks/deps.go` (`func CheckDeps(deps doctor.Deps) []doctor.Finding { _ = deps; return nil }`), `checks/baseline.go` (`func CheckBaseline...`), `checks/coherence.go` (`func CheckCoherence...`), `checks/orphans.go` (`func CheckOrphans...`), `checks/signing.go` (`func CheckSigning...` AND `func CheckAgent...` — both live here, overwritten by Plan 04). Each stub is in package `checks`, returns nil, uses the `_ = deps` discard so strict lint passes. Do NOT place any of these stubs in perms.go or in a shared stubs.go — one family per file (the redeclaration-prevention rule).
    Run tests green, commit `feat(04-01): ...`.
    REFACTOR (if needed): extract severity→exit-code into a small helper; commit `refactor(04-01): ...` only if changed.
  </action>
  <verify>
    <automated>go test ./internal/doctor/... -run 'TestExitCode|TestFindingFields|TestRunCallsAllFamilies|TestFamiliesFixedOrder|TestSeverityString' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go test ./internal/doctor/...` exits 0 after GREEN (the six stub files compile; no `Check<Family>` redeclared in this package).
    - Each family stub lives in its own file (`checks/{deps,baseline,coherence,orphans,signing}.go`), each containing exactly one `func Check<Family>` (CheckSigning + CheckAgent share signing.go); `grep -c 'func CheckDeps' internal/doctor/checks/*.go | grep -v ':0' | wc -l` == 1 (CheckDeps defined in exactly one file). This is the contract Wave 2 OVERWRITES in place.
    - `grep -rn 'internal/filewriter' internal/doctor/` returns no matches (write-free core, D-01).
    - `git log --oneline --grep='^test(04-01)'` and `--grep='^feat(04-01)'` each return a commit (RED→GREEN gate).
    - ExitCode highest-wins proven: a findings slice with both an info and a critical returns 3.
  </acceptance_criteria>
  <done>Finding/Severity/Family/FixDescriptor/Deps/Run/ExitCode/Families exist, write-free, fully fake-tested; six per-family stub files compile (one Check<Family> each, ready for Wave 2 overwrite); RED and GREEN commits present.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Permissions check family — os.Stat mode compare to KEY-02 targets (RED→GREEN)</name>
  <files>internal/doctor/checks/perms.go, internal/doctor/checks/perms_test.go</files>
  <read_first>
    - internal/doctor/doctor.go (the Finding/Severity/Deps types from Task 1 — this file consumes them)
    - .planning/phases/04-doctor/04-RESEARCH.md §"Pattern 4: File Permission Checking" (targetModes map; severity classification: private key world/group readable → critical, ~/.ssh dir & config wrong → error, .pub wrong → warning; os.Stat NOT os.Lstat per Pitfall 2; gosec G304 per Pitfall 3)
    - .planning/phases/04-doctor/04-UI-SPEC.md §"Copywriting Contract" DOC-02 entries (exact Explanation + fix: copy for key/dir/.pub/config — variation not permitted)
    - .planning/REQUIREMENTS.md KEY-02 (targets: ~/.ssh 700, key 600, .pub 644, config 600)
    - internal/identity/identity.go Account struct (KeyPath, PubPath, SSHConfigPath fields the check iterates)
    - .planning/phases/04-doctor/04-CONTEXT.md D-02 (chmod is the canonical reversible fix), D-05 (severity bands)
  </read_first>
  <behavior>
    - Test (TestCheckPermsPass): a fake Stat returning mode 0600 for a key, 0700 for ~/.ssh, 0644 for .pub, 0600 for config → CheckPermissions returns no findings.
    - Test (TestCheckPermsCritical): fake Stat returns 0644 for a private key → one Permissions finding, Severity==critical, Title contains the path + "0600", SuggestedFix=="chmod 0600 <path>", Fix non-nil (auto-fixable).
    - Test (TestCheckPermsDirError): ~/.ssh at 0755 → error severity finding, fix "chmod 0700 ~/.ssh".
    - Test (TestCheckPermsPubWarning): a .pub at 0600 → warning severity, fix "chmod 0644 <path>".
    - Test (TestCheckPermsMissingSkipped): Stat returns os.ErrNotExist → no Permissions finding (absent files are coherence's concern, not perms).
    - Test (TestPermFixerFnCallsInjectedFixPerm): the Fix.Fn closure calls deps.FixPerm(path, mode); a recording fake confirms it is invoked with the KEY-02 target mode and never widens permissions.
  </behavior>
  <action>
    RED: write perms_test.go with the table above using fake doctor.Deps (fake Stat returning synthetic os.FileInfo via a tiny fakeFileInfo type or fstest, recording FixPerm). The Task 1 perms.go RED stub is already the `_`-param failing stub; if Task 2 is executed separately, add/keep the stub here. Commit `test(04-01): ...` (or chain into Task 1's RED if executed together — keep separate commit if separable).
    GREEN: implement CheckPermissions(deps) in internal/doctor/checks/perms.go. Build the targetModes set (dir 0o700, key 0o600, pub 0o644, config 0o600). For each gitid-managed path (each account's KeyPath/PubPath + the ~/.ssh dir + ~/.ssh/config + ~/.gitconfig, sourced from deps), call deps.Stat (gosec G304 annotation `//nolint:gosec // path is a trusted gitid-managed path (G304)`), compare `info.Mode().Perm()` to target. On mismatch emit a Finding with Family=FamilyPerms, severity per D-05 band, Title/Explanation/SuggestedFix matching UI-SPEC copy exactly, and Fix=&FixDescriptor{Summary:"chmod 0NNN <path>", Fn: func() error { return deps.FixPerm(path, want) }}. Skip os.IsNotExist paths (return no finding). Use os.Stat semantics (follows symlinks) — never os.Lstat. The fixer must only tighten to the KEY-02 target, never loosen. Run green, commit `feat(04-01): ...`.
  </action>
  <verify>
    <automated>go test ./internal/doctor/checks/... -run TestCheckPerms</automated>
  </verify>
  <acceptance_criteria>
    - A fake key at 0644 yields exactly one critical Permissions finding whose SuggestedFix is `chmod 0600 <path>` and whose Fix is non-nil.
    - Correct perms (key 600, dir 700, .pub 644, config 600) yield zero findings.
    - A missing file (os.ErrNotExist) yields zero Permissions findings.
    - `make lint` passes (gosec G304 annotations present on every Stat call).
    - The Fix.Fn calls the injected deps.FixPerm — perms.go contains no direct `os.Chmod` call.
  </acceptance_criteria>
  <done>CheckPermissions detects all four KEY-02 classes with correct severity + exact copy + auto-fix descriptor, fully fake-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Minimal `gitid doctor` command + grouped renderer + root registration</name>
  <files>cmd/gitid/doctor.go, cmd/gitid/doctor_test.go, cmd/gitid/main.go</files>
  <read_first>
    - cmd/gitid/baseline.go (newBaselineSetupCmd cobra pattern, printBaselinePreview `=== ... ===` header, promptYN; the buildDeps wiring shape)
    - cmd/gitid/add.go (fp() helper lines 259-262, confirm() lines 512-517, buildDeps wiring lines 319-330, `pkg: context: %w` error format)
    - cmd/gitid/list.go (home resolution + os.ReadFile with !os.IsNotExist guard lines 36-56, 2-space indent rendering lines 78-109)
    - cmd/gitid/main.go (newRootCmd — must register root.AddCommand(newDoctorCmd()))
    - .planning/phases/04-doctor/04-UI-SPEC.md §"Report Layout Contract", §"Color", §"Glyph Contract", §"Severity Label Contract", §"Copywriting Contract" empty/all-clear + summary line, §"Information Architecture" cobra wiring, §"Apply-Fixes Gate Contract" `--yes requires --fix` error
    - .planning/phases/04-doctor/04-RESEARCH.md §"Pattern 5: TTY Detection and Color Output" (file.Stat().Mode()&os.ModeCharDevice; NO_COLOR checked first; ANSI helper)
    - internal/doctor/doctor.go (Run, Families, ExitCode, Finding — consumed here)
    - internal/doctor/checks/perms.go (CheckPermissions — wired via buildDoctorDeps)
    - internal/platform/platform.go, internal/deps/deps.go, internal/identity/loader.go, internal/gitconfig (real functions buildDoctorDeps wires)
  </read_first>
  <action>
    Create cmd/gitid/doctor.go. `newDoctorCmd()` returns *cobra.Command Use:"doctor", Short:"Run a health check on the gitid-managed environment", registered on root (not under a group). Declare `--fix` and `--yes` BoolVar flags; in RunE enforce `if yes && !fix { return fmt.Errorf("doctor: --yes requires --fix") }` (exit code 1 per UI-SPEC) — full gate/fixer behavior is Plan 05; this plan only registers the flags + guard and runs the report. `runDoctor(out io.Writer, fix, yes bool) int` resolves home (error format `doctor: resolving home dir: %w`), reads ~/.ssh/config and ~/.gitconfig with !os.IsNotExist guards (gosec G304 annotated), calls `buildDoctorDeps(home, sshBytes, gcBytes)` then `doctor.Run(deps)`, computes `doctor.ExitCode(findings)`, renders, and returns the code; main()/Execute already owns os.Exit — have runDoctor return the int and the RunE translate non-zero into the process exit (use a sentinel error or cmd.Root SilenceErrors path consistent with existing commands; do not call os.Exit inside runDoctor to keep it testable). `renderReport(out, findings, colorEnabled)` iterates `doctor.Families()` in fixed order: print `=== <Family> ===` header (bold via ansi when colorEnabled), then for each family either the findings (✗/! glyph + severity color + Title; 4-space Explanation; dim `fix:` line; `[fix]` when Fix!=nil) or a single ✓ pass line when the family produced no findings — exact glyphs, colors, indentation, and copy per UI-SPEC. Print the summary line `doctor: N critical, N error, N warning, N info` + `exit code: N`, or the all-clear `doctor: all checks passed` / `exit code: 0`. Add `ansi(code,text string, enabled bool) string`, `severityCode(doctor.Severity) string`, and `isTerminalOutput(*os.File) bool` (NO_COLOR first, then os.ModeCharDevice) per UI-SPEC/RESEARCH. `buildDoctorDeps(home string, sshBytes, gcBytes []byte) doctor.Deps` wires the real packages into the Deps fields: Stat→os.Stat, ReadFile→os.ReadFile, Identities→identity.Reconstruct(sshBytes, gcBytes, gitconfig.ReadFragment), DetectTools→deps.Detect, GitVersionAtLeast→deps.GitVersionAtLeast, CurrentOS→platform.CurrentOS, InstallHint→platform install-hint fn, and FixPerm→`func(p string, m os.FileMode) error { return os.Chmod(p, m) }` with `//nolint:gosec // chmod to KEY-02 target mode (G306)` (this keeps filewriter out of internal/doctor — D-01 approach (b)). Reuse the package-level `fp()`; do not redefine it. Register `root.AddCommand(newDoctorCmd())` in main.go's newRootCmd. Write doctor_test.go mirroring baseline_test.go: t.TempDir()+t.Setenv("HOME",...), bytes.Buffer out, call runDoctor directly; assert TestDoctorCmdRegistered (command present on root), TestDoctorRenderGrouped (output contains `=== Permissions ===` and a ✓ or ✗ line), TestDoctorCleanAllClear (clean tmp home prints `doctor: all checks passed`), TestDoctorYesRequiresFix (RunE with yes&&!fix errors "doctor: --yes requires --fix").
  </action>
  <verify>
    <automated>go test ./cmd/gitid/... -run TestDoctor && go build ./... && go run ./cmd/gitid doctor >/dev/null; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `go run ./cmd/gitid doctor` runs, prints a grouped report with `=== Permissions ===`, and exits with a tiered code (0 on a clean env).
    - `gitid doctor --yes` (without `--fix`) errors with `doctor: --yes requires --fix`.
    - `NO_COLOR=1 gitid doctor | cat` emits no ANSI escape sequences (`grep -c $'\\033' ` == 0 on piped output).
    - `grep -rn 'internal/filewriter' internal/doctor/` still returns no matches (FixPerm injected from cmd layer).
    - main.go newRootCmd contains `root.AddCommand(newDoctorCmd())`.
  </acceptance_criteria>
  <done>`gitid doctor` is a registered, runnable command rendering the grouped report with the real Permissions family end-to-end; report-only (fixer gate is Plan 05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → doctor core | doctor reads ~/.ssh keys, config, gitconfig (gitid-managed paths, not free-form user input) |
| doctor core → cmd fixer | core returns FixDescriptor with an injected Fn; only the cmd layer executes it (D-01) |
| cmd fixer → filesystem | os.Chmod (this plan) tightens permissions to KEY-02 targets only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | os.Stat/os.ReadFile on key paths (perms.go, runDoctor) | mitigate | gosec G304 `//nolint` only on trusted gitid-managed paths; doctor reads Stat (mode) for keys, never key *content*; report prints path + mode only, never key material |
| T-04-02 | Tampering | FixPerm os.Chmod widening permissions | mitigate | chmod only to KEY-02 targets (700/600/644/600); the fixer never loosens — fix descriptor carries the exact target mode; G306 annotated. `.pub` 644 is the only non-restrictive target and is annotated as correct |
| T-04-03 | Tampering | doctor core gaining write capability | mitigate | internal/doctor imports no filewriter/os-write path; fix capabilities are injected Deps fields executed by cmd; CI grep gate asserts no filewriter import |
| T-04-04 | Tampering | color/ANSI injection into piped logs | accept | ANSI emitted only when isTerminalOutput && !NO_COLOR; piped output is plain; low blast radius |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH §Package Legitimacy Audit: none); no install tasks |
</threat_model>

<verification>
- `go test ./internal/doctor/... ./cmd/gitid/... -run 'TestExitCode|TestFinding|TestRun|TestFamilies|TestCheckPerms|TestDoctor'` green.
- `go build ./...` succeeds — the six per-family stub files compile and Run() references all seven stable Check<Family> names (no redeclaration anywhere in package checks).
- `make lint` (golangci-lint v2 + gosec, hard-fail) passes — all Stat/ReadFile/Chmod carry gosec annotations.
- `grep -rn 'internal/filewriter' internal/doctor/` returns nothing (D-01 write-free core).
- `go run ./cmd/gitid doctor` produces the grouped report and a tiered exit code.
- RED→GREEN commits present for both TDD tasks (`git log --grep='^test(04-01)'`, `--grep='^feat(04-01)'`).
</verification>

<success_criteria>
- `gitid doctor` is runnable and renders a grouped-by-family report (DOC-07 CLI half, D-06).
- A 0644 private key is reported as critical with fix `chmod 0600 <path>` (DOC-02, D-05).
- A clean environment prints `doctor: all checks passed` and exits 0; tiered exit codes work (DOC-06, D-07).
- internal/doctor is write-free; fix capabilities injected via Deps (D-01).
- Renderer iterates all seven families in fixed order; six families ship as per-family stub files (one Check<Family> each) ready for Wave 2 to OVERWRITE in place without redeclaration.
- The doctor.Deps field set is frozen + documented in 04-01-SUMMARY.md before Wave 2 starts (WARNING 4 contract lock).
</success_criteria>

<output>
Create `.planning/phases/04-doctor/04-01-SUMMARY.md` when done. MUST include: RED/GREEN/REFACTOR commits; artifacts; and a **frozen `doctor.Deps` field set** section (every field name + type, including the read fields and the three injected fix fields) plus the **seven per-family file map** (which checks/<family>.go each Wave 2 plan overwrites) — this is the locked contract Wave 2 wires against (WARNING 4). Any later Deps change after this SUMMARY requires re-notifying Wave 2 plans.
</output>
</output>
