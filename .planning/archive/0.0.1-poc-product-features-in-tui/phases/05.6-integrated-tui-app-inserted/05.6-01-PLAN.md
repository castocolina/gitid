---
phase: 05.6-integrated-tui-app-inserted
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tui/overlay.go
  - tui/overlay_test.go
  - tui/styles.go
  - tui/keymap.go
  - tui/messages.go
  - tui/model_test.go
  - tui/sidebar_test.go
  - tui/health_test.go
  - tui/wizard_test.go
  - tui/confirm_test.go
  - tui/wiring_test.go
autonomous: true
requirements: [TUI-03, TUI-04, TUI-05, TUI-06]
must_haves:
  truths:
    - "A line-by-line string-overlay helper composites a modal box over a dimmed background (PlaceOverlay confirmed ABSENT at lipgloss v2.0.3)"
    - "styles.go, keymap.go, messages.go carry every new token/binding/message the shell and modal slices consume"
    - "Every Wave-0 test file exists and fails (RED) referencing the symbols later slices create"
  artifacts:
    - path: "tui/overlay.go"
      provides: "placeOverlay() line-merge fallback + overlay bounding-box math"
      min_lines: 30
    - path: "tui/styles.go"
      provides: "extended Lipgloss v2 tokens + SeverityGlyph"
      contains: "func SeverityGlyph"
    - path: "tui/keymap.go"
      provides: "extended keyMap with Palette/View1-3/SidebarToggle/Focus/Fix/Retry/Skip"
      contains: "Palette"
    - path: "tui/messages.go"
      provides: "clearModalMsg/refreshSidebarMsg/deleteResultMsg/fixResultMsg/rotateResultMsg/baselineLoadedMsg + helper cmds"
      contains: "clearModalCmd"
  key_links:
    - from: "tui/overlay.go"
      to: "lipgloss string render"
      via: "split-by-newline line replacement within bounding box"
      pattern: "strings.Split"
    - from: "tui/styles.go"
      to: "internal/doctor.Severity"
      via: "SeverityGlyph + SeverityStyle"
      pattern: "doctor.Severity"
---

<objective>
Wave 0 foundation for the Phase 5.6 integrated TUI rewrite. Resolve the single
highest-risk unknown (modal overlay compositing), extend the shared scaffolding
(styles, keymap, messages) that every later slice consumes, and lay down RED test
scaffolds so the per-slice TDD tasks start from a failing baseline that names the
symbols they will create.

This plan implements **no app behavior**. It is pure foundation: a verified overlay
primitive, extended design tokens, and failing test files. No slice depends on
guessing the PlaceOverlay outcome — this plan settles it.

Purpose: De-risk the modal architecture (D-02) and give every downstream slice a
stable, pre-extended `styles.go`/`keymap.go`/`messages.go` plus RED tests, so the
shell/health/detail/wizard/lifecycle plans never edit those shared files in conflict
and never start from a green-but-empty baseline.
Output: `tui/overlay.go` (+ test), extended `tui/styles.go`, `tui/keymap.go`,
`tui/messages.go`, and six RED `*_test.go` scaffolds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-RESEARCH.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-PATTERNS.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-UI-SPEC.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-VALIDATION.md
@tui/styles.go
@tui/keymap.go
@tui/messages.go
@tui/dashboard.go
@tui/prove.go
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from source-grounding drift checks):
- `tui/overlay.go`, `tui/overlay_test.go`
- `placeOverlay(x, y int, fg, bg string) string`, `modalBox(width int, title, body string) string`, `modalOrigin(termW, termH, modalW, modalH int) (x, y int)`, `placeOverlayAvailable` (build-time const documenting the spike result)
- New style tokens: `StyleTabActive`, `StyleTabInactive`, `StyleReadOnly`, `StyleModalTitle`, `StyleModal`, `StyleDimmed`, `StyleSidebarSection`, `StyleSidebarItem`, `StyleSidebarUnmanaged`, `StyleSidebarBadge`
- New function: `SeverityGlyph(s doctor.Severity, asciiMode bool) string`, `asciiMode() bool`
- New keyMap fields: `Palette`, `View1`, `View2`, `View3`, `SidebarToggle`, `Focus`, `FocusRev`, `Fix`, `Retry`, `Skip`, `Confirm`, `Rotate`, `Edit`, `Add`, `Copy`, `Delete`, `Top`, `Bottom`
- New message types: `clearModalMsg`, `refreshSidebarMsg`, `setToastMsg`, `clearToastMsg`, `deleteResultMsg`, `fixResultMsg`, `rotateResultMsg`, `baselineLoadedMsg`; helper cmds `clearModalCmd`, `refreshSidebarCmd`, `setToastCmd`, `clearToastAfter`
- RED scaffold test names: `TestRootModelTwoPaneRenders`, `TestSmallTerminal`, `TestSidebarVisible`, `TestSidebarCollapse`, `TestViewSwitch`, `TestPaletteOpen`, `TestWizardOpen`, `TestWizardProveRetry`, `TestInlineEdit`, `TestDeleteModal`, `TestHealthFamilies`, `TestFixModal`, `TestFooterAlwaysRendered`, `TestHelpOverlay`, `TestBuildTUIDepsNilGuard`, `TestPlaceOverlayCompositesModal`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the PlaceOverlay spike and implement the overlay fallback helper</name>
  <read_first>
    - tui/styles.go (existing lipgloss v2 token pattern; `lipgloss.NewStyle()` only)
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-RESEARCH.md § "Pitfall 3" and § "Pattern 2"
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-UI-SPEC.md § "Modal Overlay (D-02)" and § "Persistent Layout Architecture"
  </read_first>
  <action>
    The Wave-0 spike is already RESOLVED by the planner: `go doc charm.land/lipgloss/v2 PlaceOverlay` returns "no symbol PlaceOverlay" at the pinned v2.0.3 (only `Place`, `PlaceHorizontal`, `PlaceVertical` exist). Therefore implement the documented line-by-line string-overlay FALLBACK in a new file `tui/overlay.go`; do NOT call `lipgloss.PlaceOverlay` anywhere (it would not compile). Record the spike result as a top-of-file doc comment citing the exact `go doc` output.

    Implement `placeOverlay(x, y int, fg, bg string) string`: split both `fg` (the modal box render) and `bg` (the dimmed full-layout render) by "\n"; for each foreground line index i, replace the background line at row `y+i`, overwriting the background runes in columns `[x, x+fgLineWidth)` with the foreground line's runes, preserving background runes outside that span. Use `lipgloss.Width` for visible-width measurement (ANSI-aware) and rune-level slicing so multi-byte glyphs (`✓ ✗ ! ~ › ○ •`) and ANSI escape sequences in the dimmed background are not split mid-sequence — operate on a cell grid, not raw bytes. If `fg` is taller/wider than `bg`, clamp to the background bounds (never panic on out-of-range rows/cols).

    Implement `modalOrigin(termW, termH, modalW, modalH int) (x, y int)` returning the centered top-left origin: `x = (termW-modalW)/2`, `y = (termH-modalH)/2`, both floored at 0. Implement `modalBox(width int, title, body string) string` that renders `StyleModal` (added in Task 2) at `min(width-8, 72)` columns with `StyleModalTitle` on the first line per UI-SPEC § Modal box styling.

    Keep all rendering through `lipgloss.NewStyle()` (NOT `renderer.NewStyle()` — does not exist in v2, RESEARCH Pitfall 2). This helper is consumed by the shell plan's `renderContent()` (Plan 02) to composite a modal over the dimmed persistent layout.
  </action>
  <verify>
    <automated>go test ./tui/... -run TestPlaceOverlayCompositesModal -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `tui/overlay.go` exists with `placeOverlay`, `modalOrigin`, `modalBox` and a doc comment recording the PlaceOverlay-absent spike result.
    - `tui/overlay_test.go` `TestPlaceOverlayCompositesModal` passes: composing a 3-line modal over a 10-line dimmed background yields a string where the modal lines appear at the centered rows/cols and the untouched background rows are preserved verbatim; an oversized modal clamps without panic.
    - No reference to `lipgloss.PlaceOverlay` anywhere in tui/.
    - `make lint` clean (gosec/golangci-lint v2 over the whole module).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend styles.go, keymap.go, and messages.go with all shared shell/modal scaffolding</name>
  <read_first>
    - tui/styles.go (lines 1-76: keep ALL existing tokens verbatim; SeverityStyle pattern)
    - tui/keymap.go (lines 1-57: existing keyMap struct + keys var)
    - tui/messages.go (lines 1-59: keep familyResultMsg/preWriteResultMsg/resolvedResultMsg/writeResultMsg/clipboardResultMsg; remove push/pop)
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-UI-SPEC.md § "Lipgloss Style Token Reference", § "Glyph Contract", § "Keymap Contract"
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-PATTERNS.md § "tui/styles.go", "tui/keymap.go", "tui/messages.go"
  </read_first>
  <action>
    Extend `tui/styles.go`: add `StyleTabActive` (Bold+Underline), `StyleTabInactive` (Faint), `StyleReadOnly` (Faint), `StyleModalTitle` (Bold), `StyleModal` (Padding(1,2)+RoundedBorder+BorderForeground Color "4"), `StyleDimmed` (Faint(true)), `StyleSidebarSection` (Bold), `StyleSidebarItem`, `StyleSidebarUnmanaged` (Faint), `StyleSidebarBadge` exactly as declared in UI-SPEC § Lipgloss Style Token Reference. Keep all existing tokens and `SeverityStyle` verbatim. Add `SeverityGlyph(s doctor.Severity, asciiMode bool) string` per UI-SPEC: warning returns "!" (NEVER "✗"), error/critical "✗", info "~", with ASCII fallbacks "FAIL"/"!"/"i" — this implements D-10 and the Eval #2 fix. Add `asciiMode() bool` returning true when `$TERM == "dumb"` or `$TERM` is unset (glyph degradation gate per UI-SPEC § Glyph selection rule). Use `lipgloss.NewStyle()` only.

    Extend `tui/keymap.go`: keep all existing bindings; add fields and values to `keyMap`/`keys` per UI-SPEC § Keymap Contract — `Palette` (ctrl+p), `View1`/`View2`/`View3` (1/2/3), `SidebarToggle` (\\), `Focus` (tab), `FocusRev` (shift+tab), `Fix` (x), `Retry` (r), `Skip` (s), `Confirm` (enter), `Rotate` (R), `Edit` (e), `Add` (a), `Copy` (c), `Delete` (d, delete), `Top` (g), `Bottom` (G). Use `key.NewBinding(key.WithKeys(...), key.WithHelp(...))` from `charm.land/bubbles/v2/key` per PATTERNS Pattern F. Document the Tab routing note: `Focus` (pane cycle) vs `Next` (form field) share "tab"; the root model routes by `activeModal != noModal` (Plan 02 wires this; Pitfall 9).

    Extend `tui/messages.go`: REMOVE `pushScreenMsg`, `popScreenMsg`, `pushCmd()`, `popCmd()` (the screen-stack is retired, D-15). Keep `familyResultMsg`, `preWriteResultMsg`, `resolvedResultMsg`, `writeResultMsg`, `clipboardResultMsg` verbatim. Add `clearModalMsg{}`, `refreshSidebarMsg{}`, `setToastMsg{text string; style lipgloss.Style}`, `clearToastMsg{}`, `deleteResultMsg{result identity.DeleteResult; err error}`, `fixResultMsg{family doctor.Family; err error}`, `rotateResultMsg{result identity.CreateResult; err error}`, `baselineLoadedMsg{state gitconfig.BaselineState; err error}`, and helper cmds `clearModalCmd()`, `refreshSidebarCmd()`, `setToastCmd(text string, style lipgloss.Style)`, `clearToastAfter(d time.Duration) tea.Cmd` (uses `tea.Tick`). Removing push/pop will make `tui/model.go` (current), `tui/dashboard.go`, `tui/identitylist.go`, `tui/identitydetail.go`, `tui/prove.go`, `tui/createform.go`, `tui/updateform.go`, `tui/addaccountform.go`, `tui/copy.go` (current screen-stack files) fail to compile — that is EXPECTED. They are replaced/ported by Plans 02-06. To keep the module buildable at the end of THIS plan, see Task 3 (the RED scaffolds compile against new symbols; the old screen-stack files are deleted here so the package compiles without push/pop). Delete `tui/identitylist.go`, `tui/identitydetail.go`, `tui/createform.go`, `tui/updateform.go`, `tui/addaccountform.go`, `tui/dashboard.go`, `tui/prove.go`, `tui/copy.go`, `tui/model.go`, and their `*_test.go` siblings (`dashboard_test.go`, `model_test.go`, `navigation_test.go`, `prove_test.go`, `form_test.go`, `reviewfix_test.go`) ONLY AFTER copying the proven logic that Plans 02-06 port (see those plans' read_first which reference these analog files by path) — since this is a clean rewrite (D-15), preserve the originals on the working branch via git history; the executor reads them from `git show HEAD:tui/<file>` when porting. Do NOT delete `tui/tui.go`, `tui/deps.go`, `tui/doc.go`, `tui/wiring_test.go`, `tui/tui_stub_test.go`.
  </action>
  <verify>
    <automated>go build ./tui/... && go test ./tui/... -run TestSeverityGlyphWarningDistinct -count=1</automated>
  </verify>
  <acceptance_criteria>
    - styles.go has all 10 new tokens + `SeverityGlyph` + `asciiMode`; `SeverityGlyph(doctor.SeverityWarning,false) == "!"` and `!= "✗"` (asserted by `TestSeverityGlyphWarningDistinct` added here).
    - keymap.go has all new bindings; `go vet ./tui/...` clean.
    - messages.go has the new message types + helper cmds; push/pop removed.
    - `go build ./tui/...` succeeds (package compiles after old screen-stack files are removed from the build).
    - English-only: all comments/identifiers in English.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Lay down RED test scaffolds naming every downstream symbol</name>
  <read_first>
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-VALIDATION.md § "Per-Task Verification Map" and § "Wave 0 Requirements" (the exact test names)
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-RESEARCH.md § "Phase Requirements → Test Map" and § "The Recurring D-16 Blindspot"
    - tui/wiring_test.go (existing TestDepsThreadedEndToEnd pattern to extend)
    - git show HEAD:tui/dashboard_test.go and git show HEAD:tui/prove_test.go (port targets for health/wizard tests)
  </read_first>
  <action>
    Create failing (RED) test scaffolds — one per VALIDATION.md row — that reference the structs/functions later plans build, so each slice begins from a genuine RED. Per the project TDD note (MEMORY: TDD RED stub under strict lint), write stubs that fail honestly under hard-fail lint without `--no-verify`: use `t.Skip("RED: implemented in Plan NN")` ONLY where the referenced symbol does not yet exist (so the file compiles), and a real failing assertion where the symbol exists from this plan. Mark every skip with the plan that turns it green.

    Files and their tests (test names are LOCKED by VALIDATION.md — do not rename):
    - `tui/model_test.go` (new, replaces git-history original): `TestRootModelTwoPaneRenders`, `TestSmallTerminal`, `TestSidebarVisible`, `TestSidebarCollapse`, `TestViewSwitch`, `TestFooterAlwaysRendered`, `TestHelpOverlay`, `TestPaletteOpen` — Skip-RED pending Plan 02.
    - `tui/sidebar_test.go` (new): sidebar managed + unmanaged row rendering — Skip-RED pending Plan 02.
    - `tui/health_test.go` (new, port shape from `git show HEAD:tui/dashboard_test.go`): `TestHealthFamilies` (8 families emit familyResultMsg), `TestFixModal` — Skip-RED pending Plan 03.
    - `tui/wizard_test.go` (new, port shape from `git show HEAD:tui/prove_test.go`): `TestWizardOpen`, `TestWizardProveRetry`, `TestInlineEdit` — Skip-RED pending Plans 04/05.
    - `tui/confirm_test.go` (new): `TestDeleteModal` — Skip-RED pending Plan 06.
    - `tui/wiring_test.go` (EXTEND): add `TestBuildTUIDepsNilGuard` asserting the (future) 5-return `buildTUIDeps()` yields non-nil `CheckDeps, CheckPerms, CheckCoherence, CheckOrphans, CheckSigning, CheckAgent, CheckBaseline, CheckOverlap` (all 8 CheckFn fields), non-nil `FixPerm/RemoveBlock/AddWiring`, non-nil `identity.Generate`, and non-nil DeleteDeps fields (`ReadSSH, ReadGitconfig, WriteSSH, WriteGitconfig, RemoveFragment, RemoveAllowedSigners, RemoveKeyFiles`). Until Plan 06 changes the signature, gate the body behind `t.Skip("RED: DeleteDeps wiring + 5-return buildTUIDeps land in Plan 06")` so it compiles against the current 4-return signature. This is the D-16 anti-blindspot guard (RESEARCH Pitfall 7).

    Do not implement any model logic here. The scaffolds exist so `go test ./tui/...` lists every required test and the per-slice plans flip them to green.
  </action>
  <verify>
    <automated>go test ./tui/... -count=1 -run 'TestRootModelTwoPaneRenders|TestHealthFamilies|TestWizardOpen|TestDeleteModal|TestBuildTUIDepsNilGuard' -v 2>&1 | grep -E 'SKIP|PASS|ok'</automated>
  </verify>
  <acceptance_criteria>
    - All six scaffold files exist and `go test ./tui/...` compiles and runs (skips count as not-yet-green, not compile errors).
    - Every VALIDATION.md test name is present in the codebase (grep finds each).
    - `TestBuildTUIDepsNilGuard` exists in wiring_test.go (skipped pending Plan 06).
    - `make test` exits 0 (skips allowed); `make lint` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal render → screen | Overlay helper composites strings; a slicing bug could corrupt ANSI sequences (cosmetic, not a security boundary) |
| package dependency surface | No new packages added this plan; all symbols are from already-pinned charm.land v2 stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.6-01 | Tampering | placeOverlay rune/ANSI slicing | mitigate | Operate on a cell grid with `lipgloss.Width`; never split ANSI escapes or multi-byte glyphs; clamp out-of-range rows/cols; covered by TestPlaceOverlayCompositesModal |
| T-05.6-02 | Denial of Service | oversized modal vs small terminal | mitigate | Clamp modal to background bounds; "Terminal too small" gate is Plan 02, but overlay must not panic at any size (asserted in test) |
| T-05.6-SC | Tampering | npm/pip/cargo/go installs | accept | No new dependencies introduced this plan; RESEARCH § Package Legitimacy Audit: "Not applicable — no new packages"; nothing to verify |
</threat_model>

<verification>
- `go build ./tui/...` succeeds after the screen-stack files are removed from the build.
- `go doc charm.land/lipgloss/v2 PlaceOverlay` returns no symbol (spike result recorded in overlay.go doc comment).
- `make test` exits 0 (RED scaffolds skipped, not failing-to-compile).
- `make lint` clean over the whole module.
</verification>

<success_criteria>
- PlaceOverlay spike resolved (ABSENT at v2.0.3) and the line-merge fallback implemented + tested.
- styles/keymap/messages carry every token, binding, and message the downstream slices reference.
- Every VALIDATION.md test name exists in a RED (skipped) state tagged with its closing plan.
- Module builds and `make test`/`make lint` are green.
</success_criteria>

<output>
Create `.planning/phases/05.6-integrated-tui-app-inserted/05.6-01-SUMMARY.md` when done.
</output>
