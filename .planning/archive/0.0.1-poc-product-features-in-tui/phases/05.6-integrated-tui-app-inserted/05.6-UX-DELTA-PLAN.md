---
phase: 05.6-integrated-tui-app-inserted
type: delta
gap_closure: true
created: 2026-06-16
source: 05.6-UX-REVIEW.md (ui-ux-designer agent) + real-TTY D-16 smoke divergences
status: approved
---

# Phase 5.6 — UX Delta Plan

Remediation of the failed D-16 manual smoke. Source findings: `05.6-UX-REVIEW.md`
(ui-ux-designer agent) and the user's first-run divergences on a ≥100-col terminal.

The D-16 manual-smoke gate remains **OPEN**: the phase is not complete until the
user re-drives a rebuilt binary and approves.

## Locked-decision changes (user-approved 2026-06-16)

- **D-01 reopened → contextual sidebar.** The rail stays always-present at 18 cols
  with Tab-cycle focus (D-01's real intent: layout stability + a guaranteed Tab
  target), but its CONTENT becomes per-view: identities (Identities), the 8 doctor
  families as a jump/filter list (Health), config sections (Global Options).
- **D-04 extended → `←/→` view switching.** Additive third path alongside the locked
  `1/2/3` + `Ctrl+P`; Tab still owns pane focus (no conflict).

## Work packages (each = one logical commit; compiles + passes make fmt/lint; TDD authoring)

- **WP-0 (in tree)** — Tab crash: guard nil cmd in create-wizard form routing
  (`model.go`) + regression test (`wizard_test.go`). [P0]
- **WP-1** — Render correctness [P0-1, P0-3a]: explicit `textinput` widths + a single
  fixed-width border (kills phantom boxes in edit + add; placeholders render full +
  faint); make `overlayLine` ANSI/multibyte-aware so modal content lands at correct
  columns (keep dimmed backdrop — do NOT swap to `lipgloss.Place`). Files:
  `detail.go`, `wizard.go`, `overlay.go` (+tests).
- **WP-2** — Discoverability [P0-2, P1-4]: number the tabs (`1 Identities  2 Health
  3 Global Options`); priority-ordered footer (`a add`/`? help` never dropped),
  collapse with `· more in ?` instead of right-truncation; add `1·2·3 views` +
  `enter select`. Files: `model.go` (+tests).
- **WP-3** — Launch defaults + Enter [P1-5, P1-6]: real `selected=0` on
  `refreshSidebarMsg`; seed a doctor run from `Init()` so badges show at rest; wire
  Enter on a sidebar row → focus the detail pane. Files: `model.go` (+tests).
- **WP-4** — Form controls + copy [P0-3b, P2-8]: implement the locked D-06
  match-strategy selector with live `includeIf` preview + a signing toggle (replaces
  `> 1`/`> y`); relabel `R rotate` → `R new key` + gloss in `?` help. Files:
  `wizard.go`, `detail.go`, `help.go`, `keymap.go`, `model.go` (+tests).
- **WP-5** — Contextual sidebar [P1-7] (D-01 reopened): per-view rail content; Tab
  focus + 18-col geometry preserved. Files: `model.go`, `sidebar.go`, `health.go`,
  `globalopts.go` (+tests).
- **WP-6** — Arrow nav [P2-9] (D-04 extended): `←/→` cycle views; add `←/→ views` to
  footer. Files: `model.go`, `keymap.go` (+tests).
- **WP-7** — Contract + re-verify: update UI-SPEC/CONTEXT for D-01/D-04 changes;
  update VALIDATION with new tests; `make test` + `make lint` green; rebuild;
  HAND BACK to the user for the real-TTY re-smoke (D-16 gate).

## Verification

`make test` + `make lint` green after every WP; full suite green before advancing.
No injected-fake-only proof of the gate — WP-7 ends at the user's real-TTY re-smoke.
