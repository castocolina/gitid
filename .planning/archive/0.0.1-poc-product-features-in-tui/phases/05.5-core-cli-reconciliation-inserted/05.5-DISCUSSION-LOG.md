# Phase 5.5: Core & CLI Reconciliation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-13
**Phase:** 5.5-Core & CLI Reconciliation
**Areas discussed:** Create-flow loop semantics, Match strategy prompt & URL pattern, Provider reconstruction approach, Doctor overlap check + write-time warn, Install feedback

---

## Create-flow loop semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Write key to ~/.ssh immediately | Real key pair into ~/.ssh up front; only the 4 config artifacts wait for PASS | ✓ |
| Stage in temp until PASS | Nothing in ~/.ssh until PASS; not reusable on rerun (current F-2 behavior) | |

| Option | Description | Selected |
|--------|-------------|----------|
| Keep key, offer cleanup hint | Leave key in ~/.ssh on quit; print location + note doctor flags it as orphan | ✓ |
| Prompt: keep or delete | Ask on quit | |
| Always delete on quit | Full rollback, loses key each abandoned attempt | |

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-persist on PASS | PASS is the gate; write immediately, no extra prompt | ✓ |
| One final confirm after PASS | Preview + y/N after PASS | |

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit typed confirm | [s] warns + requires explicit y/N before writing unauthenticated | ✓ |
| Single keypress, no extra confirm | [s] writes immediately | |

**User's choice:** Immediate key write + keep-on-quit + auto-persist-on-PASS + gated skip escape.
**Notes:** Directly addresses F-2 ("key vanished / inaccessible on rerun") and G-05 (must verify authenticated PASS, not reachability).

---

## Match strategy prompt & URL pattern

| Option | Description | Selected |
|--------|-------------|----------|
| Picker then value(s) | Ask folder/URL/both first, then prompt only needed value(s); folder default | ✓ |
| Always ask both, blank = skip | Two prompts every time | |

| Option | Description | Selected |
|--------|-------------|----------|
| Suggest git@<hostname>:<name>/** | Pre-fill glob from hostname + name | ✓ |
| No default, free text + example | Empty prompt with inline example | |

| Option | Description | Selected |
|--------|-------------|----------|
| Wire into `identity update` too | Edit matches on existing identities | ✓ |
| Add-time prompt only (defer update) | Only `identity add` | |

| Option | Description | Selected |
|--------|-------------|----------|
| Defer flags to later | Interactive-only this phase | |
| Add flags now | Non-interactive --name/--gitdir/--url/--provider on add/update | ✓ |

**User's choice:** Picker + suggested URL default + update wiring + non-interactive flags now.
**Notes:** "Add flags now" is a deliberate scope addition beyond the ROADMAP success criteria — planner should add matching requirement(s).

---

## Provider reconstruction approach

| Option | Description | Selected |
|--------|-------------|----------|
| Store marker + hostname-map fallback | Marker in managed block; hostname-map fallback for legacy/custom | ✓ |
| Store marker only | No fallback; legacy shows empty until rewritten | |
| Hostname-map only (no marker) | No stored value; breaks self-hosted/custom | |

| Option | Description | Selected |
|--------|-------------|----------|
| Comment in SSH managed Host block | `# gitid: provider=github` in the Host block | ✓ |
| Key in the gitconfig fragment | `[gitid] provider = github` in fragment | |

| Option | Description | Selected |
|--------|-------------|----------|
| Tolerate via fallback, no rewrite | Legacy resolves via fallback; markers added on next update/rotate | ✓ |
| Doctor offers backfill | Doctor detects + backfills markerless blocks | |

**User's choice:** Marker comment in SSH Host block + hostname-map fallback + tolerant (no forced migration).
**Notes:** Honors no-sidecar IDENT-07; replaces brittle TrimPrefix derivation at loader.go:46-51.

---

## Doctor overlap check + write-time warn

| Option | Description | Selected |
|--------|-------------|----------|
| Identical gitdir | Exact same gitdir value | ✓ |
| Nested/prefix gitdir | Parent vs child dir | ✓ |
| Overlapping URL patterns | hasconfig globs matching same remote | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| Warning | Last-wins still resolves; report winner + how to disambiguate | ✓ |
| Error | Non-zero error-tier exit | |

| Option | Description | Selected |
|--------|-------------|----------|
| Warn + confirm to proceed | Show conflict, explicit y/N | ✓ |
| Block the write | Refuse overlapping match | |

**User's choice:** All three overlap kinds + warning severity + warn-and-confirm at write time.
**Notes:** Shared detection (F-7) feeds both doctor and add/update; pairs with auto-persist-on-PASS so a hard block was rejected.

---

## Install feedback

| Option | Description | Selected |
|--------|-------------|----------|
| Makefile post-install step | make resolves path, prints PATH hint | |
| gitid self-reports too | path/PATH check inside gitid (doctor/where), works for direct go install | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| Defer (separate task) | Completion auto-install later | ✓ |
| Fold into 5.5 | make install-completions now | |

**User's choice:** gitid self-reports (plus make hint); completion auto-install deferred.

---

## Claude's Discretion

- Exact prompt/menu wording, the PASS-detection substring set (reuse internal/tester),
  and the URL-overlap heuristic implementation.

## Deferred Ideas

- Shell-completion auto-install (`make install-completions`) — standalone task / TUI work.
- Integrated TUI app — Phase 5.6.
- Doctor backfill of provider markers — considered, deferred in favor of tolerant fallback.
