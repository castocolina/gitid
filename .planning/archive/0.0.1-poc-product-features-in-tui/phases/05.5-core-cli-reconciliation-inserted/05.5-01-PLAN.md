---
phase: 05.5-core-cli-reconciliation-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/harness_test.go
  - e2e/create_e2e_test.go
  - e2e/match_e2e_test.go
  - e2e/overlap_e2e_test.go
  - e2e/install_e2e_test.go
  - internal/sshconfig/marker_roundtrip_test.go
  - Makefile
autonomous: true
requirements: [FIX-CREATE-01, FIX-RECON-01, MATCH-URL-01, DOC-08, FIX-INSTALL-01]
user_setup: []

must_haves:
  truths:
    - "An agent can run `make test-e2e` and get a deterministic hermetic result with no network and no real SSH key"
    - "A fake `ssh` on PATH emits the authenticated banner or Permission-denied based on an env toggle so the auth-PASS gate is testable"
    - "The kevinburke/ssh_config provider-marker comment survives a parse→render→parse round-trip (Assumption A4 proven) before any reader relies on it"
  artifacts:
    - path: "e2e/harness_test.go"
      provides: "SandboxHome, BuildBinary, FakeSSHDir helpers behind //go:build e2e"
      contains: "func FakeSSHDir"
    - path: "internal/sshconfig/marker_roundtrip_test.go"
      provides: "A4 stability test: # gitid: provider= comment round-trips"
      contains: "provider="
    - path: "Makefile"
      provides: "test-e2e target"
      contains: "test-e2e"
  key_links:
    - from: "e2e/harness_test.go FakeSSHDir"
      to: "internal/tester exec.Command(\"ssh\", ...)"
      via: "PATH prepend via cmd.Env"
      pattern: "GITID_FAKE_SSH_MODE"
    - from: "Makefile test-e2e"
      to: "go test -tags e2e ./e2e/..."
      via: "make target"
      pattern: "tags e2e"
---

<objective>
Build the Wave-0 test infrastructure ALL downstream plans depend on: a deterministic
hermetic E2E harness (fake `ssh` on PATH + sandbox HOME + scripted-stdin driver), the
kevinburke/ssh_config comment-preservation stability test (Assumption A4) that MUST pass
before any reader relies on the provider marker, the failing E2E test shells (RED) for the
five requirements, and the `make test-e2e` target. No production code changes.

Purpose: De-risk the whole phase. The fake-ssh harness makes the auth-PASS gate
(FIX-CREATE-01) and the retry/skip/quit loop CI-runnable and regression-proof; the A4
stability test confirms the marker design (FIX-RECON-01) before it is built on.
Output: `e2e/` package (build-tagged), one new sshconfig round-trip test, a Makefile target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-CONTEXT.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-RESEARCH.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-PATTERNS.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates the test scaffolding only. New symbols introduced here (so the
source-grounding pass excludes them from drift verification):
- `e2e` package + `//go:build e2e` build tag
- Test helpers: `SandboxHome(t)`, `BuildBinary(t)`, `FakeSSHDir(t, mode)`, `repoRoot(t)`
- Env toggle constant: `GITID_FAKE_SSH_MODE` (values: `pass`, `denied`, `timeout`)
- Fake-ssh script (static string literal written via `os.WriteFile`, mode `0o700`)
- E2E test functions (RED): `TestCreateFlow_PASSGateAutoPermits`, `TestCreateFlow_QuitKeepsKey`,
  `TestCreateFlow_SkipRequiresConfirm`, `TestCreateFlow_DeniedThenPass`,
  `TestMatchStrategy_Gitdir`, `TestMatchStrategy_HasConfig`, `TestMatchStrategy_Both`,
  `TestDoctor_OverlapWarning_IdenticalGitdir`, `TestDoctor_OverlapWarning_NestedGitdir`,
  `TestInstall_PathFeedback`
- A4 stability test: `TestProviderMarker_RoundTripStable`
- Makefile target: `test-e2e`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: E2E harness package — sandbox HOME, binary builder, fake-ssh installer</name>
  <files>e2e/harness_test.go, Makefile</files>
  <read_first>
    - PATTERNS.md §`e2e/harness_test.go (new — shared E2E helpers)` (lines 458-552) — exact helper shapes, the static fake-ssh script literal, and the `cmd.Env` PATH-prepend pattern
    - RESEARCH.md §`E2E Test Harness (Agent-Driven)` (lines 354-432) — fake-ssh strategy, the `-G` branch the script must handle, gosec G-204 avoidance, build-tag rationale
    - CONTEXT.md D-18, D-20 — fake-ssh is `os.WriteFile` 0o700 static literal injected by PATH-prepend; NEVER `exec.Command("sh","-c",...)`
    - cmd/gitid/add_test.go:17-51, 93-133 — `fakeDeps`, `t.Setenv("HOME", t.TempDir())`, scripted-stdin pattern (the unit-level analog)
    - internal/tester/tester.go:106 (the `exec.Command("ssh", ...)` PATH-resolution seam the fake binary intercepts)
    - Makefile lines 11-22, 93-110 — `test` target pattern, `.PHONY` line, `GOPATH_BIN`
  </read_first>
  <action>
    Create `e2e/harness_test.go` with first line `//go:build e2e` then `package e2e`. Implement
    three exported test helpers per PATTERNS.md: `SandboxHome(t *testing.T) string`
    (`home := t.TempDir(); t.Setenv("HOME", home); return home`); `BuildBinary(t *testing.T) string`
    (run `exec.Command("go","build","-o",bin,"./cmd/gitid")` with `cmd.Dir = repoRoot(t)`, fail on
    error with CombinedOutput); `FakeSSHDir(t *testing.T, mode string) string` (write the fake ssh
    script as a STATIC string literal via `os.WriteFile(scriptPath, []byte(script), 0o700)` annotated
    `//nolint:gosec // test-only static script (G306)`, then `t.Setenv("GITID_FAKE_SSH_MODE", mode)`,
    return dir). The fake script MUST first handle `case "$1" in -G)` emitting a fixture `ssh -G`
    block (`user git`/`hostname ssh.github.com`/`port 443`/`identitiesonly yes`/`identityfile ...`,
    exit 0), then switch on `$GITID_FAKE_SSH_MODE`: `pass)` emits the literal
    `You've successfully authenticated` banner, `denied)` emits `Permission denied (publickey).`,
    `timeout)` emits a connect-timeout line, `*)` defaults to denied. Add a `repoRoot(t)` helper that
    walks up from the test working dir to the directory containing `go.mod`. Do NOT use
    `exec.Command("sh","-c",...)` anywhere. In the Makefile add a `## test-e2e:` doc comment + target
    `test-e2e: build` running `go test -tags e2e -race -timeout 60s ./e2e/...`, and append `test-e2e`
    to the `.PHONY` line.
  </action>
  <verify>
    <automated>go vet -tags e2e ./e2e/... 2>&1 | tee /dev/stderr; grep -q 'test-e2e' Makefile && grep -q '0o700' e2e/harness_test.go && ! grep -q 'sh", "-c"' e2e/harness_test.go && echo HARNESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/harness_test.go` first line is `//go:build e2e` and it declares `func FakeSSHDir(`, `func SandboxHome(`, `func BuildBinary(`, `func repoRoot(`
    - `FakeSSHDir` writes the script with `os.WriteFile(..., 0o700)` and sets `GITID_FAKE_SSH_MODE`; the file contains NO `exec.Command("sh", "-c"` substring
    - The fake script literal contains both a `-G)` branch and a `successfully authenticated` substring and a `Permission denied (publickey)` substring
    - `Makefile` contains a `test-e2e` target running `go test -tags e2e` and `test-e2e` appears on the `.PHONY` line
    - `go vet -tags e2e ./e2e/...` exits 0
  </acceptance_criteria>
  <done>The hermetic harness compiles under the e2e build tag; fake ssh is a static 0o700 literal injected by PATH; `make test-e2e` is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Failing E2E test shells (RED) for the five requirements + A4 marker stability test</name>
  <files>e2e/create_e2e_test.go, e2e/match_e2e_test.go, e2e/overlap_e2e_test.go, e2e/install_e2e_test.go, internal/sshconfig/marker_roundtrip_test.go</files>
  <read_first>
    - RESEARCH.md §`E2E hermetic scenario — create-flow` (lines 434-475) — the full `exec.CommandContext(bin,"identity","add")` + scripted-stdin + `cmd.Env` pattern and the on-disk artifact assertions
    - RESEARCH.md §`Phase Requirements → Test Map` (lines 695-718) — exact E2E test names per requirement
    - RESEARCH.md Pitfall 1 (lines 508-512) — stdin exhaustion: always append trailing `\n` and include the loop response
    - RESEARCH.md Pitfall 2 + Assumption A4 (lines 514-518, 803) — marker MUST be the LAST line of the Host stanza body; A4 must be unit-proven in this wave
    - PATTERNS.md §`e2e/create_e2e_test.go` row + §`E2E test invocation pattern` (lines 525-552)
    - internal/sshconfig/renderer.go:26-35 (RenderHostBlock body shape) and internal/sshconfig/reader.go:80-118 (parseHostBlockBody) — the A4 test feeds a body WITH a `# gitid: provider=github` line through `ssh_config.Decode` → re-serialize → decode and asserts the comment-bearing block still parses to the same Host/Hostname/Port/IdentityFile
    - CLAUDE.md — config parse→render→parse round-trip must be proven by tests (TOOL-04)
  </read_first>
  <action>
    Create the four `//go:build e2e` test files in `e2e/`. Each test calls `BuildBinary`,
    `SandboxHome`, `FakeSSHDir(t, <mode>)`, drives the real binary via
    `exec.CommandContext(ctx, bin, "identity", "add" | "list" | "doctor", ...)` with scripted stdin
    (`strings.NewReader(strings.Join([]string{...}, "\n")+"\n")`) and `cmd.Env` prepending the
    fake-ssh dir to PATH plus `HOME=`+home. Write them to FAIL NOW (RED) — assert the post-fix
    behavior that does not yet exist. Per the test map: `create_e2e_test.go` →
    `TestCreateFlow_PASSGateAutoPermits` (mode pass; assert the four config artifacts + key pair
    exist and stdout does NOT contain `Write all four artifacts now?`), `TestCreateFlow_QuitKeepsKey`
    (mode denied, stdin `q`; assert key pair exists but `.ssh/config`/`.gitconfig` do NOT),
    `TestCreateFlow_SkipRequiresConfirm` (mode denied, stdin `s` then `y`; assert artifacts written),
    `TestCreateFlow_DeniedThenPass` (mode toggled denied→pass via re-Setenv between loop iterations
    using stdin `r`; assert eventual persist). `match_e2e_test.go` → `TestMatchStrategy_Gitdir`,
    `TestMatchStrategy_HasConfig` (assert `hasconfig:remote.*.url:` present in `.gitconfig`),
    `TestMatchStrategy_Both` (assert two `[includeIf` conditions). `overlap_e2e_test.go` →
    `TestDoctor_OverlapWarning_IdenticalGitdir`, `TestDoctor_OverlapWarning_NestedGitdir` (seed two
    managed blocks with overlapping gitdir then assert `gitid doctor` stdout warns). `install_e2e_test.go`
    → `TestInstall_PathFeedback` (run `make install` or `gitid` self-report; assert output contains an
    install path + a PATH hint). Each RED test that depends on unbuilt behavior must use a real
    assertion (NOT `t.Skip`) so it is genuinely red. Then create
    `internal/sshconfig/marker_roundtrip_test.go` (NORMAL build, no tag): `TestProviderMarker_RoundTripStable`
    builds a Host body string with `# gitid: provider=github` as the LAST line, decodes via
    `ssh_config.Decode`, re-serializes with `cfg.String()`, decodes again, and asserts the Host alias,
    Hostname, Port, and IdentityFile are byte-identical across both decodes and the provider comment
    substring survives in the serialized form. This test must PASS (it proves A4) — if it fails, the
    marker placement design is wrong and Plan 02 must adjust before building on it.
  </action>
  <verify>
    <automated>go test ./internal/sshconfig/ -run TestProviderMarker_RoundTripStable -count=1 2>&1 | tee /dev/stderr | grep -q '^ok\|PASS' && go vet -tags e2e ./e2e/... && echo TESTS_COMPILE</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/sshconfig/ -run TestProviderMarker_RoundTripStable` PASSES (A4 proven)
    - `go vet -tags e2e ./e2e/...` compiles all four e2e test files
    - `e2e/create_e2e_test.go` defines `TestCreateFlow_PASSGateAutoPermits`, `TestCreateFlow_QuitKeepsKey`, `TestCreateFlow_SkipRequiresConfirm`, `TestCreateFlow_DeniedThenPass`
    - `e2e/match_e2e_test.go` defines `TestMatchStrategy_Gitdir`, `TestMatchStrategy_HasConfig`, `TestMatchStrategy_Both`
    - `e2e/overlap_e2e_test.go` defines `TestDoctor_OverlapWarning_IdenticalGitdir`, `TestDoctor_OverlapWarning_NestedGitdir`
    - `e2e/install_e2e_test.go` defines `TestInstall_PathFeedback`
    - No e2e test uses `t.Skip` to mask the RED state
  </acceptance_criteria>
  <done>The A4 stability test passes proving the marker round-trips; all RED E2E shells compile and assert the not-yet-built post-fix behavior so Wave 5/7 can turn them green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → child gitid process | E2E test sets `cmd.Env`/`cmd.Stdin` for the child binary only |
| fake-ssh script → PATH resolution | The child gitid resolves `ssh` via the prepended fake-ssh dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-01 | Tampering | fake-ssh installer | mitigate | `os.WriteFile(path, []byte(staticLiteral), 0o700)` — NO `exec.Command("sh","-c",...)`, no fmt-interpolated shell (D-20); verified by `! grep 'sh", "-c"'` gate |
| T-05.5-02 | Tampering | PATH injection | mitigate | fake-ssh dir prepended only via child `cmd.Env`; the test process PATH is never mutated; dir is `t.TempDir()` (auto-removed) |
| T-05.5-03 | Elevation | fake-ssh script perms | accept | 0o700 executable is required for PATH exec; created under per-test temp dir, removed on test exit |
| T-05.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (Go stdlib only; no new go.mod deps per RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `go test ./internal/sshconfig/ -run TestProviderMarker_RoundTripStable -count=1` PASSES (A4)
- `go vet -tags e2e ./e2e/...` exits 0
- `make lint` exits 0 (no `sh -c`, gosec-clean fake-ssh write)
- `grep -c 'sh", "-c"' e2e/harness_test.go` returns 0 (use `grep -v '^//' | grep -c` if a comment mentions it)
</verification>

<success_criteria>
- The hermetic E2E harness compiles and `make test-e2e` is runnable (tests are RED, which is expected pre-implementation)
- The A4 marker-stability test passes, unblocking Plan 02's provider marker
- No production code changed; no new go.mod dependency added
</success_criteria>

<output>
Create `.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-01-SUMMARY.md` when done
</output>
