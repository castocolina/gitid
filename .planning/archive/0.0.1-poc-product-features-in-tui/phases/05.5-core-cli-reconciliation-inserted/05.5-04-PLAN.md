---
phase: 05.5-core-cli-reconciliation-inserted
plan: 04
type: tdd
wave: 4
depends_on: ["05.5-03"]
files_modified:
  - cmd/gitid/add.go
  - cmd/gitid/update.go
  - cmd/gitid/match.go
  - cmd/gitid/match_test.go
  - cmd/gitid/add_test.go
  - cmd/gitid/update_test.go
autonomous: true
requirements: [MATCH-URL-01]
user_setup: []

must_haves:
  truths:
    - "`gitid identity add` prompts a match-strategy picker — folder (gitdir), repo URL (hasconfig), or both — and writes the chosen condition(s) (D-07)"
    - "The URL choice suggests a default `git@<hostname>:<name>/**` and writes `hasconfig:remote.*.url:<pattern>` (D-08)"
    - "`both` writes two [includeIf] conditions, OR-applied by git (D-07)"
    - "Match-strategy editing works on `identity update` too, and `--name/--gitdir/--url/--provider` flags skip the matching prompts (D-09)"
    - "The host-alias prompt is labeled 'Host alias' (D-10/F-5)"
  artifacts:
    - path: "cmd/gitid/match.go"
      provides: "shared match-strategy picker + buildMatches + default URL pattern + matchKinds"
      contains: "buildMatches"
    - path: "cmd/gitid/add.go"
      provides: "add uses the picker + --name/--gitdir/--url/--provider flags"
      contains: "buildMatches"
    - path: "cmd/gitid/update.go"
      provides: "update uses the picker pre-filled from existing.Matches + --gitdir/--url/--provider flags"
      contains: "buildMatches"
  key_links:
    - from: "cmd/gitid/match.go buildMatches"
      to: "gitconfig.Match{Kind: MatchHasconfig}"
      via: "URL choice → hasconfig:remote.*.url pattern"
      pattern: "MatchHasconfig"
    - from: "cmd/gitid/add.go flags"
      to: "picker prompts"
      via: "flag-present skips prompt"
      pattern: "--gitdir"
---

<objective>
Add match-strategy selection at write time (MATCH-URL-01 / F-5, F-6): a picker (1=folder gitdir,
2=repo URL hasconfig, 3=both) on `identity add` AND `identity update`, with the URL choice
suggesting a `git@<hostname>:<name>/**` default written as `hasconfig:remote.*.url:<pattern>`
(D-08); `both` writes two `[includeIf]` conditions (D-07). Wire non-interactive
`--name/--gitdir/--url/--provider` flags (D-09) that skip the matching prompts, and confirm the
host-alias prompt reads "Host alias" (D-10). The rendering layer already serializes both kinds —
this is input-layer only.

Purpose: Users can scope an identity by folder, by repo URL, or both — the F-6 capability the UAT
asked for. Output: a shared picker reused by add + update + flags.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-CONTEXT.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-RESEARCH.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols introduced (excluded from drift verification):
- `cmd/gitid/match.go` (new file) with: `promptMatchStrategy(r, out, gitdirDefault, urlDefault) []gitconfig.Match`,
  `buildMatches(choice, gitdirVal, urlVal string) []gitconfig.Match`, `matchKinds([]gitconfig.Match) string`
  (returns "gitdir"/"hasconfig"/"both"), `defaultURLPattern(hostname, name string) string` (→ `git@<hostname>:<name>/**`)
- New cobra flags on `newAddCmd`: `--name`, `--gitdir`, `--url`, `--provider`
- New cobra flags on `newUpdateCmd`: `--gitdir`, `--url`, `--provider` (name stays positional per Q2)
- Removed: the single `Match gitdir` prompt + `Matches: []Match{{MatchGitdir,...}}` literals in add.go gatherCreateInput and update.go
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Shared match-strategy picker + buildMatches + default URL pattern (RED→GREEN)</name>
  <files>cmd/gitid/match.go, cmd/gitid/match_test.go</files>
  <read_first>
    - internal/gitconfig/renderer.go:15-58 (MatchKind, Match struct, MatchGitdir/MatchHasconfig, the `condition()` serializer), :59-81 (RenderIncludeIf over []Match) — both kinds already render
    - internal/gitconfig/reader.go:81-85 (conditionToMatch — how stored conditions map back to Match for pre-fill)
    - cmd/gitid/list.go:114 (renderMatches — existing []Match → string for display/pre-fill defaults)
    - PATTERNS.md §`cmd/gitid/add.go` Match strategy picker (lines 299-316) and §`cmd/gitid/update.go` (lines 394-406) — picker prompt text + `buildMatches`/`matchKinds`/`strategyDefault` shapes
    - RESEARCH.md Pattern 3 (lines 249-273) — picker flow, default URL pattern `git@<hostname>:<name>/**`, value written as `hasconfig:remote.*.url:<pattern>` (D-08)
    - CONTEXT.md D-07, D-08, D-10
  </read_first>
  <behavior>
    - Test: `buildMatches("1", "~/git/personal/", "")` → one `Match{Kind:MatchGitdir, Value:"~/git/personal/"}`
    - Test: `buildMatches("2", "", "remote.*.url:git@ssh.github.com:user/**")` → one `Match{Kind:MatchHasconfig, Value:"remote.*.url:git@ssh.github.com:user/**"}` (value carries the `remote.*.url:` prefix exactly as conditionToMatch expects)
    - Test: `buildMatches("3", gitdir, url)` → two Matches (gitdir then hasconfig)
    - Test: `defaultURLPattern("ssh.github.com", "user_z3r0_gh")` → `git@ssh.github.com:user_z3r0_gh/**`
    - Test: `matchKinds([]Match{{MatchGitdir}})` → "gitdir"; `{{MatchHasconfig}}` → "hasconfig"; both → "both"; empty → "gitdir" (default)
    - Test: `promptMatchStrategy` with scripted stdin choice `2` then a URL value returns the hasconfig Match; choice `3` returns both; empty defaults to gitdir
  </behavior>
  <action>
    Create `cmd/gitid/match.go`. Implement `defaultURLPattern(hostname, name string) string` returning
    `"git@"+hostname+":"+name+"/**"`. Implement `buildMatches(choice, gitdirVal, urlVal string)
    []gitconfig.Match`: choice "2"/"url" → single `MatchHasconfig` with value `"remote.*.url:"+urlPattern`
    where the picker hands `urlVal` as the bare pattern and buildMatches prepends `remote.*.url:` so the
    stored Value matches `conditionToMatch`'s `hasconfig:` strip (verify against reader.go:81-85 to keep
    the round-trip exact); choice "3"/"both" → `[gitdir, hasconfig]`; default → single `MatchGitdir`.
    Implement `matchKinds([]gitconfig.Match) string` returning "gitdir"/"hasconfig"/"both"/("gitdir" when
    empty). Implement `promptMatchStrategy(r *bufio.Reader, out io.Writer, gitdirDefault, urlDefault
    string) []gitconfig.Match` printing the 3-option menu (per PATTERNS text), reading the choice, then
    prompting only for the needed value(s) with the supplied defaults, and returning `buildMatches(...)`.
    Reuse the existing `prompt`/`fp` helpers. Confirm the stored hasconfig Value form against
    RenderIncludeIf output (`[includeIf "hasconfig:remote.*.url:..."]`). RED-stub-under-strict-lint first.
  </action>
  <verify>
    <automated>go test -race ./cmd/gitid/ -run 'TestMatch|TestBuildMatches|TestDefaultURL' -count=1 2>&1 | tee /dev/stderr | grep -qE '^ok|PASS' && grep -q 'func buildMatches' cmd/gitid/match.go && grep -q 'remote.\*.url' cmd/gitid/match.go</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/gitid/match.go` declares `func buildMatches(`, `func matchKinds(`, `func defaultURLPattern(`, `func promptMatchStrategy(`
    - `defaultURLPattern("ssh.github.com","user_z3r0_gh")` returns `git@ssh.github.com:user_z3r0_gh/**`
    - URL/both choices produce `MatchHasconfig` values that round-trip through `gitconfig.RenderIncludeIf` → `conditionToMatch` unchanged (proven by a round-trip test)
    - `go test -race ./cmd/gitid/ -run TestMatch` exits 0
    - RED then GREEN commits present
  </acceptance_criteria>
  <done>A shared picker builds the correct []Match for gitdir/url/both with the exact stored hasconfig form; URL default derived from hostname+name.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Wire picker + non-interactive flags into add and update (RED→GREEN)</name>
  <files>cmd/gitid/add.go, cmd/gitid/update.go, cmd/gitid/add_test.go, cmd/gitid/update_test.go</files>
  <read_first>
    - cmd/gitid/add.go:30-41 (newAddCmd flag wiring), :264-314 (gatherCreateInput — the `Match gitdir` prompt at :282 + the gitdir-only `Matches` literal at :300 to replace), :278-279 (Provider + "Host alias" prompts — confirm label)
    - cmd/gitid/update.go:24-36 (newUpdateCmd, ExactArgs(1) positional name — keep per Q2), :124-141 (provider-omission comment + the single `Match gitdir` collapse to replace), :130 ("Host alias" — already correct)
    - PATTERNS.md §`cmd/gitid/add.go` Cobra flags (lines 317-331) + Loop/flag-or-prompt pattern, §`cmd/gitid/update.go` provider flag (lines 408-415)
    - RESEARCH.md Pattern 3 update wiring + flags (lines 257-273), Pitfall 6 (update match collapse drops hasconfig — must read existing.Matches and pre-fill), Pitfall 7 (update provider omission comment — flag-only restore), Open Questions Q2 (update keeps positional name; --name only on add) and Q3 (update --provider overwrites marker via flag only, no prompt)
    - cmd/gitid/match.go (Task 1 — the picker this task calls)
    - CONTEXT.md D-09, D-10; the flag-or-prompt pattern (PATTERNS §`prompt` shared pattern lines 584-595)
  </read_first>
  <behavior>
    - Test (add): scripted stdin choosing strategy `2` (URL) writes a `hasconfig:remote.*.url:` condition into the previewed includeIf; strategy `3` writes two conditions
    - Test (add flags): `--gitdir ~/work/` skips the gitdir prompt and uses the flag value; `--url <pattern>` skips the URL prompt; `--name foo` skips the name prompt; `--provider gitlab` skips the provider prompt (flags and remaining prompts coexist)
    - Test (add): the host-alias prompt label is exactly "Host alias" (D-10)
    - Test (update): pre-fills the picker from `existing.Matches` (a hasconfig-only identity defaults the picker to strategy 2, not gitdir) and does NOT silently drop a hasconfig condition (Pitfall 6 regression)
    - Test (update flags): `--gitdir`/`--url` skip the picker value prompts; `--provider gitlab` sets `edited.Provider` and writes the new marker; with no `--provider` the existing marker is preserved (Q3)
    - Test (update): name stays positional (no `--name` flag on update — Q2)
  </behavior>
  <action>
    In `newAddCmd` add `StringVar` flags `--name`, `--gitdir`, `--url`, `--provider` and thread them
    into `runIdentityAdd`→`runCreateNew`→`gatherCreateInput` (pass a small flag-values struct or named
    params). In `gatherCreateInput`: replace the single `Match gitdir` prompt + gitdir-only `Matches`
    literal with `promptMatchStrategy(r, out, gitdirDefault, defaultURLPattern(hostname, name))` (or
    `buildMatches` directly when flags are present — flag-or-prompt: if `--gitdir`/`--url` set, build from
    flags and skip the picker; if only one set, picker for the other). Apply the same flag-or-prompt to
    name/provider. Confirm the alias prompt label is "Host alias" (it already is — assert in a test).
    In `newUpdateCmd` add `--gitdir`, `--url`, `--provider` flags (NO `--name` — name is positional per
    Q2). Replace update.go:134-141 with `matchKinds(existing.Matches)` to derive the default strategy +
    `promptMatchStrategy` pre-filled from `renderMatches(existing.Matches)` values, building the new
    `edited.Matches` wholesale (never collapse to a single gitdir — Pitfall 6). Update the provider
    comment (lines 124-130): set `edited.Provider = providerFlag` only when `--provider` is given (Q3,
    flag-only, no prompt); otherwise leave the existing provider so the marker is preserved on rewrite.
    RED-stub-under-strict-lint for the RED commit.
  </action>
  <verify>
    <automated>go test -race ./cmd/gitid/ -count=1 2>&1 | tee /dev/stderr | grep -qE '^ok' && grep -q '"gitdir"' cmd/gitid/add.go && grep -q '"url"' cmd/gitid/update.go && grep -q 'promptMatchStrategy\|buildMatches' cmd/gitid/update.go</automated>
  </verify>
  <acceptance_criteria>
    - `newAddCmd` registers `--name`, `--gitdir`, `--url`, `--provider`; `newUpdateCmd` registers `--gitdir`, `--url`, `--provider` (and NOT `--name`)
    - add and update call the shared picker/`buildMatches`; neither contains a hard-coded single-gitdir `Matches` literal for the user-chosen match
    - update pre-fills the strategy from `existing.Matches` and does not drop a hasconfig condition (Pitfall 6 regression test passes)
    - update sets `edited.Provider` only when `--provider` is supplied (Q3); host-alias label is "Host alias"
    - `go test -race ./cmd/gitid/` exits 0
    - RED then GREEN commits present
  </acceptance_criteria>
  <done>add and update both offer gitdir/url/both selection with non-interactive flags; URL conditions round-trip; update no longer collapses matches to gitdir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user prompt/flag values → includeIf conditions | gitdir path and URL pattern flow into managed gitconfig blocks |
| --provider flag → SSH marker comment | provider value written into the `# gitid: provider=` marker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-12 | Tampering | shell injection via --gitdir/--url flags | mitigate | Values flow into FILE writes (RenderIncludeIf), never into `exec.Command`; all exec calls are arg-slice form (RESEARCH Security Domain) — no shell interpolation |
| T-05.5-13 | Tampering | newline injection in --provider forging a second directive | mitigate | RenderHostBlock panics on newline in provider (Plan 02 guard); input layer also validates provider with `identityNameRe` charset before use |
| T-05.5-14 | Tampering | malformed URL pattern breaking includeIf round-trip | mitigate | `buildMatches` stores the exact `remote.*.url:` form `conditionToMatch` expects; a round-trip test (Task 1) proves parse→render→parse stability (TOOL-04) |
| T-05.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; existing deps only |
</threat_model>

<verification>
- `go test -race ./cmd/gitid/ ./internal/gitconfig/` exits 0
- `make lint` exits 0 (provider validated, no shell interpolation)
- A round-trip test proves a hasconfig URL condition survives RenderIncludeIf → conditionToMatch unchanged
</verification>

<success_criteria>
- add + update prompt gitdir/url/both and write the chosen condition(s) (MATCH-URL-01, SC-2)
- URL default `git@<hostname>:<name>/**` written as `hasconfig:remote.*.url:<pattern>`
- `--name/--gitdir/--url/--provider` flags skip prompts; host-alias label is "Host alias"
- update no longer silently drops hasconfig conditions
</success_criteria>

<output>
Create `.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-04-SUMMARY.md` when done
</output>
