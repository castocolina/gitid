---
phase: 05.5-core-cli-reconciliation-inserted
plan: 06
type: tdd
wave: 6
depends_on: ["05.5-01", "05.5-05"]
files_modified:
  - internal/platform/install.go
  - internal/platform/install_test.go
  - cmd/gitid/doctor.go
  - Makefile
autonomous: true
requirements: [FIX-INSTALL-01]
user_setup: []

must_haves:
  truths:
    - "`make install` reports the resolved install path and whether it is on PATH, with an export hint when it is not (F-1, D-17)"
    - "`gitid` self-reports its binary path + PATH membership (surfaced via doctor) so the hint also works for a direct `go install` that bypasses make (D-17)"
  artifacts:
    - path: "internal/platform/install.go"
      provides: "BinaryInstallInfo() (path, onPATH, err) using os.Executable + LookPath"
      contains: "func BinaryInstallInfo"
    - path: "Makefile"
      provides: "install target prints path + PATH hint"
      contains: "PATH"
  key_links:
    - from: "cmd/gitid/doctor.go"
      to: "platform.BinaryInstallInfo"
      via: "info-level finding under a binary/install check"
      pattern: "BinaryInstallInfo"
    - from: "Makefile install"
      to: "GOPATH_BIN"
      via: "echo install path + PATH membership test"
      pattern: "GOPATH_BIN"
---

<objective>
Tell the user where the binary went and whether it is reachable (FIX-INSTALL-01 / F-1, D-17).
`make install` echoes the resolved install path and a PATH export hint when `$(GOPATH_BIN)` is not
on `$PATH`. `gitid` self-reports its own binary path + PATH membership via `os.Executable()` +
`exec.LookPath` (with the GOBIN→GOPATH/bin fallback), surfaced as an info-level doctor finding so
the hint works even for a direct `go install` that bypasses make.

Purpose: The UAT hit `make install` succeeding but `gitid not found` (GOBIN not on PATH) with zero
feedback. Output: actionable install-location + PATH guidance from two surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-CONTEXT.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-RESEARCH.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols introduced (excluded from drift verification):
- `internal/platform/install.go` (new) with `func BinaryInstallInfo() (path string, onPATH bool, err error)`
  using `os.Executable()` + `filepath.EvalSymlinks` + a testable `binaryOnPath(exePath, pathEnv string) bool`
- A `cmd/gitid/doctor.go` install-info surface: an info-level finding (FamilyDeps) rendering path + PATH membership
- Makefile `install` target additions: echo `installed: <path>` + a PATH-membership test producing an
  `export PATH=...` hint when not found
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: platform.BinaryInstallInfo — self-report path + PATH membership (RED to GREEN)</name>
  <files>internal/platform/install.go, internal/platform/install_test.go</files>
  <read_first>
    - RESEARCH.md Pattern 5 Surface A (lines 303-316) + Code Examples os.Executable + LookPath (lines 615-630) — `BinaryInstallInfo` shape, EvalSymlinks, LookPath-on-base
    - RESEARCH.md Assumptions A1, A2, A3 (lines 800-803) — EvalSymlinks/LookPath/GOBIN-empty behavior + risks
    - internal/platform/ existing files (e.g. install_hint / CurrentOS) for package conventions + test style (table tests with PATH manipulation via t.Setenv)
    - CONTEXT.md D-17
  </read_first>
  <behavior>
    - Test: with a fake binary dir prepended to PATH (via `t.Setenv("PATH", dir+":"+orig)`) and a probe path inside it, `binaryOnPath(probe, pathEnv)` reports true (testable seam so the test doesn't depend on the real running binary)
    - Test: with the binary's dir NOT on PATH, `binaryOnPath` returns false
    - Test: `BinaryInstallInfo` returns a non-empty path and a nil error on the happy path
    - Test: a symlinked exe resolves to its target via EvalSymlinks (or returns the symlink path gracefully on EvalSymlinks error — A1)
  </behavior>
  <action>
    Create `internal/platform/install.go` with `BinaryInstallInfo() (path string, onPATH bool, err error)`:
    `exe, err := os.Executable()`; on error return. `exe, evalErr := filepath.EvalSymlinks(exe)` —
    tolerate EvalSymlinks error by keeping the unresolved path (A1). Factor the PATH check into a pure
    helper `binaryOnPath(exePath, pathEnv string) bool` (split `pathEnv` on `os.PathListSeparator`, return
    true if `filepath.Dir(exePath)` matches any entry) so it is unit-testable without the real binary —
    then `onPATH = binaryOnPath(exe, os.Getenv("PATH"))`. Return `(exe, onPATH, nil)`. Follow the project's
    RED-stub-under-strict-lint convention for the RED commit.
  </action>
  <verify>
    <automated>go test -race ./internal/platform/ -count=1 2>&1 | tee /dev/stderr | grep -qE '^ok' && grep -q 'func BinaryInstallInfo' internal/platform/install.go</automated>
  </verify>
  <acceptance_criteria>
    - `internal/platform/install.go` declares `func BinaryInstallInfo() (string, bool, error)` and a testable `binaryOnPath` helper
    - Tests prove on-PATH true/false via `t.Setenv("PATH", ...)` without depending on the real running binary
    - `go test -race ./internal/platform/` exits 0
    - RED then GREEN commits present
  </acceptance_criteria>
  <done>gitid can compute its own resolved binary path and PATH membership deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Surface install info via doctor + Makefile install path/PATH echo</name>
  <files>cmd/gitid/doctor.go, Makefile</files>
  <read_first>
    - cmd/gitid/doctor.go:76-129 (runDoctor — where to append an info-level install finding), :443-494 (renderReport — how findings render), :502-540 (renderFinding — info uses the `!` glyph + `[info]`)
    - internal/doctor/doctor.go Family constants + SeverityInfo (line 27 area) — reuse `FamilyDeps` for the install info finding to avoid a new family (PATTERNS line 428: "Use the existing FamilyDeps family to avoid a new Family constant if possible")
    - RESEARCH.md Pattern 5 Surface B (lines 318-330) + GOBIN/GOPATH fallback (line 330) — the Makefile echo recipe
    - PATTERNS.md Makefile section (lines 444-454) — exact install recipe (GOPATH_BIN already defined at Makefile:22)
    - Makefile:104-110 (current install/uninstall targets)
    - internal/platform/install.go (Task 1 — BinaryInstallInfo)
    - CONTEXT.md D-17; Deferred Ideas — completion auto-install is OUT OF SCOPE (D-17 covers PATH feedback only)
  </read_first>
  <action>
    In `cmd/gitid/doctor.go`, surface the install info: call `platform.BinaryInstallInfo()` and append an
    info-level `doctor.Finding{Family: doctor.FamilyDeps, Severity: doctor.SeverityInfo}` whose Title names
    the resolved binary path, Explanation states "on PATH" or "NOT on PATH", and SuggestedFix (only when
    not on PATH) is an `export PATH="$PATH:<dir>"` hint; `Fix: nil`. Append it to the findings before
    rendering so it shows under Dependencies; keep it info severity so it never raises the exit tier. In
    the `Makefile` `install` target, after `go install ./cmd/gitid`, add a recipe block (per PATTERNS):
    set `INSTALL_PATH="$(GOPATH_BIN)/gitid"`, echo the installed path, then test PATH membership with
    `echo "$$PATH" | tr ':' '\n' | grep -qF "$(GOPATH_BIN)"` printing either an on-PATH OK line or an
    `export PATH="$$PATH:$(GOPATH_BIN)"` hint. Do NOT add completion auto-install (deferred). This is
    glue/config wiring an already-tested function.
  </action>
  <verify>
    <automated>go test -race ./cmd/gitid/ -count=1 2>&1 | tee /dev/stderr | grep -qE '^ok' && grep -q 'BinaryInstallInfo' cmd/gitid/doctor.go && grep -A5 '^install:' Makefile | grep -q 'PATH' && make build >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/gitid/doctor.go` calls `platform.BinaryInstallInfo()` and appends an info-level finding with the path + PATH membership + (when off-PATH) an export hint
    - `Makefile` `install` target echoes the install path and prints a PATH OK line or an `export PATH` hint
    - The install finding is `SeverityInfo` and does NOT change the doctor exit tier
    - `go test -race ./cmd/gitid/` exits 0 and `make build` succeeds
  </acceptance_criteria>
  <done>Both `make install` and `gitid doctor` report where the binary is and whether it is on PATH, with an actionable hint when it is not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| os.Executable / PATH env → reported path | self-location reads process metadata + the PATH env var |
| make shell recipe → echoed hint | install recipe runs in a shell with `$$PATH`/`$(GOPATH_BIN)` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-18 | Tampering | command injection via go env / install path | mitigate | No `exec.Command("sh","-c",...)` in Go; `os.Executable`/`exec.LookPath` take no user input; Makefile recipe uses `$(GOPATH_BIN)` (make-resolved) and quoted `$$PATH`, no user-interpolated strings |
| T-05.5-19 | Information Disclosure | printing the absolute binary path | accept | The install path is non-sensitive and is the whole point of the feedback (F-1) |
| T-05.5-20 | Spoofing | a different `gitid` earlier on PATH reported as "on PATH" | accept | A2: report what is found; the path printed lets the user see the discrepancy |
| T-05.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; Go stdlib only |
</threat_model>

<verification>
- `go test -race ./internal/platform/ ./cmd/gitid/` exits 0
- `make build` succeeds; `make install` echo block is present (`grep -A5 '^install:' Makefile | grep PATH`)
- `make lint` exits 0 (no shell-form exec, gosec-clean)
</verification>

<success_criteria>
- `make install` reports install path + PATH membership with an export hint when off-PATH (FIX-INSTALL-01, SC-4)
- `gitid doctor` self-reports the binary path + PATH membership (works for direct `go install`)
- The install finding is info-only and never changes the exit tier
</success_criteria>

<output>
Create `.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-06-SUMMARY.md` when done
</output>
