---
phase: 03-full-identity-crud-multi-identity
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - cmd/gitid/list.go
  - cmd/gitid/list_test.go
  - cmd/gitid/main.go
  - internal/sshconfig/coexistence_test.go
autonomous: true
requirements: [IDENT-03]
must_haves:
  truths:
    - "`gitid identity list` reconstructs every identity from disk and displays key path, alias, provider, port, and match strategy (D-03)"
    - "An identity with a partial block set shows a light 'incomplete' marker naming what is missing (D-02), not a diagnosis"
    - "Two identities on the same provider, written with distinct aliases, each resolve via `ssh -G` to their own distinct IdentityFile (SC-2)"
  artifacts:
    - path: "cmd/gitid/list.go"
      provides: "`gitid identity list` Cobra command + reconstruction wiring"
      contains: "func newListCmd"
    - path: "internal/sshconfig/coexistence_test.go"
      provides: "multi-identity coexistence proof (SC-2)"
      contains: "TestMultiIdentityCoexistence"
  key_links:
    - from: "cmd/gitid/list.go"
      to: "internal/identity.Reconstruct"
      via: "os.ReadFile(~/.ssh/config, ~/.gitconfig) → Reconstruct(.., gitconfig.ReadFragment)"
      pattern: "identity\\.Reconstruct"
    - from: "cmd/gitid/main.go"
      to: "newListCmd"
      via: "identity.AddCommand(newListCmd())"
      pattern: "AddCommand\\(newListCmd"
---

<objective>
Deliver the `gitid identity list` vertical slice (IDENT-03): a real, minimal Cobra command that reconstructs the identity set from disk (via plan 01's `identity.Reconstruct`) and renders each identity's key path, alias, provider, port, and match strategy, plus a light "incomplete" marker for partial block sets (D-02/D-03). Also land the multi-identity coexistence proof (SC-2): two same-provider identities resolve to distinct IdentityFiles under `ssh -G`.

After this plan a user can run `gitid identity list` and see every identity gitid manages, with no sidecar database — the read half of the lifecycle is observable end-to-end.

Purpose: make reconstruction (plan 01) user-visible and prove multi-identity coexistence.
Output: `cmd/gitid/list.go` (`newListCmd`/`runIdentityList`), main.go registration, and `TestMultiIdentityCoexistence`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/03-full-identity-crud-multi-identity/03-CONTEXT.md
@.planning/phases/03-full-identity-crud-multi-identity/03-PATTERNS.md

@cmd/gitid/add.go
@cmd/gitid/main.go
@internal/identity/identity.go
@internal/tester/tester.go
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan:
- `main.newListCmd() *cobra.Command`
- `main.runIdentityList(in io.Reader, out io.Writer) error`
- `internal/sshconfig` `TestMultiIdentityCoexistence` (test, SC-2 proof)
Consumes (from plan 01): `identity.Reconstruct`, `identity.Account.Incomplete`, `gitconfig.ReadFragment`.
Reuses (shared `main`-package helpers — do NOT redeclare): `fp` (add.go:260), `tester.ParseResolved`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: `gitid identity list` command — reconstruct from disk + render (D-03)</name>
  <read_first>
    - cmd/gitid/add.go — analog: `newAddCmd` (lines 30-41, Cobra factory + `RunE` delegation), `fp` (line 260, shared output helper — do NOT redefine), `buildDeps` wiring style (lines 319-423). `list` is read-only so it needs NO Deps struct.
    - cmd/gitid/main.go — `newRootCmd` (line 27), the `identity` parent command with `identity.AddCommand(newAddCmd())` etc. (lines 40-43); register `newListCmd()` here.
    - cmd/gitid/rotate.go — `gatherRotateAccount` (lines 101-131) shows resolving the gitid-managed paths from `os.UserHomeDir()` (`~/.ssh/config`, `~/.gitconfig`, `~/.gitconfig.d/<name>`).
    - internal/identity/identity.go — `Account` (incl. plan-01 `Incomplete` field), `Account.Matches []gitconfig.Match`.
    - internal/gitconfig/renderer.go — `Match`/`MatchKind` + `Match.condition()` to render the match-strategy column.
    - 03-PATTERNS.md §"cmd/gitid/list.go" — `newListCmd`/`runIdentityList` shape, imports, "no Deps struct (read-only)" note, "do NOT redefine fp".
    - 03-CONTEXT.md D-03 (list columns + light incomplete marker) + Claude's Discretion (grouped-by-identity layout, optional flat flag, plain text — no ANSI colour per 03-RESEARCH Open Q3).
  </read_first>
  <files>cmd/gitid/list.go, cmd/gitid/list_test.go, cmd/gitid/main.go</files>
  <action>
    Create cmd/gitid/list.go in `package main`. Add `func newListCmd() *cobra.Command` with `Use: "list"`, `Short: "List gitid-managed identities reconstructed from ~/.ssh/config and ~/.gitconfig (IDENT-03)"`, `RunE` delegating to `runIdentityList(cmd.InOrStdin(), cmd.OutOrStdout())` (zero logic in RunE per shared pattern 8). Optionally add a `--plain` bool flag for one-row-per-account parseable output (Claude's Discretion — include only if it earns its keep; grouped-by-identity is the default human view). Implement `func runIdentityList(in io.Reader, out io.Writer) error`: resolve HOME via `os.UserHomeDir()`; read `~/.ssh/config` and `~/.gitconfig` bytes via `os.ReadFile` (treat os.IsNotExist as empty `[]byte`, not an error — a fresh machine has no identities yet); call `identity.Reconstruct(sshBytes, gcBytes, gitconfig.ReadFragment)`; render with `fp(out, ...)`. For each Account print, grouped by identity: a header line with Name + KeyPath + GitName/GitEmail, then per-account alias / provider (or Hostname when Provider is empty, A1) / port / match strategy (render each `Account.Matches` entry via its `MatchKind`: gitdir vs hasconfig + Value). When `acct.Incomplete != ""` append a light marker line, e.g. `"  ! incomplete: missing " + acct.Incomplete` (keep it light — a marker + what's missing, never a diagnosis or fix; deep checks are Phase 4 doctor). Empty result → print a friendly "no gitid-managed identities found" line. Register in main.go: add `identity.AddCommand(newListCmd())` alongside the existing `newAddCmd`/`newTestCmd`/`newRotateCmd` registrations.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestRunIdentityList' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func newListCmd() \*cobra.Command' cmd/gitid/list.go`
    - `grep -q 'func runIdentityList(' cmd/gitid/list.go`
    - `grep -q 'identity.Reconstruct(' cmd/gitid/list.go`
    - `grep -q 'AddCommand(newListCmd())' cmd/gitid/main.go`
    - `grep -vc '^[[:space:]]*//' cmd/gitid/list.go` shows no redefinition of `fp` (reuse from add.go): `grep -c 'func fp(' cmd/gitid/list.go` returns 0
    - `go build ./...` exits 0
    - `go test ./cmd/... -count=1` exits 0
    - `make lint` passes
  </acceptance_criteria>
  <done>`gitid identity list` reconstructs and prints key path/alias/provider/port/match per identity with a light incomplete marker; registered under the `identity` command; reads-missing-config-as-empty; table-test green; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-identity coexistence proof — two same-provider aliases resolve to distinct IdentityFiles (SC-2)</name>
  <read_first>
    - internal/tester/tester.go — `ParseResolved` (lines 126-143): parses `ssh -G` output; `rc.IdentityFiles` is the resolved key-path slice.
    - internal/sshconfig/writer.go — `Write(configPath, accountName, hostBlock, globalBlock)` (line 41): writes a managed Host block keyed by account name via ReplaceBlock.
    - internal/sshconfig/renderer.go — `RenderHostBlock(alias, hostname, port, identityFile)` (line 26).
    - 03-RESEARCH.md §"Research Question 6" — the exact `TestMultiIdentityCoexistence` pattern: hermetic `t.TempDir()` HOME, write two identities, `ssh -G -F <configPath> <alias>` (the `-F` flag makes it fully hermetic — reads the given file, not real `~/.ssh/config`), assert distinct `IdentityFiles[0]`.
    - 03-PATTERNS.md §"internal/sshconfig/coexistence_test.go" — analog (`ssh -G -F` hermetic pattern, `t.Skip` guard when `ssh` absent).
    - 03-VALIDATION.md §"Manual-Only Verifications" — this automates the SC-2 check that would otherwise be manual.
  </read_first>
  <files>internal/sshconfig/coexistence_test.go</files>
  <action>
    Create internal/sshconfig/coexistence_test.go (`package sshconfig`). Add `TestMultiIdentityCoexistence`: set up a hermetic `home := t.TempDir()`, `t.Setenv("HOME", home)`; `os.MkdirAll(filepath.Join(home,".ssh"), 0o700)`; `configPath := filepath.Join(home, ".ssh", "config")`. Write identity "personal" via `Write(configPath, "personal", RenderHostBlock("personal.github.com","ssh.github.com",443, home+"/.ssh/id_ed25519_personal"), "")` and identity "work" via `Write(configPath, "work", RenderHostBlock("work.github.com","ssh.github.com",443, home+"/.ssh/id_ed25519_work"), "")`. Guard with `t.Skip` when `exec.LookPath("ssh")` fails. Define a local `resolveAlias(alias string)` that runs `exec.Command("ssh","-G","-F",configPath,alias).Output()` (`//nolint:gosec` arg-slice form) and returns `tester.ParseResolved(string(out))`. Assert both aliases produce a non-empty `IdentityFiles` and that `personalRC.IdentityFiles[0] != workRC.IdentityFiles[0]` (distinct keys → SC-2 proven). Use the existing `internal/tester` import.
  </action>
  <verify>
    <automated>go test ./internal/sshconfig/... -run 'TestMultiIdentityCoexistence' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func TestMultiIdentityCoexistence' internal/sshconfig/coexistence_test.go`
    - `grep -q 'ssh", "-G", "-F"' internal/sshconfig/coexistence_test.go` (hermetic `-F` form)
    - `grep -q 'IdentityFiles\[0\] != ' internal/sshconfig/coexistence_test.go` (distinctness assertion)
    - `go test ./internal/sshconfig/... -run 'TestMultiIdentityCoexistence' -count=1` exits 0 (or skips cleanly when `ssh` absent)
    - `make lint` passes
  </acceptance_criteria>
  <done>Two same-provider identities written via the real sshconfig writer resolve to distinct IdentityFiles under hermetic `ssh -G -F`; SC-2 proven by automated test (no real `~/.ssh` touched); lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk config → list reader | `~/.ssh/config` / `~/.gitconfig` read as untrusted bytes; missing files must not error |
| CLI arg (none) → command | `list` takes no positional identity arg, so no name-injection surface in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information disclosure | `list` printing key material | mitigate | `list` prints key PATHS and the `.pub` path only — never reads or echoes private-key bytes |
| T-03-07 | Denial of service | missing/empty config files | mitigate | `os.IsNotExist` treated as empty `[]byte`; reconstruction returns an empty slice, list prints a friendly empty message — no crash |
| T-03-08 | Tampering | `ssh -G` reading the real `~/.ssh/config` in test | mitigate | Coexistence test uses `ssh -G -F <tempConfig>` so it never reads the developer's real config (hermetic) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; Phase 3 adds zero external deps (RESEARCH §"No New External Packages") |
</threat_model>

<verification>
- `go test ./cmd/... ./internal/sshconfig/... -count=1` green
- `make test` green before wave merge
- `make lint` (golangci-lint + gosec) green
- Manual smoke (optional, end-of-phase human-verify): run `gitid identity list` against the developer's real config and confirm the displayed columns match
</verification>

<success_criteria>
- IDENT-03 satisfied: `gitid identity list` shows key path, alias, provider, port, match strategy + light incomplete marker, reconstructed from disk with no sidecar DB
- SC-2 satisfied: two same-provider identities resolve to distinct IdentityFiles (automated proof)
- Command registered under `gitid identity`; full suite + lint green
</success_criteria>

<output>
Create `.planning/phases/03-full-identity-crud-multi-identity/03-02-SUMMARY.md` when done.
</output>
