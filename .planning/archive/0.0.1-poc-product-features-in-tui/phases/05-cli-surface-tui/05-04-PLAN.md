---
phase: 05-cli-surface-tui
plan: 04
type: tdd
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - tui/identitydetail.go
  - tui/createform.go
  - tui/updateform.go
  - tui/addaccountform.go
  - tui/prove.go
  - tui/copy.go
  - tui/identitylist.go
  - tui/form_test.go
  - tui/prove_test.go
autonomous: true
requirements: [TUI-02]
user_setup: []

must_haves:
  truths:
    - "From identity detail, e opens the update form, h opens the add-account form, c copies the pubkey inline with upload instructions"
    - "From the identity list, a opens the create form; forms advance focus with Tab and retreat with Shift+Tab"
    - "Submitting a create/update/add-account form transitions to the Prove-Before-Write screen before any write occurs"
    - "The Prove-Before-Write screen shows the exact ssh command run and its real output for both phases, notes the backup, and enables the confirm only after both phases pass"
    - "A phase-1 (or phase-2) test failure disables confirm and shows the failure output; only Esc is active"
    - "Confirmed write routes through identity.Deps (the same filewriter-backed seams the CLI uses); the TUI adds no new write path"
  artifacts:
    - path: "tui/prove.go"
      provides: "proveModel two-phase state machine running tester.PreWrite/Resolved as tea.Cmds with a confirm gate (D-04)"
      contains: "func runPreWriteCmd"
    - path: "tui/createform.go"
      provides: "createFormModel (textinput per field) with ValidateName gate"
      contains: "textinput.Model"
    - path: "tui/identitydetail.go"
      provides: "identityDetailModel with e/h/c actions + delete/rotate handoff"
      contains: "identityDetailModel"
  key_links:
    - from: "tui/prove.go"
      to: "internal/tester.PreWrite + internal/tester.Resolved"
      via: "tea.Cmd goroutines (never in Update)"
      pattern: "tester\\.PreWrite|tester\\.Resolved"
    - from: "tui/prove.go confirm"
      to: "identity.Deps write seams"
      via: "on Enter-after-both-pass → identity.Create/update/AddAccount via deps"
      pattern: "identity\\.|deps\\."
    - from: "tui/createform.go"
      to: "internal/identity.ValidateName"
      via: "form field validation before submit"
      pattern: "identity\\.ValidateName"
    - from: "tui/copy.go"
      to: "internal/upload.Instructions + internal/clipboard.Copy"
      via: "inline copy overlay"
      pattern: "upload\\.Instructions|clipboard\\.Copy"
---

<objective>
Complete the TUI navigation tree (TUI-02): identity detail screen, the three in-app forms (Create, Update, Add-account — D-02), the inline Copy-pubkey action (D-06), and the shared Prove-Before-Write screen (D-04) that runs the two-phase SSH test as `tea.Cmd` goroutines, surfaces the exact command + real output, notes the backup, and gates the write behind an explicit confirm that is only active after both phases pass. Confirmed writes route through the existing `identity.Deps` seams — no new write path. Delete and Rotate remain CLI handoffs (D-03).

Purpose: Closes "add/edit forms without leaving the application" (SC-2/TUI-02) and the non-negotiable prove-before-write contract (D-04). Replaces the create/detail placeholder factories from 05-03 with real screens. Resolves RESEARCH Pitfall 7 (no blocking ssh in Update).
Output: detail + three forms + prove + copy screens, wired into the view-stack; form and prove tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-surface-tui/05-RESEARCH.md
@.planning/phases/05-cli-surface-tui/05-PATTERNS.md
@.planning/phases/05-cli-surface-tui/05-UI-SPEC.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `tui/identitydetail.go` — `identityDetailModel`, `newIdentityDetailScreen(account, tuiDeps) screenModel`, e/h/c actions + delete/rotate handoff
- `tui/createform.go` — `createFormModel` (textinput per field), `newCreateFormScreen(tuiDeps) screenModel`, ValidateName gate, submit→prove
- `tui/updateform.go` — `updateFormModel` (pre-filled, name immutable), submit→prove
- `tui/addaccountform.go` — `addAccountFormModel`, submit→prove
- `tui/prove.go` — `proveModel`, `provePhase` enum, `runPreWriteCmd`/`runResolvedCmd` tea.Cmds, confirm gate, write dispatch via identity.Deps
- `tui/copy.go` — `runClipboardCopyCmd`, `renderCopyOverlay(pubLine, provider, copyErr) string` inline action
- `tui/identitylist.go` — placeholder factories swapped for the real create-form/detail screens
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Identity detail + Create/Update/Add-account forms + inline Copy action</name>
  <files>tui/identitydetail.go, tui/createform.go, tui/updateform.go, tui/addaccountform.go, tui/copy.go, tui/identitylist.go, tui/form_test.go</files>
  <read_first>
    - tui/identitylist.go (05-03 — the placeholder newCreateFormScreen/newIdentityDetailScreen factories to replace; identityItem/account access)
    - tui/keymap.go (keys.Add/Edit/Copy/AddHost/Next/Prev/Submit/Back/Delete/Rotate) + tui/styles.go (StyleLabel/StyleInputActive/StyleInputInactive/StyleFaint/StylePass)
    - tui/messages.go (clipboardResultMsg, pushScreenMsg/popScreenMsg)
    - cmd/gitid/add.go lines 216-313 (gatherAddAccount + gatherCreateInput — exact field set/order/defaults)
    - cmd/gitid/update.go (update field set; name immutable) + cmd/gitid/upload.go→internal/upload (Instructions)
    - internal/identity/identity.go lines 25-64 (Account + CreateInput fields), internal/identity.ValidateName (05-01), internal/clipboard.Copy
    - .planning/phases/05-cli-surface-tui/05-PATTERNS.md §"createform/updateform/addaccountform", §"tui/identitydetail.go", §"tui/copy.go"
    - .planning/phases/05-cli-surface-tui/05-UI-SPEC.md Screens 3,4,5,5b + §"Copy Pubkey Action" + §"Handoff Messages"
  </read_first>
  <behavior>
    - Test (TestFormTabNavigation): Tab advances createFormModel focusIdx forward (wraps); Shift+Tab retreats. Exactly one input focused at a time.
    - Test (TestCreateFormNameValidation): a create form with an invalid Identity Name ("Bad Name") surfaces a validation error (via identity.ValidateName) and does NOT emit a submit/prove transition.
    - Test (TestIdentityDetailEditKey): 'e' on identity detail returns a pushScreenMsg toward the update form; 'h' returns a pushScreenMsg toward the add-account form.
    - Test (TestIdentityDetailCopyAction): 'c' on identity detail issues a clipboard copy cmd and the rendered overlay (renderCopyOverlay) contains the upload instructions for the provider.
    - Test (TestDetailDeleteHandoff): 'd' / 'r' on detail show the CLI handoff copy and emit no write/push (D-03).
  </behavior>
  <action>
    Create `tui/identitydetail.go`: `identityDetailModel{account identity.Account; deps tuiDeps; overlay string; ...}`, `newIdentityDetailScreen(acct identity.Account, deps tuiDeps) screenModel`, `view()` rendering the two-column metadata block per UI-SPEC Screen 3 (StyleLabel 16-wide + StyleBody/StyleFaint). `update`: keys.Edit→pushCmd(newUpdateFormScreen(account, deps)); keys.AddHost→pushCmd(newAddAccountFormScreen(account, deps)); keys.Copy→runClipboardCopyCmd + set overlay; keys.Delete/keys.Rotate→inline handoff copy (D-03, no write); keys.Back→popCmd. Create `tui/createform.go`, `tui/updateform.go`, `tui/addaccountform.go` using `charm.land/bubbles/v2/textinput` — one `textinput.Model` per field in the exact order from add.go gatherCreateInput / gatherAddAccount / update.go; Tab/Shift+Tab focus advance via `msg.Code == tea.KeyTab`/KeyBackTab (RESEARCH §textinput); validate the Identity Name field with `identity.ValidateName` (create/add) and treat the name as read-only in update (UI-SPEC Screen 5); on Enter at the last field, build the identity.CreateInput/Account and return `pushCmd(newProveScreen(action, input, deps))` (Task 2). Create `tui/copy.go`: `runClipboardCopyCmd(pubLine) tea.Cmd` calling `clipboard.Copy`, and `renderCopyOverlay(pubLine, provider string, copyErr error) string` mirroring UI-SPEC §Copy Pubkey Action (StylePass success / StyleFinding info-failure + manual-copy hint + `upload.Instructions(provider)`). In `tui/identitylist.go`, replace the placeholder factories so `newCreateFormScreen` returns the real create form and item-Enter returns `newIdentityDetailScreen`. Key handling via `tea.KeyPressMsg` + key.Matches throughout (RESEARCH Pitfall 3). Write `tui/form_test.go` with the behavior cases (direct update() unit tests; fakeDeps where writes/clipboard are involved).
  </action>
  <verify>
    <automated>go test ./tui/... -count=1 -run 'TestFormTabNavigation|TestCreateFormNameValidation|TestIdentityDetailEditKey|TestIdentityDetailCopyAction|TestDetailDeleteHandoff'</automated>
  </verify>
  <acceptance_criteria>
    - All five form/detail tests pass
    - `tui/createform.go` uses `textinput.Model` and `identity.ValidateName`
    - `tui/identitydetail.go` 'e'/'h' push update/add-account forms; 'c' triggers copy overlay; 'd'/'r' show handoff only
    - `tui/copy.go` renderCopyOverlay references `upload.Instructions`
    - identitylist placeholder factories replaced with real detail/create screens
    - `go build ./...` succeeds
  </acceptance_criteria>
  <done>Identity detail + three in-app forms (Create/Update/Add-account, D-02) + inline Copy action wired into the stack; name validation enforced; delete/rotate stay CLI handoffs (D-03). Forms submit toward the prove screen.</done>
  <maps_to>VALIDATION: TestFormTabNavigation, TestIdentityDetailEditKey (TUI-02); D-02/D-03/D-06 coverage</maps_to>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove-Before-Write screen — two-phase async test + confirm gate + write via identity.Deps</name>
  <files>tui/prove.go, tui/prove_test.go</files>
  <read_first>
    - tui/createform.go / updateform.go / addaccountform.go (Task 1 — submit handers push newProveScreen(action, input, deps))
    - tui/messages.go (preWriteResultMsg, resolvedResultMsg, writeResultMsg)
    - tui/styles.go (StyleHeader/StyleLabel/StyleBody/StyleFaint/StylePass + SeverityStyle) + tui/keymap.go (keys.Confirm, keys.Back)
    - cmd/gitid/add.go lines 459-481 (printPreWrite/printResolved — exact two-phase output shape) + lines 100-170 (identity.Create confirmed-write call sequence)
    - internal/tester/tester.go (PreWrite + Resolved signatures + Result/ResolvedConfig) + internal/identity/identity.go lines 119-235 (Deps + Create confirmed-write semantics)
    - .planning/phases/05-cli-surface-tui/05-PATTERNS.md §"tui/prove.go" (runPreWriteCmd/runResolvedCmd, two-phase state machine)
    - .planning/phases/05-cli-surface-tui/05-RESEARCH.md Pattern 5, Pitfall 7
    - .planning/phases/05-cli-surface-tui/05-UI-SPEC.md §"Screen 6: Prove-Before-Write" (layout, failure path, confirm copy) + §"Accessibility Contract" item 4-5
  </read_first>
  <behavior>
    - Test (TestProveScreenConfirmGate): the confirm action (Enter) is inert until BOTH preWriteResultMsg (pass) AND resolvedResultMsg (pass) have arrived; only then does keys.Confirm dispatch the write cmd.
    - Test (TestProveScreenPhase1Failure): a preWriteResultMsg with a Failure result sets the phase1Failed state, leaves confirm disabled, and the rendered view contains the failure output + "Cannot proceed"; Enter does nothing, only Esc is active.
    - Test (TestProveScreenRunsPhasesAsTeaCmds): entering the prove screen issues runPreWriteCmd as a tea.Cmd (Init), and a passing preWriteResultMsg issues runResolvedCmd — neither tester call happens inside Update directly.
    - Test (TestProveConfirmWritesViaDeps): after both phases pass, keys.Confirm invokes the injected write function (fake identity.Deps), proving the write routes through deps (no direct filewriter call in tui/prove.go).
  </behavior>
  <action>
    Create `tui/prove.go` (package tui). Define `provePhase` (phase1Running/phase1Done/phase1Failed/phase2Running/phase2Done/phase2Failed) and `proveModel{phase provePhase; action string; input ...; phase1Result/phase2Result tester.Result; resolved tester.ResolvedConfig; backupPath string; confirmActive bool; deps tuiDeps; spinner ...; viewport ...}`. `newProveScreen(action string, input <create/update/addaccount payload>, deps tuiDeps) proveModel` and an `init()` returning `runPreWriteCmd(keyPath, hostname, port)`. `runPreWriteCmd` / `runResolvedCmd` wrap `tester.PreWrite` / `tester.Resolved` in tea.Cmd closures (RESEARCH Pattern 5 / Pitfall 7 — NEVER call tester in Update). `update`: on preWriteResultMsg → if pass set phase1Done + issue runResolvedCmd; if fail set phase1Failed; on resolvedResultMsg → set phase2Done (or phase2Failed) and set confirmActive only when both passed; on keys.Confirm when confirmActive → return the write cmd that calls the matching `identity.Create`/update/AddAccount through `deps` (which is filewriter-backed — D-04: TUI adds no new write path) and emits writeResultMsg; on keys.Back → popCmd. `view() string` renders Screen 6 exactly per UI-SPEC: phase labels (StyleHeader), `Command:`/`Output:`/`Status:` lines (exact ssh command + real output, ✓/✗ glyphs), `Backup:` + `Action:` lines, and the confirm prompt "Write changes? [Enter to confirm / Esc to cancel]" shown ONLY after both phases pass; failure path shows the output + "Cannot proceed" and only Esc. Key handling via tea.KeyPressMsg. Write `tui/prove_test.go` with the four behavior cases using a fake identity.Deps whose write fns record invocation, and synthesized passing/failing tester.Result values.
  </action>
  <verify>
    <automated>go test ./tui/... -count=1 -run 'TestProveScreenConfirmGate|TestProveScreenPhase1Failure|TestProveScreenRunsPhasesAsTeaCmds|TestProveConfirmWritesViaDeps' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - All four prove tests pass
    - `tui/prove.go` contains `func runPreWriteCmd` and `func runResolvedCmd` wrapping tester calls in tea.Cmd
    - `grep -n "tester.PreWrite\|tester.Resolved" tui/prove.go` shows them only inside tea.Cmd closures, never in update()
    - confirm gate proven inert until both phases pass (TestProveScreenConfirmGate)
    - write routes through injected identity.Deps (TestProveConfirmWritesViaDeps); `grep -n "filewriter" tui/prove.go` returns nothing (no new write path)
    - `go build ./...` succeeds
  </acceptance_criteria>
  <done>Prove-Before-Write screen shows the exact two-phase command+output, notes the backup, gates the write behind explicit confirm only after both phases pass, and writes exclusively through identity.Deps (D-04). Failure path blocks the write. TUI-02 form→prove→write slice complete.</done>
  <maps_to>VALIDATION: TestProveScreenConfirmGate, TestProveScreenPhase1Failure (TUI-02/D-04)</maps_to>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form input → core | user-typed identity name/email/provider reach keygen, ssh exec, and the four-artifact write |
| TUI mutation → filesystem | in-app Create/Update/Add-account trigger writes to ~/.ssh/* and ~/.gitconfig* |
| tester tea.Cmd → ssh | prove screen shells out to ssh -i / ssh -G via internal/tester |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | identity name in create/add-account forms | mitigate | createform/addaccountform call `identity.ValidateName` before building CreateInput/Account; no submit/prove on invalid name (V5) |
| T-05-14 | Elevation/Tampering | in-app write path | mitigate | Writes go ONLY through identity.Deps seams (filewriter chokepoint: backup→atomic→idempotent→confirm); tui/prove.go imports no filewriter directly (D-04, RESEARCH Pitfall 10) |
| T-05-15 | Spoofing/Tampering | prove screen confirm gate | mitigate | Confirm inert until both ssh phases pass; Esc (no-write) is the safe default (UI-SPEC Accessibility 4-5); explicit Enter required before any write |
| T-05-16 | Denial of Service | blocking ssh test in Update | mitigate | tester.PreWrite/Resolved run only inside tea.Cmd goroutines; Update never blocks (RESEARCH Pattern 5 / Pitfall 7) |
| T-05-17 | Injection | ssh exec via tester | mitigate | Reuses internal/tester arg-slice (G204-clean) exec; TUI adds no new exec call |
| T-05-SC | Tampering | package installs | mitigate | No new package installs (deps from 05-01) |
</threat_model>

<verification>
- `go test ./tui/... -count=1` green (forms + prove)
- `grep -n "filewriter" tui/prove.go` returns nothing (no new write path; writes via identity.Deps)
- `grep -n "tester.PreWrite\|tester.Resolved" tui/prove.go` shows them only inside tea.Cmd closures
- `go test -race ./...` green at phase gate
</verification>

<success_criteria>
- From the dashboard a user navigates Dashboard→List→Detail→(Create/Update/Add-account form) without leaving the app (SC-2/TUI-02)
- Every in-app mutation passes through the Prove-Before-Write screen: exact command + real output shown, backup noted, explicit confirm only after both phases pass (D-04)
- Writes route exclusively through identity.Deps (filewriter chokepoint); Delete/Rotate remain CLI handoffs (D-03)
</success_criteria>

<output>
Create `.planning/phases/05-cli-surface-tui/05-04-SUMMARY.md` when done.
</output>
