---
phase: 01-bootstrap
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .pre-commit-config.yaml
  - Makefile
autonomous: false
requirements: [TOOL-02, TOOL-03]
must_haves:
  truths:
    - "pre-commit hooks run fmt + lint + gosec via make targets and block a malformed commit (TOOL-03, D-05, D-07)"
    - "the pre-push hook runs the full go test -race + coverage via make test (D-06)"
    - "hooks invoke make targets through repo: local, not ad-hoc commands or upstream hook repos (D-07)"
    - "make setup-env installs the git hooks so a fresh clone is gated after one command (TOOL-02)"
    - "no .github/workflows is created in Phase 1 (D-08)"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "repo: local hooks wired to make targets; pre-commit + pre-push stages"
      contains: "repo: local"
    - path: "Makefile"
      provides: "setup-env completes hook install via install-hooks sub-target"
      contains: "pre-commit install"
  key_links:
    - from: ".pre-commit-config.yaml pre-commit hooks"
      to: "make fmt / make lint"
      via: "entry: make <target>, language: system"
      pattern: "entry: make"
    - from: ".pre-commit-config.yaml pre-push hook"
      to: "make test"
      via: "stages: [pre-push], entry: make test"
      pattern: "make test"
    - from: "Makefile setup-env"
      to: "git hooks"
      via: "pre-commit install + pre-commit install --hook-type pre-push"
      pattern: "install --hook-type pre-push"
---

<objective>
Wire the git hooks. Create `.pre-commit-config.yaml` using `repo: local` hooks that
invoke the Makefile targets (D-07): a fast pre-commit stage running fmt + lint + gosec
(D-05) and a pre-push stage running the full `go test -race` + coverage via `make test`
(D-06). Complete the `setup-env` contract by adding the `install-hooks` sub-target
(placeholder left by 01-02) that installs both hook types. No CI / `.github/workflows`
is created (D-08).

Purpose: Close the Phase-1 goal — after `make setup-env` on a fresh clone, the engineer
is gated: bad commits are blocked locally and the full test suite runs before push. The
Makefile remains the single source of truth shared with future CI (D-07).

Output: A working pre-commit config and a complete `make setup-env`; a malformed commit
is demonstrably blocked. This plan has a human-verify checkpoint to confirm the hook
actually blocks a bad commit (Success Criterion 3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap/01-CONTEXT.md
@.planning/research/STACK.md
@CLAUDE.md
@.planning/phases/01-bootstrap/01-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (01-03) produces the hook-wiring portion of Phase 1:
- `.pre-commit-config.yaml` — `repo: local` hooks (D-07). pre-commit stage (D-05): `go-fmt` → `make fmt`, `go-lint` → `make lint` (golangci-lint + gosec embedded), each `language: system`, `pass_filenames: false`. pre-push stage (D-06): `go-test` → `make test`, `stages: [pre-push]`. No upstream hook repos (not `TekWizely/pre-commit-golang`).
- `Makefile` (edit) — add the `install-hooks` sub-target invoked by `setup-env`: `pre-commit install` (installs the pre-commit hook) and `pre-commit install --hook-type pre-push` (installs the pre-push hook). Replace the placeholder line 01-02 left in `setup-env`.
- Explicitly NOT created: any `.github/workflows/*` (D-08, CI deferred until a remote exists).

Depends on 01-02 (hooks call the make targets defined there).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author repo:local pre-commit config (fast pre-commit + full pre-push) and complete setup-env hook install</name>
  <files>.pre-commit-config.yaml, Makefile</files>
  <read_first>
    - .planning/research/STACK.md (.pre-commit-config.yaml repo: local template invoking make targets; pre-commit framework rationale)
    - .planning/phases/01-bootstrap/01-CONTEXT.md (D-05 pre-commit=fast fmt+lint+gosec, D-06 pre-push=full go test -race+coverage, D-07 hooks→make via repo:local, D-08 no CI)
    - Makefile (the setup-env placeholder from 01-02 to replace; the fmt/lint/test target names)
    - CLAUDE.md (pre-commit framework; English-only)
  </read_first>
  <action>
    Create `.pre-commit-config.yaml` with a single `repo: local` entry (D-07 — do NOT
    reference `TekWizely/pre-commit-golang` or any remote hook repo). Define the fast
    pre-commit stage hooks (D-05): a `go-fmt` hook with `entry: make fmt`, and a `go-lint`
    hook with `entry: make lint` (golangci-lint runs gosec embedded per .golangci.yml from
    01-02, satisfying the fmt+lint+gosec requirement). Each hook: `language: system`,
    `pass_filenames: false`. Define the full pre-push hook (D-06): `go-test` with
    `entry: make test`, `stages: [pre-push]`, `language: system`, `pass_filenames: false`.
    Then edit the Makefile: add an `install-hooks` sub-target running `pre-commit install`
    and `pre-commit install --hook-type pre-push`, and replace the placeholder line in
    `setup-env` so `setup-env` calls `install-hooks` after installing the tools (completes
    TOOL-02). Do NOT create any `.github/` directory or workflow file (D-08). All content
    in English.
  </action>
  <verify>
    <automated>cd /Users/ramon/git/personal/ssh-git-config && grep -q 'repo: local' .pre-commit-config.yaml && grep -q 'entry: make fmt' .pre-commit-config.yaml && grep -q 'entry: make lint' .pre-commit-config.yaml && grep -q 'entry: make test' .pre-commit-config.yaml && grep -q 'pre-push' .pre-commit-config.yaml && ! grep -qi 'TekWizely' .pre-commit-config.yaml && grep -q 'install --hook-type pre-push' Makefile && test ! -d .github && command -v pre-commit >/dev/null && pre-commit validate-config</automated>
  </verify>
  <acceptance_criteria>
    - `.pre-commit-config.yaml` contains `repo: local` and does NOT contain `TekWizely` or any `https://` remote repo URL.
    - pre-commit stage hooks invoke `make fmt` and `make lint` (`grep 'entry: make fmt'` and `grep 'entry: make lint'` both match).
    - A pre-push hook invokes `make test` and is scoped to `stages: [pre-push]` (`grep 'entry: make test'` and `grep 'pre-push'` both match).
    - `pre-commit validate-config` exits 0.
    - `Makefile` `setup-env` reaches `pre-commit install` and `pre-commit install --hook-type pre-push` (via the `install-hooks` sub-target); `grep 'install --hook-type pre-push' Makefile` matches.
    - No `.github` directory exists (`test ! -d .github`) — D-08.
  </acceptance_criteria>
  <done>.pre-commit-config.yaml wires fast pre-commit (fmt+lint) and full pre-push (test) to make targets via repo:local; setup-env installs both hook types; no CI added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify hooks block a malformed commit and gate a push</name>
  <action>
    Human-verification checkpoint (no autonomous file edits). The executor pauses here and
    asks the user to run the steps in `<how-to-verify>` to confirm the installed git hooks
    actually fire: `make setup-env` installs both hook types, a malformed commit is blocked
    by the pre-commit hook (make fmt / make lint), a clean commit succeeds, and the pre-push
    stage runs `make test`. This confirms Phase-1 Success Criterion 3 (pre-commit hooks
    block a malformed commit) and TOOL-03.
  </action>
  <what-built>
    `.pre-commit-config.yaml` (repo:local) and a completed `make setup-env` that installs
    the pre-commit and pre-push git hooks. The pre-commit hook runs `make fmt` + `make lint`;
    the pre-push hook runs `make test` (Phase-1 Success Criterion 3 and TOOL-03).
  </what-built>
  <how-to-verify>
    1. From the repo root run: `make setup-env` — confirm it completes without errors and
       installs the hooks (look for `pre-commit installed at .git/hooks/pre-commit` and
       `.git/hooks/pre-push`).
    2. Create a deliberately malformed Go file under `cmd/gitid/` (e.g. an unformatted
       file with an unchecked error or a gofmt violation), `git add` it, and attempt
       `git commit -m "test: should be blocked"`. Confirm the commit is REJECTED by the
       pre-commit hook (make fmt / make lint fails).
    3. Remove/fix the malformed file so the tree is clean again; confirm `git commit`
       now succeeds.
    4. (Optional, no remote required) Run `pre-commit run --hook-stage pre-push --all-files`
       and confirm `make test` runs and passes.
    Expected: step 2 blocks the commit; step 3 allows it; the test stage runs `make test`.
  </how-to-verify>
  <verify>
    <human-check>Malformed commit blocked by pre-commit hook; clean commit succeeds; pre-push stage runs `make test`.</human-check>
  </verify>
  <done>User confirms the malformed commit was blocked and the clean commit succeeded; hooks are installed by `make setup-env`.</done>
  <resume-signal>Type "approved" if the malformed commit was blocked and the clean commit succeeded, or describe what happened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `make setup-env` → pip install pre-commit | Installing the pre-commit framework from PyPI is a supply-chain surface |
| pre-commit hooks → shell (make targets) | Hooks execute make recipes; the entry strings must not introduce injection |
| git commit/push → local hooks | Hooks are the only Phase-1 gate (no CI, D-08); they must actually run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | pre-commit hook entries | mitigate | Hooks use `repo: local` with fixed `entry: make <target>` strings (no remote hook repo to compromise, D-07); `language: system` runs the project's own make targets only. No untrusted interpolation. |
| T-01-08 | Repudiation / Spoofing | local-only gating (no CI) | accept | D-08 defers CI until a remote exists; pre-commit + pre-push are the accepted Phase-1 gates. Risk accepted by explicit user decision; CI will re-encode the same make targets later (deferred idea, not a Phase-1 gap). |
| T-01-SC | Tampering | pre-commit install (PyPI) | mitigate | `pre-commit` is a widely-used, well-known PyPI package; install via the project's documented `setup-env`. Not an application dependency (dev tool only). The blocking human-verify checkpoint (Task 2) confirms the installed hooks actually fire, so a silently-broken hook install is caught. No `[ASSUMED]`/`[SUS]` application package is added. |
</threat_model>

<verification>
- `.pre-commit-config.yaml` uses `repo: local`, wires fmt/lint to pre-commit and test to pre-push.
- `pre-commit validate-config` exits 0.
- `make setup-env` installs both hook types.
- A malformed commit is blocked; a clean commit succeeds (human-verified, Task 2).
- No `.github/workflows` exists (D-08).
</verification>

<success_criteria>
- pre-commit hooks block a malformed commit by invoking the same make targets CI will use (TOOL-03, D-05, D-07).
- The pre-push hook runs the full test suite (D-06).
- `make setup-env` is the one-command bootstrap that leaves a fresh clone fully gated (TOOL-02 — Phase-1 goal).
- CI remains deferred (D-08).
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap/01-03-SUMMARY.md` when done.
</output>
