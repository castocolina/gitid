# Git-screen visual-divergence allowlist (Phase 4, D-12, plan 04-04 Task 2)
#
# Consumed by TWO independent gates, both against the SAME 5-field entries:
#
#   1. e2e/git_configuration_pty_e2e_test.go's
#      TestGitConfiguration_CompiledRealVsLiveDummyPTY — a RAW-PTY, real-
#      keystroke comparison of the compiled `cmd/gitid` binary against the
#      compiled `cmd/gitid-dummy` binary at 100x30 (D-12: no unit test, no
#      in-process tea.Msg synthesis stands in for this proof).
#   2. cmd/gitid/gate_visual_regression_test.go's in-process capture gate
#      (04-04 Task 3), which registers the same git-screen checkpoints
#      alongside the existing create-flow registry.
#
# Both binaries render Configure-Git through the SAME internal/tuikit
# identities.go code (the Phase 3 D-17 extraction): cmd/gitid injects a real
# Backend, cmd/gitid-dummy injects internal/dummytui.FixtureBackend. A
# structural divergence here means either a genuine, classified
# fixture-vs-live-data difference (below) or a real regression the gate must
# catch.
#
# STRICT SCHEMA — each entry must be EXACTLY:
#   checkpoint : region : predicate : decision-ref : reason
#
# where:
#   checkpoint    one of: git-form-filled | git-form-empty |
#                 match-strategy-select | review-readonly | result-success
#   region        one of: sidebar | header-status | git-form-fields |
#                 git-strategy | git-preview | git-ceremony
#   predicate     "contains:<quoted-text>" | "absent:<quoted-text>"
#                 The unconstrained "differs" predicate is FORBIDDEN — every
#                 differing region must be narrowly scoped (CR-04 precedent,
#                 03-09/03-10).
#   decision-ref  a scoped CTX-D-NN (04-CONTEXT.md) or UI-D-NN (04-UI-SPEC.md)
#                 identifier — D-NN/T-NN bare refs are Phase 3's own
#                 create-flow vocabulary and are NOT valid here.
#   reason        non-empty human-readable explanation
#
# VALIDATION RULES (enforced by TestGitConfiguration_CompiledRealVsLiveDummyPTY
# and, for the in-process gate, cmd/gitid/gate_visual_regression_test.go):
#   - No unknown checkpoint or region names
#   - No duplicate (checkpoint, region) pairs
#   - No blank reasons or missing decision-ref
#   - Every entry must be USED (a non-differing region in an allowlisted
#     entry fails — stale entries are rejected)
#   - Every checkpoint must have at least one NON-allowlisted region that is
#     structurally equal after normalization (identity token / email /
#     gitdir path / timestamp placeholders) — proving the gate is not
#     vacuously permissive
#   - "differs" predicate is NOT valid (CR-04 precedent)
#
# Blank lines and lines starting with # are ignored.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# CTX-D-12 — sidebar + header-status fixture-vs-live-HOME divergence.
# ALL 5 checkpoints: the real binary's sandbox HOME seeds exactly ONE
# identity (the checkpoint's own fixture, via seedGitPTYIdentity); the
# dummy's FixtureBackend always carries its full 8-identity
# IdentityManagerRows fixture set (internal/dummytui/data.go). The sidebar
# identity list and the header's "N ids · <health>" status line are
# therefore inherently different by construction — the SAME class of
# divergence Phase 3's create-flow allowlist classifies as T-03-SIDEBAR,
# scoped here to D-12 (the decision governing this Phase-4 real/dummy
# comparison).
# ---------------------------------------------------------------------------
git-form-filled:sidebar:absent:"clientB":CTX-D-12:real sidebar carries only the checkpoint's own seeded identity; dummy sidebar lists all 8 IdentityManagerRows fixtures including "clientB" — absent in real
git-form-filled:header-status:contains:"ids":CTX-D-12:header status shows the identity count, which differs (1 for real, 8 for dummy) — both contain the "ids" marker
git-form-empty:sidebar:absent:"clientB":CTX-D-12:real sidebar carries only the checkpoint's own seeded identity; dummy sidebar lists all 8 IdentityManagerRows fixtures including "clientB" — absent in real
git-form-empty:header-status:contains:"ids":CTX-D-12:header status shows the identity count, which differs (1 for real, 8 for dummy) — both contain the "ids" marker
match-strategy-select:sidebar:absent:"clientB":CTX-D-12:real sidebar carries only the checkpoint's own seeded identity; dummy sidebar lists all 8 IdentityManagerRows fixtures including "clientB" — absent in real
match-strategy-select:header-status:contains:"ids":CTX-D-12:header status shows the identity count, which differs (1 for real, 8 for dummy) — both contain the "ids" marker
review-readonly:sidebar:absent:"clientB":CTX-D-12:real sidebar carries only the checkpoint's own seeded identity; dummy sidebar lists all 8 IdentityManagerRows fixtures including "clientB" — absent in real
review-readonly:header-status:contains:"ids":CTX-D-12:header status shows the identity count, which differs (1 for real, 8 for dummy) — both contain the "ids" marker
result-success:sidebar:absent:"clientB":CTX-D-12:real sidebar carries only the checkpoint's own seeded identity; dummy sidebar lists all 8 IdentityManagerRows fixtures including "clientB" — absent in real
result-success:header-status:contains:"ids":CTX-D-12:header status shows the identity count, which differs (1 for real, 8 for dummy) — both contain the "ids" marker

# ---------------------------------------------------------------------------
# CTX-D-12 — gitdir includeIf preview, identity-name only (CR-14, iteration
# 4). This WAS a CTX-D-02 "gitdir default derivation" divergence: the
# dummy's frozen includeIf preview fixture
# (internal/dummytui/data.go GitScreenIncludeIfGitdirLine =
# `[includeIf "gitdir:~/personal/"]`) predated D-02's "~/git/<identity>/"
# derivation and used the pre-Phase-4 sample path (no "git/" segment), while
# the real binary correctly derived `~/git/<identity>/` per D-02
# (cmd/gitid/wiring.go matchesFor, gitconfig.Match.condition). CR-14 fixed
# FixtureBackend.IncludeIfPreview to substitute the real spec.GitDir instead
# of that frozen literal, so real and dummy now AGREE on the "~/git/"
# derivation — the CTX-D-02 divergence is gone. What remains is purely the
# identity-name substitution (fragment path + includeIf condition embed the
# selected identity), the SAME CTX-D-12 class the sidebar/header-status
# entries above already cover. Predicate: "gitdir:~/git/" must be present on
# BOTH sides — narrower than a blanket "differs", and it actively guards
# against this exact regression recurring (if the dummy's derivation ever
# regresses back to a frozen "~/<identity>/" literal, only the real side
# would carry the marker and this predicate correctly rejects it).
#
# git-form-empty carries NO entry here: its "not yet configured" checkpoint
# renders no includeIf preview content on either side (both regions extract
# empty and are skipped as non-comparable), so a git-preview entry for it
# would be stale (never triggered) — removed, not re-predicated.
# ---------------------------------------------------------------------------
git-form-filled:git-preview:contains:"gitdir:~/git/":CTX-D-12:the includeIf preview's fragment path and gitdir condition embed the selected identity's name, which differs between the real fixture and the dummy fixture by construction; CR-14 fixed the dummy's frozen preview to derive the same "~/git/<identity>/" shape D-02 requires, so no other content differs
match-strategy-select:git-preview:contains:"gitdir:~/git/":CTX-D-12:the includeIf preview's fragment path and gitdir condition embed the selected identity's name, which differs between the real fixture and the dummy fixture by construction; CR-14 fixed the dummy's frozen preview to derive the same "~/git/<identity>/" shape D-02 requires, so no other content differs

# ---------------------------------------------------------------------------
# CTX-D-01 — seeded author-name template. The real PTY suite's sandbox
# fixture (e2e/git_configuration_pty_e2e_test.go seedGitPTYIdentity, reused
# from ui_pty_e2e_test.go seedMinimalIdentity) seeds the author name as
# "<identity> User"; the dummy's frozen fixture
# (internal/dummytui/fixturebackend.go InitialState) seeds it as "<identity>
# identity". Different literal author-name text from two independently
# authored test fixtures, not a field/label/order divergence — the
# user.name/user.email field structure itself is identical (D-01 fragment
# contract).
# ---------------------------------------------------------------------------
git-form-filled:git-form-fields:absent:"User":CTX-D-01:the real PTY suite's seeded author name is "<identity> User" (seedGitPTYIdentity); the dummy's frozen fixture seeds "<identity> identity" (internal/dummytui/fixturebackend.go) — different fixture literal text, same field structure/order/labels
match-strategy-select:git-form-fields:absent:"User":CTX-D-01:the real PTY suite's seeded author name is "<identity> User" (seedGitPTYIdentity); the dummy's frozen fixture seeds "<identity> identity" (internal/dummytui/fixturebackend.go) — different fixture literal text, same field structure/order/labels

# ---------------------------------------------------------------------------
# CTX-D-12 — review-readonly ceremony preview: the real binary's full
# (non-compact) includeIf preview is rendered through
# gitconfig.RenderIncludeIf, which wraps the block in the production
# `# BEGIN gitid managed: <identity>` / `# END gitid managed: <identity>`
# sentinels — the EXACT bytes the confirmed write will persist (WYSIWYG,
# matching the SSHUI-03/T-03-HOSTBLOCK precedent: "the preview should BE the
# written text"). The dummy's frozen ceremony sample
# (internal/dummytui/data.go GitScreenIncludeIfGitdirLine) has no sentinels
# — it predates the production renderer. ux-improvement: the real binary is
# more accurate, not a regression.
# ---------------------------------------------------------------------------
review-readonly:git-ceremony:absent:"BEGIN gitid managed":CTX-D-12:the real ceremony preview renders the production sentinel-wrapped includeIf block (gitconfig.RenderIncludeIf); the dummy's frozen sample has no sentinels and predates the production renderer

# ---------------------------------------------------------------------------
# CTX-D-12 — result-success backup receipt completeness. The real binary's
# committed backup receipt lists EVERY backup actually taken during the
# transaction (nanosecond-suffixed ".bak." paths, e.g.
# ".gitconfig.d/<identity>.bak.<nanos>"), including the pre-existing fragment
# file's own backup, since the checkpoint edits an already-configured
# identity — 4 "Backed up →" lines, not 2. The dummy's ceremony echoes its
# STATIC declared backup list (gitCeremonyFor's Backups: ~/.gitconfig +
# ~/.ssh/allowed_signers only — internal/tuikit/identities.go, rendered with
# tuikit.NewBackupPath's ISO-timestamp ".backup." naming), which never names
# a fragment backup regardless of edit vs create mode. ux-improvement: the
# real receipt is more complete and traceable, not a regression — the two
# binaries' backup-path NAMING CONVENTIONS also differ (".bak." vs
# ".backup."), noted here for the record rather than silently absorbed.
# ---------------------------------------------------------------------------
result-success:git-ceremony:absent:".bak.":CTX-D-12:the real commit receipt lists every backup actually taken (4, including the pre-existing fragment file's own ".bak."-suffixed backup); the dummy's ceremony echoes its static 2-entry declared backup list (".backup."-suffixed), which never names a fragment backup
