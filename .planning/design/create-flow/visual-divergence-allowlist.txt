# Create-flow visual-divergence allowlist (DLV-04.1/D-24.1, plan 03-06 Task 2)
#
# Consumed by `make gate-visual-regression`
# (cmd/gitid/gate_visual_regression_test.go), which diffs the REAL binary's
# create-flow screen captures against the DUMMY's, screen by screen
# (internal/screenshot.CaptureCreateFlowScreens, internal/screenshot/createflow.go).
# A screen listed here is EXEMPT from the byte-exact comparison; any OTHER
# screen must render BYTE-IDENTICAL text between the two binaries or the
# gate fails, naming the offending screen and both captured texts.
#
# Format: one entry per line, "<screen-id>: <reason>". Blank lines and lines
# starting with # are ignored. <screen-id> must be one of
# internal/screenshot.CreateFlowScreenIDs.
#
# ---------------------------------------------------------------------------
# The three Phase-3-scoped design divergences (03-UI-SPEC.md "Scoped
# Divergences"), each already documented against its EXACT approved slot:
# ---------------------------------------------------------------------------

# D-02 — ReachableNotUploaded is a THIRD test-outcome state (yellow `!`
# warning, never red) the approved Phase-2 demo never had; the dummy's own
# fixture connectivity output and the real fake-ssh PATH-shim output are
# never byte-identical strings by construction (different identity name,
# different canned banner text) — the outcome GLYPH/WORD is what D-02
# actually gates, not the literal ssh output line.
test-stage1-direct: real vs. dummy connectivity fixture text differs by construction; D-02's yellow-not-red outcome contract is the thing under test, verified separately by the PTY e2e (plan 03-06 Task 1) and internal/tuikit's own render tests (03-05-SUMMARY)
test-stage2-by-alias: same as test-stage1-direct (D-02)

# D-16 — "Preview — demo data, not wired to your system yet" banner on
# every NOT-YET-WIRED view (git-screen, identity-manager, global-ssh,
# global-git, health, fixer). Does NOT apply to any screen in THIS gate's
# captured set: the create-flow wizard (Identities tab) is the ONE tab
# Phase 3 wires, so Backend.DemoBanner(TabIdentities) returns false for
# BOTH the real binary and the dummy (internal/tuikit/app.go's
# DemoBanner check) — no create-flow screen ever carries this banner.
# Recorded here for grep-completeness and as the blanket contract a LATER
# phase's own visual-regression gate (git-screen/identity-manager/etc.)
# must honor when it wires its own tab and removes its own banner.
# any-non-wired-tab: * (D-16 — out of this gate's create-flow-only scope; see 03-UI-SPEC.md "D-16")

# D-19 — the REAL binary's Git-identity step [ Continue ] carries its own
# scoped-divergence disabled reason ("— Git configuration arrives with the
# next build"), never the dummy's validity-gated one ("— needs user.name +
# a valid email").
git-form-demo: D-19 — real-binary Continue-disabled reason differs from the dummy's validity-gated one (both frozen strings, both asserted byte-exactly by gate-copy-freeze)

# ---------------------------------------------------------------------------
# A fourth, PRE-EXISTING divergence, carried forward (not new to this plan):
# realBackend.HostBlockPreview renders through the REAL
# sshconfig.RenderHostBlock (2-space indent + a trailing
# "# gitid: provider=<p>" marker comment), matching byte-for-byte what a
# confirmed write actually persists (SSHUI-03's "written exactly like this
# on confirm" contract) — the dummy's FixtureBackend.HostBlockPreview
# (internal/dummytui/fixturebackend.go) is a markerless 4-space literal
# that predates this contract and was never updated to match. First
# flagged by the 03-03 executor (03-03-SUMMARY "CARRIED INTO 03-06"),
# re-flagged by 03-04 (03-04-SUMMARY "Next Phase Readiness") — this entry
# is where that carried obligation is finally discharged. Every screen
# below renders the live Host-block preview (or a write-plan preview built
# from it) as part of its body.
ssh-form-filled: HostBlockPreview format (2-space + provider marker, real) vs. 4-space markerless (dummy) — carried from 03-03-SUMMARY
reuse-key-vs-generate: HostBlockPreview format (same as ssh-form-filled) + a brand-new D-10 picker render surface with no frozen dummy baseline yet (03-04-SUMMARY "Next Phase Readiness")
reuse-manual-path: same as reuse-key-vs-generate
mouse-focused-field: HostBlockPreview format (same as ssh-form-filled — same SSH-form layout, clicked instead of typed into)
confirm-write: HostBlockPreview format (same as ssh-form-filled — the review ceremony's preview block embeds the same Host-block text)
