# Global SSH visual-divergence allowlist (Phase 6, 06-07-PLAN.md Task 1)
#
# Consumed by cmd/gitid/gate_visual_regression_test.go's in-process capture
# gate (06-07 Task 1), which registers the Phase 6 Global SSH checkpoints
# alongside the existing create-flow / git-screen / identity-manager
# registries in the SHARED internal/screenshot RequiredScreenSpecs inventory.
# The REAL cmd/gitid backend renders from a seeded fixture home; the
# cmd/gitid-dummy FixtureBackend renders its frozen demo data. Every
# divergence between the two surfaces below is classified and documented —
# every other comparable region must be byte-identical.
#
# STRICT SCHEMA — each entry must be EXACTLY:
#   <T-06-NAME> <screen-id>:<region>:<predicate>:<decision>:<classification>:<reason>
#
# where:
#   screen-id       one of the Phase 6 Global SSH checkpoint IDs, or one of
#                   the four Phase 9.5 IDs (09.5-05-PLAN.md Task 1) added on
#                   top of them, registered in internal/screenshot's
#                   globalSSHSpecs():
#                   gss-options-list | gss-storage-current |
#                   gss-storage-other | gss-apply-preview |
#                   gss-apply-receipt | gss-storage-migrate-preview |
#                   gss-storage-migrate-receipt |
#                   gss-properties-list | gss-properties-filter |
#                   gss-properties-custom-form | gss-properties-custom-invalid
#   region          one of the shared registry region names (the Global SSH
#                   registrations add gss-options-browse, gss-storage-browse,
#                   gss-apply-ceremony, gss-apply-heading, gss-storage-ceremony)
#   predicate       "contains:<quoted-text>" | "absent:<quoted-text>" — the
#                   CR-04/CR-10 specific-needle grammar. A broad predicate
#                   that would match unrelated text is a defect in the gate.
#   decision        a scoped Phase 6 identifier: GSSH-D-NN (06-CONTEXT.md's
#                   D-NN decisions, disambiguated from 03-CONTEXT.md's bare
#                   D-NN namespace), STORE-NN (the storage-layout decisions),
#                   or DLV- (the shared DLV-04 real-vs-frozen-dummy
#                   comparison-class literal). Bare D-/T-, CTX-D-/UI-D-, and
#                   MGR-D- refs are NOT valid here (cross-registry leakage).
#   classification  improvement | defect — every entry states which it is.
#   reason          non-empty human-readable explanation.
#
# VALIDATION RULES (enforced by cmd/gitid/gate_visual_regression_test.go):
#   - TestGlobalSSHAllowlistMatchesRegistry proves a byte 1:1 correspondence
#     between these entries and the code dispositions in globalSSHSpecs()
#     (both directions): removing an entry, or widening a registry predicate,
#     fails the gate.
#   - TestGlobalSSHAllowlistFormat rejects any entry lacking a needle-form
#     predicate or a classification.
#   - TestGlobalSSHHTMLNonApplicabilityPerSpec proves the approved-HTML
#     surface is non-applicable on every Global SSH spec with the standing
#     UI-reference rule reason; the receipts additionally record their live /
#     approved-tui non-applicability naming the specific ui-frames/ PTY file
#     that carries the receipt evidence instead of a hollow in-process frame.
#
# Blank lines and lines starting with # are ignored.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# DLV-4 — fixture-vs-live comparison class (real backend reads a seeded
# fixture home; the dummy's FixtureBackend always renders its frozen
# 8-identity demo data). The real side is the more accurate render, so every
# entry below classifies as an improvement.
# ---------------------------------------------------------------------------

<T-06-OPTIONS-HEADER> gss-options-list:header-status:contains:"ids":DLV-4:improvement:header status shows the identity count, which differs (real's zero-identity seeded Global SSH fixture home vs the dummy's 8-identity IdentityManagerRows fixture set) — both sides carry the "ids" marker

<T-06-OPTIONS-LIST-ROWS> gss-options-list:sidebar:contains:"now:":DLV-4:improvement:on this surface RegionSidebar's left-of-│ extraction captures the OPTION LIST rows, not an identity sidebar — the real row set describes the live probe (D-01) while the dummy rows carry the frozen GlobalSSHOptions now-values; the list content is the fixture-vs-live divergence and "now:" survives on both sides

<T-06-OPTIONS-KEYBAR> gss-options-list:keybar:contains:"Esc":DLV-4:improvement:the Global SSH Options keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-PROVENANCE> gss-options-list:gss-options-browse:absent:"fixture value":DLV-4:improvement:the real Options body renders the live D-01/D-03 three-tier provenance labels (including baseline "not set (OpenSSH default: ...)" when unprovided), the D-13 OpenSSH-version line, and the D-11/D-12 three/four-state row states; the fixture renders its frozen GlobalSSHOptions and "fixture value — the demo does not probe this machine" provenance. Task 1's first-row-focus fix now surfaces StrictHostKeyChecking's Provenance in the detail pane by default; the real baseline provenance legitimately includes the "not set (OpenSSH default: ask)" catalog reference string. The divergence discriminator is the absence of "fixture value" marker on the real side — the whole master-detail body is the classified fixture-vs-live divergence (T-06-PROVENANCE, described in 06-01..06-03's summaries and 06-UI-SPEC's approved base states)

<T-06-STORAGE-CURRENT-HEADER> gss-storage-current:header-status:contains:"ids":DLV-4:improvement:header status identity count differs (zero-identity real fixture home vs the dummy's 8 fixtures) — both carry "ids"

<T-06-STORAGE-CURRENT-LEFT> gss-storage-current:sidebar:contains:"STORE-01":DLV-4:improvement:the Storage left pane's radio and current-layout markers render the backend's live layout (real fixture home is Include; the dummy fixture is Sentinel) — identical STORE-01 chrome, radio/current state differs by construction

<T-06-STORAGE-CURRENT-PREVIEW> gss-storage-current:gss-storage-browse:absent:"Host personal.github.com":DLV-4:improvement:the real resulting-config preview is PlanMigration's compose of the seeded machine bytes for the CURRENT layout (CR-05 fix: requesting a plan for the current layout is a legitimate no-op preview, not an error); the dummy renders its frozen personal/work IncludePreviewOwned/SentinelPreview fixtures, whose "Host personal.github.com" identity the real fixture-rendering never carries

<T-06-STORAGE-CURRENT-KEYBAR> gss-storage-current:keybar:contains:"Esc":DLV-4:improvement:the Global SSH Storage keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-STORAGE-OTHER-HEADER> gss-storage-other:header-status:contains:"ids":DLV-4:improvement:header status identity count differs (zero-identity real fixture home vs the dummy's 8 fixtures) — both carry "ids"

<T-06-STORAGE-OTHER-LEFT> gss-storage-other:sidebar:contains:"STORE-01":DLV-4:improvement:the Storage left pane's radio and current-layout markers render the backend's live layout (real fixture home is Include; the dummy fixture is Sentinel) — identical STORE-01 chrome, radio/current state differs by construction

<T-06-STORAGE-OTHER-PREVIEW> gss-storage-other:gss-storage-browse:absent:"Host personal.github.com":DLV-4:improvement:the real resulting-config preview is PlanMigration's compose of the seeded machine bytes (Include line, zero gitid-managed identities); the dummy renders its frozen personal/work IncludePreviewOwned/SentinelPreview fixtures, whose "Host personal.github.com" identity the real fixture-rendering never carries

<T-06-STORAGE-OTHER-KEYBAR> gss-storage-other:keybar:contains:"Esc":DLV-4:improvement:the Global SSH Storage keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-APPLY-KEYBAR> gss-apply-preview:keybar:contains:"Esc":DLV-4:improvement:the Global SSH apply keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-MIGRATE-KEYBAR> gss-storage-migrate-preview:keybar:contains:"Esc":DLV-4:improvement:the Global SSH migration keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-PROPERTIES-KEYBAR> gss-properties-list:keybar:contains:"Esc":DLV-4:improvement:the Global SSH properties keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-PROPERTIES-FILTER-KEYBAR> gss-properties-filter:keybar:contains:"Esc":DLV-4:improvement:the Global SSH properties keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-PROPERTIES-FORM-KEYBAR> gss-properties-custom-form:keybar:contains:"Esc":DLV-4:improvement:the Global SSH properties keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-PROPERTIES-INVALID-KEYBAR> gss-properties-custom-invalid:keybar:contains:"Esc":DLV-4:improvement:the Global SSH properties keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-DIFFERS-KEYBAR> gss-options-differs-row:keybar:contains:"Esc":DLV-4:improvement:the Global SSH differs-row keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-DIFFERS-HEADER> gss-options-differs-row:header-status:contains:"ids":DLV-4:improvement:the differs-row state shares the seeded real-vs-dummy identity-count header divergence

<T-06-DIFFERS-SIDEBAR> gss-options-differs-row:sidebar:contains:"now:":DLV-4:improvement:the differs-row state shares the fixture-vs-live option-list divergence and the now-value anchor

<T-06-DIFFERS-BODY> gss-options-differs-row:gss-options-browse:absent:"fixture value":DLV-4:improvement:the differs-row state shares the fixture-vs-live Options body divergence and provenance anchor

<T-06-STAGED-KEYBAR> gss-options-staged-apply-preview:keybar:contains:"Esc":DLV-4:improvement:the Global SSH staged-apply keybar differs because the real screen exposes the phase's editable-row affordance while the approved dummy retains its frozen detail affordance; both keybars retain the shared Esc navigation hint

<T-06-STAGED-HEADER> gss-options-staged-apply-preview:header-status:contains:"ids":DLV-4:improvement:the staged-apply state shares the seeded real-vs-dummy identity-count header divergence

<T-06-STAGED-CEREMONY> gss-options-staged-apply-preview:gss-apply-ceremony:contains:"override(s)":DLV-4:improvement:the staged-apply state differs in its real-versus-dummy summary text while both sides retain the override-count anchor

<T-06-STAGED-HEADING> gss-options-staged-apply-preview:gss-apply-heading:contains:"Write Host * managed block to ":GSSH-D-07:improvement:the staged-apply state shares the resolved-target ceremony heading divergence

# ---------------------------------------------------------------------------
# GSSH-D-06 / GSSH-D-07 — the apply ceremony's D-06 managed-block diff and
# D-07 resolved-target heading (T-06-GLOBALBLOCK and T-06-CEREMONYTARGET,
# the two plan-06-01 recorded divergences this gate must classify).
# ---------------------------------------------------------------------------

<T-06-GLOBALBLOCK> gss-apply-preview:gss-apply-ceremony:absent:"IgnoreUnknown UseKeychain":GSSH-D-06:improvement:the real apply-ceremony diff is the EnsureGlobals write target (06-01): 'IgnoreUnknown UseKeychain' as the guard line BEFORE 'Host *' (matching recipes/ssh-config.recipe), a two-space hostIndent body, and keys in globalssh.Policy declaration order — the dummy's empty GlobalSSHApplyPlan falls back to a flat '+ Key Recommended' list that never renders the block. The guard-line placement, the two-space (real) versus four-space (frozen managedHostStar fixture) indent, and the Policy-ordered key sequence are the three recorded aspects of this divergence; removing any one of those needles from this file fails the gate

<T-06-CEREMONYTARGET> gss-apply-preview:gss-apply-heading:contains:"Write Host * managed block to ":GSSH-D-07:improvement:the apply ceremony's heading names the RESOLVED storage target (D-07): the real side resolves ~/.ssh/config.d/gitid.config (the Include'd gitid-owned file, default layout), the dummy falls back to ~/.ssh/config — the shared frozen "Write Host * managed block to " prefix anchors both sides

<T-06-APPLY-HEADER> gss-apply-preview:header-status:contains:"ids":DLV-4:improvement:header status identity count differs (zero-identity real fixture home vs the dummy's 8 fixtures) — both carry "ids"

# ---------------------------------------------------------------------------
# STORE-03 — the storage-migration ceremony's plan diff.
# ---------------------------------------------------------------------------

<T-06-STORAGE-MIGRATE-HEADER> gss-storage-migrate-preview:header-status:contains:"ids":DLV-4:improvement:header status identity count differs (zero-identity real fixture home vs the dummy's 8 fixtures) — both carry "ids"

<T-06-STORAGE-MIGRATE-DIFF> gss-storage-migrate-preview:gss-storage-ceremony:contains:"Migrate SSH storage layout":STORE-03:improvement:the real storage-migration ceremony diff is PlanMigration's actual STORE-03 plan for the seeded machine; the dummy renders its frozen fixture diff — the shared "Migrate SSH storage layout" heading anchors the authorized divergence

# ---------------------------------------------------------------------------
# Phase 9.5 (09.5-05-PLAN.md Task 1) — the "All directives" browser (PROP-01)
# and the custom-directive entry flow (PROP-04) standing visual-regression
# guard. Same DLV-4 fixture-vs-live comparison class as every entry above:
# the real body renders globalssh.AllDirectives' live ssh -G resolution for
# the seeded fixture home, the dummy renders FixtureBackend's frozen 22-row
# AllSSHDirectives() slice. On the UNFILTERED list (gss-properties-list) the
# shared anchor is "more options)" (the gitCueLine overflow cue): landing
# with no navigation selects row 0, the alphabetically-first key, which is a
# DIFFERENT literal key on each side, so "stricthostkeychecking" is not
# reliably visible there — confirmed empirically during authoring (see
# 09.5-05-SUMMARY.md Deviations). On the FILTERED list
# (gss-properties-filter) the filter text "strict" narrows both sides to
# exactly the stricthostkeychecking row, so THAT screen correctly anchors on
# "stricthostkeychecking" directly. The custom-directive form/validate states
# (gss-properties-custom-form, gss-properties-custom-invalid) are driven by
# the SAME typed script on both surfaces and carry NO body disposition —
# only the standard header-status identity-count divergence.
# ---------------------------------------------------------------------------

<T-06-PROPERTIES-HEADER> gss-properties-list:header-status:contains:"ids":DLV-4:improvement:header status identity count differs (zero-identity real fixture home vs the dummy's 8 fixtures) — both carry "ids"

<T-06-PROPERTIES-BROWSE> gss-properties-list:gss-properties-browse:contains:"more options)":DLV-4:improvement:the real "All directives" body renders globalssh.AllDirectives' live ssh -G resolution for the seeded fixture home (the full ~90-directive set); the dummy renders FixtureBackend's frozen 22-row AllSSHDirectives() slice — the directive SET (and consequently which key lands in the row-0 default selection) is the classified fixture-vs-live divergence; "more options)" (the shared gitCueLine overflow cue, present because both the real ~90-item and dummy 22-item lists exceed the visible ~20-row window) survives on both sides

<T-06-PROPERTIES-LIST-ROWS> gss-properties-list:sidebar:contains:"more options)":DLV-4:improvement:on this surface RegionSidebar's left-of-│ extraction captures the master DIRECTIVE LIST rows, not an identity sidebar — the real row set is globalssh.AllDirectives' live ssh -G resolution while the dummy rows carry the frozen 22-row AllSSHDirectives() fixture; the list content (and default row-0 selection) is the same classified fixture-vs-live divergence, and "more options)" (the shared gitCueLine overflow cue) survives on both sides

<T-06-PROPERTIES-FILTER-HEADER> gss-properties-filter:header-status:contains:"ids":DLV-4:improvement:header status identity count differs (zero-identity real fixture home vs the dummy's 8 fixtures) — both carry "ids"

<T-06-PROPERTIES-FILTER-BROWSE> gss-properties-filter:gss-properties-browse:contains:"stricthostkeychecking":DLV-4:improvement:the SAME fixture-vs-live directive-set divergence as the unfiltered "All directives" list, narrowed by the filter text "strict" — both sides resolve to exactly the stricthostkeychecking row (the anchor), but the live-vs-fixture match-count denominator (the real ~90-directive set vs the dummy's 22-row fixture) still differs

<T-06-PROPERTIES-FILTER-ROWS> gss-properties-filter:sidebar:contains:"stricthostkeychecking":DLV-4:improvement:the SAME sidebar-region divergence as T-06-PROPERTIES-LIST-ROWS, narrowed by the filter text "strict" — both sides resolve to exactly the stricthostkeychecking row (the anchor) in the master-list column left of the divider

<T-06-PROPERTIES-CUSTOM-FORM-HEADER> gss-properties-custom-form:header-status:contains:"ids":DLV-4:improvement:header status identity count differs (zero-identity real fixture home vs the dummy's 8 fixtures) — both carry "ids"

<T-06-PROPERTIES-CUSTOM-FORM-BODY> gss-properties-custom-form:gss-custom-directive:contains:"Directive":DLV-4:improvement:the custom-directive form carries the real and dummy implementations' distinct field details while sharing the Directive label anchor

<T-06-PROPERTIES-CUSTOM-INVALID-HEADER> gss-properties-custom-invalid:header-status:contains:"ids":DLV-4:improvement:header status identity count differs (zero-identity real fixture home vs the dummy's 8 fixtures) — both carry "ids"

<T-06-PROPERTIES-CUSTOM-INVALID-BODY> gss-properties-custom-invalid:gss-custom-directive:contains:"Directive":DLV-4:improvement:the custom-directive invalid state carries the real and dummy implementations' distinct validation details while sharing the Directive label anchor
