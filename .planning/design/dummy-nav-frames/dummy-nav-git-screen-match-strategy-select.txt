gitid  git-screen/match-strategy-select
Identities
> ✓ complete  personal — SSH Host block and Git fragment both present.
  ! incomplete  work — SSH Host block present; no Git identity configured for th
  ! git-only  opensource — Git identity relies on the global SSH config; no own
  ! Match strategy
  ✓ gitdir: ✓ default — applies by repository directory path.
  ✓ hasconfig:remote.*.url — applies by matching remote URL, combinable with git
  ✗ both — applies both includeIf blocks together.
  ✗
    Live includeIf preview (gitdir):
Prev[includeIf "gitdir:~/personal/"]
  SS    path = ~/.gitconfig.d/personal
  Ke
  Gihasconfig alternative:
    [includeIf "hasconfig:remote.*.url:git@personal.github.com:*/**"]
[SIG    path = ~/.gitconfig.d/personal

r re[SIG-MATCH-STRATEGY-SELECT-DEFAULT-GITDIR]




