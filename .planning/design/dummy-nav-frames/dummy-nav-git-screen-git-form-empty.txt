gitid  git-screen/git-form-empty  8 identities · ! needs action
Identities
> ✓ complete  personal — SSH Host block and Git fragment both present.
  ! incomplete  work — SSH Host block present; no Git identity configured for th
  ! git-only  opensource — Git identity relies on the global SSH config; no own
  ! key-unused  archived — Key file exists on disk but no identity references it
  ✓ key-used-ssh-only  staging — Key referenced by a Host block; not wired for G
  ✓ Git identity (per-identity)
  ✗ user.name:         (empty)
  ✗ user.email:        (empty — must byte-match allowed_signers later, GITUI-04)
    gpg.format:        ssh (fixed)
Prevuser.signingkey:   (empty — a PATH to the public key, never the key material
  SScommit.gpgsign:    true (default)
  Ke
  GiLive fragment preview: (fill in the fields to see the resulting fragment)

[SIG[SIG-GIT-FORM-EMPTY]

f git-form-filled  Esc back  q quit  ? help




