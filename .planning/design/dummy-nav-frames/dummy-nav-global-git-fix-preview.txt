gitid  global-git/fix-preview
Preview fix
! Applying 10 of 10 baseline options to the managed block in ~/.gitconfig.
! user.email is intentionally absent -- gitid never writes a global [user] secti

Diff -- managed block in ~/.gitconfig:
+ defaultBranch=main  ignorecase=false  autocrlf=input  eol=lf
+ autoSetupRemote=true  rebase=true  prune=true
+ color: ui=auto branch=auto diff=auto status=auto
+ conflictstyle=diff3  colorMoved=zebra
+ alias (8): st,co,br,ci,df,lg,unstage,last

gitid only owns the block between its sentinels -- everything else in ~/.gitconf

[SIG-GGIT-FIX-PREVIEW-BASELINE-APPLY]

w confirm-write  1-5 switch view  Esc back  q quit  ? help






