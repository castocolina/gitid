gitid  git-screen/git-form-filled
Identities
> ✓ Git identity (per-identity)                      ent both present.
  ! user.name:         Personal Identity              identity configured for th
  ! user.email:        you@personal.example           global SSH config; no own
  ! gpg.format:        ssh (fixed)                   t no identity references it
  ✓ user.signingkey:   ~/.ssh/id_ed25519_personal.pubHost block; not wired for G
  ✓ commit.gpgsign:    true                          uth and Git commit signing.
  ✗                                                  y file that is absent from
  ✗ Live fragment preview:                           at a Git fragment file that
    [user]
Prev    name = Personal Identity
  SS    email = you@personal.example
  Ke    signingkey = ~/.ssh/id_ed25519_personal.pub
  Gi
    [gpg]
[SIG    format = ssh

m ma[commit]
        gpgsign = true

    [SIG-GIT-FORM-FILLED]

