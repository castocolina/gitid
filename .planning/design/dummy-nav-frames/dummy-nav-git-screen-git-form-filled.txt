gitid  git-screen/git-form-filled  8 identities · ! needs action
Identities
> ✓ Git identity (per-identity)
  ! user.name:         Personal Identity
  ! user.email:        you@personal.example
  ! gpg.format:        ssh (fixed)
  ✓ user.signingkey:   ~/.ssh/id_ed25519_personal.pub (a PATH to the public key
  ✓ commit.gpgsign:    true
  ✗
  ✗ Live fragment preview:
    [user]
Prev    name = Personal Identity
  SS    email = you@personal.example
  Ke    signingkey = ~/.ssh/id_ed25519_personal.pub
  Gi
    [gpg]
[SIG    format = ssh

m ma[commit]
        gpgsign = true

    [SIG-GIT-FORM-FILLED]

