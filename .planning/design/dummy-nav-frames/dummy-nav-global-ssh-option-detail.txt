gitid  global-ssh/option-detail
IdentitiesOnly
current: not set globally (set per-Host by gitid)   risk: High   recommended: ye

When IdentitiesOnly is not set (or set to "no"), ssh may try EVERY key it can fi

  - the wrong key can be offered first, revealing to a server which OTHER keys y
  - a host you don't fully trust can trigger authentication attempts meant for a

Setting "IdentitiesOnly yes" on a Host block restricts ssh to ONLY the IdentityF

! Recommended, not required -- you can leave any option unchanged. This is advis

[SIG-GSSH-OPTION-DETAIL-IDENTITIESONLY]

f fix-preview  1-5 switch view  Esc back  q quit  ? help







