gitid  create-flow/test-fail
Identity Manager — placeholder (replaced in Wave 4 by the real identity-manager

Esc back  q quit  ? help




    4. Test connectivity — failed
    $ ssh -T -F /tmp/gitid-test-a1b2c3.config -p 443 -i ~/.ssh/id_ed25519_person
    ✗ git@ssh.github.com: Permission denied (publickey).

    The key was not accepted. Nothing was written — only the throwaway temp
    config was exercised. Go back and check the key, alias, or algorithm.

    [SIG-TEST-FAIL]







