gitid  identity-manager/confirm-destructive  8 identities · ! needs action
Identities
> ✓ complete  personal — SSH Host block and Git fragment both present.
  ! incomplete  work — SSH Host block present; no Git identity configured for th
  ! git-only  opensource — Git identity relies on the global SSH config; no own
  ! key-unused  archived — Key file exists on disk but no identity references it
  ✓ ┌──────────────────────────────────────────────────────────────────────┐or G
  ✓ │ Confirm: Delete everything (SSH + Git + key)                         │ing.
  ✗ │ ✗ This action is irreversible.                                       │rom
  ✗ │ Removes for personal:                                                │that
    │   - SSH Host block (personal.github.com)                             │
    │   - Git configuration (~/.gitconfig.d/personal)                      │
    │   - Key file (~/.ssh/id_ed25519_personal)                            │
    │                                                                      │
    │ Default-focused: No, cancel. Type the identity name to confirm —     │
    │ destructive actions                                                  │
    │ never default to yes (§5).                                           │
    │                                                                      │
    │ [SIG-IM-CONFIRM-DESTRUCTIVE-STRONGEST-CONFIRM]                       │
    └──────────────────────────────────────────────────────────────────────┘



