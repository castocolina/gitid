gitid  identity-manager/list-empty  8 identities · ! needs action
Identities
No identities yet.

gitid manages ~/.ssh/config and ~/.gitconfig per identity — nothing has been
configured on this machine yet.

Press n to create your first identity (SSH connection + Git configuration, end t

[SIG-IM-LIST-EMPTY-FIRST-RUN]

n Create Identity  g Git Configuration  1-5 switch view  Esc back  q quit  ? hel











