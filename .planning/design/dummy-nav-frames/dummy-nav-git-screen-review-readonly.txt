gitid  git-screen/review-readonly
Identity Manager — placeholder (replaced in Wave 4 by the real identity-manager

w coReview (read-only)
    Fragment (~/.gitconfig.d/personal):
      user.name=Personal Identity   user.email=you@personal.example
      gpg.format=ssh   commit.gpgsign=true
      user.signingkey=~/.ssh/id_ed25519_personal.pub

    includeIf (gitdir, default):
    [includeIf "gitdir:~/personal/"]
        path = ~/.gitconfig.d/personal

    ~/.ssh/allowed_signers:
    you@personal.example
      ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDesignMockupFixtureKeyNotReal0
    user.email:        you@personal.example
    ✓ Byte-identical — trusted for this identity's signatures.

    [SIG-REVIEW-READONLY-ALLOWED-SIGNERS]



