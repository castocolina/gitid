gitid  identity-manager/detail-ssh-first
Identity detail — work
1. SSH (shown first)
  Host: work.github.com
  IdentityFile: ~/.ssh/id_ed25519_work
  IdentitiesOnly: yes

2. Git
! No Git identity configured for this alias — SSH-only.
  gitid never renders fabricated Git attributes here (MGR-03/MGR-07).

Per-identity health (MGR-07): ! incomplete
  SSH Host block present; no Git identity configured for this alias.

[SIG-IM-DETAIL-SSH-FIRST-NO-GIT-ATTRS]

a action-menu  n Create Identity  g Git Configuration  1-5 switch view  Esc back






