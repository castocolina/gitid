gitid  create-flow/backup-notice  8 identities · ! needs action
Identities
> ✓ complete  personal — SSH Host block and Git fragment both present.
  ! incomplete  work — SSH Host block present; no Git identity configured for th
  ! git-only  opensource — Git identity relies on the global SSH config; no own
  ! key-unused  archived — Key file exists on disk but no identity references it
  ✓ key-used-ssh-only  staging — Key referenced by a Host block; not wired for G
  ✓ key-used-both  clientA — Key wired for both SSH auth and Git commit signing.
  ✗ 6. Backup created
  ✗ ✓ ~/.ssh/config.backup.2026-07-03T03-59-12Z

PrevA full copy of your previous config was saved before any change was applied
  SSthis backup path is the undo story.
  Ke
  Gi[SIG-BACKUP-NOTICE]

[SIG-IM-LIST-POPULATED-8-LABEL]

z result-success  Esc back  q quit  ? help




