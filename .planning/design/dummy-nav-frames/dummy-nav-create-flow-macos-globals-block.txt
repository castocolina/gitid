gitid  create-flow/macos-globals-block
Identities
> ✓ complete  personal — SSH Host block and Git fragment both present.
  ! incomplete  work — SSH Host block present; no Git identity configured for th
  ! git-only  opensource — Git identity relies on the global SSH config; no own
  ! key-unused  archived — Key file exists on disk but no identity references it
  ✓ macOS Keychain globals
  ✓ A one-time global Host * block stores key passphrases in the macOS Keychain
  ✗ auto-adds new keys to the agent. IgnoreUnknown UseKeychain makes this a docu
  ✗ no-op on Linux.

PrevIgnoreUnknown UseKeychain
  SS
  KeHost *
  Gi    UseKeychain yes
        AddKeysToAgent yes
[SIG
    [SIG-MACOS-GLOBALS-BLOCK]
Esc back  q quit  ? help




