gitid  identity-manager/clone-name-prompt  8 identities · ! needs action
Identities
> ✓ complete  personal — SSH Host block and Git fragment both present.
  ! incomplete  work — SSH Host block present; no Git identity configured for th
  ! git-only  opensource — Git identity relies on the global SSH config; no own
  ! key-unused  archived — Key file exists on disk but no identity references it
  ✓ key-used-ssh-only  staging — Key referenced by a Host block; not wired for G
  ✓ ┌──────────────────────────────────────────────────────────────────────┐ing.
  ✗ │ Clone identity                                                       │rom
  ✗ │ Source identity: personal                                            │that
    │ New identity name: personal-clone (must differ from the source name  │
    │ — MGR-04)                                                            │
    │                                                                      │
    │ Cloning copies the SSH Host block and Git fragment shape under the   │
    │ new name — the key                                                   │
    │ material itself is not copied; a new key is generated for the clone. │
    │                                                                      │
    │ [SIG-IM-CLONE-NAME-PROMPT-DISTINCT-NAME]                             │
    └──────────────────────────────────────────────────────────────────────┘




