gitid  health/per-identity-health
Per-identity health -- legacy (fragment-path-missing)
Health only diagnoses -- nothing here writes to your files. Open the Fixer (key

SSH
✓ Host block present (legacy.github.com), IdentityFile present, IdentitiesOnly y

Git
✗ error  includeIf targets a missing fragment
[includeIf "gitdir:~/legacy/"] in ~/.gitconfig points at ~/.gitconfig.d/legacy,

This slice feeds the Identity Manager row for legacy (MGR-07): includeIf points

[SIG-HLTH-PER-IDENTITY-LEGACY-FRAGMENT-MISSING]

1-5 switch view  Esc back  q quit  ? help







