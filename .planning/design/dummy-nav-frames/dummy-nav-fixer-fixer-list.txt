gitid  fixer/fixer-list
Fixer
Every fix is previewed, confirmed, and backed up before anything is written -- n

SSH
✗ critical  Private key is world-readable  -- chmod 0600 ~/.ssh/id_ed25519_archi
✗ error  IdentitiesOnly no contradicts an explicit IdentityFile  -- Set Identiti
! warning  Duplicate Host * stanza  -- Merge the two Host * stanzas into one --

Git
✗ error  includeIf targets a missing fragment  -- Restore ~/.gitconfig.d/legacy,

v preview fix (IdentitiesOnly no contradicts an explicit IdentityFile)   e nothi
Apply all 4 fixes -- each one still previews its own diff and backup path before

[SIG-FIX-LIST-SSH-GIT-SPLIT]

e nothing-to-fix  v fix-preview  1-5 switch view  Esc back  q quit  ? help





