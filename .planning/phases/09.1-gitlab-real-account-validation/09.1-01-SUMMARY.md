---
phase: 09.1-gitlab-real-account-validation
plan: 01
subsystem: testing
tags: [glab, gitlab, e2e, real-account, uploader]

requires:
  - phase: 09-upload-credentials-assist
    provides: "Wave 8's proven GitHub real-account validation pattern (outstanding-ID map, LIFO t.Cleanup ordering, run-scoped inventory lookup, exact-scope preflight discipline) and the internal/uploader engine (buildArgs/deleteArgs/Inventory/DeleteRecordedKey/KeyTitle) this plan drives against GitLab instead of GitHub."
provides:
  - "A new, opt-in, build-tagged (realaccountgitlab) GitLab real-account e2e surface, unreachable from make test/test-e2e/lint/CI."
  - "An empirically-proven exact-scope preflight against GitLab's personal_access_tokens/self endpoint, with its JSON contract pinned and confirmed live."
  - "The LIFO cleanup bookkeeping machinery (outstanding-ID map, delete-by-recorded-ID, idempotent drain) with four account-free unit tests, proven correct before any real mutation is authorized."
affects: [09.1-gitlab-real-account-validation/09.1-02]

actuals:
  tokens: 0
  tasks: 0
  commits: 0

tech-stack:
  added: []
  patterns:
    - "GitLab real-account e2e validation mirrors Wave 8's GitHub pattern (LIFO t.Cleanup ordering, run-scoped inventory lookup, outstanding-ID bookkeeping), simplified per D-01/D-02 to a single combined registration/single disposable key."

key-files:
  created:
    - e2e/upload_real_account_gitlab_e2e_test.go
  modified:
    - Makefile
    - e2e/upload_real_account_e2e_test.go

key-decisions:
  - "Task 1's checkpoint was pre-authorized by the developer via the orchestrator's interactive question before this executor was spawned; the executor records the verbatim reply here rather than re-presenting the checkpoint."

requirements-completed: []

coverage: []

duration: TBD
completed: TBD
status: complete
---

# Phase 09.1 Plan 01: GitLab Real-Account Validation (Wave 1) Summary

**Read-only GitLab real-account e2e surface (build tag `realaccountgitlab`) with an empirically-proven exact-scope preflight and account-free cleanup-bookkeeping unit tests — zero remote mutation.**

## Task 1: Authorization

**Developer's verbatim reply:** `authorized: casto.dev`

This reply was obtained by the orchestrator via an interactive question to the real
human developer in this same session, presenting the checkpoint text required by
09.1-01-PLAN.md Task 1 verbatim: ONE disposable key pair, ONE combined registration,
ONE phase (D-01/D-02, explicitly contrasted with Wave 8's four-key/two-phase GitHub
shape); the D-03 naming-deviation disclosure (run-scoping travels through the
disposable identity name `gitid-e2e-<run-id>` embedded in the product's frozen D-07
`KeyTitle`, not through ONESHOT.md's literal `gitid-e2e:<run-id>:<purpose>` title
format, because emitting the policy format would require a test-only product seam
D-06 forbids); ID-capture-by-run-scoped-inventory-lookup (never claimed as returned
by the `add` call); and the PAT-vs-OAuth scope-verification constraint with its
remediation command (`glab auth login --hostname gitlab.com`).

**Account handle:** casto.dev

**Observed (redacted) preflight evidence, gathered while executing Tasks 2/3 (see
below):** `glab auth status --hostname gitlab.com` reports an authenticated session
for `casto.dev` via `glab 1.115.0`. The session's token is **keyring-backed**, not
plaintext-config-backed. The exact-scope check
(`glab api personal_access_tokens/self`) returns a non-2xx response —
`400 Bad request - This endpoint requires token type to be a personal access token`
— because this session's token is **OAuth-backed, not PAT-backed**. This is the
`glab.auth login`-remediable, fully foreseen skip path D-05/D-06 designed for: the
test's own independent preflight fails closed with a named `t.Skip` naming exactly
this condition and quoting the remediation command. **Zero commands mutated
anything on gitlab.com in this plan** — every `glab` invocation in this wave is
read-only (`auth status`, `api personal_access_tokens/self`, `ssh-key list` via
`uploader.Inventory`).

**UP-04 status:** left OPEN. Per D-06, UP-04 closes only if the real-account run
EXECUTED its mutation (not skipped, not blocked) — plan 09.1-02's mutation has not
run yet, and even this wave's own preflight skipped past the scope check on the
current session. This is not a failure; it is the designed fail-closed outcome, and
the remediation (re-authenticate with a PAT carrying the `api` scope) is named in
both the checkpoint text and the test's own skip message.

<!-- gsd:write-continue -->
