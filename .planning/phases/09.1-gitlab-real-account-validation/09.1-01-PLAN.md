---
phase: 09.1-gitlab-real-account-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/upload_real_account_gitlab_e2e_test.go
  - Makefile
autonomous: false
requirements: [UP-04]
user_setup:
  - service: gitlab
    why: "ONESHOT.md's Phase 9 External Account Policy says verbatim that GitLab and any external-account action outside the GitHub protocol still require explicit confirmation. This plan makes read-only contact with the developer's real GitLab account (auth status, token self-inspection, SSH-key inventory listing); wave 2 performs the single mutation. Both need the developer's authorization, given once at Task 1."
    env_vars: []
    dashboard_config:
      - task: "Confirm `glab auth status --hostname gitlab.com` reports an authenticated session for the account you intend to validate"
        location: "local shell"
      - task: "Confirm that session is PAT-backed and the token carries the `api` scope, so the run's exact-scope preflight can verify write authorization instead of skipping. If it is OAuth-backed, re-authenticate with a PAT: `glab auth login --hostname gitlab.com` and choose token authentication."
        location: "local shell + GitLab -> User Settings -> Access Tokens"

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single blocking human checkpoint authorizes ALL real-GitLab contact for phase 9.1 — the read-only preflight this plan performs and the single disposable-key mutation wave 2 performs — naming the exact GitLab account handle, before any command reaches the account (D-05)."
    - "The checkpoint describes the protocol GitLab actually gets: ONE disposable key pair, ONE combined auth_and_signing registration, ONE phase — not Wave 8's four-key/two-phase GitHub shape (D-01, D-02)."
    - "The checkpoint spells out the D-03 naming deviation: run-scoping travels through the disposable IDENTITY NAME (gitid-e2e-<run-id>) embedded in the product's frozen D-07 title, not through ONESHOT.md's literal gitid-e2e:<run-id>:<purpose> title format, because emitting the policy format would need a test-only product seam D-06 forbids."
    - "e2e/upload_real_account_gitlab_e2e_test.go exists behind its own realaccountgitlab build tag, is compiled by no routine gate, and is reachable only through the dedicated make verify-upload-real-account-gitlab target (D-04)."
    - "The Makefile's lint-tagged guard knows the new tag: realaccountgitlab is in KNOWN_BUILD_TAGS and has its own go vet line, both shipped in the SAME commit as the new test file, so make lint never hard-fails on an ungated tag."
    - "The exact-scope preflight is a real, empirically-proven mechanism against the developer's live glab session — not a mechanism assumed by analogy to gh auth status, which prints no scope information at all. The observed response shape is recorded as evidence."
    - "The preflight fails CLOSED: an absent glab, an unauthenticated session, an unreadable token-scope response, or a missing exact scope member each produce a named t.Skip with its remediation command — never a silent proceed and never a red build."
    - "No token ever reaches a log line, a test message, or the SUMMARY: every logged glab output passes through uploader.RedactCLIOutput first, and glab's token-revealing auth-status flag is never invoked anywhere in the file."
    - "The LIFO cleanup bookkeeping (outstanding-ID map, delete-by-recorded-ID, idempotent drain) exists and is proven correct by account-free unit tests BEFORE any code path can create a real remote key."
  artifacts:
    - "e2e/upload_real_account_gitlab_e2e_test.go (build tag realaccountgitlab; read-only end-to-end tracer: config-dir resolution, HOME bridge, exact-scope preflight, run-scoping string, baseline inventory read, read-only final sweep, cleanup bookkeeping machinery, four account-free unit tests)"
    - "Makefile (KNOWN_BUILD_TAGS entry, go vet -tags realaccountgitlab line, .PHONY entry, verify-upload-real-account-gitlab target)"
  key_links:
    - "the human checkpoint's `authorized: <handle>` reply -> the test's own independent exact-scope preflight (named command + named exact-member rule) -> the redacted evidence line recorded for the SUMMARY"
    - "the new //go:build realaccountgitlab tag -> KNOWN_BUILD_TAGS -> the go vet -tags realaccountgitlab line in lint-tagged -> make lint staying green"
    - "GLAB_CONFIG_DIR resolved from the AMBIENT pre-swap HOME -> the HOME-bridge wrapper script -> every child process's env -> the real authenticated glab session surviving the sandbox HOME swap"
    - "the outstanding-ID map + LIFO t.Cleanup registration order -> the account-free ordering/idempotency unit tests -> wave 2's mutation being safe to run at all"
---

<objective>
Authorize phase 9.1's real-GitLab contact once, then land the complete READ-ONLY
vertical slice of the GitLab real-account validation: the new build-tagged test file,
its full Makefile wiring, the exact-scope preflight proven against the developer's
live `glab` session, and the cleanup bookkeeping machinery with its account-free
unit tests.

Purpose: every GitLab upload/delete assertion in the repository today runs against
offline `glab` PATH shims (`e2e/harness_test.go`'s `FakeGLabDir`). Nothing has ever
reached a real `glab` session. Before creating a real remote key we must first prove,
end-to-end and without mutating anything, that (a) the new build tag and Makefile
target actually work and cannot leak into a routine gate, (b) the sandbox HOME swap
does not hide the developer's real authenticated `glab` credentials from a child
process, and (c) an EXACT scope check is even possible for GitLab — the one place
Wave 8's GitHub pattern provably does not port, because `glab auth status` prints no
scope information whatsoever.

Output: a committed, opt-in, read-only end-to-end slice that reaches the real GitLab
account and reports exactly what it found, plus the reviewed cleanup machinery wave 2
depends on.

**Safety posture, stated once and binding for this plan and the next.** Phase 9.1 is
the only work in this milestone that touches a real external GitLab account. Both its
plans are `autonomous: false`. ONESHOT.md's Phase 9 External Account Policy is
explicitly GitHub-scoped and states verbatim that "Existing keys, GitLab, and any
external-account action outside this protocol still require explicit confirmation" —
so even this plan's read-only contact needs the developer's consent, which Task 1
obtains.

**Relationship to the hermetic e2e boundary.** Every other test in `e2e/` is
hermetically prevented from resolving a real `gh`/`glab`. This file is the second
deliberate exception (after Wave 8's GitHub file), and it is exempt BY BUILD TAG, not
by a filename allowlist: `TestEveryE2EChildEnvIsHermetic` evaluates the constraint
against the `e2e` tag only, so a `realaccountgitlab`-tagged file is out of its scope
by construction and the hermetic guard needs zero changes. State that inversion in
the file's doc comment so a future reader does not "fix" it into hermetic conformance
and silently destroy the only real-GitLab validation in the project.

---

## Deviations from 09.1-PATTERNS.md recorded up front

**Identifier spelling, not mechanism.** PATTERNS.md says to copy Wave 8's bookkeeping
block "verbatim". Copied literally, `e2e/upload_real_account_e2e_test.go` and the new
file would define the same package-level symbols in the same Go package, so the two
tags could never be enabled together and a future combined-tag vet would break. This
plan therefore requires every top-level identifier in the new file to carry a
`GLab`/`GitLab` marker. The MECHANISM — the outstanding-ID map as single source of
truth, the LIFO `t.Cleanup` registration order, delete-by-recorded-ID after scope
re-confirmation, the read-only final sweep — is reproduced exactly, which is what
CONTEXT.md D-05 makes non-negotiable. Spelling is not part of the safety protocol.

**Scope-check transport.** The planning context suggests `glab api
personal_access_tokens/self --jq .scopes`. This plan ships `glab api
personal_access_tokens/self` and parses the `scopes` field in Go with
`encoding/json` instead. Two reasons: it does not assume `glab api` supports a jq
flag (unverified this session), and it puts the exact-member decision in a pure Go
function that the account-free table test in Task 3 can exercise — mirroring exactly
how Wave 8 split `parseGitHubScopeLine` from `missingRequiredScopes`. This adds
`encoding/json` to the import list PATTERNS.md predicted would be unchanged. Task 2
confirms the endpoint's real shape live before the parser is finalized.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ONESHOT.md
@.planning/phases/09.1-gitlab-real-account-validation/09.1-CONTEXT.md
@.planning/phases/09.1-gitlab-real-account-validation/09.1-RESEARCH.md
@.planning/phases/09.1-gitlab-real-account-validation/09.1-PATTERNS.md
@.planning/phases/09-upload-credentials-assist/09-08-PLAN.md
@.planning/phases/09-upload-credentials-assist/09-08-SUMMARY.md
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: authorize phase 9.1's real-GitLab contact</name>
  <read_first>
    - `.planning/ONESHOT.md` lines 127-141 — the Phase 9 External Account Policy in full. Note its final scope sentence: the policy is GitHub-scoped and says verbatim that "Existing keys, GitLab, and any external-account action outside this protocol still require explicit confirmation." It does NOT generalize to GitLab on its own.
    - `.planning/ONESHOT.md` lines 73-76 — rule 9: stop for a required confirmation before a real external-account mutation.
    - `.planning/phases/09.1-gitlab-real-account-validation/09.1-CONTEXT.md` `<decisions>` — D-01 through D-06 in full. D-05 is the safety posture this checkpoint implements; D-02 is why this run creates ONE key, not Wave 8's four; D-03 is the naming deviation this checkpoint must put in front of the developer rather than bury.
    - `.planning/phases/09-upload-credentials-assist/09-08-PLAN.md` Task 1 (lines 168-266) — the exact GitHub checkpoint this one mirrors: its `<instructions>` shape, its "please read this part before you approve" deviation disclosure, and its `<resume-signal>` contract.
    - `CLAUDE.md` "Working method: hypothesis -> test -> implementation" and "Engineering" — the confirmation discipline for anything that mutates the user's real state, and the rule that implementation follows only after the hypothesis is confirmed.
  </read_first>
  <action>Obtain the developer's explicit, account-named authorization for ALL real-GitLab contact in phase 9.1 — this plan's read-only preflight and inventory reads, and plan 09.1-02's single disposable-key mutation — per D-05. Present the protocol exactly as it will run (D-01/D-02: one key, one combined registration, one phase) and disclose the D-03 naming deviation explicitly. Record the developer's verbatim reply in `09.1-01-SUMMARY.md`. If the reply is `blocked`, no `glab` command touching gitlab.com runs anywhere in this phase, Tasks 2 and 3 still ship their code with the test skipping on its own unmet precondition, and UP-04 stays OPEN with the blocker named (D-06).</action>
  <instructions>
  **What this checkpoint authorizes.** Phase 9.1 proves gitid's GitLab SSH-key
  upload/delete path against a real, authenticated GitLab account. Today that path is
  proven only against offline fake `glab` PATH shims — no automated test has ever
  reached a real `glab` session. This is the GitLab counterpart of the GitHub
  validation you authorized in Phase 9 Wave 8, and it needs its own authorization:
  ONESHOT.md's existing policy is GitHub-scoped and says in its own words that GitLab
  still requires explicit confirmation.

  **It is deliberately smaller than the GitHub run.** Wave 8 created FOUR disposable
  keys in TWO phases, because GitHub has two independently titled registration
  endpoints (`user/keys` and `user/ssh_signing_keys`) and the policy's literal naming
  clause needed its own separately-keyed proof. GitLab has exactly ONE registration
  endpoint and ONE registration type — `glab ssh-key add <pub> -t <title>
  --usage-type auth_and_signing`. There is nothing for a second phase to separately
  prove. So this run creates **ONE** disposable ed25519 key pair, registers it
  **once**, and deletes it. One key, one registration, one phase.

  **What happens, in order:**

  1. *(this plan, read-only — nothing is created)* The test resolves your real `glab`
     config directory, confirms `glab auth status --hostname gitlab.com` reports an
     authenticated session, verifies your token's scopes by an exact-member check, and
     reads your SSH-key inventory once to establish a baseline count. It creates
     nothing and deletes nothing.
  2. *(plan 09.1-02, the single mutation)* It creates a throwaway identity in a
     temporary HOME — never your real `~/.ssh` or `~/.gitconfig` — whose NAME embeds
     this run's unique ID, generates a throwaway ed25519 key pair for it in a
     temporary directory, and runs the **compiled `gitid` binary** — `gitid
     register-key <identity>` — exactly as you would. That registers the public key
     once on your GitLab account as a combined authentication-and-signing key.
  3. It then re-reads your key inventory to LEARN the resource ID of the key it just
     created, matching on this run's exact scoping string.
  4. It deletes exactly that one recorded ID, after re-confirming the entry still
     carries this run's scoping string.
  5. It finishes with a read-only sweep confirming zero keys carrying this run's
     scoping string remain on your account, and that the count of your pre-existing
     keys is unchanged.

  **Please read this part before you approve — the naming deviation.** ONESHOT.md's
  policy says every key a run creates should be named `gitid-e2e:<run-id>:<purpose>`.
  gitid's shipped key-title format is `gitid: <name> @ <machine>` and it is frozen
  (decision D-07); making the compiled binary emit the policy's format would require
  adding a test-only override to the product, which decision D-06 rules out for v1.0,
  and would mean the thing being validated is not the thing that ships. So the run
  carries its unique ID inside the **identity name** instead, which puts the literal
  string `gitid-e2e-<run-id>` verbatim into the title the product creates — still
  uniquely yours, still uniquely recordable, still uniquely sweepable. This is the
  same deliberate, recorded deviation you accepted for the GitHub run, and this is why
  the checkpoint spells it out rather than glossing it.

  **How the resource ID is captured — stated plainly.** `glab ssh-key add` prints a
  short human confirmation, not a machine-readable ID. The run therefore reads your
  key inventory immediately after the registration and finds the entry by this run's
  exact scoping string, then uses that entry's ID. The inventory read is used ONLY to
  learn the ID; nothing is ever selected for deletion by a broad query.

  **One thing that may block the run, and how to avoid it.** GitLab has no equivalent
  of `gh auth status`'s scope line — `glab auth status` prints no scope information at
  all. The closest exact check is GitLab's own REST endpoint
  `personal_access_tokens/self`, which reports a token's `scopes` array. That endpoint
  only answers for a **personal-access-token-backed** session. If your `glab` session
  is OAuth-backed, the run cannot verify write authorization exactly, and it will SKIP
  rather than proceed on an unverified assumption — which under decision D-06 leaves
  UP-04 open. To avoid that, before approving please confirm on your machine:

  1. `glab auth status --hostname gitlab.com` reports an authenticated session.
  2. `glab api personal_access_tokens/self` returns JSON containing a `scopes` array
     that includes `api`. If it does not (OAuth session, or the endpoint is
     unavailable), re-authenticate with a personal access token carrying the `api`
     scope: `glab auth login --hostname gitlab.com`, choosing token authentication.
  3. You are willing for this specific account to receive and then lose ONE
     disposable, uniquely-scoped SSH key during plan 09.1-02's run.
  4. You accept the naming deviation described above.

  **What it will NOT do:** touch any pre-existing key on your account, select anything
  for deletion by a broad inventory query, write to your real `~/.ssh` or
  `~/.gitconfig`, invoke glab's token-revealing auth-status flag, or interact with
  GitHub in any way.
  </instructions>
  <verification>After you reply, every precondition is re-asserted independently by the run's own preflight — resolving `glab` on PATH, requiring an authenticated session, and requiring `api` as an EXACT member of the reported scope set — and it skips with the missing precondition named rather than proceeding on a stale assumption. `09.1-01-SUMMARY.md` records your verbatim reply, the account handle, and the observed (redacted) preflight evidence.</verification>
  <resume-signal>Reply `authorized: <gitlab-handle>` so the SUMMARY can record which account was validated — or `blocked: <reason>`, in which case no command in this phase contacts gitlab.com, the reason is recorded as blocked evidence, UP-04 is left OPEN as implemented-but-unvalidated, and Tasks 2 and 3 are still written and committed with the test skipping on its own unmet precondition. No mock is substituted for the required real-account validation.</resume-signal>
  <acceptance_criteria>
    - The developer replied either `authorized: <handle>` or `blocked: <reason>`, and the reply is recorded verbatim in `09.1-01-SUMMARY.md`.
    - The checkpoint text shown to the developer described ONE disposable key pair, ONE combined registration, and ONE phase — it did not describe Wave 8's four-key/two-phase GitHub shape (D-01, D-02).
    - The checkpoint text disclosed the identity-name run-scoping deviation from ONESHOT.md's literal title format, and named D-07 (frozen title) and D-06 (no test-only product seam) as the reason (D-03).
    - The checkpoint text described ID capture as a run-scoped inventory lookup performed after registration, and did NOT describe the ID as returned by the add command.
    - The checkpoint text named the PAT-vs-OAuth scope-verification constraint and its remediation command, so a skip caused by session type is a foreseen outcome rather than a surprise (D-05, D-06).
    - If the reply was `blocked`, `09.1-01-SUMMARY.md` records that zero commands contacted gitlab.com in this phase, and UP-04 is not claimed as closed anywhere (D-06).
  </acceptance_criteria>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: read-only end-to-end tracer — new build-tagged file, full Makefile wiring, and an exact-scope preflight proven against the live glab session</name>
  <files>e2e/upload_real_account_gitlab_e2e_test.go, Makefile</files>
  <precondition>`glab` is on PATH on the execution machine and `glab auth status --hostname gitlab.com` exits 0. The test asserts both itself and SKIPS (never fails) when either is unmet, recording which one was missing.</precondition>
  <read_first>
    - `e2e/upload_real_account_e2e_test.go` lines 1-13 — the build-tag line and the file-level doc comment stating why the file is deliberately exempt from the hermetic child-env boundary. This is the shape to adapt.
    - `e2e/upload_real_account_e2e_test.go` lines 15-63 — the import block and the argv recorder (`recordedArgv`, `argvRecorder.add`, `argvRecorder.assertNoPath`).
    - `e2e/upload_real_account_e2e_test.go` lines 65-101 — `resolveRealGHConfigDir`, `ambientEnvMap`, `realAccountChildEnv`, `realAccountProductEnv`: the config-dir resolution order and the child-env plumbing to adapt for `GLAB_CONFIG_DIR`.
    - `e2e/upload_real_account_e2e_test.go` lines 103-117 — `writeRealGHWrapper`: the HOME-bridge wrapper-script pattern to adapt.
    - `e2e/upload_real_account_e2e_test.go` lines 119-178 — `parseGitHubScopeLine`, `missingRequiredScopes`, `preflightRealGitHub`. Reuse the SHAPE (LookPath -> auth-status invocation -> redacted skip messages -> return path + redacted evidence) and the "no token-revealing flag in preflight argv" assertion; replace the scope-line parsing body entirely per this task's action.
    - `e2e/upload_real_account_e2e_test.go` lines 180-187 — `newRealAccountRunID`.
    - `e2e/upload_real_account_e2e_test.go` lines 380-436 — the top of `TestRealAccountGitHubUploadRoundTrip`: ambient-env resolution, preflight call, wrapper install, run-ID and scoping-string definition, sandbox HOME, deps construction, the EARLY read-only sweep `t.Cleanup` registration (lines 408-429, whose comment states the LIFO mechanism), and the baseline unscoped-count capture.
    - `Makefile` lines 55, 218, 228-234, 686-693 — the `.PHONY` list, `KNOWN_BUILD_TAGS := screenshot smoke e2e realaccount`, the `lint-tagged` guard loop plus its `go vet -tags` lines, and the `verify-upload-real-account` target with its comment block.
    - `Makefile` lines 675-685 — `smoke-network-test`: the original precedent for a real-network, opt-in, separately-tagged target that is a prerequisite of nothing and auto-skips rather than failing.
    - `e2e/harness_test.go` lines 47-95 — `SandboxHome` and `BuildBinary`, reused identically.
    - `e2e/harness_test.go` lines 695-724 — `fileHasE2EBuildTag` / `TestEveryE2EChildEnvIsHermetic`: confirms the hermetic guard evaluates the constraint against the `e2e` tag only, so the new file is out of its scope by construction and the guard needs zero changes.
    - `internal/uploader/inventory.go` lines 74-95 — `glabInventory`: `uploader.Inventory(uploader.ToolGLab, ...)` already paginates; the test must never write its own page loop.
    - `internal/uploader/classify.go` line 33 — `glabTokenPattern`, matching only `glpat-`-prefixed tokens. `uploader.RedactCLIOutput` does NOT redact an OAuth-format token; this is why glab's token-revealing auth-status flag must never be invoked.
    - `internal/uploader/uploader.go` lines 119-128, 177-178 — `ProviderForHostname` and `AuthCheck`, confirming `glab auth status --hostname gitlab.com` as the uniform auth-check command shape.
    - `.planning/phases/09.1-gitlab-real-account-validation/09.1-RESEARCH.md` "Common Pitfalls" 1-4 and "Open Questions" 1-2 — the four empirical unknowns this tracer exists to resolve.
  </read_first>
  <behavior>
    Write the end-to-end assertions FIRST; they are red until the tag, the Makefile
    wiring, and the preflight body all exist. Per this project's RED-stub convention,
    a first failing commit uses `_`-parameter stubs so hard-fail lint passes without
    bypassing the hooks.

    - `make verify-upload-real-account-gitlab` compiles and runs a test that exists
      under the `realaccountgitlab` tag: red until the tag, the target, and the file
      all exist.
    - `make lint` stays green with the new tag present: red until `KNOWN_BUILD_TAGS`
      and the new `go vet` line both exist.
    - With `glab` absent or unauthenticated, the target exits 0 with a skip naming the
      missing precondition and its remediation command.
    - With an authenticated PAT-backed session carrying `api`, the preflight returns
      successfully and the test logs the resolved `glab` path plus a redacted evidence
      line — proving the sandbox HOME swap did not hide the real session.
    - With an authenticated session whose reported scope set lacks `api` as an exact
      member, or whose token-scope response cannot be read at all, the target exits 0
      with a skip naming that exact condition — never a silent proceed.
    - A baseline read of the real inventory through `uploader.Inventory` with
      `uploader.ToolGLab` succeeds and reports a count.
    - The read-only final sweep, registered before anything else, finds zero entries
      carrying this run's scoping string (trivially true in this wave, and the same
      closure wave 2 relies on).
  </behavior>
  <action>
Create `e2e/upload_real_account_gitlab_e2e_test.go` under `//go:build
realaccountgitlab` — a tag distinct from `e2e`, so `make test-e2e` can never pull it
in — and wire the Makefile for it in the SAME commit.

**Naming rule, applied to every top-level identifier in the file.** The GitHub
sibling `e2e/upload_real_account_e2e_test.go` lives in the same Go package. Every
package-level const, type, function, method receiver type, and test in the new file
must carry a `GLab`/`GitLab` marker so the two tagged files can never collide:
`realAccountGLabScopeRemediation`, `requiredRealAccountGLabScopes`,
`recordedGLabArgv`, `glabArgvRecorder`, `resolveRealGLabConfigDir`,
`glabAmbientEnvMap`, `realAccountGLabChildEnv`, `realAccountGLabProductEnv`,
`writeRealGLabWrapper`, `parseGLabTokenScopes`, `missingRequiredGLabScopes`,
`preflightRealGitLab`, `newRealAccountGLabRunID`, `recordedGLabRemoteKey`,
`outstandingGLabRemoteKeys`, `glabRemoteKeyMapID`, `findRecordedGLabInventoryEntry`,
`isGLabAlreadyAbsent`, `deleteRecordedGLabRemoteKey`, `drainOutstandingGLabRemoteKeys`,
`countUnscopedGLab`, `glabEntriesWithScope`, `glabExactScopedEntry`,
`glabRegistrationLabel`, `runRealGLabBinary`, `runGLabProductPhase`,
`realAccountGLabUploaderDeps`, and every `TestRealAccountGitLab*` function. This is a
spelling adaptation only — the mechanism each one implements is reproduced exactly.

Write the file-level doc comment stating: the policy it implements; that it is the
project's GitLab real-account surface; that it is opt-in behind the
`realaccountgitlab` tag; that it is deliberately exempt from the hermetic child-env
boundary every other e2e test is bound by, and why (its entire purpose is to reach
the real `glab` — the hermetic guard evaluates the `e2e` tag only, so this file is out
of scope by construction and the guard needs no change); that D-02 makes this ONE
phase with ONE disposable key pair on purpose, so a future reader does not "fix" it
into matching the GitHub file's two-phase shape; and that Task 1's checkpoint
authorized the identity-name run-scoping deviation.

Implement, in this order:

1. **`resolveRealGLabConfigDir(env map[string]string) string`.** Resolution order:
   `GLAB_CONFIG_DIR` if non-empty; else `filepath.Join(XDG_CONFIG_HOME, "glab-cli")`
   if `XDG_CONFIG_HOME` is non-empty; else `filepath.Join(env["HOME"], ".config",
   "glab-cli")`. Resolve from the AMBIENT, pre-swap `HOME` — the whole point is that
   the real credentials must survive the sandbox HOME swap. Add
   `glabAmbientEnvMap()`, `realAccountGLabChildEnv(home, glabConfigDir)` and
   `realAccountGLabProductEnv(...)` mirroring the GitHub versions, exporting
   `GLAB_CONFIG_DIR`, `GITID_REAL_HOME`, `GITID_REAL_GLAB_CONFIG_DIR`,
   `GITID_REAL_GLAB`, and the wrapper-prefixed `PATH`.

2. **`writeRealGLabWrapper(t)`.** A `0o700` `#!/bin/sh` script named `glab` in a
   `t.TempDir()` that restores `HOME` from `$GITID_REAL_HOME`, sets
   `GLAB_CONFIG_DIR` from `$GITID_REAL_GLAB_CONFIG_DIR`, and `exec`s
   `"$GITID_REAL_GLAB" "$@"`. This is the bridge that lets a child running under the
   sandbox HOME still resolve the developer's real session.

3. **`preflightRealGitLab(t, glabConfigDir, recorder) (glabPath, redactedEvidence
   string)`.** Fail closed at every branch; every skip message names the exact failed
   precondition and quotes its remediation command; every logged output first passes
   through `uploader.RedactCLIOutput(out, os.Getenv("HOME"), 240)`:
   - `exec.LookPath("glab")` fails -> `t.Skipf` naming that `glab` is not on PATH.
   - Assert no argument in any preflight argv contains a token-revealing substring
     (keep the GitHub file's assertion in spirit: scan the argv for `token`
     case-insensitively and `t.Fatalf` if found). Record every preflight argv on the
     recorder.
   - Run `glab auth status --hostname gitlab.com` with
     `realAccountGLabChildEnv(os.Getenv("HOME"), glabConfigDir)`. Non-zero exit ->
     `t.Skipf` naming "no authenticated glab session" with the redacted output and the
     remediation constant `realAccountGLabScopeRemediation = "glab auth login
     --hostname gitlab.com"`.
   - **Scope check.** Before writing the parser, run `glab api --help` and `glab api
     personal_access_tokens/self` once by hand on the execution machine and record the
     real, redacted response shape and the exact flag set that works — this project's
     hypothesis -> test -> implementation rule forbids writing a parser against a
     remembered format. Then ship the confirmed invocation (default:
     `glab api personal_access_tokens/self`, no jq flag) and parse its stdout with
     `encoding/json` in `parseGLabTokenScopes(output string) ([]string, bool)`:
     unmarshal into a struct with a `Scopes []string` field; return `ok=false` when
     the output is not valid JSON, has no scopes field, or the array is empty.
     `missingRequiredGLabScopes(scopes []string) []string` compares against
     `requiredRealAccountGLabScopes = []string{"api"}` using EXACT string equality
     after trimming surrounding whitespace and quotes — substring matching is
     forbidden, since a scope named so as to merely contain a required name must be
     rejected.
   - The `glab api` call exits non-zero, or `parseGLabTokenScopes` returns `ok=false`
     -> `t.Skipf` naming that the session's token scopes could not be read exactly
     (typically an OAuth-backed rather than PAT-backed session), quoting the
     remediation. Never treat an unreadable scope response as permission to proceed:
     a reachable read endpoint proves nothing about write authorization.
   - `missingRequiredGLabScopes` returns a non-empty list -> `t.Skipf` naming the
     missing scope(s) and the remediation.
   - Return the resolved `glab` path and the redacted evidence string for the SUMMARY.

4. **`newRealAccountGLabRunID(t)`** — UTC `20060102t150405` plus a 4-byte
   `crypto/rand` hex suffix, exactly as the GitHub version. Characters are safe in an
   SSH alias, a filename, and a key title.

5. **`TestRealAccountGitLabUploadRoundTrip(t)`** — this wave's READ-ONLY slice, which
   plan 09.1-02 expands in place (it is the final test, not a scaffold):
   - Resolve ambient `HOME` and the real config dir; skip if either is empty.
   - Call `preflightRealGitLab`; log the resolved path and the redacted evidence.
   - Build the run ID and define the SINGLE run-scoping string `productScope :=
     "gitid-e2e-" + runID`. Per D-02 there is no second scoping string — do not
     define a policy-title prefix. `t.Fatal` if it is empty.
   - `home := SandboxHome(t)`; install the wrapper; build
     `realAccountGLabUploaderDeps` whose `LookPath` returns the wrapper for `glab` and
     whose `RunCmd` records argv, applies a 90s `context.WithTimeout`, uses
     `realAccountGLabProductEnv`, and wraps any error through `RedactCLIOutput`.
   - **Register the read-only final sweep `t.Cleanup` FIRST**, immediately after the
     scoping string is defined and before any other cleanup or key material exists.
     Reproduce the GitHub file's lines 408-429 mechanism exactly, including its
     explanatory comment: LIFO ordering means registering the sweep first makes it run
     LAST, after wave 2's deletion closure. The closure re-reads
     `uploader.Inventory(uploader.ToolGLab, glabPath, deps)`, collects entries whose
     title contains `productScope`, reports via `t.Errorf` (never `t.Fatalf`, so it
     cannot mask an original failure) if any remain, and additionally compares the
     count of entries NOT carrying the scoping string against the recorded baseline.
     The sweep is read-only and is never used as a deletion selector.
   - Read the baseline inventory once, record `countUnscopedGLab`, set the
     baseline-recorded flag, and log the baseline count.
   - Log explicitly that this wave performs no mutation, so a reader of the wave-1
     run output is not misled by the test's name.

6. **Makefile — all four edits, in this same commit.** Shipping the tag without the
   guard entry hard-fails `make lint` with a message naming `realaccountgitlab`:
   - `KNOWN_BUILD_TAGS := screenshot smoke e2e realaccount realaccountgitlab`.
   - Add a `go vet -tags realaccountgitlab ./...` line directly below the existing
     `go vet -tags realaccount ./...` line inside `lint-tagged`.
   - Add `verify-upload-real-account-gitlab` to the `.PHONY` list and to nothing else.
   - Add the target, with a comment block above the recipe modelled byte-for-byte on
     `verify-upload-real-account`'s, stating that it is a LOCAL/UAT convenience only,
     is never a prerequisite of `make test` / `make test-e2e` / `make lint` / CI, is
     opt-in under the separate `realaccountgitlab` build tag, and auto-skips rather
     than failing when glab authentication or the required scope is unavailable. The
     recipe is:
     `go test -tags realaccountgitlab -run '^TestRealAccountGitLabUploadRoundTrip$$' -v ./e2e/...`

Never touch GitHub. Never operate on any inventory entry whose title does not carry
this run's scoping string. Do not construct a provider argv for adding, listing, or
deleting keys anywhere in the test — `internal/uploader` owns those and proving the
product's own argv is the entire point.

Record in `09.1-01-SUMMARY.md`: the resolved config directory kind (env override,
XDG, or default), whether the sandbox HOME swap preserved the real session, the exact
`glab api` invocation that worked, the observed redacted response shape, the baseline
inventory count, and which of RESEARCH.md's assumptions (A1 scope name, A3
`GLAB_CONFIG_DIR` sufficiency including any keyring interaction, A4 endpoint
applicability) each of these resolved.
  </action>
  <verify>
    <automated>make lint && make verify-upload-real-account-gitlab && go vet -tags=realaccount,realaccountgitlab ./...</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/upload_real_account_gitlab_e2e_test.go` exists and its first line is the literal `//go:build realaccountgitlab`: `head -1 e2e/upload_real_account_gitlab_e2e_test.go` matches it exactly.
    - The `e2e` tag provably does not select the new test: `go test -tags e2e -list '.*' ./e2e/... | grep -c GitLab` returns 0.
    - `make lint` exits 0 with the new tag present, and the Makefile carries both guard edits: `grep -c 'KNOWN_BUILD_TAGS := screenshot smoke e2e realaccount realaccountgitlab' Makefile` returns 1, and `grep -c 'go vet -tags realaccountgitlab' Makefile` returns 1.
    - The new target is a prerequisite of nothing: `grep -n 'verify-upload-real-account-gitlab' Makefile` shows exactly two occurrences — the `.PHONY` line and the target's own definition line — and none on a `test`, `test-e2e`, `lint`, or CI recipe or prerequisite line.
    - The two tagged real-account files coexist without symbol collision: `go vet -tags=realaccount,realaccountgitlab ./...` exits 0.
    - The hermetic guard needs no change: `git diff --stat e2e/harness_test.go` is empty, and `09.1-01-SUMMARY.md` records that `fileHasE2EBuildTag` excludes the new file by construction because it declares a non-`e2e` constraint.
    - glab's token-revealing auth-status flag is never invoked: `grep -c -- '--show-token' e2e/upload_real_account_gitlab_e2e_test.go` returns 0.
    - The file does not use the hermetic child-env helper: `grep -c 'e2eEnv' e2e/upload_real_account_gitlab_e2e_test.go` returns 0.
    - The GitHub two-registration shape is not ported: with Go comment lines stripped, `grep -vE '^[[:space:]]*//' e2e/upload_real_account_gitlab_e2e_test.go | grep -c 'RegistrationSigning'` returns 0.
    - No parallel provider argv is constructed in the test: with comment lines stripped, `grep -vE '^[[:space:]]*//' e2e/upload_real_account_gitlab_e2e_test.go | grep -c '"ssh-key"'` returns 0.
    - The final sweep's `t.Cleanup` is registered before any other `t.Cleanup` in the test body, and its registration is preceded by a comment stating the LIFO reason — recorded in the SUMMARY with the two line numbers.
    - With the account authorized and a PAT-backed session carrying `api`, `make verify-upload-real-account-gitlab` runs to completion (does NOT skip), and its output shows the resolved `glab` path, a redacted evidence line, a baseline inventory count, and a final sweep reporting zero run-scoped entries.
    - With `glab` absent, unauthenticated, or without a readable exact scope set, the target exits 0 and its output names exactly which precondition failed together with its remediation command — it never fails red and never proceeds.
    - No unredacted token appears anywhere in the run output or in `09.1-01-SUMMARY.md`: the SUMMARY records that every logged `glab` output passed through `uploader.RedactCLIOutput` first, and notes the known `glpat-`-only redaction gap as the reason the token-revealing flag is banned outright.
    - Every top-level identifier declared in the new file carries a `GLab`/`GitLab` marker, and the SUMMARY records this as a spelling-only adaptation of PATTERNS.md's "copy verbatim" instruction, with the collision-safety reason and the confirming combined-tag vet result.
  </acceptance_criteria>
  <done>
The GitLab real-account surface exists end-to-end and is proven reachable: a new
build-tagged file that no routine gate can compile, a Makefile target that is a
prerequisite of nothing and whose guard entry keeps `make lint` green, an exact-scope
preflight that either reaches the developer's real authenticated session or skips
naming precisely what was missing, and a read-only sweep already registered in the
LIFO position wave 2's mutation depends on — with zero remote state changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the cleanup bookkeeping machinery and its account-free unit tests</name>
  <files>e2e/upload_real_account_gitlab_e2e_test.go</files>
  <read_first>
    - `e2e/upload_real_account_e2e_test.go` lines 189-292 — `recordedRemoteKey`, `outstandingRemoteKeys` (mutex-protected map with `add`/`remove`/`snapshot`), `remoteKeyMapID`, `findRecordedInventoryEntry`, `isAlreadyAbsent`, `deleteRecordedRemoteKey`, `drainOutstandingRemoteKeys`, `countUnscoped`. This block is the hardened result of Wave 8's R19/R20 review cycles and is already tool-agnostic (`uploader.Tool`/`uploader.Deps` parameterize it). Reproduce the mechanism exactly.
    - `e2e/upload_real_account_e2e_test.go` lines 294-318 — `entriesWithScope`, `exactScopedEntry`, `registrationLabel`. `exactScopedEntry` fatals unless EXACTLY one entry matches, which is the guard against acting on a same-titled stranger.
    - `e2e/upload_real_account_e2e_test.go` lines 532-615 — `TestRealAccountScopeParserRequiresExactMembers` (the exact-member table-test discipline to re-express against JSON input), `TestRealAccountResolvesRealGHConfigDir`, `TestRealAccountCleanupBookkeepingIsIdempotent`, and `TestRealAccountCleanupOrderingIsLIFOSweepLast`.
    - `internal/uploader/inventory.go` lines 174-232 — `DeleteKey`'s `isAllDigits(id)` guard (a non-numeric ID is refused before any delete argv is built) and `deleteArgs`' single-namespace GitLab branch. The test must route every deletion through `uploader.DeleteRecordedKey` so this guard can never be bypassed.
    - `internal/uploader/uploader.go` lines 53-56, 62-69, 81-87 — `GLabKeyTypeAuthAndSigning`, the `Registration` enum, and `KeyTypeFor`. `RegistrationCombined` is GitLab's only registration shape.
    - `internal/tuikit/design.go` lines 660-668 — `UploadRegistrationLabelCombined`, GitLab's single registration label.
  </read_first>
  <behavior>
    Four account-free tests, written before the machinery they exercise:

    - `TestRealAccountGitLabResolvesRealGLabConfigDir`: table over synthetic env maps.
      `GLAB_CONFIG_DIR` set wins outright. Only `XDG_CONFIG_HOME` set yields
      `<xdg>/glab-cli`. Neither set yields `<home>/.config/glab-cli`. An empty
      `GLAB_CONFIG_DIR` value falls through rather than returning empty.
    - `TestRealAccountGitLabScopeCheckRequiresExactMember`: table over synthetic
      `personal_access_tokens/self` JSON responses. Accept: a scopes array containing
      `api` exactly, alone or alongside others. Reject: an array whose only member
      merely contains `api` as a substring (for example a longer scope name); an array
      missing `api` entirely; an empty array; a response with no scopes field; and
      output that is not valid JSON. Rejection means the preflight skips — never
      proceeds.
    - `TestRealAccountGitLabCleanupBookkeepingIsIdempotent`: with a fake deleter, a
      fully-explicit-deletion path leaves the outstanding map empty so the drain
      performs ZERO deletions; an early-failure path leaves exactly the undeleted IDs,
      each deleted exactly once by the drain; a deleter reporting the entry already
      absent removes the ID and is not an error.
    - `TestRealAccountGitLabCleanupOrderingIsLIFOSweepLast`: register two closures
      against a real `*testing.T` from a sub-test in the order this plan specifies,
      record execution order in a shared slice, and assert the outstanding-ID
      closure's marker appears BEFORE the sweep's marker.
  </behavior>
  <action>
Add the cleanup bookkeeping machinery and its four account-free unit tests to
`e2e/upload_real_account_gitlab_e2e_test.go`. Nothing in this task contacts GitLab —
these tests run with no account, and they are what makes wave 2's mutation safe to
authorize at all.

Reproduce the mechanism from the GitHub sibling exactly, under the `GLab`-marked
names Task 2 established:

- `recordedGLabRemoteKey{entry uploader.ExistingKey; scope string}`.
- `outstandingGLabRemoteKeys` — a `sync.Mutex`-protected
  `map[string]recordedGLabRemoteKey` with `add`, `remove`, and `snapshot`, keyed by
  `glabRemoteKeyMapID(entry)` formatted as registration-and-ID. The map is the SINGLE
  SOURCE OF TRUTH for what still needs deleting; this is what makes the
  explicit-deletion path and the `t.Cleanup` path compose instead of double-deleting
  (Wave 8's R19 finding).
- `findRecordedGLabInventoryEntry(entries, recorded) (uploader.ExistingKey, bool)` —
  locate a recorded entry in a fresh inventory read by its exact ID.
- `isGLabAlreadyAbsent(err error) bool` — an already-absent report is not a failure.
- `deleteRecordedGLabRemoteKey(tool, toolPath, deps, outstanding, recorded) error` —
  re-read the inventory, re-confirm the entry with that exact ID STILL carries its
  recorded scoping string, and only then call `uploader.DeleteRecordedKey` with the
  recorded entry, removing the ID from the map on success or on an already-absent
  report. If a recorded ID's current title no longer carries its scope, stop and
  report: something changed underneath the run and deleting would be a guess. Never
  select by a broad query and never delete by title alone.
- `drainOutstandingGLabRemoteKeys(tool, toolPath, deps, outstanding, report)` —
  iterate ONLY over IDs still present in the map, reporting failures through the
  supplied reporter rather than fataling.
- `countUnscopedGLab(entries, scope)`, `glabEntriesWithScope(entries, registration,
  scope)`, and `glabExactScopedEntry(t, entries, registration, scope)` — the last
  fatals unless EXACTLY one entry matches. Per D-02 these take a single scope string,
  not the GitHub version's pair.
- `glabRegistrationLabel(registration uploader.Registration) string` — returns
  `"combined"` for `uploader.RegistrationCombined` and `"unknown"` otherwise. Do not
  port the GitHub file's two-branch label function; GitLab has exactly one
  registration shape.

Then write the four tests described in `<behavior>`, all runnable with no account.
For the scope table test, drive the same `parseGLabTokenScopes` +
`missingRequiredGLabScopes` pair the live preflight uses — a parallel copy of the
logic would prove nothing about what actually runs.

Per this project's RED-stub convention, if you commit an intermediate failing state,
use `_`-parameter stubs so the hard-fail lint hooks pass without bypassing them.

Record in `09.1-01-SUMMARY.md`: each test's name, what it proves, and the mapping from
each Wave 8 review finding (R19 double-deletion, R20 unobtained-coverage) to the
mechanism here that carries it forward.
  </action>
  <verify>
    <automated>go test -tags realaccountgitlab -count=1 -run 'TestRealAccountGitLab(ResolvesRealGLabConfigDir|ScopeCheckRequiresExactMember|CleanupBookkeepingIsIdempotent|CleanupOrderingIsLIFOSweepLast)' -v ./e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - All four named tests exist and pass with no GitLab account available: the verify command exits 0 and its `-v` output shows four `--- PASS` lines, one per named test.
    - The scope table test rejects a scope set whose only member merely contains `api` as a substring, rejects a set missing `api`, rejects an empty array, rejects a response with no scopes field, and rejects non-JSON output — and accepts only an exact `api` member. Each rejection case is a named sub-test.
    - The scope table test drives the SAME `parseGLabTokenScopes`/`missingRequiredGLabScopes` functions the live preflight calls: `grep -c 'parseGLabTokenScopes' e2e/upload_real_account_gitlab_e2e_test.go` returns at least 2 (the preflight call site and the test call site).
    - The idempotency test proves ZERO drain deletions on the fully-explicit path and exactly-one deletion per remaining ID on the early-failure path, using a fake deleter that counts calls per ID.
    - The ordering test asserts the outstanding-ID closure's marker appears before the sweep's marker in the recorded execution slice — proving LIFO behavior directly rather than relying on the registration-order prose.
    - Every deletion in the file routes through `uploader.DeleteRecordedKey`: with comment lines stripped, `grep -vE '^[[:space:]]*//' e2e/upload_real_account_gitlab_e2e_test.go | grep -c 'uploader.DeleteKey('` returns 0, so `DeleteKey`'s numeric-ID guard can never be bypassed.
    - `glabRegistrationLabel` has no signing/authentication branch: with comment lines stripped, `grep -vE '^[[:space:]]*//' e2e/upload_real_account_gitlab_e2e_test.go | grep -c 'RegistrationAuthentication'` returns 0.
    - `make lint` exits 0 after this task, with the file compiling cleanly under `go vet -tags realaccountgitlab`.
    - `09.1-01-SUMMARY.md` maps R19 (double deletion) and R20 (unobtained coverage) to the specific mechanism in this file that carries each forward.
  </acceptance_criteria>
  <done>
The safety machinery wave 2's mutation depends on is present and proven correct
without an account: the outstanding-ID map is the single source of truth, deletion is
by recorded ID after a scope re-confirmation and only through the engine's guarded
entry point, the drain is idempotent, and the LIFO ordering that puts the read-only
sweep last is asserted directly rather than assumed.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New file** `e2e/upload_real_account_gitlab_e2e_test.go` (build tag
`realaccountgitlab`), declaring:

- Consts/vars: `realAccountGLabScopeRemediation`, `requiredRealAccountGLabScopes`
- Types: `recordedGLabArgv`, `glabArgvRecorder`, `recordedGLabRemoteKey`,
  `outstandingGLabRemoteKeys`
- Env/config helpers: `resolveRealGLabConfigDir`, `glabAmbientEnvMap`,
  `realAccountGLabChildEnv`, `realAccountGLabProductEnv`, `writeRealGLabWrapper`
- Preflight: `parseGLabTokenScopes`, `missingRequiredGLabScopes`,
  `preflightRealGitLab`, `newRealAccountGLabRunID`
- Bookkeeping: `glabRemoteKeyMapID`, `findRecordedGLabInventoryEntry`,
  `isGLabAlreadyAbsent`, `deleteRecordedGLabRemoteKey`,
  `drainOutstandingGLabRemoteKeys`, `countUnscopedGLab`, `glabEntriesWithScope`,
  `glabExactScopedEntry`, `glabRegistrationLabel`
- Deps/driver (declared here, fully exercised in plan 09.1-02):
  `realAccountGLabUploaderDeps`, `runRealGLabBinary`, `runGLabProductPhase`
- Tests: `TestRealAccountGitLabUploadRoundTrip` (read-only slice in this plan,
  expanded to the full round trip in 09.1-02),
  `TestRealAccountGitLabResolvesRealGLabConfigDir`,
  `TestRealAccountGitLabScopeCheckRequiresExactMember`,
  `TestRealAccountGitLabCleanupBookkeepingIsIdempotent`,
  `TestRealAccountGitLabCleanupOrderingIsLIFOSweepLast`

**Modified file** `Makefile`:

- `KNOWN_BUILD_TAGS` gains `realaccountgitlab`
- `lint-tagged` gains a `go vet -tags realaccountgitlab ./...` line
- `.PHONY` gains `verify-upload-real-account-gitlab`
- New target `verify-upload-real-account-gitlab`, a prerequisite of nothing
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> the developer's real GitLab account | The only place in the repository where automated code reaches a real GitLab account. This plan's contact is read-only; plan 09.1-02 adds the single mutation. |
| ambient authenticated `glab` session -> sandboxed child process | The HOME swap must not hide the real credentials, and must not leak the token into any log line. |
| opt-in build tag -> routine gates | A real-account test compiled by a routine gate would contact (and, in wave 2, mutate) an account on every CI or local test run. |
| token-scope response -> the decision to proceed | An unverifiable scope response must produce a skip, never a proceed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.1-01 | Elevation of Privilege | scope preflight (`preflightRealGitLab`) | high | mitigate | `glab auth status` prints no scope information at all, so Wave 8's scope-line parser cannot be ported and a parser written by analogy would silently match nothing. The preflight instead reads `glab api personal_access_tokens/self` and requires `api` as an EXACT member of the parsed scopes array; a non-JSON, missing-field, empty, or non-zero-exit response is a named `t.Skip`, never a proceed. `TestRealAccountGitLabScopeCheckRequiresExactMember` proves substring matches are rejected. |
| T-09.1-02 | Tampering | routine gates compiling the real-account file | critical | mitigate | The file lives behind `//go:build realaccountgitlab`, distinct from `e2e`. Its Make target is a prerequisite of nothing (asserted by a two-occurrence grep over the Makefile), and `go test -tags e2e -list` provably does not select it. Mirrors the `smoke-network-test` / `verify-upload-real-account` precedent. |
| T-09.1-03 | Information Disclosure | real token in logs, test output, or the SUMMARY | high | mitigate | Every logged `glab` output passes through `uploader.RedactCLIOutput` first. glab's token-revealing auth-status flag is never invoked anywhere (asserted by a zero-count grep), because `glabTokenPattern` matches only `glpat-`-prefixed tokens and would let an OAuth-format token through unredacted — a gap this research cycle demonstrated by leaking a real token exactly that way. The preflight additionally fatals if any recorded argv contains a token-revealing substring. |
| T-09.1-04 | Denial of Service | `make lint` hard-failing on an ungated build tag | medium | mitigate | `lint-tagged`'s CR-13 guard hard-fails on any `//go:build` tag missing from `KNOWN_BUILD_TAGS`. Both Makefile guard edits ship in the SAME commit as the new tag, asserted by `make lint` exiting 0 in the task's own verify. |
| T-09.1-05 | Tampering | duplicate package-level symbols across the two tagged real-account files | medium | mitigate | Every top-level identifier in the new file carries a `GLab`/`GitLab` marker, and `go vet -tags=realaccount,realaccountgitlab ./...` is run once as proof the two files can coexist. Spelling only — the safety mechanism is byte-equivalent to Wave 8's. |
| T-09.1-06 | Spoofing | sandbox HOME hiding the real session, producing a false "unauthenticated" result | medium | mitigate | The real config directory is resolved from the AMBIENT pre-swap `HOME` (`GLAB_CONFIG_DIR` -> `XDG_CONFIG_HOME/glab-cli` -> `$HOME/.config/glab-cli`) and passed explicitly to every child through the HOME-bridge wrapper. `TestRealAccountGitLabResolvesRealGLabConfigDir` pins the resolution order. If the credential turns out to be keyring-backed rather than file-backed, this task's live run surfaces it as a named skip and the SUMMARY records it — it is not assumed away. |
| T-09.1-07 | Repudiation | claiming coverage this plan does not obtain | high | mitigate | This plan performs zero mutations, and the test logs that explicitly so a wave-1 reader is not misled by the test's name. UP-04 is not touched in this plan at all; its conditional closure belongs to 09.1-02 Task 3 (D-06). |
| T-09.1-SC | Tampering | npm/pip/cargo installs | high | accept | Not applicable — zero new module dependencies. 09.1-RESEARCH.md's Package Legitimacy Audit records that this phase installs no external packages; the only import added is Go stdlib `encoding/json`. |
</threat_model>

<verification>
- `make lint` reports zero issues with the new tag present.
- `go vet -tags=realaccount,realaccountgitlab ./...` exits 0 (no symbol collision).
- `go test -tags e2e -list '.*' ./e2e/... | grep -c GitLab` returns 0.
- `make test-e2e` passes and provably does not compile the new file.
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` passes (untagged suite unaffected).
- `git diff --stat e2e/harness_test.go` is empty — the hermetic guard needed no change.
- `make verify-upload-real-account-gitlab` either runs to completion against the real
  authenticated session or skips naming its unmet precondition; its output is recorded
  in the SUMMARY.
- The four account-free unit tests pass under `-tags realaccountgitlab`.
</verification>

<success_criteria>
- The developer's authorization (or block) for phase 9.1's real-GitLab contact is
  recorded verbatim, naming the account handle, and the checkpoint text described the
  one-key/one-registration/one-phase protocol and the naming deviation honestly.
- A new, opt-in, build-tagged GitLab real-account surface exists and is unreachable
  from `make test`, `make test-e2e`, `make lint`, and CI.
- `make lint` stays green: the new tag is registered in the guard list with its own
  vet line, shipped in the same commit as the file.
- The exact-scope preflight is an empirically-confirmed mechanism, not an analogy to
  `gh`, and it fails closed on every ambiguous branch with a named remediation.
- No token, in any format, reaches a log line, a test message, or the SUMMARY.
- The LIFO cleanup machinery exists and is proven correct with no account, so wave 2's
  single mutation is safe to authorize.
- Zero remote state was changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/09.1-gitlab-real-account-validation/09.1-01-SUMMARY.md` when done.
</output>
