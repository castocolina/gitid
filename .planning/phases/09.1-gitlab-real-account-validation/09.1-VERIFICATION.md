---
phase: 09.1-gitlab-real-account-validation
verified: 2026-08-31T14:04:03Z
status: passed
score: 15/15 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 9.1: GitLab Real-Account Validation Verification Report

**Phase Goal:** The GitLab upload/delete path is proven against a real, authenticated
GitLab account — not just offline `glab` PATH shims.
**Verified:** 2026-08-31T14:04:03Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A real-account validation run, following the disposable-key/idempotent-cleanup/final-sweep protocol, executed against a real GitLab account and closed UP-04 with recorded evidence (ROADMAP SC1) | ✓ VERIFIED | `.planning/REQUIREMENTS.md:334-344` records UP-04 as EXECUTED — account `casto.dev`, run `gitid-e2e-20260831t124959-d72bde68`, resource ID `21497184`, resolved by run-scoped inventory lookup, deleted after scope re-confirmation, final sweep `remaining=0`, baseline 3 keys unchanged. Cross-confirmed in `09.1-02-SUMMARY.md:151-211` and independently a second time in `09.1-REVIEW-FIX.md` (`glab api user/keys` — exactly 3 pre-existing keys, no residue). |
| 2 | The run is opt-in behind its own build tag, never a prerequisite of any routine gate (ROADMAP SC2) | ✓ VERIFIED | `go test -tags e2e -list '.*' ./e2e/... \| grep -c GitLab` returns 0 (confirmed live). `grep -n 'verify-upload-real-account-gitlab' Makefile` shows only the `.PHONY` line and the target definition (Makefile:55,703) — not a prerequisite of `test`/`test-e2e`/`lint`/CI. |
| 3 | A single blocking human checkpoint authorizes ALL real-GitLab contact for the phase, naming the account handle, before any command reaches the account (D-05) | ✓ VERIFIED | `09.1-01-SUMMARY.md:97` records `authorized: casto.dev` verbatim at Task 1; `09.1-02-SUMMARY.md:104` records the go/no-go's `go` reply at Task 1 of wave 2, quoting wave 1's actual findings. |
| 4 | The checkpoint disclosed GitLab's single-key/single-registration/single-phase protocol (not Wave 8's four-key/two-phase GitHub shape) and the D-03 naming deviation | ✓ VERIFIED | `09.1-01-PLAN.md` Task 1 `<instructions>` block states the protocol explicitly ("ONE disposable ed25519 key pair... one registration, one phase") and discloses the identity-name run-scoping deviation with D-06/D-07 cited; `09.1-01-SUMMARY.md` records the checkpoint was presented with this text. |
| 5 | `e2e/upload_real_account_gitlab_e2e_test.go` exists behind its own `realaccountgitlab` build tag, compiled by no routine gate, reachable only through `make verify-upload-real-account-gitlab` (D-04) | ✓ VERIFIED | `head -1` is literal `//go:build realaccountgitlab`; file is 1104 lines; `go vet -tags e2e` / `go test -tags e2e -list` never select it. |
| 6 | Makefile's lint-tagged guard knows the new tag, shipped in the same lineage as the file, so `make lint` never hard-fails on an ungated tag | ✓ VERIFIED | `Makefile:218` `KNOWN_BUILD_TAGS := screenshot smoke e2e realaccount realaccountgitlab`; `Makefile:234` `go vet -tags realaccountgitlab ./...`. `make lint` run live: 0 issues. |
| 7 | The exact-scope preflight is a real, empirically-proven mechanism against the developer's live `glab` session (not an analogy to `gh`), with observed evidence recorded | ✓ VERIFIED | `09.1-01-SUMMARY.md` records the real `glab api personal_access_tokens/self` invocation and observed shapes (an OAuth 400 error envelope in wave 1, then a real PAT 2xx `scopes`/`revoked`/`active` response captured by the orchestrator's independent re-run recorded in `09.1-02-SUMMARY.md:114-124`, matching the pinned `glabTokenSelf` contract). |
| 8 | The preflight fails CLOSED on every ambiguous branch, each with a named remediation — never a silent proceed | ✓ VERIFIED | Five `t.Skipf` branches in `preflightRealGitLab` (lines 305, 315, 332, 336, 339) plus one more in the test body (line 588) each name the exact failed precondition and a remediation string; no fallthrough proceeds without a passing check. |
| 9 | No token ever reaches a log line, a test message, or the SUMMARY — every logged `glab` output passes through `uploader.RedactCLIOutput`/`redactGLabOutputLines` first | ✓ VERIFIED (post-fix) | Code review found CR-01 (three `t.Fatalf` sites bypassed redaction: `runRealGLabBinary` line 547, two `productOutput` assertions lines 666/676). Fixed directly by the orchestrator and independently re-verified: `grep -n redactGLabOutputLines` confirms all three sites now route through the helper. `--show-token` never invoked (0 occurrences). |
| 10 | The LIFO cleanup bookkeeping (outstanding-ID map, delete-by-recorded-ID, idempotent drain) exists and is proven correct by account-free unit tests before any code path can create a real remote key | ✓ VERIFIED | `TestRealAccountGitLabCleanupBookkeepingIsIdempotent` and `TestRealAccountGitLabCleanupOrderingIsLIFOSweepLast` run live with no account (0.29s total) — both PASS, confirmed by direct run in this verification. |
| 11 | The compiled `gitid` binary — not the uploader engine — registers the disposable key, so the product path a user actually invokes is what's validated | ✓ VERIFIED | `runGLabProductPhase`'s function body (grepped independently, comments stripped) contains zero occurrences of `uploader.Deps{` or `uploader.UploadKeys`; it drives `create --no-upload` then `register-key` through the compiled binary only. |
| 12 | GitLab's ONE combined registration is what is asserted — never GitHub's per-registration authentication/signing pair | ✓ VERIFIED | `grep -c 'tuikit.UploadResultOKFmt\|tuikit.UploadRegistrationLabelCombined'` = 1; `grep -c 'Authentication key registered\|Signing key registered\|Added SSH key'` = 0; `grep -n 'RegistrationAuthentication\|RegistrationSigning'` = 0 occurrences in the file. |
| 13 | The resource ID is captured by a run-scoped INVENTORY LOOKUP after registration, never claimed as returned by the add call | ✓ VERIFIED | `09.1-02-SUMMARY.md:199` logs "resolved resource ID by run-scoped inventory lookup: registration=combined id=21497184"; code path (`glabExactScopedEntry` -> `record()`) matches. |
| 14 | Deletion never selects by a broad inventory query — only by exact recorded ID, re-confirmed to still carry the run's scoping string | ✓ VERIFIED | `deleteRecordedGLabRemoteKey` routes exclusively through `uploader.DeleteRecordedKey` (line 811); `grep -vE '^\s*//' ... \| grep -c 'uploader.DeleteKey('` returns 0 in the whole file, so `DeleteKey`'s numeric-ID guard can never be bypassed by a direct call. |
| 15 | UP-04 closes ONLY if the real-account run actually EXECUTED — a skipped/blocked run leaves it open with the blocker named (D-06), and a blocked/skipped marker never co-occurs with a closed checkbox | ✓ VERIFIED | `.planning/REQUIREMENTS.md:327` shows `[x] **UP-04**` with an EXECUTED evidence parenthetical citing concrete run data; status table (`REQUIREMENTS.md:641`) shows `Complete`; both consistent with `09.1-02-SUMMARY.md`'s stated EXECUTED outcome — no skipped/blocked marker present. |

**Score:** 15/15 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `e2e/upload_real_account_gitlab_e2e_test.go` | GitLab real-account tracer + full round trip, `realaccountgitlab` tag | ✓ VERIFIED | 1104 lines; declares preflight, bookkeeping, both compiled-binary drivers, all documented tests; wired into `TestRealAccountGitLabUploadRoundTrip`. |
| `Makefile` | `KNOWN_BUILD_TAGS`, dedicated `go vet` lines (including combined-tag), `.PHONY`, `verify-upload-real-account-gitlab` target | ✓ VERIFIED | All four elements present and confirmed by live `make lint` run (0 issues) and grep. |
| `e2e/upload_real_account_e2e_test.go` | Doc-comment correction naming the GitLab sibling | ✓ VERIFIED (not independently re-diffed byte-for-byte in this pass, but code-only claim in SUMMARY and no lint/vet regression detected). |
| `.planning/ONESHOT.md` | "Phase 9.1 GitLab External Account Policy" section | ✓ VERIFIED | Present at line 143, covers all required elements (authorization, exact-scope preflight, disposable-key-only, single combined registration, identity-name run-scoping + reason, inventory-lookup ID capture, delete-by-recorded-ID, final sweep, blocked-and-stop clause). |
| `.planning/REQUIREMENTS.md` | UP-04 outcome-conditional closure | ✓ VERIFIED | Closed with EXECUTED evidence; requirement's own wording (the "per-registration-title API" phrase) preserved byte-unchanged, with a note explaining why D-02 governs instead. |
| `.planning/ROADMAP.md` | Phase 9.1 plan list, progress row, checklist entry; Goal/SC byte-unchanged | ✓ VERIFIED | Plan list (2 plans, both `[x]`), progress row `2/2 Complete`, top-level checklist `[x]`; Goal and both Success Criteria read unchanged from the plan's stated contract. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| Human checkpoint reply `authorized: <handle>` | Test's own independent exact-scope preflight | Named command + exact-member rule | ✓ WIRED | `preflightRealGitLab` re-runs `glab auth status` + `glab api personal_access_tokens/self` independently of the checkpoint text, per-run. |
| `//go:build realaccountgitlab` | `KNOWN_BUILD_TAGS` | `lint-tagged`'s guard loop | ✓ WIRED | Verified live: `make lint` passes; removing the tag from `KNOWN_BUILD_TAGS` would hard-fail per the guard's own logic (not tested destructively, but code path confirmed present). |
| Outstanding-ID map + LIFO `t.Cleanup` order | Account-free ordering/idempotency unit tests | `TestRealAccountGitLabCleanupOrderingIsLIFOSweepLast` | ✓ WIRED | Test passes live; asserts drain marker precedes sweep marker in a recorded execution slice. |
| Compiled binary's `register-key` run | Run-scoped inventory lookup | `glabExactScopedEntry` -> `record()` -> `outstandingGLabRemoteKeys` | ✓ WIRED | Confirmed via `09.1-02-SUMMARY.md` recorded log lines and source read. |
| Recorded ID | Deletion | `deleteRecordedGLabRemoteKey` -> `uploader.DeleteRecordedKey` | ✓ WIRED | Zero direct `uploader.DeleteKey(` call sites in the file; single deletion call site confirmed. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `e2e` tag never selects the GitLab real-account test | `go test -tags e2e -list '.*' ./e2e/... \| grep -c GitLab` | `0` | ✓ PASS |
| Combined-tag compile (collision safety) | `go vet -tags=realaccount,realaccountgitlab ./...` | exit 0 | ✓ PASS |
| `make lint` stays green with new tag + combined-tag gate (WR-01 fix) | `make lint` | `0 issues.` (both golangci-lint invocations) | ✓ PASS |
| Account-free cleanup/scope/ordering unit tests pass (no live account contact) | `go test -tags realaccountgitlab -count=1 -run 'TestRealAccountGitLab(ResolvesRealGLabConfigDir\|ScopeCheckRequiresExactMember\|CleanupBookkeepingIsIdempotent\|CleanupOrderingIsLIFOSweepLast)' -v ./e2e/...` | 4 top-level `--- PASS`, 26 sub-tests including all named noise/liveness/rejection cases | ✓ PASS |
| CR-01 redaction fix present at all three previously-bypassed sites | `grep -n redactGLabOutputLines e2e/upload_real_account_gitlab_e2e_test.go` | lines 547, 664, 666, 676 all route through the helper | ✓ PASS |
| WR-02 duplicate-computation fix present | source read at lines 629-630 | single `unscopedNow :=` call, used twice | ✓ PASS |
| No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) in the new file | grep | 0 matches | ✓ PASS |

**Note:** The live round-trip test (`TestRealAccountGitLabUploadRoundTrip`, which mutates the real GitLab account) was NOT re-run during this verification pass — re-running it was outside this verifier's mandate (it is a state-changing action against a real third-party account, already executed twice with independently confirmed clean results per `09.1-02-SUMMARY.md` and `09.1-REVIEW-FIX.md`). The account-free tests plus static/wiring checks above are sufficient to confirm the delivered mechanism is genuinely present and correctly wired; the live mutation's actual historical execution is evidenced by the SUMMARY's recorded resource ID, run ID, and the orchestrator's independent `glab api user/keys` confirmations (before and after the review-fix re-run).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| UP-04 | 09.1-01, 09.1-02 | GitLab real-account SSH-key upload/delete validation | ✓ SATISFIED | `.planning/REQUIREMENTS.md:327-344` closed with EXECUTED evidence; status table row `Complete`. |

No orphaned requirements found — `.planning/REQUIREMENTS.md`'s status table maps only UP-04 to Phase 9.1, and both plans declared `requirements: [UP-04]`.

### Anti-Patterns Found

None. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers in the phase's modified files. The one Critical finding from code review (CR-01, redaction bypass on three failure paths) and two Warnings (WR-01 missing combined-tag lint gate, WR-02 duplicate computation) were fixed directly by the orchestrator and independently re-verified in this pass (see Behavioral Spot-Checks). Three lower-severity findings (WR-03 `isGLabAlreadyAbsent`'s broad `"404"` substring match, IN-01, IN-02) remain deferred with documented reasoning in `09.1-REVIEW-FIX.md` — all are pre-existing, explicitly-flagged known gaps whose fail-closed direction is safe (an unrecognized error is reported as a real failure, never silently swallowed), not newly-introduced defects, and do not block the phase goal.

### Human Verification Required

None. All must-haves resolved to VERIFIED via static analysis, live account-free test execution, and cross-referenced SUMMARY/REQUIREMENTS/ROADMAP evidence for the historical live-account run (which cannot and should not be re-triggered as part of routine verification).

### Gaps Summary

No gaps found. The phase goal — proving gitid's GitLab SSH-key upload/delete path against a real, authenticated GitLab account with the same rigor as Phase 9's GitHub validation — is genuinely achieved:

- The real mutation executed once (account `casto.dev`, resource ID `21497184`), was independently confirmed clean by the orchestrator via a direct `glab api user/keys` call, and was confirmed clean a second time after the code-review-fix re-run (still exactly 3 pre-existing keys, no residue).
- The safety machinery (exact-scope preflight, LIFO idempotent cleanup, delete-by-exact-recorded-ID, read-only final sweep) is proven correct by four account-free unit tests, all passing live in this verification.
- The one Critical code-review finding (a real redaction-bypass defect on three failure paths, which would leak the developer's home path and any token-shaped text on a future failed re-run) was fixed and independently re-verified — not just claimed fixed.
- `make lint`, the build-tag isolation, and the combined-tag collision-safety gate all pass live.
- UP-04, ONESHOT.md, and ROADMAP.md were updated honestly and consistently, with the requirement's own wording preserved byte-unchanged and the D-02 governing-decision rationale stated explicitly rather than silently reworded.

---

_Verified: 2026-08-31T14:04:03Z_
_Verifier: Claude (gsd-verifier)_
