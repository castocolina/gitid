---
phase: 9.1
reviewers: [codex-sol, xai-grok]
reviewed_at: 2026-08-31T11:18:22Z
plans_reviewed: [09.1-01-PLAN.md, 09.1-02-PLAN.md]
models:
  codex-sol: "openai/gpt-5.6-sol-fast (reasoning=low)"
  xai-grok: "xai/grok-4.6 (reasoning=low)"
model_sources:
  codex-sol: "pinned"
  xai-grok: "pinned"
---

# Cross-AI Plan Review — Phase 9.1 (Cycle 2)

> Note: `codex-sol` and `xai-grok` are configured reviewer instances (`review.reviewer_instances`)
> resolving through the `codex` and `opencode` base lanes respectively. They use different base
> CLIs, so no shared-adapter caveat applies.

> This is cycle 2. The plans were revised (commit `5cec95f`) to address cycle 1's 2 HIGH + 4
> actionable non-HIGH findings, all raised by the `xai-grok` lane (the `codex-sol` lane failed at
> the transport level in cycle 1 and produced no review). This cycle's prompt directed each
> reviewer to verify — not assume — that the revision genuinely closes those 6 findings, in
> addition to a fresh review of the plans as revised.

## Codex Review (codex-sol)

**LANE FAILED — no review obtained, same failure as cycle 1.** The `codex` CLI invoked with the
configured instance model `openai/gpt-5.6-sol-fast` again returned an API error before producing
any review content:

```
ERROR: {"type":"error","status":400,"error":{"type":"invalid_request_error","message":
"The 'openai/gpt-5.6-sol-fast' model is not supported when using Codex with a ChatGPT account."}}
```

This confirms cycle 1's diagnosis: it is an account/model-compatibility failure (the configured
model is not usable under the current Codex login's ChatGPT-account plan), not a workflow or
prompt-assembly defect — the `codex` binary is installed and detected, and the same prompt
succeeded when sent to the `opencode` lane. Fixing this is outside this review's scope (it
requires either switching `codex-sol`'s configured model in `.planning/config.json` to one
supported under the current Codex account tier, or re-authenticating Codex with an API key that
supports `gpt-5.6-sol-fast`). No findings were obtained from this lane; it contributes no votes to
the Consensus Summary below.

---

## OpenCode Review (xai-grok)

### 1. Summary

The revision is a real replan, not a citation-only patch. Cycle 1's six findings are folded into
executable partitions, grep gates, pinned contracts, and go/no-go options, and those mechanisms
match the live code they cite (`identity_upload.go:146-169`, GitHub `runRealBinary` at
`e2e/upload_real_account_e2e_test.go:320-331`, Makefile tag guard at `Makefile:218-233`, hermetic
exemption at `e2e/harness_test.go:695-720`). Residual risk is execution-time (live `glab` session,
keyring, JSON shape), which the plans now refuse to self-resolve. Overall risk: **LOW-MEDIUM**.

### 2. Strengths

- Identifier partition + comment-hygiene note so Task-2 greps cannot be defeated by comments
  naming Task-3/wave-2 symbols.
- `countUnscopedGLab` moved into Task 2; Task 3 reuses it; artifacts listed per-task.
- `glabTokenSelf` pinned (`json:"scopes"|"revoked"|"active"`, pointer bools), stdout-only
  `Output()`, `{`-trim + `json.Decoder`, live MATCHES/DIVERGES routed to 09.1-02 Task 1.
- Wave-2-only drivers; GitHub header at `e2e/upload_real_account_e2e_test.go:6` is still "only
  real-account surface" and is scheduled for a comment-only fix.
- Exit-0-only driver matches `register-key`: error only when `attempted > 0 && attempted ==
  failed` (`cmd/gitid/identity_upload.go:146-169`); GitHub sibling fatals on any `CombinedOutput`
  error (`e2e/upload_real_account_e2e_test.go:327-330`).
- Keyring / JSON mismatches are human decisions with enumerated remediations; A3 is not invented
  in-flight.
- `isGLabAlreadyAbsent` gap is fail-closed; GitHub matcher is `"404"` / `"not found"`
  (`e2e/upload_real_account_e2e_test.go:237-242`).
- Makefile edits match CR-13 (`KNOWN_BUILD_TAGS` at `Makefile:218`; vet lines `230-233`; `.PHONY`
  at `55`; GitHub target `687-693`).
- Success row from `UploadResultOKFmt` (`internal/tuikit/design.go:596`) + wiring
  (`cmd/gitid/wiring.go:1441`) matches the expected `Key key registered` output.
- Deletes only via `DeleteRecordedKey` -> `DeleteKey` + `isAllDigits`
  (`internal/uploader/inventory.go:227-248`, GitLab `ssh-key delete` at `273-274`).
- `--show-token` ban matches `glabTokenPattern` PAT-only (`internal/uploader/classify.go:33`).
- D-02 vs UP-04 wording handled by evidence note, not requirement rewrite.
- LIFO sweep template is real (`e2e/upload_real_account_e2e_test.go:408-429`).

### 3. Concerns

- **LOW — stale `read_first` lines for Combined label.** 09.1-02 still says `design.go` 660-668;
  `UploadRegistrationLabelCombined` is actually at **681-683**. Name-based follow still works;
  line-follow does not.
- **LOW — `glab ssh-key delete` interactivity.** Product argv is bare `ssh-key delete <id>`
  (`inventory.go:274`). If live `glab` prompts, the 90s timeout fatals and LIFO still drains. Not
  a protocol hole; worth capturing in SUMMARY if it happens.
- **LOW — Task 2 `<verify>` is live-network.** `make verify-upload-real-account-gitlab` on a
  Task-2-only commit is correct for HIGH-1; a blocked/unauth session must skip (already
  specified). No new ordering bug.
- **No new HIGH.** No leftover Task-2->Task-3 symbol dependency; JSON contract is pinned before
  parse; drivers are wave-2; keyring is go/no-go.

### 4. Suggestions

- Fix Combined-label `read_first` to `internal/tuikit/design.go:681-683` before execute.
- If delete hangs, record prompt/flag evidence; do not add `--yes` without a D-05 decision.
- Keep Task-2 commit greps before Task 3 lands; do not "helpfully" declare bookkeeping early.

### 5. Risk Assessment

**LOW-MEDIUM.** Safety protocol, tag isolation, and cycle-1 holes are specified against source.
Remaining risk is empirical (`glab` PAT/self JSON, keyring, delete UX), gated by skip + human
go/no-go, not silent proceed.

### 6. Cycle-1 Resolution Verdict

| Finding | Verdict | Evidence |
|---|---|---|
| **HIGH-1** `countUnscopedGLab` in Task 3 vs Task 2 sweep | **RESOLVED** | Declared in Task 2 (step 5b); Task 3 "NOT declared here"; artifacts per-task; Task-2 grep `func countUnscopedGLab` == 1; Task-2 body must not name Task-3 symbols (comment hygiene). Sweep needs that helper the way GitHub sweep calls `countUnscoped` at `e2e/upload_real_account_e2e_test.go:425`. |
| **HIGH-2** unpinned JSON contract | **RESOLVED** | Step 3a pins three fields/tags + pointer bools; 3b capture-then-confirm; 3c stdout-only, `{`-trim, `Decoder` not `Unmarshal`; mismatch -> named divergence -> 09.1-02 Task 1; Task 3 table covers noise/revoked/omit. |
| **MEDIUM-1** wave-1 drivers + GitHub "only surface" header | **RESOLVED** | Drivers reserved, not declared; zero-count grep at end of 09.1-01; 09.1-02 first-declares in the using commit. Header fix: live file still says "only real-account surface" (`e2e/upload_real_account_e2e_test.go:6`) — that is the defect the plan now edits, not an unfixed gap. |
| **MEDIUM-2** non-zero exit vs `register-key` | **RESOLVED** | 09.1-02 pins fatal-on-any-non-zero; cites `identity_upload.go:146-157` (full rule `167-169`); one-row GitLab => `attempted==1`; no second driver; grep bans `ExitError`/`ExitCode()`; matches `runRealBinary` `327-330`. |
| **MEDIUM-3** keyring / `GLAB_CONFIG_DIR` (A3) | **RESOLVED** | Wave 1 records only; forbids in-flight env widening; 09.1-02 Task 1 enumerates (i) re-auth plaintext (ii) named keyring vars (iii) no-go; SUMMARY must state occurred vs not. |
| **LOW** `isGLabAlreadyAbsent` wording | **RESOLVED** (as accept-with-capture) | Port GitHub markers `e2e/upload_real_account_e2e_test.go:241-242`; fail-closed documented; 09.1-02 captures real wording or states none; no widening. Matches T-09.1-23 accept. |

---

## Consensus Summary

Only one reviewer (`xai-grok`, via the OpenCode/Grok lane) produced a review; `codex-sol` failed
again at the transport level (same unsupported-model-for-account error as cycle 1) before reading
any plan content, so it contributes nothing to compare against. The findings below are therefore a
single independently source-grounded voice, not a cross-model consensus — treat with
correspondingly lower confidence than a normal 2+-reviewer agreement, though the findings do cite
concrete `file:line` evidence checked against the live repository, and this cycle's reviewer
explicitly verified (rather than assumed) each cycle-1 fix against source before declaring it
resolved.

### Agreed Strengths
Not applicable — only one reviewer completed. `xai-grok`'s reported strengths (identifier
partitioning with comment-hygiene enforcement, the pinned `glabTokenSelf` JSON contract with
live-capture confirmation, wave-2-only driver declarations, the exit-0-only contract matched
against `register-key`'s real code, the enumerated keyring go/no-go, and the fail-closed
`isGLabAlreadyAbsent` gap) stand on their own, unconfirmed by a second reviewer.

### Agreed Concerns
Not applicable — only one reviewer completed. All three concerns this cycle are LOW and none is a
carry-over from cycle 1:

- **LOW — stale `read_first` line numbers for `UploadRegistrationLabelCombined`** in
  09.1-02-PLAN.md (plan says `design.go:660-668`; actual location is `681-683`). Name-based lookup
  still resolves; a line-number-only follow would not.
- **LOW — `glab ssh-key delete` may prompt interactively**, which the 90s timeout would fatal
  through rather than silently mis-drain (LIFO sweep still completes); worth capturing as SUMMARY
  evidence if observed.
- **LOW — Task 2's `<verify>` is a live-network step**, correct per the HIGH-1 fix, with an
  already-specified skip path for a blocked/unauthenticated session.

### Divergent Views
Not applicable — no second reviewer to diverge from. The `codex-sol` lane's failure is a
configuration/account issue reproduced identically from cycle 1 (see its section above), not a
substantive disagreement with `xai-grok`'s findings.

### Cycle-over-Cycle Verdict

All 6 of cycle 1's findings (2 HIGH, 3 MEDIUM, 1 LOW) are verdicted **RESOLVED** by this cycle's
source-grounded reviewer, each with a specific mechanism and file:line citation checked against
the revised plan text and/or the live codebase. No new HIGH or actionable MEDIUM concerns were
raised this cycle; the three new findings are all LOW (one a stale documentation line-range
pointer, two are execution-time edge cases already handled by existing skip/timeout/capture
mechanisms in the plans, not plan defects requiring further plan changes).
