---
phase: 9.1
reviewers: [codex-sol, xai-grok]
reviewed_at: 2026-08-31T10:56:42Z
plans_reviewed: [09.1-01-PLAN.md, 09.1-02-PLAN.md]
models:
  codex-sol: "openai/gpt-5.6-sol-fast (reasoning=low)"
  xai-grok: "xai/grok-4.6 (reasoning=low)"
model_sources:
  codex-sol: "pinned"
  xai-grok: "pinned"
---

# Cross-AI Plan Review — Phase 9.1

> Note: `codex-sol` and `xai-grok` are configured reviewer instances (`review.reviewer_instances`)
> resolving through the `codex` and `opencode` base lanes respectively. They use different base
> CLIs, so no shared-adapter caveat applies.

## Codex Review (codex-sol)

**LANE FAILED — no review obtained.** The `codex` CLI invoked with the configured instance
model `openai/gpt-5.6-sol-fast` returned an API error before producing any review content:

```
ERROR: {"type":"error","status":400,"error":{"type":"invalid_request_error","message":
"The 'openai/gpt-5.6-sol-fast' model is not supported when using Codex with a ChatGPT account."}}
```

This is an account/model-compatibility failure, not a workflow or prompt-assembly defect — the
`codex` binary is installed and detected, and the same prompt succeeded when sent to a different
lane. The configured model in `.planning/config.json` (`review.reviewer_instances.codex-sol.model`)
is not usable with the current Codex login's ChatGPT-account plan. No findings were obtained from
this lane; it contributes no votes to the Consensus Summary below.

**Action needed (outside this review's scope to fix):** either switch `codex-sol`'s configured
model to one supported under the current Codex account tier, or re-authenticate Codex with an API
key that supports `gpt-5.6-sol-fast`.

---

## OpenCode Review (xai-grok)

### Document Type
GSD · plan (inline; not on disk under `.planning/phases/`)

### Files Read
- `e2e/upload_real_account_e2e_test.go` (Wave 8 template)
- `Makefile:55,218-233,675-693`
- `cmd/gitid/identity_upload.go:146-157`
- `internal/uploader/classify.go:33`
- `.planning/ONESHOT.md:129-141`

### 1. Summary

The two-wave split (read-only surface + bookkeeping, then one compiled-binary mutation) matches
Wave 8 and locked D-01–D-06. Safety (LIFO sweep, outstanding-ID map, fail-closed preflight, D-06
closure) is copied from proven code at `e2e/upload_real_account_e2e_test.go:189-451`. The
GitLab-specific work (PAT `personal_access_tokens/self`, `GLAB_CONFIG_DIR`, `GLab` identifier
prefix, Makefile `realaccountgitlab`) is the right delta. **Plan 01 Task 2 is not independently
compilable:** the round-trip body needs `countUnscopedGLab` (and related helpers) that Task 3
introduces, so a per-task commit will fail `make lint` / `verify-upload-real-account-gitlab`
after Task 2.

### 2. Strengths

- Checkpoint text matches D-01/D-02 (one key, `auth_and_signing`) and D-03 (identity-name scoping
  vs frozen D-07 `KeyTitle`).
- Makefile edits match CR-13: `KNOWN_BUILD_TAGS` at `Makefile:218` and `go vet -tags realaccount`
  at `:233`; hermetic guard stays on `e2e` only.
- Wrapper/env pattern is a correct `GLAB_CONFIG_DIR` port of `resolveRealGHConfigDir` /
  `writeRealGHWrapper` (`e2e/upload_real_account_e2e_test.go:65-117`).
- `--show-token` ban is justified: `glabTokenPattern` is `glpat-` only (`classify.go:33`).
- Wave 2 product path: constants (`UploadResultOKFmt` + `UploadRegistrationLabelCombined`), no
  `"Added SSH key."`, no engine call inside `runGLabProductPhase`.
- UP-04: D-02 over "per-registration-title API"; D-06 EXECUTED/SKIPPED/BLOCKED; ROADMAP
  Goal/criteria byte-unchanged.

### 3. Concerns

- **HIGH — Plan 01 Task 2 vs Task 3 compile order.** Task 2's sweep (`countUnscopedGLab`,
  baseline) is specified in Task 2; those symbols are added in Task 3. Wave 8 defines them in the
  same file before the test (`:277-291`, used at `:425-435`). A Task-2-only commit cannot pass
  Task 2's `<verify>`.
- **HIGH — `parseGLabTokenScopes` JSON contract unspecified.** Need `json:"scopes"` and a rule
  for extra `glab api` stdout (warnings, HTTP headers). Empty/`ok=false` skip is right; a brittle
  unmarshal will skip every live run (D-06 leaves UP-04 open).
- **MEDIUM — Plan 01 artifacts vs tasks.** `runRealGLabBinary` / `runGLabProductPhase` listed as
  wave-1 artifacts but implemented in 09.1-02; unused decls trip lint. GitHub file still says
  "only real-account surface" (`:5-6`) — not updated.
- **MEDIUM — `runRealGLabBinary` vs register-key exit contract.** Wave 8 fatals on any non-zero
  (`:327-330`). Plan 02 also wants the D-03 contract (`identity_upload.go:146-157`). With one
  GitLab row, success is exit 0; don't invent a second driver that accepts non-zero on success.
- **MEDIUM — A3 keyring.** Plan records a skip if `GLAB_CONFIG_DIR` is not enough; wave 2 still
  has no decided fallback (e.g. inherit keyring env). Belongs on the 09.1-02 go/no-go if wave 1
  hits it.
- **LOW — `isGLabAlreadyAbsent`.** Copying `"404"` / `"not found"` (`:237-242`) may miss GitLab
  wording; fail-closed is OK.

### 4. Suggestions

- Move `countUnscopedGLab` (and any sweep helpers Task 2 calls) into Task 2, or merge Task 2+3
  into one commit; keep the four unit tests in Task 3.
- After the live `glab api personal_access_tokens/self` capture, pin argv + Go struct tags in the
  plan before writing the parser.
- Wave 1: do not declare `runGLabProductPhase` until 09.1-02.
- On 09.1-02 Task 1, paste wave-1 SUMMARY fields (config-dir kind, scope JSON shape, baseline
  count, keyring yes/no) — not the planned defaults.
- Optional: one-line GitHub file doc comment that GitLab lives under `realaccountgitlab`.

### 5. Risk Assessment

**MEDIUM.** Protocol and threat model are sound and grounded in Wave 8. Residual risk is
execution-order (Task 2 won't compile) and unproven GitLab scope JSON — both can stall UP-04 via
skip, not silent mutation. After fixing helper placement and pinning the live `glab api` shape,
residual account risk is LOW (one key, delete-by-ID, LIFO sweep).

---

## Consensus Summary

Only one reviewer (`xai-grok`, via the OpenCode/Grok lane) produced a review; `codex-sol` failed
at the transport level (unsupported model for the current Codex account) before reading any
plan content, so it contributes nothing to compare against. The findings below are therefore a
single independently source-grounded voice, not a cross-model consensus — treat with
correspondingly lower confidence than a normal 2+-reviewer agreement, though the findings do cite
concrete `file:line` evidence from both the Wave 8 template and the plan text itself.

### Agreed Strengths
Not applicable — only one reviewer completed. `xai-grok`'s reported strengths (checkpoint text
matching D-01/D-02/D-03, correct Makefile CR-13 wiring, correct `GLAB_CONFIG_DIR` wrapper port,
justified `--show-token` ban, constant-composed product-path assertions, and an honest UP-04
closure discipline) stand on their own, unconfirmed by a second reviewer.

### Agreed Concerns
Not applicable — only one reviewer completed. The two HIGH findings below are `xai-grok`'s alone:

- **HIGH — Task 2/Task 3 compile-order defect in 09.1-01-PLAN.md.** Task 2's behavior/action
  sections reference `countUnscopedGLab` and the outstanding-ID bookkeeping types, but
  `artifacts_this_phase_produces` and Task 3's own `<action>` attribute those symbols to Task 3.
  If Task 2 is executed and verified as an independent, isolated step (per this project's TDD/
  atomic-commit discipline), its own `<verify>` (`make lint && make verify-upload-real-account-gitlab`)
  cannot pass without Task 3's symbols existing first.
- **HIGH — `parseGLabTokenScopes`'s JSON contract is unspecified in the plan.** The plan directs
  writing the parser only after confirming the real `glab api personal_access_tokens/self` shape
  live (Task 2, `<action>` item 3), which is the right hypothesis-before-implementation discipline,
  but does not pin the resulting Go struct tags or the extra-noise-in-stdout handling rule up
  front, leaving room for a parser mismatch that would fail the exact-scope preflight closed on
  every subsequent live run.

### Divergent Views
Not applicable — no second reviewer to diverge from. The `codex-sol` lane's failure is a
configuration/account issue (see its section above), not a substantive disagreement with
`xai-grok`'s findings.
