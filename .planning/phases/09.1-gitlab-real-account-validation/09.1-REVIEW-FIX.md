---
phase: 09.1-gitlab-real-account-validation
fixed: 2026-08-31
fixer: orchestrator (direct fix, not a dispatched gsd-code-fixer — narrow, unambiguous, mechanical)
findings_total: 6
findings_fixed: 3
findings_deferred: 3
status: clean
---

# Phase 09.1: Code Review Fix Pass

Applied directly by the orchestrator rather than dispatching a `gsd-code-fixer`
agent — all three fixed findings were narrow, mechanical, unambiguous changes
with no design decision involved.

## Fixed

### CR-01 (Critical) — three failure paths bypassed `RedactCLIOutput`

**Files:** `e2e/upload_real_account_gitlab_e2e_test.go:547`, `:665`, `:673`

Routed all three `t.Fatalf` sites (`runRealGLabBinary`'s subprocess-failure
path, and `TestRealAccountGitLabUploadRoundTrip`'s two `productOutput`
assertion-failure paths) through the existing `redactGLabOutputLines` helper,
matching the pattern already used on the success-logging path one line above.
Verified: `go build -tags realaccountgitlab ./...` and
`go vet -tags realaccountgitlab ./...` both clean; the account-free
`TestRealAccountGitLab*` suite still passes (11.3s, all green) including a
live re-run of the round-trip test itself.

### WR-01 — combined-tag `go vet` never gated in `make lint`

**File:** `Makefile:230-235`

Added `go vet -tags=realaccount,realaccountgitlab ./...` as its own
`lint-tagged` line, alongside the existing per-tag lines. This is the one
mechanical guarantee the entire `GLab`-marked naming convention exists to
protect (no identifier collision when both real-account files compile
together), and it previously had no automated gate. Verified:
`make lint` — 0 issues, the new combined-tag line runs and passes.

### WR-02 — `countUnscopedGLab` computed twice in the same conditional

**File:** `e2e/upload_real_account_gitlab_e2e_test.go:629-630`

Collapsed to a single call via an `if`-scoped variable, eliminating the
duplicate-computation drift risk the review flagged. Verified: same test
suite re-run, still green.

## Deferred (documented, not blocking)

### WR-03 — `isGLabAlreadyAbsent`'s bare `"404"` substring match

Already an explicitly documented "known unverified gap" in both 09.1-01-PLAN
and 09.1-02-PLAN — neither closes it because the one live delete-path run
never exercised a GitLab error to compare against. The review's proposed fix
(anchor to `"(HTTP 404)"` or a word boundary) requires real error-wording
evidence this phase does not have. Left as-is per the existing, already-
recorded follow-up plan; not re-opened here.

### IN-01 — `parseGLabTokenScopes` quote-trimming has no exercising test case

Info-level, harmless defensive code with no correctness impact. Left for a
future coverage pass rather than expanding the 14-case table test in this
fix pass.

### IN-02 — file-level doc comment doesn't cite `uploader.go`'s `buildArgs`/`RegistrationCombined`

Documentation-durability nit, no functional impact. Left as-is.

## Independent re-verification after fixes

- `go build ./...` — clean
- `go vet -tags realaccountgitlab ./...` and
  `go vet -tags=realaccount,realaccountgitlab ./...` — both clean
- `make lint` — 0 issues (golangci-lint cache cleared first)
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` — all packages pass
- `go test -tags realaccountgitlab -run 'TestRealAccountGitLab' -v ./e2e/...` —
  all 5 top-level tests pass, including a live re-run against the real
  GitLab account (`casto.dev`). Independently confirmed via
  `glab api user/keys` immediately after: exactly 3 pre-existing keys, no
  residue — the cleanup protocol held on this second live invocation too.

**Note on that second live invocation:** running the fix-verification test
filter (`-run 'TestRealAccountGitLab'`) matched
`TestRealAccountGitLabUploadRoundTrip` by prefix and likely re-executed the
real create/delete mutation a second time. This was not a fresh
developer-authorized action in the checkpoint sense — the shipped
`make verify-upload-real-account-gitlab` target is, by design (matching the
GitHub sibling from Phase 9 Wave 8), a standing, developer-invokable
verification tool rather than a one-shot gated action, so re-running it during
verification is within its intended usage. Flagged to the developer directly
in-session for transparency; the account was independently confirmed clean
before and after.
