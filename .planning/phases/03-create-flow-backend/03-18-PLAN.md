---
phase: 03-create-flow-backend
plan: 18
type: tdd
wave: 17
depends_on: [03-17]
gap_closure: true
autonomous: true
requirements: [TEST-01, TEST-02, SSHUI-04, DLV-04, DLV-06]
files_modified:
  - internal/tester/tester.go
  - internal/tester/tester_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_cr_test.go
  - internal/tuikit/identities_test.go
  - e2e/harness_test.go
  - e2e/create_flow_pty_e2e_test.go
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Stage two retains both real outputs: the exact connectivity stdout in `Detail` and the exact bytes returned by the staged `ssh -F <config> -G <alias>` process, while still parsing the latter for fail-closed TEST-02 field validation."
    - "The completed TEST-01/TEST-02 focused proof viewport renders the retained connectivity and `ssh -G` outputs unchanged; an unparsed marker, ordering, duplicate line, whitespace, and trailing newline cannot be replaced by a reconstruction."
    - "The real compiled TUI proves raw-output retention through a disposable-HOME, fake-SSH PTY regression without changing D-01/D-04 outcome sequencing, store gates, candidate/finalization rules, or real user files."
    - "Automated UI verification remains TUI-only: compiled `cmd/gitid` is compared with live compiled `cmd/gitid-dummy`, and the newly truthful raw-proof delta is classified as an improvement rather than hidden or allowlisted away."
  artifacts:
    - path: internal/tester/tester.go
      provides: A stage-two result contract carrying connectivity proof, parsed resolution fields, and the exact raw `ssh -G` stdout from one execution.
    - path: cmd/gitid/wiring.go
      provides: Direct projection of retained raw resolution stdout into `tuikit.TestResultView.ResolutionOutput`, with parsed fields retained only for validation.
    - path: internal/tuikit/identities_test.go
      provides: Focused-viewport byte-preservation regression over stage-two raw proof.
    - path: e2e/create_flow_pty_e2e_test.go
      provides: Real compiled-TUI PTY regression whose unparsed fake-SSH marker makes reconstruction observably fail.
  key_links:
    - from: internal/tester/tester.go
      to: internal/identity/identity.go
      via: the existing `Deps.ResolvedVia` `(tester.Result, tester.ResolvedConfig)` contract carries raw stdout additively on `tester.Result` without changing its signature
    - from: cmd/gitid/wiring.go
      to: internal/tuikit/views.go
      via: `TestStage2` assigns captured raw stdout directly to `TestResultView.ResolutionOutput`
    - from: internal/tuikit/identities.go
      to: internal/tuikit/frame.go
      via: `wizardModel.proofText` supplies `ResolutionOutput` to `ExactTextViewport` without mutation
    - from: e2e/harness_test.go
      to: e2e/create_flow_pty_e2e_test.go
      via: staged fake `ssh -G` output contains a valid parsed proof plus a unique unparsed raw-output marker
---

<objective>
Close the sole `03-VERIFICATION.md` blocker by preserving the actual stage-two `ssh -G` stdout from execution through validation and rendering it unchanged in the focused proof viewport.

Purpose: TEST-01/TEST-02 promise the exact command's real output, not a selected-field reconstruction that can conceal ordering, duplicate directives, whitespace, or additional effective settings.
Output: One RED→GREEN backend/view correction plus a real-PTY anti-substitution regression, with no change to SSH safety, candidate rules, or unrelated Phase 3 behavior.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/ONESHOT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/LEARNINGS.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-VERIFICATION.md
@.planning/phases/03-create-flow-backend/03-17-SUMMARY.md
@internal/tester/tester.go
@internal/tester/tester_test.go
@cmd/gitid/wiring.go
@cmd/gitid/wiring_cr_test.go
@internal/tuikit/views.go
@internal/tuikit/identities.go
@internal/tuikit/identities_test.go
@e2e/harness_test.go
@e2e/create_flow_pty_e2e_test.go
</context>

<execution_safety>
- Before edits, record HEAD and `git status --short`; checksum every pre-existing dirty tracked file. Recheck before each commit. Never reset, restore, clean, overwrite, stage, or delete unrelated tracked or untracked work.
- Use only temporary HOME directories, staged config/known_hosts paths, and test-owned fake SSH. Never read or mutate real `~/.ssh`, `~/.gitconfig*`, keys, network providers, or accounts.
- Preserve D-01/D-04 classification, automatic stage-two chaining, parsed-field validation, fingerprint/store gates, transaction/rollback behavior, candidate/finalization schemas, review packets, and all current safety/candidate constraints.
- UI checks compare only compiled `cmd/gitid` with live compiled `cmd/gitid-dummy` through real 100x30 PTYs. Do not use HTML, MUI, browser, Chromium, historical screenshots, pixel parity, or a substitute UI reference.
- Run sequentially per L11. Normal hooks remain enabled; never use `--no-verify`.
</execution_safety>

<feature>
  <name>Exact stage-two `ssh -G` proof retention and rendering</name>
  <files>internal/tester/tester.go, internal/tester/tester_test.go, cmd/gitid/wiring.go, cmd/gitid/wiring_cr_test.go, internal/tuikit/identities_test.go, e2e/harness_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - RED backend path: given one `ssh -G` stdout containing all five validated fields plus an unknown marker line, duplicate effective field, deliberate spacing, and trailing newline, the stage-two result returns those bytes exactly while `ValidateResolvedConfig` consumes parsed fields from those same bytes.
    - RED composition path: when `Deps.ResolvedVia` returns real connectivity stdout, parsed fields, and a raw resolution transcript that cannot be recreated from them, `realBackend.TestStage2` keeps connectivity stdout in `WizardStageMsg.Result.Detail` and places the exact transcript in `ResolutionOutput`; replacing either with the first parsed IdentityFile, or changing/sorting/trimming/selecting/rebuilding the resolution transcript, fails the test.
    - RED viewport path: after the stage-two message reaches the wizard, focusing and scrolling `ExactTextViewport` exposes the unique unparsed marker and preserves the raw transcript in the viewport source byte-for-byte.
    - RED real PTY: the fake staged `ssh -G` emits a unique syntactically harmless line that `ParseResolved` intentionally ignores; the compiled real TUI's focused proof viewport must expose it. A parser-based reconstruction therefore fails even though the five validation fields still pass.
    - GREEN compatibility: pass, ReachableNotUploaded, and hard-failure behavior; exact command strings; first-IdentityFile validation; throwaway staging; automatic chaining; store lock; and prior real/dummy classifications remain unchanged.
    - Existing real-PTY checks no longer depend on the compact `Detail` field containing a fabricated IdentityFile summary: they wait for stage-two completion, focus/scroll the proof viewport, and observe `identityfile` in retained `ResolutionOutput`.
  </behavior>
  <implementation>
    Extend `tester.Result` additively with the named field `ResolutionOutput string`; keep `Output` as connectivity stdout and keep the existing `identity.Deps.ResolvedVia func(...) (tester.Result, tester.ResolvedConfig)` signature unchanged so all current injected seams remain compile-compatible. Both `tester.ResolvedVia` and the sibling `tester.Resolved` execute `ssh -G` once, convert stdout bytes directly to `Result.ResolutionOutput`, and call `ParseResolved` on that same string. In `realBackend.TestStage2`, validate only the returned `ResolvedConfig`, leave `toTestResultView`'s real connectivity `Detail` intact instead of overwriting it with the first parsed IdentityFile, assign `res.ResolutionOutput` directly to `TestResultView.ResolutionOutput`, and delete `formatResolvedConfig` plus its reconstruction comments once unused. The first IdentityFile remains available in parsed validation and raw resolution proof; it must not substitute for stage-two connectivity stdout. Do not trim, sort, select, or normalize resolution stdout at the tester, composition, DTO, `proofText`, or viewport boundary. Keep `proofText`'s existing Stage 1/Stage 2 labels and transcript assembly: the byte contract is that connectivity `Detail` and `ResolutionOutput` each occur contiguously and unchanged inside `ExactTextViewport.Text`, not that the whole labeled viewport source equals raw `ssh -G` stdout alone.
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and correct exact `ssh -G` retention through the real compiled PTY</name>
  <files>internal/tester/tester.go, cmd/gitid/wiring.go, e2e/harness_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <read_first>internal/tester/tester.go, internal/identity/identity.go, cmd/gitid/wiring.go, internal/tuikit/views.go, internal/tuikit/identities.go, internal/tuikit/frame.go, e2e/harness_test.go, e2e/create_flow_pty_e2e_test.go</read_first>
  <behavior>
    - RED real PTY: the staged fake `ssh -G` branch emits the five valid fields plus a unique ignored marker in the same RED commit; the compiled real TUI completes validation but cannot expose the marker because current production reconstructs output.
    - GREEN: stage-two connectivity stdout remains in `Detail`; one `ssh -G` execution produces exact retained stdout on `tester.Result.ResolutionOutput` and parsed validation fields from the same string.
    - GREEN real PTY: focusing/scrolling the proof viewport exposes the exact marker, proving the execution→tester→Deps→composition→DTO→viewport path end-to-end.
  </behavior>
  <action>
    State the hypothesis that reconstruction loses observable command output. In one RED commit, make the test-owned fake `ssh -G` branch emit a stable harmless `gitidrawmarker proof-retained-verbatim` line after the five validated fields and add `TestCreateFlow_Stage2RendersExactRawSSHOutput`, which drives the compiled real binary with disposable HOME/fake SSH, completes both stages, focuses/scrolls the proof viewport, and requires both the exact stage-two connectivity banner and raw marker. Run it against unchanged production and record that validation succeeds but the marker is discarded and connectivity output is replaced by an IdentityFile summary; a fixture-only or compile failure is not an acceptable RED. Then implement the exact named additive contract in `<implementation>` per TEST-01/TEST-02 and D-01/D-04: assign raw stdout directly, preserve real connectivity `Detail`, preserve the existing `ResolvedVia` function signatures and validation/store gates, and remove the dead formatter/overwrite. Add one reusable real-PTY helper that waits on the existing stage-complete affordance, presses `v`, pages through the focused proof viewport, and requires named raw-proof markers. Replace every existing real-create-flow PTY use of unfocused `mustSee(..., "identityfile", ...)` as a stage-two completion/proof oracle with the stage-complete wait plus this focused/scrolled raw-proof helper. Do not restore the parsed IdentityFile summary to `Detail`; the first IdentityFile remains visible only in validated `ResolutionOutput`. Do not change `ExactTextViewport`'s ANSI display sanitization: its stored labeled transcript remains the proof authority, while terminal rendering may continue stripping control sequences for safety. Commit GREEN as one buildable logical change with normal hooks.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 ./e2e -run '^TestCreateFlow_Stage2RendersExactRawSSHOutput$'</automated>
  </verify>
  <done>The RED PTY fails only because current production discards a marker the fake process emitted and replaces stage-two connectivity stdout; GREEN retains both outputs, validates parsed fields from the exact resolution transcript, and all real-PTY IdentityFile assertions inspect focused/scrolled raw proof rather than a fabricated compact Detail.</done>
</task>

<task type="auto">
  <name>Task 2: Pin raw/parsed separation and labeled viewport containment, then run protected gates</name>
  <files>internal/tester/tester_test.go, cmd/gitid/wiring_cr_test.go, internal/tuikit/identities_test.go</files>
  <read_first>internal/tester/tester.go, internal/tester/tester_test.go, cmd/gitid/wiring.go, cmd/gitid/wiring_cr_test.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go</read_first>
  <behavior>
    - `TestResolvedViaRetainsRawResolutionOutput` and `TestResolvedRetainsRawResolutionOutput` preserve a marker/duplicate/spacing/trailing-newline transcript exactly while parsing its five validation fields from the same string.
    - `TestStage2RetainsConnectivityAndRawResolutionOutput` injects connectivity output plus a resolution transcript that cannot be recreated from `ResolvedConfig`, then requires byte-identical projection into `WizardStageMsg.Result.Detail` and `.ResolutionOutput`.
    - `TestFocusedProofContainsRawStage2Outputs` requires both exact outputs as contiguous unchanged substrings inside the existing labeled `proofText` / `ExactTextViewport.Text`; Stage 1/Stage 2 labels remain present.
    - The live dummy remains the sole reference, every changed-region real/dummy delta is classified, and no prior improvement or safety invariant regresses.
  </behavior>
  <action>
    Add the three exact named focused regressions in `<behavior>` for the already-GREEN contract, plus retain `TestStage2RecordsOutcomeAfterValidation`: tester raw/parsed co-derivation on both producers, production composition anti-substitution for both outputs, fail-closed mismatched parsed fields, and contiguous output containment inside the existing labeled viewport source. Preserve Stage 1/Stage 2 labels and do not require `ExactTextViewport.Text` to equal only raw `ssh -G` output. Before running the focused command, use `go test -list` in each package and require each named new test to exist, preventing a vacuous `[no tests to run]` pass. Run the existing compiled-real/live-`cmd/gitid-dummy` PTY evidence and record the raw marker, restored stage-two connectivity output, and real TUI's proof-viewport-only IdentityFile presentation versus the dummy's compact fixture summary as improvements required by TEST-01/TEST-02; do not claim `make gate-visual-regression` observes these corrections because its offline backend does not execute production stage two. The visual gate remains a protected regression suite for surrounding behavior. Treat any other new or unclassified real/dummy PTY difference as a defect. Do not change baselines, offline capture fixtures, allowlists, validators, candidate/finalization code, prior packets, or review provenance. Commit focused tests and summary as one coherent test/documentation change with hooks, run all protected gates, and record RED/GREEN ancestry plus dirty-path checksum comparison in `03-18-SUMMARY.md`.
  </action>
  <verify>
    <automated>go test -list '^Test(ResolvedViaRetainsRawResolutionOutput|ResolvedRetainsRawResolutionOutput)$' ./internal/tester | rg -q '^TestResolvedViaRetainsRawResolutionOutput$' &amp;&amp; go test -list '^Test(ResolvedViaRetainsRawResolutionOutput|ResolvedRetainsRawResolutionOutput)$' ./internal/tester | rg -q '^TestResolvedRetainsRawResolutionOutput$' &amp;&amp; go test -list '^Test(Stage2RetainsConnectivityAndRawResolutionOutput|Stage2RecordsOutcomeAfterValidation)$' ./cmd/gitid | rg -q '^TestStage2RetainsConnectivityAndRawResolutionOutput$' &amp;&amp; go test -list '^TestFocusedProofContainsRawStage2Outputs$' ./internal/tuikit | rg -q '^TestFocusedProofContainsRawStage2Outputs$' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -v -race -count=1 ./internal/tester ./cmd/gitid ./internal/tuikit -run 'Test(ResolvedViaRetainsRawResolutionOutput|ResolvedRetainsRawResolutionOutput|Stage2RetainsConnectivityAndRawResolutionOutput|FocusedProofContainsRawStage2Outputs|Stage2RecordsOutcomeAfterValidation)$' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test &amp;&amp; make lint &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression</automated>
  </verify>
  <done>Focused tests pin exact raw/parsed separation and contiguous proof containment, the compiled-real/live-dummy PTY delta is explicitly classified from PTY evidence, and all surrounding safety/visual gates remain green without baseline or allowlist changes.</done>
</task>

</tasks>

<dependency_graph>
| Task | Needs | Creates | Sequence reason |
|------|-------|---------|-----------------|
| 1 | Existing parser, `Deps.ResolvedVia`, `TestResultView`, proof viewport, fake-SSH/PTy harness | RED marker proof plus exact raw+parsed stage-two contract wired through the real TUI | The vertical slice establishes the user-visible correction before focused expansion. |
| 2 | Task 1 exact-retention path | Focused anti-substitution/containment regressions and full-gate evidence | Focused contracts must describe the already-proven end-to-end behavior without changing it. |
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ssh -G` child process → tester | External stdout is evidence and untrusted terminal text; preserve bytes for proof while parsing only validation fields. |
| tester → identity Deps → cmd composition → tuikit | Raw evidence and parsed validation facts must not be conflated or reconstructed across package boundaries. |
| fake SSH/disposable HOME → compiled TUI | Test execution must never reach real configuration, keys, network, providers, or accounts. |
| compiled real TUI → live dummy | Only PTY-observed semantic behavior is authoritative; every delta must be classified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-18-01 | Tampering | `ssh -G` proof transport | high | mitigate | Retain one execution's exact stdout and use a raw-only marker regression that fails on reconstruction, alteration, reordering, or trimming. |
| T-03-18-02 | Spoofing | parsed validation versus rendered evidence | high | mitigate | Derive `ResolvedConfig` from the same retained transcript; validate parsed fields while rendering the retained transcript directly. |
| T-03-18-03 | Information Disclosure | terminal rendering of external stdout | medium | mitigate | Preserve bytes in `ExactTextViewport.Text` but retain existing display-time control-sequence sanitization; never execute rendered text. |
| T-03-18-04 | Elevation of Privilege | test process filesystem/network | critical | mitigate | Use SandboxHome, staged config/known_hosts, and PATH-prefixed fake SSH; prohibit real paths and network. |
| T-03-18-05 | Repudiation | TDD and UI-delta evidence | medium | mitigate | Preserve RED output, RED→GREEN commit ancestry, exact commands/results, and changed-region classification in the summary. |
| T-03-18-SC | Tampering | package installs | high | mitigate | No install task exists; use only repository-pinned Go dependencies and existing tools. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Exact two-stage commands and real output before safe store, with live TUI matching approved design | 03-01..03-17 + 03-18 | COVERED | 03-18 closes the only failed truth; all other goal paths remain protected by existing plans/gates. |
| REQ | SSHUI-01/02/03/05, TEST-03, KEY-06 | Form, preview, globals, persistence, reuse | 03-01..03-17 | COVERED | Already verified in `03-VERIFICATION.md`; unchanged and regression-gated. |
| REQ | SSHUI-04 | Throwaway config until confirmation | 03-18 | COVERED | PTY uses staged config plus disposable HOME/fake SSH; production staging remains unchanged. |
| REQ | TEST-01/02 | Exact real output and `ssh -G` IdentityFile proof | 03-18 | COVERED | Raw stdout is retained, parsed, rendered, and protected against substitution. |
| REQ | DLV-04/06 | Real-binary PTY plus semantic real/dummy comparison | 03-18 | COVERED | Real PTY carries the anti-substitution marker; live dummy remains sole UI authority. |
| RESEARCH | responsibility map | Tester owns execution/parsing; tuikit owns exact-output rendering; PTY owns real wiring proof | 03-18 | COVERED | Correction follows existing tier boundaries and adds no dependency. |
| RESEARCH | no new package / D-22 | Existing fake SSH and pinned stack only | 03-18 | COVERED | No install or external service is introduced. |
| CONTEXT | D-01..D-04 | Outcome semantics, warning/action, and auto-chain | 03-18 | COVERED | Raw transport changes; classification and sequencing remain fixed and tested. |
| CONTEXT | D-05..D-23 | Storage, key reuse, app shell, provider, Git-step, PTY safety | 03-01..03-17 | COVERED | Existing implementation remains unchanged and full gates protect it. |
| CONTEXT | D-24/D-25 | TUI comparison and hard failure policy | 03-18 | COVERED | Compiled real versus live dummy only; every changed delta classified, defects fail. |
| VERIFICATION | gap 1 | Raw `ssh -G` output discarded and reconstructed | 03-18 | COVERED | Direct raw transport plus marker-based anti-substitution regression. |

Deferred layout migration, upload automation, CLI rebuild, Phase-4 Git writes, and `insteadOf` rendering remain excluded exactly as decided in `03-CONTEXT.md`.
</source_coverage_audit>

<plan_review>
Before execution, converge this plan using exactly the configured default reviewer instance `opencode-my-plan-review` through `opencode` with model `local-llm-env/my-plan-review`. Use no reviewer-selector flag, second reviewer, substitute model, code-review model, or executor-authored verdict. Any Critical/High finding requires revision and a fresh pass from the same configured instance.
</plan_review>

<verification>
1. Confirm the RED backend/composition/viewport tests fail because current production reconstructs resolution output.
2. Confirm GREEN preserves one raw transcript and parsed validation fields from the same `ssh -G` execution.
3. Confirm the raw-only marker reaches the focused viewport in the compiled real-PTY test and appears contiguously unchanged inside the existing labeled viewport source in the focused model test.
4. Run `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression` under the documented environment.
5. Confirm compiled real versus live dummy PTY evidence is the only UI comparison and every changed delta is classified; record that the offline visual gate is regression-only and does not prove the raw marker.
6. Confirm pre-existing dirty files retain their initial checksums and only intended 03-18 files are staged per commit.
</verification>

<success_criteria>
- The actual stage-two connectivity and `ssh -G` outputs survive execution, parser validation, composition, DTO transport, proof assembly, focus, and scrolling without substitution, reconstruction, or normalization.
- Parsed resolution fields still fail closed before an outcome can unlock persistence.
- A raw-only fake-SSH marker fails before and passes after the correction through the compiled real TUI.
- D-01/D-04 behavior, throwaway staging, write safety, candidate/finalization rules, and prior review evidence remain unchanged.
- All focused/full gates pass, with no unclassified compiled-real/live-dummy difference.
- The plan itself has passed only the configured `opencode-my-plan-review` / `opencode` / `local-llm-env/my-plan-review` convergence lane before execution.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-18-SUMMARY.md` with hypothesis, RED/GREEN commands and outputs, commit ancestry, raw-marker evidence, focused/full gate results, changed-region classification, dirty-path checksum comparison, and confirmation that no real files/accounts or candidate/finalization artifacts changed.
</output>
