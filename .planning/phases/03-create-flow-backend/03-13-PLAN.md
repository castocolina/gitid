---
phase: 03-create-flow-backend
plan: 13
type: tdd
wave: 12
depends_on: [03-12]
files_modified:
  - internal/tuikit/frame.go
  - internal/tuikit/frame_test.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - internal/tuikit/ceremony.go
  - e2e/create_flow_pty_e2e_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_test.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_packet.go
  - internal/screenshot/createflow_packet_test.go
  - cmd/gitid-evidence/main.go
  - cmd/gitid-evidence/main_test.go
  - .planning/phases/03-create-flow-backend/03-13-review-packet/
autonomous: true
gap_closure: true
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, SSHUI-04, SSHUI-05, TEST-01, TEST-02, TEST-03, KEY-06, DLV-04, DLV-06]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "At fixed 100x30 geometry, a developer can focus and navigate an explicit proof viewport to inspect the exact, untruncated command and raw output for completed stage 1 and completed stage 2, including the full effective User, Hostname, Port, IdentitiesOnly, and first IdentityFile proof. (TEST-01, TEST-02; D-01 through D-04, D-22)"
    - "The required evidence includes distinct completed PASS, ReachableNotUploaded, and hard-failure/retry states; warning and failure each prove their glyph, word, ANSI semantic color, raw output, and applicable retry/copy action through the real PTY. (DLV-04, DLV-06; D-01 through D-04, D-22, D-24, D-25)"
    - "Generate/reuse, manual-path reuse, approved TUI, approved HTML, mouse focus, Git form, proof, and confirm evidence are labeled only when the captured state contains that ScreenSpec's state-specific semantic marker; a route name alone never satisfies the marker gate. (KEY-06; D-10 through D-13, D-18, D-24, D-25)"
    - "The confirm screen lets the developer inspect the complete sentinel-delimited managed block, including both BEGIN and END sentinels and the full key path, without hidden bytes. (SSHUI-03, TEST-03; D-05, D-06, D-08)"
    - "Every packet screen has meaningful nonempty named region comparisons, deterministic canonical bytes, a validated canonical self-hash, and complete review provenance declared only after the raw independent reviews exist. (DLV-04; D-24, D-25)"
    - "Duplicate screen IDs and duplicate hashes fail unless a same-route interaction variant has an explicit base screen, interaction, state marker, and rationale; route ownership and state-marker mismatch always fail. (DLV-04; D-24, D-25)"
    - "Corrected committed source produces a new immutable packet under its own full SHA, and two fresh independent reviews of the same candidate inventory report zero open Critical/High findings before final publication. (D-24, D-25; L14)"
  artifacts:
    - path: "internal/tuikit/frame.go"
      provides: "Byte-preserving focusable exact-text viewport with visible page/offset cues"
    - path: "internal/tuikit/identities.go"
      provides: "Completed two-stage proof, warning/failure semantics, and restored frozen Git hint"
    - path: "internal/tuikit/ceremony.go"
      provides: "Inspectable full sentinel-delimited confirmation preview"
    - path: "e2e/create_flow_pty_e2e_test.go"
      provides: "Real 100x30 raw-PTY tests for viewport controls, proof bytes, semantic colors, and actions"
    - path: "internal/screenshot/createflow.go"
      provides: "Typed ScreenSpec registry with route, interaction, marker, and applicability contracts"
    - path: "internal/screenshot/createflow_packet.go"
      provides: "Canonical manifest, duplicate/route/marker rejection, and final provenance validation"
    - path: ".planning/phases/03-create-flow-backend/03-13-review-packet/<source-full-sha>/"
      provides: "New immutable evidence packet with declared raw UI and Codex reviews"
  key_links:
    - from: "internal/tuikit/identities.go"
      to: "e2e/create_flow_pty_e2e_test.go"
      via: "advertised viewport keys are injected as raw terminal bytes and reveal every exact proof segment"
      pattern: "ExactTextViewport|ProofViewport|PageUp|PageDown"
    - from: "internal/screenshot/createflow.go"
      to: "cmd/gitid-evidence/main.go"
      via: "one ScreenSpec registry supplies capture scripts, approved routes, interactions, and required semantic markers"
      pattern: "ScreenSpec|StateMarker|InteractionVariant|ReferenceRoute"
    - from: "internal/screenshot/createflow_regions.go"
      to: "internal/screenshot/createflow_packet.go"
      via: "nonempty normalized region records become mandatory declared packet members before review"
      pattern: "RegionDiff|Canonical|manifest_sha256"
    - from: ".planning/phases/03-create-flow-backend/03-13-review-packet/<source-full-sha>/REVIEW-PROVENANCE.json"
      to: "UI-REVIEW.raw.txt and CODEX-REVIEW.raw.txt"
      via: "candidate inventory hash plus exact prompt/output/tool/session hashes"
      pattern: "candidate_manifest_sha256|prompt_sha256|output_sha256|source_commit"
---

<objective>
Close the remaining Phase 3 evidence defects reproduced from candidate packet `56c83c47d33068d391184c5c7faddaf3fcb5fe6e`: expose complete proof and confirmation bytes at 100x30, bind every label to an actual semantic state, and publish a canonical provenance-complete packet only after two fresh independent reviews.

Purpose: Make D-24/D-25 visual evidence independently auditable without changing real configuration, external accounts, frozen invalid packets, dirty 03-09 evidence, debug files, unrelated worktrees, or deferred Phase 4/9 behavior.
Output: TDD-backed proof/review viewports, strict state-aware capture and packet validation, one new immutable 03-13 packet from corrected source, and two fresh hash-bound reviews.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/ONESHOT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/LEARNINGS.md
@.planning/REQUIREMENTS.md
@.planning/design/create-flow/FIELDS.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-12-PLAN.md
@.planning/phases/03-create-flow-backend/03-12-SUMMARY.md
@.planning/phases/03-create-flow-backend/03-12-review-packet/56c83c47d33068d391184c5c7faddaf3fcb5fe6e/UI-REVIEW.md
@.planning/phases/03-create-flow-backend/03-12-review-packet/56c83c47d33068d391184c5c7faddaf3fcb5fe6e/MANIFEST.json
@.planning/phases/03-create-flow-backend/03-12-review-packet/56c83c47d33068d391184c5c7faddaf3fcb5fe6e/EVIDENCE.json
@.planning/phases/03-create-flow-backend/03-12-review-packet/56c83c47d33068d391184c5c7faddaf3fcb5fe6e/REGION-DIFFS.json
@internal/tuikit/frame.go
@internal/tuikit/identities.go
@internal/tuikit/ceremony.go
@e2e/create_flow_pty_e2e_test.go
@internal/screenshot/createflow.go
@internal/screenshot/createflow_regions.go
@internal/screenshot/createflow_packet.go
@cmd/gitid-evidence/main.go

<interfaces>
- `PreviewBlock` currently applies `ansi.Truncate(..., "…")` to every long line. Preserve it for unrelated snapshot call sites; add a stateful exact-text viewport and use it only for proof and confirm-review content.
- `TestResultView` already carries `Command`, `Detail`, `ResolutionCommand`, and `ResolutionOutput`. The viewport renders these captured values directly; no presentation-layer command or proof reconstruction is permitted.
- `wizardModel` owns step-local state and key precedence. Add viewport focus/offset state there, below D7 Shift-arrow handling, with controls advertised in the contextual footer.
- Current `CreateFlowScreenIDs`, `approvedHTMLRoutesInternal`, and the publisher switch are parallel string tables. Replace them with one typed `ScreenSpec` registry consumed by capture, validation, region generation, and publication.
- `captureTUIScreen` currently drives `reuse-manual-path` with Right then Left while focus remains on the key-source toggle, returning to Generate. The corrected script must focus the picker/manual row, type and resolve a sandbox key path, then require its unique marker before capture.
- Approved HTML capture currently gates only on route text. Each approved route capture must also execute the declared interaction and assert the ScreenSpec's state marker in the same page before saving.
- Manifest self-hash means SHA-256 over canonical JSON with `manifest_sha256` empty, members sorted by path, fixed UTF-8 encoding, and no trailing newline. Expose this rule in schema/provenance and validate it from stored bytes.
</interfaces>
</context>

<reproduction_evidence>
All commands were read-only and ran against packet `56c83c47d33068d391184c5c7faddaf3fcb5fe6e`:

1. `python3 -c '... semantic-marker check for reuse states ...'` reported `reuse-key: reuse_selected=True manual_row=True`, but `reuse-manual: generate_selected=True manual_row=False`; the two approved-HTML sidecars were byte-identical.
2. `python3 -c '... completed-stage/exact-proof check ...'` reported stage 1 `running=True exact_ssh_T=False exact_ssh_G=False ellipsis=True resolved_fields=[]`; stage 2 reported `exact_ssh_T=False exact_ssh_G=False ellipsis=True resolved_fields=[identityfile]`.
3. `python3 -c '... semantic outcome inventory ...'` listed only eight success/in-progress IDs and reported `has_reachable_screen=False`, `has_failure_screen=False`, `warning_copy=False`, and `failure_retry=False`.
4. `python3 -c '... confirm sentinel visibility check ...'` reported `BEGIN=True END=False ellipsis=True full_summary_key=False`.
5. `python3 -c '... packet provenance/self-hash check ...'` reported `regions=0`, declared self-hash `d1e834...966da`, raw stored-manifest hash `46a97d...aca5bb`, and no declared review provenance/UI review or Codex output.
6. `python3 -c '... duplicate approved-HTML hash check ...'` found duplicate hashes for `mouse-focused-field`/`ssh-form-filled` and `reuse-key-vs-generate`/`reuse-manual-path`, with `explicit_variant_metadata=False`.
7. `python3 -c '... source mechanism check ...'` reported no `ExactTextViewport` or `StateSignature`, `PreviewBlock ansi.Truncate=True`, `buildRegionDiffsJSONPlaceholder=True`, and the Right+Left manual capture sequence still present.
</reproduction_evidence>

<execution_safety>
- Before RED work, record `git status --short`, HEAD, worktree inventory, and SHA-256 values for the dirty 03-09 evidence named in 03-12-PLAN. Recheck them after every task and before committing.
- Preserve all existing 03-09, 03-11, and 03-12 packet directories, `.planning/debug/`, `.planning/ui-reviews/`, `.planning/phases/05.7-*`, and unrelated worktrees byte-for-byte. Never clean, restore, overwrite, rename, stage, or commit them.
- Run all behavioral/capture work with disposable HOME, sandbox key fixtures, and the D-22 PATH-shim fake SSH. Do not read or write real `~/.ssh`, `~/.gitconfig*`, external accounts, GitHub keys, or network-smoke targets.
- Follow RED → GREEN → REFACTOR, but commit each coherent test+implementation change together per AGENTS.md. Never use `--no-verify`.
- Execute Tasks 1-3 sequentially in the current phase worktree per L11. Do not create or modify another worktree.
</execution_safety>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end exact completed proof and semantic outcome viewport</name>
  <files>internal/tuikit/frame.go, internal/tuikit/frame_test.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - RED tracer: a real 100x30 PTY completes stage 1, holds completed stage 2 behind the D-22 barrier, and lets the user focus/navigate a proof viewport that reveals the exact captured stage-1 command and raw output without an ellipsis replacing any byte.
    - RED completed stage 2: after the barrier releases, the same viewport exposes the exact stage-2 connectivity command/output plus `ResolutionCommand` and raw `ResolutionOutput`, including User, Hostname, Port, IdentitiesOnly, and first IdentityFile.
    - RED state semantics per D-01 through D-04: PASS, ReachableNotUploaded, and hard Failure each produce distinct completed real-PTY frames; ANSI assertions prove green `✓`, yellow `!` plus warning words/copy/action, and red `✗` plus failure/retry words/action.
    - RED interaction: viewport focus and page/offset controls are visible at 100x30, preserve D7 Shift-arrow precedence, clamp after resize, and expose the original exact bytes through raw PTY keystrokes.
    - RED D-19 contract: the real Git screen retains the real disabled reason and restores the frozen always-visible Continue hint required by 03-UI-SPEC/FIELDS; D-18 Skip remains functional.
  </behavior>
  <action>
    Start with one failing raw-PTY tracer that runs the real binary under disposable HOME and D-22 fake SSH at exactly 100x30. Capture completed stage 1 after its result has arrived, not `testRunning2` decoration alone; hold completed stage 2 at the external fake boundary so the stage-1 screen remains stable without changing D-04 auto-chain production behavior. Release the barrier, capture completed stage 2, focus the proof viewport, and inject its advertised raw navigation bytes until every expected command/output token is observed.

    Add an `ExactTextViewport`-style state/value API alongside `PreviewBlock`. It must retain the source string byte-for-byte, compute visible slices without mutating it, expose current line/column range and continuation cues, clamp offsets, and support fixed dimensions. Use it for the create-flow proof only. Store focus and offsets in `wizardModel`; route non-conflicting page/horizontal controls only while the viewport owns focus, advertise them in the footer, and leave D7 Shift+Left/Right as the higher-priority wizard chord per D-07/D-17.

    Render the captured `TestResultView` fields directly per TEST-01/02 and D-22: completed stage 1 command/raw output; completed stage 2 connectivity command/raw output; resolution command/raw output with the five validated effective values. No command rebuilding, summary-only proof, or ellipsis substitution is accepted. Expand tests to D-22 `pass`, `denied`, and `timeout`; assert the exact warning/failure glyph+word copy, ANSI Warning/Error style, copy/retry presence, and forbidden cross-state action absence per D-01 through D-04. Restore `wizardContinueHint` beside D-19's real disabled reason because the frozen design requires both rows; do not enable Continue or implement deferred Git writes.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... -run 'Test.*(ExactTextViewport|CompletedStage|ResolutionProof|ReachableSemantic|FailureSemantic|GitContinueHint)' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 180s ./e2e/... -run 'TestCreateFlow_.*(CompletedStage1ProofViewport|CompletedStage2ProofViewport|ReachableNotUploadedEvidence|HardFailureRetryEvidence|GitStepDisabledReason)'</automated>
  </verify>
  <done>A real developer at 100x30 can navigate every exact byte of both completed stages, warning and failure are independently recognizable and actionable, and the frozen Git hint coexists truthfully with D-19's disabled reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Truthful ScreenSpec capture and full confirmation review</name>
  <files>internal/tuikit/ceremony.go, internal/tuikit/identities_test.go, internal/screenshot/createflow.go, internal/screenshot/createflow_test.go, cmd/gitid-evidence/main.go</files>
  <behavior>
    - RED screen truth: each logical ID has one ScreenSpec with source surface, approved route, interaction steps, state marker, required semantic regions, and optional same-route variant metadata; capture fails when the marker is absent or belongs to another state.
    - RED reuse states per D-10 through D-13: generate/reuse shows Reuse selected and candidates/manual row; manual reuse focuses the manual input, types a sandbox key path, resolves it, and displays a unique resolved-manual marker on live and approved-applicable surfaces.
    - RED approved HTML: route capture executes the ScreenSpec interaction and waits for a state-specific DOM/text marker; route breadcrumb alone cannot authorize a packet member.
    - RED inventory: completed stage 1, completed stage 2, ReachableNotUploaded, and hard failure/retry are distinct required ScreenSpecs and captures.
    - RED confirm review: the 100x30 confirm screen exposes the complete summary/key path and full BEGIN-to-END managed block through a focusable review viewport with visible controls.
  </behavior>
  <action>
    Replace `CreateFlowScreenIDs`, `approvedHTMLRoutesInternal`, capture switches, and local route tables with one typed `ScreenSpec` registry. Each spec names logical ID, user-visible state, route owner, interaction script, unique state marker, required regions, applicability by live/approved-TUI/approved-HTML, and optional `VariantOf` plus nonempty rationale. Capture and publication must call the same `ValidateCapturedState(spec, text/DOM)` gate before saving. A matching route without the state marker fails; a marker from another route/state fails even when hashes are unique per D-24/D-25.

    Correct the D-10/D-11 interactions: generate/reuse must visibly select Reuse; manual reuse must move focus from the source toggle into the picker and manual input, type a sandbox public fixture path, resolve it, and assert the resolved path/fingerprint marker. For approved HTML, drive the actual declared interaction on the correct approved route and wait for its marker before screenshot. Where the approved source cannot express a Phase-3-only state such as D-02, emit an explicit non-applicability record tied to the owning decision instead of assigning an unrelated route.

    Add ScreenSpecs and scripts for completed stage 1, completed stage 2, D-02 ReachableNotUploaded, and hard failure/retry. Reuse Task 1's external barriers/modes; do not label a running frame as a completed outcome. Change the confirmation ceremony's exact-change area to the same byte-preserving viewport contract so both sentinel lines, full summary/key path, and all managed-block lines are reachable at 100x30. Add raw capture assertions for `# BEGIN gitid managed: acme`, `# END gitid managed: acme`, and the complete key path per D-05/D-06/D-08; leave write behavior unchanged.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... -run 'Test.*Confirm.*(Viewport|Sentinel|FullKeyPath)' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test.*(ScreenSpec|StateMarker|ManualReuse|GenerateReuse|ApprovedHTMLInteraction|CompletedStage|WarningFailure|ConfirmReview)'</automated>
  </verify>
  <done>Every evidence label is backed by its actual state and route marker, all required outcomes are capturable, and the complete confirmation block is reviewable at fixed geometry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Canonical packet finalization, duplicate policy, and fresh independent reviews</name>
  <files>internal/screenshot/createflow_regions.go, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go, .planning/phases/03-create-flow-backend/03-13-review-packet/</files>
  <behavior>
    - RED region evidence: REGION-DIFFS contains a nonempty record for every required screen and every ScreenSpec region, with normalized full text, hashes, equality/divergence result, and D-02/D-19 justification where applicable.
    - RED canonicalization: stored canonical manifest bytes reproduce the declared self-hash under the documented empty-self-field algorithm; unsorted members, altered encoding, trailing bytes, or a stale hash fail.
    - RED duplicate policy: duplicate `(surface, screen_id)` and duplicate hashes fail; the only hash exception requires same approved route plus explicit VariantOf, interaction, distinct state marker, and rationale, while any route/state mismatch fails before duplicate evaluation.
    - RED finalization: candidate evidence is immutable and reviewable before reviews; after both raw independent outputs exist, exact prompts, raw stdout/stderr, tool/model/session/time/exit provenance, and dispositions become declared hashed final members.
    - RED freshness: final publication fails if source HEAD changed after candidate capture, either review targets another candidate hash, either latest review has open Critical/High, or any final member is undeclared.
  </behavior>
  <action>
    Replace `buildRegionDiffsJSONPlaceholder` with region generation over every ScreenSpec. Extract all required named regions from live and applicable approved surfaces, normalize only disposable absolute prefixes/timestamps while retaining full commands, outputs, config values, ANSI semantic codes, and interaction markers, then hash and record both sides. Require at least one meaningful region per screen and reject empty required regions. REGION-DIFFS must enumerate every packet screen, including warning, failure, manual reuse, and confirm review per D-02/D-10/D-19/D-24.

    Formalize canonical packet serialization in one exported/tested routine: UTF-8 indented JSON, members sorted by normalized slash path, fixed field order from the schema, `manifest_sha256` empty during hashing, and no trailing newline. Generate and validate from that routine, document the rule in EVIDENCE/manifest schema, and add stored-byte self-hash tests. Validate ScreenSpec route+marker ownership before duplicate checks. Reject duplicate IDs and hashes by default; allow an approved-HTML same-route hash only when explicit variant metadata names its base, interaction, unique marker, and rationale. Never let that exception authorize a route or state mismatch.

    Implement two-stage finalization without circular provenance. First create an immutable candidate inventory from corrected committed source; its canonical candidate hash is the exact review target. Run a fresh clean-context UI audit and a separate fresh Codex review over the same candidate hash, recipes, FIELDS, 03-UI-SPEC, all panels/text, EVIDENCE, REGION-DIFFS, and prior findings per D-24/D-25. Preserve exact prompts and unedited stdout/stderr with executable/version/provider/model/session/timestamps/exit status. Then build a sibling final directory declaring and hashing the candidate manifest, prompts, raw review outputs, human-readable review files, REVIEW-PROVENANCE, EVIDENCE, REGION-DIFFS, and all panels; atomically rename only after full validation. Any Critical/High requires a test-first source fix, new commit SHA, new candidate, and two new reviews.

    Run candidate generation twice in separate processes and compare every canonical member. Publish only to `.planning/phases/03-create-flow-backend/03-13-review-packet/<new-full-source-sha>/`; never overwrite or supplement a prior packet. Do not update phase-completion state or claim success beyond this corrective plan.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test.*(RegionDiffCoverage|CanonicalManifest|SelfHash|DuplicateScreen|DuplicateHash|ExplicitVariant|RouteMarkerMismatch|ReviewProvenance|FinalPacket|FreshReviews|BlockingSeverity)' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test &amp;&amp; make lint &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression</automated>
    <human-check>Inspect only provenance and verdict binding: both raw independent reviews must target the same latest candidate/source SHA and explicitly close every reproduced finding with zero open Critical/High.</human-check>
  </verify>
  <done>A new immutable packet from corrected source has complete canonical evidence and two fresh declared independent reviews; all prior packets, dirty evidence, debug content, real user files, accounts, keys, and unrelated worktrees remain unchanged.</done>
</task>

</tasks>

<dependency_graph>
| Task | Needs | Creates | Sequence reason |
|------|-------|---------|-----------------|
| 1 | Existing TestResultView DTOs, D-22 fake SSH, reproduced proof/outcome gaps | Exact proof viewport and completed semantic states | Evidence capture must target behavior that is genuinely inspectable first. |
| 2 | Task 1 viewport/state behavior | Typed ScreenSpec registry, truthful interactions, full confirm review | Packet labels and capture scripts must bind to the corrected real states. |
| 3 | Tasks 1-2 committed and green | Canonical packet, nonempty region evidence, declared raw reviews | Hashes/reviews are valid only for stable corrected source and truthful captures. |
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Captured SSH process output → proof viewport | Exact security-relevant argv/output must remain byte-preserving and fully reachable at fixed geometry. |
| PTY/DOM interaction → ScreenSpec label | A route or asynchronous frame must not impersonate another logical state. |
| Approved source → comparison evidence | Historical routes and interaction variants must be owned, marked, and applicable. |
| Candidate files → canonical manifest | Region records, hashes, ordering, encoding, and self-hash must resist omission/substitution. |
| Candidate packet → independent reviewers → final packet | Raw reviews must bind to the exact candidate and become declared immutable members only after they exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13-01 | Tampering | ExactTextViewport | high | mitigate | Preserve source bytes, expose range cues, and raw-PTY-test navigation over complete captured fields. |
| T-03-13-02 | Spoofing | ScreenSpec capture | critical | mitigate | Route ownership, declared interaction, and unique state marker are all mandatory before save/publication. |
| T-03-13-03 | Spoofing | warning/failure evidence | high | mitigate | Separate D-22 modes plus ANSI glyph/word/color/action assertions on completed frames. |
| T-03-13-04 | Tampering | canonical manifest and region diffs | critical | mitigate | One serializer, stored-byte self-hash validation, complete region coverage, and undeclared-member rejection. |
| T-03-13-05 | Repudiation | UI/Codex reviews | high | mitigate | Exact prompt/raw-output/tool/session hashes bound to candidate/source/approval hashes. |
| T-03-13-06 | Information Disclosure | PTY/review artifacts | medium | mitigate | Disposable HOME, public sandbox fixtures, normalized temp prefixes, and no private-key material. |
| T-03-13-07 | Elevation of Privilege | HOME/accounts/worktrees | critical | mitigate | D-22 fake SSH only, no network/account commands, protected-path hashes, and current-worktree-only execution. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Exact two-stage create flow stored behind the approved design | 1-3 | COVERED | Corrected proof, confirm, truthful evidence, and review gate. |
| REQ | SSHUI-01/02/03 | Four reachable fields and exact recipe Host/confirm preview | 1-2 | COVERED | Existing field behavior retained; exact confirm viewport added. |
| REQ | SSHUI-04/05 | Throwaway testing and recipe globals remain safe | 1-3 | COVERED | D-22 sandbox only; full regression gates. |
| REQ | TEST-01/02 | Exact completed two-stage commands/output and ssh-G proof | 1-3 | COVERED | Focusable viewport, full fields, raw PTY, packet regions. |
| REQ | TEST-03 | Confirmed backed-up persistence | 2-3 | COVERED | Complete pre-write review; existing transaction unchanged and re-tested. |
| REQ | KEY-06 | Generate/reuse and manual-path reuse | 2-3 | COVERED | Actual interactions, markers, route/applicability metadata. |
| REQ | DLV-04/06 | Approval diff and per-screen real-binary PTY | 1-3 | COVERED | Expanded semantic inventory, region evidence, fresh reviews. |
| RESEARCH | responsibility map | Presentation owns viewport; e2e owns PTY; screenshot/build tooling owns packet | 1-3 | COVERED | No backend tier reassignment. |
| RESEARCH | D-22 pitfall | PATH-shim fake SSH, not in-Go substitution | 1-3 | COVERED | External barrier/modes retained. |
| RESEARCH | no new dependencies | Existing Go/TUI/PTY/capture stack only | 1-3 | COVERED | No installs. |
| CONTEXT | D-01..D-04 | Gate taxonomy, warning/copy, auto-chain | 1-3 | COVERED | Completed distinct PASS/warning/failure evidence. |
| CONTEXT | D-05..D-09 | Layout target, Include default, globals, collision | 1-3 | COVERED | Full confirm review plus regression suite; no migration UX. |
| CONTEXT | D-10..D-13 | Reuse picker/manual path/warn/algorithm breadth | 2-3 | COVERED | State-specific interactions and evidence. |
| CONTEXT | D-14..D-17 | Real shell, preview banner, shared backend-free UI | 1-3 | COVERED | Shared presentation remains backend-free. |
| CONTEXT | D-18/D-19 | Functional Skip and disabled Continue reason | 1-3 | COVERED | Frozen hint restored; Continue stays disabled; no Git writes. |
| CONTEXT | D-20/D-21 | Provider inference and custom endpoints | 1-3 | COVERED | Existing behavior retained in exact Host/confirm review. |
| CONTEXT | D-22/D-23 | Deterministic fake SSH; network smoke separate | 1-3 | COVERED | Offline fake only; D-23 network smoke not invoked. |
| CONTEXT | D-24/D-25 | Automated diff, two reviews, severity gate | 2-3 | COVERED | Canonical packet and fresh review convergence. |
| REVIEW | R-01 | Screen labels/routes lack state-specific proof | 2-3 | COVERED | ScreenSpec marker gate and interaction metadata. |
| REVIEW | R-02 | Running/clipped proof instead of completed exact outcomes | 1-3 | COVERED | Completed stage viewports and raw PTY evidence. |
| REVIEW | R-03 | Warning/failure evidence absent | 1-3 | COVERED | Required IDs, semantic ANSI/action checks, regions. |
| REVIEW | R-04 | Confirm preview hides sentinel/key bytes | 2-3 | COVERED | Full review viewport. |
| REVIEW | R-05 | Empty regions, unclear self-hash, missing declared reviews | 3 | COVERED | Formal canonicalization, complete regions, staged finalization. |
| REVIEW | R-06 | Duplicate IDs/hashes accepted too broadly | 2-3 | COVERED | Explicit variant-only exception after route/marker validation. |
| REVIEW | R-07 | Corrected source needs new packet and two fresh reviews | 3 | COVERED | New SHA directory and independent reruns. |

Deferred layout migration, Git writes, upload instructions/automation, CLI rebuild, and `insteadOf` rendering remain excluded exactly as 03-CONTEXT.md requires.
</source_coverage_audit>

<verification>
1. Record protected dirty/debug/packet/worktree state before execution and prove it byte-identical after each task.
2. Record each RED command and actual failure, then GREEN output, in `03-13-SUMMARY.md`; commit coherent test+implementation changes only.
3. Exercise exact viewport navigation with raw PTY bytes at 100x30 for completed stage 1, completed stage 2, warning, hard failure/retry, and full confirm review.
4. Prove warning/failure glyph, word, ANSI color, output, and allowed actions; prove no cross-state action leakage.
5. Run negative capture tests for wrong route, absent marker, another state's marker, mislabeled manual reuse, and running-state substitution.
6. Require nonempty REGION-DIFFS coverage for every ScreenSpec and reproduce the canonical manifest self-hash from stored bytes.
7. Run duplicate ID/hash tests, including explicit valid same-route variant and invalid mislabeled-route cases.
8. Generate corrected candidates twice, obtain two fresh independent reviews of the same candidate hash, then validate the final declared inventory before atomic publication.
9. Run `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression`; all exit zero.
10. Confirm no real HOME/config/key/account/network mutation and no change to any protected pre-existing artifact or unrelated worktree.
</verification>

<success_criteria>
- Exact completed stage-1 and stage-2 commands, raw outputs, and full resolution proof are navigable at 100x30 with no hidden bytes.
- ReachableNotUploaded and hard failure/retry each have distinct real-PTY and packet evidence with validated glyph/color/text/action semantics.
- Generate/reuse, manual reuse, approved routes, and interaction variants cannot be mislabeled because state markers are mandatory.
- The confirm review exposes the complete BEGIN-to-END sentinel block and full summary/key path.
- REGION-DIFFS is meaningful for every screen; canonical manifest self-hash and declared member hashes validate from stored bytes.
- Duplicate IDs/hashes fail except for an explicitly documented, correctly routed same-route interaction variant.
- Corrected source produces a new immutable packet with two fresh declared independent reviews and zero open Critical/High.
- Prior packets, dirty 03-09 evidence, debug files, unrelated worktrees, real user files, accounts, and GitHub keys are unchanged.
- The summary reports only corrective-plan execution and does not claim Phase 3 or milestone completion.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-13-SUMMARY.md` with RED/GREEN commands and real outputs, exact viewport/raw-PTY controls and observed proof bytes, ScreenSpec inventory and markers, region/canonicalization validation, candidate/final hashes, complete independent-review provenance and dispositions, and protected-path before/after hashes. Do not modify prior packet contents or mark Phase 3 complete.
</output>
