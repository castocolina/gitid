---
phase: 03-create-flow-backend
plan: 19
type: tdd
wave: 18
depends_on: [03-18]
gap_closure: true
autonomous: true
requirements: [TEST-03, DLV-04, DLV-06]
files_modified:
  - internal/tuikit/ceremony.go
  - internal/tuikit/ceremony_test.go
  - e2e/create_flow_pty_e2e_test.go
  - e2e/dummy_demo_e2e_test.go
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Before confirmation, the create-flow ceremony visibly includes the approved exact copy `Nothing has changed yet` adjacent to the exact-change preview in both the compiled real TUI and live compiled dummy."
    - "The copy is rendered by the shared backend-free ceremony path per D-17, not duplicated or conditionally injected by either binary."
    - "The no-backup state remains truthful, backup promises remain truthful, and no receipt or post-confirm state claims that nothing changed."
    - "Real and dummy 100x30 PTY workflows reach the ceremony and observe the exact copy before Enter confirms; no web, browser, HTML, MUI, screenshot, or historical artifact is used as evidence."
    - "All 03-18 raw `ssh -G` retention, focused proof viewport, fake-SSH marker, and PTY assertions remain intact and green."
  artifacts:
    - path: internal/tuikit/ceremony.go
      provides: One shared pre-confirm rendering of the approved exact sentence without weakening backup-state honesty.
    - path: internal/tuikit/ceremony_test.go
      provides: Exact-copy and state-boundary regression for ceremonies with and without backup paths.
    - path: e2e/create_flow_pty_e2e_test.go
      provides: Compiled-real-TUI pre-confirm PTY evidence in a disposable HOME while preserving 03-18 raw-proof coverage.
    - path: e2e/dummy_demo_e2e_test.go
      provides: Live compiled-dummy pre-confirm PTY evidence through the approved create workflow.
  key_links:
    - from: internal/tuikit/ceremony.go
      to: cmd/gitid/main.go
      via: the real app's create wizard renders the shared `ceremonyModel.view` through `tuikit`
    - from: internal/tuikit/ceremony.go
      to: cmd/gitid-dummy/main.go
      via: `dummytui.NewFixtureBackend` drives the same shared `tuikit` ceremony renderer
    - from: e2e/create_flow_pty_e2e_test.go
      to: internal/tuikit/ceremony.go
      via: raw keystrokes reach the real pre-confirm ceremony before any sandbox file exists
    - from: e2e/dummy_demo_e2e_test.go
      to: internal/tuikit/ceremony.go
      via: the live dummy walk reaches the same pre-confirm ceremony before its in-memory confirmation
---

<objective>
Close the active Phase 3 UAT copy-contract finding by restoring `Nothing has changed yet` to the shared confirmation ceremony and proving it through both compiled PTY workflows.

Purpose: The frozen Phase 2 create-flow contract requires this explicit pre-write assurance; Phase 3 cannot close while the real and dummy ceremony omit it.
Output: One RED→GREEN shared-render correction, an exact copy regression, and paired real/dummy PTY evidence, with all 03-18 raw-proof work and unrelated paths preserved.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/ONESHOT.md
@.planning/ROADMAP.md
@.planning/LEARNINGS.md
@.planning/UAT-AUDIT.md
@.planning/design/create-flow/FIELDS.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-18-PLAN.md
@.planning/phases/03-create-flow-backend/03-18-SUMMARY.md
@internal/tuikit/ceremony.go
@internal/tuikit/ceremony_test.go
@e2e/create_flow_pty_e2e_test.go
@e2e/dummy_demo_e2e_test.go
</context>

<execution_safety>
- Before edits, record HEAD and `git status --short`; checksum every pre-existing dirty tracked file. Recheck before each commit. Never reset, restore, clean, overwrite, stage, or delete unrelated tracked or untracked work.
- Use only test-owned disposable HOME directories and the existing fake SSH seam. Never read or mutate real `~/.ssh`, `~/.gitconfig*`, keys, network providers, or accounts.
- Preserve every 03-18 behavior and assertion: `tester.Result.ResolutionOutput`, raw/parsed separation, the unparsed fake-SSH marker, focused/scrolled proof viewport helpers, connectivity `Detail`, and all TEST-01/TEST-02 gates. This plan does not modify `internal/tester`, `cmd/gitid/wiring.go`, `internal/tuikit/identities.go`, `e2e/harness_test.go`, packets, baselines, allowlists, or proof artifacts.
- UI evidence is exclusively the compiled `cmd/gitid` and compiled `cmd/gitid-dummy` through real 100x30 PTYs. Do not use or modify web, browser, HTML, MUI, Chromium, screenshot, PNG, or historical design artifacts.
- Run sequentially per L11. Keep all hooks enabled; never use `--no-verify`.
</execution_safety>

<feature>
  <name>Approved pre-confirmation copy in the shared ceremony</name>
  <files>internal/tuikit/ceremony.go, internal/tuikit/ceremony_test.go, e2e/create_flow_pty_e2e_test.go, e2e/dummy_demo_e2e_test.go</files>
  <behavior>
    - State A with no backup path contains the byte-exact sentence `Nothing has changed yet` exactly once adjacent to the exact-change preview, while remaining explicit and truthful that no backup exists.
    - State A with one or more backup paths contains the same byte-exact sentence exactly once while retaining every `Backup →` path and the undo explainer.
    - Pending, failed, and successful receipt states do not render the pre-confirm sentence.
    - The compiled real create flow and live compiled dummy create flow both expose the sentence before the confirmation keystroke at 100x30.
    - Existing preview focus/scroll controls, button focus, row fit, backup semantics, and all 03-18 raw-proof behavior remain unchanged.
  </behavior>
  <implementation>
    Render the approved sentence from `ceremonyModel.view` on the shared state-A path per D-17. Keep it immediately adjacent to the bounded exact-change preview and within the existing 100x30 ceremony row budget; retain the viewport-control hint in both focused and unfocused states, all real or promised backup paths, the no-existing-backup truth, and the state-A button row. Do not add backend-specific copies. Do not render the sentence from pending, commit-error, or receipt branches. The phrase must appear with the exact capitalization and words shown in the approved contract; surrounding layout must not cause it to wrap, clip, or disappear in either PTY workflow.
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore exact state-A copy through the compiled real TUI</name>
  <files>internal/tuikit/ceremony.go, internal/tuikit/ceremony_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <read_first>internal/tuikit/ceremony.go, internal/tuikit/ceremony_test.go, e2e/create_flow_pty_e2e_test.go, .planning/design/create-flow/FIELDS.md, .planning/phases/03-create-flow-backend/03-UI-SPEC.md, .planning/phases/03-create-flow-backend/03-18-SUMMARY.md</read_first>
  <behavior>
    - RED unit: the exact sentence is absent from state A for both backup shapes, and the test separately proves it cannot leak into pending, failure, or receipt states.
    - RED PTY: the compiled real binary reaches the fresh-layout review ceremony with live sandbox config still absent, but the exact sentence is not visible before confirmation.
    - GREEN: one shared render change makes both focused contracts pass without dropping backup/no-backup truth, controls, preview, or button affordances.
    - GREEN PTY: the exact sentence is visible before Enter and the existing write/receipt assertions still pass afterward.
  </behavior>
  <action>
    State the hypothesis that the UAT failure is shared-render copy drift, not backend behavior. First add `TestCeremonyStateAShowsApprovedNothingChangedCopy` covering both nil and nonempty `Backups`, exact occurrence count, adjacency to the exact-change preview region, and absence from pending/error/receipt states. In the same RED commit, extend `TestCreateFlow_GitStepDisabledReasonAndConfirmWrite` to require the exact sentence after the ceremony heading and before its existing pre-confirm filesystem check and Enter; record a behavioral RED against unchanged production. Then implement the shared state-A rendering described in `<implementation>` per D-17 and the approved field/UI contract. Preserve the no-backup test's honesty requirement rather than deleting it, preserve backup promises and viewport controls, and keep the copy out of all post-confirm branches. Do not modify or weaken any 03-18 helper/assertion in this file; the raw-proof viewport remains a separate earlier stage. Commit GREEN with hooks.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit -run '^TestCeremony(StateAShowsApprovedNothingChangedCopy|StateAWithNoBackupsNeverClaimsOne)$' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 ./e2e -run '^TestCreateFlow_GitStepDisabledReasonAndConfirmWrite$'</automated>
  </verify>
  <done>The shared ceremony and compiled real TUI show the exact approved sentence before confirmation for a fresh target, backup semantics remain truthful, post-confirm states omit it, and the RED commit precedes the GREEN commit without altering 03-18 raw-proof behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Prove live-dummy parity and protect the 03-18 PTY path</name>
  <files>e2e/dummy_demo_e2e_test.go</files>
  <read_first>e2e/dummy_demo_e2e_test.go, e2e/create_flow_pty_e2e_test.go, cmd/gitid-dummy/main.go, cmd/gitid/main.go, .planning/phases/03-create-flow-backend/03-18-SUMMARY.md</read_first>
  <action>
    Extend `TestDummyDemo_LiveWalk` at the existing create ceremony heading to require the exact sentence before its confirmation Enter. Run that live compiled-dummy PTY test together with the compiled-real ceremony test, then rerun the 03-18 raw-marker PTY test unchanged. Record terminal evidence that the same shared sentence is visible in both workflows at 100x30, classify the correction as removal of a defect against the approved live dummy contract, and confirm there are no other changed or unclassified real/dummy differences. Do not add a browser or screenshot lane and do not edit the live dummy fixture/backend merely to satisfy the assertion. Run the protected repository gates and commit the PTY regression/evidence with hooks.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 ./e2e -run '^(TestCreateFlow_GitStepDisabledReasonAndConfirmWrite|TestDummyDemo_LiveWalk|TestCreateFlow_Stage2RendersExactRawSSHOutput)$' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test &amp;&amp; make lint &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression</automated>
  </verify>
  <done>Both compiled PTY workflows assert the exact pre-confirm copy, the unchanged 03-18 raw-marker test and full gates pass, and no unrelated source, proof, packet, baseline, allowlist, or user path changes.</done>
</task>

</tasks>

<dependency_graph>
| Task | Needs | Creates | Sequence reason |
|------|-------|---------|-----------------|
| 1 | Shared ceremony, approved copy contract, existing real create PTY | RED→GREEN shared rendering plus compiled-real proof | The vertical slice proves the user-visible correction through production before parity expansion. |
| 2 | Task 1 shared render | Live-dummy PTY assertion, paired evidence, protected full-gate result | Dummy parity must exercise the exact already-corrected shared path, not introduce a second implementation. |
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ceremony state → terminal user | Copy must describe the actual pre-confirm mutation boundary and must not survive into states where a write is pending, failed, or complete. |
| disposable HOME/fake SSH → compiled real TUI | PTY evidence must remain hermetic and cannot touch real SSH/Git files, keys, network, or accounts. |
| shared tuikit → real and dummy binaries | One backend-free renderer must supply identical contractual copy without binary-specific drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-19-01 | Spoofing | pre-confirm ceremony copy | high | mitigate | Assert the sentence only while state A is active and retain direct filesystem non-mutation proof before confirmation. |
| T-03-19-02 | Tampering | shared real/dummy copy path | medium | mitigate | Render once in `ceremonyModel.view` and drive both compiled binaries through PTY assertions. |
| T-03-19-03 | Elevation of Privilege | real-binary PTY environment | critical | mitigate | Keep SandboxHome, fake SSH, and existing no-write-before-confirm assertions; prohibit real paths and external services. |
| T-03-19-04 | Repudiation | TDD and PTY evidence | medium | mitigate | Preserve behavioral RED output, RED→GREEN ancestry, exact focused commands/results, and real/dummy classification in `03-19-SUMMARY.md`. |
| T-03-19-SC | Tampering | package installs | high | mitigate | No install task exists; use only repository-pinned Go dependencies and existing tools. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | End-to-end create with exact test/store ceremony and live TUI matching approved design | 03-01..03-18 + 03-19 | COVERED | 03-19 closes the sole active copy defect; prior goal paths remain protected. |
| REQ | SSHUI-01..05, TEST-01/02, KEY-06 | Form, test, reuse, globals, exact proof | 03-01..03-18 | COVERED | Already implemented and verified; 03-19 explicitly preserves them, including all raw-proof work. |
| REQ | TEST-03 | Confirmation before persistence | 03-19 | COVERED | Exact pre-write assurance is visible before the existing confirmation and sandbox write assertions. |
| REQ | DLV-04/06 | Semantic approved-TUI match and compiled-real PTY | 03-19 | COVERED | Compiled real and live dummy PTYs observe the shared correction; no web artifacts. |
| RESEARCH | responsibility map / backend-free presentation | 03-19 | COVERED | Copy stays in shared `internal/tuikit`; no backend or new dependency is added. |
| CONTEXT | D-05/D-06 | Resolved target and fresh Include layout | 03-19 | COVERED | Real PTY uses the existing fresh-layout ceremony and preserves target behavior. |
| CONTEXT | D-17 | One shared backend-free presentation source | 03-19 | COVERED | One renderer supplies both binaries. |
| CONTEXT | D-22 | PATH-shim fake SSH PTY safety | 03-19 | COVERED | Existing hermetic real-PTY setup remains unchanged. |
| CONTEXT | D-24/D-25 | TUI comparison and hard defect policy | 03-19 | COVERED | The omission is classified and corrected; any other new/unclassified delta fails. |
| UAT | item 1 | Approved `Nothing has changed yet` copy omitted | 03-19 | COVERED | Exact unit and paired compiled-PTY assertions close the active finding. |

Deferred layout migration, upload automation, CLI rebuild, Phase-4 Git writes, and `insteadOf` rendering remain excluded exactly as decided in `03-CONTEXT.md`. UAT items 2 and 3 are stale closure bookkeeping, not corrective implementation scope; ROADMAP/STATE freshness and current code/UI review are separate closeout work and are not silently folded into this copy-only plan.
</source_coverage_audit>

<plan_review>
Before execution, converge this plan using only the configured default reviewer instance `opencode-my-plan-review` through `opencode` with model `local-llm-env/my-plan-review`. Use no reviewer selector flag, second reviewer, substitute model, code-review model, or executor-authored verdict. Revise for any Critical/High or actionable finding and rerun the same configured instance until it reports zero.
</plan_review>

<verification>
1. Confirm the exact-copy unit and real-PTY assertions fail behaviorally before production changes.
2. Confirm one shared state-A render change passes both backup shapes and excludes pending/error/receipt states.
3. Confirm compiled real and live dummy 100x30 PTYs each expose the exact sentence before confirmation.
4. Confirm `TestCreateFlow_Stage2RendersExactRawSSHOutput` and the full test/lint/e2e/copy/visual gates remain green without modifying 03-18 proof behavior or artifacts.
5. Confirm only the four declared implementation/test files changed and all pre-existing dirty/untracked paths retain their initial state.
</verification>

<success_criteria>
- The approved exact sentence is visible once, adjacent to the preview, before confirmation in both compiled workflows.
- Backup and no-backup truth remains explicit; the sentence never appears after confirmation begins.
- One shared backend-free renderer owns the copy per D-17.
- Exact unit coverage and paired PTY evidence fail before and pass after the correction.
- All 03-18 raw-proof work and unrelated paths remain intact and green.
- Plan convergence uses only `opencode-my-plan-review` / `opencode` / `local-llm-env/my-plan-review` and records zero open Critical/High or actionable findings.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-19-SUMMARY.md` with hypothesis, behavioral RED/GREEN commands and outputs, commit ancestry, exact-copy location/state coverage, compiled-real/live-dummy PTY evidence, 03-18 raw-marker recheck, full-gate results, delta classification, and dirty-path checksum comparison.
</output>
