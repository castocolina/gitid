---
phase: 03-create-flow-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/identity/identity.go
  - internal/identity/modes.go
  - internal/identity/modes_test.go
  - internal/keygen/keyscan.go
  - internal/keygen/keyscan_test.go
  - internal/tester/tester.go
  - internal/tester/tester_command_test.go
autonomous: true
requirements: [KEY-06, TEST-01, TEST-02]
must_haves:
  truths:
    - "Reuse of an encrypted private key that already has a matching .pub sibling succeeds without any passphrase prompt"
    - "Scanning ~/.ssh returns each parseable private key with its algorithm and SHA256 fingerprint, and never aborts the whole scan on one unparseable/encrypted key"
    - "The stage-2 by-alias command string shown to the user is byte-identical to the argv tester.ResolvedVia actually executes"
  artifacts:
    - path: "internal/identity/modes.go"
      provides: "ensurePub reads the existing .pub directly (no re-derive) when present — encrypted-key-with-.pub reuse (D-11)"
      contains: "ReadPub"
    - path: "internal/keygen/keyscan.go"
      provides: "ScanReusableKeys — filename + algorithm + fingerprint enumeration of ~/.ssh (D-10/D-13)"
      contains: "func ScanReusableKeys"
    - path: "internal/tester/tester.go"
      provides: "ResolvedViaCommand — display-only stage-2 command string, argv-parity with ResolvedVia (Pitfall 7)"
      contains: "func ResolvedViaCommand"
  key_links:
    - from: "internal/identity/modes.go ensurePub"
      to: "deps.ReadPub"
      via: "PubExists==true branch reads existing .pub instead of DerivePub"
      pattern: "ReadPub"
    - from: "internal/tester/tester.go ResolvedViaCommand"
      to: "the same arg-builder ResolvedVia uses"
      via: "shared preWrite/resolved arg slice, exec.Command(...).String() without executing"
      pattern: "ResolvedViaCommand"
---

<objective>
Close the three small, load-bearing UI-free backend GAPS the create-flow wiring
depends on — all pure functions with defined I/O, none touching the TUI. These
are the only genuinely-new backend code in Phase 3; every other capability
already exists as tested substrate.

1. Fix `internal/identity.ensurePub` so reusing an ENCRYPTED private key that
   already has a matching `.pub` succeeds (KEY-06 / D-11 / RESEARCH Pitfall 2).
2. Add `internal/keygen.ScanReusableKeys` — the D-10 key-reuse picker's backend
   (scan `~/.ssh`, parse, fingerprint, skip-not-abort on unparseable keys).
3. Add `internal/tester.ResolvedViaCommand` — a display-only stage-2 command
   string with guaranteed argv-parity to `ResolvedVia` (TEST-01 "shown command
   == run command", RESEARCH Pitfall 7).

Purpose: give Wave 2+ the exact seams the real create-flow Backend wires,
proven test-first, so the TUI layer never hand-rolls key parsing or command
strings.
Output: three tested functions + their RED-first tests. No UI, no file mutation
of user config.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@recipes/README.md
@.planning/ROADMAP.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/phases/03-create-flow-backend/03-PATTERNS.md
</context>

## Artifacts this phase produces (plan 03-01)

New/changed symbols this plan introduces (exclude from source-drift flagging):

- `internal/identity.Deps.ReadPub func(pubPath string) (pubLine string, err error)` — new injected seam field
- `internal/keygen.ScanReusableKeys(sshDir string) ([]ReusableKey, error)` — new function
- `internal/keygen.ReusableKey` struct (fields: `Path`, `Algorithm`, `Fingerprint`, `HasPub`, `Encrypted`, `ParseError`) — new type
- `internal/tester.ResolvedViaCommand(configPath, keyPath, alias string) string` — new function

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix ensurePub for encrypted-key-with-existing-.pub reuse (KEY-06 / D-11)</name>
  <files>internal/identity/identity.go, internal/identity/modes.go, internal/identity/modes_test.go</files>
  <read_first>
    - internal/identity/modes.go (the exact ensurePub function, lines ~40-65 — read current state, do not assume)
    - internal/identity/identity.go (the Deps struct, ~line 117-160 — PubExists/DerivePub/WritePub seams and their doc contract)
    - internal/keygen/derive.go (DerivePublicKey — confirm it calls ssh.ParsePrivateKey, which errors on an encrypted key with no passphrase)
    - .planning/phases/03-create-flow-backend/03-RESEARCH.md (Common Pitfall 2 — the exact failure mode and required fix)
    - .planning/phases/03-create-flow-backend/03-PATTERNS.md (the ensurePub section — current code + required change + error-wrapping convention)
  </read_first>
  <behavior>
    - RED: a table test in modes_test.go drives Reuse (or ensurePub directly) with PubExists==true and a DerivePub stub that returns an error (simulating ssh.ParsePrivateKey failing on an encrypted key). It asserts the returned pubLine equals the EXISTING .pub content and NO error is returned. Against today's code this FAILS (ensurePub always calls DerivePub in the PubExists branch).
    - GREEN: when deps.PubExists(pubPath) is true, ensurePub reads the existing .pub via a new deps.ReadPub seam and returns it verbatim — DerivePub is NOT called on this path.
    - Edge: when the .pub is ABSENT, behavior is unchanged — DerivePub is called then WritePub persists it (only passphraseless keys are supported on this path, matching the DerivePublicKey doc contract).
    - Edge: deps.ReadPub returning an error is wrapped as `fmt.Errorf("identity: reading existing public key %s: %w", pubPath, err)` and propagated.
  </behavior>
  <action>
    Add a `ReadPub func(pubPath string) (pubLine string, err error)` field to `identity.Deps`, documented alongside the existing PubExists/DerivePub/WritePub reuse-flow seams. In `ensurePub` (modes.go), change ONLY the `deps.PubExists(pubPath)==true` branch: call `deps.ReadPub(pubPath)` and return its result instead of `deps.DerivePub(privateKeyPath, comment)`. Leave the `.pub`-absent ELSE branch exactly as-is (DerivePub then WritePub). Keep the package-prefixed `fmt.Errorf("identity: <verb>: %w", err)` wrapping convention used throughout modes.go. Follow the project RED-stub convention (a `_`-parameter failing stub so the RED state passes the hard-fail lint without `--no-verify`; TDD drives authoring order, RED/GREEN need not be separate commits). Do NOT wire the real ReadPub implementation here — that belongs to the cmd/gitid composition root in plan 03-03; this plan only adds the seam + the pure-function branch + fake-driven tests.
  </action>
  <verify>
    <automated>go test -race ./internal/identity/... -run 'TestReuse|TestEnsurePub'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/identity.Deps` has a `ReadPub func(pubPath string) (string, error)` field (grep: `grep -n 'ReadPub' internal/identity/identity.go` returns a match)
    - ensurePub's PubExists==true branch calls `deps.ReadPub` and does NOT call `deps.DerivePub` (grep the PubExists branch body)
    - `go test -race ./internal/identity/...` passes, including a new test proving reuse succeeds when DerivePub would error but ReadPub returns the existing line
    - `make lint` clean (no `--no-verify`)
  </acceptance_criteria>
  <done>Reusing an encrypted key whose .pub exists returns the existing .pub line with no passphrase prompt and no private-key parse; the .pub-absent path is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScanReusableKeys — key-reuse picker backend (KEY-06 / D-10 / D-13)</name>
  <files>internal/keygen/keyscan.go, internal/keygen/keyscan_test.go</files>
  <read_first>
    - internal/keygen/derive.go (DerivePublicKey — the ssh.ParsePrivateKey parse pattern + `fmt.Errorf("keygen: ...: %w", err)` wrapping to mirror)
    - internal/identity/inventory.go (listKeyFilesReal — the ~/.ssh glob/enumerate pattern that excludes .pub siblings, ~lines 200-208)
    - internal/keygen/catalog.go (AlgoInfo / algorithm token vocabulary — to label the scanned key's algorithm consistently)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-10, D-12, D-13 — picker requirements: filename + algorithm + fingerprint, manual-path row, in-use labelling is the TUI's job, any-parseable-key never blocks)
    - .planning/phases/03-create-flow-backend/03-RESEARCH.md (Open Question 3 recommendation: extend internal/keygen, use ssh.FingerprintSHA256 — no new dependency)
  </read_first>
  <behavior>
    - RED: keyscan_test.go seeds a temp dir with (a) a valid unencrypted ed25519 private key + its .pub, (b) a valid rsa key without a .pub, (c) an encrypted key with a .pub sibling, (d) a non-key file, (e) a `.pub`-only file. ScanReusableKeys returns entries for a/b/c only, each with a populated Algorithm and Fingerprint where parseable; the encrypted key (c) is present but flagged Encrypted with HasPub=true and no fingerprint; the non-key/.pub-only files are skipped; the whole call returns without error. Against no implementation this FAILS to compile/run (RED).
    - GREEN: implement the scan.
    - Edge: an unparseable/garbage file does not abort the scan — it is skipped (D-13: any parseable key is offered; unparseable is not a hard error).
    - Edge: a missing sshDir returns an empty slice + nil error (nothing to reuse), not an error.
  </behavior>
  <action>
    Add `internal/keygen/keyscan.go` with `func ScanReusableKeys(sshDir string) ([]ReusableKey, error)` and a `ReusableKey` struct carrying `Path string`, `Algorithm string`, `Fingerprint string`, `HasPub bool`, `Encrypted bool`, `ParseError error`. Enumerate sshDir the way inventory.go's `listKeyFilesReal` does (private-key candidates, exclude `.pub` siblings). For each candidate, `os.ReadFile` then `ssh.ParsePrivateKey`; on success set Algorithm from the parsed key type and Fingerprint via `ssh.FingerprintSHA256(signer.PublicKey())`; set HasPub from a `.pub` sibling stat. On an encrypted-key parse error (detected via `*ssh.PassphraseMissingError`) set Encrypted=true and keep the entry (with HasPub from the sibling) but no fingerprint; on any OTHER parse error record ParseError and SKIP the entry (do not append it and do not abort the scan). Use the `fmt.Errorf("keygen: <verb>: %w", err)` wrapping convention only for genuine I/O failures that should abort (e.g. an unreadable directory). Do NOT scan for or read the private-key BYTES into any returned field — never surface key material. Do NOT exec anything (pure parse). This is the picker's data source only; the TUI (plan 03-04) renders it and adds the in-use labelling (D-12) and manual-path row.
  </action>
  <verify>
    <automated>go test -race ./internal/keygen/... -run TestScanReusableKeys</automated>
  </verify>
  <acceptance_criteria>
    - `internal/keygen.ScanReusableKeys` and `internal/keygen.ReusableKey` exist and are exported
    - The test proves: parseable keys are returned with algorithm+fingerprint; an encrypted-with-.pub key is returned flagged Encrypted (no fingerprint, no crash); an unparseable file is skipped (not returned, no scan abort); a `.pub`-only or non-key file is skipped
    - No `exec.Command` and no field carrying private-key bytes anywhere in keyscan.go (grep-negative)
    - `make lint` clean
  </acceptance_criteria>
  <done>ScanReusableKeys enumerates ~/.ssh, returns each usable key with algorithm + SHA256 fingerprint, tolerates encrypted and unparseable keys without aborting, and never surfaces private-key material.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ResolvedViaCommand — stage-2 display/exec argv parity (TEST-01/02 / Pitfall 7)</name>
  <files>internal/tester/tester.go, internal/tester/tester_command_test.go</files>
  <read_first>
    - internal/tester/tester.go (PreWriteCommand ~lines 61-70 — the exact display-only pattern to mirror; ResolvedVia ~lines 149-164 — the argv it actually builds and executes)
    - .planning/phases/03-create-flow-backend/03-PATTERNS.md (the stage2 command helper section — same-file sibling-function pattern)
    - .planning/phases/03-create-flow-backend/03-RESEARCH.md (Pitfall 7 — the dummy's hand-built stage2 string vs the real ResolvedVia argv; TEST-01 "shown command == run command" contract)
  </read_first>
  <behavior>
    - RED: tester_command_test.go asserts `ResolvedViaCommand(configPath, keyPath, alias)` returns a string equal to `exec.Command("ssh", <the exact args ResolvedVia builds>).String()` for the connectivity call — extract or share the arg-builder so the test compares against the SAME slice ResolvedVia executes, not a re-typed literal. Against no implementation this FAILS (RED).
    - GREEN: implement the sibling function by constructing the same arg slice ResolvedVia uses and returning `exec.Command("ssh", args...).String()` WITHOUT executing.
    - Edge: the returned string contains `-F <configPath>` and the alias and `-i <keyPath>` exactly matching the ResolvedVia connectivity invocation (not the `-G` invocation, which per tester.go takes no `-i`).
  </behavior>
  <action>
    Refactor `ResolvedVia`'s connectivity-call argument construction into a small unexported arg-builder (mirroring how `preWriteArgs` backs both `PreWrite` and `PreWriteCommand`), then add `func ResolvedViaCommand(configPath, keyPath, alias string) string` that builds the identical arg slice, wraps it in `exec.Command("ssh", args...)` and returns `.String()` — never executing. This guarantees the on-screen stage-2 command (rendered by plan 03-05) is byte-identical to what ResolvedVia runs, closing Pitfall 7. Keep the `//nolint:gosec // arg-slice form for cmd.String() display; not executed here` annotation exactly as PreWriteCommand uses it. Preserve arg-slice discipline (never `sh -c`, never string interpolation).
  </action>
  <verify>
    <automated>go test -race ./internal/tester/... -run 'TestResolvedViaCommand|TestPreWriteCommand'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/tester.ResolvedViaCommand` exists and is exported
    - A test compares its output against the SAME arg slice ResolvedVia executes (shared arg-builder), proving parity — not a hand-retyped expected string
    - ResolvedViaCommand contains no exec of ssh (returns a string only); the shared arg-builder is used by both ResolvedVia and ResolvedViaCommand (grep both call sites)
    - `make lint` clean
  </acceptance_criteria>
  <done>ResolvedViaCommand returns the exact ssh argv ResolvedVia executes, as a display string, guaranteeing TEST-01's shown==run contract for stage 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied key path → parser | ScanReusableKeys / ensurePub read files at paths the user (later, via the picker) points at |
| private-key bytes → process memory | key material must never leak into returned structs, logs, or command strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | ScanReusableKeys return struct | mitigate | ReusableKey carries Path/Algorithm/Fingerprint/flags ONLY — never PrivPEM/key bytes; asserted grep-negative in test |
| T-03-02 | Tampering | ensurePub encrypted-key path | mitigate | Never attempt a passphrase prompt (D-11); read the existing .pub, never re-parse an encrypted private key |
| T-03-03 | Tampering | ResolvedViaCommand string build | mitigate | Arg-slice `exec.Command`, no `sh -c`, not executed — display only; parity-tested against ResolvedVia's real argv |
| T-03-04 | Denial of Service | ScanReusableKeys over ~/.ssh | accept | One unparseable/garbage file skips, never aborts the scan (D-13); a genuinely unreadable dir returns a wrapped error, not a panic |
</threat_model>

<verification>
- `go test -race ./internal/identity/... ./internal/keygen/... ./internal/tester/...` green.
- `make lint` (golangci-lint + gosec) clean — no new `sh -c`, no G304/G204 regressions, no `--no-verify`.
- No new external dependency (`go list -m all` unchanged vs main).
</verification>

<success_criteria>
- Encrypted-key-with-.pub reuse succeeds (KEY-06 / D-11) — proven by a failing-then-passing test.
- ScanReusableKeys returns algorithm + fingerprint per key, tolerant of encrypted/unparseable entries (D-10 / D-13).
- ResolvedViaCommand is argv-identical to ResolvedVia (TEST-01/02 shown==run).
- Zero private-key material in any returned value, log, or command string.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-01-SUMMARY.md` when done.
</output>
