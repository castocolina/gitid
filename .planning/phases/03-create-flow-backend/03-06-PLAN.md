---
phase: 03-create-flow-backend
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - e2e/ui_pty_e2e_test.go
  - e2e/create_e2e_test.go
  - Makefile
  - .planning/design/create-flow/visual-divergence-allowlist.txt
  - internal/screenshot/tui.go
autonomous: true
requirements: [DLV-04, DLV-06]
must_haves:
  truths:
    - "Every create-flow screen has at least one PTY e2e test driving the REAL built gitid binary via raw keystrokes, using the fake-ssh PATH shim for the connectivity stages"
    - "A make gate diffs the real binary's View() text captures against the approved dummy goldens and hard-fails on any diff outside the documented D-02/D-16/D-19 allowlist"
    - "agent-ui-ux-designer AND Codex review both the text diffs and the PNG pairs; CRITICAL/HIGH findings block, MEDIUM/LOW are recorded and fixed or accepted"
  artifacts:
    - path: "e2e/ui_pty_e2e_test.go"
      provides: "Per-screen PTY e2e for the real wizard (form, both test stages via FakeSSHDir, git-form-demo'd, confirm-write) — DLV-06"
      contains: "FakeSSHDir"
    - path: "Makefile"
      provides: "gate-visual-regression target — golden-text diff with the divergence allowlist (DLV-04.1)"
      contains: "gate-visual-regression"
    - path: ".planning/design/create-flow/visual-divergence-allowlist.txt"
      provides: "The per-screen documented divergence allowlist (D-02/D-16/D-19) the golden gate honors"
      contains: "D-02"
  key_links:
    - from: "e2e/ui_pty_e2e_test.go new wizard cases"
      to: "the REAL gitid binary + e2e/harness_test.go FakeSSHDir"
      via: "raw-keystroke PTY drives the real binary; PATH-shim ssh returns recorded outputs"
      pattern: "FakeSSHDir|startPTY"
    - from: "Makefile gate-visual-regression"
      to: "the approved dummy goldens + the divergence allowlist"
      via: "byte-exact text diff except allowlisted per-screen divergences (D-25 hard-fail)"
      pattern: "gate-visual-regression"
---

<objective>
Close the Phase-3 UI-wave gate: DLV-06 (per-screen PTY e2e on the REAL binary)
and DLV-04 (visual-regression against the approved design), the two hard gates
the phase's success criteria require.

- **DLV-06**: extend `e2e/ui_pty_e2e_test.go` with per-screen PTY tests driving
  the REAL built `gitid` binary via raw keystrokes, reusing `e2e/harness_test.go`'s
  `FakeSSHDir` PATH-shim (modes pass/denied/timeout → PASS/ReachableNotUploaded/
  Failure, D-22) for the connectivity stages. Supersede the POC-CLI
  `create_e2e_test.go`. Screens: algorithm+SSH form, both test stages (all three
  outcomes), the git-form-demo'd step (assert the D-19 disabled reason), and the
  confirm-write ceremony (assert live `~/.ssh/config` untouched pre-confirm, and
  the resolved target after).
- **DLV-04.1 (D-24.1/D-25)**: a `make gate-visual-regression` target diffing the
  real binary's deterministic `View()` text captures against the approved dummy
  goldens — byte-exact EXCEPT the per-screen documented divergence allowlist
  (D-02, D-16, D-19). Any unallowlisted diff hard-fails.
- **DLV-04.2 (D-24.2/D-25)**: an orchestrator-run cross-AI review —
  `agent-ui-ux-designer` AND Codex critique BOTH the text diffs and the PNG
  pairs; CRITICAL/HIGH block the wave, MEDIUM/LOW recorded and fixed or accepted.
  Per DLV-08 the loop runs UNATTENDED here — this is an agent/orchestrator exit
  gate, NOT a human approval checkpoint.

Purpose: prove the real create flow is drivable end-to-end and visually matches
the approved design within its three documented divergences.
Output: per-screen PTY e2e, the golden-text gate + allowlist, the cross-AI
review exit gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-PATTERNS.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/design/REFERENCE-INDEX.md
</context>

## Artifacts this phase produces (plan 03-06)

New/changed symbols (exclude from source-drift flagging):

- New per-screen PTY e2e test functions in `e2e/ui_pty_e2e_test.go` driving the real binary via `FakeSSHDir`
- Removed/superseded `e2e/create_e2e_test.go` (POC-CLI `identity add` target, archived by D-14)
- New `Makefile` target `gate-visual-regression` (golden-text diff honoring the divergence allowlist)
- New `Makefile` target `smoke-network-test` (D-23 skippable real-network two-stage smoke — local/UAT, auto-skipped when the provider is unreachable; NOT a required CI gate)
- New `.planning/design/create-flow/visual-divergence-allowlist.txt` (the D-02/D-16/D-19 per-screen allowlist)
- Any small additive `internal/screenshot/tui.go` capture-helper needed to dump the real binary's View() text goldens (reusing CaptureTUI's 100×30 geometry)

<tasks>

<task type="auto">
  <name>Task 1: DLV-06 — per-screen PTY e2e on the REAL binary via the fake-ssh shim</name>
  <files>e2e/ui_pty_e2e_test.go, e2e/create_e2e_test.go</files>
  <read_first>
    - e2e/ui_pty_e2e_test.go (the existing harness: ptySession ~68-76, startPTY ~96, sendKey ~226, snapshot/waitFor ~234-254, BuildBinary — the real-binary PTY pattern)
    - e2e/harness_test.go (FakeSSHDir ~121-183 — modes pass/denied/timeout; how it prepends a fake ssh to PATH via cmd.Env)
    - e2e/create_e2e_test.go (the POC-CLI use of FakeSSHDir to supersede — DO NOT keep; replace with PTY equivalents)
    - .planning/phases/03-create-flow-backend/03-PATTERNS.md (the e2e extension section — the fake-ssh-in-PTY injection snippet; modes → outcome mapping; the screen list to cover)
    - .planning/phases/03-create-flow-backend/03-RESEARCH.md (D-22 deterministic seam; Pitfall 6 denied→ReachableNotUploaded not Failure; the removed dummy_nav harness — do not resurrect)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-22 PATH-shim fake ssh; D-06 dual-file first create; D-08 globals; D-18/D-19 git-form)
  </read_first>
  <action>
    Extend `e2e/ui_pty_e2e_test.go` with new per-screen PTY test functions that build and drive the REAL `gitid` binary (bare invocation → the tuikit app shell) via raw keystrokes, with `FakeSSHDir(t, mode)` prepended to the PTY-launched `cmd.Env` PATH (per 03-PATTERNS.md's injection snippet) so the real process execs the fake `ssh` for the connectivity stages. Cover, at minimum, one test per create-flow screen:
    (1) algorithm + SSH form — enter fields, assert the live Host preview shows Port 443 + IdentitiesOnly yes; assert an alias collision blocks (seed an existing host in a fake HOME);
    (2) test stage 1 with mode `pass` → PASS green, mode `denied` → ReachableNotUploaded yellow `!` "Reachable — key not uploaded yet" (NOT red, Pitfall 6), mode `timeout` → Failure red + retry;
    (3) test stage 2 (`ssh -G` alias resolution) auto-chaining after a stage-1 success;
    (4) the git-form-demo'd step — assert the D-19 real reason `— Git configuration arrives with the next build` and that Skip Git advances to review;
    (5) confirm-write ceremony — assert the live `~/.ssh/config` (in the fake HOME) is UNTOUCHED before confirm (SSHUI-04), then after confirm the resolved Include'd target exists with a backup (D-05/06) and the macOS globals block is present on darwin (D-08/SSHUI-05).
    Delete/replace `e2e/create_e2e_test.go` (the POC-CLI `identity add` target archived by D-14). Reuse the existing `startPTY`/`sendKey`/`waitFor` harness — do not invent a new one; do not resurrect the removed `dummy_nav`/`BuildDummyBinary` harness. Keep the bounded ptySession.close() (ctrl+c grace + SIGKILL fallback) already in the harness.
  </action>
  <verify>
    <automated>make test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - New PTY test functions exist for each of the five create-flow screens above, driving the REAL binary (grep `FakeSSHDir` + `startPTY` in the new cases)
    - `denied` mode renders the yellow ReachableNotUploaded warning, never red (asserted in the captured PTY frame); `timeout` → red Failure + retry; `pass` → green
    - The confirm-write test asserts live `~/.ssh/config` untouched pre-confirm and the Include'd target + backup + macOS globals present post-confirm (in a fake HOME)
    - `e2e/create_e2e_test.go` (POC CLI) is removed; `make test-e2e` green
  </acceptance_criteria>
  <done>Every create-flow screen has a real-binary PTY e2e covering all three test outcomes, the git-form divergence, and the pre-confirm-untouched / post-confirm-persisted invariant.</done>
</task>

<task type="auto">
  <name>Task 2: DLV-04.1 — golden-text visual-regression make gate + divergence allowlist (D-24.1/D-25)</name>
  <files>Makefile, .planning/design/create-flow/visual-divergence-allowlist.txt, internal/screenshot/tui.go</files>
  <read_first>
    - Makefile (gate-no-backend-files ~245-253 — the single-shell-target-with-explicit-pass/fail-echo shape to mirror; screenshot-tui ~213 CaptureTUI invocation + the vendored font/theme deterministic-render values ~49-54)
    - internal/screenshot/tui.go (CaptureTUI ~65 + TUIOptions ~25 — the 100×30 View()-dump→text/PNG capture mechanism to reuse for the real binary's goldens)
    - .planning/phases/03-create-flow-backend/03-UI-SPEC.md (the three scoped divergences with their exact allowlist entries: D-02 test-stage1/2 outcome line; D-16 blanket non-wired-view top-of-body banner; D-19 git-form step-2 real-binary-only Continue reason; 100×30 geometry kept identical)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-24 two-layer gate; D-25 hard gate + severity triage; Claude's Discretion: capture geometry/golden layout/allowlist file format — keep Phase 1's 100×30)
    - .planning/design/REFERENCE-INDEX.md (the approved dummy goldens the real captures diff against)
    - .planning/phases/03-create-flow-backend/03-PATTERNS.md (the Makefile gate section + "No Analog Found: D-24 golden-file storage/layout is genuinely new" — CaptureTUI is the capture-side analog)
  </read_first>
  <action>
    Author `.planning/design/create-flow/visual-divergence-allowlist.txt` (Claude's Discretion on exact format — a simple per-screen `screen-id: reason (D-NN)` line list) enumerating ONLY the three documented divergences: D-02 (`test-stage1-direct` and `test-stage2-by-alias` gain the ReachableNotUploaded outcome line), D-16 (any non-wired view gains the top-of-body Preview banner row — one blanket entry), D-19 (`create-flow` git-form step-2, REAL-binary capture only, one Continue-reason line). Add a `gate-visual-regression` Makefile target following the `gate-no-backend-files` shape (single shell script, explicit OK/FAILED echo, non-zero exit on violation): capture the REAL binary's deterministic `View()` text for each create-flow screen via the `internal/screenshot` CaptureTUI mechanism at the SAME 100×30 geometry + vendored font/theme as `screenshot-tui` (apples-to-apples), diff each against its approved dummy golden, and FAIL on any diff line NOT covered by the allowlist file (D-25 hard gate). Add any small additive helper to `internal/screenshot/tui.go` needed to dump the real binary's per-screen View() text goldens (reuse CaptureTUI; do not change its geometry). Do NOT weaken to a fuzzy/approximate diff — it is byte-exact modulo the explicit allowlist. Wire `gate-visual-regression` as a prerequisite of the phase gate (documented in the SUMMARY; CI wiring on the existing 3-runner matrix). ALSO add a separate `smoke-network-test` Makefile target (D-23): run the REAL two-stage connectivity flow against github.com and assert the outcome is ReachableNotUploaded or PASS, AUTO-SKIPPING (exit 0 with a clear "skipped: provider unreachable" message) when the network/provider is unreachable. This is a LOCAL + UAT convenience target only — do NOT wire it as a required CI gate (CI stays deterministic via the FakeSSHDir PTY e2e). Keep it argv-slice, no `sh -c`.
  </action>
  <verify>
    <automated>make gate-visual-regression</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/design/create-flow/visual-divergence-allowlist.txt` lists exactly the D-02/D-16/D-19 divergences (grep each `D-02`/`D-16`/`D-19`)
    - `make gate-visual-regression` passes when the only diffs are the three allowlisted divergences, and FAILS (non-zero) when an unallowlisted line differs — prove the fail path by temporarily perturbing one captured line, observing the red gate, then reverting (document in the SUMMARY)
    - Captures use the same 100×30 geometry + vendored font/theme as `screenshot-tui` (grep the target for the shared values)
    - The gate is byte-exact modulo the allowlist (no fuzzy diff)
    - A `smoke-network-test` target exists (D-23): asserts ReachableNotUploaded/PASS against github.com and auto-skips (exit 0) when unreachable; it is NOT a `make test`/CI prerequisite
  </acceptance_criteria>
  <done>An automated hard gate proves the real create-flow screens match the approved goldens except the three documented divergences.</done>
</task>

<task type="auto">
  <name>Task 3: DLV-04.2 — cross-AI visual-regression review exit gate (agent-ui-ux-designer + Codex, D-24.2/D-25)</name>
  <files>.planning/phases/03-create-flow-backend/03-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-24.2 cross-AI review of BOTH text diffs and screenshots; D-25 severity triage — CRITICAL/HIGH block, MEDIUM/LOW recorded/accepted)
    - .planning/phases/03-create-flow-backend/03-UI-SPEC.md (the three divergences to confirm read as intended; no undocumented divergence)
    - .planning/design/REFERENCE-INDEX.md + .planning/design/ (the approved goldens/PNGs to diff against)
    - .planning/STATE.md (the Phase-2 "Review findings resolution (post-plan fix pass)" precedent — the record format and orchestrator-run convention)
  </read_first>
  <action>
    This is an ORCHESTRATOR-RUN exit gate, NOT a human checkpoint (DLV-08: Phases 3-9 run unattended). The executor's job is to ASSEMBLE the review inputs and RECORD the outcome; the orchestrator spawns the reviewers at wave close.

    Executor: assemble the review packet — the Task-2 golden-text diffs plus the PNG pairs (real-binary create-flow screens vs the approved dummy goldens from `.planning/design/`), and a short manifest listing the three expected divergences (D-02 ReachableNotUploaded outcome line; D-16 Preview banner on non-wired tabs; D-19 real git-form Continue reason). Write a "Cross-AI visual-regression review" section into the SUMMARY with placeholders for each reviewer's findings + severity.

    Orchestrator (wave close, per the Phase-2 pattern): run `agent-ui-ux-designer` (engaging `/mui`) to critique BOTH the text diffs and the PNG pairs against the approved design, and run Codex (cross-AI) independently over the SAME artifacts. Triage by severity (D-25): CRITICAL/HIGH BLOCK the wave and route back to plans 03-04/03-05 for fixes; MEDIUM/LOW are recorded and fixed or explicitly accepted (Phase-2 F1-F10 convention). Confirm the three divergences read as intended and that NO undocumented divergence slipped in. Record all findings + their resolution in the SUMMARY before the phase is marked complete.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-create-flow-backend/03-06-SUMMARY.md && grep -q 'Cross-AI visual-regression review' .planning/phases/03-create-flow-backend/03-06-SUMMARY.md</automated>
    <human-check>agent-ui-ux-designer + Codex have both reviewed the text diffs AND the PNG pairs; every CRITICAL/HIGH finding is resolved; MEDIUM/LOW findings are recorded and fixed-or-accepted; the SUMMARY's review section is complete.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a "Cross-AI visual-regression review" section naming BOTH reviewers (agent-ui-ux-designer + Codex) and enumerating findings with severities
    - Every CRITICAL/HIGH finding is resolved (or routed back to 03-04/03-05 and then resolved); MEDIUM/LOW are recorded with a fixed/accepted disposition
    - The section confirms the three divergences (D-02/D-16/D-19) read as intended and no undocumented divergence slipped in
  </acceptance_criteria>
  <done>Both AIs reviewed the text diffs + screenshots; blocking findings are resolved and the review outcome is recorded — the phase's visual-regression gate is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fake-ssh PATH shim | the e2e seam swaps the external ssh binary — it must stay test-only, never shipped |
| captured goldens | the visual gate's source of truth must be deterministic (fixed geometry/font/theme) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Tampering | fake-ssh PATH shim leaking into the binary | mitigate | FakeSSHDir lives only in `e2e/` (build-tag/test-only); the real binary execs the real `ssh` — asserted by the gate-no-backend-files + import-graph separation (e2e never imported by cmd/gitid) |
| T-03-21 | Spoofing | visual-gate false pass | mitigate | Byte-exact golden-text diff modulo an explicit, enumerated allowlist (D-25 hard fail); fail-path proven by perturbation; cross-AI review confirms no undocumented divergence |
| T-03-22 | Tampering | non-deterministic captures | mitigate | Reuse CaptureTUI's 100×30 geometry + vendored font/theme (screenshot-tui determinism, Phase-1 Pitfall 6) — same values, apples-to-apples |
| T-03-23 | Repudiation | connectivity-outcome e2e realism | mitigate | PTY drives the REAL binary end-to-end; only the external `ssh` is swapped (D-22) — real exec plumbing, real output parsing, real UI (closes the injected-seam blindspot) |
</threat_model>

<verification>
- `make test-e2e` green with the new per-screen PTY cases (DLV-06).
- `make gate-visual-regression` green (only the three allowlisted divergences); fail-path proven by perturbation.
- `make test`, `make lint`, `make gate-no-backend-files` green (orchestrator runs the full race-enabled battery at wave close per the project's review-gate convention).
- The cross-AI review (agent-ui-ux-designer + Codex) has run; CRITICAL/HIGH resolved, MEDIUM/LOW recorded — captured in the SUMMARY.
</verification>

<success_criteria>
- Every create-flow screen has a real-binary PTY e2e (DLV-06) covering all three test outcomes + the pre-confirm-untouched invariant.
- The golden-text gate hard-fails on any undocumented divergence (DLV-04.1/D-25).
- agent-ui-ux-designer + Codex reviewed both text diffs and screenshots; blocking findings resolved (DLV-04.2/D-25).
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-06-SUMMARY.md` when done.
</output>
