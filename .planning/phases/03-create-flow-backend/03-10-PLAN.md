---
phase: 03-create-flow-backend
plan: 10
type: execute
wave: 9
depends_on: [03-09]
files_modified:
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - internal/tester/tester.go
  - internal/tester/tester_command_test.go
  - internal/sshconfig/validation.go
  - internal/sshconfig/validation_test.go
  - internal/tuikit/views.go
  - internal/tuikit/store.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_test.go
  - e2e/create_flow_pty_e2e_test.go
  - Makefile
  - .planning/design/create-flow/visual-divergence-allowlist.txt
  - .planning/phases/03-create-flow-backend/03-10-review-packet/
autonomous: true
gap_closure: true
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, SSHUI-04, SSHUI-05, TEST-01, TEST-02, TEST-03, KEY-06, DLV-04, DLV-06]
estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Before confirmation, create-flow testing performs no write or chmod under the real SSH directory; confirmation alone may create or change it, and every transaction failure restores bytes, existence, and modes. (CR-08, CR-09; D-05, D-06, D-08, D-11)"
    - "Stage 2 displays both exact commands and both raw outputs, fails closed on any ssh -G execution/output/field mismatch, and accepts proof only when User, Hostname, Port, IdentitiesOnly, and the expected effective IdentityFile all match. (CR-07; D-01, D-04; TEST-01, TEST-02)"
    - "The selected algorithm and validated provider survive the view DTO, proof fingerprint, staged-key cache, and commit DTO unchanged; rsa-4096 and multi-label/custom providers persist correctly. (CR-10, WR-01; D-10, D-13, D-20, D-21)"
    - "Manual IdentityFile values are rejected unless they are representable as one safe unquoted OpenSSH token, immediately before rendering and execution. (CR-11; D-09, D-11)"
    - "Visual evidence is deterministic, offline, approval-commit-backed, fail-closed, content-addressed, and immutable; routine gate execution never changes the committed packet. (CR-01 through CR-05; D-22, D-24, D-25)"
    - "Two genuinely independent authenticated reviewers rerun against the corrected immutable evidence, with unedited provenance-bearing outputs; any Critical/High finding blocks and causes a new evidence bundle plus both review reruns. (CR-06; D-24, D-25)"
  artifacts:
    - path: "cmd/gitid/wiring.go"
      provides: "Complete stage-2 proof, preserved algorithm/provider, pre-confirm isolation, and rollback-capable reused-key permission transaction"
    - path: "internal/sshconfig/validation.go"
      provides: "Strict IdentityFile token validation at the render boundary"
    - path: "cmd/gitid/gate_visual_regression_test.go"
      provides: "Read-only routine gate, explicit packet generator, strict applicable-region policy, and approval provenance validation"
    - path: ".planning/phases/03-create-flow-backend/03-10-review-packet/"
      provides: "Content-addressed immutable corrected evidence and authenticated independent review outputs"
  key_links:
    - from: "internal/tester/tester.go"
      to: "cmd/gitid/wiring.go"
      via: "an error-bearing stage-2 result carries connectivity and ssh -G commands, raw outputs, errors, and parsed fields into fail-closed expected-value validation"
      pattern: "ResolvedVia|Resolution"
    - from: "internal/tuikit/identities.go"
      to: "cmd/gitid/wiring.go"
      via: "DemoIdentity carries CreateSpec.Algorithm and CreateSpec.Provider unchanged into CommitCreate/createInput"
      pattern: "Algorithm|Provider"
    - from: "cmd/gitid/gate_visual_regression_test.go"
      to: "3c3130e404329cf42baafdf63a6c22758437edc6"
      via: "isolated approval-commit capture records separately hashed approved HTML and approved TUI sources"
      pattern: "approval_commit|approved_html|approved_tui"
    - from: ".planning/phases/03-create-flow-backend/03-10-review-packet/*/EVIDENCE.json"
      to: "UI-REVIEW.md and CODEX-REVIEW.md in the same content-addressed bundle"
      via: "both prompts and review provenance bind to the evidence manifest SHA-256 and corrected source commit"
      pattern: "evidence_sha256|source_commit|reviewer"
---

<objective>
Close every finding in the 2026-08-21 deep review without relying on the stale completion claims in 03-VALIDATION or 03-09-SUMMARY: CR-01 through CR-11 and WR-01.

Purpose: Restore the product's safety boundary and produce review evidence that can independently prove the corrected create flow rather than self-certifying mutable output.
Output: TDD-backed production corrections, a deterministic fail-closed visual-evidence pipeline, one new content-addressed Phase 3 review packet, and two authenticated independent review transcripts. This plan does not itself claim Phase 3 completion.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/ONESHOT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/LEARNINGS.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-VALIDATION.md
@.planning/phases/03-create-flow-backend/03-REVIEW.md
@.planning/phases/03-create-flow-backend/03-07-SUMMARY.md
@.planning/phases/03-create-flow-backend/03-08-SUMMARY.md
@.planning/phases/03-create-flow-backend/03-09-SUMMARY.md

<interfaces>
Current contracts to extend rather than bypass:

- `tester.Result { Command string; Output string; Outcome Outcome }` and `tester.ResolvedConfig { User, Hostname, Port, IdentitiesOnly string; IdentityFiles []string }`; current `ResolvedVia(configPath, keyPath, alias, knownHostsPath) (Result, ResolvedConfig)` discards the `ssh -G` error/output provenance and must become an error-bearing two-command proof.
- `tuikit.TestResultView { Outcome TestOutcome; Command string; Detail string }`; extend this backend-free DTO to carry both stage-2 commands and both displayed outputs without importing `internal/tester` into `internal/tuikit`.
- `tuikit.CreateSpec` already carries `Provider` and `Algorithm`; `tuikit.DemoIdentity` currently carries neither. Add both fields and populate them in `wizardModel.finishIdentity()` so `realBackend.CommitCreate()` never reconstructs provider or defaults algorithm.
- `realBackend.createInput(row tuikit.DemoIdentity)` currently calls `providerFromAlias(row.SSHHost)` and hardcodes `Algo: "ed25519"`; replace both with the propagated validated values. Include algorithm, key source, and reuse path in `specFingerprint` and staged-cache identity.
- `sshconfig.ValidateHostBlock(alias, hostname, portStr, identityFile)` is the final pre-render validation chokepoint; current IdentityFile handling rejects only empty/newline/CR/NUL.
- `CaptureCreateFlowScreens(backend tuikit.Backend) map[string]string` currently injects stage messages directly and can capture ignored idle-state messages; the corrected capture must drive valid state transitions through a controllable offline backend.
- Approval provenance is commit `3c3130e404329cf42baafdf63a6c22758437edc6` (`docs(02-12): record DLV-08 design approval — APPROVED 2026-07-06 by Pepe`). Recover approved HTML and TUI sources in isolated temporary storage from that commit; current `dummytui.NewFixtureBackend()` is not an approval baseline.
</interfaces>
</context>

<execution_safety>
- At plan start, record `git status --short` and SHA-256 hashes for the already-dirty 03-REVIEW.md, 03-09-review-packet/EVIDENCE.json, and 03-09-review-packet/panel-pngs/reuse-key-vs-generate.png. Never checkout, restore, rewrite, delete, stage, or commit those paths; assert their baseline hashes are unchanged at every task boundary.
- All behavioral tests run with a disposable HOME. No test, capture, or review command may write or chmod the developer's real `~/.ssh` directory or files.
- TDD authoring order is RED then GREEN then REFACTOR, but commit each coherent implementation with its tests as one logical commit per AGENTS.md. Record the failing command and real failure output before implementation; never ship a standalone broken RED commit and never use `--no-verify`.
- Execute Tasks 1, 2, and 3 sequentially. Task 2 requires Task 1's corrected production commit. Task 3 requires Task 2's passing gate implementation and a committed corrected source SHA.
</execution_safety>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end corrected create proof and confirmed transaction tracer</name>
  <files>cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/tester/tester.go, internal/tester/tester_command_test.go, internal/sshconfig/validation.go, internal/sshconfig/validation_test.go, internal/tuikit/views.go, internal/tuikit/store.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <read_first>
    - cmd/gitid/wiring.go:73-114 (`realBackend` staged/proof cache), 156-204 (`buildIdentityDeps.Generate`), 548-596 (`Stage2Command`/`TestStage2`), 900-1043 (`createInput`, `createInputFromSpec`, staged cache, proof fingerprint), 1061-1203 (`CommitCreate` transaction)
    - internal/tester/tester.go:24-45 (`Result`, `ResolvedConfig`, runner), 91-128 (stage-2 command builder), 178-204 (`ResolvedVia`)
    - internal/tuikit/views.go:60-106 (`TestResultView`, `CreateSpec`), internal/tuikit/store.go:19-40 (`DemoIdentity`), internal/tuikit/identities.go:923-936 (`spec`) and 1223-1240 (`finishIdentity`)
    - internal/sshconfig/validation.go:47-70 (`ValidateHostBlock`) and internal/sshconfig/renderer.go:17-41 (unquoted IdentityFile interpolation)
    - e2e/create_flow_pty_e2e_test.go (existing real-binary raw-PTY and fake-SSH harness usage)
    - .planning/phases/03-create-flow-backend/03-REVIEW.md (CR-07 through CR-11 and WR-01)
  </read_first>
  <behavior>
    - RED CR-07: stage 2 fails on `ssh -G` process failure, empty output, wrong User/Hostname/Port/IdentitiesOnly, missing expected IdentityFile, or a different earlier effective IdentityFile; success retains and displays the exact connectivity command/output and exact resolution command/output.
    - RED CR-08: starting either test stage for a generated identity leaves the sandbox's final SSH directory absent when initially absent, and preserves its existing mode and bytes when present; no pre-confirm call performs EnsureDir/Chmod beneath the final SSH path.
    - RED CR-09: a reused private key initially mode 0644 remains 0644 before confirmation, becomes 0600 only on a successful confirmed transaction, and returns to 0644 when failure is injected after permission normalization; its .pub mode follows the same previewed 0644/rollback contract.
    - RED CR-10/WR-01: selecting rsa-4096 and a multi-label/custom provider carries both values through `CreateSpec` → `DemoIdentity` → `createInput` → `specFingerprint`/staged cache → persisted key path and Host provider marker without fallback or suffix reconstruction.
    - RED CR-11: IdentityFile rejects every whitespace/control-bearing or syntax-significant unquoted token (space, tab, form feed, quotes, backslash ambiguity, comment/directive separators) before render/exec; accepted tokens round-trip through parser plus real `ssh -G` in a temp HOME.
    - RED integration: a raw-PTY rsa-4096 create shows both stage-2 command/output pairs, confirms, and persists the RSA path plus validated provider metadata; an injected post-chmod failure leaves the complete sandbox snapshot unchanged.
  </behavior>
  <action>
    Implement CR-07 as a UI-free tester contract: introduce a stage-2 proof/result type carrying connectivity `Result`, the exact `ssh -F <tmp> -G <alias>` command, raw resolution stdout/stderr, resolution error, and parsed `ResolvedConfig`. Build display and execution from shared argv helpers. In the real backend compare normalized expected values for User=`git`, Hostname, Port, IdentitiesOnly=`yes`, and effective IdentityFile ordering before calling `recordOutcomeFor`; any command error, empty output, or mismatch returns `TestOutcomeFailure` with field-specific evidence. Extend only tuikit-local DTOs to render both commands and outputs; preserve the no-backend import gate. This implements TEST-01/TEST-02 and D-01/D-04 without weakening ReachableNotUploaded semantics.

    Remove CR-08's pre-confirm `EnsureDir(b.sshDir, 0700)` from key generation. Generate entirely under `stagingDir()`; carry final destinations and a write-plan entry describing final directory creation/mode. Create or chmod the real SSH directory only inside `commitCreateTransaction` after the explicit confirmation message, snapshoting prior existence/mode for rollback. Do not touch the real directory on scan, stage, command display, connectivity test, cancellation, or visual capture (D-05/D-06/D-11/D-22).

    Close CR-09 by representing confirmed filesystem permission operations explicitly. For reused keys with nil `PrivPEM`, snapshot the private key mode, chmod it to 0600 during the confirmed transaction, snapshot/write or chmod the public sibling to 0644, add each operation to reverse rollback, and preview both changes. Preserve content bytes. Extend failure injection to immediately after private-mode and public-mode operations and prove restoration of prior modes and bytes.

    Close CR-10 and WR-01 by adding `Algorithm` and `Provider` to `tuikit.DemoIdentity`, setting them from `wizardModel.spec()` in `finishIdentity`, and consuming them directly in `realBackend.createInput`. Include Algorithm, Provider, reuse/generate source, and reuse path in proof/staged-cache identity so accepted evidence cannot be replayed across a selection change. Never infer provider identity from final label count. Preserve D-10/D-12/D-13 reuse semantics and D-20/D-21 known/custom endpoint behavior.

    Close CR-11 at `ValidateHostBlock`, immediately before `RenderHostBlock`: choose the review's reject strategy and accept only a single unquoted OpenSSH IdentityFile token with no Unicode/ASCII whitespace, control characters, quotes, comment markers, directive separators, or ambiguous escaping. Keep the UI's earlier validation, but the core renderer boundary is authoritative. Add parser and real `ssh -G` round-trip tests for accepted paths. Do not introduce a second renderer or shell interpolation.

    Keep D-02/D-03 warning/copy behavior, D-05/D-06 layout detection, D-08 globals ordering, D-09 effective collision, D-14..D-19 shared-shell/Git-disabled behavior, and recipe Host shape unchanged. Commit production and tests as one coherent buildable change after all focused tests and the raw-PTY tracer pass.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race ./internal/tester/... ./internal/sshconfig/... ./internal/tuikit/... ./cmd/gitid/... -run 'Test.*(Stage2|Resolved|IdentityFile|Algorithm|Provider|PreConfirm|Permission|Rollback|Transaction)' -count=1 &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -timeout 180s ./e2e/... -run 'TestCreateFlow_.*(RSA|Resolved|Reuse|Rollback|NoPreConfirm)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - CR-07 has one negative test per required `ssh -G` field/error case and one success test asserting both exact commands plus both raw outputs are present in the rendered PTY frame.
    - CR-08 tests prove no pre-confirm write or chmod under the final SSH directory, including directory creation and mode changes; the final directory is changed only after confirmation.
    - CR-09 tests prove successful 0600/0644 normalization and rollback to original modes/bytes after every later injected failure in reused-key mode.
    - CR-10 raw-PTY coverage selects rsa-4096 and proves the generated path, proof fingerprint, staged cache, and persisted Host block all use RSA rather than ed25519.
    - WR-01 coverage uses a multi-label/custom provider and proves its validated value survives to persisted metadata without `providerFromAlias` reconstruction.
    - CR-11 tests reject unsafe IdentityFile tokens and prove accepted values parse and resolve through real `ssh -G` against a throwaway config.
    - `go list -deps ./internal/tuikit/...` contains no first-party backend package; `git diff -- recipes/` is empty; the pre-existing dirty evidence hashes are unchanged.
  </acceptance_criteria>
  <done>One real-binary create path now proves complete stage-2 resolution, preserves algorithm/provider, performs no pre-confirm SSH-directory mutation, commits reused-key permissions transactionally, and rejects unsafe IdentityFile syntax.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic fail-closed approval evidence pipeline</name>
  <files>Makefile, cmd/gitid/gate_visual_regression_test.go, internal/screenshot/createflow.go, internal/screenshot/createflow_test.go, .planning/design/create-flow/visual-divergence-allowlist.txt</files>
  <read_first>
    - cmd/gitid/gate_visual_regression_test.go:62-225 (allowlist parser), 228-250 (random reusable-key fixture), 252-269 (unconstrained predicate), 274-376 (region gate), 378-448 (tracked packet mutation), 450-524 (current-HEAD approved TUI generator)
    - internal/screenshot/createflow.go:169-218 (offline backend/spec), 220-304 (capture state transitions)
    - internal/screenshot/createflow_test.go:21-58 (coverage/determinism), 164-214 (single negative control/offline timeout)
    - Makefile:275-290 (`gate-visual-regression` routine target)
    - .planning/design/APPROVAL.md and git commit `3c3130e404329cf42baafdf63a6c22758437edc6`
    - .planning/phases/03-create-flow-backend/03-REVIEW.md (CR-01 through CR-05)
  </read_first>
  <behavior>
    - RED CR-01: two independent temporary runs over fixed public test material, normalized temp paths, and an injected fixed clock/backup descriptor produce byte-identical text and PNG hashes; a routine gate run leaves every tracked file byte-identical.
    - RED CR-02: stage-1 and stage-2 captures differ, each is reached through a valid running state, and each includes its stage-specific exact command, raw output, outcome, and (stage 2) complete `ssh -G` proof.
    - RED CR-03: approved HTML and TUI evidence identifies full approval commit `3c3130e404329cf42baafdf63a6c22758437edc6`, is captured from isolated files at that commit, and fails if either approved source cannot be reproduced; current HEAD cannot satisfy the approved-source interface.
    - RED CR-04: every screen declares required applicable non-empty regions; empty extraction fails; no unconstrained predicate parses; a mutation in every protected subregion of every screen turns the gate red.
    - RED CR-05: absent font/tool, capture/render error, missing or extra panel, missing hash/provenance, stale/untracked packet member, or evidence serialization/write error is fatal; exactly eight live, eight approved-TUI, and eight approved-HTML panels are required.
  </behavior>
  <action>
    Separate validation from publication. `make gate-visual-regression` must render two candidate bundles under separate temporary directories, compare their complete text/PNG hash inventories for determinism, validate them against the committed content-addressed packet when one exists, and exit without writing any tracked path. Add an explicit `make generate-visual-review-packet SOURCE_COMMIT=&lt;full-sha&gt; OUTPUT_DIR=&lt;new-empty-dir&gt;` publication target for Task 3; it refuses an existing/nonempty destination, a dirty corrected-source set, a short/unknown SHA, or an approval commit other than the recorded full SHA. Routine execution must not update timestamps, source commit fields, EVIDENCE.json, panel bytes, manifests, or reviews.

    Replace generated random private material with committed deterministic public test fixtures or a deterministic picker DTO that contains no secret/private material. Normalize every disposable path in text before rendering. Inject a fixed clock and deterministic backup placeholder through the capture backend. Drive Enter and controllable offline backend completion so `testRunning1` and `testRunning2` are real state transitions; pause after stage 1, capture, release stage 2, capture. Assert stage panels differ and expose the Task-1 complete proof.

    Recover the approved HTML and approved TUI implementation/artifacts from the approval commit into isolated temporary storage. Use the existing HTML/TUI capture tooling at 100x30 and the pinned font/theme. Do not construct the approved side from current `dummytui.NewFixtureBackend()` and do not alter `.planning/design`. Record separate approval/live capture commands, full commits, screen IDs, hashes, tool versions, geometry, font, theme, and normalized-input policy.

    Replace blanket region iteration with a per-screen applicable-region schema. Every required extraction must be non-empty. Remove the unconstrained allowlist predicate; permit only exact before/after values or narrowly named normalization functions whose tests pin accepted input/output. Add table-driven negative controls for every protected subregion of all eight screens. Make every prerequisite and output fail closed: no skip/log-and-continue branches for fonts, Chromium, Freeze, captures, evidence, panel counts, or hashes. Task 2 validates the pipeline only in temp directories; Task 3 alone publishes a new packet.
  </action>
  <verify>
    <automated>go test -tags screenshot -race ./internal/screenshot/... ./cmd/gitid/... -run 'Test.*(CaptureCreateFlowScreens|VisualRegression|ApplicableRegion|NegativeControl|ApprovalProvenance|Deterministic|FailClosed|RoutineGateReadOnly)' -count=2 &amp;&amp; make gate-visual-regression</automated>
  </verify>
  <acceptance_criteria>
    - CR-01: two temporary runs have identical per-file hashes and tracked-file before/after hashes are identical; the test specifically covers confirm-write and reuse-key panels.
    - CR-02: stage panel hashes differ and assertions find stage-specific command/output/proof text in the corresponding captures.
    - CR-03: EVIDENCE schema requires separate approval/live provenance and full SHAs; approved HTML and TUI are both recovered from the approval commit, never current HEAD.
    - CR-04: all applicable regions are non-empty; no broad exemption grammar remains; table-driven negative controls mutate every protected region of every screen.
    - CR-05: exactly 8+8+8 panels and all declared hashes are mandatory; every missing tool/input/output case fails rather than skips or logs success.
    - The routine make target writes only beneath temporary directories and does not modify the existing dirty 03-09 packet or the new committed 03-10 packet.
  </acceptance_criteria>
  <done>The routine visual gate is deterministic, read-only, approval-backed, region-meaningful, and fail-closed; an explicit one-shot publisher is ready to create a new immutable packet after the corrected source commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish immutable corrected evidence and rerun authenticated independent reviews</name>
  <files>.planning/phases/03-create-flow-backend/03-10-review-packet/&lt;source-full-sha&gt;/MANIFEST.md, .planning/phases/03-create-flow-backend/03-10-review-packet/&lt;source-full-sha&gt;/EVIDENCE.json, .planning/phases/03-create-flow-backend/03-10-review-packet/&lt;source-full-sha&gt;/REVIEW-PROVENANCE.json, .planning/phases/03-create-flow-backend/03-10-review-packet/&lt;source-full-sha&gt;/UI-REVIEW.md, .planning/phases/03-create-flow-backend/03-10-review-packet/&lt;source-full-sha&gt;/CODEX-REVIEW.md</files>
  <read_first>
    - .planning/phases/03-create-flow-backend/03-09-review-packet/EVIDENCE.json, MANIFEST.md, UI-REVIEW.md, CODEX-REVIEW.md (read as invalid evidence patterns; do not modify)
    - .planning/phases/03-create-flow-backend/03-REVIEW.md (CR-06 and the observed invalid review provenance)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-24 two reviewers over text and PNG; D-25 severity policy)
    - .planning/design/APPROVAL.md (approval identity/date) and commit `3c3130e404329cf42baafdf63a6c22758437edc6`
    - Task 2's publication CLI/Make target and evidence schema tests
  </read_first>
  <behavior>
    - RED: provenance validation rejects executor-authored prose, missing reviewer/run/session ID, missing exact CLI/tool version, unresolved model/provider ID, missing authentication preflight status, missing prompt hash, missing raw-output hash, evidence-manifest hash mismatch, review time before evidence generation, or edited transcript content.
    - RED: the packet publisher refuses overwrite and produces the same candidate hashes in two temporary pre-publication runs before creating the single content-addressed destination.
    - RED: a Critical/High result in either review blocks the acceptance command; after a production fix, a new source SHA requires a new bundle and both reviews rerun—one stale pass cannot be reused.
  </behavior>
  <action>
    After Tasks 1-2 are committed and all local gates pass, obtain the full corrected source commit SHA. Run the publication target twice into disposable directories and require identical manifests/hashes, then once into the new empty `.planning/phases/03-create-flow-backend/03-10-review-packet/&lt;source-full-sha&gt;/` directory. Include complete unfiltered region diffs, 8 live panels, 8 approval-commit TUI panels, 8 approval-commit HTML panels, MANIFEST.md, and EVIDENCE.json. Hash every member and the canonical manifest. Never overwrite this directory; a later correction publishes under the later full SHA.

    Run two genuinely independent reviews only after the packet is immutable: (1) a clean-context `agent-ui-ux-designer`/UI reviewer through the runtime's actual agent-dispatch interface, and (2) an independently authenticated Codex CLI review. Before each dispatch, run the tool's supported authentication/status preflight discovered from its local help (do not invent a flag), and stop on unavailable/unauthenticated status. The executor must not draft or summarize either verdict as a substitute. Feed both reviewers the same manifest SHA, complete text diffs, all 24 panels, approval commit, corrected source commit, recipes, Phase 3 decisions, and CR-01..CR-11/WR-01 checklist.

    Persist each raw unedited output as UI-REVIEW.md/CODEX-REVIEW.md. In REVIEW-PROVENANCE.json record reviewer role, runtime/CLI executable path, exact version, provider and resolved model ID, authenticated status result (without secrets), run/session ID, UTC start/end, exact invocation or dispatch parameters, prompt SHA-256, raw stdout/stderr SHA-256, exit status, source commit, approval commit, and evidence-manifest SHA-256. Add a deterministic validator test before dispatch and run it after writing both outputs.

    If either review reports Critical/High, halt acceptance, fix the owning source test-first, commit the correction, generate a NEW content-addressed packet, and rerun BOTH independent reviews against it. Medium/Low requires an explicit fixed/accepted disposition with concrete rationale. Do not update 03-VALIDATION, STATE, REQUIREMENTS, or ROADMAP to passed/complete in this plan; a separate verifier reconciles status only after independently checking these artifacts. Preserve all prior plan files and the dirty 03-09 review evidence unchanged.
  </action>
  <verify>
    <automated>make test &amp;&amp; make lint &amp;&amp; make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression &amp;&amp; go test -tags screenshot -race ./cmd/gitid/... -run 'Test.*(ReviewProvenance|ImmutablePacket|BlockingVerdict)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The final packet directory name equals the full corrected source SHA, was created once, contains exactly the declared 24 panels plus evidence/review files, and every hash validates.
    - Routine `make gate-visual-regression` run after packet publication leaves `git status --short` and packet hashes unchanged.
    - UI and Codex outputs are generated by separate authenticated tools/runs after evidence publication, bind to the same evidence-manifest hash, and pass the provenance validator.
    - Neither review is executor-authored or reconstructed; raw outputs are unedited and hash-bound.
    - No Critical/High remains open in either review; every lower finding has a concrete disposition. Any corrected source SHA has two fresh reviews of its own bundle.
    - `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression` pass; no real SSH/Git path is touched; recipes, existing plans, 03-REVIEW.md, and 03-09 packet hashes remain unchanged.
  </acceptance_criteria>
  <done>A new immutable corrected evidence packet exists and two authenticated independent reviewers have rerun against exactly that packet with no open blocking finding; no phase-completion claim is made.</done>
</task>

</tasks>

<dependency_graph>

| Task | Needs | Creates | Why sequential |
|------|-------|---------|----------------|
| 1 | Existing 03-07/08 safety architecture and 03-REVIEW findings | Corrected production behavior + committed source SHA candidate | Evidence must depict corrected behavior, not known-broken code. |
| 2 | Task 1 production interfaces and tests | Deterministic read-only routine gate + fail-closed explicit publisher | The evidence schema and capture flow depend on Task 1's complete stage-2 DTO and transaction semantics. |
| 3 | Tasks 1-2 committed and green | Content-addressed packet + authenticated independent review outputs | Review is meaningful only after immutable corrected evidence exists. |

</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted form/path → OpenSSH syntax/process | Alias, host, port, and IdentityFile become config directives and argv. |
| pre-confirm staging → real SSH directory | Key/config material may cross into persistent HOME only after consent. |
| confirmed multi-file transaction → rollback | Directory modes, reused keys, public key, Include line, and Host block must commit or restore as one unit. |
| capture harness → real composition root | Evidence must retain real wiring while denying network and real-HOME effects. |
| approval commit/current source → evidence packet | Approved and live sources require distinct immutable provenance. |
| evidence packet → independent reviewers | Reviewers must receive complete, hash-bound evidence and produce authenticated non-executor outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10-01 | Tampering | pre-confirm Generate / SSH directory | critical | mitigate | Staging-only generation, temp HOME tests, final-directory snapshot, and confirmed-only create/chmod (CR-08). |
| T-03-10-02 | Tampering | reused-key transaction | critical | mitigate | Snapshot bytes/modes, explicit 0600/0644 operations, reverse rollback, and post-operation failure injection (CR-09). |
| T-03-10-03 | Spoofing | stage-2 resolution proof | high | mitigate | Error-bearing two-command proof plus full expected-field/effective-IdentityFile comparison before recording acceptance (CR-07). |
| T-03-10-04 | Tampering | IdentityFile interpolation | high | mitigate | Strict single-token validation immediately before checked render and parser/ssh-G round-trip tests (CR-11). |
| T-03-10-05 | Spoofing | algorithm/provider commit identity | high | mitigate | Explicit DTO propagation and proof/cache fingerprint binding through persisted output (CR-10, WR-01). |
| T-03-10-06 | Tampering | visual evidence generation | high | mitigate | Fixed inputs/clock/path normalization, two-run hashes, fail-closed prerequisites, temp-only routine gate, content-addressed publish (CR-01/02/05). |
| T-03-10-07 | Spoofing | approved baseline | high | mitigate | Isolated capture from full approval commit for both HTML and TUI, separate hashes and commands (CR-03). |
| T-03-10-08 | Tampering | divergence policy | high | mitigate | Per-screen applicable non-empty regions, exact normalization predicates, exhaustive negative controls, no broad exemptions (CR-04). |
| T-03-10-09 | Repudiation | independent review | high | mitigate | Authentication preflights, independent run IDs/model IDs, prompt/output hashes, raw transcripts, evidence hash binding, and rerun-on-new-SHA policy (CR-06). |
</threat_model>

<source_coverage_audit>

| SOURCE | ID | Feature/Requirement | Plan task | Status | Notes |
|--------|----|---------------------|-----------|--------|-------|
| GOAL | — | Algorithm → four-field SSH → isolated two-stage proof → confirmed store matching approved design | 1-3 | COVERED | Production correctness, evidence, then independent review. |
| REQ | SSHUI-01/02/03 | Four ordered fields, input reachability, WYSIWYG recipe preview | 1, 2 | COVERED | CR-10/11 corrections plus regression evidence. |
| REQ | SSHUI-04/05 | Throwaway testing; macOS globals after specific hosts | 1 | COVERED | CR-08 no-real-SSH mutation; D-08 preserved by transaction tests. |
| REQ | TEST-01/02/03 | Exact two-stage commands/output, complete ssh-G proof, confirmed backed-up store | 1 | COVERED | CR-07/08/09. |
| REQ | KEY-06 | Existing-key reuse | 1 | COVERED | Confirmed-only chmod plus rollback. |
| REQ | DLV-04/06 | Approval-backed visual review and real-binary PTY | 1-3 | COVERED | Corrected PTY tracer, immutable packet, two reviews. |
| REVIEW | CR-01 | Nondeterministic evidence and routine mutation | 2 | COVERED | Fixed inputs/clock/paths, temp double-run, explicit publish. |
| REVIEW | CR-02 | Stage screenshots ignore outcomes | 2 | COVERED | Valid running-state transition and distinct stage assertions. |
| REVIEW | CR-03 | Current HEAD masquerades as approval baseline | 2 | COVERED | Full approval commit HTML+TUI capture. |
| REVIEW | CR-04 | Empty/broad region policy | 2 | COVERED | Applicable non-empty schema and exhaustive controls. |
| REVIEW | CR-05 | Evidence generation fails open | 2 | COVERED | Fatal tooling/count/hash/provenance failures. |
| REVIEW | CR-06 | Reviews were not independent | 3 | COVERED | Authenticated clean-context UI + Codex runs with raw provenance. |
| REVIEW | CR-07 | Incomplete ssh-G proof | 1 | COVERED | Both command/output pairs and all expected fields. |
| REVIEW | CR-08 | Pre-confirm real SSH-dir mutation | 1 | COVERED | Staging only; confirmation creates/chmods final directory. |
| REVIEW | CR-09 | Reused private key not normalized/rolled back | 1 | COVERED | Explicit mode op and reverse rollback. |
| REVIEW | CR-10 | Algorithm lost before commit | 1 | COVERED | DTO/cache/proof/commit propagation + rsa PTY. |
| REVIEW | CR-11 | Unsafe IdentityFile syntax | 1 | COVERED | Strict token rejection plus real ssh-G proof. |
| REVIEW | WR-01 | Provider re-derived from final labels | 1 | COVERED | Validated provider is retained directly. |
| RESEARCH | — | UI-free core, real composition root, no new packages, arg-slice exec, fake SSH, filewriter safety | 1-2 | COVERED | Existing packages extended; no install task. |
| CONTEXT | D-01..D-04 | Outcome gate, warning/copy, automatic stage chain | 1-2 | COVERED | Complete proof retains PASS/ReachableNotUploaded semantics and valid state transitions. |
| CONTEXT | D-05..D-09 | Auto layout, Include default, no migrate UX, globals, collision | 1 | COVERED | Transaction tests preserve locked storage behavior. |
| CONTEXT | D-10..D-13 | Reuse picker, pair/perms, warnings, algorithm breadth | 1 | COVERED | Reused-key transaction + propagated algorithm. |
| CONTEXT | D-14..D-19 | Real shell/shared UI, preview banner, SSH-only Git skip/reason | 1-3 | COVERED | Full regression gates preserve existing locked behavior; no deferred Git wiring. |
| CONTEXT | D-20..D-21 | Provider inferred from Host; known/custom endpoints | 1 | COVERED | Provider propagated, not reconstructed. |
| CONTEXT | D-22..D-23 | Deterministic fake SSH; optional network smoke separate | 1-2 | COVERED | Offline evidence; smoke remains outside routine CI gate. |
| CONTEXT | D-24..D-25 | Automated gate + two independent reviews; severity blocks | 2-3 | COVERED | Immutable packet, authenticated reruns, new-SHA rerun policy. |

Deferred layout-migration UX, Git form writes, CLI rebuild, upload automation, and `insteadOf` rendering remain excluded exactly as 03-CONTEXT specifies.
</source_coverage_audit>

<verification>
1. Run every task's focused TDD command and retain the observed RED and GREEN outputs in 03-10-SUMMARY.md.
2. Run `TERM=dumb SSH_AUTH_SOCK= make test`, `make lint`, `TERM=dumb SSH_AUTH_SOCK= make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression`; all exit 0.
3. Run the routine visual gate twice after packet publication; `git status --short` and SHA-256 of every committed packet member remain unchanged.
4. Validate the final content-addressed packet: full corrected source SHA, full approval SHA, 8 live + 8 approved-TUI + 8 approved-HTML panels, complete region diffs, and all hashes.
5. Validate two independent authenticated review provenance records and raw outputs against the same evidence-manifest hash; reject any open Critical/High.
6. Compare baseline/final hashes for the dirty 03-REVIEW.md and 03-09 evidence paths; require exact equality. Confirm `git diff -- recipes/` and `git diff -- .planning/phases/03-create-flow-backend/03-0[1-9]-PLAN.md` are empty.
</verification>

<success_criteria>
- Every CR-01 through CR-11 and WR-01 maps to a passing regression, immutable artifact, or authenticated independent review requirement above.
- No write/chmod reaches the real SSH directory before explicit confirmation; generated and reused modes roll back correctly.
- Stage 2 fully verifies and displays `ssh -G` command/output and all expected effective fields.
- Selected algorithm and provider persist end to end; IdentityFile syntax fails closed.
- Routine visual validation is deterministic and read-only; approved HTML/TUI evidence comes from the approval commit and generation failures are fatal.
- Both genuinely independent reviewers rerun after corrected immutable evidence exists, with authenticated provenance and no open Critical/High.
- Existing plans and dirty review evidence are preserved; this plan and its summary do not claim phase completion.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-10-SUMMARY.md` when done. Record observed RED/GREEN commands, source/evidence commits, packet manifest hash, reviewer provenance, all CR/WR dispositions, and the unchanged dirty-evidence baseline/final hashes. Do not mark Phase 3 complete; hand the artifacts to an independent verifier.
</output>
