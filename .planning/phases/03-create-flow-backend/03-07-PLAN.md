---
phase: 03-create-flow-backend
plan: 07
type: execute
wave: 6
depends_on: [03-06]
files_modified:
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - internal/identity/modes.go
  - internal/identity/modes_test.go
  - internal/tester/tester.go
  - internal/tester/tester_command_test.go
  - internal/tuikit/ceremony.go
  - internal/tuikit/ceremony_test.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - e2e/create_flow_pty_e2e_test.go
  - internal/doctor/checks/reserved_test.go
autonomous: true
requirements: [SSHUI-04, TEST-01, TEST-03, KEY-06, DLV-06]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Before explicit confirmation, generated/reused key preparation and both SSH stages leave the sandbox HOME's final key paths, live SSH config, and known_hosts byte-identical or absent per D-04 and D-11."
    - "Stage 2 starts automatically after a PASS or ReachableNotUploaded stage-1 result per D-01 and D-04, with no intervening keypress."
    - "Confirmation starts persistence; a success receipt appears only after the complete write transaction succeeds, while a failure is visible and retryable without a false success claim."
    - "A failed key/include/target write rolls every earlier mutation back, preserving non-managed content and leaving no dangling Include, Host block, or key half."
    - "Configuration-read failures and a missing/currently stale test outcome block persistence rather than becoming an empty healthy state."
  artifacts:
    - path: "cmd/gitid/wiring.go"
      provides: "Hermetic staging, coordinated confirmed persistence, rollback, and fail-closed backend state"
    - path: "internal/tuikit/identities.go"
      provides: "Automatic stage chaining and persistence-result-driven ceremony flow"
    - path: "e2e/create_flow_pty_e2e_test.go"
      provides: "Real-binary temp-HOME proofs for cancellation, automatic chaining, success, and injected persistence failure"
    - path: "internal/doctor/checks/reserved_test.go"
      provides: "Doctor non-destruction coverage for final reserved artifacts and absence of abandoned staging artifacts"
  key_links:
    - from: "internal/tuikit/identities.go"
      to: "cmd/gitid/wiring.go"
      via: "confirmation dispatches an asynchronous create-commit command and handles its explicit result message"
      pattern: "Persist|Commit|Wizard.*Msg"
    - from: "cmd/gitid/wiring.go"
      to: "internal/identity/identity.go"
      via: "confirmed transaction persists staged key material and SSH output as one rollback-capable unit"
      pattern: "PersistSSH|rollback|staged"
    - from: "internal/tester/tester.go"
      to: "temporary known_hosts"
      via: "both SSH calls receive an explicit UserKnownHostsFile under the mode-0700 staging directory"
      pattern: "UserKnownHostsFile"
---

<objective>
Make the create ceremony non-destructive before consent and truthful after consent, closing review findings CR-01, CR-02, CR-07, CR-09, CR-13, CR-14 and WR-02 without changing Phase 3's product boundary.

Purpose: Restore the repository's test → confirm plus backup → re-test safety contract while retaining D-01's PASS/ReachableNotUploaded gate and D-04's automatic stage sequencing.
Output: A hermetically staged, rollback-capable SSH-only create path with explicit persistence results and real-binary regression coverage.
</objective>

<execution_context>
@/Users/ramon/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-REVIEW.md
@.planning/phases/03-create-flow-backend/deferred-items.md
@.planning/phases/03-create-flow-backend/03-06-SUMMARY.md

<interfaces>
Use the existing `identity.StagedKey` contract as the lifecycle carrier: `TempPrivatePath` is test-only, final paths are destinations, private bytes must never be logged, and existing-key reuse has no private bytes to copy. Preserve `tuikit.Backend.Persist` for unrelated actions; introduce a create-specific optional asynchronous commit seam/message so the fixture backend can retain its in-memory fallback while the real backend reports completion or failure explicitly. Preserve the backend-free `internal/tuikit` import boundary.
</interfaces>
</context>

<review_dispositions>

| Finding | Disposition | Evidence and remediation owner |
|---------|-------------|--------------------------------|
| CR-01 | CONFIRMED | `ceremony.handleKey` marks the receipt done on confirmation, while `AddIdentity` is emitted only on receipt dismissal and `PersistError` is never consumed. Tasks 1-2 reverse this order and render failure. |
| CR-02 | CONFIRMED | `Generate` writes final key paths, reuse can write a missing `.pub`, and `StrictHostKeyChecking=accept-new` defaults to the user's known_hosts. Task 1 stages all three effects under a private temp directory. |
| CR-07 | CONFIRMED | `handleMsg` stops at `testStage1`; stage 2 requires Enter. Task 2 returns the stage-2 command immediately from the accepted stage-1 message. |
| CR-09 | CONFIRMED | key, Include, and target writes are independent. Task 1 adds snapshots, ordered commit, and reverse rollback with failure injection at every boundary. |
| CR-13 | CONFIRMED | account/Host/config read errors return nil/empty state. Task 3 propagates a blocking initialization/validation error. |
| CR-14 | CONFIRMED | lexical path prefix accepts sibling names such as `config.different`. Task 3 replaces it with exact glob equivalence or separator-aware containment. |
| WR-02 | CONFIRMED | `storeUnlocked` accepts `outcomeKnown == false`. Task 3 requires stage records tied to the current create specification. |

</review_dispositions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end cancellation and confirmed transaction tracer</name>
  <files>cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/tuikit/ceremony.go, internal/tuikit/identities.go</files>
  <behavior>
    - RED: generated-key testing followed by cancellation leaves final private/public paths, live config, Include target, and known_hosts absent or byte-identical.
    - RED: injected failure after each ordered mutation restores every pre-existing byte/mode and removes every newly-created final artifact; private/public keys remain a matched pair.
    - RED: confirmation does not render `Wrote`/`Backed up` or the success message until an explicit successful commit result is handled; failure keeps the ceremony recoverable and displays the concrete error.
  </behavior>
  <action>Start with focused failing tests that snapshot a temp HOME before entering the test step, after generated-key cancellation, and after each injected persistence failure. Change generation to write the private test key only under the backend's mode-0700 staging directory while carrying final destinations in `StagedKey`. At confirmation, snapshot existence, bytes, and modes for the final private key, public key, main SSH config, and resolved Include target; apply the key pair and SSH layout in dependency order, using existing filewriter backup/atomic primitives, and roll back in reverse order on any error, including removal of transaction-created files/directories when safe. Preserve foreign config bytes and the recipe Host shape. Add an optional create-commit command/result seam at the tuikit boundary: confirmation enters a pending state, the real backend performs the transaction off the Bubble Tea update loop, and only the result message can select receipt or failure. Keep unrelated Backend.Persist actions and fixture behavior intact. Author tests before implementation, then commit the coherent tested change once green per AGENTS.md rather than shipping separate RED/GREEN commits.</action>
  <verify>
    <automated>go test -race ./internal/identity/... ./internal/tuikit/... ./cmd/gitid/... -run 'Test.*(Cancel|PreConfirm|Transaction|Rollback|PersistResult|Ceremony)' -count=1</automated>
  </verify>
  <done>Cancellation produces zero final mutations; a confirmed successful create commits the complete SSH unit before showing success; every injected failure restores the initial temp-HOME snapshot and renders a retryable failure instead of a receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Automatically chain isolated SSH stages with a private known-hosts file</name>
  <files>internal/tester/tester.go, internal/tester/tester_command_test.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - RED: delivery of stage 1 PASS or ReachableNotUploaded immediately schedules stage 2; no second Enter is sent by the PTY test.
    - RED: stage-1 Failure schedules nothing and retains retry behavior.
    - RED: both executed/displayed SSH invocations use an explicit staging-directory known_hosts path and cannot touch `$HOME/.ssh/known_hosts`.
    - RED: changing the form/key source invalidates old stage outcomes so confirmation cannot reuse proof from a stale CreateSpec.
  </behavior>
  <action>Extend tester command construction to accept the backend-provided temporary known_hosts path and append the same strict options to the displayed and executed argv; retain `accept-new` only against that isolated file. In `identitiesModel.handleMsg`, transition an accepted stage-1 result directly to running stage 2 and return `Backend.TestStage2(w.spec())` in the same Bubble Tea update, per D-04; never expose a manual stage-2 button between stages. Bind both outcomes to a stable fingerprint of the current CreateSpec/key selection and clear them whenever relevant form, algorithm, or reuse values change. Rewrite the PTY flow so one Enter starts stage 1 and observation—not another keypress—proves stage 2 appeared. Test PASS, ReachableNotUploaded, and hard Failure with the PATH-shim fake SSH required by D-22; all assertions run under a temp HOME and compare known_hosts before/after.</action>
  <verify>
    <automated>go test -race ./internal/tester/... ./internal/tuikit/... -run 'Test.*(AutoChain|Stage|KnownHosts|StaleOutcome)' -count=1 &amp;&amp; go test -tags e2e -race -timeout 180s ./e2e/... -run 'TestCreateFlow_.*(AutoChain|Reachable|Failure|NoPreConfirmMutation)' -count=1</automated>
  </verify>
  <done>One user action starts the two-stage sequence, accepted stage 1 auto-runs stage 2, failures stop, every shown command matches executed argv, and neither stage changes the live known_hosts or config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail closed on unreadable configuration, unsafe Include containment, and absent proof</name>
  <files>internal/identity/modes.go, internal/identity/modes_test.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/doctor/checks/reserved_test.go</files>
  <behavior>
    - RED: unreadable/malformed main or included config produces a blocking backend error, never an empty identity/collision set.
    - RED: reuse with a missing `.pub` derives public material in staging but does not create the sibling until confirmation; reuse with an existing `.pub` does not chmod either file before confirmation.
    - RED: an Include beneath the exact `~/.ssh/config.d` boundary is recognized; a sibling such as `config.different` is not.
    - RED: no outcomes, only stage 1, stale outcomes, or any hard Failure all refuse persistence; current accepted outcomes for both stages unlock it per D-01.
    - RED: doctor fixes never classify transaction staging/rollback residue as managed identities and never remove the final `ssh-include`, `_global`, or `gitid.config` artifacts.
  </behavior>
  <action>Add a read-only reuse-staging path in `internal/identity`: when `.pub` is absent derive its line into StagedKey state without calling the final WritePub seam; when present read it without chmod or rewrite. The confirmed backend transaction writes/normalizes the public sibling later. Replace silent nil returns in initialization, inventory, Host discovery, storage adoption, and Include detection with one recorded blocking error surfaced through the create flow before tests or confirmation. Use cleaned path equality plus `filepath.Rel`/separator-boundary checks and compare the canonical Include glob, not lexical prefix matching. Replace the single last-outcome flag with two current-spec stage records; require both accepted outcomes before persistence and fail closed when no proof exists. Extend the existing reserved-artifact doctor regression with a failed-transaction fixture and assert no destructive orphan loop is introduced. Preserve D-05/D-06 layout auto-detection, D-07 no migration choice, D-08 `_global` ordering, and all recipe-managed content.</action>
  <verify>
    <automated>go test -race ./cmd/gitid/... ./internal/doctor/checks/... -run 'Test.*(FailClosed|Include|StoreGate|Reserved|Unreadable|Malformed|Rollback)' -count=1</automated>
  </verify>
  <done>Every relevant read/parse error blocks the create path visibly, only the canonical Include boundary is accepted, persistence requires current two-stage proof, and doctor remains non-destructive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard → backend | User-controlled create values and consent cross into filesystem/network effects. |
| staging → HOME | Private key material and tested config move from disposable storage to persistent files only after confirmation. |
| SSH process → filesystem | OpenSSH may write host-key state unless explicitly redirected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07-01 | Tampering | pre-confirm key/config/known_hosts lifecycle | critical | mitigate | Mode-0700 staging, explicit temporary known_hosts, snapshot tests, and no final-path writes before consent. |
| T-03-07-02 | Repudiation | ceremony receipt | high | mitigate | Receipt is reachable only from an explicit successful persistence result; failure text carries the operation error. |
| T-03-07-03 | Tampering | multi-file confirmed write | high | mitigate | Ordered transaction plus reverse rollback and failure injection at every mutation boundary. |
| T-03-07-04 | Elevation of Privilege | unreadable config interpreted as empty | high | mitigate | Propagate parse/read errors into a blocking UI state and deny persistence. |
| T-03-07-05 | Spoofing | stale or absent test proof | high | mitigate | Bind both stage outcomes to the current CreateSpec fingerprint and require both before write. |
</threat_model>

<verification>
Run the focused commands from each task, then run `make test`, `make lint`, and `make test-e2e`. Expected output: every command exits 0; focused tests print PASS for cancellation/no-mutation, automatic chaining, transaction rollback, fail-closed reads, and reserved doctor artifacts. The e2e suite must show stage 2 without a second input and must operate only under its reported temporary HOME. Inspect `git diff -- recipes/` and require no recipe changes.
</verification>

<success_criteria>
- CR-01, CR-02, CR-07, CR-09, CR-13, CR-14, and WR-02 have passing regression tests and no remaining execution path matching the reviewed behavior.
- Generated/reused key handling obeys D-11 and final private/public modes are 0600/0644 only after confirmation.
- The resulting Host block remains recipe-faithful: alias, alt-SSH endpoint/port, `User git`, explicit `IdentityFile`, and `IdentitiesOnly yes`.
- All writes remain timestamp-backed, idempotent, atomic per file, coordinated as one create transaction, and preserve foreign bytes.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-07-SUMMARY.md` when done.
</output>
