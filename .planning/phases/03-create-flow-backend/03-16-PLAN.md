---
phase: 03-create-flow-backend
plan: 16
type: tdd
wave: 15
depends_on: [03-14]
revises: [03-15]
gap_closure: true
autonomous: false
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, SSHUI-04, SSHUI-05, TEST-01, TEST-02, TEST-03, KEY-06, DLV-04, DLV-06]
files_modified:
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_test.go
  - internal/screenshot/createflow_packet.go
  - internal/screenshot/createflow_packet_test.go
  - cmd/gitid-evidence/main.go
  - cmd/gitid-evidence/main_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - internal/tuikit/identities_test.go
  - e2e/create_flow_pty_e2e_test.go
  - .planning/phases/03-create-flow-backend/03-16-SUMMARY.md
  - .planning/phases/03-create-flow-backend/03-16-review-packet/<source-sha>/
must_haves:
  truths:
    - "Exactly one configured review record, opencode-my-plan-review via opencode using local-llm-env/my-plan-review, is necessary and sufficient for finalization; every substitute, duplicate, missing, stale, nonzero, or blocker verdict fails closed."
    - "The full normalized stage-two identityfile line can authorize only from a focused proof viewport frame. A complete duplicate outside that region, a generic token, or a clipped line cannot authorize evidence."
    - "Both in-process and real-PTY TUI capture either produce every RequiredScreenSpecs member with its authorizing region and exact marker or return an error before creating candidate evidence."
    - "Phase 3 candidate and routine evidence compare only the compiled TUI and cmd/gitid-dummy; browser, HTML, Chromium, and pnpm tooling are absent from the candidate and routine-gate execution paths."
    - "All targeted tests exist before their selected gates run, and pre-existing dirty packet artifacts are checksum-verified unchanged around every capture gate."
  artifacts:
    - path: internal/screenshot/createflow_packet.go
      provides: Single-record configured-review validation and final-packet provenance.
    - path: internal/screenshot/createflow.go
      provides: Region-scoped dynamic marker authorization and complete TUI capture contract.
    - path: cmd/gitid-evidence/main.go
      provides: Single-review finalizer CLI, TUI-only candidate capture, and GOPATH-aware freeze preflight.
---

<objective>
Resolve every HIGH concern in `03-15-REVIEWS.md` before executing the remaining Phase 3 evidence correction. Preserve all existing dirty and untracked artifacts, use disposable HOME and fake SSH only, and keep `cmd/gitid-dummy` as the sole visual reference.
</objective>

<execution_safety>
- Before each task, record `git status --short`, HEAD, and SHA-256 values for every pre-existing dirty tracked path and every pre-existing Phase 3 packet directory. Compare them after each task and all capture gates. Do not reset, clean, restore, delete, or stage those paths.
- State the hypothesis, run the named RED test, then implement the smallest change that makes it green. Do not commit a red tree and never use `--no-verify`.
- Run sequentially per L11. Every behavioral test uses a new disposable HOME, public fixtures, and a PATH-prefixed fake SSH. Do not invoke gh, glab, network smoke, browser tooling, or any product write against the real HOME.
- Before each selective `go test -run` gate, run `go test -tags <tag> -list "^<exact-test-name>$" <package> | rg -qx "<exact-test-name>"` once per named test and fail if any assertion is absent. This applies to screenshot, e2e, and routine-gate tests. `go test -list` exit status alone is not sufficient.
</execution_safety>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Replace the obsolete two-review finalization contract</name>
<files>internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go</files>
<read_first>.planning/config.json, 03-15-REVIEWS.md, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go</read_first>
<behavior>
- RED: `FinalizeCandidate` and `ValidateReviewProvenance` reject zero, duplicate, substitute, CLI-mismatched, model-mismatched, stale, nonzero, empty-provider, provider-inconsistent, or blocking review records.
- RED: one fresh zero-blocker record with the exact configured reviewer, CLI, and model finalizes successfully. The provider remains nonempty and internally consistent provenance, not a configured literal.
- RED: the CLI accepts exactly one `--review-dir`; `TestFinalizeCLIRejectsSecondReviewDirectory` proves a second directory and all old two-review assumptions fail.
</behavior>
<action>
Replace every `len(...)=2` and distinct-two-review rule with one configured review record. Introduce one small shared configured-review validator used before asset copy and when reading stored provenance. Set new final-review provenance to version `03-16.1`; prior packet directories remain byte-identical and are not regenerated or rewritten. Rewrite `TestFinalizeCandidateRequiresTwoBoundDistinctZeroBlockerReviews` into an explicitly named single-configured-review positive test, then add each directly-selectable negative test named by Plan 15. Do not read configuration at runtime; the exact reviewer/CLI/model constants must match `.planning/config.json` and be asserted by tests. Update finalizer argument parsing and help text to require one review directory.
</action>
<verify>
<automated>for test in TestFinalizeCandidateSingleConfiguredReview TestFinalizeCandidateRejectsSubstituteReviewer TestFinalizeCandidateRejectsReviewerCLIMismatch TestFinalizeCandidateRejectsReviewerModelMismatch TestFinalizeCandidateRejectsEmptyReviewerProvider TestFinalizeCandidateRejectsTamperedReviewerProviderProvenance TestFinalizeCandidateRejectsDuplicateConfiguredReviewer TestFinalizeCandidateRejectsMissingConfiguredReviewer TestFinalizeCandidateRejectsStaleOrMismatchedConfiguredReview TestValidateReviewProvenanceRejectsSubstituteReviewerTriple TestValidateReviewProvenanceRejectsCLIOrModelMismatch TestValidateReviewProvenanceRejectsEmptyProvider TestValidateReviewProvenanceRejectsTamperedProviderProvenance TestValidateReviewProvenanceRejectsDuplicateMissingStaleOrMismatchedTriple TestFinalizeCLIAcceptsOneReviewDirectory TestFinalizeCLIRejectsSecondReviewDirectory; do go test -tags screenshot -list "^${test}$" ./internal/screenshot/... ./cmd/gitid-evidence/... | rg -qx "${test}" || exit 1; done &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test(FinalizeCandidate|ValidateReviewProvenance|FinalizeCLI)'</automated>
</verify>
<done>Finalization accepts only one fresh zero-blocker record from `opencode-my-plan-review` / `opencode` / `local-llm-env/my-plan-review` and cannot silently retain a two-review path.</done>
</task>

<task type="tracer" tdd="true">
<name>Task 2: Make focused-region markers and both TUI capture engines fail closed</name>
<files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_test.go, internal/screenshot/createflow_packet_test.go, internal/tuikit/identities_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go, cmd/gitid/gate_visual_regression_test.go, e2e/create_flow_pty_e2e_test.go</files>
<read_first>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_test.go, cmd/gitid-evidence/main.go, cmd/gitid/gate_visual_regression_test.go, e2e/create_flow_pty_e2e_test.go</read_first>
<behavior>
- RED: a generic `identityfile`, clipped identityfile line, or complete line outside the focused proof region is rejected; the complete normalized first line from the captured stage-two resolution output inside a navigated focused frame passes.
- RED: confirmation sentinels and key paths outside their declared viewport do not authorize a frame.
- RED: missing required frame, empty authorizing region, unreachable marker, or partial map returns an error from both in-process and real-PTY capture before any candidate member is written.
- RED: the routine gate derives the two-surface inventory from `RequiredScreenSpecs`, requires classifications for all differences, and has no Phase 3 browser/HTML dependency.
</behavior>
<action>
Pass an explicit capture context containing the normalized stage-two resolution marker to `ValidateCapturedState`; do not infer a dynamic marker from a static ScreenSpec. Each ScreenSpec names a single authorizing extractor. Capture a bounded sequence of focused proof frames, and define success as the union of validated navigated frames, not impossible single-frame visibility for a line wider than the viewport. Change `CaptureCreateFlowScreens` to return `(map[string]string, error)` and migrate every caller atomically. Apply the same complete-inventory validation before real-PTY PNG capture. Remove Phase 3 approved-HTML capture, pnpm, Chromium, and browser provenance from candidate generation and the routine gate only; historical helpers may remain but must not be reachable from either path. Add `TestCandidateUsesOnlyTUISurfaces` and a routine-gate assertion that candidate/routine members contain only real and dummy TUI surfaces and no browser tool. Resolve freeze through `go env GOPATH` if it is absent from PATH. Add exact named tests for every selective gate and a test that asserts all selected names exist.
</action>
<verify>
<automated>for test in TestValidateCapturedStateRejectsOutOfViewportDuplicate TestValidateCapturedStateRequiresCompleteResolvedIdentityFile TestConfirmationRegionRejectsOutOfViewportDuplicate TestCaptureCreateFlowScreensFailsOnMissingRequiredFrame TestCaptureCreateFlowScreensFailsOnMissingRegionMarker TestCaptureTUIScreenExactProofFrames TestCaptureTUIScreenConfirmationKeyPathFrame TestCaptureTUIScreenConfirmationManagedBlockFrame TestCandidateUsesOnlyTUISurfaces TestCandidateToolPreflight; do go test -tags screenshot -list "^${test}$" ./internal/screenshot/... ./cmd/gitid-evidence/... ./cmd/gitid/... | rg -qx "${test}" || exit 1; done &amp;&amp; for test in TestProofViewport TestConfirmationViewport TestConfirmationViewportRoutesAdvertisedControls; do go test -list "^${test}$" ./internal/tuikit/... | rg -qx "${test}" || exit 1; done &amp;&amp; for test in TestCreateFlow_CompletedStageExactProofViewport TestCreateFlow_ConfirmationExactViewport; do go test -tags e2e -list "^${test}$" ./e2e/... | rg -qx "${test}" || exit 1; done &amp;&amp; for test in TestGateVisualRegression TestGateVisualRegressionReadOnly TestApprovalCommitRecorded TestAllScreensCapturedAndNonEmpty TestNegativeControls_AllProtectedRegionsDetectMutation; do go test -tags screenshot -list "^${test}$" ./cmd/gitid/... | rg -qx "${test}" || exit 1; done &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... ./cmd/gitid/... -run 'Test(ValidateCapturedStateRejectsOutOfViewportDuplicate|ValidateCapturedStateRequiresCompleteResolvedIdentityFile|ConfirmationRegionRejectsOutOfViewportDuplicate|CaptureCreateFlowScreensFailsOnMissingRequiredFrame|CaptureCreateFlowScreensFailsOnMissingRegionMarker|CaptureTUIScreenExactProofFrames|CaptureTUIScreenConfirmationKeyPathFrame|CaptureTUIScreenConfirmationManagedBlockFrame|CandidateUsesOnlyTUISurfaces|CandidateToolPreflight)$' && TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... -run 'Test(ProofViewport|ConfirmationViewport|ConfirmationViewportRoutesAdvertisedControls)$' && TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 300s ./e2e/... -run 'TestCreateFlow_(CompletedStageExactProofViewport|ConfirmationExactViewport)$' && make gate-visual-regression</automated>
</verify>
<done>Evidence authorization is region-bound, complete, TUI-only, and fail closed in both capture engines.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Verify protected artifacts and create the unfinalized candidate</name>
<files>cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go, .planning/phases/03-create-flow-backend/03-16-SUMMARY.md</files>
<action>
After Tasks 1-2 are committed with tests, assert `TestGateVisualRegression`, `TestGateVisualRegressionReadOnly`, `TestApprovalCommitRecorded`, `TestAllScreensCapturedAndNonEmpty`, and `TestNegativeControls_AllProtectedRegionsDetectMutation` each appear in `go test -tags screenshot -list` output for `./cmd/gitid/...`; do not rely on list exit status. Then run `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression`. Record every gate result and pre/post checksums in the summary. Build the screenshot evidence binary, generate two process-isolated TUI-only candidates, compare their canonical manifest bytes and member-hash inventories with `cmp`, retain one candidate outside the repository, and record both candidate hashes and the retained path. Do not review, create a review directory, author review assets, call finalization, publish, edit phase status documents, or mark Phase 3 complete in this task.
</action>
<verify>
<automated>for test in TestGateVisualRegression TestGateVisualRegressionReadOnly TestApprovalCommitRecorded TestAllScreensCapturedAndNonEmpty TestNegativeControls_AllProtectedRegionsDetectMutation; do go test -tags screenshot -list "^${test}$" ./cmd/gitid/... | rg -qx "${test}" || exit 1; done &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test &amp;&amp; make lint &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression &amp;&amp; HANDOFF_ROOT="$(mktemp -d "${TMPDIR:-/tmp}/gitid-03-16-handoff.XXXXXX")" &amp;&amp; go build -tags screenshot -o "$HANDOFF_ROOT/gitid-evidence" ./cmd/gitid-evidence &amp;&amp; "$HANDOFF_ROOT/gitid-evidence" --candidate --source-commit "$(git rev-parse HEAD)" --output-dir "$HANDOFF_ROOT/candidate-a" &amp;&amp; "$HANDOFF_ROOT/gitid-evidence" --candidate --source-commit "$(git rev-parse HEAD)" --output-dir "$HANDOFF_ROOT/candidate-b" &amp;&amp; cmp "$HANDOFF_ROOT/candidate-a/CANDIDATE-MANIFEST.json" "$HANDOFF_ROOT/candidate-b/CANDIDATE-MANIFEST.json"</automated>
</verify>
<done>A clean committed source has a deterministic candidate, all protected artifacts are unchanged, and exactly one configured-review handoff is ready without any substitute reviewer or external mutation.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
<what-built>One committed source has passed the full local gates and produced an unfinalized, deterministic, TUI-only candidate whose path and hashes are recorded in `03-16-SUMMARY.md`.</what-built>
<action>The orchestrator, not the executor, invokes exactly the configured default reviewer with no selector flags: `opencode-my-plan-review` through `opencode` using `local-llm-env/my-plan-review`. Preserve raw review output, one nonempty internally consistent provider provenance record, and a machine-readable zero-blocker verdict bound to the recorded source SHA and candidate manifest hash. No Claude, Grok, Kimi, Codex, `--all`, substitute reviewer, or executor-authored review is permitted. Only when this exact review returns zero Critical/High may the orchestrator run the one-review finalizer and atomically publish `.planning/phases/03-create-flow-backend/03-16-review-packet/&lt;source-sha&gt;/`.</action>
<verification>Verify the review record matches the configured reviewer/CLI/model triple, candidate hash, and source SHA before finalization, then run the finalizer and `ValidatePacket`. On any mismatch, nonzero exit, missing provider, provider inconsistency, or Critical/High finding, do not finalize; create a fresh corrective plan and candidate.</verification>
<resume-signal>Orchestrator review and finalization complete, or corrective findings recorded.</resume-signal>
</task>
</tasks>

<success_criteria>
- Every HIGH finding in `03-15-REVIEWS.md` is covered by a direct test and executable task.
- No planned test command can pass because its selected test does not exist.
- The final packet remains blocked until exactly the configured default reviewer returns a fresh zero-blocker review of the exact candidate.
- No browser, external account, real SSH/Git configuration, or unrelated dirty path is touched.
</success_criteria>
