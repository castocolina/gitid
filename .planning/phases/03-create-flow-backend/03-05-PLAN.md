---
phase: 03-create-flow-backend
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - internal/tuikit/identities.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - internal/tuikit/identities_test.go
  - Makefile
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "Both test stages show the EXACT command run and the real ssh output; the shown command is byte-identical to what the process executes"
    - "A 'Permission denied (publickey)' result renders the NEW yellow `!` warning state 'Reachable — key not uploaded yet', never a red failure"
    - "At the warning state the user can copy the .pub to the clipboard and sees one hint line naming the provider's key-settings page"
    - "Stage 2 chains instantly after a stage-1 success when an alias is set; a hard Failure stops with a retry affordance"
    - "In the REAL binary the Git-form [ Continue ] stays disabled with the reason 'Git configuration arrives with the next build'; the dummy keeps 'needs user.name + a valid email'; Skip Git proceeds to the SSH-only review ceremony"
    - "internal/tuikit branches ONLY on the tuikit-local tuikit.TestOutcome enum carried in tuikit.TestResultView — no tuikit file names tester.Outcome or tester.ReachableNotUploaded, and the no-backend import-graph gate still passes (Codex HIGH #5)"
  artifacts:
    - path: "internal/tuikit/identities.go"
      provides: "D-02 ReachableNotUploaded render + D-03 copy-pub hint + exact-command render + D-19 real git-disabled reason + D-18 functional skip — all driven by the tuikit-local TestResultView/TestOutcome DTOs (never a tester type)"
      contains: "Reachable — key not uploaded yet"
  key_links:
    - from: "internal/tuikit test-stage render"
      to: "tuikit.TestOutcome (TestOutcomePass/TestOutcomeReachableNotUploaded/TestOutcomeFailure)"
      via: "the tuikit-local enum carried in tuikit.TestResultView drives the outcome line; the backend substring classification stays in internal/tester and is converted in cmd/gitid/wiring.go — never an exit code, never a tester import inside tuikit"
      pattern: "TestOutcomeReachableNotUploaded"
    - from: "internal/tuikit exact-command render"
      to: "tuikit.TestResultView.Command"
      via: "wiring.go populates TestResultView.Command from tester.PreWriteCommand / tester.ResolvedViaCommand, so shown == run (TEST-01/02) with no backend type crossing into tuikit"
      pattern: "TestResultView"
    - from: "warning-state copy-pub action"
      to: "internal/clipboard.Copy (via Backend seam)"
      via: "D-03 copy .pub only, never PrivPEM"
      pattern: "Copy|clipboard"
---

<objective>
Wire the connectivity-test outcome screens and the three remaining scoped
divergences of the create flow — the deltas the approved demo did not have.

- **TEST-01 / TEST-02**: both stages show the EXACT command (from
  `tester.PreWriteCommand` / `tester.ResolvedViaCommand`) and the real output;
  stage 2 (`ssh -G` alias resolution) chains instantly after a stage-1 success
  when an alias is set (D-04); a hard Failure stops with retry.
- **D-02 / D-01 (RESEARCH Pitfall 6)**: a `ReachableNotUploaded` outcome (the
  exact "Permission denied (publickey)" substring) renders the NEW yellow `!`
  warning state "Reachable — key not uploaded yet" — NOT the red failure
  styling; the store gate unlocks on PASS OR ReachableNotUploaded.
- **D-03**: at that warning state, a copy-.pub-to-clipboard action + ONE hint
  line naming the provider's key-settings page (no duplicated Phase-9 upload copy).
- **D-19 / D-18**: in the REAL binary the Git-form `[ Continue ]` stays disabled
  with the reason "Git configuration arrives with the next build" (the dummy
  keeps "needs user.name + a valid email" unchanged); Skip Git is FUNCTIONAL —
  it proceeds to the review ceremony writing SSH artifacts only, storing the
  identity SSH-only/incomplete.

This is a UI-wave: `/mui` + `agent-ui-ux-designer` engaged; the new states are
captured for the plan-03-06 visual-regression gate and join the §6 copy-freeze.

Purpose: complete the create flow's real behavior with honest, recipe- and
contract-faithful outcome/divergence rendering.
Output: the three scoped divergences + exact-command render + functional skip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@recipes/README.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-PATTERNS.md
@.planning/design/create-flow/FIELDS.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md
</context>

## Artifacts this phase produces (plan 03-05)

New/changed symbols (exclude from source-drift flagging):

- D-02 ReachableNotUploaded warning render in `internal/tuikit/identities.go` (frozen string `! Reachable — key not uploaded yet`), branched on `tuikit.TestOutcomeReachableNotUploaded`
- D-03 copy-pub hint line + clipboard action at the warning state
- Exact-command render for both stages from `tuikit.TestResultView.Command` (populated in `cmd/gitid/wiring.go` from `tester.PreWriteCommand` / `tester.ResolvedViaCommand`)
- Consumption of the tuikit-local DTOs defined in plan 03-02 (`tuikit.TestOutcome` with its three constants, `tuikit.TestResultView`) — this plan adds NO first-party backend import to `internal/tuikit` (Codex HIGH #5)
- Frozen result-copy string for the stored-at-warning state: `Stored — key not uploaded yet; this identity is not proven for Git yet` (§6 copy-freeze entry)
- D-19 real-binary git-disabled reason (frozen string `— Git configuration arrives with the next build`) alongside the unchanged dummy string
- Copy-freeze §6 gate entries for the two new frozen strings (asserting BOTH binaries' strings)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02 ReachableNotUploaded warning state + exact-command render (TEST-01/02, Pitfall 6)</name>
  <files>internal/tuikit/identities.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/tuikit/identities_test.go, Makefile</files>
  <read_first>
    - internal/tuikit/identities.go (the test-stage render: the stage-1/stage-2 output block ~lines 2160-2215; stage1Cmd/stage2Cmd effect points; the test-fail render currently red)
    - internal/tuikit/views.go (from plan 03-02 — the tuikit-local `TestOutcome` enum with EXACTLY `TestOutcomePass`/`TestOutcomeReachableNotUploaded`/`TestOutcomeFailure`, and `TestResultView{Outcome, Command, Detail}`: the ONLY outcome/command types tuikit may name)
    - internal/tester/tester.go (Outcome: PASS/ReachableNotUploaded/Failure ~lines 11-21; ClassifyPreWrite ~50-59; PreWriteCommand ~61-70; ResolvedViaCommand from plan 03-01) — READ FOR THE CONVERSION SIDE ONLY: these types are consumed in `cmd/gitid/wiring.go` (plan 03-03), NEVER imported by `internal/tuikit`
    - cmd/gitid/wiring.go (the composition root from plan 03-03 — where `toTestResultView(tester.Result, string) tuikit.TestResultView` maps the backend outcome + exact command into the view DTO)
    - internal/dummytui/data.go (CreateFlowTestFailOutput = "git@ssh.github.com: Permission denied (publickey)." — the exact string that classifies as ReachableNotUploaded, NOT Failure)
    - .planning/phases/03-create-flow-backend/03-UI-SPEC.md (Scoped Divergences → D-02: EXISTING Theme.Warning + `!`, replaces the outcome line, draft copy; store-gate consequence key-unused not complete; allowlist entry for both stage screen IDs)
    - .planning/phases/03-create-flow-backend/03-RESEARCH.md (Pitfall 6 — "Permission denied (publickey)" is ReachableNotUploaded; reserve red for connection-refused/DNS/timeout; Pitfall 7 — render exact command from tester, not a hand-built string)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-01 store gate PASS-or-ReachableNotUploaded; D-04 stage sequencing auto-chain)
  </read_first>
  <behavior>
    - Stage-1/2 render shows `$ <exact command>` from `TestResultView.Command` (populated in wiring.go from `tester.PreWriteCommand` for stage 1 / `tester.ResolvedViaCommand` for stage 2) — byte-identical to the executed argv — followed by the real ssh output in `TestResultView.Detail`.
    - `TestResultView.Outcome == tuikit.TestOutcomePass` → green `✓` success; `== tuikit.TestOutcomeReachableNotUploaded` → yellow `!` "Reachable — key not uploaded yet" (NOT red); `== tuikit.TestOutcomeFailure` → red `✗` with the exact output + retry affordance.
    - "Permission denied (publickey)." classifies (in `internal/tester`, converted in wiring.go) as `TestOutcomeReachableNotUploaded` and renders the yellow warning, never red (Pitfall 6). Connection-refused/DNS/timeout classify as `TestOutcomeFailure` → red.
    - Store gate unlocks on `TestOutcomePass` OR `TestOutcomeReachableNotUploaded` (D-01); an identity stored at the warning outcome carries the key-unused taxonomy state and the ceremony/result copy says so plainly (no silent "success").
    - Stage 2 auto-chains instantly after a stage-1 success when an alias is set (D-04); a stage-1 `TestOutcomeFailure` stops with retry, no auto-chain.
    - The outcome classification comes from `tester.ClassifyPreWrite`/`Result.Outcome` in the BACKEND, mapped to the tuikit enum in wiring.go — never from a process exit code, and never by tuikit importing tester.
  </behavior>
  <action>
    Add the warning branch to the test-stage outcome render in tuikit: when `TestResultView.Outcome == tuikit.TestOutcomeReachableNotUploaded`, render the frozen line `! Reachable — key not uploaded yet` in the EXISTING `Theme.Warning` role with the EXISTING `!` glyph + word — replacing the pass/failure rendering on that outcome row (no new row, no new screen). Reserve the red `✗`/`Theme.Error` rendering for `tuikit.TestOutcomeFailure` only. Render both stages' `$ <command>` line from `TestResultView.Command` (populated in `cmd/gitid/wiring.go` by `toTestResultView` from `tester.PreWriteCommand` / `tester.ResolvedViaCommand`) so shown==run (TEST-01/02). **tuikit MUST NOT import `internal/tester`** — it names only `tuikit.TestOutcome*` / `tuikit.TestResultView`; the substring classification stays in the backend and crosses the boundary exactly once, in wiring.go (Codex HIGH #5; the sanctioned remedy is a view DTO, never widening the no-backend allowlist). Ensure the store gate (persist unlock, wired in plan 03-03) is driven by `TestOutcomePass || TestOutcomeReachableNotUploaded` and that the review/result copy names the key-unused state for a warning-outcome store, using the frozen string `Stored — key not uploaded yet; this identity is not proven for Git yet` (do NOT invent a variant; add it to the §6 gate). Keep the D-04 stage-1→stage-2 instant auto-chain on success and the stage-1-failure retry stop. Register BOTH new frozen strings with the copy-freeze mechanism CONCRETELY — note that the §6 copy freeze is enforced by Go test assertions (the frozen-string tests in `internal/dummytui/identities_test.go`, e.g. the "missing the frozen adjacent hint" style checks), NOT by a standalone Makefile grep target: add explicit assertions for `! Reachable — key not uploaded yet` and `Stored — key not uploaded yet; this identity is not proven for Git yet` in the corresponding `internal/tuikit` frozen-string test, so `make test` is the gate that fails if either string drifts. Name the test function you add/extend in the SUMMARY. Add tests: a tuikit test with a stub Backend returning each `tuikit.TestOutcome` asserts the warning-vs-error rendering; a `cmd/gitid/wiring_test.go` test asserts `toTestResultView` maps all three tester outcomes to the correct tuikit constants and that "Permission denied (publickey)." → `TestOutcomeReachableNotUploaded` (warning, not red), plus that the store gate unlocks on that outcome. RED-stub convention for new assertions. Engage `/mui` + `agent-ui-ux-designer` on the warning-state visual and record input in the SUMMARY.
  </action>
  <verify>
    <automated>go test -race ./internal/tuikit/... ./cmd/gitid/... -run 'TestOutcome|TestReachable|TestStage|TestStoreGate'</automated>
  </verify>
  <acceptance_criteria>
    - "Permission denied (publickey)." maps to `tuikit.TestOutcomeReachableNotUploaded` and renders `! Reachable — key not uploaded yet` in Theme.Warning (yellow) with `!` — asserted; it never renders red/`✗` (grep-negative in that test's captured output)
    - `TestOutcomePass`→green `✓`, `TestOutcomeFailure`(connection-refused/timeout)→red `✗` + retry — asserted per outcome
    - Both stage command lines equal `TestResultView.Command` as populated from `tester.PreWriteCommand`/`tester.ResolvedViaCommand` (shown==run) — asserted
    - The store gate unlocks on `TestOutcomePass` or `TestOutcomeReachableNotUploaded` and blocks on `TestOutcomeFailure`; a warning-outcome store surfaces the key-unused state with the frozen copy `Stored — key not uploaded yet; this identity is not proven for Git yet` (no "success" framing)
    - `grep -rq 'Reachable — key not uploaded yet' internal/tuikit` passes
    - BOTH frozen strings are asserted by a named Go test (the copy-freeze mechanism is test-based, not a Makefile grep target): a test in `internal/tuikit` asserts `! Reachable — key not uploaded yet` AND `Stored — key not uploaded yet; this identity is not proven for Git yet` byte-exactly, so `make test` goes red if either drifts — the SUMMARY names the test function
    - **`internal/tuikit` does not import `internal/tester`**: `go list -deps ./internal/tuikit | grep -q 'castocolina/gitid/internal/tester'` exits NON-zero (no match), and `grep -rn 'tester\.' internal/tuikit/` returns nothing — the Codex HIGH #5 boundary holds after this plan's deltas
    - `toTestResultView` in `cmd/gitid/wiring.go` maps all three tester outcomes to the three tuikit constants — asserted exhaustively in `cmd/gitid/wiring_test.go`
  </acceptance_criteria>
  <done>The test screens show exact commands + real output and render ReachableNotUploaded as an honest yellow warning that still unlocks store; red is reserved for real failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-03 copy-.pub action + provider key-settings hint at the warning state</name>
  <files>internal/tuikit/identities.go, cmd/gitid/wiring.go, internal/tuikit/identities_test.go</files>
  <read_first>
    - internal/tuikit/identities.go (the outcome-line render from Task 1 — where the D-03 follow-on hint + action attach; the hint-persistence pattern that never collapses)
    - internal/clipboard (Copy — the D-03 clipboard action, reused via the Backend seam)
    - .planning/phases/03-create-flow-backend/03-UI-SPEC.md (D-02 follow-on affordance D-03: directly below the outcome line, ONE hint line naming the provider's key-settings page + copy-.pub action, EXISTING Theme.Hint role, hint-persistence pattern; no duplicated UP-01 copy)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-03 — copy .pub to clipboard + ONE hint line naming the provider's key-settings page; full instructions/automation stay in Phase 9)
    - .planning/phases/03-create-flow-backend/03-RESEARCH.md (V7/V10 — never log/copy PrivPEM; clipboard = .pub line only)
  </read_first>
  <behavior>
    - At ReachableNotUploaded, directly below the outcome line, ONE persistent hint line (Theme.Hint) names the provider's key-settings page (derived from the provider, e.g. github → GitHub key settings) and a copy-.pub action is offered (keybar/footer action).
    - Triggering copy places ONLY the .pub authorized-key line on the clipboard (via internal/clipboard.Copy through the Backend seam) — never PrivPEM.
    - The hint is a reserved line that does not collapse (hint-persistence pattern); it is NOT shown at PASS or Failure.
    - The copy is a Phase-3-scoped affordance — it does NOT duplicate Phase 9's full UP-01 upload instructions.
  </behavior>
  <action>
    Below the ReachableNotUploaded outcome line, render ONE persistent hint line in the EXISTING `Theme.Hint` role naming the provider's key-settings page (map the provider to a short label; do not embed full Phase-9 upload steps — that is deferred, D-03). Add a copy-.pub action (a footer/keybar action) that calls the Backend clipboard seam (real: `internal/clipboard.Copy` of the .pub authorized-key line ONLY — never PrivPEM). Reuse the existing hint-persistence pattern (reserved, non-collapsing). Show this affordance ONLY at ReachableNotUploaded. Add a tuikit test asserting the hint line + copy action appear at ReachableNotUploaded and are absent at PASS/Failure, and that the copy seam receives the .pub line, not key material. RED-stub convention.
  </action>
  <verify>
    <automated>go test -race ./internal/tuikit/... -run 'TestCopyPub|TestWarningHint|TestReachableHint'</automated>
  </verify>
  <acceptance_criteria>
    - At ReachableNotUploaded a Theme.Hint line names the provider's key-settings page and a copy-.pub action is present; both are ABSENT at PASS/Failure (asserted)
    - The copy action passes ONLY the .pub line to the clipboard seam (test asserts the copied value contains the authorized-key line and no private-key material)
    - The hint does not duplicate Phase-9 upload instructions (short key-settings reference only)
  </acceptance_criteria>
  <done>The warning state offers a scoped copy-.pub + key-settings hint without leaking private material or pre-empting Phase 9.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-19 real git-disabled reason + D-18 functional Skip Git (two frozen strings, two binaries)</name>
  <files>internal/tuikit/identities.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/tuikit/identities_test.go, Makefile</files>
  <read_first>
    - internal/tuikit/identities.go (the Git-identity wizard step: the [ Skip Git ] / [ Continue ] one-row buttons ~2051 wizardButton, the disabled-reason row, the step transitions; finishIdentity/stepForward)
    - .planning/phases/03-create-flow-backend/03-UI-SPEC.md (D-19: REAL-binary [ Continue ] stays disabled with `— Git configuration arrives with the next build`; dummy keeps `— needs user.name + a valid email`; two frozen strings, two binaries, same slot; copy-freeze must assert BOTH; D-18 Skip functional; Copywriting Contract git-step frozen Skip/Continue labels + hints byte-exact)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-18 Skip functional → review ceremony SSH-only, identity SSH-only/incomplete; D-19 new scoped-divergence reason, do not reuse the validation reason to lie about capability)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md (§4 frozen git-step copy; §6 copy-freeze grep gate — extend to assert both strings in their respective binaries)
  </read_first>
  <behavior>
    - REAL binary (cmd/gitid): the Git-identity step's `[ Continue ]` is ALWAYS disabled with the reason `— Git configuration arrives with the next build` (regardless of the form's client-side validity) — because there is no Git backend yet (Phase 4).
    - Dummy (internal/dummytui / FixtureBackend): UNCHANGED — `[ Continue ]` keeps its `— needs user.name + a valid email` validation reason. The two strings are deliberately different, in deliberately different binaries, for the same slot.
    - `[ Skip Git ]` is FUNCTIONAL in both: it proceeds to the review ceremony writing SSH artifacts ONLY; the stored identity is SSH-only and marked incomplete (the frozen hint already says so).
    - The frozen Skip/Continue labels + hints stay byte-exact.
  </behavior>
  <action>
    Drive the Git-step `[ Continue ]` disabled reason through the Backend seam so the REAL binary supplies `— Git configuration arrives with the next build` while the dummy's FixtureBackend keeps `— needs user.name + a valid email`. In the real binary, `[ Continue ]` is disabled unconditionally at this step (D-19) — do not reuse the demo's validity-driven reason. Make `[ Skip Git ]` functional (D-18): Skip advances to the review ceremony with SSH-only artifacts (no Git fragment/includeIf/allowed_signers — those are Phase 4), and the persisted identity is SSH-only/incomplete. Keep the frozen labels/hints byte-exact. Extend the §6 copy-freeze grep gate to assert BOTH strings: `grep -rq '— Git configuration arrives with the next build' cmd/gitid internal/tuikit` (real path present) AND `grep -rq '— needs user.name + a valid email' internal/dummytui` (dummy string preserved, unchanged) — the executor must NOT collapse these into one string or delete the dummy's original. Add tests: a wiring_test.go/tuikit test asserts the real binary's disabled reason string and that Skip stores an SSH-only incomplete identity; a dummytui test (or the existing frozen-string test) confirms the dummy still shows its original reason. RED-stub convention.
  </action>
  <verify>
    <automated>go test -race ./internal/tuikit/... ./cmd/gitid/... ./internal/dummytui/... -run 'TestGitStep|TestContinueDisabled|TestSkipGit|TestFrozen'</automated>
  </verify>
  <acceptance_criteria>
    - Real binary Git-step `[ Continue ]` is disabled with exactly `— Git configuration arrives with the next build` (asserted); it is disabled regardless of form validity
    - Dummy still renders `— needs user.name + a valid email` (unchanged) — both grep assertions in the §6 gate pass
    - Skip Git advances to the review ceremony and stores an SSH-only, incomplete identity (no Git artifacts written) — asserted end-to-end via the real seam
    - The frozen Skip/Continue labels + hints are byte-exact (existing frozen-string tests green)
  </acceptance_criteria>
  <done>The real Git step honestly signals "not yet" via its own frozen reason while the dummy is untouched; Skip Git works, storing an SSH-only identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ssh output → outcome classification | untrusted ssh output classified by substring, never trusted exit code |
| .pub → clipboard | only the public authorized-key line may cross to the clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Spoofing / Repudiation | test-outcome mis-render | mitigate | Outcome from tester.ClassifyPreWrite substring in the BACKEND, never exit code, mapped once in wiring.go to tuikit.TestOutcome; "Permission denied" → TestOutcomeReachableNotUploaded warning, never a false red failure or false green success (Pitfall 6). Exhaustive three-outcome mapping test prevents a silent enum drift that would mis-render an outcome |
| T-03-17 | Information Disclosure | copy-.pub action | mitigate | Clipboard receives the .pub authorized-key line ONLY (via internal/clipboard) — never PrivPEM; asserted |
| T-03-18 | Spoofing | git-step capability claim | mitigate | Real [ Continue ] disabled with an honest "arrives with the next build" reason — does not imply the form will work (D-19); Skip writes SSH-only, no Git artifacts |
| T-03-19 | Tampering | exact-command integrity | mitigate | Shown command rendered from tester.PreWriteCommand/ResolvedViaCommand (argv-parity, plan 03-01) — shown==run (TEST-01) |
</threat_model>

<verification>
- `go test -race ./internal/tuikit/... ./cmd/gitid/... ./internal/dummytui/...` green; `make test`, `make lint` clean.
- CI parity (L1/L3 — executors have reported PASS without `-race`; CI is headless): `TERM=dumb SSH_AUTH_SOCK= go test -race ./...` green.
- `/mui` + `agent-ui-ux-designer` engaged on the warning/git-step visuals (DLV-02), recorded in the SUMMARY.
- §6 copy-freeze gate green: both new strings present in their binaries, dummy's original preserved; 100×30 PTY walk green.
- `make gate-no-backend-files` still green (dummy unchanged; tuikit deltas add no backend import).
- Backend-free boundary proof for this plan's deltas: `go list -deps ./internal/tuikit | grep -E 'castocolina/gitid/internal/(identity|tester|sshconfig|keygen|filewriter|doctor|adopter|platform|clipboard|uploader)'` returns NO matches (Codex HIGH #5).
</verification>

<success_criteria>
- Both stages show exact commands + real output; shown==run (TEST-01/02), rendered from `TestResultView.Command`.
- `TestOutcomeReachableNotUploaded` renders as an honest yellow warning that unlocks store (D-01/D-02); red reserved for real failures.
- `internal/tuikit` stays backend-free: it branches on the tuikit-local `TestOutcome` enum only and imports no first-party backend package; all conversion lives in `cmd/gitid/wiring.go` (Codex HIGH #5).
- Copy-.pub + key-settings hint at the warning state, no private-material leak, no Phase-9 duplication (D-03).
- Real git-step honestly disabled (D-19); dummy unchanged; Skip Git functional (D-18).
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-05-SUMMARY.md` when done.
</output>
