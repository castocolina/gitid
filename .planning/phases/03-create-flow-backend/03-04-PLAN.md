---
phase: 03-create-flow-backend
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - internal/tuikit/frame.go
  - internal/tuikit/identities.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - internal/tuikit/identities_test.go
  - Makefile
autonomous: true
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, KEY-06]
must_haves:
  truths:
    - "Editing the SSH Host suffix auto-fills Real hostname + Port from the known-provider table (github→ssh.github.com:443, gitlab→altssh.gitlab.com:443, bitbucket→altssh.bitbucket.org:443; unknown→host:22), everything still editable"
    - "The live Host block preview matches the recipe shape exactly (alias, Hostname, Port 443, User git, IdentityFile, IdentitiesOnly yes) and a blank prefix yields the provider host verbatim (WYSIWYG)"
    - "An alias colliding with any existing Host pattern (managed or hand-written) shows an inline error naming the conflict and blocks form advance"
    - "The reuse-existing-key picker lists scanned ~/.ssh keys with filename + algorithm + fingerprint, an in-use label, and a manual-path row; selecting one reuses it instead of generating"
    - "Not-yet-wired tabs (git-screen, identity-manager, global-ssh, global-git, health, fixer) show the persistent Preview banner"
  artifacts:
    - path: "internal/tuikit/frame.go"
      provides: "D-16 Preview banner render (top-of-body, Theme.Warning, driven by Backend.DemoBanner)"
      contains: "Preview — demo data, not wired to your system yet"
    - path: "internal/tuikit/identities.go"
      provides: "Create form: provider-reactive autofill via seam, recipe-faithful preview, alias-collision error, reuse-key picker render"
      contains: "reuse"
  key_links:
    - from: "internal/tuikit create form provider edit"
      to: "Backend provider-defaults (identity.DefaultHostname/DefaultPort)"
      via: "reactive autofill on SSH Host suffix keystroke (D-20/21)"
      pattern: "DefaultHostname|providerDefault"
    - from: "internal/tuikit reuse-key picker"
      to: "Backend key-scan (keygen.ScanReusableKeys)"
      via: "list scanned keys + fingerprints + manual-path row (D-10/D-13)"
      pattern: "ScanReusableKeys|Reusable"
    - from: "internal/tuikit/frame.go banner"
      to: "Backend.DemoBanner(tab)"
      via: "top-of-body warning row on non-wired tabs (D-16)"
      pattern: "DemoBanner"
---

<objective>
Wire the create-flow SSH FORM correctness against the real Backend and add the
D-16 demo banner — the first per-screen UI wave of Phase 3. Deltas only; the
frozen form render already lives in tuikit (plan 03-02).

- **SSHUI-01 / D-20 / D-21**: provider inference from the SSH Host suffix drives
  Real hostname + Port autofill via the known-provider table
  (`identity.DefaultHostname`/`DefaultPort`) — replacing the dummy's github-only
  fixture defaults for the REAL binary, without adding or removing a form field.
- **SSHUI-03**: the live Host block preview is generated by
  `sshconfig.RenderHostBlock` so it matches `recipes/ssh-config.recipe` exactly
  (Port 443, IdentitiesOnly yes, alias); a blank prefix yields the provider host
  verbatim (WYSIWYG).
- **SSHUI-02**: fields stay mouse-clickable AND keyboard-navigable in the real
  shell (mouse routing preserved from the approved design).
- **D-09**: alias-collision validation against ALL parsed Host patterns blocks
  form advance with an inline naming error.
- **KEY-06 / D-10 / D-12 / D-13**: the reuse-existing-key picker lists scanned
  keys (filename + algorithm + fingerprint), labels in-use keys, offers a
  manual-path row, and reuses instead of generating.
- **D-16**: the "Preview — demo data" banner on every not-yet-wired tab.

This plan is a UI-wave: `/mui` and `agent-ui-ux-designer` are engaged in
planning, build, and review; the render deltas are captured for the plan-03-06
visual-regression gate.

Purpose: make the SSH form real (provider table, recipe-faithful preview,
collision, reuse) while honestly signposting the still-demo'd tabs.
Output: real provider reactivity + preview + collision + reuse picker + banner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@recipes/README.md
@recipes/ssh-config.recipe
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-PATTERNS.md
@.planning/design/create-flow/FIELDS.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md
</context>

## Artifacts this phase produces (plan 03-04)

New/changed symbols (exclude from source-drift flagging):

- D-16 banner render in `internal/tuikit/frame.go` (frozen string `! Preview — demo data, not wired to your system yet`), driven by `Backend.DemoBanner(tab)`
- Provider-reactive autofill in the create form calling the Backend provider-defaults seam (real: `identity.DefaultHostname`/`DefaultPort`)
- Alias-collision inline error in the create form calling the Backend collision seam
- Reuse-existing-key picker render in `internal/tuikit/identities.go` consuming `keygen.ReusableKey` via the seam
- Copy-freeze grep entry for the banner string (Makefile / §6 gate)

<tasks>

<task type="auto">
  <name>Task 1: D-16 Preview banner on not-yet-wired tabs (/mui + agent-ui-ux-designer)</name>
  <files>internal/tuikit/frame.go, internal/tuikit/identities_test.go, Makefile</files>
  <read_first>
    - internal/tuikit/frame.go (the shell chrome — header nav, breadcrumb/ActiveArea line, footer; where a top-of-body row goes)
    - internal/tuikit/backend.go (Backend.DemoBanner(tab) bool — the seam flag from plan 03-02)
    - .planning/phases/03-create-flow-backend/03-UI-SPEC.md (Scoped Divergences → D-16: EXISTING Theme.Warning role, `!` glyph, ONE row top-of-body directly under ActiveArea, chrome-cost not body-cost; draft copy; removal contract; blanket allowlist entry)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md (§6 copy-freeze grep gate; §7 row budget — banner is a chrome line, must not push body over 25 rows)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md (§2 glyph contract — color + glyph + word, never color alone)
  </read_first>
  <action>
    Add a persistent one-row Preview banner directly under the header/breadcrumb (ActiveArea) line, rendered ONLY when `Backend.DemoBanner(activeTab)` is true. Use the EXISTING `Theme.Warning` role (yellow) with the EXISTING `!` glyph plus the word — never a new role or glyph. Frozen copy, byte-exact: `! Preview — demo data, not wired to your system yet`. It is a chrome line (0 extra body-content rows) — keep the 100×30 body budget at ≤25 rows (do not resize the frame; measure against the tightest wizard pane per §7). The create-flow/Identities tab renders NO banner (Phase 3 wires it; real DemoBanner returns false for that tab per plan 03-03). Add the frozen banner string to the §6 copy-freeze grep gate (a `grep -rq` presence assertion over `internal/tuikit`, alongside the existing frozen strings). Engage `/mui` and `agent-ui-ux-designer` on the banner's placement/contrast during build (DLV-02) and record their input in the SUMMARY.
  </action>
  <verify>
    <automated>go test -race ./internal/tuikit/... -run 'TestBanner|TestDemoBanner|TestFrame'</automated>
  </verify>
  <acceptance_criteria>
    - A test renders a non-wired tab with DemoBanner=true and asserts the exact line `! Preview — demo data, not wired to your system yet` is present at top-of-body; with DemoBanner=false (create-flow tab) the line is ABSENT
    - The banner uses Theme.Warning + `!` glyph + word (grep the render helper; no new theme role/glyph added)
    - The 100×30 body budget is respected (existing row-budget test / PTY walk stays green; banner adds no body row)
    - `grep -rq 'Preview — demo data, not wired to your system yet' internal/tuikit` passes and is asserted by the §6 gate
  </acceptance_criteria>
  <done>Every not-yet-wired tab carries the honest Preview banner; create-flow does not; the string is copy-frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provider-reactive autofill + recipe-faithful live Host preview (SSHUI-01/03, D-20/21)</name>
  <files>internal/tuikit/identities.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/tuikit/identities_test.go</files>
  <read_first>
    - internal/tuikit/identities.go (the create form: sshForm, providerDefaults ~line 61, newSSHForm ~159, autoHost ~170, hostBlockPreview ~642 — the effect points to route through the seam)
    - internal/identity/identity.go (DefaultHostname ~214 — github→ssh.github.com, gitlab→altssh.gitlab.com, bitbucket→altssh.bitbucket.org, unknown→host; DefaultPort ~240 returns 443; DefaultAlias ~243)
    - internal/sshconfig/renderer.go (RenderHostBlock — the exact preview text: alias/Hostname/Port/User git/IdentityFile/IdentitiesOnly yes)
    - .planning/design/create-flow/FIELDS.md (the 4-field contract: Alias prefix → SSH Host → Real hostname → Port; blank-prefix WYSIWYG rule; the preview row)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-20 provider inferred from Host suffix, no new field, known-provider table; D-21 unknown→port 22 + alt-SSH hint)
    - .planning/phases/03-create-flow-backend/03-RESEARCH.md (Anti-pattern: do not re-derive the provider table in the TUI — use identity.DefaultHostname; DefaultHostname code excerpt)
    - recipes/ssh-config.recipe (canonical Host-block shape to match)
  </read_first>
  <behavior>
    - Real binary: typing/editing the SSH Host suffix `github.com` autofills Real hostname `ssh.github.com` + Port `443`; `gitlab.com` → `altssh.gitlab.com` + `443`; `bitbucket.org` → `altssh.bitbucket.org` + `443`; an unknown host `example.com` → Real hostname `example.com` + Port `22` with the alt-SSH hint (D-21). All autofilled values remain editable (endpointTouched suppresses re-autofill after manual edit, preserving the approved behavior).
    - Blank prefix → SSH Host = the provider host verbatim (no invented suffix); preview reflects it (WYSIWYG).
    - The live preview equals `sshconfig.RenderHostBlock(...)` output for the current field values — containing `Port 443` (or the edited port), `IdentitiesOnly yes`, `User git`, the alias, and the IdentityFile path — matching recipes/ssh-config.recipe structurally.
    - The dummy (FixtureBackend) is UNCHANGED — it keeps its github-only fixture defaults; only the REAL binary gets the full provider table (a data difference allowed under the seam).
  </behavior>
  <action>
    Route the create form's provider→endpoint autofill through the Backend provider-defaults seam and its preview through the Backend preview seam (both defined in plan 03-02). For the REAL binary (wiring.go, plan 03-03 already exposes these), back them with `identity.DefaultHostname`/`identity.DefaultPort` (the FULL github/gitlab/bitbucket alt-SSH table + unknown→22) and `sshconfig.RenderHostBlock`. Do NOT add or remove any form field — the approved 4-field form (Alias prefix → SSH Host → Real hostname → Port) stays byte-exact (D-20). Preserve the existing `endpointTouched` guard so a manual hostname/port edit is not clobbered. Add the D-21 unknown-provider alt-SSH hint into the existing hint row (no new row). Add tests: a tuikit-level test drives the form with a stub Backend returning the DefaultHostname table and asserts autofill + preview text; a `cmd/gitid/wiring_test.go` test asserts the real seam maps github/gitlab/bitbucket/unknown correctly and that the preview equals `RenderHostBlock` output for a sample identity (recipe fidelity). Follow the project RED-stub convention for the new assertions.
  </action>
  <verify>
    <automated>go test -race ./internal/tuikit/... ./cmd/gitid/... -run 'TestProvider|TestPreview|TestHostBlock|TestSSHForm'</automated>
  </verify>
  <acceptance_criteria>
    - Real-binary autofill: github→ssh.github.com:443, gitlab→altssh.gitlab.com:443, bitbucket→altssh.bitbucket.org:443, unknown→host:22 (wiring_test.go asserts each via identity.DefaultHostname/DefaultPort)
    - The live preview equals `sshconfig.RenderHostBlock(...)` and contains `Port 443` + `IdentitiesOnly yes` + `User git` + the alias (grep the assertion)
    - Blank prefix yields the provider host verbatim in SSH Host + preview (WYSIWYG test)
    - The form still has exactly 4 user-facing fields (no field added/removed vs the approved dummy); endpointTouched still suppresses re-autofill after manual edit
    - The dummy's FixtureBackend provider defaults are unchanged (dummy tests still green)
  </acceptance_criteria>
  <done>The real SSH form infers the provider from the Host suffix, autofills the recipe alt-SSH endpoint + port, and previews a recipe-faithful Host block; the dummy is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Alias-collision block + reuse-existing-key picker (D-09, KEY-06/D-10/D-12/D-13)</name>
  <files>internal/tuikit/identities.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/tuikit/identities_test.go</files>
  <read_first>
    - internal/tuikit/identities.go (the create form validation: nameTaken ~665, step0Valid ~659 — where collision gates advance; the algorithm/reuse selection area)
    - internal/keygen/keyscan.go (ScanReusableKeys + ReusableKey fields Path/Algorithm/Fingerprint/HasPub/Encrypted — from plan 03-01)
    - internal/identity/identity.go (Reuse path + CreateInput; the identity inventory used for in-use labelling)
    - internal/identity/inventory.go (how existing identities/keys are enumerated — for the in-use "in use by: NAME" label, D-12)
    - internal/sshconfig (Include-aware host parsing used by the real collision check from plan 03-03)
    - .planning/phases/03-create-flow-backend/03-CONTEXT.md (D-09 collision vs ALL parsed Host patterns, inline error naming the conflict, block advance; D-10 picker scan + manual-path row; D-12 in-use warn-not-block, same-provider warning; D-13 any parseable key, informational note, never block — only generate is catalog-locked)
    - .planning/phases/03-create-flow-backend/03-RESEARCH.md (Security Domain: manual-path row applies os.Lstat symlink-rejection before parsing; V5 alias validation reuse — identity.ValidateName)
    - .planning/design/create-flow/FIELDS.md (the reuse-key / algorithm selection screen field contract)
  </read_first>
  <behavior>
    - Alias collision: entering an alias equal to an existing Host pattern (managed OR hand-written) shows an inline error naming the conflicting entry, and `step0Valid`/advance is BLOCKED (Shift+→/Continue disabled) — never writes ambiguous first-match-wins config.
    - Reuse picker: the picker lists each scanned ~/.ssh key as `filename + algorithm + fingerprint`; a key already referenced by an identity shows an `in use by: NAME` label; a manual-path row accepts a path elsewhere; selecting a key routes the create through the reuse path (no keygen), and an encrypted-with-.pub key is accepted (via plan 03-01's ensurePub fix).
    - Same-provider reuse shows a warning on selection but does NOT block (D-12); a legacy non-catalog algorithm key shows an informational note but is NOT blocked (D-13).
    - Manual-path row rejects a symlinked candidate before parsing (os.Lstat), surfacing an error rather than following it.
  </behavior>
  <action>
    Wire the create form's alias-collision check to the Backend collision seam (real: Include-aware parse of `~/.ssh/config` from plan 03-03) so `step0Valid` returns false and the render shows an inline `Theme.Error` line naming the conflict when the alias matches any existing Host pattern (D-09). Render the reuse-existing-key picker from the Backend key-scan seam (`keygen.ScanReusableKeys` → `[]ReusableKey`): show filename + algorithm + fingerprint per row, an `in use by: NAME` label derived from the identity inventory (D-12), a manual-path row, and a same-provider selection warning (advisory, non-blocking). Apply `os.Lstat` symlink rejection to the manual-path candidate before parsing (RESEARCH Security Domain). Selecting a key sets the create to the reuse path (existing key, no generate). Non-catalog algorithms get an informational note, never a block (D-13). Add tests: a tuikit test drives the picker with a stub Backend returning fixture ReusableKeys (incl. one encrypted-with-.pub and one in-use) asserting the render + non-blocking warnings; a wiring_test.go test asserts the real collision seam flags a seeded existing Host and that reuse of an encrypted-with-.pub key succeeds end-to-end (exercising the plan 03-01 ensurePub fix). Follow the RED-stub convention.
  </action>
  <verify>
    <automated>go test -race ./internal/tuikit/... ./cmd/gitid/... -run 'TestCollision|TestReuse|TestPicker|TestAlias'</automated>
  </verify>
  <acceptance_criteria>
    - An alias matching an existing Host pattern blocks advance and renders an inline error naming the conflict (test with a seeded existing host)
    - The picker lists scanned keys with filename + algorithm + fingerprint, an in-use label, and a manual-path row (test with fixture ReusableKeys)
    - Reuse of an encrypted-with-.pub key succeeds (no passphrase prompt) via the ensurePub fix; a same-provider selection warns but does not block; a non-catalog algorithm shows an info note, not a block
    - The manual-path row rejects a symlinked candidate before parsing (os.Lstat) — asserted
  </acceptance_criteria>
  <done>Alias collisions block at the form; the reuse picker lists real scanned keys and reuses them (encrypted-with-.pub included) without blocking legitimate cross-provider or legacy-algorithm reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-typed alias/host → config write | alias/host validated before any advance or write |
| user-supplied reuse key path → parser | picker + manual-path row read files the user points at |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | alias-collision bypass | mitigate | Collision checked against ALL parsed Host patterns (Include-aware); advance blocked on match (D-09) — never ambiguous first-match config; reuse identity.ValidateName |
| T-03-13 | Information Disclosure / Path traversal | manual-path reuse row | mitigate | os.Lstat symlink rejection before parsing the user-pointed key (RESEARCH Security Domain) |
| T-03-14 | Information Disclosure | reuse picker render | mitigate | Picker shows filename/algorithm/fingerprint/paths only — never private-key bytes (ReusableKey carries no key material) |
| T-03-15 | Spoofing | frozen design (banner/form) | mitigate | Banner reuses existing Theme.Warning+`!`; form stays 4-field byte-exact; render deltas captured for the 03-06 visual-regression gate + agent-ui-ux-designer/Codex review |
</threat_model>

<verification>
- `go test -race ./internal/tuikit/... ./cmd/gitid/...` green; `make test`, `make lint` clean.
- `/mui` + `agent-ui-ux-designer` engaged during build; their input recorded in the SUMMARY (DLV-02).
- Copy-freeze §6 gate green with the new banner string; 100×30 PTY walk green (banner is chrome-cost).
- The dummy remains byte-identical (FixtureBackend unchanged); `make gate-no-backend-files` still green (tuikit deltas add no backend import).
</verification>

<success_criteria>
- Provider-reactive autofill via the recipe alt-SSH table + recipe-faithful live preview (SSHUI-01/03).
- Fields mouse-clickable and keyboard-navigable in the real shell (SSHUI-02).
- Alias collisions block with a naming error (D-09); reuse picker lists real keys and reuses them (KEY-06).
- Not-yet-wired tabs carry the honest Preview banner (D-16), copy-frozen.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-04-SUMMARY.md` when done.
</output>
