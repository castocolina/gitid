---
phase: 03-create-flow-backend
plan: 11
type: tdd
wave: 10
depends_on: [03-10]
files_modified:
  - internal/tester/tester.go
  - internal/tester/tester_command_test.go
  - internal/tuikit/views.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - internal/sshconfig/validation.go
  - internal/sshconfig/validation_test.go
  - internal/sshconfig/renderer.go
  - internal/sshconfig/renderer_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_cr_test.go
  - e2e/create_flow_pty_e2e_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_test.go
  - internal/screenshot/createflow_packet.go
  - internal/screenshot/createflow_packet_test.go
  - cmd/gitid-evidence/main.go
  - cmd/gitid-evidence/main_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - Makefile
  - .planning/design/create-flow/visual-divergence-allowlist.txt
  - .planning/phases/03-create-flow-backend/03-11-review-packet/
autonomous: true
gap_closure: true
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, SSHUI-04, SSHUI-05, TEST-01, TEST-02, TEST-03, KEY-06, DLV-04, DLV-06]
estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Stage two accepts and stores proof only after the production path has run both commands, retained both raw outputs/errors, and validated User, Hostname, Port, IdentitiesOnly, and the first effective IdentityFile; the live TUI displays that complete proof. (CR-05, CR-06; D-01, D-04; TEST-01, TEST-02)"
    - "Changing algorithm, provider, generate/reuse source, or normalized reuse path invalidates staged key material and both accepted outcomes; a raw-PTY rsa-4096 flow confirms the selected key and metadata in the persisted recipe-shaped Host block. (CR-07; D-10 through D-13; KEY-06)"
    - "Preview, staged config, and confirmed persistence all use one checked Host-block renderer and the same validated provider; every Unicode whitespace/control-bearing IdentityFile fails at that final boundary. (CR-08, WR-01; D-09, D-20, D-21; SSHUI-01, SSHUI-03)"
    - "Every failed confirmed transaction restores a byte-for-byte and mode-for-mode filesystem snapshot, removes child directories before parents, and reports rollback failure rather than hiding it. (CR-09; D-05, D-06, D-08, D-11; TEST-03)"
    - "The routine visual gate independently reproduces exactly 8 live, 8 approval-commit TUI, and 8 approval-commit HTML PNGs, validates hashes and mandatory non-empty regions, and rejects every adversarial mutation while leaving tracked files unchanged. (CR-02, CR-03, CR-04, CR-10; D-22, D-24)"
    - "The explicit publisher creates one immutable content-addressed packet from a full clean source commit, and two fresh independent authenticated reviews bind raw outputs to that packet; blocking findings trigger another fix/evidence/review cycle until clean. (CR-01 plus review closure; D-24, D-25; L14)"
  artifacts:
    - path: "internal/tester/tester.go"
      provides: "Error-bearing two-command production stage-two proof"
    - path: "internal/sshconfig/renderer.go"
      provides: "Authoritative checked Host-block render boundary"
    - path: "cmd/gitid/wiring.go"
      provides: "Production proof consumption, complete staged fingerprint, provider-consistent preview, and snapshot-correct rollback"
    - path: "internal/screenshot/createflow_packet.go"
      provides: "Canonical 24-panel packet generation and validation"
    - path: "cmd/gitid-evidence/main.go"
      provides: "Real one-shot immutable packet publication command"
    - path: ".planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/EVIDENCE.json"
      provides: "Content-addressed immutable production evidence"
    - path: ".planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/REVIEW-PROVENANCE.json"
      provides: "Hash-bound independent-review provenance and convergence result"
  key_links:
    - from: "internal/tester/tester.go"
      to: "cmd/gitid/wiring.go"
      via: "ResolvedVia returns a complete proof consumed and validated before recordOutcomeFor"
      pattern: "Stage2Proof|ValidateResolvedConfig"
    - from: "cmd/gitid/wiring.go"
      to: "internal/tuikit/identities.go"
      via: "backend-free TestResultView carries both commands and outputs into the real render"
      pattern: "ConnectivityCommand|ResolutionCommand|ResolutionOutput"
    - from: "cmd/gitid/wiring.go"
      to: "internal/sshconfig/renderer.go"
      via: "preview, staging, and commit all call the checked renderer"
      pattern: "RenderCheckedHostBlock"
    - from: "cmd/gitid-evidence/main.go"
      to: "3c3130e404329cf42baafdf63a6c22758437edc6"
      via: "isolated full-SHA export and pinned HTML/TUI capture"
      pattern: "approval_commit|approved_html|approved_tui"
    - from: ".planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/MANIFEST.json"
      to: "UI-REVIEW.md and CODEX-REVIEW.md"
      via: "prompt, raw output, reviewer identity, source SHA, and manifest SHA-256 are validated together"
      pattern: "manifest_sha256|source_commit|prompt_sha256|output_sha256"
---

<objective>
Close all ten Critical findings and WR-01 in the latest 03-REVIEW.md with production wiring and independently reproducible evidence, then converge through fresh independent review rather than stopping after one corrective pass. This implements L14 while preserving Phase 9's L15 authorization boundary: no GitHub key or external-account operation belongs to this plan.

Purpose: Make Phase 3's two-stage safety proof, confirmed transaction, visual approval evidence, and review provenance genuinely fail closed and independently auditable.
Output: Production fixes driven by RED tests, a checked 24-panel evidence pipeline and immutable publisher, a new content-addressed review packet, and two fresh hash-bound independent reviews with no open Critical/High finding.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/ONESHOT.md
@.planning/ONESHOT-GOAL-PROMPT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/LEARNINGS.md
@.planning/design/APPROVAL.md
@.planning/design/create-flow/FIELDS.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/phases/03-create-flow-backend/03-REVIEW.md
@.planning/phases/03-create-flow-backend/03-10-PLAN.md
@.planning/phases/03-create-flow-backend/03-10-SUMMARY.md

<interfaces>
- `tester.ResolvedVia(...) (Result, ResolvedConfig)` currently loses the `ssh -G` process error and raw output. Replace this production contract with an error-bearing proof that carries the connectivity command/output/error/outcome and resolution command/stdout/stderr/error/parsed fields.
- `tuikit.TestResultView` currently carries one `Command` and one `Detail`. Extend this backend-free DTO so `internal/tuikit` remains backend-free while rendering both production command/output pairs and field proof.
- `realBackend.TestStage2` currently calls `recordOutcomeFor` before resolution validation. Accepted stage-two state must be recorded only after complete proof validation.
- `realBackend.stagedKeyFor` currently caches on identity name plus reuse path, while `specFingerprint` omits key source and reuse path. Use one canonical fingerprint for staged material and outcomes.
- `sshconfig.RenderHostBlock` is callable without validation. Add `RenderCheckedHostBlock` (or equivalent error-returning API) and route Phase 3 preview, staged config, and confirmed persistence through it.
- `CaptureCreateFlowScreens` currently wraps production with synthetic stage messages. The evidence path must use the same complete proof DTO and production renderer through a deterministic external `ssh` PATH shim or controllable command runner; a display-only constant is not provenance.
- Approval commit is the full SHA `3c3130e404329cf42baafdf63a6c22758437edc6`. Approved HTML and approved TUI bytes must be captured from isolated files exported from that commit, not from current `dummytui.NewFixtureBackend()`.
</interfaces>
</context>

<reproduction_evidence>
The planner independently reproduced every latest-review root cause at current source commit `a2b646138c8e57c9e203b1e64c9adfddae47b171` without touching real HOME/config or external accounts:

1. CR-01 — `make generate-visual-review-packet SOURCE_COMMIT=a2b646138c8e57c9e203b1e64c9adfddae47b171 OUTPUT_DIR=<fresh-temp>/packet` printed the two arguments, exited `0`, and produced `created=no members=0`.
2. CR-02 — `rg -n 'approvalCommitFull|NewFixtureBackend|CaptureHTML|CaptureTUI|\.png' cmd/gitid/gate_visual_regression_test.go` found only the SHA constant/hex test and current-HEAD `NewFixtureBackend` calls; it found no approval HTML/TUI capture consumer.
3. CR-03 — `go test -tags screenshot -count=1 -run TestAllScreensCapturedAndNonEmpty -v ./cmd/gitid/...` passed while the test contract at lines 545-570 requires only 8 real + 8 dummy text maps; the approval/PNG search above returned no PNG capture path.
4. CR-04 — `go test -tags screenshot -count=1 -run TestNegativeControls_AllProtectedRegionsDetectMutation -v ./cmd/gitid/...` passed; source search showed empty-region `continue`, predicates applied only to `realRegion`, and mutation-insensitive extraction ending in `t.Logf` rather than failure.
5. CR-05 — focused validator tests passed (`5 passed`) while source search showed `ResolvedVia` uses `gOut, _`, `TestStage2` records line 590 before any validator call, and `TestResultView` has only `Command`/`Detail`.
6. CR-06 — source search showed `offlineCaptureBackend.TestStage2` fabricates `Command: Stage2Command(spec)` plus `Detail: "identityfile " + spec.KeyPath`; the real-PTY suite only asserts the token `identityfile`, not both commands/outputs or all five effective fields.
7. CR-07 — source search showed the staged cache hit checks only `stagedFor == in.Name` plus reuse path, and `specFingerprint` omits generate/reuse source and normalized reuse path; existing RSA tests manufacture outcomes instead of driving a raw PTY RSA confirmation.
8. CR-08 — source search found three direct `RenderHostBlock` calls in production and only the earlier UI-facing `ValidateHostBlock`; `validation.go` has no `unicode.IsSpace` check despite claiming Unicode whitespace rejection.
9. CR-09 — a disposable filesystem reproduction of current `createdDirs` order `[.ssh, .ssh/config.d]` output `[('.ssh', 'OSError'), ('.ssh/config.d', 'removed')] ssh_exists_after=True`; source appends in that order and removes with a forward loop.
10. CR-10 — two independent `TestGateVisualRegression` processes both passed, but source shows fresh cryptographic material hidden behind process-local `sync.OnceValue`, text-only hash comparisons, and no packet/PNG inventory comparison.
11. WR-01 — source search showed preview passes `providerFromAlias(spec.Alias)` while staging/commit pass `in.Provider`; therefore preview and persisted metadata still diverge for multi-label providers.
12. Review closure — `.planning/phases/03-create-flow-backend/03-10-review-packet` is missing, so no immutable evidence-bound fresh reviews exist.
</reproduction_evidence>

<execution_safety>
- Before RED work, record `git status --short`, full HEAD, and SHA-256 for the already-modified 03-09 EVIDENCE.json and PNG. Record every unrelated untracked path. Never checkout, restore, rewrite, delete, stage, or commit those paths; compare hashes/status at every task boundary.
- Every behavioral command uses a disposable HOME and isolated temporary directories. Assert the developer's real `~/.ssh`, `~/.gitconfig*`, keys, and external accounts are unchanged. L15 authorizes Phase 9 disposable GitHub-key tests only and is explicitly out of scope here.
- Follow RED → GREEN → REFACTOR authoring, but obey AGENTS.md commit granularity: commit each coherent implementation together with its tests; do not leave standalone broken RED commits in final history and never use `--no-verify`.
- Execute Tasks 1, 2, and 3 sequentially. Task 2 consumes Task 1's production proof/render contracts. Task 3 publishes only after Tasks 1-2 are committed and all affected gates pass.
</execution_safety>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end production stage-two proof, fingerprint, checked render, and rollback tracer</name>
  <files>internal/tester/tester.go, internal/tester/tester_command_test.go, internal/tuikit/views.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, internal/sshconfig/validation.go, internal/sshconfig/validation_test.go, internal/sshconfig/renderer.go, internal/sshconfig/renderer_test.go, cmd/gitid/wiring.go, cmd/gitid/wiring_cr_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - RED CR-05/CR-06: production stage two fails on connectivity failure, `ssh -G` spawn/exit failure, empty/truncated raw output, and each wrong User/Hostname/Port/IdentitiesOnly/first-IdentityFile case; no failing proof is recorded, and success renders both exact commands, both complete raw outputs, and every validated field.
    - RED CR-07: changing algorithm, provider, generate/reuse source, or normalized reuse path clears staged material and both outcomes; a real raw-keystroke PTY rsa-4096 flow passes both stages, confirms, and persists only the RSA key path and selected provider metadata.
    - RED CR-08/WR-01: one error-returning checked renderer rejects Unicode spaces plus all controls/syntax separators and is used by preview, staged config, and commit; a multi-label provider produces byte-identical preview and persisted Host-body bytes.
    - RED CR-09: failure injection immediately after every directory/mode/file mutation restores a complete pre-transaction snapshot; fresh nested directories disappear child-first, existing directory/file modes and bytes return exactly, and rollback failure is returned with the original error.
  </behavior>
  <action>
    Start with one failing real-PTY tracer that selects rsa-4096, uses a multi-label/custom provider, observes the complete stage-two proof, confirms into a disposable HOME, and compares the final Host body to the preview. Then add focused negative tests before implementation.

    Replace `ResolvedVia` with a `Stage2Proof`-shaped UI-free result built from shared argv constructors. Retain connectivity combined output/error and resolution stdout/stderr/error independently. In `realBackend.TestStage2`, validate non-empty raw resolution plus User=`git`, expected Hostname/Port, IdentitiesOnly=`yes`, and the first effective IdentityFile before converting to `TestResultView` and before `recordOutcomeFor`. Preserve D-01/D-02 ReachableNotUploaded classification for connectivity while making any resolution defect a hard stage-two failure. Extend `TestResultView` and `renderStageOutcome` so the real TUI displays both exact commands and both raw outputs without truncating away the required proof; keep the tuikit import-graph allowlist closed per D-17.

    Define one canonical create fingerprint over identity, alias, hostname, port, algorithm, provider, explicit key source, and normalized absolute reuse path. Use it for both staged material and accepted outcomes; on any mismatch securely drop stale generated staging material and clear both outcomes. Remove fallback/default reconstruction after the wizard has supplied validated algorithm/provider. Add raw-PTY negative navigation that stages one algorithm/source, goes back, changes it, and proves old material cannot unlock confirmation per D-10 through D-13.

    Add `sshconfig.RenderCheckedHostBlock` as the authoritative parser/render boundary. It must call strict validation including `unicode.IsSpace` and Unicode controls, return an error, render once, parse the result, and support a real temp-config `ssh -G` round trip. Route `HostBlockPreview`, `StageTestConfig`, and `commitCreateTransaction` through it; preview must consume `spec.Provider`, exactly matching the confirmed `in.Provider` for WR-01. Keep the recipe order `Host`, `Hostname`, `Port`, `User git`, `IdentityFile`, `IdentitiesOnly yes`, then provider marker.

    Refactor confirmed transaction rollback into explicit journal operations whose undo runs in strict reverse mutation order. Snapshot full relevant tree existence/type/mode/content before mutation, remove newly created children before parents, and collect/report rollback errors alongside the triggering error. Failure-inject after every step, including directory creation/chmod, reused private/public chmod/write, Include line, and Host block. Preserve D-05/D-06/D-08/D-11 and the no-pre-confirm-mutation guarantee.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tester/... ./internal/sshconfig/... ./internal/tuikit/... ./cmd/gitid/... -run 'Test.*(Stage2Production|CompleteProof|Fingerprint|CheckedHost|ProviderPreview|RollbackSnapshot|RollbackError)' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 180s ./e2e/... -run 'TestCreateFlow_.*(RSA|Stage2Complete|FingerprintInvalidation|ProviderPreview|Rollback)'</automated>
  </verify>
  <done>The real binary alone proves the complete stage-two contract, stale staged state cannot cross any key/provider choice, preview equals confirmed checked rendering, and every injected transaction failure restores the full disposable-HOME snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-closed approval-commit 24-panel generation and immutable publication</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_test.go, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go, cmd/gitid/gate_visual_regression_test.go, Makefile, .planning/design/create-flow/visual-divergence-allowlist.txt</files>
  <behavior>
    - RED CR-01: the publication command rejects missing/short/unknown/dirty source commits and every existing destination, then atomically creates a directory named by full source SHA with canonical manifest and verified hashes; no successful no-op path exists.
    - RED CR-02/CR-03: approved HTML and approved TUI are exported from full approval SHA `3c3130e404329cf42baafdf63a6c22758437edc6`; current HEAD substitution, missing source/tool/font/theme, stale panel, or any count other than exactly 8 live + 8 approved TUI + 8 approved HTML is fatal.
    - RED CR-04: every screen has an explicit mandatory non-empty region schema; equality is exact except narrowly named deterministic normalizers; mutation of every protected and normalized subregion is passed through the actual gate and must be rejected, while mutation-insensitive extraction fails the test.
    - RED CR-10: two separate OS processes generate complete bundles from committed public-only fixture inputs and an injected clock/path map; text, PNG, canonical manifest, and every per-file hash are identical.
  </behavior>
  <action>
    Implement a real `go run -tags screenshot ./cmd/gitid-evidence` publisher called by `make generate-visual-review-packet`. Validate the full source commit with Git, require the relevant source tree clean, require a destination that does not exist, generate under a sibling temporary directory, fsync files/directories, validate the completed bundle, then atomically rename to `<OUTPUT_ROOT>/<full-source-sha>`. On any failure remove the temporary candidate and return nonzero. Publication must be immutable: refuse every overwrite, including an empty existing directory.

    Export approved source from the full approval commit into isolated temporary storage. Use the repository's pinned Chromium, Freeze, vendored JetBrains Mono, Dracula theme, and 100x30 geometry to capture eight mapped create-flow states from the approval HTML implementation and eight from the approval TUI implementation. Capture eight live panels from the compiled current real binary using the production backend with disposable HOME and D-22 external fake-ssh process. Record exact commands/argv, source and approval SHAs, mapping IDs/routes/keystrokes, tool versions, geometry, theme/font hashes, normalized temp-path map, and fixed clock in canonical JSON. Never build an approved panel from current `NewFixtureBackend`.

    Replace random process-local key generation with committed public-only deterministic view/proof fixture data; evidence must contain no private key bytes. Drive Task 1's complete proof and renderer rather than injecting one command/detail pair. Generate two complete candidates in separate subprocesses and require identical inventories, PNG bytes, text captures, and canonical manifest bytes before publication.

    Replace the broad allowlist grammar with explicit per-screen mandatory region schemas and exact expected before/after values or named normalizers with pinned input/output tests. All mandatory extraction is non-empty. Build adversarial controls by mutating every region in every screen and invoking the same production gate; every mutation must fail for the expected region. Missing tools, captures, packet members, hashes, provenance, or extra files must fail. `make gate-visual-regression` generates and validates two complete candidates only in temporary directories, compares them to the latest immutable 03-11 packet when present, and never changes a tracked file.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid/... ./cmd/gitid-evidence/... -run 'Test.*(Packet|ApprovalCommitCapture|Exactly24|MandatoryRegion|AdversarialMutation|CrossProcessDeterminism|Publisher|RoutineGateReadOnly)' &amp;&amp; make gate-visual-regression</automated>
  </verify>
  <done>The routine gate independently validates deterministic approval-backed 24-panel evidence and adversarial controls without tracked writes, and the explicit command can atomically publish one validated immutable content-addressed packet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish evidence and converge through two fresh independent reviews</name>
  <files>.planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/MANIFEST.json, .planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/EVIDENCE.json, .planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/REGION-DIFFS.json, .planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/REVIEW-PROVENANCE.json, .planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/UI-REVIEW.md, .planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/CODEX-REVIEW.md</files>
  <behavior>
    - RED review validator: reject a review created before packet publication, edited raw output, missing authenticated status/run ID/executable/version/provider/resolved-model, prompt/output hash mismatch, source/approval/manifest mismatch, stale review from another source SHA, executor-authored verdict, or any unclosed Critical/High.
    - RED convergence: a synthetic blocking verdict makes acceptance fail; after any production/evidence correction, the changed source SHA requires a new packet and two new independent review runs, so one prior pass can never be reused.
    - RED immutable publication: two disposable pre-publication runs are byte-identical; the tracked destination is created once and a second invocation fails without changing any member hash.
  </behavior>
  <action>
    Commit Tasks 1-2 as coherent buildable changes, obtain the full clean source SHA, run two disposable publisher invocations in separate processes, and require identical canonical manifests and all member hashes. Publish once under `.planning/phases/03-create-flow-backend/03-11-review-packet/<source-full-sha>/`. Validate exactly 24 PNG panels, corresponding text/provenance/region evidence, no undeclared member, and all hashes before review dispatch.

    Run two genuinely independent clean-context reviews after publication: the runtime's UI/UX reviewer and an independently authenticated Codex CLI session. Discover each supported auth/status command from local help, run it, and record the non-secret result. Feed both exactly the immutable manifest hash, all 24 panels, complete unfiltered text/region diffs, recipes, D-01 through D-25, latest 03-REVIEW CR-01 through CR-10 plus WR-01, approval SHA, and corrected source SHA. Persist raw unedited stdout/stderr as the review files; do not author a substitute verdict.

    In canonical `REVIEW-PROVENANCE.json`, bind reviewer role, executable path/version, provider and resolved model, authenticated status, run/session ID, UTC start/end, exact invocation/dispatch parameters, prompt SHA-256, raw output SHA-256, exit status, source SHA, approval SHA, and evidence-manifest SHA-256. Validate provenance and severity automatically.

    Apply L14 convergence: if either reviewer reports Critical/High or discovers a failing target, return to the owning RED test in Task 1 or 2, implement the production correction, rerun all gates, commit, publish a new packet under the new full SHA, and rerun both reviewers. Repeat until both fresh reviews for the same latest immutable packet have zero open Critical/High. Medium/Low requires a concrete fixed or accepted disposition. Do not mark Phase 3 complete or rewrite 03-REVIEW/STATE/ROADMAP/REQUIREMENTS; independent phase verification owns reconciliation.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= make test &amp;&amp; make lint &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test.*(ImmutablePacket|ReviewProvenance|BlockingVerdict|FreshReviews|Convergence)'</automated>
  </verify>
  <done>The latest full source SHA has one immutable validated 24-panel packet and two fresh independent authenticated, raw-output-hash-bound reviews with zero open Critical/High; any earlier failed iteration remains immutable history and no real HOME/config/key/account was touched.</done>
</task>

</tasks>

<dependency_graph>
| Task | Needs | Creates | Sequence reason |
|------|-------|---------|-----------------|
| 1 | Latest review findings and existing create flow | Correct production proof/render/cache/transaction contracts | Evidence must depict corrected production behavior. |
| 2 | Task 1 complete proof DTO and renderer | Deterministic fail-closed 24-panel validator and publisher | Capture and region contracts must consume production outputs. |
| 3 | Tasks 1-2 committed and green | Immutable packet plus fresh review provenance | Reviews bind only to a committed immutable source/evidence pair. |
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form/key choice → staged proof cache | User-controlled algorithm/provider/path must not reuse stale material or outcomes. |
| External ssh process → accepted stage-two state | Untrusted process status/output becomes permission to persist only after complete validation. |
| Form/path → OpenSSH render/execute | IdentityFile and provider values enter config syntax and process argv. |
| Confirmed transaction → user filesystem | Multi-file/directory/mode mutations must be atomic and reversible. |
| Approval/source commits → evidence generator | Immutable historical and current source must remain distinct and attributable. |
| Evidence packet → independent reviewers | Review inputs/outputs must be complete, immutable, authenticated, and hash-bound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11-01 | Spoofing | stage-two proof | critical | mitigate | Error-bearing dual-command proof, full field validation, and record-after-validation production order. |
| T-03-11-02 | Tampering | staged cache/outcomes | high | mitigate | One canonical fingerprint including algorithm/provider/source/normalized path plus invalidation PTY tests. |
| T-03-11-03 | Tampering | Host-block render | high | mitigate | One checked error-returning renderer used at preview, staging, and commit boundaries. |
| T-03-11-04 | Tampering | confirmed filesystem transaction | critical | mitigate | Reverse journal, complete snapshots, per-mutation failure injection, and observable rollback errors. |
| T-03-11-05 | Spoofing | approval baseline | high | mitigate | Full-SHA isolated HTML/TUI export and separate provenance/hashes. |
| T-03-11-06 | Tampering | visual packet | high | mitigate | Exactly-24 inventory, canonical manifests, per-file hashes, adversarial region controls, atomic no-overwrite publication. |
| T-03-11-07 | Repudiation | independent reviews | high | mitigate | Auth preflight, run/model IDs, raw output and prompt hashes, immutable manifest binding, and new-SHA rerun policy. |
| T-03-11-08 | Information Disclosure | evidence fixtures | medium | mitigate | Committed public-only deterministic DTO/key data; validator rejects private key material. |
| T-03-11-09 | Elevation of Privilege | Phase 9 account authorization | critical | mitigate | No external-account command in Phase 3; L15 remains scoped exclusively to Phase 9. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Algorithm → SSH form → isolated two-stage exact proof → confirmed store matching approved design | 1-3 | COVERED | Production tracer, immutable evidence, convergence review. |
| REQ | SSHUI-01/02/03 | Four fields, mouse/keyboard reachability, recipe-faithful WYSIWYG preview | 1-2 | COVERED | Checked provider-consistent renderer and PTY/PNG proof. |
| REQ | SSHUI-04/05 | Throwaway testing and ordered macOS globals | 1 | COVERED | Transaction snapshot and regression suite preserve both. |
| REQ | TEST-01/02/03 | Both exact commands/outputs, complete ssh-G proof, confirmed backed-up store | 1 | COVERED | Production call chain and rollback journal. |
| REQ | KEY-06 | Existing-key reuse | 1 | COVERED | Source/path fingerprint plus permission/snapshot rollback. |
| REQ | DLV-04/06 | Approval-backed visual review and real-binary PTY | 1-3 | COVERED | Raw PTY, 24 panels, two fresh reviews. |
| REVIEW | CR-01 | Real immutable packet publication | 2-3 | COVERED | Atomic command, exact inventory, no successful no-op. |
| REVIEW | CR-02 | Approval commit consumed | 2 | COVERED | Isolated full-SHA HTML/TUI exports. |
| REVIEW | CR-03 | Fail-closed 24 PNG/hash gate | 2 | COVERED | Exactly 8+8+8 and canonical hashes. |
| REVIEW | CR-04 | Required non-empty regions and adversarial controls | 2 | COVERED | Explicit schema and actual-gate mutation matrix. |
| REVIEW | CR-05/06 | Production-complete ssh-G proof and rendering | 1 | COVERED | Error-bearing proof, validate-before-record, both command/output pairs. |
| REVIEW | CR-07 | Complete staged-key/proof fingerprint and PTY RSA | 1 | COVERED | Canonical source-aware fingerprint and real confirmation. |
| REVIEW | CR-08 | Checked final Host render boundary | 1 | COVERED | Unicode-safe checked renderer at all three callers. |
| REVIEW | CR-09 | Reverse rollback/snapshot correctness | 1 | COVERED | Reverse journal and every-step failure injection. |
| REVIEW | CR-10 | Cross-process complete-bundle determinism | 2 | COVERED | Public fixtures plus subprocess text/PNG/manifest hash equality. |
| REVIEW | WR-01 | Preview provider consistency | 1 | COVERED | `spec.Provider` through same checked renderer as commit. |
| REVIEW | independent closure | Fresh reviews bound to immutable evidence | 3 | COVERED | L14 reruns both for every corrected source SHA. |
| RESEARCH | — | UI-free core, real composition root, no new package dependency, arg-slice exec, fake SSH, filewriter safety | 1-2 | COVERED | Existing dependencies only; new command is repository tooling. |
| CONTEXT | D-01..D-04 | Store outcomes, warning/copy, automatic stage chain | 1-2 | COVERED | Complete stage proof preserves locked outcome semantics. |
| CONTEXT | D-05..D-09 | Auto layout, Include default, no migration UX, globals, collision | 1 | COVERED | Snapshot transaction preserves all locked storage behavior. |
| CONTEXT | D-10..D-13 | Reuse picker, pair/perms, warning, algorithm breadth | 1 | COVERED | Complete fingerprint and reuse rollback. |
| CONTEXT | D-14..D-19 | Real shell/shared UI, demo banners, SSH-only Git skip/reason | 1-3 | COVERED | Regression gates preserve locked shell/copy behavior. |
| CONTEXT | D-20..D-21 | Provider inference and known/custom endpoints | 1 | COVERED | Validated provider is propagated without suffix reconstruction. |
| CONTEXT | D-22..D-23 | Deterministic fake SSH; optional network smoke separate | 1-2 | COVERED | External fake SSH drives evidence; network smoke remains separate. |
| CONTEXT | D-24..D-25 | Automated gate, two independent reviews, severity policy | 2-3 | COVERED | Immutable packet and convergence loop. |

Deferred layout-migration UX, Git writes, CLI rebuild, upload automation, and `insteadOf` rendering remain excluded exactly as 03-CONTEXT specifies. L15 GitHub keys remain Phase 9-only.
</source_coverage_audit>

<verification>
1. Preserve the initial dirty/untracked inventory and hashes; compare after every task and at plan close.
2. Record every focused RED command and actual failing output before implementation, followed by GREEN output, in 03-11-SUMMARY.md.
3. Run Task 1's complete production and raw-PTY matrix with disposable HOME; verify no external account or developer config/key path changes.
4. Run Task 2's gate in two independent processes and validate identical text, PNG, manifest, and member hashes plus every adversarial negative control.
5. Publish one full-SHA packet, validate exactly 24 panels and all declared members/hashes, then prove overwrite refusal leaves hashes unchanged.
6. Validate two fresh authenticated review outputs against the same latest source/approval/manifest hashes and zero open Critical/High; if not clean, repeat fix → test → packet → both reviews per L14.
7. Run `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression`; all exit zero.
8. Confirm recipes, 03-REVIEW.md, all existing plans/summaries/evidence, user HOME/config/keys, external accounts, and unrelated untracked paths are byte-identical to baseline.
</verification>

<success_criteria>
- Every latest 03-REVIEW Critical finding CR-01 through CR-10 and WR-01 has an explicit production regression and measurable immutable evidence.
- No helper API or provenance constant can satisfy acceptance without production consumption demonstrated by raw PTY or generated packet output.
- The routine gate is fail-closed, read-only, approval-commit-backed, exactly 24 panels, mandatory-region strict, and cross-process deterministic.
- Confirmed transaction rollback restores complete filesystem snapshots and surfaces rollback errors.
- The latest immutable packet has two fresh independent authenticated reviews with zero open Critical/High after autonomous convergence.
- No real HOME/config/key/account mutation occurs; Phase 9 L15 authorization is unused.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-11-SUMMARY.md` with observed RED/GREEN commands and outputs, source/approval/manifest hashes, packet inventory, review provenance, every finding disposition, convergence iterations, and baseline/final safety hashes. Do not mark Phase 3 complete; hand the immutable artifacts to independent phase verification.
</output>
