---
phase: 03-create-flow-backend
plan: 14
type: tdd
wave: 13
depends_on: [03-13]
files_modified:
  - internal/tuikit/frame.go
  - internal/tuikit/frame_test.go
  - internal/tuikit/identities.go
  - internal/tuikit/ceremony.go
  - e2e/create_flow_pty_e2e_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_test.go
  - internal/screenshot/html.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_packet.go
  - internal/screenshot/createflow_packet_test.go
  - cmd/gitid-evidence/main.go
  - cmd/gitid-evidence/main_test.go
  - .planning/phases/03-create-flow-backend/03-14-review-packet/
autonomous: true
gap_closure: true
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, SSHUI-04, SSHUI-05, TEST-01, TEST-02, TEST-03, KEY-06, DLV-04, DLV-06]
estimate:
  tokens: 98000
  raw_tokens: 98000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "At 100x30, the production TUI exposes every byte of completed stage-1 and stage-2 commands, outputs, and ssh -G User/Hostname/Port/IdentitiesOnly/IdentityFile proof through advertised vertical and horizontal viewport controls. (TEST-01, TEST-02; D-01 through D-04, D-22)"
    - "Generate, selected reuse, resolved manual-path reuse, mouse focus, completed PASS, ReachableNotUploaded, hard failure with retry, the D-19 Git state, and complete confirmation are separately captured from their real semantic states rather than inferred from route names. (KEY-06, DLV-04, DLV-06; D-02, D-03, D-10 through D-13, D-18, D-19, D-24, D-25)"
    - "The confirmation viewport exposes the complete summary/key path and both sentinel lines before any write, while all tests and captures use disposable HOME plus fake SSH and never mutate user configuration or external accounts. (SSHUI-03, SSHUI-04, TEST-03; D-05, D-06, D-08, D-22)"
    - "A corrected committed source SHA produces a new immutable packet whose manifest declares every required PNG, rendered text, raw ANSI evidence, named-region comparison, prompt, raw review output, verdict, and provenance member; no pre-existing evidence directory is changed. (DLV-04; D-24, D-25)"
    - "REVIEW-PROVENANCE.json can be created only after two distinct raw independent reviews target the exact candidate inventory hash and report zero open Critical and High findings; a stale source, missing review, mismatched hash, or blocking finding prevents publication. (D-24, D-25; L14)"
  artifacts:
    - path: "internal/tuikit/frame.go"
      provides: "Byte-preserving two-axis exact-text viewport"
    - path: "internal/tuikit/identities.go"
      provides: "Production proof-state and D-19 rendering wired to the viewport"
    - path: "internal/tuikit/ceremony.go"
      provides: "Focusable complete pre-write confirmation viewport"
    - path: "internal/screenshot/createflow.go"
      provides: "State- and frame-aware capture registry covering all required semantic states"
    - path: "internal/screenshot/createflow_regions.go"
      provides: "Meaningful named-region extraction and normalized comparison evidence"
    - path: "internal/screenshot/createflow_packet.go"
      provides: "Candidate/final packet validation with review-bound provenance"
    - path: ".planning/phases/03-create-flow-backend/03-14-review-packet/<new-full-source-sha>/"
      provides: "Immutable source-SHA packet containing complete PNG/text evidence and two declared raw reviews"
  key_links:
    - from: "internal/tuikit/identities.go"
      to: "e2e/create_flow_pty_e2e_test.go"
      via: "Raw PTY keys focus and scroll the exact production proof viewport"
      pattern: "ExactTextViewport|HorizontalOffset|PageDown|PageUp"
    - from: "internal/screenshot/createflow.go"
      to: "cmd/gitid-evidence/main.go"
      via: "One typed registry supplies semantic state, surface applicability, interaction, frames, markers, and regions"
      pattern: "ScreenSpec|FrameSpec|SurfaceSpec|StateMarker"
    - from: "internal/screenshot/createflow_regions.go"
      to: "internal/screenshot/createflow_packet.go"
      via: "Every required frame contributes nonempty named regions to the declared candidate inventory"
      pattern: "RegionComparison|NormalizedText|LiveSHA256|ApprovedSHA256"
    - from: ".planning/phases/03-create-flow-backend/03-14-review-packet/<new-full-source-sha>/REVIEW-PROVENANCE.json"
      to: "reviews/ui/raw-stdout.txt and reviews/codex/raw-stdout.txt"
      via: "Distinct reviewer identities and raw-output hashes bind both zero-blocker verdicts to one candidate manifest hash"
      pattern: "candidate_manifest_sha256|source_commit|reviewer|prompt_sha256|stdout_sha256|open_high"
---

<objective>
Correct the production TUI, capture driver, and evidence publisher defects confirmed by the independent 03-UI-REVIEW, then publish one new immutable source-SHA packet only after two independent raw reviews of that exact candidate pass.

Purpose: Make Phase 3's D-24/D-25 evidence complete, state-truthful, inspectable, and independently attributable without touching real user configuration, external accounts, or any existing evidence directory.
Output: TDD-backed production viewport and state fixes, an expanded state-aware PNG/text capture inventory, a fail-closed candidate/final publisher, one new source commit, and one review-complete `03-14-review-packet/<source-full-sha>/` directory.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/ONESHOT.md
@.planning/LEARNINGS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/design/create-flow/FIELDS.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-13-PLAN.md
@.planning/phases/03-create-flow-backend/03-13-SUMMARY.md
@.planning/phases/03-create-flow-backend/03-UI-REVIEW.md
@internal/tuikit/frame.go
@internal/tuikit/identities.go
@internal/tuikit/ceremony.go
@e2e/create_flow_pty_e2e_test.go
@internal/screenshot/createflow.go
@internal/screenshot/createflow_regions.go
@internal/screenshot/createflow_packet.go
@cmd/gitid-evidence/main.go

<interfaces>
- `ExactTextViewport` currently exists only as a vertical renderer. No production model stores it, routes keys to it, or renders its controls; long lines are still irretrievably hard-truncated. Add two-axis state and wire it into the real test and confirmation panes.
- `TestResultView.Command`, `Detail`, `ResolutionCommand`, and `ResolutionOutput` are the authoritative captured values. Proof rendering and packet text must consume these bytes directly, never rebuild commands or summarize `ssh -G` output.
- `captureTUIScreen` currently implements manual reuse as Right then Left on the source toggle, which returns to Generate. The corrected interaction must select Reuse, focus the picker/manual row, type a sandbox key path, resolve it, and assert its unique path/fingerprint marker before capture.
- The current inventory is the legacy eight-screen `CreateFlowScreenIDs`; stage 1 is deliberately captured while stage 2 is running, and no hard-failure/retry member exists. Replace the fixed count/table with registry-derived required states and frame variants.
- `captureApprovedHTMLPanels` currently validates route text only and assigns synthetic route text to packet evidence. Extend HTML capture with a declared interaction plus state-specific DOM/text assertion before saving each applicable approved frame.
- `publish` currently atomically publishes the candidate immediately after two-process comparison. There is no finalization implementation despite comments claiming one. Split candidate generation from final publication and make raw reviews mandatory inputs to finalization.
- `BuildRegionDiffs` currently emits one whole-screen hash pair per legacy ID even though `ExtractRegion` already defines semantic regions. Region evidence must carry nonempty named-region normalized text and hashes, not screen-level equality alone.
</interfaces>
</context>

<confirmed_findings>
| ID | Confirmed defect | Required closure |
|----|------------------|------------------|
| A-01 | Manual reuse capture executes Right then Left and returns to Generate. | Capture an actually resolved sandbox manual key with a unique marker. |
| A-02 | Only the legacy eight screens are captured. | Add completed PASS, ReachableNotUploaded, and hard-failure/retry evidence plus viewport frame variants required to expose full proof. |
| A-03 | Stage proof is running/clipped; long-line bytes are not reachable. | Wire two-axis exact viewports into production and drive them through raw PTY controls. |
| A-04 | Confirmation summary/key evidence is clipped. | Capture navigated summary and full BEGIN-to-END managed block before confirmation. |
| A-05 | The candidate has no canonical manifest/review provenance. | Final packet must declare all members and review binding; candidate must remain unpublished until reviews exist. |
| A-06 | Region records are whole-screen hashes, not meaningful regions. | Emit complete named-region text/hash comparisons with explicit divergence decisions. |
| A-07 | Publisher has no implemented review/finalization path. | Require two distinct raw reviews of one candidate hash, zero Critical/High, then atomically publish. |
</confirmed_findings>

<execution_safety>
- Before RED work, record `git status --short`, current HEAD, worktree inventory, and SHA-256 inventories for every pre-existing `03-09-review-packet`, `03-11*-review-packet`, `03-12-review-packet`, and `03-13-review-packet` directory. Recheck after every task and before every commit.
- Treat all pre-existing evidence, `.planning/debug/`, `.planning/ui-reviews/`, `.planning/phases/05.7-*`, and unrelated worktrees as protected. Never clean, restore, rename, overwrite, append to, stage, or commit them.
- Every PTY/capture run uses a newly created disposable HOME, public sandbox key fixtures, D-22 PATH-shim fake SSH, and no network-smoke target. Never read or write real `~/.ssh`, `~/.gitconfig*`, provider keys, credentials, or account state.
- Follow RED → GREEN → REFACTOR in authoring order and record the exact command plus real failing/passing output. Per AGENTS.md, commit tests and implementation for each coherent correction together; never use `--no-verify`.
- Execute Tasks 1-3 sequentially in the current phase worktree per L11. The packet source SHA is the clean committed HEAD after all production/publisher corrections, before generated packet artifacts are added.
</execution_safety>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end production proof and confirmation viewports</name>
  <files>internal/tuikit/frame.go, internal/tuikit/frame_test.go, internal/tuikit/identities.go, internal/tuikit/ceremony.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - RED A-03 tracer: a real 100x30 PTY completes stage 1 and stage 2 under D-22 fake SSH, focuses the advertised proof viewport, and uses raw vertical and horizontal navigation bytes to reveal the exact captured commands, outputs, ResolutionCommand, and all five required ResolutionOutput fields.
    - RED state coverage per D-01 through D-04: pass, denied/ReachableNotUploaded, and timeout/hard Failure each settle into a completed semantic state; warning exposes copy, failure exposes retry, and neither action leaks into the other state.
    - RED A-04 confirmation: raw navigation exposes the unabridged summary key path, BEGIN sentinel, every managed-block line, and END sentinel before the write action is activated.
    - RED D-19: the real Git pane visibly contains both the Phase-3 disabled reason and the frozen always-visible Continue hint at 100x30.
    - RED viewport integrity: source bytes remain unchanged; resize clamps offsets; ANSI sequences do not corrupt display-width slicing; Shift+Left/Right wizard precedence remains higher than viewport controls.
  </behavior>
  <action>
    Start with the raw-PTY happy-path test so one production path proves real binary → fake external SSH process → captured TestResultView → focusable viewport → terminal bytes. Extend `ExactTextViewport` with horizontal offset, display-width-safe slicing, explicit line/column range cues, and deterministic vertical/horizontal controls. Store viewport focus and offsets in the owning wizard/ceremony models and advertise only controls that are actually routed. Per D-04, do not stop auto-chain merely to make a screenshot; render stage 1 as explicitly complete while stage 2 is separately marked in flight, then render both complete after stage 2 settles.

    Build the proof viewport text directly from the captured result fields per TEST-01/02 and D-22. Include stage labels, full command, raw output, resolution command, and unfiltered resolution output; never reconstruct argv or replace source bytes with presentation summaries. Add D-22 pass, denied, and timeout PTY cases that prove completed markers, warning/error ANSI roles, glyph+word semantics, allowed action, forbidden cross-state action, and retry transition per D-01 through D-04/D-25.

    Route the create confirmation's exact summary and managed block through the same focusable contract, preserving Cancel as default focus and leaving persistence untouched. Prove D-05/D-06/D-08 content is fully reachable before confirmation. Keep D-18 Skip functional and ensure D-19's real disabled reason and frozen hint both remain visible; do not implement deferred Git writes, upload behavior, migration UX, or CLI rebuilding.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... -run 'Test.*ExactTextViewport' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 300s ./e2e/... -run 'TestCreateFlow_.*(CompletedStage1ExactProof|CompletedStage2ExactResolution|ReachableNotUploadedCompleted|HardFailureRetryCompleted|ConfirmationExactViewport|GitStepDisabledReason)'</automated>
  </verify>
  <done>A developer can inspect every exact proof and confirmation byte through real advertised controls at 100x30, with completed PASS/warning/failure semantics and no real-file or account effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State-truthful expanded PNG and text capture</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_test.go, internal/screenshot/html.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go</files>
  <behavior>
    - RED A-01: manual reuse capture fails unless Reuse is selected, the manual input contains a sandbox private-key path, and the resolved algorithm/fingerprint marker is visible.
    - RED A-02/A-03: the required registry includes completed stage-1 PASS, completed stage-2 PASS plus full resolution proof, completed ReachableNotUploaded, completed hard failure/retry, D-19 Git, and navigated confirmation frames in addition to form/reuse/mouse states.
    - RED capture truth: each surface/frame requires its own interaction, before-save marker, applicability, required regions, and raw/rendered evidence; a route breadcrumb, running marker, or another state's marker fails.
    - RED approved sources: approved TUI is built from `PacketApprovalCommit`; approved HTML executes the declared interaction and waits for the declared DOM/text marker, with an explicit decision-linked comparator for Phase-3-only D-02 rather than a fabricated approved state.
    - RED artifact completeness: each applicable frame yields a nonempty PNG, rendered `.txt`, and normalized raw ANSI/transcript member; missing tools or members fail without creating the source-SHA destination.
  </behavior>
  <action>
    Replace the legacy parallel ID/route/switch tables with one registry of `ScreenSpec`, per-surface expectations, and ordered `FrameSpec` interactions. Make the required semantic inventory explicit: filled SSH form, selected reuse, resolved manual reuse, mouse-focused Port, completed stage-1 PASS, completed stage-2 PASS with resolution proof, completed D-02 warning/copy state, completed hard-failure/retry state, real D-19 Git form, and confirmation summary/managed-block viewport frames. Add as many named viewport frames as needed to show every required command/output/key/sentinel byte; derive counts from the registry rather than a numeric constant.

    Fix manual reuse by creating a parseable public sandbox key fixture under disposable HOME, selecting Reuse, moving focus to the trailing manual row and its input, typing the path, and waiting for the unique resolved path/algorithm/fingerprint marker. Parameterize fake SSH as pass, denied, and timeout and wait for completed semantic markers, not sleep-only or running-state markers. Capture both the normalized rendered frame and raw terminal/ANSI evidence used to produce the PNG, and call state validation before writing any artifact.

    Build the approved TUI and HTML from the fixed approval commit per D-24. For HTML, add an interaction callback/step description to `HTMLOptions`, execute it in the captured page, and require the state-specific marker before saving. For D-02, record the decision-linked approved comparator and divergence rather than claiming the approved source contains the later warning state. Reject missing applicability reasons, marker collisions, wrong-route markers, duplicate IDs, and unexplained duplicate frame hashes per D-25.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test.*(ScreenSpec|FrameSpec|ManualReuseResolved|RequiredSemanticInventory|CompletedCapture|HardFailureCapture|ApprovedInteraction|StateMarker|ArtifactCompleteness)'</automated>
  </verify>
  <done>The capture registry drives real interactions for every required semantic state and emits truthful nonempty PNG/rendered/raw text artifacts without publishing or changing an existing evidence directory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Review-gated immutable source-SHA publication</name>
  <precondition>`freeze`, the pinned Chromium capture runtime, `pnpm`, the independent UI reviewer route, and Codex are available for read-only candidate review; if either reviewer is unavailable, halt before publication.</precondition>
  <files>internal/screenshot/createflow_regions.go, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go</files>
  <behavior>
    - RED A-06: every required frame has nonempty named-region comparisons containing normalized live/approved text, hashes, equality/divergence status, and a D-XX justification for each accepted difference.
    - RED A-05/A-07: candidate generation writes only to a fresh temporary directory; final publication is impossible without a canonical candidate manifest plus two declared raw review outputs and structured zero-blocker verdicts bound to its exact hash.
    - RED independence: UI and Codex records must have different reviewer/tool/provider identities, exact prompts, raw stdout/stderr, model/session/version/timestamps/exit status, and matching source/candidate hashes.
    - RED freshness/immutability: changed HEAD, dirty production source, candidate-member drift, candidate-hash mismatch, missing PNG/text/region/review member, malformed verdict, nonzero review exit, open Critical/High, duplicate reviewer identity, existing destination, or undeclared file fails closed.
    - RED canonicalization: members are slash-normalized and sorted; candidate/final self-hashes reproduce from stored bytes; final provenance is absent from the candidate and created only after both raw reviews exist.
  </behavior>
  <action>
    Use `ExtractRegion` through registry-declared required regions instead of whole-screen comparison. Emit one record per frame/region carrying normalized live and applicable approved text, both hashes, result, and a specific D-02/D-16/D-19 or structural-fixture justification. Normalization may replace only disposable HOME/workspace prefixes and deterministic timestamps; retain complete commands, outputs, resolution values, paths relative to the sandbox, ANSI semantics, controls, and state markers. Reject empty required regions and ensure warning, failure/retry, manual reuse, D-19, and confirmation viewport frames all have meaningful region evidence.

    Implement an explicit candidate/final protocol. `candidate` captures twice in separate processes, compares every canonical member, validates registry coverage, and leaves a reviewable temporary bundle with `CANDIDATE-MANIFEST.json`; it must not create the source-SHA publication directory or claim review provenance. After tests and all source changes are green, commit the coherent correction with hooks enabled, resolve the new full HEAD SHA, assert the production tree is clean outside protected planning artifacts, and generate the candidate from that exact SHA.

    Dispatch a fresh agent-ui-ux-designer review and a separate fresh Codex review against the exact candidate-manifest hash. Give both the same immutable inventory plus recipes, FIELDS, 03-UI-SPEC, confirmed A-01 through A-07 findings, all PNG/text/raw members, region comparisons, and a required machine-readable verdict footer. Capture each exact prompt and unedited stdout/stderr with executable/version/provider/model/session/start/end/exit metadata. Reject reused output or reviewer identity. If either review reports any Critical/High, return to RED with a source fix, create a new commit/SHA and candidate, and rerun both reviews; never append reviews to the rejected candidate.

    Only after two matching zero-blocker raw reviews exist, have `finalize` build a fresh staging copy that declares `CANDIDATE-MANIFEST.json`, all capture members, region evidence, prompts, raw stdout/stderr, parsed verdicts, human-readable review reports, and newly created `REVIEW-PROVENANCE.json`. Validate hashes, source freshness, reviewer independence, complete inventory, and no undeclared files, then atomically rename to `.planning/phases/03-create-flow-backend/03-14-review-packet/<new-full-source-sha>/`. Refuse every existing destination and never modify any prior packet. Do not mark Phase 3 complete.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test.*(NamedRegion|CandidateManifest|Canonical|SelfHash|ReviewProvenance|ReviewerIndependence|BlockingSeverity|FreshSource|Finalization|ImmutableDestination|UndeclaredMember)' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test &amp;&amp; make lint &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression &amp;&amp; test -f .planning/phases/03-create-flow-backend/03-14-review-packet/$(git rev-parse HEAD)/MANIFEST.json</automated>
    <human-check>Inspect provenance only: both raw independent reviews identify the same current source SHA and candidate manifest hash, use distinct reviewer identities, and report zero open Critical/High.</human-check>
  </verify>
  <done>A new committed source SHA has one immutable, complete PNG/text packet published only after two declared independent raw reviews passed; all protected evidence, user config, keys, accounts, network state, and unrelated worktrees remain unchanged.</done>
</task>

</tasks>

<dependency_graph>
| Task | Needs | Creates | Sequence reason |
|------|-------|---------|-----------------|
| 1 | Existing production TUI, TestResultView fields, D-22 fake SSH | Inspectable completed proof/warning/failure/confirmation states | Capture must target behavior that is genuinely reachable and complete. |
| 2 | Task 1 production states and controls | Truthful expanded registry and PNG/text capture artifacts | State labels, scripts, and viewport frames must bind to corrected production behavior. |
| 3 | Tasks 1-2 green and committed | Named regions, candidate hash, raw independent reviews, immutable final packet | Reviews and provenance are valid only for stable source and a precise complete candidate. |
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fake SSH process → production TestResultView → viewport | Security-relevant commands and output must remain exact, complete, and attributable to the run. |
| PTY/HTML interactions → ScreenSpec/FrameSpec labels | A route, running frame, or generated-key state must not impersonate another semantic state. |
| Approval commit → approved TUI/HTML artifacts | Reference evidence must come from the fixed approved source and declared interactions. |
| Candidate files → region evidence and candidate manifest | Required members, normalized regions, paths, hashes, ordering, and self-hash must resist omission or substitution. |
| Candidate manifest → independent reviewers → final manifest | Review identity, raw output, verdict, and exact candidate binding must be non-circular and fail closed. |
| Capture process → local machine | Disposable HOME and fake SSH must prevent reads/writes to user configuration, keys, network providers, and external accounts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14-01 | Tampering | ExactTextViewport | high | mitigate | Preserve source bytes, add two-axis display-width-safe navigation, and prove complete content via raw PTY. |
| T-03-14-02 | Spoofing | capture registry | critical | mitigate | Require per-surface interaction, completed marker, applicability, frame markers, and before-save validation. |
| T-03-14-03 | Tampering | manual reuse/failure captures | high | mitigate | Resolve a sandbox key and parameterize fake SSH outcomes with unique semantic markers. |
| T-03-14-04 | Tampering | region evidence and manifests | critical | mitigate | Require nonempty named-region content, canonical member hashes, reproducible self-hashes, and undeclared-file rejection. |
| T-03-14-05 | Repudiation | independent reviews | critical | mitigate | Preserve exact prompts/raw streams/tool identities and bind both zero-blocker verdicts to one candidate hash. |
| T-03-14-06 | Information Disclosure | capture/review artifacts | high | mitigate | Use disposable HOME/public fixtures, normalize temp prefixes, reject private-key or real-home paths, and avoid network smoke. |
| T-03-14-07 | Elevation of Privilege | filesystem/account boundary | critical | mitigate | D-22 PATH shim, protected-directory hash inventories, fresh-only atomic destination, and no config/account mutation commands. |
| T-03-14-08 | Denial of Service | PTY/reviewer execution | medium | mitigate | Bounded waits/process termination and fail-closed retries without partial publication. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Complete algorithm → SSH → two-stage test → confirmed store flow behind approved design | 1-3 | COVERED | Correct production proof/confirm surfaces and trustworthy review evidence. |
| REQ | SSHUI-01/02/03 | Four-field form, mouse/keyboard focus, exact Host preview | 1-3 | COVERED | Existing behavior retained and captured through real interactions. |
| REQ | SSHUI-04/05 | Throwaway testing and recipe-faithful macOS globals | 1-3 | COVERED | Disposable HOME/fake SSH plus full regression suite; no real mutation. |
| REQ | TEST-01/02 | Exact completed two-stage commands/output and ssh -G proof | 1-3 | COVERED | Two-axis viewport, raw PTY, frame artifacts, named regions. |
| REQ | TEST-03 | Confirmed backed-up persistence | 1-3 | COVERED | Full pre-write evidence; existing transaction re-tested, not executed against real HOME. |
| REQ | KEY-06 | Selected and manual resolved key reuse | 2-3 | COVERED | Real picker interaction and unique fixture marker. |
| REQ | DLV-04/06 | Visual review and per-screen real-binary PTY | 1-3 | COVERED | Expanded PNG/text inventory and two independent reviews. |
| RESEARCH | responsibility map | Presentation owns viewport; PTY owns real interaction; screenshot/build tooling owns evidence | 1-3 | COVERED | Corrections remain in assigned tiers. |
| RESEARCH | D-22 constraint | External PATH-shim fake SSH, no in-Go tester substitution | 1-3 | COVERED | All production/evidence runs use the external fake. |
| RESEARCH | package audit | No new dependency or install | 1-3 | COVERED | Existing Go, freeze, Chromium, and pnpm tooling only. |
| CONTEXT | D-01..D-04 | Store taxonomy, warning/copy, auto-chain, failure retry | 1-3 | COVERED | Distinct completed semantic states and evidence. |
| CONTEXT | D-05..D-09 | Auto-detected Include default, globals, collision, complete confirm | 1-3 | COVERED | Full review viewport and regression gates; no migration UX. |
| CONTEXT | D-10..D-13 | Reuse picker/manual path/in-use warning/algorithm breadth | 2-3 | COVERED | State-correct resolved reuse capture. |
| CONTEXT | D-14..D-17 | Real shell, demo banner, shared backend-free UI | 1-3 | COVERED | Production shell retained; captures validate real/shared boundary. |
| CONTEXT | D-18/D-19 | Functional Skip and truthful disabled Continue | 1-3 | COVERED | Both required D-19 rows visibly captured; Git writes remain deferred. |
| CONTEXT | D-20/D-21 | Provider inference and known/custom endpoints | 1-3 | COVERED | Existing form/spec behavior retained in proof and confirm captures. |
| CONTEXT | D-22/D-23 | Deterministic fake SSH; network smoke separate | 1-3 | COVERED | Fake-only required gates; D-23 smoke is not invoked. |
| CONTEXT | D-24/D-25 | Automated diff, two independent reviews, hard severity gate | 2-3 | COVERED | Candidate/final protocol and zero Critical/High publication gate. |
| AUDIT | A-01 | Manual reuse mislabeled as Generate | 2-3 | COVERED | Resolved sandbox path marker required before capture. |
| AUDIT | A-02 | Legacy eight-screen inventory only | 2-3 | COVERED | Registry adds all required completed/warning/failure/frame evidence. |
| AUDIT | A-03/A-04 | Running/clipped proof and confirmation | 1-3 | COVERED | Production two-axis viewport plus navigated frames. |
| AUDIT | A-05/A-06 | Missing manifest/provenance and weak regions | 3 | COVERED | Canonical manifests and full named-region content. |
| AUDIT | A-07 | No review/finalization path | 3 | COVERED | Two raw independent reviews gate atomic publication. |

Deferred layout migration, Git writes, upload instructions/automation, CLI rebuilding, and `insteadOf` rendering remain excluded exactly as 03-CONTEXT.md requires.
</source_coverage_audit>

<verification>
1. Preserve before/after SHA-256 inventories for every pre-existing evidence/debug directory and unrelated worktree.
2. Record each RED command and real failure, then GREEN output, in `03-14-SUMMARY.md`; keep each coherent test+implementation correction in one hook-verified commit.
3. Drive completed PASS, ReachableNotUploaded, hard-failure/retry, D-19, and confirmation through the real 100x30 PTY using raw viewport controls.
4. Prove exact command/output/resolution/key/sentinel bytes are reachable vertically and horizontally with no reconstruction or hidden suffix.
5. Run negative capture tests for Generate-as-manual, running-as-complete, wrong route, absent marker, another state's marker, missing frame, empty PNG/text/raw member, and unexplained duplicate.
6. Require meaningful nonempty named-region records for every applicable required frame and reproduce candidate/final self-hashes from stored bytes.
7. Commit corrected source, build two process-isolated candidates from that exact clean HEAD, and compare every canonical candidate member.
8. Obtain two fresh distinct raw reviews of the same candidate hash; any Critical/High restarts source correction and invalidates both reviews.
9. Atomically publish only the fully declared final packet to a new source-SHA directory; validate no undeclared files and refuse all pre-existing destinations.
10. Run `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression`; all must exit zero.
11. Confirm no real HOME/config/key/account/network mutation and no change to any protected artifact.
</verification>

<success_criteria>
- Production proof and confirmation viewports expose every required byte at 100x30 through tested controls.
- Manual reuse, completed PASS, warning/copy, hard failure/retry, D-19, and complete confirmation each have state-correct PNG and text evidence.
- Approved TUI/HTML captures come from the approval commit and validate declared interactions/markers.
- Every required frame has meaningful named-region evidence; candidate and final manifests validate canonically from stored bytes.
- The packet source is a new clean committed HEAD and the source-SHA destination did not exist before execution.
- Two distinct raw independent reviews bind to the same precise candidate and report zero open Critical/High before provenance or final publication exists.
- All prior evidence, dirty planning/debug artifacts, real user files, keys, external accounts, network state, and unrelated worktrees remain unchanged.
- The summary reports corrective-plan completion only and does not mark Phase 3 complete.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-14-SUMMARY.md` with protected-path before/after hashes, RED/GREEN commands and real outputs, source commit SHA, registry/frame inventory, exact PTY controls and observed proof bytes, candidate/final manifest hashes, named-region coverage, both independent raw-review provenance records and dispositions, full gate results, and confirmation that no prior evidence or real user/account state changed. Do not mark Phase 3 complete.
</output>
