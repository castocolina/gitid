---
phase: 03-create-flow-backend
plan: 09
type: execute
wave: 8
depends_on: [03-08]
files_modified:
  - Makefile
  - cmd/gitid/gate_visual_regression_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_test.go
  - .planning/design/create-flow/visual-divergence-allowlist.txt
  - .planning/phases/03-create-flow-backend/03-09-review-packet/MANIFEST.md
  - .planning/phases/03-create-flow-backend/03-09-review-packet/EVIDENCE.json
  - .planning/phases/03-create-flow-backend/03-09-review-packet/text-diffs.txt
  - .planning/phases/03-create-flow-backend/03-09-review-packet/approved-html-contact-sheet.png
  - .planning/phases/03-create-flow-backend/03-09-review-packet/approved-tui-contact-sheet.png
  - .planning/phases/03-create-flow-backend/03-09-review-packet/live-tui-contact-sheet.png
  - .planning/phases/03-create-flow-backend/03-09-review-packet/UI-REVIEW.md
  - .planning/phases/03-create-flow-backend/03-09-review-packet/CODEX-REVIEW.md
  - .planning/phases/03-create-flow-backend/03-VALIDATION.md
autonomous: true
requirements: [DLV-04, DLV-06, SSHUI-01, SSHUI-02, SSHUI-03, TEST-01, TEST-02, TEST-03, KEY-06]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The visual gate contains meaningful non-exempt coverage: a documented divergence can permit only an exact normalized region/predicate, never an entire screen."
    - "Every captured screen is deterministic and offline; screenshot capture cannot contact a provider or mutate HOME."
    - "Reviewers can inspect labeled, deterministic PNG evidence comparing the final real binary against both approved HTML and approved TUI references at the fixed geometry."
    - "Unknown, duplicate, stale, empty-reason, over-broad, or unused exemptions fail the gate."
    - "Both required independent reviews complete against text and PNG evidence, and every Critical/High finding is fixed before Phase 3 validation can pass per D-24/D-25."
  artifacts:
    - path: "cmd/gitid/gate_visual_regression_test.go"
      provides: "Strict region-scoped visual-difference gate and allowlist schema validation"
    - path: "internal/screenshot/createflow.go"
      provides: "Deterministic final real-binary screen capture with injected SSH effects"
    - path: ".planning/phases/03-create-flow-backend/03-09-review-packet/live-tui-contact-sheet.png"
      provides: "Labeled final live-TUI visual evidence"
    - path: ".planning/phases/03-create-flow-backend/03-09-review-packet/UI-REVIEW.md"
      provides: "agent-ui-ux-designer review disposition"
    - path: ".planning/phases/03-create-flow-backend/03-09-review-packet/CODEX-REVIEW.md"
      provides: "Codex review disposition"
  key_links:
    - from: "cmd/gitid/gate_visual_regression_test.go"
      to: ".planning/design/create-flow/visual-divergence-allowlist.txt"
      via: "strict parser validates screen, region/predicate, decision ID, and explanation, then proves every entry was used"
      pattern: "allowlist|region|predicate"
    - from: "internal/screenshot/createflow.go"
      to: "cmd/gitid/wiring.go"
      via: "capture injects deterministic command effects while retaining the real backend conversion/wiring path"
      pattern: "CaptureCreateFlowScreens"
    - from: ".planning/phases/03-create-flow-backend/03-09-review-packet/EVIDENCE.json"
      to: "three contact-sheet PNGs"
      via: "records source commit/provenance, geometry, font/theme, panel IDs, and SHA-256 hashes"
      pattern: "sha256|source_commit|screens"
---

<objective>
Replace the vacuous text-only visual gate with deterministic, reviewable design evidence and close review findings CR-10, CR-11 and WR-01.

Purpose: Make DLV-04 and D-24/D-25 capable of detecting unrelated visual drift instead of exempting every screen, then obtain the two required independent review verdicts on the remediated create flow.
Output: Strict regional text comparison, deterministic PNG contact-sheet pairs with provenance, cross-AI findings/dispositions, and an updated Phase 3 validation ledger.
</objective>

<execution_context>
@/Users/ramon/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/design/APPROVAL.md
@.planning/design/REFERENCE-INDEX.md
@.planning/design/create-flow/FIELDS.md
@.planning/design/create-flow/CRITIQUE.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-REVIEW.md
@.planning/phases/03-create-flow-backend/03-07-SUMMARY.md
@.planning/phases/03-create-flow-backend/03-08-SUMMARY.md
@.planning/phases/03-create-flow-backend/03-06-review-packet/MANIFEST.md

<interfaces>
Reuse `internal/screenshot.CaptureTUI` with the vendored JetBrains Mono font, fixed theme, metadata stripping, SHA-256 result, and 100×30 terminal geometry. Treat `APPROVAL.md` plus its git commit as the reference provenance: recover the approved live HTML and approved live TUI states from that commit into a temporary worktree/capture directory rather than silently blessing the current implementation. A contact sheet must preserve each panel at readable scale, label it with the exact screen ID, and contain the full enumerated screen set; it is evidence packaging, not a substitute for per-screen comparison.
</interfaces>
</context>

<review_dispositions>

| Finding | Disposition | Evidence and remediation owner |
|---------|-------------|--------------------------------|
| CR-10 | CONFIRMED | all eight `CreateFlowScreenIDs` differ and all eight are exempt at screen granularity. Tasks 1-2 require non-exempt regions and reject whole-screen entries. |
| CR-11 | CONFIRMED | the packet explicitly says no PNG pairs and the gate compares two freshly rendered backends rather than approved visual evidence. Tasks 1-3 recover approved provenance, capture final PNGs, and run both reviews. |
| WR-01 | CONFIRMED | in-process command draining calls the real backend's SSH effects. Task 1 injects a deterministic PATH/effect seam and asserts no network invocation. |
| WR-04 | ACCEPT UNTIL PLAN COMPLETION | roadmap/requirements/validation status is orchestrator-owned and correctly remains pending while blockers exist. Task 3 updates the Phase 3 validation ledger only after all gates/reviews pass; phase closeout then reconciles roadmap/requirements without rewriting completed plans. |

</review_dispositions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deterministic final-real-binary capture through one strict non-exempt screen</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_test.go, cmd/gitid/gate_visual_regression_test.go, Makefile, .planning/phases/03-create-flow-backend/03-09-review-packet/live-tui-contact-sheet.png</files>
  <behavior>
    - RED: capture fails if the real backend attempts to resolve/run system SSH instead of the injected deterministic executable/effect.
    - RED: the SSH form panel is captured from the real composition root at 100×30 and its non-divergent header/sidebar/form regions compare byte-exactly rather than being exempted.
    - RED: capture leaves temp HOME byte-identical except explicitly-created disposable capture fixtures and never creates known_hosts/final identity files.
    - RED: contact-sheet generation includes every enumerated screen exactly once, labels every panel, uses the vendored font/theme, strips metadata, and reproduces the same SHA-256 on rerun.
  </behavior>
  <action>Begin with tests around a single `ssh-form-filled` tracer and then generalize. Refactor capture construction to inject deterministic SSH/test outcomes through the existing command seam or a controlled PATH executable while still constructing `newBackendForHome(tempHome)` and driving the real tuikit model; do not replace the real backend with FixtureBackend. Add an offline guard that records every attempted argv and fails on unexpected binaries/arguments. Render each final live capture with `CaptureTUI`, then assemble a readable labeled contact sheet without resampling individual terminal panels. Update `make gate-visual-regression` to produce/check deterministic evidence as part of the gate. Preserve D-22's real-process PATH-shim principle and ensure D-02 warning, D-03 copy hint, D-19 disabled Git reason, the four-field D-20 form, and recipe Host preview are represented.</action>
  <verify>
    <automated>go test -tags screenshot -race ./internal/screenshot/... ./cmd/gitid/... -run 'Test.*(CreateFlowCapture|VisualRegression|ContactSheet|Offline)' -count=2</automated>
  </verify>
  <done>The tracer screen and then every final live screen capture deterministically offline from the real wiring, the gate has at least one meaningful non-exempt comparison, and two consecutive runs reproduce the same labeled live contact-sheet hash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recover approved references and enforce region-scoped divergence policy</name>
  <files>cmd/gitid/gate_visual_regression_test.go, .planning/design/create-flow/visual-divergence-allowlist.txt, .planning/phases/03-create-flow-backend/03-09-review-packet/approved-html-contact-sheet.png, .planning/phases/03-create-flow-backend/03-09-review-packet/approved-tui-contact-sheet.png, .planning/phases/03-create-flow-backend/03-09-review-packet/EVIDENCE.json</files>
  <behavior>
    - RED: whole-screen entries, unknown screen IDs, duplicate screen/region pairs, blank reasons, missing D-XX provenance, stale entries, and entries matching no actual difference fail.
    - RED: each approved/live screen compares all normalized regions; only the exact region/predicate named by a used allowlist entry may differ.
    - RED: unrelated changes to header, breadcrumb, sidebar, field order, focus marker, preview border, buttons, or footer fail even on a screen with an allowed warning/output region.
    - RED: evidence records the approval source commit, capture commands, screen IDs, geometry, font/theme, and SHA-256 for all three PNGs and fails validation if a field/artifact is absent.
  </behavior>
  <action>Resolve the git commit containing the recorded Phase 2 approval and recover/capture the authoritative interactive HTML and approved live TUI states from that commit in temporary storage; do not treat current Phase 3 output as its own baseline and do not restore stale reference files into `.planning/design`. Produce labeled HTML and TUI contact sheets matching the final live sheet's screen order. Replace the allowlist grammar with exact screen plus normalized region/predicate plus D-02/D-16/D-19 decision reference and explanation. Normalize only nondesign evidence such as provider output/path values; compare every remaining semantic/visual region. Require at least one non-allowlisted region per screen and reject unused exemptions. Record immutable provenance/hashes in EVIDENCE.json and assert byte/pixel evidence exists and is nonempty. No design divergence beyond locked D-02, D-16, and D-19 may be silently accepted.</action>
  <verify>
    <automated>make gate-visual-regression &amp;&amp; go test -tags screenshot -race ./cmd/gitid/... -run 'TestGateVisualRegression' -count=2</automated>
  </verify>
  <done>All screens retain strict non-exempt coverage, only exact approved divergence regions pass, unrelated drift turns the gate red, and three provenance-linked deterministic contact sheets are present.</done>
</task>

<task type="auto">
  <name>Task 3: Run both independent reviews and close only passing validation rows</name>
  <files>.planning/phases/03-create-flow-backend/03-09-review-packet/MANIFEST.md, .planning/phases/03-create-flow-backend/03-09-review-packet/text-diffs.txt, .planning/phases/03-create-flow-backend/03-09-review-packet/UI-REVIEW.md, .planning/phases/03-create-flow-backend/03-09-review-packet/CODEX-REVIEW.md, .planning/phases/03-create-flow-backend/03-VALIDATION.md</files>
  <action>Generate a fresh packet from the passing gate: complete unfiltered text/region diffs, the three contact sheets, EVIDENCE.json, expected-divergence manifest, and focused instructions to inspect appearance, four-field order, labels, options, focus, warnings, exact commands, safety ceremony, and recipe Host preview. Dispatch one clean-context `agent-ui-ux-designer` review and one independent Codex review of both text and PNG evidence per D-24. Preserve each review verbatim with model/tool provenance and a severity/disposition table. If either reviewer reports Critical/High, stop, fix it test-first in the owning production task, regenerate all evidence/hashes, and rerun both reviews; never mark validation complete over a blocking finding. Record Medium/Low as fixed or explicitly accepted with concrete rationale per D-25. After both verdicts pass, update only the Phase 3 validation ledger rows with commands, real outputs, artifact hashes, review paths, and finding dispositions. Do not rewrite plans 03-01 through 03-06 or claim Phase 4/5/9 deferred features.</action>
  <verify>
    <automated>make test &amp;&amp; make lint &amp;&amp; make test-e2e &amp;&amp; make gate-visual-regression &amp;&amp; test -s .planning/phases/03-create-flow-backend/03-09-review-packet/UI-REVIEW.md &amp;&amp; test -s .planning/phases/03-create-flow-backend/03-09-review-packet/CODEX-REVIEW.md &amp;&amp; ! rg -n 'Verdict:.*(BLOCK|FAIL)|\| *(CRITICAL|HIGH) *\| *(open|unresolved)' .planning/phases/03-create-flow-backend/03-09-review-packet/{UI-REVIEW.md,CODEX-REVIEW.md}</automated>
  </verify>
  <done>Both independent reviewers pass the final regenerated evidence, no Critical/High remains open, every lower-severity item has a disposition, and 03-VALIDATION cites reproducible commands/output and immutable evidence hashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture harness → real backend | Test infrastructure must retain real composition while preventing network/HOME effects. |
| allowlist → gate verdict | Human-authored exemptions can hide unrelated regressions if over-broad or stale. |
| approved commit → current evidence | Reference provenance must not be replaced by current output. |
| review packet → reviewer | Evidence must be complete and unfiltered so reviewers can independently detect drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09-01 | Tampering | visual allowlist | high | mitigate | Exact region/predicate schema, strict validation, used-entry accounting, and mandatory non-exempt regions. |
| T-03-09-02 | Spoofing | approved baseline provenance | high | mitigate | Capture from the approval-bearing git commit and record commit/hash/tool parameters in EVIDENCE.json. |
| T-03-09-03 | Information Disclosure | screenshot capture HOME/network | medium | mitigate | Temp HOME, deterministic SSH effect, argv audit, and no real provider operations. |
| T-03-09-04 | Repudiation | cross-AI review verdict | medium | mitigate | Persist verbatim review artifacts with model/tool provenance and per-finding disposition. |
</threat_model>

<source_coverage_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | End-to-end algorithm → four-field SSH → isolated two-stage test → confirmed store matching approved design | 03-07, 03-08, 03-09 | COVERED | Safety, correctness, then final visual evidence. |
| REQ | SSHUI-01 | Four ordered fields and provider inference | 03-08 | COVERED | CR-12 plus strict input remediation. |
| REQ | SSHUI-02 | Mouse and keyboard reachability | 03-08, 03-09 | COVERED | Focus tests plus final screenshots/e2e. |
| REQ | SSHUI-03 | WYSIWYG Host preview | 03-08, 03-09 | COVERED | Checked renderer plus evidence gate. |
| REQ | SSHUI-04 | Throwaway-file tests and no live mutation | 03-07 | COVERED | Key/config/known_hosts staging. |
| REQ | SSHUI-05 | Idempotent macOS globals after specific hosts | 03-07 | COVERED | Transaction and doctor non-destruction retain D-08. |
| REQ | TEST-01 | Two exact command-visible stages | 03-07, 03-08 | COVERED | Automatic chain and both stage-2 commands. |
| REQ | TEST-02 | Complete ssh -G proof | 03-08 | COVERED | All expected fields and errors. |
| REQ | TEST-03 | Confirmed backed-up persistence | 03-07 | COVERED | Explicit result plus coordinated rollback. |
| REQ | KEY-06 | Verified existing-key reuse | 03-07, 03-08 | COVERED | No preconfirm write, pair check, confirmed chmod. |
| REQ | DLV-04 | Visual regression against approved references | 03-09 | COVERED | Regional text gate, PNG evidence, two reviews. |
| REQ | DLV-06 | Per-screen real-binary PTY e2e | 03-07, 03-08 | COVERED | Updated behavioral assertions. |
| RESEARCH | — | UI-free core and thin real composition root | 03-07, 03-08 | COVERED | Logic remains identity/sshconfig/keygen/tester; tuikit stays backend-free. |
| RESEARCH | — | Existing filewriter, fake SSH, parser, catalog, and screenshot substrate | 03-07, 03-08, 03-09 | COVERED | Reused; no package install. |
| CONTEXT | D-01..D-04 | Gate taxonomy, warning/copy semantics, automatic sequencing | 03-07, 03-08, 03-09 | COVERED | D-02/D-03 also reviewed visually. |
| CONTEXT | D-05..D-09 | Layout, Include default, globals, complete collision gate | 03-07, 03-08 | COVERED | No migration UX introduced. |
| CONTEXT | D-10..D-13 | Reuse picker, matching pair/perms, warn/allow, algorithm breadth | 03-07, 03-08 | COVERED | Encrypted pair proof preserves no-passphrase decision. |
| CONTEXT | D-14..D-19 | Real app/shared UI/SSH-only skip and Git-disabled divergence | Existing 03-02..03-05, 03-09 | COVERED | Remediation preserves shipped decisions; D-19 gets final evidence. |
| CONTEXT | D-20..D-21 | Provider inferred from Host, known/custom endpoints | 03-08 | COVERED | Editable Provider removed. |
| CONTEXT | D-22..D-23 | PATH-shim deterministic tests and separate network smoke | 03-07..03-09, existing 03-06 | COVERED | Visual capture is offline; smoke remains optional. |
| CONTEXT | D-24..D-25 | Two-layer gate, PNG/text cross-AI review, severity policy | 03-09 | COVERED | Both reviews block on Critical/High. |

Deferred Git writes, upload automation, CLI rebuild, migration UX, and W1 `insteadOf` remain excluded exactly as CONTEXT specifies.
</source_coverage_audit>

<verification>
Run every task command, followed by the complete matrix: `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression`. Expected output: all exit 0; two consecutive screenshot runs produce the same hashes; mutation of any protected non-divergent region makes the visual test fail in a deliberate local negative-control test that is reverted before commit; both review artifacts report no open Critical/High. Confirm all commands and real outputs are copied into 03-VALIDATION and no recipe or completed PLAN/SUMMARY file changed.
</verification>

<success_criteria>
- CR-10, CR-11, and WR-01 are closed with deterministic automated and review evidence.
- Every Phase 3 Critical/High finding listed across Plans 07-09 has a confirmed remediation and passing regression proof.
- The final live TUI is compared against approval-provenance HTML and TUI evidence, not against itself.
- D-24's agent-ui-ux-designer and Codex reviews cover both text and screenshots; D-25 severity policy is enforced.
- WR-04 remains pending only until successful phase closeout metadata reconciliation; completed plans are preserved.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-09-SUMMARY.md` when done.
</output>
