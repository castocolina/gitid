---
phase: 03-create-flow-backend
plan: 08
type: execute
wave: 7
depends_on: [03-07]
files_modified:
  - cmd/gitid/wiring.go
  - internal/keygen/keyscan.go
  - internal/keygen/keyscan_test.go
  - internal/sshconfig/validation.go
  - internal/sshconfig/validation_test.go
  - internal/tester/tester.go
  - internal/tester/tester_command_test.go
  - internal/tuikit/design.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - e2e/create_flow_pty_e2e_test.go
autonomous: true
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, TEST-01, TEST-02, KEY-06, DLV-06]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The SSH screen contains exactly Alias prefix, SSH Host, Real hostname, and Port in that order; provider inference is derived from SSH Host per D-20 and is not separately editable."
    - "Malformed aliases, hostnames, ports, paths, wildcard/directive/control-character input, ambiguous Host patterns, and configuration parse failures block advance and cannot reach rendering or exec."
    - "The algorithm list rendered, navigated, previewed, tested, and persisted is one backend-probed catalog; unavailable/unimplemented entries cannot be selected and selected paths use the selected algorithm."
    - "Stage 2 shows both exact commands and succeeds only when unpinned alias connectivity and successful `ssh -G` resolve User, Hostname, Port, IdentitiesOnly, and IdentityFile to the expected values."
    - "Every reused private/public key pair is verified, including encrypted private keys without a passphrase prompt, and permission changes are previewed then applied only in the confirmed transaction."
  artifacts:
    - path: "internal/sshconfig/validation.go"
      provides: "UI-free strict Host-block validation and effective OpenSSH-pattern collision checking"
    - path: "internal/tester/tester.go"
      provides: "Unpinned alias connectivity plus complete, error-bearing resolved-configuration proof"
    - path: "internal/keygen/keyscan.go"
      provides: "Non-interactive private/public fingerprint matching for reusable keys"
    - path: "internal/tuikit/identities.go"
      provides: "Four-field form, catalog-consistent navigation, actionable validation, and full stage-2 evidence"
  key_links:
    - from: "internal/tuikit/identities.go"
      to: "internal/sshconfig/validation.go"
      via: "optional backend validation seam blocks the form using the complete alias and concrete error"
      pattern: "ValidateCreate|ValidationError"
    - from: "cmd/gitid/wiring.go"
      to: "internal/tester/tester.go"
      via: "expected CreateSpec fields are supplied to the checked stage-2 proof and projected into the result DTO"
      pattern: "ResolvedVia|ResolvedConfig"
    - from: "cmd/gitid/wiring.go"
      to: "internal/keygen/keyscan.go"
      via: "reuse selection verifies matching fingerprints before staging and records confirmed permission normalization"
      pattern: "Fingerprint|Verify.*Pair"
---

<objective>
Make every SSH create input, catalog choice, key-reuse choice, and stage-2 proof trustworthy, closing review findings CR-03, CR-04, CR-05, CR-06, CR-08, CR-12 and WR-03.

Purpose: Prevent malformed or ambiguous OpenSSH configuration and ensure the UI proves the exact recipe configuration it will persist.
Output: A strict UI-free validation/collision boundary, complete alias-resolution proof, verified key reuse, and the frozen four-field SSH form.
</objective>

<execution_context>
@/Users/ramon/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/phases/03-create-flow-backend/03-REVIEW.md
@.planning/phases/03-create-flow-backend/03-07-SUMMARY.md

<interfaces>
`tuikit.CreateSpec` is the view DTO crossing into the real backend. Keep validation logic in UI-free `internal/sshconfig`/`internal/keygen`/`internal/tester`; `cmd/gitid/wiring.go` is only the composition and conversion boundary. Preserve `internal/tuikit`'s no-backend-import contract. Extend `AlgorithmCatalogEntry` with explicit implementation/availability state rather than inferring it from note text. Preserve D-13: any verified parseable reuse algorithm is allowed even when it is not a generate-catalog entry.
</interfaces>
</context>

<review_dispositions>

| Finding | Disposition | Evidence and remediation owner |
|---------|-------------|--------------------------------|
| CR-03 | CONFIRMED | rendering iterates static `AlgorithmCatalog`, selection indexes backend catalog, availability is reduced to notes, `keyPath` is ed25519, and persisted `createInput` forces ed25519. Task 2 establishes one explicit catalog state. |
| CR-04 | CONFIRMED | collision receives identity name rather than full alias, compares equality only, and errors collapse to no collision. Task 1 checks complete aliases against effective OpenSSH patterns and fails closed. |
| CR-05 | CONFIRMED | nonempty/digit-only checks permit unsafe tokens and out-of-range ports before direct interpolation. Task 1 validates at the core renderer boundary and the UI. |
| CR-06 | CONFIRMED | connectivity pins `-i`; `ssh -G` command/error is hidden; only the first IdentityFile is displayed and no expected fields are enforced. Task 3 makes both commands and every expected field part of success. |
| CR-08 | CONFIRMED | encrypted-key `.pub` content is trusted without pair proof and reused modes are not normalized. Task 2 compares fingerprints non-interactively and defers chmod to confirmation per D-11. |
| CR-12 | CONFIRMED | Provider remains visible and first in the focus ring despite D-20/SSHUI-01. Task 1 removes it and retains suffix-driven inference. |
| WR-03 | CONFIRMED | final-two-label extraction fails for custom/multi-label providers. Task 1 preserves provider metadata from the validated form inference rather than reconstructing it from the alias. |

</review_dispositions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Four-field form through strict rendering and effective collision proof</name>
  <files>internal/sshconfig/validation.go, internal/sshconfig/validation_test.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, cmd/gitid/wiring.go</files>
  <behavior>
    - RED: the form and Tab/mouse ring expose exactly Alias prefix → SSH Host → Real hostname → Port, with no Provider field, while known-provider suffix edits still autofill editable endpoints per D-20/D-21.
    - RED: aliases/hostnames reject empty labels, whitespace, controls, wildcards, negation, directive-like tokens, and line breaks; ports reject 0, values above 65535, signs, and non-digits; IdentityFile paths reject controls/newlines and unsafe empty values.
    - RED: `personal.github.com` collides with exact, wildcard, comma-separated, or first-match-effective Host patterns in the main and recursively included configs; negated patterns obey OpenSSH semantics.
    - RED: unreadable, cyclic, symlink-unsafe, or malformed Include/config input returns a blocking validation error rather than `false`.
  </behavior>
  <action>Create table-driven failing tests first. Add UI-free `ValidateHostBlock` and checked render entry points in `internal/sshconfig/validation.go`; validation must occur immediately before interpolation as defense in depth, even when the TUI already gated the form. Add Include-aware effective Host-pattern discovery using the existing SSH parser and OpenSSH positive/negative wildcard semantics, with explicit cycle/read/parse errors. In tuikit, remove `provider` input, `sshFieldProvider`, rendering, and focus behavior; initialize SSH Host directly to `acme.github.com`, infer provider from the validated Host using the known-provider mapping supplied by the backend, and preserve that inferred provider in CreateSpec so multi-label/custom hosts are not later truncated. Call the complete-alias collision/validation seam as the user types and render its specific error inline. Keep all four approved fields visible/clickable and preserve recipe preview order. Do not add Git fields or Phase 9 upload behavior.</action>
  <verify>
    <automated>go test -race ./internal/sshconfig/... ./internal/tuikit/... -run 'Test.*(Validate|Collision|HostPattern|FourField|Provider|Focus|Port)' -count=1</automated>
  </verify>
  <done>The approved four-field form is byte/order correct, known/custom provider behavior remains D-20/D-21 compliant, and no unsafe or ambiguous value can advance, render, execute, or persist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use one selectable catalog and verify reusable key pairs</name>
  <files>internal/keygen/keyscan.go, internal/keygen/keyscan_test.go, internal/tuikit/design.go, internal/tuikit/identities.go, cmd/gitid/wiring.go</files>
  <behavior>
    - RED: rendering and navigation use the same injected catalog order and selection; empty/all-disabled catalogs do not panic or loop.
    - RED: explicit `Implemented` and `Available` flags disable every unsupported entry independent of note wording; only implemented/available selections reach generation.
    - RED: selecting rsa-4096 produces the rsa-4096 preview/test/final path and persists rsa-4096 rather than ed25519.
    - RED: matching unencrypted and encrypted private/public pairs are accepted; mismatched, malformed, unreadable, or missing unverifiable pairs are rejected with no mutation.
    - RED: `ssh-keygen -lf` fingerprint extraction for an encrypted private key is non-interactive and equals the parsed `.pub` fingerprint; no passphrase text or private bytes are logged.
  </behavior>
  <action>Extend the catalog DTO and real converter with explicit implementation and machine-availability fields from `keygen.ResolveAvailability`; render, disable, navigate, derive key path, build CreateInput, and persist from this same catalog entry. Guard zero/no-enabled cases without modulo loops. In `internal/keygen`, add an injectable argv-runner helper that invokes `ssh-keygen -lf` without a shell or passphrase prompt to read the public fingerprint exposed by an encrypted OpenSSH private file, then compare it to the canonical parsed sibling `.pub` fingerprint; use direct x/crypto public derivation for unencrypted keys. Treat mismatches and unverifiable encrypted pairs as blocking, satisfying D-11's matching-pair requirement without adding a passphrase prompt. Report required 0600/0644 normalization in the write plan and apply it only through Plan 07's confirmed transaction; do not chmod during scan/test. Keep non-catalog algorithms reusable per D-13.</action>
  <verify>
    <automated>go test -race ./internal/keygen/... ./internal/tuikit/... ./cmd/gitid/... -run 'Test.*(Catalog|Algorithm|KeyPair|Encrypted|Fingerprint|Permission|Reuse)' -count=1</automated>
  </verify>
  <done>Catalog display, selection, paths, testing, and persistence cannot disagree; every reused pair is proven matching; and permissions change only in the previewed confirmed transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Require complete unpinned alias connectivity and ssh -G resolution</name>
  <files>internal/tester/tester.go, internal/tester/tester_command_test.go, internal/tuikit/identities.go, cmd/gitid/wiring.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - RED: stage-2 connectivity argv uses `-F <temp-config>` and the alias but no explicit identity override; the staged Host block must supply the key.
    - RED: the UI shows the exact connectivity command and exact `ssh -F <temp-config> -G <alias>` command before/during/after execution.
    - RED: nonzero/failed `ssh -G`, empty output, wrong user, hostname, port, IdentitiesOnly, missing expected IdentityFile, or an unexpected earlier IdentityFile makes stage 2 Failure with field-specific detail.
    - RED: a complete matching resolution plus PASS or ReachableNotUploaded connectivity is accepted and auto-advances from Plan 07's chain.
  </behavior>
  <action>Split stage-2 command construction into two exported pure display helpers backed by the exact argv slices used for execution: alias connectivity must not pin the key, and resolution must use the same explicit temp config. Return an error-bearing proof that retains both commands, outputs, and the parsed ResolvedConfig. Compare normalized paths and every expected recipe field: `User git`, selected hostname/port, `IdentitiesOnly yes`, and the staged IdentityFile in effective order. Project both commands and proof failures through the existing TestResultView command/detail surface and render them in the bounded stage-2 area without hiding either command. Update the fake-SSH PATH shim expectations and real-binary PTY tests to prove that removing/changing any one directive turns stage 2 red; assert no intervening user input due D-04.</action>
  <verify>
    <automated>go test -race ./internal/tester/... ./internal/tuikit/... ./cmd/gitid/... -run 'Test.*(ResolvedVia|Stage2|ExactCommand|ExpectedConfig)' -count=1 &amp;&amp; go test -tags e2e -race -timeout 180s ./e2e/... -run 'TestCreateFlow_.*(Resolved|Stage2|Algorithm|Reuse)' -count=1</automated>
  </verify>
  <done>Stage 2 proves—not bypasses—the staged alias configuration, displays both commands and real evidence, and rejects every incomplete or mismatched resolved field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form → SSH renderer | Untrusted text becomes OpenSSH configuration syntax. |
| config files → collision decision | Hand-written and included Host patterns determine whether a new alias is safe. |
| filesystem key pair → reuse flow | User-selected private/public files are security identity material. |
| staged config → SSH process | Stage 2 must prove effective configuration without overriding it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08-01 | Tampering | Host-block interpolation | critical | mitigate | Strict core validation immediately before checked rendering plus table/fuzz-style invalid-input tests. |
| T-03-08-02 | Spoofing | alias collision | high | mitigate | Effective OpenSSH pattern matching across main/includes; every read/parse failure blocks. |
| T-03-08-03 | Spoofing | reused key pair | high | mitigate | Canonical public fingerprints must match, including non-interactive encrypted-private fingerprint extraction. |
| T-03-08-04 | Spoofing | stage-2 proof | high | mitigate | Unpinned connectivity and complete expected-field comparison over successful `ssh -G`. |
| T-03-08-05 | Denial of Service | empty/all-disabled catalog | medium | mitigate | Explicit availability state and bounded navigation with no modulo/infinite loop. |
</threat_model>

<verification>
Run all task commands, then `make test`, `make lint`, `make test-e2e`, and `make gate-copy-freeze`. Expected output: exit 0; table tests reject every invalid token/range/pattern case; encrypted private/public fingerprint tests report equality without prompting; PTY output shows four fields only, selected algorithm-specific path, automatic stage 2, both commands, and complete resolution. Verify `go list -deps ./internal/tuikit/...` contains no first-party backend package and `git diff -- recipes/` is empty.
</verification>

<success_criteria>
- CR-03, CR-04, CR-05, CR-06, CR-08, CR-12, and WR-03 are closed by focused and real-entry tests.
- The form is exactly SSHUI-01's four fields and retains SSHUI-02 mouse/keyboard reachability.
- Live preview and persisted output use checked recipe-faithful rendering and the same selected catalog entry.
- Existing-key reuse honors D-10 through D-13 without a passphrase prompt or pre-confirm chmod/write.
- TEST-01/TEST-02 show and validate the full stage-2 proof rather than merely displaying one IdentityFile line.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-08-SUMMARY.md` when done.
</output>
