---
phase: 03-create-flow-backend
plan: 12
type: tdd
wave: 11
depends_on: [03-11]
files_modified:
  - internal/tuikit/frame.go
  - internal/tuikit/frame_test.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - e2e/create_flow_pty_e2e_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_packet.go
  - internal/screenshot/createflow_packet_test.go
  - cmd/gitid-evidence/main.go
  - cmd/gitid-evidence/main_test.go
  - .planning/phases/03-create-flow-backend/03-12-review-packet/
autonomous: true
gap_closure: true
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, SSHUI-04, SSHUI-05, TEST-01, TEST-02, TEST-03, KEY-06, DLV-04, DLV-06]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "At 100x30, a developer can inspect the complete recipe-shaped Host preview and every byte of both test commands and outputs through bounded, visibly scrollable panes; no security-relevant token is replaced by an ellipsis. (SSHUI-03, TEST-01, TEST-02; D-01 through D-04, D-20 through D-22)"
    - "Stage 1 and stage 2 are captured from distinct genuine PTY states, while ReachableNotUploaded and hard failure each have their own raw-PTY and visual evidence with glyph, word, and semantic treatment. (DLV-04, DLV-06; D-02 through D-04, D-22)"
    - "The real Git step keeps Continue disabled without promising an unavailable action, while Skip Git remains functional and the approved dummy copy remains frozen. (D-18, D-19)"
    - "Every final packet screen has one truthful logical identity, route, interaction script, state signature, and distinct capture; reuse-manual references the approved key-reuse route and git-form references the approved Git form rather than unrelated create-flow states. (D-10 through D-13, D-18, D-24, D-25)"
    - "The final packet declares and hashes all panels, text transcripts, EVIDENCE.json, REGION-DIFFS.json, REVIEW-PROVENANCE.json, prompts, and raw review outputs; duplicate named screens, duplicate cross-screen images, missing records, and undeclared review artifacts fail publication. (DLV-04; D-24, D-25)"
    - "A fresh independent UI review of the final immutable packet reports zero open Critical/High findings; any blocking finding requires a new source commit, packet, and review rather than editing prior evidence. (D-24, D-25; L14)"
  artifacts:
    - path: "internal/tuikit/frame.go"
      provides: "Bounded scrollable exact-text viewport primitive with visible position and continuation cues"
    - path: "internal/tuikit/identities.go"
      provides: "Complete Host preview and two-stage proof flow at 100x30 plus truthful Phase-3 Git-step copy"
    - path: "e2e/create_flow_pty_e2e_test.go"
      provides: "Raw-keystroke acceptance tests for preview/proof scrolling, distinct stages, warning, failure, reuse-manual, and Git-step behavior"
    - path: "internal/screenshot/createflow_packet.go"
      provides: "Strict screen registry, packet schema, provenance inventory, and duplicate/undeclared-artifact rejection"
    - path: "cmd/gitid-evidence/main.go"
      provides: "Genuine PTY state capture, correct approved-route mapping, and atomic final packet publication"
    - path: ".planning/phases/03-create-flow-backend/03-12-review-packet/<source-full-sha>/"
      provides: "New immutable evidence and independent-review packet; all 03-09 and 03-11 packets remain unchanged"
  key_links:
    - from: "internal/tuikit/identities.go"
      to: "e2e/create_flow_pty_e2e_test.go"
      via: "raw PageUp/PageDown and horizontal-scroll keystrokes expose exact Host/command/output bytes at 100x30"
      pattern: "PreviewViewport|ProofViewport|PgUp|PgDn"
    - from: "cmd/gitid-evidence/main.go"
      to: "internal/screenshot/createflow_packet.go"
      via: "one typed ScreenSpec registry drives routes, PTY scripts, expected signatures, panel names, and manifest records"
      pattern: "ScreenSpec|ReferenceRoute|StateSignature"
    - from: ".planning/phases/03-create-flow-backend/03-12-review-packet/<source-full-sha>/MANIFEST.json"
      to: "REVIEW-PROVENANCE.json, UI-REVIEW.md, and CODEX-REVIEW.md"
      via: "manifest-declared prompt/output hashes, reviewer run identity, source SHA, approval SHA, and final severity validation"
      pattern: "manifest_sha256|prompt_sha256|output_sha256|source_commit|approval_commit"
---

<objective>
Correct the smallest remaining Phase 3 UI/evidence slice identified by the completed independent UI review: make the fixed-geometry preview and proof genuinely inspectable, capture distinct and complete semantic states, and publish a strict provenance-complete packet that a fresh independent reviewer can trust.

Purpose: Close every Critical/High item in `03-11-final-review-packet/.../UI-REVIEW.md` without changing real user configuration, external accounts, frozen invalid packets, or deferred Phase 4/9 behavior.
Output: TDD-backed 100x30 viewport/proof behavior, expanded raw-PTY state coverage, a fail-closed packet publisher, one new immutable 03-12 packet, and fresh independent UI/Codex review evidence.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/ONESHOT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/LEARNINGS.md
@.planning/REQUIREMENTS.md
@.planning/design/create-flow/FIELDS.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/phases/03-create-flow-backend/03-REVIEW.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-11-PLAN.md
@.planning/phases/03-create-flow-backend/03-11-SUMMARY.md
@.planning/phases/03-create-flow-backend/03-11-final-review-packet/e130012c3016e4a652f62fc71269fbf849495594/UI-REVIEW.md
@.planning/phases/03-create-flow-backend/03-11-final-review-packet/e130012c3016e4a652f62fc71269fbf849495594/MANIFEST.json
@internal/tuikit/frame.go
@internal/tuikit/identities.go
@internal/screenshot/createflow.go
@internal/screenshot/createflow_packet.go
@cmd/gitid-evidence/main.go
@e2e/create_flow_pty_e2e_test.go

<interfaces>
- `PreviewBlock` currently truncates every long line with `ansi.Truncate` and clips by line count. Add a stateful bounded viewport API rather than weakening the existing stable snapshot helper used by unrelated screens.
- `wizardModel` already owns step-specific state and `handleWizardKey`; add preview/proof offsets there and route only unclaimed test/preview navigation keys, preserving the D7 Shift-arrow precedence contract.
- `TestResultView` already carries connectivity `Command`/`Detail` and resolution `ResolutionCommand`/`ResolutionOutput`. Render these exact captured values; do not recompute or summarize proof.
- `CreateFlowScreenIDs` and the publisher's local `routes` map currently permit unrelated labels/routes. Replace parallel string tables with one typed screen registry consumed by capture, validation, and publication.
- Approved reference routes that actually exist are `/create-flow/reuse-key-vs-generate` and `/git-screen/git-form-filled`. `reuse-manual-path` is an interaction variant of the former, not `ssh-form-blank-prefix`; `git-form-demo` is a Phase-3 live state compared with the latter, not `create-flow/backup-notice`.
- The prior packet schema declares only 48 panel/text members. The corrected final schema must also declare evidence, region diff, review provenance, exact prompts, and raw review outputs before atomic publication.
</interfaces>
</context>

<reproduction_evidence>
Read-only reproduction at HEAD `e130012c3016e4a652f62fc71269fbf849495594` produced:

1. Stage duplication: approved-TUI stage 1 and stage 2 both hash to `3e08fb66a8969c892aad71a2f7fe0a6ca50b4476b17b0ee4a83afea4cb96f1ad`; live stage 1 and stage 2 both hash to `eeabd59cb6014515f114eeb97ff824c52d2b4b183168292f566f811658561a43`.
2. Missing packet records: `EVIDENCE.json`, `REGION-DIFFS.json`, and `REVIEW-PROVENANCE.json` each reported `exists=False declared=False`.
3. Fixed-frame clipping: every inspected text capture had 30 rows; `ssh-form-filled` omitted `IdentitiesOnly yes`, and both stage captures omitted visible exact `ssh -T`/`ssh -G` plus required effective fields; stage captures contained ellipses.
4. Missing semantic states: the PNG inventory contained only eight IDs and no `test-fail` or ReachableNotUploaded-specific screen.
5. Mislabeled routes: source maps `reuse-manual-path` to `create-flow/ssh-form-blank-prefix`, `mouse-focused-field` to `create-flow/ssh-form-empty`, and `git-form-demo` to `create-flow/backup-notice`.
6. No partial Codex output was recoverable from repository/debug/packet paths or review-named local output. A fresh run is mandatory; no executor-authored reconstruction counts.
</reproduction_evidence>

<execution_safety>
- Before RED work, record `git status --short`, HEAD, and SHA-256 for `.planning/phases/03-create-flow-backend/03-09-review-packet/EVIDENCE.json` and `panel-pngs/reuse-key-vs-generate.png`. Their reproduced hashes are `b0801041eb3ce30f1556289a630cf8f9e06f51f4d651891458e6e007f8950ea8` and `378eeaa4db94a3734bcf147b6f89477afc31593a5f84a0ccb958ada7c515f0a4`. Recheck after each task.
- Preserve `.planning/debug/`, both existing `03-11-*review-packet/` trees, and `.planning/phases/05.7-*` byte-for-byte. Never amend, remove, rename, stage, or commit them.
- Run every behavioral and capture command under a disposable HOME with the D-22 PATH-shim fake SSH. Do not read or write real `~/.ssh`, `~/.gitconfig*`, provider accounts, GitHub keys, or other external account state.
- Follow RED → GREEN → REFACTOR while honoring AGENTS.md commit policy: tests and implementation for each coherent buildable change land together in one logical commit; do not ship standalone RED commits and never use `--no-verify`.
- Execute Tasks 1, 2, and 3 sequentially. Do not run parallel executors in this Go module per L11.
</execution_safety>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end 100x30 exact preview and stage-proof tracer</name>
  <files>internal/tuikit/frame.go, internal/tuikit/frame_test.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - RED UI Critical/High: at a real 100x30 PTY, the default SSH form exposes the complete recipe Host block including `IdentitiesOnly yes`; bounded content that exceeds its pane has an explicit position/continuation cue and raw navigation reaches every exact byte.
    - RED TEST-01/02: a genuine stage-1 intermediate frame contains only the direct command/output state, while the genuine stage-2 frame adds alias connectivity plus the exact `ssh -G` command/raw proof; navigation exposes User, Hostname, Port, IdentitiesOnly, and first IdentityFile without ellipsis substitution.
    - RED semantic states: separate raw-PTY tests drive D-22 `pass`, `denied`, and `timeout` modes and save distinct stage-success, ReachableNotUploaded, and hard-failure frames; warning uses `!` plus the warning word/copy, failure uses `✗` plus retry copy, and neither can masquerade as the other per D-01 through D-04.
    - RED Git copy: when D-19 disables Continue in the real backend, the actionable Continue hint is absent; D-18 Skip remains present and functional, while the dummy backend retains its frozen valid-form hint.
  </behavior>
  <action>
    Begin with one failing real-binary raw-PTY tracer at exactly 100 columns by 30 rows: open the SSH form, assert the final recipe line is inspectable, enter test, hold/capture the genuine stage-1 state, allow stage 2 to complete, and navigate through every exact command/output line. Add focused unit failures for viewport bounds, offsets, cues, resize clamping, and byte-preserving horizontal slices before implementation.

    Add a reusable stateful exact-text viewport alongside `PreviewBlock`, with explicit vertical and horizontal offsets, fixed width/height, visible range/continuation indicators, and no mutation of source text. Do not apply `ansi.Truncate` to the underlying security/config content. Keep unrelated call sites on existing `PreviewBlock`. In the create wizard, allocate enough initial Host-preview rows to show the entire recipe block at 100x30; place long algorithm explanation detail in the bounded viewport rather than pushing the Host block below the frame. Preserve all five always-visible algorithm choices and D-20/D-21 field behavior.

    Render Task 03-11's captured `TestResultView` fields as a focusable proof flow: stage 1 direct command and raw connectivity output, then stage 2 connectivity command/output and resolution command/raw output. Use discoverable, non-conflicting controls such as PageUp/PageDown for vertical movement and bracket keys for horizontal movement, advertise them in the contextual footer, and preserve D7's Shift-arrow wizard navigation precedence. A viewport page may clip visually only with an explicit continuation/position cue; scrolling must reveal the original exact bytes.

    Capture stage 1 before the D-04 auto-chain completes by making the test-owned D-22 fake SSH barrier-controlled, not by pausing or changing production sequencing. Expand the raw-PTY matrix to warning and hard failure, including navigation through long proof. For D-18/D-19, suppress the real binary's contradictory actionable Continue hint whenever `GitStepDisabledReason` is nonempty; leave dummy copy and Phase 4 wiring untouched.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... -run 'Test.*(ExactTextViewport|HostPreview100x30|ProofViewport|DistinctStage|Reachable|Failure|GitDisabledHint)' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 180s ./e2e/... -run 'TestCreateFlow_.*(HostPreviewScrollable|DistinctStageCaptures|ExactStageProof|ReachableNotUploaded|FailureRetry|GitStepDisabledReason|ReuseManualPath)'</automated>
  </verify>
  <done>A real user at 100x30 can inspect the full Host preview and every exact proof byte, stage states are genuinely distinct, warning/failure are visibly and behaviorally covered, and the Phase-3 Git step no longer promises disabled behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strict truthful screen registry and provenance-complete publisher</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go</files>
  <behavior>
    - RED duplicate integrity: publication fails when two entries on one surface share a logical screen name, when different named screens share PNG bytes, or when captured text lacks that screen's unique state signature.
    - RED route truth: reuse-manual is declared as a manual-path interaction variant of `/create-flow/reuse-key-vs-generate`; mouse focus is an interaction variant of `/create-flow/ssh-form-filled`; git-form uses `/git-screen/git-form-filled`; no checkpoint is labeled with an unrelated approved route.
    - RED state inventory: the canonical registry includes distinct genuine stage-1 and stage-2 checkpoints plus warning and failure captures, and every required registry entry has live, approved-TUI, and approved-HTML evidence or an explicit source-artifact applicability record validated against D-02/D-24.
    - RED packet closure: `EVIDENCE.json`, `REGION-DIFFS.json`, `REVIEW-PROVENANCE.json`, exact reviewer prompts, and raw UI/Codex outputs are mandatory manifest members; an extra undeclared review file, missing required record, hash mismatch, stale source/approval SHA, or unlisted filesystem member fails validation.
  </behavior>
  <action>
    Replace `CreateFlowScreenIDs`, the publisher route map, and duplicated capture switches with one typed `ScreenSpec` registry carrying logical ID, user-visible state, source surface/route, interaction variant, PTY script, unique required state signature, required semantic regions, and packet applicability. Use the approved key-source route for reuse-manual with a declared manual-path action; use the approved filled Git form under the git-screen surface for git-form. Do not present blank-prefix, empty-form, or backup-notice screenshots under different names.

    Expand the registry and packet inventory to include independent stage-1, stage-2, ReachableNotUploaded, and hard-failure states from Task 1's genuine raw-PTY scripts. For live stage 1, use the D-22 barrier to capture after stage-1 output but before stage 2 answers. Approved TUI stage 1 must stop at its real manual stage-2 boundary. Every capture script must assert its unique breadcrumb/state signature before saving; remove the current shared `runStages` path that advances both IDs to the same terminal state.

    Make packet validation derive the expected inventory from the registry rather than a numeric constant. Reject duplicate `(surface, logicalID)` entries and duplicate PNG SHA-256 values across different logical IDs on the same surface. Validate route ownership against exported approval source and require each text transcript to contain its declared signature. Generate `EVIDENCE.json` with exact argv/keystrokes/routes/tools/source hashes, and `REGION-DIFFS.json` with full non-ellipsized normalized regions and every scoped D-02/D-19 divergence. Normalization may replace disposable absolute prefixes while retaining full argv and field values.

    Add a finalization mode that takes read-only candidate evidence plus two raw independent-review outputs and their exact prompts/provenance, validates all required review fields, builds the complete manifest in a sibling temporary directory, fsyncs, and atomically publishes to `03-12-review-packet/<full-source-sha>`. The manifest must declare every review artifact before publication; walking the destination must reject any undeclared file, including an extra review markdown file. Add the explicit publisher regressions requested by the UI review: duplicate named screen fails, duplicate cross-screen image fails, and undeclared review artifact fails.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test.*(ScreenRegistry|CorrectReferenceRoutes|DistinctStage|WarningFailureInventory|DuplicateNamedScreen|DuplicatePNG|StateSignature|RequiredProvenance|UndeclaredReviewArtifact|FinalPacket)' &amp;&amp; make gate-visual-regression</automated>
  </verify>
  <done>The publisher cannot mislabel routes, reuse a named screen or image, omit semantic states/provenance, or accept undeclared review files; one typed registry drives truthful capture and strict final validation.</done>
</task>

<task type="auto">
  <name>Task 3: Raw-PTY acceptance, immutable publication, and strict final independent UI review</name>
  <files>.planning/phases/03-create-flow-backend/03-12-review-packet/</files>
  <action>
    After Tasks 1-2 are green and committed, run the full raw-PTY acceptance matrix at 100x30 in disposable HOMEs. Save exact commands and real outputs showing: complete Host preview navigation; distinct stage-1 and stage-2 frames; all exact connectivity/resolution proof pages; warning and hard-failure paths; reuse-manual route/action; and truthful disabled Git form plus functional Skip. Run the packet candidate twice in separate processes and require byte-identical declared evidence before any reviewer sees it.

    Run a fresh clean-context independent UI reviewer against the complete candidate: all declared panels, raw PTY transcripts, EVIDENCE.json, REGION-DIFFS.json, recipes, FIELDS.md, 03-UI-SPEC.md, and the prior failed UI review checklist. Run a separate fresh Codex review over the identical manifest hash and inputs. Record exact unedited prompts/stdout/stderr plus executable/version/provider/model/session/timestamps/exit status and prompt/output hashes in REVIEW-PROVENANCE.json. The repository search found no recoverable partial Codex verdict, so no reconstructed or executor-authored substitute is permitted.

    Require the final independent UI review to explicitly pass: distinct genuine stage states; full exact proof accessibility at 100x30; complete Host preview; warning/failure coverage; route labels; Git-step copy; packet declarations; and duplicate/undeclared-artifact controls. Any Critical/High from either reviewer blocks finalization. Fix it test-first in Task 1 or 2, commit a new source SHA, regenerate a new candidate, and rerun both reviews. Once both latest reviews have zero open Critical/High and every Medium/Low has a concrete disposition, finalize and atomically publish one new 03-12 packet. Never edit prior 03-09/03-11 packets and do not update phase-completion state.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= make test &amp;&amp; make lint &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test.*(FinalPacket|ReviewProvenance|FreshIndependentReview|BlockingSeverity|UndeclaredReviewArtifact)'</automated>
    <human-check>Inspect only the independent final UI review's provenance and verdict: it must bind to the latest 03-12 manifest/source SHA and explicitly report zero open Critical/High findings across every prior UI-review item.</human-check>
  </verify>
  <done>A new immutable 03-12 packet passes raw-PTY, strict publisher, and independent-review gates for the same latest source SHA; prior dirty/debug/invalid evidence is unchanged, and no phase-completion claim is made.</done>
</task>

</tasks>

<dependency_graph>
| Task | Needs | Creates | Sequence reason |
|------|-------|---------|-----------------|
| 1 | Existing exact proof DTOs and failed UI review | Inspectable 100x30 preview/proof states plus raw-PTY scripts | Publisher evidence must depict corrected genuine behavior. |
| 2 | Task 1 state machine, controls, and semantic frames | Typed registry and strict provenance-complete finalizer | Route/state validation must consume the actual corrected interaction paths. |
| 3 | Tasks 1-2 committed and green | Latest immutable packet and fresh independent reviews | Review hashes and source SHA are valid only after implementation stabilizes. |
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Captured SSH output → viewport | Security-relevant commands and effective config must remain exact and fully inspectable despite fixed terminal geometry. |
| PTY state → named packet screen | Asynchronous stage transitions and interaction variants must not be mislabeled or duplicated. |
| Approval source route → comparison panel | A historical route may be used only under its truthful surface/state identity. |
| Candidate evidence → final packet | Every file, hash, provenance record, and review output must be declared before atomic publication. |
| Packet → independent reviewer | Reviewer identity, prompt, output, source, approval, and manifest must be bound against substitution or replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12-01 | Tampering | exact-text viewport | high | mitigate | Preserve source bytes, expose offset cues, and raw-PTY-test every proof segment at 100x30. |
| T-03-12-02 | Spoofing | stage capture | critical | mitigate | Barrier-controlled genuine PTY states, per-state signatures, and duplicate image rejection. |
| T-03-12-03 | Spoofing | approved route mapping | high | mitigate | Typed route ownership validated against exported approval source. |
| T-03-12-04 | Tampering | packet filesystem | critical | mitigate | Manifest-complete inventory, required provenance records, hash checks, undeclared-file rejection, and atomic publication. |
| T-03-12-05 | Repudiation | independent reviews | high | mitigate | Exact prompt/output hashes, executable/model/session provenance, fresh-source rule, and severity gate. |
| T-03-12-06 | Information Disclosure | PTY evidence | medium | mitigate | Disposable HOME, normalized temp prefixes, public-only fixtures, and no private-key bytes. |
| T-03-12-07 | Elevation of Privilege | real HOME/external accounts | critical | mitigate | D-22 fake SSH and sandbox HOME only; Phase 9 GitHub-key authorization is out of scope. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Exact two-stage create flow stored behind approved design | 1-3 | COVERED | Corrected inspectability, PTY evidence, and final review gate. |
| REQ | SSHUI-01/02/03 | Four reachable fields and exact live Host preview | 1,3 | COVERED | Full recipe block at 100x30 plus raw mouse/keyboard acceptance. |
| REQ | SSHUI-04/05 | Throwaway testing and recipe globals remain safe | 1,3 | COVERED | Sandbox-only matrix and regression gates; no storage behavior redesign. |
| REQ | TEST-01/02 | Two exact command/output stages and ssh-G proof | 1-3 | COVERED | Exact-text viewport, distinct states, transcripts, packet signatures. |
| REQ | TEST-03 | Confirmed backed-up persistence | 1,3 | COVERED | Existing ceremony retained and full suite rerun. |
| REQ | KEY-06 | Existing-key reuse including manual path | 1-3 | COVERED | Raw PTY plus truthful approved reference mapping. |
| REQ | DLV-04/06 | Approval diff and per-screen real-binary PTY | 1-3 | COVERED | Expanded semantic matrix, strict publisher, independent reviews. |
| RESEARCH | architecture map | Presentation owns viewport; test infrastructure owns PTY; screenshot/build tooling owns packet | 1-2 | COVERED | No backend tier reassignment or new dependency. |
| RESEARCH | D-22 pitfall | PATH-shim fake SSH, not in-Go test substitution | 1-3 | COVERED | Barrier is implemented in the external test fake. |
| RESEARCH | no new dependencies | Existing Bubble Tea/Lipgloss/PTY/VT/freeze/Chromium stack only | 1-2 | COVERED | No install task or package-legitimacy checkpoint. |
| CONTEXT | D-01..D-04 | Gate taxonomy, warning/copy, automatic chain | 1-3 | COVERED | Genuine stage barrier observes rather than changes auto-chain. |
| CONTEXT | D-05..D-09 | Layout, Include default, globals, collision | 1,3 | COVERED | Regression suite preserves behavior; no migration UX. |
| CONTEXT | D-10..D-13 | Reuse picker/manual path/warn/algorithm breadth | 1-3 | COVERED | Manual interaction and route provenance are explicit. |
| CONTEXT | D-14..D-17 | Real shell, preview banners, shared backend-free UI | 1-3 | COVERED | Changes remain in shared presentation and existing composition. |
| CONTEXT | D-18/D-19 | Functional Skip and truthfully disabled Git Continue | 1-3 | COVERED | Real hint suppressed; dummy and deferred Git writes untouched. |
| CONTEXT | D-20/D-21 | Provider inference and custom endpoint behavior | 1,3 | COVERED | Full Host preview and existing tests retained. |
| CONTEXT | D-22/D-23 | Deterministic fake SSH; network smoke separate | 1-3 | COVERED | Offline PTY only; no network smoke or external account use. |
| CONTEXT | D-24/D-25 | Automated diff plus UI/Codex review; severity blocks | 2-3 | COVERED | Complete immutable packet and fresh review convergence. |
| UI REVIEW | Critical/High | Duplicate stages, missing provenance, clipped preview/proof, warning/error omissions, copy contradiction, incomplete proof | 1-3 | COVERED | Every listed blocker has a RED behavior and final review criterion. |

Deferred layout migration, Git writes, upload automation/instructions, CLI rebuild, and `insteadOf` rendering remain excluded exactly as `03-CONTEXT.md` specifies.
</source_coverage_audit>

<verification>
1. Preserve and re-hash protected dirty 03-09 evidence, debug files, both 03-11 packet trees, and Phase 05.7 untracked content at every task boundary.
2. Record each RED command and its real behavioral failure, then GREEN output, in `03-12-SUMMARY.md`; keep test and implementation in logical buildable commits per AGENTS.md.
3. Run raw-PTY tests at exactly 100x30 for full Host preview, each proof viewport segment, distinct stages, warning, failure, reuse-manual, mouse, Git-disabled copy, Skip, and confirm.
4. Run publisher negative tests for duplicate name, duplicate image, wrong route/signature, missing provenance record, undeclared review artifact, and stale hashes.
5. Generate two independent candidates and compare every declared byte before review; final publication occurs only after fresh UI and Codex outputs validate against the same candidate.
6. Require the independent final UI review to enumerate and pass every prior Critical/High item; a generic PASS line is insufficient.
7. Run `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression`; all must exit zero.
8. Confirm no real HOME/config/key/account mutation and no change to any protected pre-existing packet/debug path.
</verification>

<success_criteria>
- Host preview and complete two-stage proof are inspectable without hidden or ellipsized security-relevant bytes at 100x30.
- Stage 1, stage 2, ReachableNotUploaded, and hard failure each have distinct genuine raw-PTY and packet evidence.
- Reuse-manual, mouse-focus, and git-form evidence identify truthful approved routes and interaction variants.
- Publisher validation fails on duplicate named screens, duplicate cross-screen images, missing provenance, or undeclared review artifacts.
- The latest immutable 03-12 packet has fresh independent UI and Codex reviews with zero open Critical/High and complete hash-bound provenance.
- Dirty 03-09 evidence, debug files, invalid 03-11 packets, real user files, external accounts, and GitHub keys remain untouched.
- The summary reports corrective-plan execution only; it does not claim Phase 3 or milestone completion.
</success_criteria>

<output>
Create `.planning/phases/03-create-flow-backend/03-12-SUMMARY.md` with RED/GREEN commands and real outputs, raw-PTY acceptance results, screen/route registry, candidate/final manifest hashes, provenance inventory, independent review findings/dispositions, and protected-path before/after hashes. Do not mark Phase 3 complete and do not rewrite STATE, ROADMAP, REQUIREMENTS, 03-REVIEW, or prior packets.
</output>
