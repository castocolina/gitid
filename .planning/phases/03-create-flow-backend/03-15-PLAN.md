---
phase: 03-create-flow-backend
plan: 15
type: tdd
wave: 14
depends_on: [03-14]
gap_closure: true
autonomous: false
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, SSHUI-04, SSHUI-05, TEST-01, TEST-02, TEST-03, KEY-06, DLV-04, DLV-06]
files_modified:
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_test.go
  - internal/screenshot/createflow_packet.go
  - internal/screenshot/createflow_packet_test.go
  - internal/tuikit/identities_test.go
  - cmd/gitid-evidence/main.go
  - cmd/gitid-evidence/main_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - e2e/create_flow_pty_e2e_test.go
  - .planning/phases/03-create-flow-backend/03-15-review-packet/<new-full-source-sha>/
  - .planning/phases/03-create-flow-backend/03-15-SUMMARY.md
estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The complete first resolved sandbox `identityfile <full-path>` line is visible inside the focused proof viewport before it can authorize `test-stage2-resolution-identities-key`; generic `identityfile` text elsewhere in the terminal cannot authorize evidence. (TEST-02, DLV-04; D-22, D-24, D-25)"
    - "Proof and confirmation markers are validated only inside the actual focused viewport or registry-declared semantic region, and a duplicate token outside that region is a passing negative control only when authorization still fails. (TEST-01, TEST-02, TEST-03, DLV-04; D-24, D-25)"
    - "Both in-process capture and real-PTY capture fail closed when any required live-applicable frame, focused viewport, declared region, or exact marker cannot be exposed; omission, an empty map entry, or a fallback frame never authorizes a candidate. (DLV-04, DLV-06; D-22, D-24, D-25)"
    - "Candidate evidence compares the real compiled TUI only with `cmd/gitid-dummy`; D-24's active interpretation is real TUI versus the live Bubble Tea mockup, while Phase-2 web artifacts are historical and prohibited as Phase-3 targets. (DLV-04; D-24, L16)"
    - "Candidate generation requires `freeze`, the vendored font, Go/build dependencies, and PTY dependencies; it requires no browser runtime or JavaScript package manager. (DLV-04; D-24)"
    - "Publication is impossible until the exact candidate receives one fresh independent review from the sole configured reviewer instance: (`opencode-my-plan-review`, tool/CLI `opencode`, model `local-llm-env/my-plan-review`). `my-plan-review` supplies provider and time-window fallback internally; those internal fallback attempts remain one configured review and never create a direct Claude, Grok, Kimi, or other reviewer gate. `ReviewInput.Provider` and stored provider metadata are required to be nonempty, hash/provenance-preserved, and internally consistent across supplied input, provenance, verdict, and review record where applicable. `FinalizeCandidate` and `ValidateReviewProvenance` fail closed on a substitute, triple mismatch, provider omission/inconsistency/tampering, duplicate, omission, or stale review, and the executor cannot run, author, or finalize the review. (D-24, D-25; L14)"
    - "All tests and captures use disposable HOME, PATH-shim fake SSH, public sandbox fixtures, and no real config, key, network, or account mutation. (SSHUI-04, DLV-06; D-22)"
  artifacts:
    - path: "internal/screenshot/createflow.go"
      provides: "Registry contracts and region-scoped exact-marker validation for every required frame"
    - path: "internal/screenshot/createflow_regions.go"
      provides: "Focused proof-viewport and confirmation-viewport extraction boundaries"
    - path: "cmd/gitid-evidence/main.go"
      provides: "Fail-closed TUI-only candidate capture and orchestrator-consumable finalization boundary"
    - path: "cmd/gitid/gate_visual_regression_test.go"
      provides: "Complete RequiredScreenSpecs real-TUI versus cmd/gitid-dummy gate"
    - path: ".planning/phases/03-create-flow-backend/03-15-review-packet/<new-full-source-sha>/"
      provides: "Immutable final packet created only by the orchestrator after the sole configured review passes"
  key_links:
    - from: "internal/screenshot/createflow.go"
      to: "internal/screenshot/createflow_regions.go"
      via: "Each ScreenSpec declares the sole semantic region whose extracted bytes may satisfy its exact markers"
      pattern: "RequiredScreenSpecs|ValidateCapturedState|ExtractRegion"
    - from: "cmd/gitid-evidence/main.go"
      to: "e2e/create_flow_pty_e2e_test.go"
      via: "Both drive the same advertised raw viewport controls and require the full resolved IdentityFile line"
      pattern: "focusAndNavigateViewport|navigateFocusedViewport|identityfile"
    - from: "cmd/gitid/gate_visual_regression_test.go"
      to: "internal/screenshot/createflow.go"
      via: "The routine gate consumes the complete fail-closed RequiredScreenSpecs inventory for real and dummy TUI surfaces"
      pattern: "RequiredScreenSpecs|CaptureCreateFlowScreens"
    - from: "03-15-review-packet/<sha>/REVIEW-PROVENANCE.json"
      to: "opencode-my-plan-review raw review record"
      via: "The fresh configured reviewer instance binds its zero-blocker verdict to the source SHA and candidate manifest hash while handling provider/time-window fallback internally"
      pattern: "candidate_manifest_sha256|source_commit|reviewer|open_critical|open_high"
---

<objective>
Close the remaining evidence-authorization defects on current HEAD, remove the obsolete Phase-3 web-capture dependency, and hand one exact TUI-only candidate to the orchestrator for one fresh independent review from the sole configured reviewer instance and review-gated publication.

Purpose: Ensure Phase 3 evidence can be authorized only by exact bytes inside the focused viewport or declared semantic region, never by a duplicate elsewhere in a rendered frame, while preserving the already-implemented algorithm, viewport, capture-registry, region, candidate, provenance, and finalization coverage.
Output: Focused-region marker validation, fail-closed in-process and PTY capture completeness, a `cmd/gitid-dummy`-only candidate pipeline, a fresh candidate handoff, one orchestrator-owned configured review, and—only after it passes—one immutable `03-15-review-packet/<source-sha>/` publication.
</objective>

<execution_context>
@AGENTS.md
@recipes/README.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/ONESHOT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/LEARNINGS.md
@docs/agent-instructions/product-ui.md
@docs/agent-instructions/workflow.md
@docs/agent-instructions/engineering.md
@docs/agent-instructions/configuration-stack.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/config.json
@.planning/REQUIREMENTS.md
@.planning/phases/03-create-flow-backend/03-CONTEXT.md
@.planning/phases/03-create-flow-backend/03-RESEARCH.md
@.planning/phases/03-create-flow-backend/03-PATTERNS.md
@.planning/phases/03-create-flow-backend/03-UI-SPEC.md
@.planning/phases/03-create-flow-backend/03-VALIDATION.md
@.planning/phases/03-create-flow-backend/03-14-PLAN.md
@.planning/phases/03-create-flow-backend/03-14-REVIEW.md

<current_head_interfaces_and_remaining_deltas>
- Existing coverage—not new RED work: `TestEvidenceFakeSSHAlgorithmAvailability` already exercises evidence fake `ssh -Q key`; `TestCreateFlow_AlgorithmAvailability` already covers the real PTY catalog; `TestAlgorithmAvailability` and `TestAllAlgorithmsUnavailable` already cover fail-closed selection.
- Existing coverage—not new RED work: `TestProofViewport`, `TestConfirmationViewport`, and `TestConfirmationViewportRoutesAdvertisedControls` already cover viewport routing; `TestCreateFlow_CompletedStageExactProofViewport` and `TestCreateFlow_ConfirmationExactViewport` already exercise the real 100x30 PTY.
- Existing coverage—not new RED work: `TestRequiredScreenSpecsCoverExactFrames`, `TestRegionNonComparable`, `TestRegionClassification`, `TestDummyOnlyFrameClassification`, and the `TestCaptureTUIScreen*` tests already cover registry, capture, and region scaffolding. `RequiredScreenSpecs`, candidate validation/finalization, review provenance, and region classification already exist.
- Remaining D-24/D-25 defect: the `test-stage2-resolution-identities-key` registry contract still accepts the generic `identityfile` token. It must require the complete first resolved sandbox line emitted by stage 2, including the normalized full path.
- Remaining D-24/D-25 defect: `ValidateCapturedState`, `captureViewportMarkers`, PTY `focusAndNavigateViewport`/`navigateFocusedViewport`, and current tests search whole rendered frames or snapshots. Authorization must instead inspect the actual focused viewport or the exact ScreenSpec-declared semantic region.
- Remaining completeness defect: `CaptureCreateFlowScreens` conditionally omits frames when marker navigation fails, and callers can receive a partial/default map. Both the in-process API and the real-PTY capture loop must return an error unless every required applicable frame and marker is present in its authorizing region.
- Remaining policy defect: candidate generation still invokes approved web capture and records `pnpm`/browser tooling. Per the active D-24 interpretation, AGENTS.md, ROADMAP, product policy, and L16, the only Phase-3 comparison is real compiled TUI versus `cmd/gitid-dummy`; Phase-2 web artifacts are historical and prohibited/out of scope as candidate, parity, classification, reviewer, or tooling targets.
- Current `finalize` and `FinalizeCandidate` are retained foundations, not greenfield work. The delta is to bind them to the sole reviewer instance currently selected by `.planning/config.json` and to prevent executor-owned review/finalization.
</current_head_interfaces_and_remaining_deltas>
</context>

<execution_safety>
- State the hypothesis and run a focused observable check before each RED delta. Preserve every pre-existing packet and unrelated dirty path byte-for-byte; never reset, clean, restore, or overwrite unrelated work.
- Use only newly created disposable HOME directories, public fixture keys, `GITID_STAGE_DIR` under the sandbox, and a PATH-prefixed fake `ssh`. Do not run network smoke, `gh`, `glab`, provider/account commands, or any product write against the real HOME.
- D-24 active interpretation: real compiled TUI versus `cmd/gitid-dummy` only. Phase-2 web artifacts are historical; browser automation, web capture, Chromium, and `pnpm` are prohibited/out of scope for this plan.
- Follow test-first RED → GREEN → REFACTOR only for the remaining deltas below. Do not recreate, rename, or claim first-time coverage for the existing tests listed in current-HEAD interfaces; extend them with the exact missing assertions and add narrowly named negative controls.
- Execute Tasks 1-3 sequentially per L11. Task 3 remains one explicitly split executor/orchestrator handoff; this plan is at its scope ceiling, so additional work requires a subsequent replan rather than another task here. Normal hooks remain enabled; never use `--no-verify`.
</execution_safety>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Authorize one complete IdentityFile line only inside the focused viewport</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_packet_test.go, internal/tuikit/identities_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <read_first>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_packet_test.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, e2e/create_flow_pty_e2e_test.go, .planning/phases/03-create-flow-backend/03-14-REVIEW.md</read_first>
  <behavior>
    - Existing baseline stays green: algorithm-availability, all-unavailable, proof viewport, confirmation viewport, advertised-control, and exact PTY tests named in current-HEAD interfaces continue to pass without being recreated.
    - New RED delta: `test-stage2-resolution-identities-key` rejects `identityfile`, rejects a clipped prefix, and accepts only the complete first resolved sandbox `identityfile <full-path>` line paired with `identitiesonly yes` inside the focused proof viewport per D-22/D-24/D-25.
    - New RED negative control: the complete token placed in shell chrome, another pane, transcript history, or any out-of-viewport line does not authorize the proof frame when the focused viewport lacks it.
    - New RED confirmation negative control: a complete key path or sentinel outside the focused confirmation viewport does not authorize its frame; the actual declared confirmation region must contain the required bytes.
    - Regression extension: the real PTY test accumulates proof across raw PgUp/PgDn and Left/Right navigation, records the first complete resolved IdentityFile line, and proves generic/out-of-viewport duplicates cannot satisfy the assertion.
  </behavior>
  <action>
    Preserve current viewport behavior and extend its evidence boundary, not its user-facing design. Add explicit focused proof-viewport and focused confirmation-viewport region extractors to `createflow_regions.go`, bounded by the rendered focus label and viewport rows rather than by a whole right pane. Give each exact `ScreenSpec` one authorizing region and marker contract. Evolve `ValidateCapturedState` so it extracts that region first and validates only those bytes; callers must not pass a whole frame as implicit authorization.

    For `test-stage2-resolution-identities-key`, resolve the expected marker from the captured stage-2 `ResolutionOutput` and require its first complete `identityfile <full-path>` line after sandbox-path normalization. The generic keyword remains useful only as a parser anchor and is never sufficient authorization per D-24/D-25. Extend existing `TestRequiredScreenSpecsCoverExactFrames`, `TestProofViewport`, `TestConfirmationViewport`, `TestCreateFlow_CompletedStageExactProofViewport`, and `TestCreateFlow_ConfirmationExactViewport` with the remaining exact-region assertions; add narrowly named region-negative tests rather than replacing established coverage.

    Keep `cmd/gitid-dummy` as the sole UI reference per active D-24/L16. Preserve TEST-01/02 source bytes, D-04 auto-chain, D-19, and existing production controls. Do not change persistence, Git writes, upload behavior, migration UX, or CLI scope.
  </action>
  <acceptance_criteria>
    - The registry and runtime validator require the complete first resolved normalized sandbox IdentityFile line and reject generic or clipped forms.
    - Marker validation is scoped to the actual focused proof or confirmation viewport/declared region.
    - A test places the same complete marker outside the focused viewport and proves it cannot authorize the frame.
    - All named current-HEAD tests remain and pass with precise regression extensions; no stale first-time coverage claim remains.
  </acceptance_criteria>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... -run 'Test(RequiredScreenSpecsCoverExactFrames|ValidateCapturedStateRejectsOutOfViewportDuplicate|ValidateCapturedStateRequiresCompleteResolvedIdentityFile|ConfirmationRegionRejectsOutOfViewportDuplicate)$' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... -run 'Test(AlgorithmAvailability|AllAlgorithmsUnavailable|ProofViewport|ConfirmationViewport|ConfirmationViewportRoutesAdvertisedControls)$' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 300s ./e2e/... -run 'TestCreateFlow_(AlgorithmAvailability|CompletedStageExactProofViewport|ConfirmationExactViewport)$'</automated>
  </verify>
  <done>The end-to-end real PTY exposes and authorizes the complete first resolved IdentityFile line only from the focused proof viewport, and both proof and confirmation reject identical out-of-viewport decoys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make both capture engines complete and fail closed against the TUI-only registry</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_test.go, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go, cmd/gitid/gate_visual_regression_test.go</files>
  <read_first>internal/screenshot/createflow.go, internal/screenshot/createflow_test.go, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go, cmd/gitid/gate_visual_regression_test.go, .planning/config.json</read_first>
  <behavior>
    - Existing baseline stays green: fake-SSH algorithm availability, RequiredScreenSpecs exact frames, capture frame tests, region non-comparability/classification, dummy-only classification, candidate validation, provenance, and finalization tests remain established coverage.
    - New RED in-process delta: a required live-applicable frame whose focused viewport/declared region cannot expose every exact marker causes `CaptureCreateFlowScreens` to return an error; no partial map, silent conditional omission, empty value, or fallback frame is returned as success.
    - New RED PTY delta: `captureTUIPanels`/`captureTUIScreen` fail before rendering or candidate generation when any required applicable frame, region, or complete marker cannot be exposed through advertised controls.
    - New RED completeness delta: the routine gate and candidate validator derive their full two-surface inventory from `RequiredScreenSpecs`; missing live or applicable `cmd/gitid-dummy` members and undeclared one-sided frames fail closed.
    - Policy delta: candidate capture, packet schema, tool provenance, and review inventory contain only live TUI and approved Bubble Tea TUI surfaces per active D-24; no historical web surface or prohibited tool is required or invoked.
  </behavior>
  <action>
    Change `CaptureCreateFlowScreens` to return `(map[string]string, error)` and centralize a completeness validator that iterates every `RequiredScreenSpecs` entry applicable to the requested TUI surface. Replace all conditional `if exact, ok` omissions with explicit errors naming screen ID, authorizing region, missing full marker, and exhausted control sequence. Update the routine gate and all callers to propagate errors before region comparison; remove default-map authorization paths.

    Apply the same completeness helper to real-PTY capture before PNG rendering. Validate each normalized frame against its extracted authorizing region, preserve raw PTY evidence, and reject missing/empty rendered text, transcript, region, or exact marker before writing candidate members. Extend the existing capture tests with one failure injection per engine and retain the current positive frame tests rather than rewriting them as greenfield work.

    Enforce D-24's active real-TUI versus `cmd/gitid-dummy` interpretation throughout the packet. Remove the historical web surface from required visual panels, candidate commands, tool versions, geometry, evidence routes, capture loops, region classification, and reviewer inventory. Do not invoke or require browser automation, Chromium, or `pnpm`; leave Phase-2 historical source artifacts untouched. Required candidate tooling is Go/build, PTY dependencies, `freeze`, and the vendored font. Keep one-sided real/dummy TUI states explicitly non-comparable with decision-linked `ux-improvement` or `defect` classifications.
  </action>
  <acceptance_criteria>
    - In-process capture has an error-returning complete-inventory API and fails on any absent frame, authorizing region, or exact marker.
    - Real-PTY capture fails before candidate member creation on the same completeness failures.
    - RequiredScreenSpecs is the sole inventory for routine gate, candidate panels, packet validation, and region evidence across exactly the real and dummy TUI surfaces.
    - Candidate generation requires `freeze`, vendored font, Go/build, and PTY support, and contains no prohibited historical-web target or tooling dependency.
    - Existing positive coverage remains; new tests specifically exercise missing-frame, empty-region, out-of-region marker, and partial-map failure modes.
  </acceptance_criteria>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... ./cmd/gitid/... -run 'Test(EvidenceFakeSSHAlgorithmAvailability|RequiredScreenSpecsCoverExactFrames|RegionNonComparable|RegionClassification|DummyOnlyFrameClassification|CaptureCreateFlowScreensFailsOnMissingRequiredFrame|CaptureCreateFlowScreensFailsOnMissingRegionMarker|CaptureTUIScreenExactProofFrames|CaptureTUIScreenConfirmationKeyPathFrame|CaptureTUIScreenConfirmationManagedBlockFrame|CandidateUsesOnlyTUISurfaces|CandidateToolPreflight)$' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make gate-visual-regression</automated>
  </verify>
  <done>Both capture engines either produce the complete region-authorized RequiredScreenSpecs TUI inventory or return an error, and the candidate pipeline has no historical-web target or tooling dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Prepare the exact candidate, then stop for orchestrator-owned reviews and finalization</name>
  <precondition>Before candidate generation, `freeze`, the repository-vendored font, the Go compiler/build toolchain, and PTY dependencies are locally available; before publication, one orchestrator-created review directory contains a fresh output for the exact candidate from (`opencode-my-plan-review`, tool/CLI `opencode`, model `local-llm-env/my-plan-review`) with zero open Critical/High. `my-plan-review` owns provider/time-window fallback internally. Each supplied and stored provider field must be nonempty and internally consistent with the review's provenance and verdict/record, but provider is not part of the configuration-grounded allowlist and its value cannot substitute for a reviewer/CLI/model mismatch.</precondition>
  <files>cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, .planning/phases/03-create-flow-backend/03-15-SUMMARY.md, .planning/phases/03-create-flow-backend/03-15-review-packet/&lt;new-full-source-sha&gt;/</files>
  <read_first>.planning/config.json, cmd/gitid-evidence/main.go, cmd/gitid-evidence/main_test.go, internal/screenshot/createflow_packet.go, internal/screenshot/createflow_packet_test.go, docs/agent-instructions/workflow.md, .planning/phases/03-create-flow-backend/03-14-REVIEW.md</read_first>
  <behavior>
    - Positive publication contract: `FinalizeCandidate` and `ValidateReviewProvenance` accept exactly one configured reviewer/CLI/model triple: (`opencode-my-plan-review`, `opencode`, `local-llm-env/my-plan-review`), with reviewer/tool/model interpreted in that order and grounded in `.planning/config.json`. `ReviewInput.Provider` and stored provider metadata remain mandatory nonempty provenance fields whose exact supplied bytes and hashes are preserved through the verdict and review record. Provider/time-window fallback is internal to `my-plan-review` and does not add reviewer records or direct external reviewer gates.
    - Fail-closed contract: both functions reject any substitute reviewer; wrong CLI/tool or model; duplicate or missing configured reviewer; empty provider; provider disagreement or tampering between supplied input, provenance, verdict, and review record; stale source/candidate binding; verdict/review identity disagreement; or any other partial/mismatched configured triple. A different nonempty provider value does not authorize a substitute and is accepted only when consistently preserved for an otherwise exact configured triple.
    - Exact RED negative tests are `TestFinalizeCandidateRejectsSubstituteReviewer`, `TestFinalizeCandidateRejectsReviewerCLIMismatch`, `TestFinalizeCandidateRejectsEmptyReviewerProvider`, `TestFinalizeCandidateRejectsTamperedReviewerProviderProvenance`, `TestFinalizeCandidateRejectsReviewerModelMismatch`, `TestFinalizeCandidateRejectsDuplicateConfiguredReviewer`, `TestFinalizeCandidateRejectsMissingConfiguredReviewer`, `TestFinalizeCandidateRejectsStaleOrMismatchedConfiguredReview`, `TestValidateReviewProvenanceRejectsSubstituteReviewerTriple`, `TestValidateReviewProvenanceRejectsCLIOrModelMismatch`, `TestValidateReviewProvenanceRejectsEmptyProvider`, `TestValidateReviewProvenanceRejectsTamperedProviderProvenance`, and `TestValidateReviewProvenanceRejectsDuplicateMissingStaleOrMismatchedTriple`.
    - Candidate generation performs tool preflight and two process-isolated captures, compares canonical semantic inventories, validates the retained candidate, and never invokes review or finalization.
    - Finalization is impossible until the orchestrator has created the exact review directory; the finalizer revalidates the candidate, configured reviewer/CLI/model triple, nonempty internally consistent and hash-preserved provider metadata, raw assets, verdict bindings, immutable destination, and final packet before atomic publication.
  </behavior>
  <action>
    Before candidate handoff, implement one exact configured-reviewer triple allowlist and apply it independently at both publication boundaries: `FinalizeCandidate` validates the incoming `ReviewInput` before copying any review asset, and `ValidateReviewProvenance` validates the stored provenance record before accepting the final packet. The complete configuration-grounded allowed set is exactly (`opencode-my-plan-review`, tool `opencode`, model `local-llm-env/my-plan-review`). Do not invent a provider literal or treat provider as configuration-grounded. Require `ReviewInput.Provider` and every stored provider field to be nonempty, preserve its exact value and hash in provenance, and reject disagreement or tampering between input, provenance, verdict, and review record where applicable. Record that `my-plan-review` performs provider/time-window fallback internally; the finalizer consumes one resulting configured-instance review record, not one record per internal attempt. No direct Claude, Grok, Kimi, or substitute reviewer gate remains. Add the exact positive and negative tests named in behavior; table-driven subcases may share setup, but each named test must exist so the gate is directly selectable.

    Executor-owned work: run the focused suites and full local gates, commit the coherent source/test correction with hooks enabled, resolve the resulting clean full source SHA, and build the screenshot-tagged `gitid-evidence` binary into a new disposable temporary handoff root. Run the explicit freeze/vendored-font/Go/PTY preflight without any historical-web tooling, then invoke candidate mode with that source SHA and a retained candidate directory under the same handoff root. Candidate mode must itself create two process-isolated captures, compare their complete canonical semantic inventories, and validate every member/hash/region/exact marker before returning success. Record `SOURCE_SHA`, `EVIDENCE_BIN`, `CANDIDATE_DIR`, candidate manifest hash, and every command/result in `03-15-SUMMARY.md`. Stop there. The executor must not invoke the reviewer, create a review directory, fabricate review artifacts, interpret itself as an independent reviewer, call finalization, publish the packet, edit project status documents, or mark Phase 3 complete.

    Orchestrator-owned handoff: after the executor has stopped, invoke exactly the sole configured independent reviewer instance from `.planning/config.json`: `opencode-my-plan-review` using CLI `opencode` and model `local-llm-env/my-plan-review`. Do not invent or pin a provider argument from configuration; `my-plan-review` supplies provider/time-window fallback internally. Do not invoke direct Claude, Grok, Kimi, Codex, `--all`, executor-only review, another default reviewer, or any substitute gate. The review must be fresh, read-only, independently generated against the exact source SHA and candidate manifest hash, and scoped only to real compiled TUI versus `cmd/gitid-dummy`, with all PNG/rendered/raw TUI members, region evidence, recipes, active D-24 interpretation, and the 03-14/03-15 defects supplied. Preserve the exact prompt, raw stdout/stderr, configured reviewer/CLI/model triple, actual nonempty provider metadata emitted or supplied by the configured instance, internal fallback provenance exposed by that instance, session/version/timestamps/exit status, and machine-readable verdict in one orchestrator-created `OPENCODE_REVIEW_DIR`. Preserve provider values/hashes consistently through provenance and verdict/review records without treating a provider literal or internal fallback attempt as a separate reviewer.

    Orchestrator-owned finalization: if the exact configured reviewer is unavailable, substituted, duplicated, missing, stale, reviewer/CLI/model-triple-mismatched, has empty or internally inconsistent/tampered provider provenance, exits nonzero, or reports any Critical/High, publication remains impossible; route findings to a fresh test-first corrective plan and require one new review from the same configured instance on the replacement candidate. Provider/time-window variance arising from `my-plan-review`'s internal fallback is not a reviewer mismatch and never relaxes the configured triple. Only after the exact review directory exists and the configured review reports zero Critical/High may the orchestrator run the finalizer command in the orchestrator-only verify lane. `FinalizeCandidate` must reject a bad triple or provider provenance before staging publication, and its mandatory post-assembly `ValidatePacket` call must exercise `ValidateReviewProvenance` before the atomic rename publishes `.planning/phases/03-create-flow-backend/03-15-review-packet/<new-full-source-sha>/`. The executor never performs this handoff half.
  </action>
  <acceptance_criteria>
    - Executor stops with a clean committed source SHA, a complete deterministic unfinalized candidate, green local gates, and a summary that records the candidate hash and explicit orchestrator handoff.
    - No review record or final packet can be produced by the executor.
    - `FinalizeCandidate` and `ValidateReviewProvenance` allow only (`opencode-my-plan-review`, `opencode`, `local-llm-env/my-plan-review`); all three configuration-grounded fields are exact, while provider metadata is nonempty, hash-preserved, and internally consistent across input/provenance/verdict/review records, and the one review is fresh, independent, and bound to the candidate. Internal provider/time-window fallback does not create another reviewer requirement.
    - The thirteen exact negative tests named in behavior prove substitute reviewer, CLI/tool mismatch, model mismatch, empty provider, tampered/inconsistent provider provenance, duplicate, missing, stale, and triple/verdict/source/candidate mismatch cases fail closed at both finalization and stored-provenance validation; no test compares provider to a fixed configured literal.
    - Finalization also rejects nonzero exit, reused raw review output, or any open Critical/High.
    - The final packet is TUI-only, immutable, complete, and published only after the sole configured review reports zero blockers.
  </acceptance_criteria>
  <verify>
    <automated>EXECUTOR-ONLY CANDIDATE LANE — stop after recording the retained handoff paths: TERM=dumb SSH_AUTH_SOCK= make test &amp;&amp; make lint &amp;&amp; TERM=dumb SSH_AUTH_SOCK= make test-e2e &amp;&amp; make gate-copy-freeze &amp;&amp; make gate-visual-regression &amp;&amp; SOURCE_SHA="$(git rev-parse HEAD)" &amp;&amp; HANDOFF_ROOT="$(mktemp -d "${TMPDIR:-/tmp}/gitid-03-15-handoff.XXXXXX")" &amp;&amp; EVIDENCE_BIN="$HANDOFF_ROOT/gitid-evidence" &amp;&amp; CANDIDATE_DIR="$HANDOFF_ROOT/candidate" &amp;&amp; go build -tags screenshot -o "$EVIDENCE_BIN" ./cmd/gitid-evidence &amp;&amp; test -x "$EVIDENCE_BIN" &amp;&amp; command -v freeze &amp;&amp; test -s ".planning/design/fonts/JetBrainsMono-Regular.ttf" &amp;&amp; go version &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./cmd/gitid-evidence/... -run 'Test(CandidateToolPreflight|CaptureTUIScreenExactProofFrames)$' &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot/... ./cmd/gitid-evidence/... -run 'Test(FinalizeCandidateRejectsSubstituteReviewer|FinalizeCandidateRejectsReviewerCLIMismatch|FinalizeCandidateRejectsEmptyReviewerProvider|FinalizeCandidateRejectsTamperedReviewerProviderProvenance|FinalizeCandidateRejectsReviewerModelMismatch|FinalizeCandidateRejectsDuplicateConfiguredReviewer|FinalizeCandidateRejectsMissingConfiguredReviewer|FinalizeCandidateRejectsStaleOrMismatchedConfiguredReview|ValidateReviewProvenanceRejectsSubstituteReviewerTriple|ValidateReviewProvenanceRejectsCLIOrModelMismatch|ValidateReviewProvenanceRejectsEmptyProvider|ValidateReviewProvenanceRejectsTamperedProviderProvenance|ValidateReviewProvenanceRejectsDuplicateMissingStaleOrMismatchedTriple|CandidateUsesOnlyTUISurfaces|CandidateManifestIsReviewableButNotFinal)$' &amp;&amp; "$EVIDENCE_BIN" --candidate --source-commit "$SOURCE_SHA" --output-dir "$CANDIDATE_DIR" &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./cmd/gitid-evidence/... -run 'Test(CompareInventoriesRequiresSemanticEqualityButIgnoresPNGBytes|CompareInventoriesRejectsInvalidPNG)$' &amp;&amp; test -s "$CANDIDATE_DIR/CANDIDATE-MANIFEST.json"</automated>
    <orchestrator-only>ORCHESTRATOR-ONLY REVIEW/FINALIZATION LANE — do not run until the executor has stopped and the orchestrator has created the exact review directory: test -n "$SOURCE_SHA" &amp;&amp; test -x "$EVIDENCE_BIN" &amp;&amp; test -d "$CANDIDATE_DIR" &amp;&amp; test -d "$OPENCODE_REVIEW_DIR" &amp;&amp; "$EVIDENCE_BIN" finalize --source-commit "$SOURCE_SHA" --candidate-dir "$CANDIDATE_DIR" --review-dir "$OPENCODE_REVIEW_DIR" --output-root ".planning/phases/03-create-flow-backend/03-15-review-packet" &amp;&amp; test -s ".planning/phases/03-create-flow-backend/03-15-review-packet/$SOURCE_SHA/MANIFEST.json" &amp;&amp; test -s ".planning/phases/03-create-flow-backend/03-15-review-packet/$SOURCE_SHA/REVIEW-PROVENANCE.json". A successful `finalize` is itself the concrete final-packet validation because it calls `ValidatePacket`, which calls `ValidateReviewProvenance`, before atomic publication.</orchestrator-only>
    <human-check>Orchestrator verifies the preserved record contains exactly the allowlisted reviewer/CLI/model triple (`opencode-my-plan-review`, `opencode`, `local-llm-env/my-plan-review`), binds to the source SHA and candidate manifest hash, carries nonempty provider metadata preserved consistently with matching hashes through provenance and verdict/review records, documents any provider/time-window fallback as internal to `my-plan-review`, and reports zero open Critical/High before running the orchestrator-only finalization lane. No provider literal or internal fallback attempt is treated as a separately configured reviewer.</human-check>
  </verify>
  <done>Executor candidate preparation stops at the recorded unfinalized handoff; the orchestrator either keeps publication blocked with an exact reviewer/CLI/model-triple, provider-provenance, binding, or verdict reason or finalizes one validated immutable TUI-only packet after the sole non-substitutable configured reviewer instance passes with nonempty internally consistent provider metadata.</done>
</task>

</tasks>

<dependency_graph>
| Task | Needs | Creates | Sequence reason |
|------|-------|---------|-----------------|
| 1 | Existing current-HEAD viewport, registry, and PTY coverage | Exact focused-region authorization and out-of-viewport negative controls | Capture completeness must use the corrected authorization boundary. |
| 2 | Task 1 authorizing regions and complete marker contract | Fail-closed in-process/PTY TUI-only capture and gate | Candidate generation must not consume partial or whole-frame-authorized evidence. |
| 3 executor | Tasks 1-2 green and committed; required local TUI tooling | Exact clean-source candidate and summary handoff | Reviews are meaningful only for a stable complete candidate. |
| 3 orchestrator | Executor candidate plus exact configured reviewer availability | One fresh bound zero-blocker review and immutable final packet | Review execution and publication are deliberately outside executor authority. |
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Whole terminal frame → focused viewport/declared semantic region | Duplicate text outside the inspected region must not authorize evidence. |
| Fake SSH resolution output → exact IdentityFile marker | The first complete resolved sandbox path must survive display and normalization without degrading to a generic keyword. |
| RequiredScreenSpecs → in-process and PTY capture maps | Missing frames, regions, or markers must become errors rather than omissions/default values. |
| Real compiled TUI → cmd/gitid-dummy | The Bubble Tea mockup is the sole comparison authority under active D-24/L16. |
| Candidate manifest → configured reviewer → final packet | The exact reviewer/CLI/model triple, nonempty hash-preserved and internally consistent provider metadata, freshness, raw output, verdict, source SHA, and candidate hash must be non-circular and non-substitutable; provider/time-window fallback remains internal to the configured instance. |
| Capture process → local machine | Disposable HOME and fake SSH isolate user config, keys, network, and accounts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15-01 | Spoofing | `ValidateCapturedState` | critical | mitigate | Extract the ScreenSpec-declared focused region first and add out-of-viewport duplicate negative controls. |
| T-03-15-02 | Tampering | IdentityFile marker contract | high | mitigate | Require the complete first resolved normalized sandbox line, not a generic token or clipped prefix. |
| T-03-15-03 | Tampering | in-process/PTY capture inventory | critical | mitigate | Return errors for every missing frame/region/marker and reject partial maps before rendering or candidate creation. |
| T-03-15-04 | Repudiation | independent review | critical | mitigate | Require one fresh raw record from the sole configured non-substitutable reviewer/CLI/model triple bound to one candidate hash; require nonempty provider metadata, preserve internal provider/time-window fallback provenance, and reject provider hash/value disagreement across input, provenance, verdict, and review records without inventing an allowlisted provider literal. |
| T-03-15-05 | Information Disclosure | capture artifacts | high | mitigate | Use disposable HOME, public fixtures, path normalization, fake SSH, and no network/account actions. |
| T-03-15-06 | Elevation of Privilege | executor publication boundary | critical | mitigate | Executor stops at candidate handoff; only the orchestrator may invoke configured reviews and finalization. |
| T-03-15-07 | Denial of Service | capture navigation/tooling | medium | mitigate | Bound viewport navigation and fail before candidate generation when required TUI tooling is absent. |
| T-03-15-SC | Tampering | package installs | high | mitigate | No package-manager install task exists; use only repository-existing dependencies and required local TUI tooling. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Algorithm → SSH → exact two-stage test → confirmed store behind approved design | 1-3 | COVERED | Existing flow retained; evidence authorization and publication corrected. |
| REQ | SSHUI-01/02/03 | Four-field SSH form, mouse/keyboard focus, exact preview | 1-3 | COVERED | Existing coverage acknowledged and retained through complete TUI capture. |
| REQ | SSHUI-04/05 | Throwaway testing and macOS globals | 1-3 | COVERED | Disposable HOME/fake SSH; existing transaction behavior remains regression-covered. |
| REQ | TEST-01/02 | Exact commands/output and full ssh -G proof | 1-3 | COVERED | Complete IdentityFile line inside focused viewport is mandatory. |
| REQ | TEST-03 | Confirmed persistence with backup | 1-3 | COVERED | Confirmation region exactness plus existing hermetic PTY persistence coverage. |
| REQ | KEY-06 | Reuse existing key | 1-3 | COVERED | Existing selected/manual capture remains in complete registry. |
| REQ | DLV-04/06 | TUI visual gate and real-binary PTY | 1-3 | COVERED | Sole reference is `cmd/gitid-dummy`; both engines fail closed. |
| RESEARCH | responsibility map | Presentation owns viewport, PTY owns real interaction, screenshot tooling owns evidence | 1-3 | COVERED | Changes remain in current tiers. |
| RESEARCH | D-22 constraint | PATH-shim fake SSH, no in-Go e2e substitution | 1-3 | COVERED | Required for every test/capture. |
| RESEARCH | package audit | No new dependency/install | 1-3 | COVERED | Existing Go/TUI tooling only. |
| CONTEXT | D-01..D-04 | Store semantics, warning/copy, auto-chain, retry | 1-3 | COVERED | Existing behavior retained and fully captured. |
| CONTEXT | D-05..D-09 | Auto-detected target, Include default, globals, collision | 1-3 | COVERED | Existing confirmation/persistence tests retained; no migration UX. |
| CONTEXT | D-10..D-13 | Reuse picker/manual/in-use/algorithm breadth | 1-3 | COVERED | Existing registry and capture states remain complete. |
| CONTEXT | D-14..D-17 | Real shell, demo warning, backend-free shared UI | 1-3 | COVERED | Real and dummy TUI remain the compared surfaces. |
| CONTEXT | D-18/D-19 | Functional Skip and truthful disabled Continue | 1-3 | COVERED | Existing state remains registry-required; Git writes remain deferred. |
| CONTEXT | D-20/D-21 | Provider inference and endpoint defaults | 1-3 | COVERED | Existing form behavior remains regression-covered. |
| CONTEXT | D-22/D-23 | Deterministic fake SSH; optional network smoke | 1-3 | COVERED | Fake-only gates; network smoke prohibited here. |
| CONTEXT | D-24/D-25 | Automated TUI comparison, independent review, hard severity gate | 1-3 | COVERED | Active D-24 is real TUI versus `cmd/gitid-dummy`; the sole configured reviewer/CLI/model triple plus nonempty internally consistent provider provenance gates publication, with provider/time-window fallback internal to `my-plan-review`. |
| REVIEW | Directive 1 | Replace stale greenfield test instructions | 1-2 | COVERED | Existing tests enumerated as baseline; only deltas are RED. |
| REVIEW | Directive 2 | Complete resolved IdentityFile line | 1 | COVERED | Generic keyword cannot authorize. |
| REVIEW | Directive 3 | Focused viewport/region plus negative control | 1 | COVERED | Same token outside viewport must fail authorization. |
| REVIEW | Directive 4 | Both capture engines fail closed | 2 | COVERED | No silent omission/empty/default map. |
| REVIEW | Directive 5/7 | Tool/review preconditions and orchestrator handoff | 3 | COVERED | The exact non-substitutable configured reviewer/CLI/model triple is named; provider remains required provenance, not invented configuration. |
| REVIEW | Directive 8 | Post-publication planning-doc reconciliation | Handoff | COVERED | Orchestrator-only subsequent action below; not executor files. |
| REVIEW | Directive 9 | Dummy-only reference and disposable safety | 1-3 | COVERED | Enforced throughout. |

Deferred layout migration, Git writes, upload automation, CLI rebuilding, and `insteadOf` rendering remain excluded exactly as 03-CONTEXT.md requires.
</source_coverage_audit>

<artifacts_this_phase_produces>
- Focused-region exact-marker validation and regression tests for proof and confirmation.
- Error-returning complete in-process and PTY capture APIs driven by `RequiredScreenSpecs`.
- TUI-only candidate evidence with real TUI and `cmd/gitid-dummy` members, exact regions, raw PTY records, PNGs, and canonical hashes.
- One fresh raw review record from exactly the configured (`opencode-my-plan-review`, `opencode`, `local-llm-env/my-plan-review`) triple, with nonempty internally consistent provider metadata and provider/time-window fallback handled internally by `my-plan-review`, created by the orchestrator only.
- One immutable `03-15-review-packet/<new-full-source-sha>/` after the configured review passes.
- `03-15-SUMMARY.md` recording executor evidence, candidate handoff, orchestrator review provenance, finalization result, and safety checks.
</artifacts_this_phase_produces>

<post_publication_reconciliation_handoff>
After evidence publication, the orchestrator must open a separate planning-document reconciliation action for ROADMAP/REQUIREMENTS/CONTEXT/STATE conflicts, including D-24's stale wording and Phase-3 plan counts/statuses. The active interpretation to record is: real compiled TUI versus `cmd/gitid-dummy` only; Phase-2 web artifacts are history, not a target. `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/phases/03-create-flow-backend/03-CONTEXT.md`, and `.planning/STATE.md` are deliberately absent from `files_modified`; this plan's executor is not authorized to edit them. Reconciliation occurs only after the packet is published and is owned by the orchestrator as a subsequent planning-doc action.
</post_publication_reconciliation_handoff>

<verification>
1. Confirm every existing current-HEAD test named in the interfaces section still exists and runs; document only the new assertions/tests as RED deltas.
2. Prove the exact first resolved `identityfile <full-path>` line appears inside the focused proof viewport and that generic, clipped, and out-of-viewport duplicates fail authorization.
3. Prove confirmation key path and managed block markers authorize only from the focused confirmation region.
4. Inject missing-frame, empty-region, and marker-unreachable failures into both in-process and PTY capture; both must return errors before candidate output.
5. Verify the complete real/dummy TUI symmetric inventory comes solely from `RequiredScreenSpecs` and every one-sided/different region has a decision-linked classification.
6. Verify candidate preflight checks `freeze`, vendored font, Go/build, and PTY dependencies, with prohibited historical-web tooling absent from the execution path and provenance.
7. Run `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and `make gate-visual-regression` with disposable HOME/fake SSH boundaries.
8. Executor commits corrected source/tests, generates two process-isolated candidates from the exact clean source SHA, validates semantic equality, records the handoff, and stops without reviews or finalization.
9. Orchestrator obtains one fresh raw review of the exact candidate from configured triple (`opencode-my-plan-review`, `opencode`, `local-llm-env/my-plan-review`). `my-plan-review` supplies provider/time-window fallback internally; no direct Claude, Grok, Kimi, or substitute reviewer gate is invoked. An unavailable configured instance, triple substitution/mismatch, empty or inconsistent/tampered provider provenance, nonzero exit, or Critical/High blocks publication and starts a corrective replan; provider/time-window variance internal to the configured instance neither blocks the exact triple nor authorizes a substitute.
10. Only after the exact configured review reports zero Critical/High does the orchestrator finalize and atomically publish the immutable packet.
11. Confirm no prior packet, unrelated work, real HOME/config/key, network, or external account changed.
12. After publication, route the separate orchestrator-owned reconciliation handoff; do not perform it in this plan.
</verification>

<success_criteria>
- Existing fake-SSH, algorithm, viewport, capture, region, candidate, provenance, and finalization coverage remains intact and is no longer described as unimplemented.
- Generic `identityfile` cannot authorize evidence; the complete first resolved sandbox line must be visible in the actual focused proof viewport.
- Proof/confirmation duplicate tokens outside the authorizing region fail negative controls.
- Both in-process and PTY capture fail closed on every required live-applicable frame/marker and cannot return a successful partial inventory.
- The candidate and routine gate compare only the real compiled TUI with `cmd/gitid-dummy`; every difference is classified as `ux-improvement` or `defect`.
- Candidate tooling is limited to `freeze`, vendored font, Go/build, and PTY dependencies.
- Executor cannot self-review or publish; exactly configured triple (`opencode-my-plan-review`, `opencode`, `local-llm-env/my-plan-review`) reviews the exact candidate, with no substitute or direct Claude/Grok/Kimi gate, carrying nonempty internally consistent provider metadata and any provider/time-window fallback provenance supplied internally by `my-plan-review`, and it must report zero Critical/High before finalization.
- The immutable packet and summary are complete, while real user files, keys, network, accounts, prior artifacts, and deferred features remain untouched.
- ROADMAP/REQUIREMENTS/CONTEXT/STATE reconciliation is explicitly handed off after publication and is not authorized in this plan's files.
</success_criteria>

<output>
Executor creates `.planning/phases/03-create-flow-backend/03-15-SUMMARY.md` first as a candidate-handoff record containing current-HEAD baseline tests, focused RED/GREEN deltas, exact commands/results, complete first IdentityFile line evidence, out-of-viewport negative controls, capture completeness failures, tool preflight, clean source SHA, candidate manifest hash, and safety inventory. The orchestrator later appends only the exact configured reviewer/CLI/model-triple provenance record—including the actual nonempty, hash-preserved, internally consistent provider value and any provider/time-window fallback provenance supplied internally by `my-plan-review` without claiming it is separately configured—and the finalization result, then publishes `.planning/phases/03-create-flow-backend/03-15-review-packet/<new-full-source-sha>/` if the review passes. Do not mark Phase 3 complete; emit the separate post-publication planning-document reconciliation handoff.
</output>
