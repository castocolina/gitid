---
phase: 03-create-flow-backend
plan: 15
type: tdd
wave: 14
depends_on: [03-14]
gap_closure: true
autonomous: true
requirements: [SSHUI-01, SSHUI-02, SSHUI-03, SSHUI-04, TEST-01, TEST-02, TEST-03, KEY-06, DLV-04, DLV-06]
files_modified:
  - cmd/gitid-evidence/main.go
  - cmd/gitid-evidence/main_test.go
  - e2e/create_flow_pty_e2e_test.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_test.go
  - internal/screenshot/createflow_packet.go
  - internal/screenshot/createflow_packet_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - .planning/phases/03-create-flow-backend/03-15-SUMMARY.md
---

<objective>
Correct the four High findings from the authenticated Codex review of rejected candidate
`65c4cb2`, then produce a fresh candidate from a new clean source commit for two independent
reviews. Never modify the rejected candidate, real user configuration, keys, accounts, or network
state.

The real compiled TUI is compared with `cmd/gitid-dummy`; HTML is not a parity target. Every
visible or interaction difference must be classified as a UX improvement or a defect. Pixel and
byte parity are not requirements.
</objective>

<findings>
1. The capture fake `ssh` must implement `ssh -Q key`, so the real catalog retains available
   ed25519/rsa entries. Form navigation and validation must fail safely when no algorithm is
   available rather than accepting or looping on a disabled default.
2. Raw-PTY tests and capture scripts must navigate the proof viewport until all exact stage-one,
   stage-two, and `ssh -G` User, Hostname, Port, IdentitiesOnly, and first IdentityFile bytes are
   individually observed. Captured frame names must state which required bytes they expose.
3. Raw-PTY tests and capture scripts must navigate both axes of the confirmation viewport until
   the complete summary key path and complete BEGIN-to-END managed block are separately observed.
4. Region evidence for every live-only or approved-inapplicable frame must be explicitly marked
   non-comparable with its ScreenSpec decision and a concrete UX-improvement or defect
   classification. It must never claim equality solely because an approved comparison is absent.
</findings>

<tasks>
<task type="tracer" tdd="true">
<name>Restore truthful algorithm availability and prove exact real-PTY viewport navigation</name>
<action>
First add failing tests. Extend the evidence PATH-shim fake SSH with a deterministic `-Q key`
response matching the supported catalog, and test that exact script through the screenshot build
tag rather than only the unrelated e2e harness. Make the wizard reject an unavailable selected algorithm
and make algorithm navigation bounded when every entry is unavailable. Add 100x30 raw-PTY tests
that use the advertised focus, vertical, and horizontal controls, and assert each required exact
proof field and the complete confirmation summary key path in the viewport itself, not elsewhere
on the page.

Keep the source TUI text byte-preserving. The viewport is a UX improvement over the frozen dummy;
the new controls and frames are required evidence of that improvement, not a demand for historical
or pixel parity.
</action>
<verify>
Use this exact test mapping: `TestEvidenceFakeSSHAlgorithmAvailability` runs with `-tags screenshot ./cmd/gitid-evidence/...`; `TestAlgorithmAvailability`, `TestAllAlgorithmsUnavailable`, `TestProofViewport`, and `TestConfirmationViewport` run against `./internal/tuikit/...`; and `TestCreateFlow_AlgorithmAvailability`, `TestCreateFlow_CompletedStageExactProofViewport`, and `TestCreateFlow_ConfirmationExactViewport` run with `-tags e2e ./e2e/...`. For each mapping, run `go test` with the stated tag and package, `-list '^TestName$'`, and assert `rg -x '^TestName$' | wc -l` equals exactly `1`; then use the identical tag/package arguments with anchored `-run '^TestName$'`.
</verify>
</task>

<task type="auto" tdd="true">
<name>Make capture frames and region evidence independently truthful</name>
<action>
Add failing capture and packet-validator tests. Replace fixed key-count scrolling with
marker-driven navigation that refuses to save a frame until its declared exact bytes are visible.
Make `RequiredScreenSpecs()` the sole completeness oracle for registry tests, evidence capture,
packet inventory, and region diffs; the legacy eight-entry `CreateFlowScreenIDs` cannot authorize
or omit a required live frame. Update the typed `ScreenSpecRegistry` and its tests with exact
frame IDs and state markers: `test-stage1-command-output`, `test-stage2-command-output`,
`test-stage2-resolution-user-host-port`, `test-stage2-resolution-identities-key`,
`confirm-summary-key-path`, and `confirm-managed-block`. Capture the stated exact bytes in each
frame. A generic focus marker, `identityfile`, a clipped column-range cue, or Down-arrow-only
navigation cannot authorize them. Each proof/confirmation capture must use the advertised raw `v`,
PgUp/PgDn, and Left/Right controls until its marker is visible.

The exact visible marker mapping is: `test-stage1-command-output`=`Stage 1 output:`,
`test-stage2-command-output`=`Stage 2 output:`,
`test-stage2-resolution-user-host-port`=`user git` plus `hostname ssh.github.com` plus `port 443`,
`test-stage2-resolution-identities-key`=`identitiesonly yes` plus `identityfile`,
`confirm-summary-key-path`=the complete expected sandbox key path, and
`confirm-managed-block`=both `# BEGIN gitid managed:` and `# END gitid managed:`. Add negative
tests proving generic, clipped, and another frame's marker are rejected.

Add required failing then passing tests named `TestRequiredScreenSpecsCoverExactFrames`,
`TestCaptureTUIScreenExactProofFrames`, `TestCaptureTUIScreenConfirmationKeyPathFrame`,
`TestCaptureTUIScreenConfirmationManagedBlockFrame`,
`TestCreateFlow_CompletedStageExactProofViewport`, and
`TestCreateFlow_ConfirmationExactViewport`. The managed-block test must navigate the real viewport
and observe both sentinels and every managed Host line. The verification must require every exact
test name, so a zero-test match fails the plan.

Extend the region-diff schema and validator so approved-inapplicable regions have an explicit
non-applicability reason, a classification, and a decision-linked justification. They must not be
reported equal. Reject absent classifications, empty required live regions, unexplained unequal
applicable regions, and any candidate whose region document contradicts its ScreenSpec. Validate
the symmetric union of real and dummy `RequiredScreenSpecs` frames: a state available on only one
surface must be retained as an explicit non-comparable region with its ScreenSpec decision and a
`ux-improvement` or `defect` classification. No dummy-only or live-only state may be silently
omitted or treated equal.
</action>
<verify>
Use this exact test mapping: `TestRequiredScreenSpecsCoverExactFrames`, `TestRegionNonComparable`, `TestRegionClassification`, and `TestDummyOnlyFrameClassification` run with `-tags screenshot ./internal/screenshot/...`; `TestCaptureTUIScreenExactProofFrames`, `TestCaptureTUIScreenConfirmationKeyPathFrame`, and `TestCaptureTUIScreenConfirmationManagedBlockFrame` run with `-tags screenshot ./cmd/gitid-evidence/...`; and `TestCreateFlow_CompletedStageExactProofViewport` and `TestCreateFlow_ConfirmationExactViewport` run with `-tags e2e ./e2e/...`. For each mapping, run `go test` with the stated tag and package, `-list '^TestName$'`, and assert `rg -x '^TestName$' | wc -l` equals exactly `1`; then use the identical tag/package arguments with anchored `-run '^TestName$'`. Then run `make gate-visual-regression`.
</verify>
</task>

<task type="auto">
<name>Regenerate evidence and converge through independent reviews</name>
<action>
After the two implementation tasks are committed and every affected gate passes, generate a new
two-process candidate from the resulting clean full source SHA in a new temporary directory. Do
not reuse or alter candidate `65c4cb2`. Verify every manifest member and hash, all state markers,
and the final region classifications.

Update the routine visual gate so its scope is the complete `RequiredScreenSpecs()` inventory and
it rejects every live-versus-`cmd/gitid-dummy` difference without an explicit `ux-improvement` or
`defect` classification. Remove its byte-identity requirement. HTML may remain captured for
approval provenance but is never a parity, classification, or reviewer target.

Run fresh read-only independent OpenCode UI/UX and authenticated Codex reviews against that exact
candidate. Both reviews must use the real TUI versus `cmd/gitid-dummy` only. Persist unedited
prompts and raw stdout/stderr, authenticate/provenance metadata, and machine-readable verdicts.
If either reports Critical or High, return to a new test-first corrective plan and repeat. Only
when both report zero Critical/High may `gitid-evidence finalize` atomically publish the new
source-SHA packet.
</action>
<verify>
TERM=dumb SSH_AUTH_SOCK= make test && make lint && TERM=dumb SSH_AUTH_SOCK= make test-e2e && make gate-copy-freeze && make gate-visual-regression
</verify>
</task>
</tasks>

<safety>
All test and capture commands use a disposable HOME and the fake SSH executable. Do not invoke
network smoke targets, `gh`, `glab`, or any product write against real user paths. Preserve all
existing packet directories byte-for-byte and never use `--no-verify`.
</safety>

<verification>
1. Record `git status --short`, full HEAD, and protected-packet SHA-256 inventories before work;
   prove existing packet artifacts remain unchanged after every task.
2. Record each focused RED command and its actual failure before implementation, then its GREEN
   result. Run all exact named raw-PTY and capture tests at 100x30.
3. Confirm `RequiredScreenSpecs()` contains every required exact proof and confirmation frame and
   the candidate contains PNG, rendered text, raw transcript, nonempty regions, and an explicit
   classification for every real-versus-dummy difference.
4. Run `make test`, `make lint`, `make test-e2e`, `make gate-copy-freeze`, and the revised
   classification-based `make gate-visual-regression` after implementation and again before
   finalization.
5. Generate two process-isolated candidates from the new clean source SHA, compare their complete
   semantic inventories, then require fresh OpenCode and authenticated Codex reviews of that same
   candidate hash with zero Critical/High before finalization.
</verification>

<success_criteria>
- The evidence fake SSH truthfully reports supported algorithms; an unavailable algorithm cannot
  be selected or cause unbounded navigation.
- Raw PTY capture proves all exact stage-proof fields and the horizontally revealed confirmation
  summary key path through advertised controls at 100x30.
- RequiredScreenSpecs is the sole complete frame inventory and every live frame has fully declared
  packet members and meaningful classified region evidence.
- The real TUI is assessed against cmd/gitid-dummy without HTML, pixel, or byte-parity targets;
  every difference is classified as a UX improvement or defect.
- A new immutable packet is finalized only after two fresh independent zero-Critical/High reviews;
  no real user files, keys, accounts, network targets, or prior packets are changed.
</success_criteria>
