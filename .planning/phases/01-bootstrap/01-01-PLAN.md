---
phase: 01-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - cmd/gitid/main.go
  - cmd/gitid/main_test.go
  - internal/filewriter/doc.go
  - internal/filewriter/filewriter_stub_test.go
  - internal/sshconfig/doc.go
  - internal/sshconfig/sshconfig_stub_test.go
  - internal/gitconfig/doc.go
  - internal/gitconfig/gitconfig_stub_test.go
  - internal/identity/doc.go
  - internal/identity/identity_stub_test.go
  - internal/doctor/doc.go
  - internal/doctor/doctor_stub_test.go
  - internal/keygen/doc.go
  - internal/keygen/keygen_stub_test.go
  - internal/tester/doc.go
  - internal/tester/tester_stub_test.go
  - internal/clipboard/doc.go
  - internal/clipboard/clipboard_stub_test.go
  - internal/deps/doc.go
  - internal/deps/deps_stub_test.go
  - internal/platform/doc.go
  - internal/platform/platform_stub_test.go
  - tui/doc.go
  - tui/tui_stub_test.go
  - LICENSE
  - README.md
  - .gitignore
autonomous: true
requirements: [TOOL-04]
must_haves:
  truths:
    - "go build ./... compiles the full module with zero errors (D-09)"
    - "go test ./... discovers and passes at least one stub test per package (D-09, TOOL-04)"
    - "The module path is github.com/castocolina/gitid (D-01) and go.mod declares go 1.26 (D-02)"
    - "cmd/gitid produces a runnable gitid binary that exits 0 (D-09)"
  artifacts:
    - path: "go.mod"
      provides: "Module declaration with pinned Go floor"
      contains: "module github.com/castocolina/gitid"
    - path: "cmd/gitid/main.go"
      provides: "Buildable entrypoint package main"
      min_lines: 5
    - path: "internal/filewriter/doc.go"
      provides: "Compilable placeholder for filewriter package"
    - path: "internal/sshconfig/doc.go"
      provides: "Compilable placeholder for sshconfig package"
    - path: "internal/gitconfig/doc.go"
      provides: "Compilable placeholder for gitconfig package"
    - path: "internal/identity/doc.go"
      provides: "Compilable placeholder for identity package"
    - path: "internal/doctor/doc.go"
      provides: "Compilable placeholder for doctor package"
    - path: "internal/keygen/doc.go"
      provides: "Compilable placeholder for keygen package"
    - path: "internal/tester/doc.go"
      provides: "Compilable placeholder for tester package"
    - path: "internal/clipboard/doc.go"
      provides: "Compilable placeholder for clipboard package"
    - path: "internal/deps/doc.go"
      provides: "Compilable placeholder for deps package"
    - path: "internal/platform/doc.go"
      provides: "Compilable placeholder for platform package"
    - path: "tui/doc.go"
      provides: "Compilable placeholder for tui package"
  key_links:
    - from: "cmd/gitid/main.go"
      to: "go.mod module path"
      via: "package main under module github.com/castocolina/gitid"
      pattern: "package main"
---

<objective>
Scaffold the complete gitid Go module: initialize `go.mod` with the locked module
path and Go floor, create every package directory from the architecture layout as a
compilable placeholder with one passing stub test, a trivial buildable `cmd/gitid`
entrypoint, and minimal LICENSE/README/.gitignore.

Purpose: Establish the test substrate so the next plan's `make test`/`make lint`
targets have real packages to run against. Without this skeleton there is nothing
for the toolchain to verify. No domain logic is written here (D-09): every internal
package is an empty placeholder that compiles and reports one green stub test.

Output: A module where `go build ./...` and `go test ./...` both exit 0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (01-01) produces the module + skeleton portion of Phase 1:
- `go.mod` — `module github.com/castocolina/gitid` (D-01), `go 1.26` directive (D-02). No dependencies added in this plan (libraries arrive in Phase 2 when real code lands).
- `cmd/gitid/main.go` — trivial `package main` with a `main()` that prints the binary name/version line and exits 0. `cmd/gitid/main_test.go` — one stub test.
- `internal/<pkg>/doc.go` + `<pkg>_stub_test.go` for each of: `filewriter`, `sshconfig`, `gitconfig`, `identity`, `doctor`, `keygen`, `tester`, `clipboard`, `deps`, `platform` (D-09).
- `tui/doc.go` + `tui/tui_stub_test.go` — placeholder peer package (D-09).
- `LICENSE` (minimal, planner discretion), `README.md` (minimal stub), `.gitignore` (ignore `bin/`, `coverage.out`).

Later plans add: `Makefile` + `.golangci.yml` (01-02), `.pre-commit-config.yaml` + hook wiring (01-03).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Initialize module and buildable entrypoint</name>
  <files>go.mod, cmd/gitid/main.go, cmd/gitid/main_test.go, LICENSE, README.md, .gitignore</files>
  <read_first>
    - .planning/phases/01-bootstrap/01-CONTEXT.md (D-01, D-02, Claude's-Discretion on LICENSE/README)
    - .planning/research/STACK.md (module init command, Go version rationale)
    - .planning/research/ARCHITECTURE.md (cmd/gitid layout, one-directional dependency rule)
    - CLAUDE.md (English-only artifacts; Technology Stack version table)
  </read_first>
  <action>
    Run `go mod init github.com/castocolina/gitid` (D-01). Edit go.mod so the version
    directive reads `go 1.26` (D-02) — note the dev host runs Go 1.23.2, so Go's
    auto-toolchain will fetch 1.26 on first build; do NOT downgrade the directive to
    match the local install. Do not add a `toolchain` line and do not run `go get` for
    any library in this plan (dependencies belong to Phase 2). Create `cmd/gitid/main.go`
    as `package main` with a `main()` that writes a single line identifying the binary
    (name `gitid`) to stdout and returns normally (exit 0); no Cobra, no flags, no
    business logic. Add `cmd/gitid/main_test.go` with one test asserting the package
    builds and `main` is invocable without panicking (a trivial `TestMainRuns` that calls
    a small extracted `run() error` returning nil, or asserts a version constant is
    non-empty). Create a minimal `LICENSE` (planner discretion — a short permissive
    notice) and a minimal `README.md` stub naming the project and the `make setup-env`
    entry point. Create `.gitignore` ignoring `bin/` and `coverage.out`. All content in
    English.
  </action>
  <verify>
    <automated>cd /Users/ramon/git/personal/ssh-git-config && go build ./cmd/gitid && go vet ./cmd/gitid && grep -q '^module github.com/castocolina/gitid$' go.mod && grep -q '^go 1.26' go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^module github.com/castocolina/gitid$' go.mod` matches exactly one line.
    - `grep '^go 1.26' go.mod` matches (D-02 floor present).
    - `go build ./cmd/gitid` exits 0 and `go vet ./cmd/gitid` exits 0.
    - Running the built `gitid` binary exits 0 (`./gitid; echo $?` prints `0`).
    - `LICENSE`, `README.md`, and `.gitignore` exist and are non-empty; `.gitignore` contains the line `bin/` and the line `coverage.out`.
  </acceptance_criteria>
  <done>go.mod declares the locked module path and `go 1.26`; cmd/gitid builds and runs to a clean exit; LICENSE/README/.gitignore exist.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold every internal package and tui as compilable placeholders with stub tests</name>
  <files>internal/filewriter/doc.go, internal/filewriter/filewriter_stub_test.go, internal/sshconfig/doc.go, internal/sshconfig/sshconfig_stub_test.go, internal/gitconfig/doc.go, internal/gitconfig/gitconfig_stub_test.go, internal/identity/doc.go, internal/identity/identity_stub_test.go, internal/doctor/doc.go, internal/doctor/doctor_stub_test.go, internal/keygen/doc.go, internal/keygen/keygen_stub_test.go, internal/tester/doc.go, internal/tester/tester_stub_test.go, internal/clipboard/doc.go, internal/clipboard/clipboard_stub_test.go, internal/deps/doc.go, internal/deps/deps_stub_test.go, internal/platform/doc.go, internal/platform/platform_stub_test.go, tui/doc.go, tui/tui_stub_test.go</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (full internal/ package list + responsibilities + one-directional dependency rule; tui/ is a peer of cmd/, never imported by internal/)
    - .planning/phases/01-bootstrap/01-CONTEXT.md (D-09: full skeleton, doc.go + one passing stub test per package, no real logic)
    - CLAUDE.md (English-only; package comments in English)
  </read_first>
  <action>
    For each package directory listed in files, create a `doc.go` containing a one-line
    `package <name>` declaration preceded by an English package-doc comment stating the
    package's eventual responsibility (sourced from the ARCHITECTURE.md Component
    Responsibilities table) and a note that implementation lands in a later phase. Package
    names: `filewriter`, `sshconfig`, `gitconfig`, `identity`, `doctor`, `keygen`,
    `tester`, `clipboard`, `deps`, `platform`, and `tui` (the tui package lives at
    `tui/doc.go` with `package tui`). For each package add one `<pkg>_stub_test.go` in the
    same package with a single passing test named `TestStub` that asserts a trivial truth
    (e.g. `if false { t.Fatal(...) }` or asserts the package name constant) — its only job
    is to prove the harness runs green for that package (D-09). Do NOT add any cross-package
    imports, no `internal/` package may import `tui/` or `cmd/` (enforce the one-directional
    rule). No real domain logic, no TODO stubs that fail to compile.
  </action>
  <verify>
    <automated>cd /Users/ramon/git/personal/ssh-git-config && go build ./... && go test ./... 2>&1 | tee /tmp/gitid_t.txt && test "$(ls internal | wc -l | tr -d ' ')" = "10" && ! grep -rn 'gitid/tui' internal/ && ! grep -rn 'gitid/cmd' internal/</automated>
  </verify>
  <acceptance_criteria>
    - `ls internal/` lists exactly these 10 directories: `filewriter sshconfig gitconfig identity doctor keygen tester clipboard deps platform`.
    - Every package directory contains a `doc.go` and exactly one `*_stub_test.go`.
    - `go build ./...` exits 0 (whole module compiles).
    - `go test ./...` exits 0 and reports `ok` (not `no test files`) for each of the 11 packages plus cmd/gitid.
    - `grep -rn 'gitid/tui' internal/` and `grep -rn 'gitid/cmd' internal/` each return no matches (one-directional dependency rule, ARCHITECTURE.md).
  </acceptance_criteria>
  <done>All 10 internal packages, tui, and cmd/gitid compile and each reports one passing stub test under `go test ./...`; no internal package imports tui or cmd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev shell → go toolchain | `go mod init` / `go build` run locally; no network input is trusted into source |
| go.mod `go 1.26` directive → Go auto-toolchain fetch | The toolchain may download a Go release; integrity is enforced by Go's module/toolchain checksum machinery (GONOSUMCHECK off) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | go.mod / go.sum | mitigate | No third-party deps added in this plan; go.sum is empty/absent so there is no supply-chain surface yet. Library installs (with audit) are deferred to Phase 2. |
| T-01-02 | Tampering | Go auto-toolchain download for `go 1.26` | accept | Go verifies downloaded toolchains against the signed checksum database by default; no custom GOTOOLCHAIN override is introduced. Low risk on a developer machine. |
| T-01-03 | Tampering | npm/pip/cargo installs | accept | This plan performs NO package-manager installs (no npm/pip/cargo, no `go get`). Tool/installer threats are handled in plans 01-02 and 01-03 where they actually occur. No T-01-SC needed for this plan. |
</threat_model>

<verification>
- `go build ./...` exits 0.
- `go test ./...` exits 0 with one passing stub test per package.
- `go.mod` declares `module github.com/castocolina/gitid` and `go 1.26`.
- No `internal/` package imports `tui/` or `cmd/`.
</verification>

<success_criteria>
- The full architecture package layout exists and compiles (D-09).
- The TDD harness substrate is green: every package has at least one passing test (TOOL-04 substrate).
- Module identity matches locked decisions D-01 and D-02.
- cmd/gitid builds into a runnable binary.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap/01-01-SUMMARY.md` when done.
</output>
