---
phase: 05-identity-manager
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - internal/identity/delete.go
  - internal/identity/delete_test.go
  - internal/identity/loader.go
  - internal/identity/loader_test.go
  - internal/identity/scan.go
  - internal/identity/scan_test.go
  - internal/identity/deleteplan.go
  - internal/identity/deleteplan_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
autonomous: true
requirements: [MGR-06]
estimate:
  tokens: 104000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Deleting the LAST identity for a provider removes that provider's `provider-rewrite:<host>` block; deleting one of several identities for the same provider leaves the block in place (D-09)."
    - "The provider reference count counts hand-written Host stanzas targeting the same provider, not only gitid-managed identities, so an unmanaged alias keeps the rewrite alive (D-09)."
    - "Reference counting compares NORMALIZED provider keys, never raw hostnames: a hand-written `Host foo.github.com` with `Hostname ssh.github.com` on port 443 — the recipe's own canonical alt-SSH endpoint — counts as a reference to the `github.com` rewrite block (review R-05)."
    - "A Git-only delete NEVER removes a provider rewrite, because the identity's SSH alias survives and still needs it — the ref-count question only arises on the everything path."
    - "Delete-everything COPIES the key pair into the gitid archive directory (mode 0700, key copies 0600) with the copy-only primitive, removes the live key files LAST, and reports the copy path — so a failure at any earlier step leaves BOTH the archive copy and the live key intact (D-11, review R-03)."
    - "The per-identity fragment file is backed up before it is unlinked in BOTH scopes, because an orphaned fragment is the project's known doctor-loop bug class (D-11)."
    - "When the identity's key is referenced by one or more sibling identities, delete-everything auto-downgrades: SSH and Git artifacts are removed, the key is KEPT, and every sibling that still uses it is named (D-12)."
    - "The unmanaged-reference scan matches the alias on whole-token boundaries and never on a bare substring, so `work` does not match `work-archive` (D-13, mirroring the superstring-principal guard)."
    - "Every scan hit is labelled with the region it came from — the identity's OWN managed block (never reported), ANOTHER gitid-managed block, or unmanaged foreign text — so a reference inside a sibling's managed block is disclosed rather than silently dropped (review R-09)."
    - "No private key file is ever a scan source; the scanned set is exactly the SSH config, the gitconfig, the identity's fragment, and allowed_signers."
    - "The scan is advisory only: it returns hits and never blocks or alters the delete, and it always reports the fixed disclaimer that repo remotes cannot be scanned (D-13)."
    - "The delete plan is a pure data value describing what WILL happen; it performs no write, so the confirm screen and the CLI dry-run render the same facts from the same source."
    - "A plan target names a LOGICAL region (file plus managed-block name), not just a file path, so a test comparing the plan against the write cannot pass while the wrong block inside the right file was removed (review R-11)."
  artifacts:
    - path: "internal/identity/scan.go"
      provides: "ScanUnmanagedReferences + UnmanagedHit + ScanRegion + SplitScanRegions — the D-13 region-tagged token-boundary scanner"
    - path: "internal/identity/deleteplan.go"
      provides: "DeletePlan + DeleteTarget + PlanDelete — the pure, error-returning preview the confirm screen and the CLI dry-run both render"
    - path: "internal/identity/delete.go"
      provides: "normalized provider ref-counting, copy-to-archive-then-remove-last key removal, and shared-key downgrade inside Delete"
    - path: "internal/identity/loader.go"
      provides: "RewriteProviderKey + ProviderHostForSSHHostname + ProviderKeyForHost — one provider normalizer for the write path and the count path"
  key_links:
    - from: "internal/identity/deleteplan.go PlanDelete"
      to: "internal/identity/delete.go Delete"
      via: "shared deleteTargets helper, never two independent lists — the plan enumerates exactly the regions Delete then acts on"
      pattern: "PlanDelete|deleteTargets"
    - from: "internal/identity/delete.go"
      to: "gitconfig.RemoveProviderRewrite"
      via: "the D-09 ref-count predicate over NORMALIZED provider keys gating the call"
      pattern: "RemoveProviderRewrite|ProviderRefCount|ProviderKeyForHost"
---

<objective>
Complete the delete semantics the frozen two-option ceremony promises: reference-counted provider cleanup, recoverable key removal, shared-key auto-downgrade, and an advisory unmanaged-reference scan — all as pure or fully-injected domain logic.

Purpose: plan 05-01's tracer landed the Git-only half. The everything half is where the irreversible work lives, and where four separate CONTEXT decisions (D-09, D-11, D-12, D-13) all land on the SAME two approved screens. Getting each one wrong has a distinct, real cost: an orphaned insteadOf block silently breaks every https clone for a provider, an over-eager one breaks a surviving sibling identity, an unbacked key deletion is unrecoverable, and a substring-matching scan produces false warnings that train the user to ignore real ones.
Output: A pure `DeletePlan` preview plus the four behaviors behind it, each tested including its opposite-direction failure mode.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/phases/05-identity-manager/05-CONTEXT.md
@.planning/phases/05-identity-manager/05-RESEARCH.md
@.planning/phases/05-identity-manager/05-UI-SPEC.md
@.planning/phases/05-identity-manager/05-01-SUMMARY.md
@.planning/phases/05-identity-manager/05-02-SUMMARY.md

<authority>Every bare D-NN citation means `05-CONTEXT.md`. Where this plan resolves an ambiguity CONTEXT.md left open, the resolution is stated explicitly in the task action and must be carried into `05-04-SUMMARY.md`.</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reference-counted provider cleanup and recoverable key removal (D-09, D-11)</name>
  <files>internal/identity/delete.go, internal/identity/delete_test.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go</files>
  <read_first>
    - internal/identity/delete.go as landed by plan 05-01 (DeleteScope, DeleteDeps, DeleteResult, Delete)
    - internal/gitconfig/renderer.go (ProviderRewriteBlockName, WriteProviderRewrite, HasProviderRewrite, and RemoveProviderRewrite added by plan 05-02)
    - internal/keygen/archive.go (ArchivedPair, CopyKeyPairToArchive, MoveKeyPairToArchive) and internal/sshconfig/include.go (ArchiveDir)
    - internal/identity/loader.go lines 10-30 (hostnameToProvider) and lines 124-140 (rewriteLookupProvider — the normalizer this task promotes)
    - internal/gitconfig/renderer.go validProviderHostname (what a valid provider key must satisfy)
    - internal/identity/identity.go lines 24-60 (Account, including Provider and Alias)
    - recipes/README.md the "What the recipes establish" list (alias, `Hostname ssh.github.com`, Port 443, provider-keyed `insteadOf`)
    - internal/sshconfig — the parser entry point that enumerates ALL Host stanzas including hand-written ones, with their Hostname values
    - cmd/gitid/wiring.go lines 1570-1615 (accounts, keyOwners) and the buildDeleteDeps added by plan 05-01
    - .planning/phases/05-identity-manager/05-02-SUMMARY.md (the archive stamp convention)
  </read_first>
  <behavior>
    - Deleting the only identity for a provider removes that provider's rewrite block and names it among the removed targets.
    - Deleting one of two identities on the same provider leaves the rewrite block present and does NOT name it among the removed targets.
    - A hand-written, non-gitid Host stanza whose hostname resolves to the same provider keeps the reference count above zero, so the block survives.
    - A hand-written `Host foo.github.com` whose `Hostname` is `ssh.github.com` counts as a `github.com` reference, because that is the recipe's canonical alt-SSH endpoint for the provider.
    - A managed account whose reconstructed `Provider` is the short form (`github`, no dot) counts as a `github.com` reference, because the rewrite block is keyed on the FQDN form.
    - A Git-only delete never removes a provider rewrite regardless of the count.
    - Delete-everything copies the private and public key into the archive directory with the COPY primitive, removes the live key files as the LAST step of the transaction, reports both copy paths, and leaves the copies readable at the expected modes.
    - Delete-everything with a missing key file succeeds, reports empty copy paths, and does not error.
    - The fragment file is backed up before it is unlinked under both scopes, and the backup path is reported.
    - A failure at any step after the key copy and before the final live-key removal leaves BOTH the archive copies and the live key files in place.
    - The `identity.ErrScopeNotAvailable` refusal branch plan 05-01 installed for the everything scope is removed in this plan, and a test asserts the everything scope now returns a real result.
  </behavior>
  <action>
Extend `internal/identity/delete.go` with the two artifact-cleanup rules, and remove plan 05-01's `ErrScopeNotAvailable` branch for the everything scope.

**Provider key normalization first (review R-05).** Before any counting, give the package ONE way to turn a host into the key the rewrite block is stored under. `internal/gitconfig`'s block name is `provider-rewrite:<FQDN>` (`ProviderRewriteBlockName` over `validProviderHostname`), while the two reference sources speak different dialects: reconstructed accounts may carry the SHORT provider form (`github`) from `hostnameToProvider`, and a Host stanza's `Hostname` is typically the recipe's canonical alt-SSH endpoint (`ssh.github.com`, port 443) rather than the bare provider. Comparing either of those against `github.com` directly under-counts, and an under-count deletes a rewrite that other config still needs — the silent https-clone breaker both reviewers named. So:

  - Export the existing unexported normalizer: add `func RewriteProviderKey(provider, alias string) string` to `internal/identity`, implemented as the CURRENT body of `rewriteLookupProvider`, and reduce `rewriteLookupProvider` to a one-line call of it. Do not re-derive the logic; this is a promote-in-place so the write path and the count path can never diverge.
  - Add `func ProviderHostForSSHHostname(hostname string) string` mapping the canonical endpoints the project already knows to their FQDN provider key: `ssh.github.com` and `github.com` → `github.com`; `altssh.gitlab.com` and `gitlab.com` → `gitlab.com`. Build it from the SAME `hostnameToProvider` table plus a short-to-FQDN suffix rule so adding a provider stays a one-line change in one place. Return "" for an unknown hostname (honest unknown, matching D-13's existing convention).
  - Add `func ProviderKeyForHost(alias, hostname, providerMarker string) string` resolving one Host stanza to a provider key with this precedence: the `# gitid: provider=` marker when present, else `ProviderHostForSSHHostname(hostname)`, else `RewriteProviderKey("", alias)`'s alias-suffix fallback. Document the precedence.

Provider ref-count (D-09). Add `func ProviderRefCount(accounts []Account, providerKey, excludingName string) int` counting every account for which `RewriteProviderKey(acct.Provider, acct.Alias)` equals `providerKey`, skipping the account being deleted — note it compares NORMALIZED keys on both sides, never raw `Account.Provider`. Then extend the reference source beyond gitid-managed accounts: add a `DeleteDeps.ForeignProviderRefs func(providerKey string) (int, error)` seam that the composition root fills by walking every Host stanza in the parsed SSH config — INCLUDING hand-written ones outside any managed block — resolving each through `ProviderKeyForHost`, and counting the matches. D-09 requires this explicitly ("Ref-count must account for hand-written aliases targeting the same provider"). The removal fires only when the managed count and the foreign count are BOTH zero.

Add `DeleteDeps.RemoveProviderRewrite func(providerKey string) (backupPath string, err error)`, filled in the composition root from `gitconfig.RemoveProviderRewrite` against `b.gitconfigPath`. Call it only under `DeleteScopeEverything`. Record the resolution explicitly in a comment and in the summary: a Git-only delete leaves the identity's SSH alias in place, so the identity still counts as a user of the provider and the rewrite stays — the ref-count question only arises on the everything path.

Recoverable key removal (D-11), with COPY semantics (review R-03). Replace the current plain `RemoveKeyFiles` behavior for the everything scope with copy-then-remove-last, using plan 05-02's COPY primitive: add `DeleteDeps.CopyKeyPairToArchive func(privPath, pubPath string) (archivedPriv, archivedPub string, err error)` filled from `keygen.CopyKeyPairToArchive` against `sshconfig.ArchiveDir(sshDir)`. Do NOT use `MoveKeyPairToArchive` here — rotation needs move, deletion needs copy, and 05-02 ships both precisely so this plan does not have to overload one. `deps.RemoveKeyFiles` stays as the live-key removal seam and runs LAST. State the planner resolution in the doc comment and in the summary: D-11 asks for "the timestamped backup dir (700) with key copies (600)", and the archive directory plan 05-02 created is exactly that shape, already registered as doctor-reserved and already the target of Phase 8's pruning story — so delete-everything reuses it rather than inventing a second backup location. `DeleteResult.KeyBackup`/`PubBackup` now carry the archive copy paths.

Extend `DeleteResult` with `ProviderRewriteRemoved bool`, `ProviderRewriteBackup string`, and `ArchivedKeyPaths []string` (the paths this call created, so the caller's journal can undo them — review R-10).

Order the transaction so a partial failure is never destructive in a new way, and write the ordering rationale as a comment: copy the key pair into the archive FIRST, then remove the managed blocks, then unlink the fragment, then remove the provider rewrite, then remove the LIVE key files LAST. Any failure before that last step therefore leaves both a valid archive copy and a working live key — the property the plan must guarantee and a test must assert.

In `cmd/gitid/wiring.go`, fill the three new `DeleteDeps` fields non-nil and extend the reflection guard coverage. Add a real-constructor test over a hermetic fake home with two identities on one provider: delete one, assert the rewrite block survives; delete the second, assert it is gone.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/identity/... ./cmd/gitid/... -run 'Delete|ProviderRef'</automated>
  </verify>
  <acceptance_criteria>
    - A table-driven test pins `ProviderKeyForHost` for: an explicit `# gitid: provider=github.com` marker; `Hostname ssh.github.com` with alias `foo.github.com`; `Hostname ssh.github.com` with a dotless alias `mygh`; `Hostname altssh.gitlab.com`; and an unrecognized `Hostname git.example.internal` (asserting "" rather than a guess).
    - A test asserts `ProviderRefCount` counts an account whose reconstructed `Provider` is the SHORT form `github` as a reference to the `github.com` key.
    - A test with two identities on one provider deletes one and asserts `HasProviderRewrite` still reports true; a follow-up delete asserts it reports false.
    - A test seeds a hand-written Host `foo.github.com` with `Hostname ssh.github.com` and `Port 443`, deletes the only managed identity for that provider, and asserts the rewrite block SURVIVES — the exact recipe-canonical endpoint case review R-05 names.
    - A test performs a Git-only delete of the only identity for a provider and asserts the rewrite block survives and `ProviderRewriteRemoved` is false.
    - A test asserts both archived key copies exist with the expected modes and that the live key paths no longer exist after a successful delete-everything.
    - A test asserts the copy seam is invoked and the `RemoveKeyFiles` seam is the LAST seam invoked, using a call-order recorder.
    - A test deletes an identity whose key file is already absent and asserts a nil error with empty copy paths.
    - A failure-injection test forces an error at SSH block removal and asserts the live private key AND public key still exist, the archive copies are present, and `DeleteResult.ArchivedKeyPaths` names both copies.
    - A test asserts the everything scope no longer returns `identity.ErrScopeNotAvailable`.
  </acceptance_criteria>
  <reversibility rating="costly">Key removal is recoverable only through the archive copy; the copy convention is user-visible on disk and shared with rotation, so changing it later strands earlier copies. Chosen deliberately to avoid a second backup location.</reversibility>
  <done>Provider cleanup is reference-counted across managed and hand-written references, and key removal is recoverable from a doctor-reserved archive copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared-key auto-downgrade and the advisory unmanaged-reference scan (D-12, D-13)</name>
  <files>internal/identity/scan.go, internal/identity/scan_test.go, internal/identity/delete.go, internal/identity/delete_test.go</files>
  <read_first>
    - internal/gitconfig/reader.go lines 180-210 (RemoveAllowedSignersLine's exact-first-field match plus the `namespaces="git"` check — the superstring guard this scan mirrors)
    - internal/gitconfig/reader_test.go (the superstring-principal test cases 05-CONTEXT.md names as the analog for D-13)
    - cmd/gitid/wiring.go lines 1594-1607 (keyOwners — today it maps a key path to exactly ONE label)
    - internal/identity/state.go (the `key-used-*` states that already compute shared usage)
    - .planning/phases/05-identity-manager/05-UI-SPEC.md the "UI Considerations" table rows for shared-key ownership and scan hits
  </read_first>
  <behavior>
    - An identity whose key path is also referenced by one other identity downgrades an everything-delete: the key is kept and the sibling is named.
    - An identity whose key path is referenced by two or more other identities downgrades identically and names ALL of them, in stable order.
    - An identity whose key is referenced by nobody else deletes the key normally, with no downgrade note.
    - The scan finds the alias when it appears as a whole token in unmanaged SSH config text, in the gitconfig, in a fragment, and in allowed_signers.
    - The scan does NOT report a hit when the alias appears only as a prefix or suffix of a longer token.
    - The scan does NOT report hits inside the identity's OWN managed blocks — those are what the delete is removing.
    - The scan DOES report a hit inside ANOTHER gitid-managed identity's block, labelled as such, because a sibling identity referencing this alias is operationally significant and must not be silently dropped.
    - No private key file is among the scan sources; a test asserts the composition root's source list contains exactly the SSH config, the gitconfig, the identity's fragment, and allowed_signers.
    - The scan returns file, line number, region label, and the matched line verbatim for each hit, and always returns the fixed disclaimer sentence about unscannable repo remotes.
    - Zero hits yields an empty slice, never a placeholder entry.
  </behavior>
  <action>
Add `internal/identity/scan.go` with:

```
type ScanRegion string   // ScanRegionOwnManaged, ScanRegionOtherManaged, ScanRegionUnmanaged
type UnmanagedHit struct { File string; Line int; Region ScanRegion; Text string }
type ScanSource struct { File string; Bytes []byte; Region ScanRegion }
func ScanUnmanagedReferences(alias string, sources []ScanSource) []UnmanagedHit
```

Implement matching with whole-token boundaries. Split each line into fields and also test the common `git@<alias>:` and `<alias>:` shapes by trimming the surrounding punctuation set that OpenSSH and git URLs use; a bare `strings.Contains` is explicitly forbidden by D-13's discretion note and by 05-RESEARCH.md's Security Domain table. Add a test proving `work` does not match `work-archive`, `mywork`, or `work.example.com` when the alias is exactly `work`, and DOES match `git@work:org/repo`, a bare `work`, and `Host work`.

**Three regions, not two (review R-09).** D-13 says "unmanaged regions", but a reference inside a SIBLING identity's managed block is a real operational consequence of the delete and must be disclosed, not silently dropped. Split each file into region-tagged sources rather than pre-stripping to one blob:

  - `ScanRegionOwnManaged` — bytes inside the identity being deleted's own managed blocks. `ScanUnmanagedReferences` DROPS these; they are what the delete removes.
  - `ScanRegionOtherManaged` — bytes inside another gitid-managed block. Reported, tagged, and rendered with the block's name so the user sees which sibling still references the alias.
  - `ScanRegionUnmanaged` — foreign text outside every managed block. Reported and tagged.

Add `func SplitScanRegions(file string, content []byte, ownIdentity string) []ScanSource` building the three-way split from `filewriter.ListBlocks`, so the region rule lives in ONE place and both the composition root and the tests use it. Document that the composition root passes ONLY the four config artifacts (SSH config, gitconfig, the identity's fragment, allowed_signers) and never a private key file.

Return the fixed disclaimer as an exported constant `UnmanagedScanDisclaimer` so the copy-freeze gate can grep for it and the UI never re-words it: the sentence stating that repo remotes using the git-at-alias form cannot be scanned and will break after the delete. Store the exact wording from 05-UI-SPEC.md's Copywriting Contract. **This constant is the single definition of that string for the whole phase (review R-27):** plan 05-06 registers THIS constant in the copy-freeze gate and renders it by reference; it must not declare a second copy in `internal/tuikit`. Say so in the constant's doc comment.

Shared-key downgrade (D-12). Add `func SharedKeyOwners(accounts []Account, keyPath, excludingName string) []string` returning EVERY other identity whose `KeyPath` equals the target, sorted by name for stable output. Resolve the UI-SPEC's open zero-one-many question here: the substrate CAN return more than one sibling, because several identities may point at one key path, so the return type is a slice and the render joins it — the existing `keyOwners()` map in the composition root collapses to one label per key and MUST be replaced by this slice-returning function for the delete path. In `Delete`, when the slice is non-empty under `DeleteScopeEverything`, skip the archive-and-remove key step entirely and set a new `DeleteResult.KeyKeptFor []string` field naming the siblings.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/identity/... -run 'Scan|SharedKey|Downgrade'</automated>
  </verify>
  <acceptance_criteria>
    - A table-driven test asserts non-matches for a prefix, a suffix, and a dotted extension of the alias, and asserts matches for the bare token, a `Host` directive value, and a git-at-alias URL form.
    - A test drives `SplitScanRegions` over a file containing the identity's own block, a sibling's block, and foreign text, and asserts three sources with the three distinct region tags and non-overlapping byte spans covering the whole file.
    - A test asserts a hit inside a sibling's managed block IS returned with `Region == ScanRegionOtherManaged`, and a hit inside the identity's own block is NOT returned.
    - A test asserts the composition root's scan-source list contains exactly four entries and that none of their file paths is a private key path.
    - A test with three identities sharing one key path asserts `SharedKeyOwners` returns the two siblings in sorted order.
    - A test asserts an everything-delete with a non-empty sibling list leaves the key files on disk, skips the archive copy entirely, and populates `KeyKeptFor`.
    - A test asserts an everything-delete with an empty sibling list removes the key files and leaves `KeyKeptFor` empty.
    - A test asserts scanning sources that contain only the identity's own managed blocks returns zero hits.
    - A test asserts the returned hits carry the correct 1-based line numbers and the verbatim matched line.
  </acceptance_criteria>
  <done>Shared keys are provably kept with every sibling named, and the scan warns on real whole-token references without false positives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DeletePlan — one pure preview both the confirm screen and the CLI dry-run render</name>
  <files>internal/identity/deleteplan.go, internal/identity/deleteplan_test.go, internal/identity/delete.go</files>
  <read_first>
    - internal/identity/delete.go as landed by Tasks 1 and 2 of this plan
    - internal/identity/scan.go from Task 2
    - .planning/design/identity-manager/FIELDS.md the `confirm-destructive` and `backup-notice` sections
    - .planning/phases/05-identity-manager/05-UI-SPEC.md the Spacing exceptions table (which additions are conditional and which fold into an existing list)
    - internal/tuikit/views.go (the DTO conventions the composition root converts into)
  </read_first>
  <behavior>
    - The plan for an everything-delete of a sole-provider identity lists the SSH config, the gitconfig, the fragment, the key pair, and the provider rewrite as targets.
    - The plan for the same identity with a surviving provider sibling omits the provider rewrite from the target list entirely.
    - The plan for a Git-only delete lists the gitconfig and the fragment, and does not list the SSH config, the key pair, or the allowed_signers file.
    - The plan carries the shared-key sibling list, the scan hits, and the disclaimer as separate fields so the renderer decides layout, not the domain.
    - Building a plan performs no write: a test asserts every seeded file is byte-identical after the call.
    - The plan's target list and the targets `Delete` actually acts on are derived from ONE shared helper, so they cannot drift.
  </behavior>
  <action>
Add `internal/identity/deleteplan.go`.

**Model logical targets, not bare file paths (review R-11).** A provider rewrite, an allowed_signers block, and an SSH Host block are REGIONS inside shared files, not distinct files, so a test that compares "the set of files actually modified" against a list of target strings can pass while the wrong block inside the right file was removed. Define:

```
type DeleteTarget struct {
    File  string      // the physical file the region lives in
    Block string      // the managed block name, or "" for a whole-file target
    Label string      // the render label; the UI never re-derives one
}
```

`type DeletePlan struct` carries `Scope DeleteScope`, `Name string`, `Targets []DeleteTarget`, `ProviderRewriteTarget *DeleteTarget` (nil when the ref-count says it survives), `SharedKeyOwners []string`, `UnmanagedHits []UnmanagedHit`, `Disclaimer string`, and `KeyPaths []string`; plus `func PlanDelete(acct Account, scope DeleteScope, deps PlanDeps) (DeletePlan, error)`.

`PlanDeps` holds the read-only seams the plan needs: the account list, the foreign-provider counter, and the scan sources. It has no writer field at all — that is the structural guarantee that planning cannot mutate. `PlanDelete` returns an ERROR, and every read/parse failure inside it must surface as that error rather than a shorter target list — a plan that fails to read a file must never be indistinguishable from a legitimately small plan (this is the domain half of review R-07's fail-closed requirement; plan 05-06 carries it through the Backend seam).

Extract the target-derivation into an unexported `deleteTargets(acct Account, scope DeleteScope, providerSurvives bool) []DeleteTarget` that both `PlanDelete` and `Delete` call, so the preview can never promise a target the write does not touch or omit one it does. In `Delete`, record the `DeleteTarget` for each region it actually modified onto `DeleteResult.Modified []DeleteTarget`, and add a test that runs `PlanDelete` and then `Delete` against the same fixture and asserts `DeleteResult.Modified` equals `DeletePlan.Targets` as SETS of (File, Block) pairs — the equality review R-11 asks for, which a file-path-only comparison cannot express.

Leave the matched-line truncation to the renderer: the plan carries each hit's full text. Record the planner assumption in the summary — 05-UI-SPEC.md leaves the truncation width unresolved, and the resolution is that the domain never truncates and the render applies the existing preview-block clip convention. Plan 05-06 implements that side.

Do not put copy strings in this file beyond the frozen disclaimer constant already defined in `scan.go`; every other label belongs to the render layer.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/identity/... -run 'PlanDelete|DeletePlan'</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the everything-scope plan for a sole-provider identity carries a non-nil `ProviderRewriteTarget` whose `Block` is `provider-rewrite:<host>`, and that the same plan for a shared-provider identity carries nil.
    - A test asserts the Git-only plan omits the SSH config, the key paths, and the allowed_signers file from its target list.
    - A test snapshots every seeded file's bytes, calls `PlanDelete`, and asserts all bytes are unchanged.
    - A test runs `PlanDelete` then `Delete` over one fixture and asserts `DeleteResult.Modified` equals `DeletePlan.Targets` as sets of (File, Block) pairs, not merely as sets of file paths.
    - A negative-control test mutates `deleteTargets` output for one caller in a test double and asserts the equality assertion FAILS, proving the comparison is not vacuous.
    - A test injects a failing read seam into `PlanDeps` and asserts `PlanDelete` returns a non-nil error and a zero-value plan, never a partial plan with a nil error.
    - A test asserts `DeletePlan.UnmanagedHits` carries untruncated matched lines.
  </acceptance_criteria>
  <done>One pure plan value describes exactly what a delete will do, and a test proves the plan and the write cannot diverge.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind | Notes |
|---|---|---|
| `identity.RewriteProviderKey(provider, alias)` | exported func | promoted from `rewriteLookupProvider`; one normalizer for write + count |
| `identity.ProviderHostForSSHHostname(hostname)` | exported func | canonical alt-SSH endpoint → FQDN provider key |
| `identity.ProviderKeyForHost(alias, hostname, marker)` | exported func | one Host stanza → provider key, documented precedence |
| `identity.ProviderRefCount(accounts, providerKey, excludingName)` | exported func | D-09 managed-side count over NORMALIZED keys |
| `identity.DeleteDeps.ForeignProviderRefs` | new seam field | D-09 hand-written-alias count |
| `identity.DeleteDeps.RemoveProviderRewrite` | new seam field | gated by the ref-count |
| `identity.DeleteDeps.CopyKeyPairToArchive` | new seam field | D-11 copy-only; live removal stays last |
| `identity.DeleteResult.ProviderRewriteRemoved` / `.ProviderRewriteBackup` / `.KeyKeptFor` / `.ArchivedKeyPaths` / `.Modified` | new struct fields | receipt + rollback + plan-equality facts |
| `identity.ScanRegion` + `ScanRegionOwnManaged/OtherManaged/Unmanaged` | exported type + constants | R-09 three-way region model |
| `identity.UnmanagedHit{File, Line, Region, Text}` | exported struct | D-13 scan hit |
| `identity.ScanUnmanagedReferences(alias, sources)` | exported func | token-boundary scan |
| `identity.ScanSource{File, Bytes, Region}` | exported struct | region-tagged source |
| `identity.SplitScanRegions(file, content, ownIdentity)` | exported func | the one place the region rule lives |
| `identity.UnmanagedScanDisclaimer` | exported const | frozen copy, grep-gated, single definition for the phase |
| `identity.SharedKeyOwners(accounts, keyPath, excludingName)` | exported func | D-12, returns ALL siblings |
| `identity.DeleteTarget{File, Block, Label}` | exported struct | logical region, not a bare file path |
| `identity.DeletePlan` / `identity.PlanDelete` / `identity.PlanDeps` | exported types + func | the pure preview, error-returning |
| `internal/identity/scan.go`, `scan_test.go`, `deleteplan.go`, `deleteplan_test.go` | new files | |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shared provider state → per-identity delete | one identity's delete can break every other identity on the provider |
| shared key file → per-identity delete | one identity's delete can strand a sibling identity |
| unmanaged user text → scan matcher | a loose matcher produces false warnings that erode trust in real ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-16 | Denial of Service | orphaned provider rewrite after delete | high | mitigate | Ref-count over managed AND hand-written references; tests assert both the removal and the survival directions. |
| T-05-17 | Denial of Service | over-eager provider rewrite removal breaking a sibling | high | mitigate | The hand-written-alias test is the explicit negative control 05-RESEARCH.md Pitfall 4 asks for. |
| T-05-18 | Denial of Service | deleting a key a sibling identity still uses | high | mitigate | D-12 auto-downgrade keyed off the full sibling slice, not a one-label map; a three-identity test pins it. |
| T-05-19 | Repudiation | confirm copy overclaiming irreversibility | medium | mitigate | The plan carries the archive copy path so the ceremony states the key was removed from active use with a copy at a named path (D-11), never a blanket cannot-be-undone claim for the key. |
| T-05-20 | Tampering | substring alias match deleting or warning about the wrong identity | medium | mitigate | Whole-token matching mirroring the superstring-principal guard, with prefix/suffix/dotted negative tests. |
| T-05-21 | Information Disclosure | scan hits echoing secret-bearing lines | low | accept | Sources are the user's own config files being shown back to the user in their own terminal; no transmission, no logging, and private key files are not among the scanned sources. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
REQ | MGR-06 | Delete everything vs Git identity only, applied with backup | 05-01, 05-04, 05-06, 05-07 | COVERED | Everything-path semantics land here.
RESEARCH | Pitfall 4 | RemoveURLRewritesBlock is the wrong function | 05-02, 05-04 | COVERED | Ref-count calls the per-provider remover only.
RESEARCH | Don't Hand-Roll | keyOwners already computes shared usage | 05-04 | COVERED | Replaced with a slice-returning function; the map collapsed multiple owners.
CONTEXT | D-09 | Per-provider insteadOf reference-counted | 05-04 | COVERED | Task 1, incl. hand-written references.
CONTEXT | D-11 | Key backed up before deletion, precise confirm copy | 05-04, 05-06 | COVERED | Archive copy here; copy wording in 05-06.
CONTEXT | D-12 | Shared key auto-downgrades delete-everything | 05-04, 05-06 | COVERED | Task 2, sibling list.
CONTEXT | D-13 | Unmanaged-reference scan, warn-never-block | 05-04, 05-06 | COVERED | Task 2 + the frozen disclaimer constant.
UI-SPEC | zero-one-many | Can the substrate return more than one sibling? | 05-04 | COVERED | Resolved: yes; the return type is a slice and the render comma-joins.
UI-SPEC | long-text | D-13 matched-line truncation width | 05-04, 05-06 | COVERED | Resolved: the domain never truncates; the render applies the existing clip convention (implemented in 05-06).
REVIEW | R-05 | Foreign-provider ref-count compares incompatible values | 05-04 | COVERED | `RewriteProviderKey` promoted + `ProviderHostForSSHHostname`/`ProviderKeyForHost`, with the `ssh.github.com` regression test.
REVIEW | R-03 | Archive semantics conflict (move vs copy) | 05-02, 05-04 | COVERED | Delete-everything uses the COPY primitive; live removal is the last step; failure test asserts both copies survive.
REVIEW | R-09 | Scan-source own/other/foreign distinction | 05-04 | COVERED | Three-region `ScanSource`/`UnmanagedHit` + `SplitScanRegions`.
REVIEW | R-10 | Archive entries unknown to rollback | 05-04 | COVERED | `DeleteResult.ArchivedKeyPaths` reports exactly what this call created.
REVIEW | R-11 | DeletePlan target-equality ambiguity | 05-04 | COVERED | `DeleteTarget{File, Block, Label}` + set equality over (File, Block) with a negative control.
REVIEW | R-26 | Private key files must never be scan sources | 05-04 | COVERED | Explicit four-source assertion in Task 2.
REVIEW | R-27 | Disclaimer constant defined twice | 05-04, 05-06 | COVERED | Frozen once in `identity`; 05-06 references it.
</source_audit>

## Review resolutions (cycle 1)

| Finding | Severity | Resolution in this plan |
|---|---|---|
| Foreign-provider reference predicate compares incompatible concepts | HIGH | `rewriteLookupProvider` is promoted to `identity.RewriteProviderKey`; `ProviderHostForSSHHostname` maps the recipe's canonical alt-SSH endpoints; `ProviderKeyForHost` gives one documented precedence. Regression test: `Host foo.github.com` / `Hostname ssh.github.com` counts as a `github.com` reference. |
| Archive semantics conflict with 05-02 | HIGH | Delete-everything uses `CopyKeyPairToArchive` and removes live keys LAST; a failure-injection test asserts both copies and both live keys survive an earlier failure. |
| `ProviderRefCount` needs normalization | MEDIUM | Both sides of the comparison are normalized keys; a short-form `Provider` test pins it. |
| Scan-source stripping may miss sibling managed blocks | MEDIUM | Three-region model; sibling hits are reported and labelled rather than dropped. |
| Target equality is ambiguous | MEDIUM | `DeleteTarget{File, Block}` set equality plus a negative control proving the assertion is not vacuous. |
| Archive-then-remove-last leaves entries unknown to cleanup | MEDIUM | `DeleteResult.ArchivedKeyPaths` reports them for 05-07's journal. |
| Do not scan private key files | MEDIUM | Explicit four-source assertion. |
| Disclaimer constant vs 05-06 copy-freeze | LOW | Frozen once in `internal/identity`; 05-06 registers and renders that constant by reference. |

<verification>
Run `make test` and `make lint`. Confirm `go list -deps ./internal/identity` shows no UI import, and confirm the reflection guard in `cmd/gitid/wiring_test.go` reports every `identity.DeleteDeps` field non-nil.
</verification>

<success_criteria>
- Provider cleanup fires exactly when no reference of any kind survives, proven in both directions.
- Deleting a shared key is structurally impossible; the downgrade names every sibling.
- The scan warns without blocking and without false positives, and one pure plan value drives both the confirm screen and the CLI dry-run.
</success_criteria>

<output>
Create `.planning/phases/05-identity-manager/05-04-SUMMARY.md` when done, recording the two planner resolutions (Git-only never removes a rewrite; delete-everything reuses the archive directory as D-11's backup dir).
</output>
