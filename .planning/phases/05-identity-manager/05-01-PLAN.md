---
phase: 05-identity-manager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/identity/delete.go
  - internal/identity/delete_test.go
  - internal/tuikit/backend.go
  - internal/tuikit/views.go
  - internal/tuikit/identities.go
  - internal/tuikit/backend_stub_test.go
  - internal/dummytui/fixturebackend.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - cmd/gitid/main.go
  - cmd/gitid/identity.go
  - cmd/gitid/identity_read.go
  - cmd/gitid/identity_delete.go
  - cmd/gitid/identity_test.go
  - e2e/identity_manager_pty_e2e_test.go
  - e2e/identity_cli_e2e_test.go
autonomous: true
requirements: [MGR-01, MGR-03, MGR-06, MGR-07, MGR-08, SHELL-01, SHELL-03]
estimate:
  tokens: 96000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Deleting the Git identity of an existing identity from the real TUI removes the includeIf block and the fragment file from the user's actual files, and the removal is still gone after the binary restarts and re-reads disk (closes RESEARCH Pitfall 1 for one verb)."
    - "The allowed_signers line SURVIVES a Git-only delete and the key pair is untouched (D-10), so historic `git log --show-signature` verification keeps working."
    - "`gitid identity delete <name> --git-only --yes` produces byte-equivalent on-disk state to the TUI path because both call one shared domain chokepoint, never a second write pipeline (D-02)."
    - "`gitid identity list` and `gitid identity show <name>` render an aligned table on a TTY, tab-delimited when piped, and a stable JSON document under `--json` (D-03)."
    - "The `--json` document reports BOTH health axes (IdentityState and KeyState) plus the Problems list, not a single collapsed label — the frozen schema decision for D-03."
    - "Every identity row and JSON record is reconstructed from parsed managed blocks on each invocation with no sidecar state file (MGR-08)."
    - "An SSH-only identity renders and marshals with EMPTY Git fields, never zero-valued placeholders (MGR-03)."
    - "`gitid identity` exposes the D-01 noun-verb tree with top-level flat aliases, and `gitid completion bash|zsh|fish` emits a script naming the identity subcommands."
  artifacts:
    - path: "cmd/gitid/identity.go"
      provides: "the `gitid identity` noun group, shared flag set, and D-01 flat aliases"
    - path: "cmd/gitid/identity_read.go"
      provides: "`list`/`show` with TTY table, piped tab-delimited, and `--json` output (D-03)"
    - path: "cmd/gitid/identity_delete.go"
      provides: "`gitid identity delete --git-only` calling the shared domain chokepoint"
    - path: "internal/tuikit/backend.go"
      provides: "CommitDelete async seam + DeleteCommitMsg"
    - path: "e2e/identity_manager_pty_e2e_test.go"
      provides: "raw-keystroke PTY proof that the TUI delete really writes"
    - path: "e2e/identity_cli_e2e_test.go"
      provides: "headless CLI proof of the same on-disk outcome"
  key_links:
    - from: "internal/tuikit/identities.go handleDeleteKey"
      to: "cmd/gitid/wiring.go realBackend.CommitDelete"
      via: "Backend.CommitDelete tea.Cmd delivering DeleteCommitMsg"
      pattern: "CommitDelete|DeleteCommitMsg"
    - from: "cmd/gitid/identity_delete.go"
      to: "internal/identity Delete chokepoint"
      via: "the same deleteIdentityTransaction the TUI CommitDelete calls"
      pattern: "deleteIdentityTransaction"
---

<objective>
Prove the whole Phase 5 architecture with ONE lifecycle verb wired end-to-end through every layer this phase touches, then add the read surface every later plan asserts against.

Purpose: `realBackend.Persist` currently falls through to the in-memory `tuikit.Reduce` for every mutation except AddIdentity/Reset, so MGR-04/05/06 produce ZERO real writes in the live binary (05-RESEARCH.md Pitfall 1). Before building four lifecycle ceremonies on that seam, one thin slice must prove the seam works: domain chokepoint → tuikit Backend method → real composition root → Cobra CLI → live PTY evidence. D-02's hardest architectural claim ("CLI and TUI MUST call the same test → backup → write → re-test chokepoint — no behavioral fork") is proven here or it is never proven.
Output: A working Git-only delete from both skins, a stable `--json`/table read surface, and the D-01 command tree the remaining CLI verbs plug into.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@recipes/README.md
@recipes/gitconfig.recipe
@.planning/phases/05-identity-manager/05-CONTEXT.md
@.planning/phases/05-identity-manager/05-RESEARCH.md
@.planning/phases/05-identity-manager/05-UI-SPEC.md
@.planning/design/identity-manager/FIELDS.md

<authority>Every bare D-NN citation means `05-CONTEXT.md`. `05-RESEARCH.md` supplies the verified file:line map — do not re-derive it. Where 05-UI-SPEC.md and 05-CONTEXT.md name the same identifier, CONTEXT wins.</authority>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — Git-only delete really writes, from the TUI and from the CLI</name>
  <files>internal/identity/delete.go, internal/identity/delete_test.go, internal/tuikit/backend.go, internal/tuikit/identities.go, internal/tuikit/backend_stub_test.go, internal/dummytui/fixturebackend.go, cmd/gitid/wiring.go, cmd/gitid/identity.go, cmd/gitid/identity_delete.go, e2e/identity_manager_pty_e2e_test.go</files>
  <read_first>
    - internal/identity/delete.go (whole file — Delete, DeleteDeps, DeleteResult)
    - internal/tuikit/backend.go (the Backend interface contract and its file header rules)
    - internal/tuikit/identities.go lines 2239-2333 (deleteCeremonyFor, handleDeleteKey) and 1767-1834 (handleMsg's GitCommitMsg precedent)
    - cmd/gitid/wiring.go lines 1-60 (the three composition-root contracts), 344-406 (Persist/persistCreate/PersistError), 816-862 (CommitGit — the async pattern to mirror), 863-1161 (mutationJournal), 1570-1615 (accounts, keyOwners)
    - cmd/gitid/main.go (newRootCmd and its D-14 doc comment)
    - internal/dummytui/fixturebackend.go (the second Backend implementer that must also satisfy any new method)
    - e2e/harness_test.go (SandboxHome, BuildBinary, FakeSSHDir, FakeGitDir) and e2e/ui_pty_e2e_test.go lines 68-260 (ptySession, startPTY, sendKey, snapshot, waitFor)
    - .planning/phases/04-git-configuration-screen/04-03-SUMMARY.md (how the Git write transaction and its rollback were landed)
  </read_first>
  <behavior>
    - Given an identity with an SSH Host block, an includeIf block, a fragment file, an allowed_signers line, and a key pair, a Git-only delete removes the includeIf block and the fragment file.
    - The SSH Host block, the key pair, AND the allowed_signers line all survive that delete (D-10).
    - The fragment file is backed up before it is unlinked, and the backup path is reported in the result (D-11's fragment half).
    - A mid-transaction failure restores every touched file to its pre-transaction bytes and reports the restored paths.
    - Re-running the same delete on an already-deleted identity is a no-op that reports no error (idempotent).
    - The CLI verb and the TUI ceremony, run against two identical sandbox HOMEs, leave byte-identical `~/.gitconfig` and `~/.gitconfig.d/` state.
  </behavior>
  <action>
Add a Git-only-aware delete chokepoint and wire it through every layer.

1. `internal/identity/delete.go`: introduce `type DeleteScope string` with exported constants `DeleteScopeGitOnly` ("git-only") and `DeleteScopeEverything` ("everything"). Change `Delete` to take `scope DeleteScope` in place of the current `keepKey bool` parameter, and update every existing caller and test. Under `DeleteScopeGitOnly` the function MUST skip `deps.RemoveAllowedSigners` entirely and skip `deps.RemoveKeyFiles`, per D-10; under `DeleteScopeEverything` it keeps today's behavior for both. Under BOTH scopes it still removes the per-identity gitconfig includeIf block and the fragment file. Document on `Delete` itself that the SSH Host block removal is scope-dependent: `DeleteScopeGitOnly` MUST NOT touch `~/.ssh/config` at all (today's unconditional `filewriter.RemoveBlock` on the SSH bytes is wrong for the Git-only path), while `DeleteScopeEverything` keeps it. Add `DeleteResult.SSHUntouched bool` so callers can render the correct target list.

2. `internal/tuikit/backend.go`: add `CommitDelete(name string, scope string) tea.Cmd` to the `Backend` interface and a `DeleteCommitMsg` struct carrying `Backups []string`, `Restored []string`, `Removed []string`, and `Err string` — mirroring `GitCommitMsg`'s existing shape. Keep the file-header rule intact: the method signature speaks only plain Go types and tuikit view types, never an `identity` type.

3. `internal/tuikit/identities.go`: in `handleDeleteKey`, on `ceremonyFinished` stop emitting the `DeleteIdentity` Action optimistically. Instead set a `deleteCommitPending` field, return the `backend.CommitDelete(sel.Name, m.deleteScope)` command, and reduce `DeleteIdentity` only from `handleMsg` once `DeleteCommitMsg` arrives with an empty `Err` — the exact sequencing `GitCommitMsg` already uses at identities.go:1768. On a non-empty `Err`, route through `ceremony.commitFailed` and append the restored paths to the message, as the Git ceremony does. Populate `DeleteIdentity.Backup` from the FIRST real backup path the message carries, never from `NewBackupPath` (that helper is a dummy-fixture display convention, not the real filewriter naming).

4. `internal/dummytui/fixturebackend.go` and `internal/tuikit/backend_stub_test.go`: implement `CommitDelete` for both non-real Backends. The fixture returns a `DeleteCommitMsg` with its existing fixture backup constants after a brief tick, so the dummy binary keeps compiling and behaving.

5. `cmd/gitid/wiring.go`: add `buildDeleteDeps(b *realBackend) identity.DeleteDeps` alongside the existing `buildIdentityDeps`, filling EVERY field non-nil (the reflection guard in wiring_test.go is the contract). Add `func (b *realBackend) CommitDelete(name, scope string) tea.Cmd` mirroring `CommitGit`: take `txMu`, build a `mutationJournal`, `watchFile` every file the scope will touch, call a new `deleteIdentityTransaction(name string, scope identity.DeleteScope) ([]string, []string, error)` that resolves the `identity.Account` from `b.accounts()` and calls `identity.Delete`, and on error call `journal.restore()` and return the restored list. Add a `tuikit.DeleteIdentity` case to `Persist` that returns `b.InitialState()` (the write already happened in CommitDelete; Persist must re-read disk, never `tuikit.Reduce`).

6. `cmd/gitid/identity.go` (new): `newIdentityCmd()` returning the `identity` noun group with `Use: "identity"`, `Aliases: []string{"id"}`, and a short description. Register it from `newRootCmd()` in main.go and delete the stale half of that function's doc comment that says the v1.0 surface is not yet built.

7. `cmd/gitid/identity_delete.go` (new): `newIdentityDeleteCmd()` implementing `gitid identity delete <name>` with `--git-only`, `--all`, `--yes`, and `--dry-run` flags. `--git-only` and `--all` are mutually exclusive and one is required in non-TTY mode. This plan implements the `--git-only` path only; `--all` MUST return a clear "not yet available in this build" error rather than silently doing something else (05-08 lands it). The handler resolves HOME through `newBackendForHome`, calls the SAME `deleteIdentityTransaction` the TUI calls, prints the backup and removed paths, and exits non-zero on error. `--yes` suppresses ONLY the confirmation prompt; the timestamped backup is taken unconditionally (D-02). `--dry-run` prints the target list and exits 0 without writing.

8. `e2e/identity_manager_pty_e2e_test.go` (new): a raw-keystroke PTY test at 100x30 against `BuildBinary`, over a `SandboxHome` pre-seeded with one complete identity's four artifacts plus a key pair. Drive `d` to open delete-choice, Enter on the default-focused safer option, then the ceremony keys through to the receipt. After the receipt, assert on the SANDBOX FILESYSTEM (not the rendered frame): the includeIf block and fragment are gone, and `~/.ssh/config`, the key pair, and `~/.ssh/allowed_signers` are byte-identical to their pre-delete contents. Then restart the binary in a second PTY session and assert the deleted Git side is still absent from the rendered list — the restart is what proves this was a real write and not a `Reduce` illusion.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/identity/... ./internal/tuikit/... ./cmd/gitid/... && TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 360s ./e2e -run '^TestIdentityManager_DeleteGitOnly'</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 and `var _ tuikit.Backend = (*realBackend)(nil)` still compiles with the new method present.
    - `internal/identity/delete_test.go` has a case proving `DeleteScopeGitOnly` leaves the allowed_signers block present and the SSH bytes unchanged, and a sibling case proving `DeleteScopeEverything` still removes both.
    - The PTY test asserts post-delete sandbox bytes for `~/.ssh/config`, the private key, the public key, and `~/.ssh/allowed_signers` are equal to the pre-delete bytes captured in the same test.
    - The PTY test performs a SECOND binary launch after the delete and asserts the list still shows the identity without its Git side.
    - `cmd/gitid/wiring_test.go`'s reflection guard passes over `identity.DeleteDeps` as well as `identity.Deps` (extend the guard to cover the new struct).
    - Running the CLI verb and the TUI ceremony over two identical sandbox HOMEs yields equal `~/.gitconfig` bytes, asserted in the e2e suite.
  </acceptance_criteria>
  <reversibility rating="reversible">`DeleteScope` and `CommitDelete` are internal package surface with no external consumers; `identity.Delete` has no production caller today (only its own tests), so the signature change is a compile-time-checked refactor.</reversibility>
  <done>Git-only delete performs a real, backed-up, rollback-capable write from both the TUI and the CLI, proven by filesystem assertions and a post-delete binary restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read surface — `identity list`/`show` with TTY table, piped, and `--json` (D-03)</name>
  <files>cmd/gitid/identity_read.go, cmd/gitid/identity_test.go, cmd/gitid/wiring.go, e2e/identity_cli_e2e_test.go</files>
  <read_first>
    - internal/identity/state.go lines 14-120 (the 8 State constants, Problem constants, IdentityHealth)
    - internal/identity/inventory.go lines 17-60 and 141-190 (Inventory, InventoryDeps, BuildInventoryDeps, InventoryDepsForHome)
    - internal/identity/identity.go lines 24-60 (Account, including Incomplete)
    - cmd/gitid/debug.go (newDebugCapsCmd — the existing precedent for a headless readout built from BuildInventory)
    - cmd/gitid/wiring.go lines 1409-1460 (toDemoIdentity — the existing Account → view conversion and its Git-field handling)
  </read_first>
  <behavior>
    - `list` with a TTY stdout prints an aligned column table with a header row; the same command with a piped stdout prints one tab-delimited record per identity and no header decoration.
    - `list --json` prints a single JSON array; `show <name> --json` prints a single JSON object.
    - Each JSON record carries `name`, `identity_state`, `key_state`, `problems`, `alias`, `hostname`, `port`, `key_path`, `pub_path`, `fragment_path`, `provider`, `force_ssh`, `git_name`, `git_email`, `match_strategy`, and `complete`.
    - For an SSH-only identity, `git_name`, `git_email`, `match_strategy`, and `fragment_path` marshal as empty strings — never a fabricated default (MGR-03).
    - `show` on an unknown name exits non-zero with an error naming the unknown identity and does not print a partial record.
    - Two consecutive invocations with no intervening write produce byte-identical output (MGR-08 — nothing is cached or persisted between runs).
  </behavior>
  <action>
Implement the D-03 read surface as a new `cmd/gitid/identity_read.go`.

Define an unexported `identityRecord` struct with explicit lowercase snake_case json tags for every field named in the behavior block. FREEZE the schema decision here and record it in a doc comment on the struct: `--json` marshals the RICHER two-axis shape (`identity_state` from `IdentityHealth.IdentityState`, `key_state` from `IdentityHealth.KeyState`, `problems` from `IdentityHealth.Problems`), not a single collapsed `ClassifyState` label — a consumer can always flatten, but cannot recover detail that was never emitted. Cite D-03's "keep it stable once shipped" clause in the comment.

Build records by calling `identity.BuildInventory(identity.InventoryDepsForHome(home))` for the health axes and `b.accounts()` for the Account-shaped fields, joining on identity name. Never re-derive classification in `cmd/gitid` — that logic lives in `internal/identity` (05-RESEARCH.md's Architectural Responsibility Map).

Detect TTY with `term.IsTerminal(int(os.Stdout.Fd()))`, already imported by main.go. Render three writers: `writeTable` (aligned via text/tabwriter with a header row), `writeTabDelimited` (one line per record, tab-separated, no header), and `writeJSON` (`json.MarshalIndent` with two-space indent, single trailing newline). Route all three through an injected `io.Writer` and an injected `isTTY bool` so unit tests exercise every branch without a real terminal — the same testability-seam pattern `runDebugCapsWithDeps` established.

Add `newIdentityListCmd()` and `newIdentityShowCmd()` and register both on the `identity` noun from Task 1. Reads are ALWAYS headless — neither command ever opens the TUI regardless of TTY state (D-02).

In `e2e/identity_cli_e2e_test.go`, add a case that seeds a sandbox HOME with one complete identity and one SSH-only identity, runs `gitid identity list --json` with a plain `exec.Command` (no PTY — this is the PTY-free assertion channel D-03 exists to provide), unmarshals the output, and asserts the SSH-only record's Git fields are empty strings while the complete record's are populated.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./cmd/gitid/... -run 'TestIdentity(List|Show|Record)' && TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 360s ./e2e -run '^TestIdentityCLI_ListShow'</automated>
  </verify>
  <acceptance_criteria>
    - `gitid identity list --json | python3 -c 'import json,sys; json.load(sys.stdin)'` parses without error in the e2e test (assert via `json.Unmarshal` in Go, not a shell pipe).
    - A unit test drives `writeTable`, `writeTabDelimited`, and `writeJSON` against the same fixture record set and asserts the table output contains a header row while the tab-delimited output does not.
    - A unit test asserts an SSH-only fixture marshals `git_name`, `git_email`, `match_strategy`, and `fragment_path` as empty strings.
    - A unit test asserts `show` with an unknown name returns a non-nil error whose message contains the requested name, and writes nothing to the output writer.
    - The e2e test runs `list --json` twice and asserts byte-identical stdout.
  </acceptance_criteria>
  <reversibility rating="costly">D-03 states the JSON schema must stay stable once shipped; changing a field name after release breaks any script consuming it. Mitigated by choosing the strict superset (both axes + problems) so later needs are satisfied by ADDING fields, never renaming or removing one.</reversibility>
  <done>`list` and `show` render all three output modes from live disk state, and the JSON schema is documented as frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The D-01 command tree — noun groups, flat aliases, completions</name>
  <files>cmd/gitid/identity.go, cmd/gitid/main.go, cmd/gitid/identity_test.go, cmd/gitid/completion_test.go</files>
  <read_first>
    - cmd/gitid/main.go (newRootCmd, Execute, noArgsAction)
    - cmd/gitid/completion_test.go (the existing completion coverage this task extends)
    - .planning/phases/05-identity-manager/05-CONTEXT.md the "CLI parity surface (SHELL-03)" decisions block (D-01 through D-04)
  </read_first>
  <behavior>
    - `gitid identity --help` lists the identity verbs registered so far.
    - `gitid list`, `gitid show`, and `gitid delete` resolve to the same commands as their `gitid identity …` forms (D-01 flat aliases).
    - `gitid ssh`, `gitid git`, `gitid health`, and `gitid fix` each exist as a noun group that prints a clear "arrives in a later phase" message and exits non-zero, so the taxonomy is claimed and no future verb collides with a flat alias.
    - `gitid completion bash`, `zsh`, and `fish` each emit a non-empty script that mentions the `identity` subcommand.
    - `gitid --help` output names every noun group.
  </behavior>
  <action>
Formalize D-01's taxonomy in `cmd/gitid/identity.go` and `newRootCmd`.

Register the reserved noun groups `ssh`, `git`, `health`, and `fix` as commands whose `RunE` returns an error naming the phase that will implement them (Phase 6 for ssh, Phase 7 for git, Phase 8 for health and fix). Claiming the nouns now is the whole point of D-01: it guarantees a later phase's verb can never collide with a top-level flat alias.

Implement flat aliases WITHOUT duplicating command construction: build each identity verb once, register it under the `identity` noun, and register the SAME `*cobra.Command` pointer's siblings at root by constructing a thin wrapper command with `Use` set to the bare verb, `Hidden: false`, and a `RunE` that delegates to the identity verb's own `RunE`. Document in a comment why a shared pointer cannot be added to two parents in Cobra (a command has exactly one parent) and that the wrapper is the deliberate mechanism.

Do NOT call `InitDefaultCompletionCmd` explicitly — Cobra auto-registers `completion` once the root has subcommands; extend `completion_test.go` instead to assert each shell's generated script is non-empty and names `identity`.

Add a table-driven `identity_test.go` case that walks the whole command tree from `newRootCmd()` and asserts every expected `Use` string is present exactly once, so a future phase adding a verb cannot silently shadow an alias.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./cmd/gitid/... -run 'TestRootCommandTree|TestCompletion'</automated>
  </verify>
  <acceptance_criteria>
    - A test walks `newRootCmd()` and asserts the set of direct child `Use` names contains `identity`, `ssh`, `git`, `health`, `fix`, `debug`, `completion`, plus the flat aliases registered so far.
    - A test asserts each reserved noun group returns a non-nil error whose message names the implementing phase.
    - `completion_test.go` asserts the bash, zsh, and fish scripts are each non-empty and each contain the token `identity`.
    - A test asserts no two commands anywhere in the tree share the same fully-qualified path.
  </acceptance_criteria>
  <done>The D-01 noun-verb tree, flat aliases, and reserved noun groups exist and are pinned by a tree-walking test.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind | Notes |
|---|---|---|
| `identity.DeleteScope`, `identity.DeleteScopeGitOnly`, `identity.DeleteScopeEverything` | Go type + constants | replaces `Delete`'s `keepKey bool` |
| `identity.DeleteResult.SSHUntouched` | struct field | scope-aware target reporting |
| `tuikit.Backend.CommitDelete(name, scope string) tea.Cmd` | interface method | new async seam |
| `tuikit.DeleteCommitMsg{Backups, Restored, Removed, Err}` | Go struct | typed async result |
| `identitiesModel.deleteCommitPending` | struct field | gates the optimistic reduce |
| `realBackend.CommitDelete`, `realBackend.deleteIdentityTransaction`, `buildDeleteDeps` | Go funcs | composition root |
| `cmd/gitid/identity.go` `newIdentityCmd` | Go func + file | `gitid identity` noun group |
| `cmd/gitid/identity_read.go` `newIdentityListCmd`, `newIdentityShowCmd`, `identityRecord`, `writeTable`, `writeTabDelimited`, `writeJSON` | Go funcs/types + file | D-03 read surface |
| `cmd/gitid/identity_delete.go` `newIdentityDeleteCmd` | Go func + file | `--git-only`, `--all`, `--yes`, `--dry-run` flags |
| reserved noun groups `ssh`, `git`, `health`, `fix` | Cobra commands | D-01 taxonomy claim |
| `e2e/identity_manager_pty_e2e_test.go`, `e2e/identity_cli_e2e_test.go` | new test files | PTY and headless evidence |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user's real `~/.ssh` + `~/.gitconfig` → delete transaction | irreversible removals cross here; only backed-up, rollback-capable writes may proceed |
| CLI flag input → domain chokepoint | a name from argv selects which managed blocks are removed |
| test harness → compiled binary | evidence must come from the real binary, not a fixture model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | `identity.Delete` block removal | high | mitigate | Removal is keyed by the exact identity NAME through `filewriter.RemoveBlock`; a test proves a sibling identity's block and the shared `_global` block survive. |
| T-05-02 | Denial of Service | delete transaction | high | mitigate | `mutationJournal.watchFile` snapshots every target before the first write; a failure-injection test proves every file is restored byte-identically. |
| T-05-03 | Repudiation | Git-only delete losing signing trust | medium | mitigate | D-10 keeps the allowed_signers line; a test asserts the file is byte-identical after a Git-only delete. |
| T-05-04 | Information Disclosure | `--json` output | low | mitigate | The record carries key PATHS only, never key bytes; a test asserts no field value begins with an OpenSSH private-key header. |
| T-05-05 | Elevation of Privilege | CLI name argument | medium | mitigate | The name is matched against reconstructed account names, never interpolated into a path or a shell; `os/exec` is not used in the delete path. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Manage identities from the main view; delete with the right choices | 05-01, 05-04, 05-06 | COVERED | Tracer lands the Git-only half; 05-04/05-06 land the everything half.
REQ | MGR-01 | Completeness per row | 05-01, 05-07 | COVERED | Read surface + real list wiring.
REQ | MGR-03 | SSH-first detail, no fabricated Git fields | 05-01, 05-07 | COVERED | JSON/table empty-field rule here; detail render in 05-07.
REQ | MGR-06 | Delete choice everything vs Git-only | 05-01, 05-04, 05-06 | COVERED | Git-only path here.
REQ | MGR-07 | Per-identity health | 05-01, 05-07 | COVERED | Two-axis health in the record.
REQ | MGR-08 | No sidecar DB | 05-01 | COVERED | Reconstructed per invocation; asserted by repeat-invocation equality.
REQ | SHELL-01 | Integrated Bubble Tea app | 05-01 | COVERED | Existing app shell drives the new seam.
REQ | SHELL-03 | CLI parity + completion | 05-01, 05-08 | COVERED | Tree + reads + one write verb here; remaining verbs in 05-08.
RESEARCH | Pitfall 1 | Persist's silent fallthrough to the demo reducer | 05-01 | COVERED | Tracer's restart assertion is the direct countermeasure.
RESEARCH | Pitfall 6 | newRootCmd is a blank slate | 05-01 | COVERED | Task 3 builds the tree from scratch.
RESEARCH | Open Q2 | JSON schema shape | 05-01 | COVERED | Resolved to the richer IdentityHealth shape, documented as frozen.
CONTEXT | D-01 | Noun-verb + flat aliases | 05-01 | COVERED | Task 3.
CONTEXT | D-02 | Adaptive depth, unconditional backups, one chokepoint | 05-01, 05-08 | COVERED | `--yes`/`--dry-run` on delete here; full adaptive matrix in 05-08.
CONTEXT | D-03 | `--json` + TTY-aware plain output | 05-01 | COVERED | Task 2.
CONTEXT | D-10 | allowed_signers kept in Git-only mode | 05-01 | COVERED | Tracer behavior + test.
</source_audit>

<verification>
Run `make test`, `make lint`, and `make test-e2e`. Confirm `go list -deps ./internal/tuikit` still reports no first-party backend import and `go test ./internal/dummytui/ -run TestNoBackendAllowlist` passes — the new Backend method must not have leaked a backend type into the shared render stack.
</verification>

<success_criteria>
- One lifecycle verb performs a real, backed-up, rollback-capable write from both the TUI and the CLI, proven by a post-delete binary restart.
- The `--json` read surface is stable, documented as frozen, and usable as the PTY-free assertion channel for every later plan.
- The D-01 command tree exists with reserved noun groups so Phases 6-8 cannot collide with a flat alias.
</success_criteria>

<output>
Create `.planning/phases/05-identity-manager/05-01-SUMMARY.md` when done.
</output>
