---
phase: 05-identity-manager
plan: 06
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - internal/tuikit/store.go
  - internal/tuikit/store_test.go
  - internal/tuikit/views.go
  - internal/tuikit/backend.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - internal/tuikit/backend_stub_test.go
  - internal/dummytui/fixturebackend.go
  - Makefile
autonomous: true
requirements: [MGR-04, MGR-05, MGR-06, KEY-05, KEY-07]
estimate:
  tokens: 100000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A distinct RotateIdentity action exists in the shared render vocabulary; rotate and repair are never one action distinguished by a boolean field."
    - "The action menu opens on its allocated key from the list or detail screen and offers exactly the four approved rows, in the approved order."
    - "The single key row routes to rotate or to repair purely from the identity's classified state — one entry point, no new screen, no second row."
    - "The delete-choice screen keeps its two options with the safer one default-focused; the shared-key note is a conditional extra row that never moves focus off that default."
    - "When a key is shared by more than one sibling identity, the downgrade note names ALL of them comma-joined on one line — the note is a list, not a singular template (resolves the UI-SPEC zero-one-many question against a substrate that can return several)."
    - statement: "With zero unmanaged-reference hits the warning block does not render at all; with two or more hits it renders one line per hit capped by the existing clip cue."
      verification: backstop
    - "A matched scan line longer than the pane is truncated by the existing preview clip convention, not by a new truncation rule invented here (planner assumption resolving the UI-SPEC long-text gap)."
    - "The confirm screen's cannot-be-undone heading stays scoped to the managed-block removals and never claims irreversibility for a key that has an archive copy (D-11)."
    - "The backup notice lists every path THIS ceremony actually touched — the archived key on rotate, the key-pair copy on delete-everything — with the list length varying by ceremony rather than being a frozen two-path golden."
    - "Rotate's result reuses the existing reachable-not-uploaded outcome row verbatim and adds exactly one faint grace-window hint line beneath it (D-08)."
    - "Rotate and repair reuse the existing two-stage test screens and the four-beat ceremony; neither adds a fifth beat and neither adds a new theme role or glyph."
    - "Every new user-visible string added by this plan is registered in the copy-freeze gate."
  artifacts:
    - path: "internal/tuikit/store.go"
      provides: "RotateIdentity action + its Reduce case"
    - path: "internal/tuikit/views.go"
      provides: "DeletePlanView and KeyCeremonyView — the DTOs the new screens render from"
    - path: "internal/tuikit/identities.go"
      provides: "the action-menu pane, the key ceremony pane, and the delete-screen additions"
    - path: "Makefile"
      provides: "copy-freeze coverage for every string this plan adds"
  key_links:
    - from: "internal/tuikit/identities.go action menu key row"
      to: "Backend.KeyActionFor"
      via: "state-driven routing to the rotate or repair ceremony"
      pattern: "KeyActionFor"
    - from: "internal/tuikit/identities.go delete screens"
      to: "Backend.DeletePlan"
      via: "DeletePlanView supplying targets, siblings, hits, and disclaimer"
      pattern: "DeletePlan"
---

<objective>
Render the four Phase 5 ceremonies on the approved screens: a real action menu, a state-routed key ceremony, and the three conditional additions the delete path needs.

Purpose: 05-RESEARCH.md Pitfall 3 records that the shared render vocabulary has no rotate action at all, and that adding one touches `internal/tuikit` — package surface BOTH binaries share, which is why it belongs to its own task and commit. The approved `identity-manager/FIELDS.md` names an action menu with four rows that the live render stack has never implemented (today the detail pane exposes bare keys). This plan makes the approved design real and attaches every D-06 through D-13 addition to the screen 05-UI-SPEC.md budgets it against.
Output: A backend-free render layer that can drive rotate, repair, and both delete scopes, waiting only on the real Commit implementations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-identity-manager/05-CONTEXT.md
@.planning/phases/05-identity-manager/05-RESEARCH.md
@.planning/phases/05-identity-manager/05-UI-SPEC.md
@.planning/design/identity-manager/FIELDS.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/05-identity-manager/05-03-SUMMARY.md
@.planning/phases/05-identity-manager/05-04-SUMMARY.md
@.planning/phases/05-identity-manager/05-05-SUMMARY.md

<authority>Every bare D-NN citation means `05-CONTEXT.md`. Layout, row budgets, theme roles, and copy come from `05-UI-SPEC.md` and the Phase 2 artifacts it cites; where 05-UI-SPEC.md marks a consideration unresolved, this plan's stated resolution is authoritative and must be recorded in the summary.</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RotateIdentity action + the approved action menu with state-routed key entry (MGR-05, KEY-05, KEY-07)</name>
  <files>internal/tuikit/store.go, internal/tuikit/store_test.go, internal/tuikit/backend.go, internal/tuikit/views.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, internal/tuikit/backend_stub_test.go, internal/dummytui/fixturebackend.go</files>
  <read_first>
    - internal/tuikit/store.go (whole file — the Action union, every isAction method, and Reduce's per-action cases, especially the existing NewKey case)
    - internal/tuikit/identities.go lines 38-55 (identPane), 1684-1726 (identitiesModel, newIdentitiesModel), 1855-1936 (handleKey, handleDetailKey), 3105-3192 (renderDetail), 3614-3760 (view, paneCapturesKeys, footers)
    - .planning/design/identity-manager/FIELDS.md the action-menu section (the four rows and their exact order) and the key-allocation note naming the intra-surface keys
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md the key-allocation table (the single authority)
    - .planning/phases/05-identity-manager/05-UI-SPEC.md the Approved Base States table row for action-menu
    - internal/identity/state.go KeyActionFor as landed by plan 05-03
  </read_first>
  <behavior>
    - The action menu opens from the list or detail screen on its allocated intra-surface key and closes on Escape back to the detail pane.
    - It renders exactly four rows in the approved order: view detail, clone, key, delete.
    - Arrow keys and Tab move focus through the rows; Enter activates the focused row.
    - The clone row opens the clone prompt; the delete row opens the delete-choice screen; the view row returns to detail.
    - The key row opens the key ceremony in rotate mode for a healthy identity and in repair mode for a key-missing or shared-key identity.
    - Reducing a rotate action updates the identity's row note and prepends the backup path, and leaves every other identity untouched.
    - Reducing a rotate action for an unknown identity name is a no-op that returns the input state unchanged.
    - The existing new-key action's reduction is unchanged, so the dummy fixture's frozen behavior is preserved.
  </behavior>
  <action>
Extend the shared vocabulary, then build the menu.

In `internal/tuikit/store.go`: add `type RotateIdentity struct { Name string; Backup string; ArchivedKeyPath string }` with its `isAction` method and a `Reduce` case. Do NOT add a boolean to the existing `NewKey` action — 05-RESEARCH.md names that as the anti-pattern precisely because it compiles while hiding the distinction from every future reader of the switch. Document on both actions which ceremony owns each: rotate is the retirement ceremony that archives, new-key is the repair action that never touches pre-existing key material. The rotate reduction sets the identity's note to name the rotation and prepends the backup path to the backups list, mirroring the shape every other mutating case already uses.

In `internal/tuikit/backend.go`: add `KeyActionFor(name string) string` returning the routing answer for one identity. The signature speaks a plain string, per the file-header rule; the real backend maps `identity.KeyActionFor`'s typed result to its string form at the one conversion site, and the fixture backend answers from its fixture rows.

In `internal/tuikit/identities.go`: add `paneActions` to the `identPane` enum and an `actionsFocus int` field to `identitiesModel`. Add `handleActionsKey` to the `handleKey` switch and a renderer for the pane. Open it from `handleDetailKey` on the intra-surface key the key-allocation table assigns to the action menu — read the table and use the key it names; do not claim `n` or `g`, which belong to the create flow and the git screen.

Keep the existing direct keys working. The detail pane's current clone and delete keys stay as shortcuts; the menu is an additional entry point, not a replacement. Document that both paths converge on the same handlers so there is exactly one implementation per action.

Wire the key row to `backend.KeyActionFor(sel.Name)` and set a `keyCeremonyMode` field on the model to the returned value before opening the ceremony pane that Task 3 builds. In this task the ceremony pane may be a minimal placeholder that records the mode; Task 3 gives it its content. Do not branch the menu row itself — the row is one row with one label, exactly as the approved manifest specifies.

Add model tests for every behavior above and a store test for the rotate reduction including the unknown-name no-op.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... ./internal/dummytui/... -run 'Rotate|ActionMenu|Reduce|NoBackendAllowlist'</automated>
  </verify>
  <acceptance_criteria>
    - A render test asserts the action-menu pane's rendered body contains the four approved row labels in the approved order.
    - A model test asserts the menu opens on the key the key-allocation table assigns and that the create-flow and git-screen launch keys still perform their own actions from the detail pane.
    - A model test drives the key row for a fixture identity classified healthy and asserts the recorded ceremony mode is the rotate mode; a sibling case with a key-missing fixture asserts the repair mode.
    - A store test asserts a rotate reduction changes only the named identity and prepends its backup path.
    - A store test asserts a rotate reduction for an unknown name returns a state deeply equal to the input.
    - `go test ./internal/dummytui/ -run TestNoBackendAllowlist` passes.
  </acceptance_criteria>
  <reversibility rating="reversible">`RotateIdentity` is additive to an internal action union with two in-repo implementers; the existing `NewKey` action and its reduction are left untouched so the dummy's approved behavior does not change.</reversibility>
  <done>The shared vocabulary distinguishes rotate from repair, and the approved four-row action menu routes the single key row by classified state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete-choice, confirm-destructive and backup-notice additions (D-09, D-11, D-12, D-13)</name>
  <files>internal/tuikit/views.go, internal/tuikit/backend.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, internal/tuikit/backend_stub_test.go, internal/dummytui/fixturebackend.go, Makefile</files>
  <read_first>
    - internal/tuikit/identities.go lines 2239-2333 (deleteCeremonyFor and handleDeleteKey as landed by plan 05-01)
    - internal/tuikit/ceremony.go (ceremonyConfig, FixDestructive, the four beats, and how Targets/Backups/Preview render)
    - internal/tuikit/frame.go (PreviewBlock and its maxLines/clip-cue mechanism)
    - internal/identity/deleteplan.go and internal/identity/scan.go as landed by plan 05-04
    - .planning/phases/05-identity-manager/05-UI-SPEC.md the Spacing exceptions table, the Color note on the downgrade note versus the scan warning, the Copywriting Contract rows for D-11/D-12/D-13/D-06, and the D-09 removal-note section
    - .planning/design/identity-manager/FIELDS.md the delete-choice, confirm-destructive, and backup-notice sections
    - Makefile lines 256-300 (the copy-freeze gate this task extends)
  </read_first>
  <behavior>
    - The delete-choice screen still shows two options with the safer one focused on entry, and focus does not move when the conditional note renders.
    - With a shared key, both delete-choice and confirm-destructive render the downgrade note naming every sibling, comma-joined on one line.
    - With no shared key, neither screen renders a downgrade note.
    - With zero scan hits, confirm-destructive renders no warning block at all.
    - With two or more scan hits, confirm-destructive renders one line per hit plus the fixed disclaimer, clipped by the existing preview mechanism when the list runs long.
    - A scan hit whose matched line exceeds the pane width is clipped by the existing preview convention rather than overflowing or reflowing.
    - When the identity is the last one for its provider, the provider rewrite appears as one more named target in the existing what-will-be-removed list; when a sibling survives, it does not appear anywhere.
    - The cannot-be-undone heading names the managed-block removals; the key is described as removed from active use with its copy path named.
    - Backup-notice lists exactly the paths this ceremony touched, and the list length differs between a Git-only delete and a delete-everything.
    - The whole confirm screen with a downgrade note, two scan hits, and a provider-removal target still fits the fixed frame geometry.
  </behavior>
  <action>
Feed the delete screens from one plan value and attach the four additions to their budgeted slots.

In `internal/tuikit/views.go`: add `type DeletePlanView struct` carrying `Name`, `Scope`, `Targets []string`, `ProviderRewriteTarget string`, `SharedKeyOwners []string`, `Hits []UnmanagedHitView`, `Disclaimer string`, `KeyCopyPath string`, and `Backups []string`, plus `type UnmanagedHitView struct { File string; Line int; Text string }`. Plain Go types only.

In `internal/tuikit/backend.go`: add `DeletePlan(name, scope string) DeletePlanView`. Implement it on the fixture backend from its fixture rows and on the test stub; the real implementation lands in plan 05-07.

In `internal/tuikit/identities.go`: change `deleteCeremonyFor` to take the `DeletePlanView` instead of deriving target strings from a `DemoIdentity`. Today it invents target and backup paths in the UI layer — that is the exact class of defect the Phase 4 write-plan seam was introduced to fix, and it must not survive into the delete path. Fetch the plan when the delete-choice pane opens and again when the scope changes, so the screen always describes the scope the user currently has focused.

Attach each addition to its budgeted slot exactly as 05-UI-SPEC.md's Spacing table specifies:
the downgrade note is one conditional row directly under the everything option and the confirm warning, styled with the faint hint role because it states a fact rather than a risk;
the scan warning is a block under the confirm warning and above the default-no control, styled with the warning role because it names an unscannable risk the user must accept;
the provider-rewrite target folds into the existing removal list with no extra row and no separate heading;
the key copy path and the archived key path are first-class lines in the backup notice, never buried.

Render the downgrade note by joining every sibling name with a comma and a space. Record the resolution in the summary: the UI-SPEC left open whether the substrate can return more than one sibling, plan 05-04 resolved that it can, so the note is a list and its template is plural-safe.

Clip long matched lines with the existing preview block mechanism and its clip cue; do not add a new truncation rule. Record this as the planner's resolution of the UI-SPEC long-text gap in the summary.

Add every new user-visible string as a package constant and register each one in the Makefile copy-freeze gate's string list, so a later reword fails the gate. Take the exact wording from 05-UI-SPEC.md's Copywriting Contract rows; the strings are drafts there and become frozen the moment they are added to the gate.

Add render tests for every behavior above, including a fixture with three sibling names to prove the comma-joined list, a fixture with two planted hits, and a maximal fixture combining a downgrade note, two hits, and a provider-removal target that asserts the rendered frame's line count still fits the fixed geometry.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... -run 'Delete|Confirm|BackupNotice|Scan' && TERM=dumb SSH_AUTH_SOCK= make gate-copy-freeze</automated>
  </verify>
  <acceptance_criteria>
    - A render test with three sibling names asserts the downgrade note contains all three joined by a comma and a space, on a single rendered line.
    - A render test with an empty sibling list asserts neither delete screen contains the downgrade note constant.
    - A render test with zero hits asserts the confirm body contains neither the disclaimer constant nor any hit line.
    - A render test with two hits asserts two hit lines plus the disclaimer are present.
    - A render test supplies a matched line longer than the pane width and asserts the rendered line width does not exceed the pane width.
    - A render test asserts the provider rewrite target string is present when the plan carries it and absent when it does not.
    - A render test asserts the key copy path appears in the backup notice and that the heading's cannot-be-undone sentence is not applied to the key path line.
    - A render test asserts the delete-choice focus index after the conditional note renders equals the focus index without it.
    - A render test builds the maximal fixture and asserts the total rendered frame height equals the fixed frame height.
    - `make gate-copy-freeze` passes with every new constant listed.
  </acceptance_criteria>
  <done>Every delete addition renders in its budgeted slot from one plan value, within the fixed frame, with its copy frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The key ceremony — rotate and repair over the existing test-gate screens (KEY-05, KEY-07, D-08)</name>
  <files>internal/tuikit/views.go, internal/tuikit/backend.go, internal/tuikit/identities.go, internal/tuikit/identities_test.go, internal/tuikit/backend_stub_test.go, internal/dummytui/fixturebackend.go, Makefile</files>
  <read_first>
    - internal/tuikit/identities.go lines 1343-1445 (renderStageOutcome, renderStageEvidence, proofText, renderProof) and lines 1117-1185 (copyable, keyUnused, providerKeySettingsLabel/URL, reachableHint)
    - internal/tuikit/identities.go lines 2361-2700 (handleWizardKey — how the wizard drives the test phases and the ceremony)
    - internal/tuikit/ceremony.go (the four beats, commitSucceeded, commitFailed)
    - internal/identity/rotate.go RotateResult as landed by plan 05-03
    - .planning/phases/05-identity-manager/05-UI-SPEC.md the Rotate ceremony and New-key repair ceremony sections, and the Copywriting Contract rows for the grace hint and the archive path
    - .planning/phases/03-create-flow-backend/03-05-SUMMARY.md (the D-02/D-03 warning-state render this ceremony reuses verbatim)
  </read_first>
  <behavior>
    - The key ceremony reuses the existing test-running, test-pass, and test-fail states; no new loading or error affordance appears.
    - In rotate mode the result screen renders the existing reachable-not-uploaded outcome row plus exactly one additional faint hint line naming the provider.
    - In repair mode the result screen renders no grace hint, because there is no old key to keep valid.
    - In rotate mode the backup notice includes the archived key path line; in repair mode it does not.
    - The copy-public-key affordance stays a footer action, not an extra body row.
    - A ceremony failure renders the existing failure state with the restored-paths list, exactly as the Git ceremony does.
    - Neither mode adds a fifth ceremony beat.
    - Both modes fit the fixed frame geometry with the longest fixture values.
  </behavior>
  <action>
Give the key ceremony pane its content, sharing everything with the create flow.

In `internal/tuikit/views.go`: add `type KeyCeremonyView struct` carrying `Mode string`, `IdentityName`, `ProviderHost`, `KeyPath`, `PubKeyPath`, `ArchivedKeyPath`, `Targets []string`, and `Backups []string`. In `internal/tuikit/backend.go`: add `KeyCeremonyPlan(name, mode string) KeyCeremonyView` plus the two async seams `CommitRotate(name string) tea.Cmd` and `CommitNewKey(name string) tea.Cmd`, and a shared `KeyCommitMsg` carrying `Mode`, `Backups []string`, `Restored []string`, `ArchivedKeyPath string`, and `Err string`. Implement all of them on the fixture backend and the test stub; the real implementations land in plan 05-07.

In `internal/tuikit/identities.go`: build `paneKeyCeremony` on top of the SAME stage-render helpers the wizard uses. Call `renderStageOutcome` and `renderStageEvidence` directly rather than writing new outcome rendering — D-08 says the post-rotate gate reuses the Phase 3 states verbatim, and reusing the functions is how that stays true under future edits.

Add the grace hint as one faint line appended below the outcome row, rendered only when the mode is rotate AND the outcome is the reachable-not-uploaded warning. Take the exact sentence from the UI-SPEC Copywriting Contract, interpolating the provider host. Add it as a package constant with the provider host as a format argument, and register the constant's literal prefix in the copy-freeze gate.

Add the archived key path line to the ceremony's backup list only in rotate mode, using the same row template every other backup path already uses. Register its label constant in the copy-freeze gate too.

Route the commit exactly as the Git ceremony does: on ceremony confirmation set a pending flag and return the appropriate commit command; reduce the rotate or new-key action only from `handleMsg` once the typed message arrives with an empty error. Populate the action's backup and archived-path fields from the message, never from the display-side backup-path helper.

Add render tests for every behavior above, including a maximal-length fixture asserting the fixed frame height, and a negative test asserting the repair mode's rendered body does not contain the grace hint constant.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... ./internal/dummytui/... -run 'KeyCeremony|Rotate|Repair|Grace' && TERM=dumb SSH_AUTH_SOCK= make gate-copy-freeze</automated>
  </verify>
  <acceptance_criteria>
    - A render test in rotate mode with the reachable-not-uploaded outcome asserts the outcome row and exactly one additional hint line naming the provider host.
    - A render test in repair mode asserts the grace hint constant is absent from the rendered body.
    - A render test asserts the archived key path line is present in rotate mode and absent in repair mode.
    - A model test asserts the rotate action is reduced only after the typed commit message arrives, and not on ceremony confirmation alone.
    - A model test asserts a commit message carrying an error renders the failure state with the restored paths and reduces no action.
    - A render test asserts the ceremony's beat count equals the create flow's beat count.
    - A render test with maximal fixture values asserts the rendered frame height equals the fixed frame height in both modes.
    - `make gate-copy-freeze` passes with the grace-hint and archive-path constants listed.
  </acceptance_criteria>
  <done>Rotate and repair render on the existing gate and ceremony screens, differing only by the two additions D-08 and D-06 budget for.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind | Notes |
|---|---|---|
| `tuikit.RotateIdentity{Name, Backup, ArchivedKeyPath}` | exported action | distinct from `NewKey` |
| `tuikit.Backend.KeyActionFor(name string) string` | interface method | state-driven routing |
| `tuikit.Backend.DeletePlan(name, scope string) DeletePlanView` | interface method | one plan value for both delete screens |
| `tuikit.Backend.KeyCeremonyPlan(name, mode string) KeyCeremonyView` | interface method | ceremony facts |
| `tuikit.Backend.CommitRotate` / `.CommitNewKey` | interface methods | async write seams |
| `tuikit.KeyCommitMsg{Mode, Backups, Restored, ArchivedKeyPath, Err}` | exported struct | typed async result |
| `tuikit.DeletePlanView`, `tuikit.UnmanagedHitView`, `tuikit.KeyCeremonyView` | exported structs | render DTOs |
| `paneActions`, `paneKeyCeremony` | identPane values | new panes |
| `identitiesModel.actionsFocus`, `.keyCeremonyMode`, `.keyCommitPending` | struct fields | pane state |
| grace-hint, archive-path, downgrade-note, scan-disclaimer copy constants | package constants | registered in the copy-freeze gate |
| Makefile `gate-copy-freeze` string list | extended target | freezes this plan's new copy |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| domain plan value → rendered confirm screen | the screen is the user's only description of an irreversible act |
| render layer → write trigger | an optimistic reduce would report a write that never happened |
| frozen approved design → new conditional elements | unbudgeted additions can push content out of the fixed frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-26 | Repudiation | confirm screen describing a different delete than the one performed | high | mitigate | Both delete screens render from the single `DeletePlanView` the domain plan produced; the UI no longer invents target or backup paths. |
| T-05-27 | Repudiation | optimistic success shown before the write | high | mitigate | Actions are reduced only from the typed commit message; a model test asserts confirmation alone reduces nothing. |
| T-05-28 | Tampering | copy drift away from the approved contract | medium | mitigate | Every new string is a constant registered in the copy-freeze gate, which fails the build when a string disappears. |
| T-05-29 | Denial of Service | conditional additions overflowing the fixed frame | medium | mitigate | A maximal-fixture render test asserts the frame height for the confirm screen and both ceremony modes. |
| T-05-30 | Information Disclosure | scan hit lines rendered on screen | low | accept | The user's own configuration text shown back to the user locally; private key files are not scan sources. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
REQ | MGR-04 | Clone entry point | 05-05, 05-06 | COVERED | Menu row here; derivation and pre-fill in 05-05.
REQ | MGR-05 | New key distinct from rotate | 05-03, 05-06 | COVERED | One row, state-routed.
REQ | MGR-06 | Delete choice with backup | 05-04, 05-06 | COVERED | Screens here.
REQ | KEY-05 | Rotate re-points and re-runs the test | 05-03, 05-06, 05-07 | COVERED | Ceremony here.
REQ | KEY-07 | New key for an existing identity | 05-03, 05-06, 05-07 | COVERED | Repair mode here.
RESEARCH | Pitfall 3 | No distinct rotate action exists | 05-06 | COVERED | Task 1, own task and commit.
CONTEXT | D-05 | One entry point routing to two ceremonies | 05-06 | COVERED | Task 1.
CONTEXT | D-06 | Archive path is a first-class backup-notice line | 05-06 | COVERED | Task 3.
CONTEXT | D-08 | Grace hint extends the existing outcome row | 05-06 | COVERED | Task 3.
CONTEXT | D-09 | Removal note folded into the existing file list | 05-06 | COVERED | Task 2.
CONTEXT | D-11 | Precise confirm copy, never overclaiming | 05-06 | COVERED | Task 2.
CONTEXT | D-12 | Shared-key downgrade note | 05-06 | COVERED | Task 2, plural-safe.
CONTEXT | D-13 | Scan warning, advisory | 05-06 | COVERED | Task 2, with the backstop-verified zero and many cases.
UI-SPEC | zero-one-many (siblings) | Confirm whether more than one sibling is possible | 05-06 | COVERED | Resolved plural; comma-joined single line.
UI-SPEC | zero-one-many (hits) | Backstop-verified at PTY time | 05-06, 05-09 | COVERED | Render tests here; planted-fixture PTY proof in 05-09.
UI-SPEC | long-text | Matched-line truncation width | 05-06 | COVERED | Resolved: existing preview clip convention, no new rule.
</source_audit>

<verification>
Run `make test`, `make lint`, and `make gate-copy-freeze`. Confirm `go test ./internal/dummytui/ -run TestNoBackendAllowlist` passes and `go list -deps ./internal/tuikit` still reports no first-party backend import — this plan adds five Backend methods and three DTOs, which is exactly when that boundary is most at risk.
</verification>

<success_criteria>
- Rotate is a first-class action in the shared vocabulary, never a flag on another action.
- The approved four-row action menu exists and routes its single key row by classified state.
- Every delete and ceremony addition renders in its budgeted slot, inside the fixed frame, from a domain-supplied plan value, with its copy frozen.
</success_criteria>

<output>
Create `.planning/phases/05-identity-manager/05-06-SUMMARY.md` when done, recording the plural-sibling and long-line-clipping resolutions and listing every string added to the copy-freeze gate.
</output>
