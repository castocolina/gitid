---
phase: 05-identity-manager
plan: 09
type: execute
wave: 9
depends_on: ["05-08"]
files_modified:
  - e2e/identity_manager_pty_e2e_test.go
  - e2e/harness_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - .planning/design/identity-manager/visual-divergence-allowlist.txt
  - Makefile
autonomous: true
requirements: [MGR-01, MGR-03, MGR-04, MGR-05, MGR-06, MGR-07, SHELL-01, SHELL-02, KEY-05, KEY-07, DLV-04, DLV-06]
estimate:
  tokens: 104000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every approved identity-manager state is driven by raw keystrokes in the COMPILED real binary at the fixed frame geometry, including the two ceremony modes the phase adds (DLV-06)."
    - "Every state's required fields are asserted DIRECTLY against the real binary's decoded frame from the FIELDS.md manifest — an independent backstop that does not depend on the real-versus-dummy diff, because both binaries share `internal/tuikit`'s render stack and a defect present in both is invisible to that diff (review R-26)."
    - "Raw mouse clicks focus the clone name field and both delete-choice options, proving the whole rendered row is the hit target."
    - "The first-run empty state is driven from an empty sandbox home and renders the frozen empty copy plus its call to action — the designed empty state, never a blank list."
    - statement: "The unmanaged-reference warning is verified at PTY time against a sandbox with two or more planted hits, and against a sandbox with none — the zero case renders no block at all — and each hit is asserted by its FILE and LINE identity, not by a bare count (review R-27)."
      verification: backstop
    - "The list fixture asserts all EIGHT taxonomy labels across the two health axes and the SEVEN reachable collapsed row words, per the resolution frozen in plan 05-01 — it does not claim eight distinct row words, which the production classifier cannot produce (review R-06)."
    - "A paired workflow drives the compiled real binary and the live compiled dummy through the same semantic checkpoints and classifies every difference as an improvement or a defect; an unclassified difference fails and any defect fails (DLV-04)."
    - "No browser, Chromium, HTML route, or pixel comparison participates in Phase 5 acceptance — the live dummy is the only parity reference."
    - "The repeatable visual gate includes the Phase 5 states, fails closed on a missing or empty required state, and has a negative control proving a single mutation is detected."
    - "Every divergence entry names its checkpoint, its region, its classification, its scoped decision reference, and its rationale; stale entries are rejected."
    - "The full gate battery is green at phase close: build, race tests, lint across every build tag, end-to-end, copy freeze, the no-backend boundary, and the visual gate."
  artifacts:
    - path: "e2e/identity_manager_pty_e2e_test.go"
      provides: "per-state raw-keystroke PTY coverage of the compiled real binary"
    - path: ".planning/design/identity-manager/visual-divergence-allowlist.txt"
      provides: "the named, decision-cited classification of every real-versus-dummy difference"
    - path: "cmd/gitid/gate_visual_regression_test.go"
      provides: "Phase 5 registration in the repeatable fail-closed gate"
  key_links:
    - from: "e2e/identity_manager_pty_e2e_test.go"
      to: "cmd/gitid and cmd/gitid-dummy"
      via: "BuildBinary and BuildDummyBinary PTY sessions at the fixed geometry"
      pattern: "BuildBinary|BuildDummyBinary|startPTYAt"
    - from: "cmd/gitid/gate_visual_regression_test.go"
      to: ".planning/design/identity-manager/visual-divergence-allowlist.txt"
      via: "fails on an unclassified or defect difference"
      pattern: "classification|defect|ux-improvement"
---

<objective>
Close Phase 5 with compiled-binary evidence for every manager state and a fail-closed gate that keeps it true.

Purpose: this project's acceptance mechanism for a TUI surface is a raw-keystroke PTY session against the compiled binary plus a semantic comparison with the live compiled dummy — the same shape Phase 4 established and the same shape the roadmap's fifth success criterion names. Phase 5 adds two ceremony modes and four conditional screen elements that no approved golden anticipated, so each needs either PTY coverage or a named, decision-cited divergence entry.
Output: Per-state PTY coverage, a paired real-versus-dummy comparison, a registered gate with a negative control, and a green full battery.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ONESHOT.md
@.planning/phases/05-identity-manager/05-CONTEXT.md
@.planning/phases/05-identity-manager/05-UI-SPEC.md
@.planning/design/identity-manager/FIELDS.md
@.planning/phases/04-git-configuration-screen/04-04-PLAN.md
@.planning/phases/04-git-configuration-screen/04-04-SUMMARY.md
@.planning/phases/05-identity-manager/05-06-SUMMARY.md
@.planning/phases/05-identity-manager/05-07-SUMMARY.md
@.planning/phases/05-identity-manager/05-08-SUMMARY.md

<authority>Every bare D-NN citation means `05-CONTEXT.md`; Phase 4 decisions are cited as CTX-D-NN and this phase's UI-SPEC additions as UI-D-NN, matching the scoped-reference convention plan 04-04 established. This is a terminal-only phase: no browser lane participates in acceptance.</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive every manager state in the compiled real binary (DLV-06)</name>
  <files>e2e/identity_manager_pty_e2e_test.go, e2e/harness_test.go</files>
  <read_first>
    - e2e/identity_manager_pty_e2e_test.go as landed by plan 05-01
    - e2e/git_configuration_pty_e2e_test.go (the Phase 4 per-state suite this one mirrors, including its decoded-frame coordinate helpers and its mouse-click cases)
    - e2e/harness_test.go (SandboxHome, BuildBinary, FakeSSHDir and its mode selection, FakeGitDir)
    - e2e/ui_pty_e2e_test.go lines 68-260 (ptySession, sendKey, snapshot, waitFor, close)
    - .planning/design/identity-manager/FIELDS.md (the eight approved states and their required fields)
    - .planning/phases/05-identity-manager/05-UI-SPEC.md the Approved Base States table and the Scoped Additions section
  </read_first>
  <behavior>
    - Each of the eight approved states is reached by raw keystrokes in the compiled real binary at the fixed geometry, and each state's required fields — read from the FIELDS.md manifest, not hand-copied into the test — are present in the decoded frame.
    - The manifest assertion runs against the REAL binary alone and passes or fails independently of the dummy comparison, so a defect shared by both render stacks still fails this suite.
    - The list-populated state is driven from a sandbox seeded to produce all eight taxonomy labels across the two health axes; each row's collapsed state word is asserted, and the set of distinct row words observed is asserted to be the seven reachable values.
    - The list-empty state is driven from an empty sandbox home and asserts the frozen empty copy and its call to action.
    - The detail state targets an SSH-only identity and asserts the Git-absence note with no author line.
    - The action menu is opened by its allocated key and its four rows are asserted in order.
    - The key ceremony is driven in both modes: rotate for a healthy identity asserting the grace hint, and repair for a key-missing identity asserting its absence.
    - The delete-choice, confirm-destructive, and backup-notice states are driven for both scopes, and the everything scope is driven twice: once over a sandbox with two planted unmanaged references and once over a sandbox with none.
    - Mouse clicks focus the clone name field and each delete-choice option.
    - After each write ceremony, the sandbox filesystem is asserted, not only the rendered frame.
  </behavior>
  <action>
Grow the file plan 05-01 started into the full per-state suite, following the Phase 4 suite's structure so a reader moving between them finds the same shape.

Add sandbox seeding helpers to `e2e/harness_test.go` for the fixtures this suite needs: an empty home, a home producing all eight taxonomy labels across the two axes, a home with two identities sharing one key, a home with two identities on one provider, and a home with planted unmanaged references in hand-written regions. Keep each helper's intent in its name so a future phase can reuse them.

**Add the independent manifest backstop (review R-26).** Both compiled binaries render through the same `internal/tuikit` stack, so Task 2's real-versus-dummy diff cannot detect a layout, label, or content defect present in BOTH. This suite is the countermeasure, and it must not lean on the diff. For each approved state, read that state's required-field list from `.planning/design/identity-manager/FIELDS.md` at test time (parse the manifest; do not transcribe the field names into the test source, or the test stops tracking the design) and assert every required field is present in the REAL binary's decoded frame. Add a negative control: run the same assertion against a frame with one required field deleted and assert it fails. Record in the file's header comment that this assertion is the shared-renderer backstop and must never be weakened to a dummy comparison.

Drive every state with raw bytes, never by calling model functions. Use the decoded-frame coordinate helpers for mouse cases, exactly as the Phase 4 suite does, so the click assertions prove the real terminal decoding path.

For each ceremony, assert on the sandbox filesystem after the receipt renders: the archived key path after a rotation, the absence of an archive entry after a repair, the surviving allowed_signers line after a Git-only delete, and the key copy path after a delete-everything.

**Cover the D-13 backstop with deterministic, identity-asserted fixtures (review R-27).** Two cases: the planted-hits sandbox and the clean sandbox. An exact hit COUNT is brittle — an alias can legitimately occur in several scanned artifacts, and a count assertion breaks for the wrong reason when a fixture grows. So control the fixture exactly and assert identities: the seeding helper documents the complete set of scan sources and plants each reference at a KNOWN file and line, and the test asserts the rendered hit lines name exactly those (file, line) pairs as a set, plus the disclaimer. The clean sandbox asserts neither the disclaimer constant nor any hit line appears. 05-UI-SPEC.md marks this consideration as backstop-verified rather than spec-verified, which means the PTY run is the evidence, not the render test alone.

**Align the list fixture with plan 05-01's frozen taxonomy resolution (review R-06).** Seed identities producing all eight labels across `IdentityState` and `KeyState`, and assert: every one of the eight labels is observable through the `--json` read surface for the same sandbox, and every rendered row's state word matches `ClassifyState`'s collapsed answer for its identity. Do NOT assert eight distinct row words — `key-used-both` is folded into `complete` by documented design. Orphan keys belonging to no identity appear only under `unused_keys` in the JSON and have no manager row in Phase 5; assert their ABSENCE from the rendered list so a future phase adding them is a deliberate change.

Do not start, invoke, or depend on any browser tool anywhere in this suite.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 600s ./e2e -run '^TestIdentityManager_'</automated>
  </verify>
  <acceptance_criteria>
    - A case exists for each of the eight approved states and each asserts that state's required fields, parsed from `FIELDS.md` at test time rather than transcribed into the test source.
    - A negative control asserts the manifest assertion FAILS when one required field is removed from a captured frame, proving the backstop is not vacuous.
    - The list case asserts all eight taxonomy labels are observable in the same sandbox's `--json` output, and that the distinct rendered row words are the seven reachable values.
    - The list case asserts an orphan key seeded into the sandbox appears under `unused_keys` and does NOT appear as a rendered manager row.
    - The rotate case asserts the grace hint is present and the repair case asserts it is absent.
    - The planted-hits case asserts the rendered hit lines name exactly the planted (file, line) pairs as a set, plus the disclaimer; the clean case asserts neither appears.
    - Mouse cases assert focus moves to the clone field and to each delete-choice option after a click anywhere on the rendered row.
    - Each write case asserts sandbox file state after the receipt.
    - No test in the file references a browser, a headless browser revision, or an HTML route.
  </acceptance_criteria>
  <done>Every approved manager state and both new ceremony modes are proven in the compiled real binary with raw input and filesystem evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Paired compiled-real versus live-dummy comparison with a decision-cited divergence registry (DLV-04)</name>
  <files>e2e/identity_manager_pty_e2e_test.go, .planning/design/identity-manager/visual-divergence-allowlist.txt</files>
  <read_first>
    - e2e/git_configuration_pty_e2e_test.go the paired comparison case plan 04-04 added
    - .planning/design/git-screen/visual-divergence-allowlist.txt (the entry format, its predicates, and its decision-reference convention)
    - internal/screenshot/createflow.go lines 186-260 and 640-780 (uxRegionDifference, uxRegionDifferenceScoped, validRegionPredicate, regionPredicateSatisfied, validDecisionRef, ValidateScreenSpecs)
    - internal/dummytui/data.go the identity-manager fixture rows and action-target constants
    - .planning/phases/05-identity-manager/05-UI-SPEC.md the Scoped Additions section (the complete list of permitted divergences)
    - .planning/phases/04-git-configuration-screen/04-REVIEW-FIX.md the Resolution section (the accepted scope of the shared-renderer limitation)
  </read_first>
  <behavior>
    - One semantic checkpoint script drives both compiled binaries at the fixed geometry.
    - The comparison inventories row order, field labels, controls, defaults, state order, ceremony beats, and required visible content.
    - Every unequal or one-sided checkpoint consumes exactly one registry entry naming its checkpoint, region, classification, scoped decision reference, and rationale.
    - A difference with no entry fails the comparison.
    - An entry naming a checkpoint that no longer differs fails as stale.
    - Any entry classified as a defect fails.
    - Each entry's decision reference resolves to a real decision in this phase's context or UI specification.
  </behavior>
  <action>
Add the paired case and its registry, reusing the Phase 4 mechanism rather than inventing a second one.

Pair two real PTY sessions, one for each compiled binary, and capture normalized semantic checkpoints instead of comparing terminal escape bytes. Reuse the normalization helpers the Phase 4 case already established.

Create the registry file with one entry per expected divergence. The complete permitted set is the Scoped Additions section of this phase's UI specification: the rotate ceremony's grace hint and archive path line, the repair ceremony's plainness, the shared-key downgrade note, the unmanaged-reference warning block, the provider-rewrite removal target folded into the existing list, the clone review flag on the two author fields, and the action menu becoming a real pane in the live render stack. Every entry cites its scoped decision reference. Any difference not on that list is a defect by construction and must be fixed rather than registered.

Record in the file's header the accepted limitation carried from Phase 4: both binaries render through the same shared render stack, so this comparison catches wiring and content differences, not a defect inside the shared renderer itself. That was reviewed and accepted as a scoped divergence, and stating it here prevents a future reader from mistaking the gate for something stronger than it is.

Reject stale entries and fail on any defect classification, matching the Phase 4 case's behavior exactly.

Read no HTML route, no browser capture, and no image file anywhere in this task.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 600s ./e2e -run '^TestIdentityManager_CompiledRealVsLiveDummyPTY$'</automated>
  </verify>
  <acceptance_criteria>
    - The paired case passes with zero defect classifications and zero unclassified differences.
    - A negative control asserts the case fails when an entry is removed while its difference persists.
    - A negative control asserts the case fails when an entry names a checkpoint that no longer differs.
    - Every entry's decision reference is validated against the accepted reference format and resolves to a decision in this phase's artifacts.
    - The registry's header states the shared-renderer limitation and cites its Phase 4 acceptance.
    - The test file contains no reference to a browser or an image capture.
  </acceptance_criteria>
  <done>Every real-versus-dummy difference on the manager surface is named, classified, and decision-cited, and the comparison fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register Phase 5 in the repeatable gate and run the full battery</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_test.go, cmd/gitid/gate_visual_regression_test.go, Makefile</files>
  <read_first>
    - internal/screenshot/createflow.go lines 90-260 and 540-780 (ScreenSpec, ScreenSpecRegistry, RequiredScreenSpecs, ScreenAppliesToSurface, ValidateCapturedState, ValidateScreenSpecs, ValidateScreenSpecRegistry)
    - internal/screenshot/createflow_regions.go lines 27-140 (RegionName, ExtractRegion, AllRegionNames) and the extractor helpers
    - cmd/gitid/gate_visual_regression_test.go (the gate, its required-state inventory, and its existing negative controls)
    - Makefile the gate-visual-regression, test, test-e2e, lint-tagged, and gate-no-backend-files targets
    - .planning/phases/04-git-configuration-screen/04-04-SUMMARY.md (how Phase 4 registered its states without disturbing Phase 3 coverage)
  </read_first>
  <behavior>
    - The repeatable gate covers the Phase 5 manager states in addition to the existing Phase 3 and Phase 4 coverage.
    - The gate fails closed when a required state is missing or captures empty text.
    - The gate rejects stale, overly broad, unclassified, and defect entries.
    - A negative control proves a single mutation in one region is detected.
    - The gate remains deterministic and read-only outside temporary directories.
    - The end-to-end target runs the paired comparison.
    - The full battery is green: build, race tests, tagged lint, end-to-end, copy freeze, the no-backend boundary check, and the visual gate.
  </behavior>
  <action>
Register the new states and regions, then run everything.

Extend the screen-spec registry with one entry per Phase 5 manager state, and extend the region vocabulary with the regions this surface needs — the identity list rows, the detail SSH section, the detail Git section, the detail findings section, the action-menu rows, the delete-choice options, the confirm warning block, and the backup path list. Add an extractor for each new region alongside the existing ones, following their line-scanning shape.

Extend the gate to consume the new registry alongside the existing ones without disturbing Phase 3 or Phase 4 coverage. Generalize the decision-reference validation only as far as this phase needs: it must accept the scoped references this phase uses in addition to those already accepted.

Add negative controls for a missing state, a stale classification, and cross-registry leakage, mirroring the ones Phase 4 added.

State exactly in the Makefile comments which target invokes which gate, so the division stays legible: the visual gate target runs the semantic screen gate, the end-to-end target runs the paired compiled PTY workflow.

**Convert the previously-planned manual walk into automated assertions (review R-28).** The earlier revision of this plan declared `autonomous: true` while carrying a manual human-verification block describing a manual walk of the manager — a direct contradiction, and a second human stop that DLV-08 does not permit (the phase's one hard stop is Phase 2's design approval, already recorded). The three things that walk was meant to confirm are all mechanically checkable, so assert them in the PTY suite instead:

  - "every receipt names real paths that exist on disk" — after each ceremony's receipt renders, parse every filesystem-looking token out of the decoded receipt frame and `os.Stat` each one in the sandbox, asserting each exists. Add a negative control asserting the helper fails for a fabricated receipt naming a non-existent path.
  - "the delete confirm never claims the key cannot be recovered when a copy exists" — assert that when `DeletePlanView.KeyCopyPath` is non-empty, the rendered confirm frame does NOT contain the cannot-be-undone sentence on the same block as the key line, and DOES contain the copy path.
  - "nothing reported success without doing the work" — already covered by every write case's post-receipt filesystem assertion; add an explicit cross-check that each ceremony's success receipt is only reachable after the corresponding file change is observable on disk.

Phase-level human acceptance remains where the workflow already puts it: `/gsd-verify-work` at phase close, outside this plan. Record that in the summary so a reader does not think the check was dropped.

Then run the full battery and record each command and its RESULT LINE in the summary. Record the terminal status line, the test count, and the wall-clock duration for each command — not unrestricted output, which carries incidental sandbox paths and noise into a tracked artifact (review R-29-LOG).
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= make gate-visual-regression && TERM=dumb SSH_AUTH_SOCK= make test && TERM=dumb SSH_AUTH_SOCK= make lint && TERM=dumb SSH_AUTH_SOCK= make test-e2e && TERM=dumb SSH_AUTH_SOCK= make gate-no-backend-files</automated>
  </verify>
  <acceptance_criteria>
    - The screen-spec registry validation passes with the Phase 5 entries present.
    - A negative control asserts the gate fails when one required state is absent from the capture set.
    - A negative control asserts the gate fails when one region's captured text is mutated by a single character.
    - A negative control asserts the gate fails when a classification entry is stale.
    - A receipt-path test parses every filesystem-looking token from each ceremony's receipt frame and asserts each exists in the sandbox; a negative control asserts the helper fails for a fabricated non-existent path.
    - A render test asserts the delete confirm's cannot-be-undone sentence is not applied to the key line whenever a key copy path is present, and that the copy path is rendered.
    - The plan declares no manual human-verification block and `autonomous: true` holds; the summary records that phase-level human acceptance happens through `/gsd-verify-work` at phase close.
    - The Makefile comments name which target runs which gate.
    - The summary records, per battery command, the terminal status line, the test count, and the wall-clock duration — not unrestricted output.
  </acceptance_criteria>
  <done>Phase 5 is registered in the repeatable fail-closed gate, the manual walk is fully automated, and the full battery is green with recorded evidence.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind | Notes |
|---|---|---|
| per-state manager PTY cases | test cases | one per approved state plus both ceremony modes |
| sandbox seeding helpers in `e2e/harness_test.go` | test helpers | empty, eight-taxonomy, shared-key, shared-provider, planted-references |
| paired real-versus-dummy PTY case | test case | DLV-04 semantic comparison |
| `.planning/design/identity-manager/visual-divergence-allowlist.txt` | new registry file | decision-cited classifications |
| Phase 5 screen specs and region names | registry entries | added to the existing screenshot registry |
| Phase 5 region extractors | Go funcs | list rows, detail sections, menu rows, delete options, confirm block, backup list |
| gate negative controls | test cases | missing state, single-character mutation, stale entry, cross-registry leakage |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test harness → compiled binaries | evidence must come from production wiring, not a fixture model |
| divergence registry → acceptance | a broad or stale entry can hide a real interaction defect |
| evidence capture → repository | captured frames must not carry secret material into tracked paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-41 | Spoofing | PTY evidence taken from the wrong binary | high | mitigate | Both binaries are built and identified explicitly; filesystem assertions are made only against the real binary's sandbox. |
| T-05-42 | Tampering | divergence registry hiding a defect | high | mitigate | Exact checkpoint and region matching, decision citations, stale-entry rejection, defect hard failure, and three negative controls. |
| T-05-43 | Information Disclosure | captured frames carrying key material | medium | mitigate | Sandboxes use synthetic keys under a temporary home; captures record rendered text, and a check asserts no capture contains an OpenSSH private-key header. |
| T-05-44 | Denial of Service | gate too slow or flaky to run routinely | medium | mitigate | The gate stays deterministic and read-only; the end-to-end timeout is raised deliberately with a recorded justification if the suite grows past it. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Roadmap success criterion 5: PTY drives every screen; automated review classifies every difference | 05-09 | COVERED | Tasks 1 and 2.
REQ | DLV-06 | At least one real PTY path per screen and state | 05-09 | COVERED | Task 1, per approved state plus both ceremony modes.
REQ | DLV-04 | Live real TUI versus approved Bubble Tea mockup, every difference classified | 05-09 | COVERED | Task 2, terminal-only.
REQ | MGR-01, MGR-03, MGR-07 | List, detail, per-identity health | 05-07, 05-09 | COVERED | PTY assertions over the real render.
REQ | MGR-04, MGR-05, MGR-06, KEY-05, KEY-07 | Clone, new key, delete, rotate | 05-03..05-08, 05-09 | COVERED | PTY plus filesystem evidence.
REQ | SHELL-01, SHELL-02 | Integrated app, five reachable views | 05-07, 05-09 | COVERED | Driven in the compiled binary.
CONTEXT | D-06, D-08, D-11, D-12, D-13, D-14 | Scoped additions to approved screens | 05-09 | COVERED | Each is a named registry entry or a PTY assertion.
UI-SPEC | zero-one-many (hits) | Backstop verification against a planted fixture | 05-09 | COVERED | Two PTY cases, planted and clean, asserted by (file, line) identity.
REVIEW | R-26 | Paired comparison cannot detect shared-renderer defects | 05-09 | COVERED | Independent FIELDS.md manifest assertion against the real binary, with a negative control.
REVIEW | R-06 | Eight taxonomy labels are not eight identity rows | 05-01, 05-09 | COVERED | Eight labels asserted via `--json` axes; seven collapsed row words asserted in the list.
REVIEW | R-27 | Exact two-hit assertions are brittle | 05-09 | COVERED | Deterministic seeding with asserted (file, line) identities instead of a count.
REVIEW | R-28 | Manual human walk conflicts with autonomous:true | 05-09 | COVERED | The walk's three claims are automated; the manual human-verification block is removed; UAT stays at `/gsd-verify-work`.
REVIEW | R-29-LOG | Recording literal full command output is noisy | 05-09 | COVERED | Status line + test count + duration per command instead.
REVIEW | R2-12 | Wave 7-9 token estimates are aggressive | 05-07, 05-08, 05-09 | COVERED | Estimate raised in this plan's frontmatter; rationale below. Task and plan boundaries unchanged.
</source_audit>

## Review resolutions (cycle 1)

| Finding | Severity | Resolution in this plan |
|---|---|---|
| Paired comparison cannot detect shared-renderer defects | MEDIUM (both) | Task 1 adds an INDEPENDENT manifest assertion parsed from `FIELDS.md` and run against the real binary alone, with a field-removal negative control; the file header forbids weakening it to a dummy comparison. Task 2's registry header still records the accepted limitation. |
| Eight "taxonomy labels" are not eight identity rows | MEDIUM | Aligned with plan 05-01's frozen resolution: eight labels asserted on the two axes through `--json`, seven collapsed row words asserted in the list, orphan keys asserted ABSENT from the manager list. |
| Exact two-hit assertions can be brittle | MEDIUM | The seeding helper documents the complete scan-source set and plants references at known file and line positions; the test asserts the (file, line) set, not a count. |
| Manual human walk conflicts with `autonomous: true` | LOW | The manual human-verification block is removed and its three claims are automated (receipt-path existence, key-copy phrasing, success-after-write). Phase-level UAT remains at `/gsd-verify-work`, which DLV-08 already accommodates. |
| Recording literal full command output risks noise | LOW | The summary records the status line, test count, and duration per command. |

## Review resolutions (cycle 2)

| Finding | Severity | Resolution in this plan |
|---|---|---|
| Wave 7-9 token estimates are aggressive; rollback and journal work is the likely slip (R2-12, plus Codex's LOW note that per-plan scope remains very large) | LOW | Agreed and acted on across all three waves rather than argued: 05-07 rises 116k → 132k, 05-08 96k → 108k, and this plan 92k → 104k. The rationale here is the PTY suite's real cost profile — per-state captures against a compiled binary at fixed geometry, plus the independent FIELDS.md manifest backstop and its negative control, are read-heavy in a way the cycle-1 projection under-weighted, and this plan also inherits the two new ceremony modes' states from 05-06. No plan is split and no wave is added: LEARNINGS L11 pins the nine-sequential-plan structure, and the correct response to an optimistic projection is an honest projection, not a restructure. The estimate stays `confidence: low` because the phase has no completed calibration samples yet. |

**Sequencing note (review, divergent view on 05-09):** xai-grok reads the shared-renderer limitation as a sequencing dependency — the paired gate cannot catch `identities.go` fabrication until 05-07 lands AND the dummy fixtures stay dummy. That ordering already holds (this plan is wave 9, `depends_on: ["05-08"]`, and 05-07 is wave 7). The divergence registry must not gain an entry for any remaining fabrication: 05-07 Task 3 removes the substituted hostname, port, and derived signing key, so a surviving fabrication is a defect to fix, never a divergence to classify.

<verification>
Run the full battery listed in Task 3 and record every command's literal output line in the summary. Confirm no target in the invoked command graph starts a browser, and confirm `go test ./internal/dummytui/ -run TestNoBackendAllowlist` and `go list -deps ./internal/tuikit` both still hold the render-stack boundary.
</verification>

<success_criteria>
- Every approved manager state and both new ceremony modes are proven in the compiled real binary with raw input.
- The paired comparison has zero defects and zero unclassified differences.
- The repeatable gate covers Phase 5, fails closed, and has negative controls proving it is not vacuous.
- The full battery is green with recorded evidence.
</success_criteria>

<output>
Create `.planning/phases/05-identity-manager/05-09-SUMMARY.md` when done, including the literal output line of every battery command and the complete divergence registry contents.
</output>
