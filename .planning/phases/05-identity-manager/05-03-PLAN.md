---
phase: 05-identity-manager
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - internal/identity/identity.go
  - internal/identity/identity_test.go
  - internal/identity/modes.go
  - internal/identity/rotate.go
  - internal/identity/rotate_test.go
  - internal/identity/repair.go
  - internal/identity/repair_test.go
  - internal/identity/modes_test.go
  - internal/identity/state.go
  - internal/identity/state_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
autonomous: true
cross_ai: true
requirements: [KEY-05, KEY-07, MGR-05]
estimate:
  tokens: 116000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`runPipeline` is decomposed into four named phases — gate, persist key, write artifacts, resolved test — so rotate and repair select the phases they need instead of re-running a monolith (review R-01)."
    - "Rotate persists the new key pair EXACTLY ONCE: the persist phase is invoked by rotate's own sequence and is not re-entered by the artifact-writing phase (review R-01)."
    - "Rotate archives the old key pair with MOVE semantics BEFORE the new key is persisted, so the canonical key path is vacated rather than overwritten (D-06)."
    - "Rotate appends the new signing line and KEEPS the old one, leaving two lines for the same email in the identity's allowed_signers block (D-07)."
    - "Rotate leaves the SSH `IdentityFile` value and the fragment's `user.signingkey` value textually unchanged, because the new key takes the same canonical path (D-08)."
    - "Rotate's result carries the grace-window facts (provider host, archived key path) so the ceremony can render the D-08 hint without re-deriving them in the UI layer."
    - "A rotation that fails at any writer restores every touched file and leaves the OLD key pair back at its canonical path — a failed rotation never leaves the identity keyless."
    - "`RotateResult` carries the archive paths for a failure at ANY step from the archive onward, INCLUDING a failure inside the archive step itself, because the paths are copied onto the result before the archive error is inspected (review R3-01)."
    - "The production archive seam is bound to a transaction's journal, not to the backend: `buildIdentityDeps` wires a closure that REFUSES to archive outside a transaction, and only `depsForTransaction(j)` produces one that can — so an archive copy no journal is watching cannot be created (review R3-01)."
    - "Repair NEVER writes to a key path that any other identity references: it targets this identity's OWN canonical path, and when that path is referenced by a sibling it fails closed with a typed error instead of overwriting the sibling's bytes (review R-02, D-05)."
    - "Repair never archives, never removes, and never reads any pre-existing key material, so a key shared with a sibling identity is never disturbed (D-05)."
    - "One menu entry routes to rotate or repair from the identity's classified state AND its key-owner count, with no new screen and no boolean flag on a shared action (D-05, and the FIELDS.md finding that only one key row exists)."
    - "Every new `identity.Deps` field is wired non-nil in the real composition root the moment it is added, enforced by the existing reflection guard."
  artifacts:
    - path: "internal/identity/identity.go"
      provides: "the four named pipeline phases runPipeline, Rotate and RepairKey all compose"
    - path: "internal/identity/rotate.go"
      provides: "RotateKey + RotateResult — the D-06/D-07/D-08 retirement ceremony core"
    - path: "internal/identity/repair.go"
      provides: "RepairKey + RepairKeyPath + ErrRepairTargetShared — the KEY-07/MGR-05 repair action"
    - path: "internal/identity/state.go"
      provides: "KeyAction + KeyActionFor — the D-05 rotate-versus-repair routing predicate"
  key_links:
    - from: "internal/identity/rotate.go"
      to: "keygen.MoveKeyPairToArchive and keygen.AppendAllowedSigners"
      via: "new injected Deps fields ArchiveKeyPair and AppendAllowedSigners"
      pattern: "ArchiveKeyPair|AppendAllowedSigners"
    - from: "internal/identity/rotate.go and internal/identity/repair.go"
      to: "internal/identity/identity.go pipeline phases"
      via: "explicit phase composition, never a second call to runPipeline"
      pattern: "persistStagedKey|writeArtifacts|preWriteGate"
    - from: "cmd/gitid/wiring.go buildIdentityDeps"
      to: "internal/keygen archive + append primitives"
      via: "non-nil seam wiring proven by the wiring_test reflection guard"
      pattern: "buildIdentityDeps"
    - from: "cmd/gitid/wiring.go depsForTransaction"
      to: "cmd/gitid/wiring.go mutationJournal.recordCreatedFile"
      via: "the archive seam's onCreated observer bound to THIS transaction's journal"
      pattern: "depsForTransaction|recordCreatedFile|errArchiveOutsideTransaction"
---

<objective>
Build the two key-lifecycle ceremonies D-05 splits apart — rotate (retirement) and new-key (repair) — as UI-free domain orchestration, on top of a `runPipeline` decomposed into phases they can each compose safely.

Purpose: `identity.Rotate` exists today as a correct SKELETON (staging, gate, four-writer pipeline) but its allowed_signers step REPLACES the block and it never archives the old key, directly contradicting D-06 and D-07. Cross-AI review found a second, harder defect: `runPipeline` itself calls `PersistKey` internally (`internal/identity/identity.go` — the persist happens between the pre-write gate and the four writers), so any rotate that archives and then calls `runPipeline` unchanged persists the new key a SECOND time with archive/rollback ordering undefined. The repair path does not exist at all, and the naive reading of "repair at the canonical path" would overwrite a sibling identity's key bytes when the key is shared.
Output: Four named pipeline phases, two orchestration functions that compose them, a state-and-ownership router between them, and the composition-root wiring for the two new injected seams.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@.planning/phases/05-identity-manager/05-CONTEXT.md
@.planning/phases/05-identity-manager/05-RESEARCH.md
@.planning/phases/05-identity-manager/05-02-SUMMARY.md

<authority>Every bare D-NN citation means `05-CONTEXT.md`. 05-RESEARCH.md's Open Question 1 (repair signer semantics) is RESOLVED by this plan's "Planner resolution: repair signer semantics" section below — it is no longer an open question and no longer a human checkpoint.</authority>
</context>

## Planner resolution: repair signer semantics (05-RESEARCH Open Q1 / assumption A1)

**Resolution: APPEND. Repair uses the same `keygen.AppendAllowedSigners` writer rotate uses.**

The previous revision of this plan carried a `checkpoint:decision` with `gate="blocking"` for this question. Cross-AI review raised two things about it, and both are honored here:

1. **DLV-08 conflict (review R-24).** `.planning/REQUIREMENTS.md` DLV-08 states the autonomous build loop runs unattended except for ONE hard stop — design approval, already recorded in Phase 2. A second blocking human checkpoint in wave 3 breaks that contract. The reviewer's recommendation was explicit: resolve the semantics during planning and remove the extra checkpoint. That is what this section does. The checkpoint is REMOVED and this plan is `autonomous: true`.

2. **The research recommendation was based on a factual error.** 05-RESEARCH.md recommended REPLACE on the grounds that "a stale line pointing at an absent key verifies nothing today". That is wrong. An `allowed_signers` entry verifies a signature from the PUBLIC key blob recorded in the line; the private key's presence on disk is irrelevant to verification. A line whose private key has been archived, rotated away, or deleted still verifies every commit that was signed with it.

Given that, APPEND wins on every axis that matters here:

- **Correctness:** historic `git log --show-signature` verification keeps working after a repair, exactly as D-07 requires it to after a rotation.
- **Reversibility:** appending is the reversible choice. A surplus line can be pruned later (Phase 8's fixer already owns that story, and 05-CONTEXT.md's Deferred Ideas names it). A replaced line is gone from the live file with no signal to the user that historic verification just stopped working; a timestamped backup exists but nothing prompts anyone to look at it.
- **Consistency:** one rule across both key ceremonies. `AppendAllowedSigners` is idempotent on a repeated line (plan 05-02 Task 2), so a repair that regenerates the same public line is a no-op rather than a duplicate.
- **Consistency with D-07's own reasoning:** D-07 is scoped to rotate in CONTEXT.md, but its stated rationale ("same email, old blob, so `git log --show-signature` keeps verifying pre-rotation commits") applies verbatim to repair. Extending it is the conservative reading of the locked decision, not a new decision.

**Cost accepted:** a repaired identity whose old key never existed accumulates one line that verifies nothing. That is bounded (one line per ceremony), invisible, and prunable in Phase 8. Recorded as an accepted trade in `05-03-SUMMARY.md`.

Carry this section verbatim into `05-03-SUMMARY.md` under a "Repair signer semantics" heading, and name the resolution in `RepairKey`'s doc comment, so the decision is traceable from the code and 05-RESEARCH.md's Open Question 1 is not re-opened.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decompose runPipeline into four named phases (review R-01)</name>
  <files>internal/identity/identity.go, internal/identity/identity_test.go, internal/identity/modes_test.go</files>
  <read_first>
    - internal/identity/identity.go the whole `runPipeline` implementation and its doc comment (note the `deps.PersistKey` call between the pre-write gate and the four writers — this is the defect)
    - internal/identity/identity.go lines 128-205 (the Deps struct and every field's contract) and the CreateResult struct
    - internal/identity/modes.go (Create, Reuse, AddAccount, Rotate — every current caller of runPipeline)
    - internal/identity/identity_test.go and modes_test.go (the existing coverage that must stay green unchanged — this task is behavior-preserving for all four current callers)
  </read_first>
  <behavior>
    - `Create`, `Reuse`, `AddAccount`, and the existing `Rotate` produce byte-identical results and identical seam call ORDER before and after the decomposition, proven by a call-order recording test.
    - The pre-write gate phase performs the clipboard copy, builds the signer line, runs the pre-write connectivity test, and renders the previews, and returns a partially-populated `CreateResult` plus the rendered host block.
    - The persist phase writes the staged key pair and is a guaranteed no-op when `staged.PrivPEM` is nil.
    - The artifact phase runs the four writers in the existing order and records each backup path on the result.
    - The resolved phase runs the post-write resolution test and records its result.
    - The artifact phase accepts the signer writer as an explicit argument, so a caller selects the replacing writer or the appending writer without a boolean on `Deps`.
    - Calling the persist phase twice for the same staged key is impossible by construction from the exported orchestrators, asserted by a recording seam that counts invocations across a full `Create`, a full `Rotate`, and a full `RepairKey`.
  </behavior>
  <action>
Split `runPipeline` into four unexported, individually-callable phases in `internal/identity/identity.go`, keeping `runPipeline` itself as their composition so `Create`, `Reuse`, and `AddAccount` are untouched:

  - `func preWriteGate(in CreateInput, staged StagedKey, deps Deps) (res CreateResult, hostBlock string, err error)` — everything `runPipeline` currently does BEFORE the `deps.PersistKey` call: the best-effort `CopyPub`, `keygen.AllowedSignersLine`, `deps.PreWrite` with its abort-on-Failure contract, and the preview rendering against the FINAL paths.
  - `func persistStagedKey(staged StagedKey, deps Deps) error` — exactly the current `if staged.PrivPEM != nil { deps.PersistKey(staged) }` branch and its error wrapping, and nothing else.
  - `func writeArtifacts(in CreateInput, hostBlock, signersLine string, res CreateResult, deps Deps, writeSigners signersWriter) (CreateResult, error)` — the four writers in their current order with their current error wrapping, recording each backup path on the result. Declare `type signersWriter func(path, identity, line string) (backupPath string, err error)` so the caller passes either `deps.WriteAllowedSigners` or an adapter over `deps.AppendAllowedSigners`. This is the explicit-parameter form review R-01 asks for in place of another boolean on `Deps`.
  - `func resolvedPhase(in CreateInput, res CreateResult, deps Deps) CreateResult` — the closing `deps.Resolved` call and its two result fields.

Rewrite `runPipeline` as `preWriteGate` → `persistStagedKey` → `writeArtifacts(..., deps.WriteAllowedSigners)` → `resolvedPhase`, and state in its doc comment that it is now ONE composition among several and that rotate and repair compose the same phases in different orders — and that no orchestrator may call `runPipeline` after performing its own persist, because that is the double-write defect this decomposition exists to make structurally impossible.

**Task 1 leaves `Rotate` calling `runPipeline`; Task 2 is what removes that call (review R2-09).** This task is behavior-preserving for all four current callers, `Rotate` included — its existing skeleton keeps the signature it has today and keeps going through `runPipeline`, and the call-order test below pins exactly that. Task 2 then changes `Rotate`'s signature, moves it to `rotate.go`, and composes the phases explicitly. An executor must NOT read Task 1's "existing `Rotate`" as the final ceremony, and must not try to satisfy Task 2's grep gate (no `runPipeline` reference in the rotate path) while working Task 1 — that gate is Task 2's acceptance criterion and is expected to be red until Task 2 lands. Record the same note in `05-03-SUMMARY.md`.

Do not change any observable behavior in this task. Prove that with a call-order test: wrap every `Deps` seam in a recorder, run `Create`, `Reuse`, and `AddAccount`, and assert the recorded sequence of seam names matches a literal expected sequence committed in the test. Write that test FIRST, against the pre-decomposition code, so it captures today's real order rather than the order you intend to produce.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/identity/... ./cmd/gitid/...</automated>
  </verify>
  <acceptance_criteria>
    - A call-order recording test asserts the exact seam sequence for `Create`, `Reuse`, and `AddAccount`, and passes both before and after the decomposition (commit the test first; record in the SUMMARY that it was green pre-refactor).
    - A test asserts that at the END of this task `Rotate` still reaches `runPipeline` and still has its pre-task signature, so the behavior-preserving claim is checked rather than assumed and the Task 1/Task 2 boundary is unambiguous (review R2-09). Task 2 deletes this test in the same commit that removes the call.
    - A test asserts `persistStagedKey` is invoked exactly ONCE during a full `Create` and exactly ONCE during a full `Rotate`, counted through a recording `Deps.PersistKey` seam.
    - A test asserts `persistStagedKey` is a no-op when `staged.PrivPEM` is nil, by asserting the recording seam count is zero.
    - A test drives `writeArtifacts` with an adapter signer writer and asserts the adapter, not `deps.WriteAllowedSigners`, received the call.
    - `go vet ./internal/identity/...` and `make lint` are clean, and the existing `internal/identity` suite passes unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">All four phases are unexported and every current caller keeps its signature; the decomposition is a compile-time-checked internal refactor with a call-order test pinning behavior equivalence.</reversibility>
  <done>The pipeline is four named phases, every existing caller behaves identically, and a recording test proves the key is persisted exactly once per ceremony.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rotate becomes a retirement ceremony — archive (move), append, grace facts (KEY-05)</name>
  <files>internal/identity/identity.go, internal/identity/rotate.go, internal/identity/rotate_test.go, internal/identity/modes.go, internal/identity/modes_test.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go</files>
  <read_first>
    - internal/identity/modes.go the existing `Rotate` skeleton and `rotateInput`
    - internal/identity/identity.go the four phases as landed by this plan's Task 1
    - internal/keygen/signers.go (WriteAllowedSigners, WriteAllowedSignersReplacing, and the AppendAllowedSigners added by plan 05-02)
    - internal/keygen/archive.go (ArchivedPair, CopyKeyPairToArchive, MoveKeyPairToArchive, RemoveArchivedPair, added by plan 05-02)
    - internal/sshconfig/include.go (ArchiveDir, added by plan 05-02)
    - cmd/gitid/wiring.go lines 166-313 (buildIdentityDeps — every field must stay non-nil) and lines 1868-2005 (commitCreateTransaction — the rollback ordering to mirror)
    - cmd/gitid/wiring_test.go's reflection guard over identity.Deps
    - .planning/phases/05-identity-manager/05-02-SUMMARY.md (the exact archive stamp convention chosen and the copy-versus-move split)
  </read_first>
  <behavior>
    - Rotating a healthy identity produces a new key pair at the SAME canonical private and public paths the identity already used.
    - The previous key pair is archived with MOVE semantics before the new material is persisted, and the archived paths are reported in the result.
    - The new key pair is persisted exactly once during the rotation, counted through a recording seam.
    - The identity's allowed_signers block ends up with two lines for the same email: the pre-rotation line first, the new line second.
    - The rendered SSH Host block's IdentityFile value and the fragment's signingkey value are byte-identical before and after the rotation.
    - The result carries the provider host and the archived private-key path so the caller can render the grace-window hint without re-deriving them.
    - A failure injected at the key-persist step restores the archived pair back to the canonical paths, leaves allowed_signers unchanged, and reports the archive paths it created so the caller can remove them.
    - A failure injected INSIDE the archive step — the second source removal — still returns a `RotateResult` carrying both archive paths, and the transaction's journal has already recorded both, so a rollback removes them and restores the canonical pair.
    - The archive seam obtained from the backend-wide `b.deps` refuses to archive; only a seam obtained from `depsForTransaction(j)` archives, and it records every archive copy in `j` before any source is removed.
    - Rotating twice yields three lines in the block and two archived generations, with no filename collision.
  </behavior>
  <action>
Move rotation into its own `internal/identity/rotate.go` and compose it from Task 1's phases.

Add two fields to `identity.Deps`:

  - `ArchiveKeyPair func(privPath, pubPath string) (archivedPriv, archivedPub string, err error)` — the composition root fills this from `keygen.MoveKeyPairToArchive` against `sshconfig.ArchiveDir(b.sshDir)`. Document on the field that it is the MOVE primitive, that delete-everything deliberately uses the COPY primitive through a different seam (plan 05-04), and that the two must not be collapsed. Document the error contract too, because a caller depends on it: **the seam returns whatever archive paths it created even when it returns an error**, so a partial move is still fully reportable.
  - `AppendAllowedSigners func(path, identity, email, pubLine string) (backupPath string, err error)` — used by BOTH key ceremonies per this plan's signer-semantics resolution.

Document each field with the D-NN it serves. Adding fields to `Deps` obliges the composition root to fill them immediately — that is the point of the reflection guard.

Define `type RotateResult struct { CreateResult; ArchivedPrivatePath, ArchivedPublicPath string; ProviderHost string }` and change `Rotate` to `func Rotate(existing Account, deps Deps) (RotateResult, error)`. `Rotate` has no production caller today (only its own tests), so this is a compile-checked refactor; update `modes_test.go` accordingly and leave `Reuse`/`AddAccount` untouched in `modes.go`.

Sequence rotation EXPLICITLY over the phases, never through `runPipeline`:

  1. `deps.Generate` into hermetic staging.
  2. `preWriteGate` — gate on the STAGED key before anything on disk moves.
  3. `deps.ArchiveKeyPair` — move the old pair out of the canonical paths. Copy the returned archive paths onto the result IMMEDIATELY — **before inspecting the seam's error, not after** — so a caller that must roll back always knows what to remove (this is what plan 05-07's journal consumes; reviews R-10 and R3-01).
  4. `persistStagedKey` — the new pair takes the vacated canonical paths. Exactly once.
  5. `writeArtifacts` with the APPEND signer writer adapter.
  6. `resolvedPhase`.

Write the ordering rationale as a comment on the function: staging and gating first means an archive only ever happens when a proven replacement is ready; archiving before persisting means the canonical path is vacated rather than overwritten (D-06); and the phase composition — not a call to `runPipeline` — is what guarantees the key is persisted once.

**On an error at step 3 OR LATER, return the populated `RotateResult` carrying the archive paths alongside the error (review R3-01).** Cycle 2 guaranteed this only from step 4 onward, which left the archive step itself — the one step that can fail with archive copies already on disk — as the single case where the caller got an error and no paths. `keygen.MoveKeyPairToArchive` returns its `ArchivedPair` together with an `ErrArchiveIncomplete`-wrapped error when a source removal fails, so the information exists; the domain simply has to not throw it away. Structure step 3 as: call the seam, assign both returned paths onto the result unconditionally, THEN check the error and return `(res, err)`. Write that ordering as a comment naming the reason, because the obvious `if err != nil { return RotateResult{}, err }` shape is exactly the bug. State in the doc comment that restoring the archived pair to the canonical paths is the CALLER's transaction responsibility (plan 05-07's `mutationJournal`), and that the domain's contract is to report precisely what it created so the caller can undo it — at EVERY step, with no gap. Add domain-level tests that inject a failing `PersistKey` and a failing `ArchiveKeyPair` (the latter returning both paths plus an error, exactly as the primitive does) and assert the returned result carries both archive paths in each case.

Populate `ProviderHost` from the account's existing `Provider` field — never re-derive it from the alias, which loses multi-label hosts (the WR-01 precedent already recorded in project decisions).

In `cmd/gitid/wiring.go`, fill both new `Deps` fields and bind the archive seam to a TRANSACTION, not to the backend. `AppendAllowedSigners` is the easy one: `buildIdentityDeps` fills it from `keygen.AppendAllowedSigners` against `b.allowedSigners`. The archive seam needs more, because of where its closure lives.

**Bind the archive observer to the transaction's journal, at the one place the closure is constructed (review R3-01).** `buildIdentityDeps(b)` runs ONCE, inside `newBackendForHome`, and its result is stored in `b.deps`. A closure built there has no transaction in scope and can never reach a journal — which is precisely why the reviewer found that plan 05-07's "the archive closures call `journal.recordCreatedFile`" was unimplementable as written. Close it here, in the task that first writes the closure, with three pieces:

  - **One seam constructor, two bindings.** Add `func (b *realBackend) archiveKeyPairSeam(onCreated keygen.CreatedFunc) func(privPath, pubPath string) (string, string, error)` holding the ONLY implementation: resolve `sshconfig.ArchiveDir(b.sshDir)`, build the project's UnixNano stamp, call `keygen.MoveKeyPairToArchive(dir, priv, pub, stamp, b.archiveRemove(), onCreated)`, and own the monotonic stamp retry described below. There is exactly one archive implementation; only its observer varies.
  - **The backend-wide binding REFUSES.** `buildIdentityDeps` fills `ArchiveKeyPair` with `b.archiveKeyPairSeam(func(string) error { return errArchiveOutsideTransaction })`, a new unexported sentinel. The field stays non-nil so the reflection guard is satisfied and its intent is preserved, but the value fails closed: an archive attempted with no journal watching returns an error before the copy, rather than creating an untracked archive entry. Document on the field that this refusal is deliberate and that `depsForTransaction` is the only way to obtain a working archive seam. A fail-open no-op observer here would reintroduce exactly the defect this fix exists to remove.
  - **`depsForTransaction` produces the working seam.** Add `func (b *realBackend) depsForTransaction(j *mutationJournal) identity.Deps` returning a shallow copy of `b.deps` with only `ArchiveKeyPair` rebound to `b.archiveKeyPairSeam(j.recordCreatedFile)`. `identity.Deps` is a value type, so this is a copy of the ONE wiring rather than a second wiring — say so in the comment, because plan 05-07's "do not build a second `Deps` value inside the commit path" rule means do not RE-WIRE, not do not re-bind one seam. Every transactional caller (this task's tests, and plans 05-07's `runRotate`/`runRepair`) passes `depsForTransaction(j)` to the domain, never `b.deps`.

Extend the existing `mutationJournal` with the created-file half it needs to receive those calls — it lives in this file already, so this task can land it rather than waiting for plan 05-07:

  - `func (j *mutationJournal) recordCreatedFile(path string) error` — records a path this transaction created, and returns a programming-error naming the path if it is already WATCHED. Recording a path already recorded as created is an idempotent no-op, because plan 05-07's lifecycle also records whatever the domain result names and the two sources will normally agree. It returns an `error` (not a panic and not a bare void) precisely so that it can be handed to `keygen.CreatedFunc` directly and so a refusal STOPS the archive instead of merely being noticed afterwards.
  - `watchFile` correspondingly rejects a path already recorded as created. The two sets are disjoint by construction because a path in both has no correct rollback outcome (this is review R2-08's contract, now enforced at the wave where the first recorder appears).
  - `restore()` removes every recorded created path AFTER restoring every watched file. Plan 05-07 extends this to directories and to mode restoration; this task ships the file half so a wave-3 rotation is already all-or-nothing.

Add a test-only `failArchiveRemoveAt func(path string) error` field on `realBackend`, consulted by `b.archiveRemove()` (which returns `os.Remove` when the field is nil), mirroring the existing `failCommitAt` injection precedent in this file. That is what lets a `cmd/gitid` test drive the second-source-removal failure through the REAL composition root rather than through a hand-built fake.

Confirm the reflection guard in `wiring_test.go` still passes and add a wiring test that drives a real rotation over a hermetic fake home through the REAL constructor, asserting both files on disk — a unit test with fakes never substitutes for the real-constructor proof in this project.

**`archiveKeyPairSeam` owns the stamp-collision retry, and this task tests it (review R2-05).** Plan 05-02 ships the primitive whose exclusive-create turns a same-nanosecond collision into a hard error that removes nothing; the single monotonic retry that consumes that error lives HERE, inside `archiveKeyPairSeam`, because this is the first task that writes the closure at all — plan 05-02 does not touch `cmd/gitid/wiring.go`. On a collision error the seam bumps the stamp monotonically and retries exactly once, then surfaces the failure. A collision leaves nothing behind and announces nothing, so the retry and the created-file observer do not interact. Make the clock injectable through the existing composition-root seam conventions so the collision is reproducible without racing a real nanosecond clock.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/identity/... ./cmd/gitid/... -run 'Rotate|Deps|Pipeline'</automated>
  </verify>
  <acceptance_criteria>
    - A test with a recording `Deps.PersistKey` seam asserts the invocation count after one `Rotate` is exactly 1.
    - A test asserts the archived private key exists inside the archive directory and the canonical private key path holds DIFFERENT bytes afterwards.
    - A test asserts the archive seam is invoked BEFORE the persist seam, using the call-order recorder from Task 1.
    - A test reads the allowed_signers file after rotation and asserts exactly two lines whose first field is the identity email, with the pre-rotation line at the lower index.
    - A test captures the rendered SSH Host block and the fragment before and after rotation and asserts the IdentityFile and signingkey lines are byte-identical.
    - A failure-injection test forces an error at key persistence and asserts the returned `RotateResult` carries both archive paths and that the allowed_signers file is byte-unchanged.
    - A domain test injects an `ArchiveKeyPair` seam that returns BOTH archive paths together with an error (the shape `keygen.MoveKeyPairToArchive` produces on a source-removal failure) and asserts `Rotate` returns a `RotateResult` carrying both paths alongside that error — the step-3 half of review R3-01, and the case cycle 2's "step 4 or later" wording left uncovered.
    - **The second-source-removal rollback regression (review R3-01).** A `cmd/gitid` test builds a real backend over a hermetic fake home, seeds an identity with a real key pair, creates a `mutationJournal`, watches both canonical key paths, sets `failArchiveRemoveAt` to fail on the SECOND source, and runs `identity.Rotate` with `b.depsForTransaction(j)`. It asserts, in this order: the call returned an error; the journal recorded BOTH archive paths (so they were discoverable BEFORE the failing removal, not after); `j.restore()` succeeds; the archive directory afterwards holds NO entry for this identity; and both canonical key paths hold their pre-transaction bytes. Assert the recorded paths, not merely that an error came back — an error alone is what the current plan already produced and is exactly what the reviewer found insufficient.
    - A test asserts `b.deps.ArchiveKeyPair` (the backend-wide binding) returns `errArchiveOutsideTransaction` and creates no archive entry, while the same call through `b.depsForTransaction(j)` succeeds and records its paths in `j` — the fail-closed proof that an untracked archive copy cannot be created.
    - A test asserts `recordCreatedFile` rejects an already-watched path and `watchFile` rejects an already-recorded created path, each naming the path (review R2-08's disjointness, enforced from the wave the first recorder appears).
    - A test asserts `restore()` removes every path passed to `recordCreatedFile` and restores every watched file's bytes, and that a rotation whose archive step recorded two paths leaves neither behind after a rollback.
    - A test rotates twice and asserts three signer lines and two distinct archived private-key paths.
    - `cmd/gitid/wiring_test.go`'s reflection guard passes with the two new fields present, and a real-constructor test drives a rotation end-to-end over a fake home through `b.depsForTransaction(newMutationJournal(b))` — not through `b.deps`, whose archive binding refuses by design; assert both files on disk and that the journal holds both archive paths afterwards.
    - A test forces the `ArchiveKeyPair` closure's first stamp to collide with an existing archive path and asserts it produces a second, DISTINCT archive path; a sibling test forces a second collision and asserts the error surfaces rather than looping. (This is plan 05-02's R-21 contract, tested in the plan that writes the closure — review R2-05.)
    - A grep gate over `internal/identity/rotate.go` with comment lines stripped (`grep -v '^\s*//'` before the count) asserts zero pipeline-composition call sites survive in the rotate path — comments explaining WHY rotate no longer composes that way are expected and must not fail the gate. Task 1's temporary "rotate still uses the old composition" test is deleted in the same commit that removes the call (review R2-09).
  </acceptance_criteria>
  <reversibility rating="costly">Changing `Rotate`'s signature and adding two `Deps` fields is a compile-time-checked internal refactor, but the on-disk consequence (an accumulating second signer line and an archived key generation per rotation) is user-visible and only prunable by the Phase 8 fixer.</reversibility>
  <done>Rotation archives with move semantics, persists once, appends, and re-points, and reports exactly what it created so a failed rotation can be fully undone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repair with a non-shared key target, plus the state-and-ownership router (KEY-07, MGR-05)</name>
  <files>internal/identity/repair.go, internal/identity/repair_test.go, internal/identity/state.go, internal/identity/state_test.go</files>
  <read_first>
    - internal/identity/state.go lines 14-236 (the 8 State constants, Problem constants, IdentityHealth, Classify, ClassifyState and its documented precedence — note that ClassifyState's key switch omits StateKeyUsedBoth by design)
    - internal/identity/rotate.go as landed by this plan's Task 2
    - internal/identity/identity.go the four phases from Task 1 and the Deps contract
    - internal/keygen (KeyPaths — the canonical `id_ed25519_<name>` derivation)
    - .planning/design/identity-manager/FIELDS.md lines 62-73 (the action-menu's SINGLE key row, `action_new_key`)
    - .planning/phases/05-identity-manager/05-UI-SPEC.md the "New-key repair ceremony" section
    - this plan's "Planner resolution: repair signer semantics" section
  </read_first>
  <behavior>
    - Repair targets the key path derived from THIS identity's own name via `keygen.KeyPaths`, never `Account.KeyPath`.
    - Repairing a `key-missing` identity generates a fresh key at that target and re-points the four artifacts to it.
    - Repairing an identity that currently points at a SIBLING's key path writes the new key at this identity's own canonical path and leaves the sibling's key file bytes and the sibling's Host block IdentityFile value untouched.
    - When this identity's own canonical path is itself referenced by another identity, repair fails closed with `ErrRepairTargetShared` naming every other referencing identity, and writes nothing.
    - Repair never calls the archive seam and never reads, stats, backs up, or removes any pre-existing key file.
    - Repair persists the new key exactly once, counted through a recording seam.
    - Repair appends the allowed_signers line (this plan's resolution), and a test pins the resulting line count.
    - The router reports repair when the key state is `key-missing`, or when the identity's key is owned by more than one identity; it reports rotate otherwise.
    - The router is a pure function of a classified health value plus an owner count — it performs no I/O and reads no file.
  </behavior>
  <action>
Add `internal/identity/repair.go`.

**Target-path allocation (review R-02).** Define `func RepairKeyPath(existing Account) (privPath, pubPath string)` returning the paths `keygen.KeyPaths` derives from `existing.Name`. This is the whole fix for the contradiction the review found: "the canonical path" means the path canonical FOR THIS IDENTITY, which is derived from its own name, NOT the value currently sitting in `Account.KeyPath`. When an identity shares a sibling's key, `Account.KeyPath` is `id_ed25519_<sibling>` while the repair target is `id_ed25519_<name>` — a different file — so re-pointing this identity is inherently non-destructive to the sibling.

Define `var ErrRepairTargetShared = errors.New("identity: repair target key is referenced by another identity")`. Define `func RepairKey(existing Account, otherOwnersOfTarget []string, deps Deps) (CreateResult, error)`. Before generating anything, if `otherOwnersOfTarget` is non-empty, return a wrapped `ErrRepairTargetShared` naming every entry and perform NO effect at all. That is the pathological case (a sibling Host block pointing at `id_ed25519_<thisname>`); failing closed is correct because the alternative is overwriting a key another identity depends on, which D-05 forbids. The caller (plan 05-07's composition root) supplies the owner list from `SharedKeyOwners` computed against the repair TARGET path, not against `Account.KeyPath`.

Compose repair from Task 1's phases, minus the retirement steps: `deps.Generate` into staging → `preWriteGate` → `persistStagedKey` → `writeArtifacts` with the APPEND signer adapter → `resolvedPhase`. Do NOT call `runPipeline`. Do NOT call `deps.ArchiveKeyPair`. Do NOT stat, read, back up, or remove any pre-existing key file — D-05's rule is that repair never touches pre-existing key material because that material may be absent or may belong to a sibling identity. Write that rule and the `RepairKeyPath` rationale as the first lines of the function's doc comment, and name this plan's signer-semantics resolution ("APPEND") there too, so both decisions are traceable from the code.

**The router.** Add to `internal/identity/state.go`, next to the classification it consumes: `type KeyAction string` with exported constants `KeyActionRotate` and `KeyActionRepair`, and `func KeyActionFor(h IdentityHealth, keyOwnerCount int) KeyAction`.

The owner count is a REQUIRED second input, not an inference (review R-07): `IdentityHealth`'s key axis reports how a key is USED (`key-used-ssh-only`, `key-used-both`, `key-unused`, `key-missing`), and carries no ownership cardinality at all, so a router taking only `IdentityHealth` cannot tell one owner from several and would silently route a shared-key identity into the destructive retirement ceremony. `keyOwnerCount` is the number of identities referencing this identity's current key path, including itself.

Implement over the `KeyState` axis and the owner count, and document the mapping as a table in the comment:

  - `KeyState == StateKeyMissing` → repair (there is nothing to retire).
  - `keyOwnerCount > 1` → repair (retiring would strand a sibling).
  - otherwise → rotate.

State in the comment why this is a routing decision behind ONE UI entry point rather than a second menu row: the approved FIELDS.md action-menu has exactly one key row, and 05-UI-SPEC.md confirms Phase 5 adds no new screen for this. State also that the router deliberately does NOT consume `ClassifyState`'s collapsed label, because that collapse is lossy on the key axis (it never returns `key-used-both`), which is exactly the kind of hidden fold that would misroute a ceremony.

Add a table-driven test over the cross product of all eight `State` values (as `KeyState`) and owner counts {1, 2, 3}, asserting the router's answer for each combination and failing on any unhandled value, so a future taxonomy change cannot silently reroute a ceremony.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/identity/... -run 'Repair|KeyAction|Classify'</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `RepairKeyPath` returns the `keygen.KeyPaths` derivation of `Account.Name` and NOT `Account.KeyPath`, for an account whose `KeyPath` names a sibling.
    - A test seeds two identities where identity A points at identity B's key file, repairs A, and asserts B's key file bytes and B's rendered Host block are byte-unchanged while A's Host block now names A's own canonical path.
    - A test calls `RepairKey` with a non-empty owner list and asserts `errors.Is(err, ErrRepairTargetShared)`, that the error message names every owner, and that NO seam on `Deps` was invoked (all seams recorded, count zero).
    - A test injects an `ArchiveKeyPair` seam that fails the test if called, runs `RepairKey`, and asserts it was never invoked; the same test asserts no read, stat, or remove seam touched a pre-existing key path.
    - A test with a recording `Deps.PersistKey` seam asserts the invocation count after one `RepairKey` is exactly 1.
    - A test asserts the allowed_signers block holds two lines after repairing an identity that already had one, pinning the APPEND resolution.
    - A table-driven test covers every `State` constant crossed with owner counts 1, 2, and 3 and asserts `KeyActionFor`'s answer, including that owner count 2 forces repair even for `key-used-both`.
    - A test asserts `KeyActionFor` performs no I/O by calling it with a zero-value health record and a nil filesystem seam in scope.
  </acceptance_criteria>
  <reversibility rating="reversible">Repair appends rather than replaces the allowed_signers line, so nothing the user had is destroyed and a surplus line is prunable by the Phase 8 fixer; the shared-target case fails closed rather than writing. See this plan's signer-semantics resolution for the full rationale.</reversibility>
  <done>Repair regenerates at this identity's own key path, provably never disturbing a sibling, and one pure predicate routes the single menu row from state plus ownership.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind | Notes |
|---|---|---|
| `preWriteGate`, `persistStagedKey`, `writeArtifacts`, `resolvedPhase` | unexported funcs | the four named pipeline phases |
| `identity.signersWriter` | unexported func type | explicit signer-writer selection, not a `Deps` boolean |
| `identity.Deps.ArchiveKeyPair` | new struct field | MOVE primitive; must be non-nil in the composition root |
| `identity.Deps.AppendAllowedSigners` | new struct field | key-ceremony signer writer (rotate + repair) |
| `identity.RotateResult{CreateResult, ArchivedPrivatePath, ArchivedPublicPath, ProviderHost}` | exported struct | carries the D-08 grace-window facts and the rollback-undo facts, populated for failures from step 3 onward |
| `realBackend.archiveKeyPairSeam(onCreated keygen.CreatedFunc)` | method | the ONE archive implementation; only its observer varies |
| `realBackend.depsForTransaction(j *mutationJournal)` | method | a copy of the one wiring with the archive seam bound to THIS transaction's journal |
| `errArchiveOutsideTransaction` | unexported sentinel | the backend-wide archive binding's fail-closed refusal |
| `mutationJournal.recordCreatedFile(path string) error` | method | records archive entries created mid-transaction; rejects an already-watched path |
| `realBackend.failArchiveRemoveAt` | test-only field | drives the second-source-removal failure through the real composition root |
| `identity.Rotate(existing Account, deps Deps) (RotateResult, error)` | changed signature | moved to `rotate.go`, composed from phases |
| `identity.RepairKeyPath(existing Account) (privPath, pubPath string)` | exported func | own-name canonical target, never `Account.KeyPath` |
| `identity.ErrRepairTargetShared` | exported sentinel | fail-closed guard for the pathological shared-target case |
| `identity.RepairKey(existing Account, otherOwnersOfTarget []string, deps Deps) (CreateResult, error)` | exported func | KEY-07/MGR-05 |
| `identity.KeyAction`, `identity.KeyActionRotate`, `identity.KeyActionRepair` | exported type + constants | D-05 routing vocabulary |
| `identity.KeyActionFor(h IdentityHealth, keyOwnerCount int) KeyAction` | exported func | pure router over state AND ownership |
| `internal/identity/rotate.go`, `rotate_test.go`, `repair.go`, `repair_test.go` | new files | |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| staged key material → canonical `~/.ssh` paths | new private key bytes cross onto disk at the live location |
| old key pair → archive directory | the only remaining copy of a retired credential moves |
| identity health classification → ceremony routing | a misclassification selects a destructive ceremony instead of a safe one |
| one identity's repair → a sibling identity's key file | a wrong target path overwrites a credential another identity depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-11 | Denial of Service | rotate leaving the identity keyless | critical | mitigate | Generation into hermetic staging and the pre-write gate both happen BEFORE the archive step; the archive uses MOVE semantics only after a proven replacement is staged; the result carries the archive paths so the caller's journal can restore. A failure-injection test asserts the result carries them. |
| T-05-11a | Denial of Service | an archive copy created outside any transaction journal | critical | mitigate | The backend-wide `ArchiveKeyPair` binding refuses with `errArchiveOutsideTransaction`; only `depsForTransaction(j)` yields a working seam, and it announces every archive copy to `j.recordCreatedFile` before the first source removal. A test asserts the refusal and a second asserts a second-source-removal failure is fully rolled back (R3-01). |
| T-05-12 | Repudiation | losing the pre-rotation signing line | high | mitigate | D-07 append writer plus a test asserting the older line survives at the lower index. |
| T-05-13 | Tampering | repair overwriting a sibling identity's shared key | critical | mitigate | The repair target is derived from THIS identity's name (`RepairKeyPath`), never from `Account.KeyPath`; the pathological case where the own-name path is referenced by a sibling fails closed with `ErrRepairTargetShared` and performs no effect. Pinned by a sibling-untouched test and a no-seam-invoked test. |
| T-05-14 | Elevation of Privilege | comma injection through the append path | high | mitigate | The append writer routes through `keygen.AllowedSignersLine` (CR-18 guard), inherited from plan 05-02. |
| T-05-15 | Information Disclosure | private key bytes in results or logs | high | mitigate | `RotateResult` carries PATHS only; a test asserts no result field contains an OpenSSH private-key header. |
| T-05-45 | Spoofing | a double persist writing the new key twice with undefined ordering | high | mitigate | The pipeline decomposition makes the persist phase a separate, singly-invoked step; a recording seam asserts exactly one invocation per ceremony. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
REQ | KEY-05 | Rotate re-points artifacts and re-runs the test | 05-03, 05-06, 05-07 | COVERED | Domain here; ceremony in 05-06; real wiring in 05-07.
REQ | KEY-07 | New key for an existing identity (repair) | 05-03, 05-06, 05-07 | COVERED | Domain here.
REQ | MGR-05 | New key distinct from rotate | 05-03, 05-06 | COVERED | Router + separate function.
RESEARCH | Pitfall 2 | WriteAllowedSigners replaces where D-07 needs append | 05-03 | COVERED | Both ceremonies select the append writer.
RESEARCH | A1 / Open Q1 | Does repair append or replace? | 05-03 | COVERED | RESOLVED to APPEND by this plan's planner-resolution section; no longer a checkpoint.
RESEARCH | Anti-pattern | Reusing Rotate unmodified | 05-03 | COVERED | Explicitly recomposed, with failure-injection tests.
CONTEXT | D-05 | Rotate = retirement, new-key = repair | 05-03 | COVERED | Two functions plus the pure router.
CONTEXT | D-06 | Archive before overwrite | 05-03 | COVERED | MOVE primitive, ordering documented and tested.
CONTEXT | D-07 | Append on rotate | 05-03 | COVERED | Rotate selects the append writer; repair does too, per the recorded resolution.
CONTEXT | D-08 | Grace-window facts surfaced, paths textually unchanged | 05-03 | COVERED | RotateResult fields plus the byte-identical assertion.
REVIEW | R-01 | runPipeline already persists — rotate would double-write | 05-03 | COVERED | Task 1 decomposition + exactly-once recording assertions.
REVIEW | R-02 | Shared-key repair overwrites the sibling's bytes | 05-03 | COVERED | `RepairKeyPath` targets the own-name path; `ErrRepairTargetShared` fails closed.
REVIEW | R-07 | Router cannot distinguish one owner from several | 05-03 | COVERED | `KeyActionFor(h, keyOwnerCount)` with a cross-product table test.
REVIEW | R-24 | Blocking checkpoint conflicts with DLV-08 | 05-03 | COVERED | Checkpoint removed; decision resolved in-plan and recorded.
REVIEW | R-25 | Router must cover orthogonal axes, not a collapsed label | 05-03 | COVERED | Router consumes KeyState + owner count and explicitly rejects `ClassifyState`.
REVIEW | R-21 | Stamp collision across rotate and delete | 05-02, 05-03 | COVERED | Primitive half in 05-02; the composition-root monotonic retry is written and tested in this plan's Task 2.
REVIEW | R2-05 | 05-02 tested a composition-root retry that does not exist until this plan | 05-02, 05-03 | COVERED | The criterion moves to Task 2, alongside the `ArchiveKeyPair` closure it exercises, with an injectable clock.
REVIEW | R2-09 | Task 1 / Task 2 rotate-behavior sequencing is ambiguous | 05-03 | COVERED | Task 1 explicitly leaves rotate on the old composition and pins that with a temporary test; Task 2 owns the removal and the comment-stripped grep gate.
REVIEW | R3-01 | Rollback cannot discover archive entries created by a failing archive step | 05-02, 05-03, 05-07 | COVERED | Task 2 populates `RotateResult` from step 3 onward before inspecting the error, binds the archive observer to the transaction journal through `depsForTransaction`, makes the backend-wide binding refuse, and lands the second-source-removal rollback regression through the real composition root.
</source_audit>

## Review resolutions (cycle 1)

| Finding | Severity | Resolution in this plan |
|---|---|---|
| Rotation would persist the replacement twice via `runPipeline` | HIGH | Task 1 decomposes `runPipeline` into four named phases; rotate and repair compose them directly and never call `runPipeline`. A recording `PersistKey` seam asserts exactly one invocation per ceremony. |
| Shared-key repair is internally impossible as specified | HIGH | `RepairKeyPath` derives the target from the identity's OWN name, so the sibling's file is a different path. The pathological own-path-shared case fails closed with `ErrRepairTargetShared`. |
| Repair cannot "never touch key material" while writing the same shared path | HIGH | Same resolution — repair never writes `Account.KeyPath`; it writes its own canonical path or refuses. |
| Blocking checkpoint violates DLV-08's single-stop contract | MEDIUM (divergent) | Checkpoint REMOVED. The decision is made in planning (APPEND), with the research's factual premise corrected in writing, and recorded in the plan, the SUMMARY, and `RepairKey`'s doc comment. |
| Routing shared-key state is not representable by `IdentityHealth` | MEDIUM | `KeyActionFor` gains a required `keyOwnerCount` parameter with a cross-product table test. |
| Rotate-twice signer accumulation undocumented | LOW | Pinned in 05-02 Task 2's test and restated in this plan's rotate-twice criterion. |

## Review resolutions (cycle 2)

| Finding | Severity | Resolution in this plan |
|---|---|---|
| Task 1 says keep the existing `Rotate` behavior-identical while Task 2 changes its signature and stops using the monolith — an executor could read Task 1's "existing `Rotate`" as the final ceremony (R2-09) | MEDIUM | Task 1 now states outright that it LEAVES rotate on the old composition, that Task 2 is what removes the call and changes the signature, and that Task 2's grep gate is expected to be red until Task 2 lands. A temporary Task 1 test pins that rotate still reaches the old composition at the end of Task 1, and Task 2 deletes that test in the same commit that removes the call — so the boundary is checked rather than described. The note is carried into `05-03-SUMMARY.md`. |
| Plan 05-02's composition-root stamp-retry criterion exercises code this plan introduces (R2-05) | MEDIUM | The retry criterion moves into Task 2, next to the `ArchiveKeyPair` closure that implements it, with an injectable clock so the collision is reproducible. Task 2 gains two criteria: a first collision produces a distinct path, a second surfaces the error. 05-02 keeps only the primitive's half. |

## Review resolutions (cycle 3)

| Finding | Severity | Resolution in this plan |
|---|---|---|
| `RotateResult` is only guaranteed populated for failures at step 4 or later, so an error returned by the archive step ITSELF gives the caller an error and no archive paths — leaving a partially removed key pair with an archive copy the rollback cannot identify (R3-01, 05-03:192) | HIGH | Step 3 is restructured to assign both returned archive paths onto the result BEFORE inspecting the seam's error, and the guarantee is restated as "step 3 or later, with no gap". The seam's own contract now says it returns whatever it created even when it errors, matching what `keygen.MoveKeyPairToArchive` actually does on an `ErrArchiveIncomplete` source-removal failure. A domain test injects exactly that shape — both paths plus an error — and asserts the result still carries them, because the obvious `return RotateResult{}, err` shape is the bug and only a test pins it shut. |
| The archive closures were planned to call `journal.recordCreatedFile` but are constructed in `buildIdentityDeps`, which runs once at backend construction with no transaction in scope and no journal argument — so the recording could never fire (R3-01, 05-07:182/186, `cmd/gitid/wiring.go:863`) | HIGH | The binding moves into THIS task, which is where the closure is first written, instead of being described in plan 05-07 where it was unreachable. `archiveKeyPairSeam(onCreated)` holds the one archive implementation; `buildIdentityDeps` binds it to a closure returning `errArchiveOutsideTransaction` so the backend-wide seam fails CLOSED; `depsForTransaction(j)` returns a copy of the one wiring with that seam rebound to `j.recordCreatedFile`. `identity.Deps` is a value type, so this re-binds one seam rather than building a second wiring — which is what plan 05-07's "do not build a second `Deps`" rule was actually protecting. `mutationJournal` already exists in `wiring.go` today, so `recordCreatedFile`, its disjointness rejection against `watchFile`, and its removal pass in `restore()` land here rather than in wave 7; a wave-3 rotation is therefore already all-or-nothing. A test-only `failArchiveRemoveAt` field (mirroring the existing `failCommitAt` precedent) makes the second-source-removal failure drivable through the real composition root. |
| No test proved an archive copy was DISCOVERABLE after a second-source-removal failure — only that an error was returned (R3-01) | HIGH | Task 2 gains the explicit regression: seed a real pair, watch both canonical key paths, fail the SECOND removal, then assert the journal recorded both archive paths, `restore()` leaves no archive entry for the identity, and both canonical paths hold their pre-transaction bytes. Two supporting assertions keep it from decaying: the backend-wide seam refuses to archive at all, and the two journal sets reject each other's paths at registration. |

<verification>
Run `make test` and `make lint`. Confirm the `wiring_test.go` reflection guard reports every `identity.Deps` field non-nil, and confirm `go list -deps ./internal/identity` still shows no UI package import (the domain stays UI-free per DLV-07). Confirm the comment-stripped grep gate over `internal/identity/rotate.go` and `internal/identity/repair.go` reports zero pipeline-composition call sites — the two ceremonies compose phases, never the monolith, and explanatory comments naming the old composition are expected.
</verification>

<success_criteria>
- The pipeline is four named phases and every ceremony persists its key exactly once.
- Rotate archives with move semantics, appends, and re-points, and a failed rotation reports exactly what it created so the caller can restore the working key.
- Repair regenerates at this identity's own key path, provably never disturbing a sibling, and refuses rather than overwriting in the pathological case.
- One pure predicate routes the single approved menu row from classified state AND key ownership.
</success_criteria>

<output>
Create `.planning/phases/05-identity-manager/05-03-SUMMARY.md` when done, including the "Repair signer semantics" resolution carried over verbatim from this plan and the pre-refactor call-order test observation.
</output>
