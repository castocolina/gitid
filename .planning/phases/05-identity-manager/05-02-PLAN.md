---
phase: 05-identity-manager
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/sshconfig/include.go
  - internal/sshconfig/include_test.go
  - internal/keygen/archive.go
  - internal/keygen/archive_test.go
  - internal/keygen/signers.go
  - internal/keygen/signers_test.go
  - internal/gitconfig/renderer.go
  - internal/gitconfig/renderer_test.go
  - internal/identity/inventory.go
  - internal/identity/inventory_test.go
  - internal/doctor/checks/orphans_test.go
autonomous: true
requirements: [KEY-05, MGR-06]
estimate:
  tokens: 84000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An archived key pair lands in `~/.ssh/gitid-archive/` with the directory at mode 0700, the archived private key at 0600, and the archived public key at 0644 (D-06)."
    - "Archiving is TWO primitives, not one: `CopyKeyPairToArchive` never removes its sources, and `MoveKeyPairToArchive` copies then removes them — so rotation gets move semantics and delete-everything gets copy-then-later-remove semantics from the same package without either contract being overloaded (review R-03)."
    - "`RemoveArchivedPair` deletes exactly the archive entries a caller created, so a rolled-back transaction can undo its own archive step (review R-10)."
    - "Archiving vacates the canonical `id_ed25519_<name>` path so the new key can take it, and never overwrites an existing archive entry (D-06)."
    - "`~/.ssh/gitid-archive/` is registered as doctor-reserved: a planted archived key produces ZERO orphan findings and ZERO permission findings, so `--fix` can never propose removing it (D-06 MANDATORY, and the project's known destructive-loop bug class)."
    - "The reserved-path exclusion is applied to the RESULT of `InventoryDeps.ListKeyFiles`, not only inside the production globber, so the guard is causal for ANY enumeration source — and a negative-control test that disables the predicate provably produces the orphan finding (review R-04)."
    - "`IsReservedPath` reports true for the archive directory and for any path underneath it at ANY depth, so a future per-identity or per-generation archive layout cannot become visible to the doctor (review R-19)."
    - "Appending a signer line to an identity's allowed_signers block KEEPS the existing line and adds the new one, so the block holds two lines for the same email (D-07)."
    - "Appending is idempotent: re-running with the same public line yields a byte-identical file, never a third duplicate line."
    - "Every appended line is built through `keygen.AllowedSignersLine`, so the CR-18 comma-injection guard applies to the new write path exactly as it applies to the existing one."
    - "`gitconfig.RemoveProviderRewrite` removes ONLY the `provider-rewrite:<host>` block for the named provider and leaves every other managed block, including the global url-rewrites baseline block, untouched (D-09)."
  artifacts:
    - path: "internal/keygen/archive.go"
      provides: "ArchivedPair + CopyKeyPairToArchive + MoveKeyPairToArchive + RemoveArchivedPair — the D-06 archive primitives"
    - path: "internal/sshconfig/include.go"
      provides: "ArchiveDirName/ArchiveDir + the extended ReservedPaths/IsReservedPath registry"
    - path: "internal/keygen/signers.go"
      provides: "AppendAllowedSigners — the D-07 append-not-replace writer"
    - path: "internal/gitconfig/renderer.go"
      provides: "RemoveProviderRewrite — the missing half of the per-provider insteadOf pair (D-09)"
  key_links:
    - from: "internal/keygen/archive.go"
      to: "internal/filewriter"
      via: "exclusive-create copy + explicit modes, never os.WriteFile"
      pattern: "filewriter\\."
    - from: "internal/identity/inventory.go BuildInventory"
      to: "internal/sshconfig.IsReservedPath"
      via: "InventoryDeps.IsReservedKeyPath filtering the ListKeyFiles result"
      pattern: "IsReservedKeyPath|IsReservedPath|ArchiveDir"
---

<objective>
Build the four missing substrate primitives every Phase 5 lifecycle ceremony needs, each in the package that already owns its concern.

Purpose: 05-RESEARCH.md verified that three functions D-06/D-07/D-09 assume simply do not exist — there is no archive-to-dedicated-directory helper (`filewriter.BackupAndRemove` writes a SIBLING `.bak` path), no append-aware allowed_signers writer (`WriteAllowedSigners` replaces the whole block by design), and no `RemoveProviderRewrite` (only the writer and a presence check exist). Building them as isolated, fully-tested primitives BEFORE the orchestration that composes them keeps the risky parts small and keeps the orchestration plans focused on sequencing.
Output: Three new primitives plus the doctor-reserved registration D-06 makes mandatory, each with a failing-test-first cycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@recipes/gitconfig.recipe
@.planning/phases/05-identity-manager/05-CONTEXT.md
@.planning/phases/05-identity-manager/05-RESEARCH.md
@.planning/phases/05-identity-manager/05-01-SUMMARY.md

<authority>Every bare D-NN citation means `05-CONTEXT.md`. 05-RESEARCH.md's "Common Pitfalls" 2, 4, and 5 describe exactly the three traps this plan exists to avoid.</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Key archive directory + the mandatory doctor-reserved registration (D-06)</name>
  <files>internal/sshconfig/include.go, internal/sshconfig/include_test.go, internal/keygen/archive.go, internal/keygen/archive_test.go, internal/identity/inventory.go, internal/identity/inventory_test.go, internal/doctor/checks/orphans_test.go</files>
  <read_first>
    - internal/sshconfig/include.go lines 55-101 (IsReservedBlockName, ReservedPaths, IsReservedPath and their hand-off comments)
    - internal/filewriter/filewriter.go lines 120-215 (backupExistingTarget's naming convention, Write, and the exclusive-create copy idiom)
    - internal/keygen/signers.go (the package's existing file-writing conventions and mode constants)
    - internal/identity/inventory.go lines 256-278 (listKeyFilesRealForHome — the key enumeration doctor consumes)
    - internal/doctor/checks/orphans.go lines 12-40 and 126-160 (Class 3 unused-key detection over deps.KeyPaths)
    - internal/doctor/checks/perms.go lines 27-50 (CheckPermissions over deps.KeyPaths/PubKeyPaths)
  </read_first>
  <behavior>
    - Archiving a key pair creates the archive directory at mode 0700 when absent, and tightens an existing directory whose mode is wider than 0700 back to 0700.
    - An archive directory path that is a symlink is rejected with an error before any copy, and no chmod is applied to it.
    - The archived private key lands at `<archiveDir>/<basename>.<stamp>` with mode 0600 and the archived public key at `<archiveDir>/<basename>.pub.<stamp>` with mode 0644.
    - `CopyKeyPairToArchive` leaves BOTH source paths present and unmodified after a successful copy.
    - `MoveKeyPairToArchive` leaves neither source path present after a successful move, so the canonical `id_ed25519_<name>` slot is free.
    - A `MoveKeyPairToArchive` that fails while removing the second source leaves the archive copies intact and reports which sources still exist.
    - When a destination archive path already exists, either call fails without overwriting it and without removing any source.
    - Archiving a pair whose public half is absent succeeds and reports an empty public archive path.
    - `RemoveArchivedPair` removes exactly the paths carried in the `ArchivedPair` it is given, tolerates an already-absent path, and refuses any path that is not inside the archive directory.
    - A key file planted inside the archive directory produces no doctor orphan finding and no doctor permission finding.
    - With the reserved-path predicate disabled, the SAME planted archive key DOES produce an orphan finding — the negative control that proves the guard is load-bearing.
    - `IsReservedPath` reports true for the archive directory itself, for a file directly inside it, and for a file nested one directory deeper inside it; it still reports false for `~/.ssh/config` and for `~/.ssh/config.d/foo.txt`, and true for `~/.ssh/config.d/foo.config`.
  </behavior>
  <action>
Split the work between the package that owns the reserved-path registry and the package that owns key material, so neither imports the other.

In `internal/sshconfig/include.go`: add an exported constant `ArchiveDirName` with the value `gitid-archive` and an exported `ArchiveDir(sshDir string) string` returning `filepath.Join(sshDir, ArchiveDirName)`. Extend `ReservedPaths` to return the archive directory and the `**` recursive glob beneath it, appended AFTER the existing two `config.d` entries so the existing ordering assertion in `include_test.go` stays a prefix. Extend `IsReservedPath` so the archive directory itself and any path UNDER it at any depth report true — implement the containment with a cleaned-path prefix check (`filepath.Clean` on both sides plus a separator-bounded `strings.HasPrefix`), not a `filepath.Dir` equality test. Review R-19 is the reason: a future per-identity or per-generation archive layout would nest, and a `Dir`-equality guard would silently stop covering it. Update the doc comments on both functions to name D-06 as the reason, and keep the existing Phase-8 D-06.2 hand-off note. Do NOT create a second registry — 05-RESEARCH.md names a parallel list as the exact bug class the project has already hit.

In a new `internal/keygen/archive.go`: define `type ArchivedPair struct { PrivatePath, PublicPath string }` and THREE functions, because one primitive cannot serve both callers (review R-03: 05-03's rotation needs move semantics, 05-04's delete-everything needs copy-then-remove-live-last so a mid-failure leaves both copies intact):

  - `func CopyKeyPairToArchive(archiveDir, privPath, pubPath, stamp string) (ArchivedPair, error)` — copies both halves into the archive and NEVER touches the sources. This is the primitive delete-everything uses.
  - `func MoveKeyPairToArchive(archiveDir, privPath, pubPath, stamp string) (ArchivedPair, error)` — calls `CopyKeyPairToArchive`, then removes the sources. This is the primitive rotation uses. Implement it as literally that composition so the two can never drift; on a source-removal failure it returns the `ArchivedPair` it created together with an error naming which sources survived, so the caller can decide (the archive copies are valid either way).

    **Make the source-removal failure injectable rather than provoked (review R2-06).** The acceptance criteria require a deterministic failure while removing the SECOND source, and the obvious way to produce one — revoking directory permissions — behaves differently across macOS and Linux and does nothing at all when the suite runs as root in CI, so the test would be flaky on one platform and vacuous on another. Instead, split the function: put the body in an unexported `moveKeyPairToArchive(archiveDir, privPath, pubPath, stamp string, remove func(string) error) (ArchivedPair, error)`, and make the exported `MoveKeyPairToArchive` a one-line call passing `os.Remove`. The failure test drives the unexported form with a `remove` that succeeds for the first path and returns a sentinel error for the second — deterministic, identical on both supported operating systems, and testing the real control flow rather than a filesystem quirk. Do NOT expose the seam as a package-level mutable variable: a test that swaps a package global cannot run in parallel and leaks into every other test in the package. Add a test asserting the exported wrapper passes `os.Remove` (assert by moving a real pair and observing both sources gone), so the wrapper cannot silently diverge from the tested body.
  - `func RemoveArchivedPair(archiveDir string, pair ArchivedPair) error` — removes exactly the paths in `pair`, tolerating an already-absent path, and returning an error for any path not contained in `archiveDir`. This is the rollback primitive review R-10 requires: a transaction that created archive entries mid-flight must be able to undo exactly its own entries.

The archive directory is passed IN as a parameter so `internal/keygen` never imports `internal/sshconfig` — the composition root resolves it via `sshconfig.ArchiveDir`. Add package-level mode constants `archiveDirMode` 0o700 and reuse the existing private/public key modes. Implement the copy as: `os.Lstat` the archive directory and reject a symlink with an error before doing anything (review R-20 — never chmod through a symlink); create the directory with `os.MkdirAll`; then `os.Chmod` it to the explicit mode whenever its current `Perm()` is not already 0700, so an existing world-readable directory is TIGHTENED rather than left wide (also R-20); build each destination as the source basename plus a dot plus `stamp`, inserting `.pub` before the stamp for the public half; copy each file with an exclusive-create open so an existing destination is a hard error.

**Reproduce the exclusive-open idiom in `keygen`; do NOT reach for `filewriter.copyFileExclusive` (review R2-07).** That helper is unexported, so "reuse it" is not implementable as written and would cost an executor a mid-task detour into either an import cycle or an unplanned API change. Exporting it is the wrong trade in the other direction: `filewriter` is the project's write chokepoint and its copy helper is bound to backup semantics this caller does not want, so widening that package's public surface for one consumer weakens the chokepoint's meaning. The idiom is four lines — `os.OpenFile(dst, os.O_WRONLY|os.O_CREATE|os.O_EXCL, mode)`, `io.Copy`, `Close`, and an explicit `os.Chmod` — so `internal/keygen` writes its own. Reference `internal/filewriter/filewriter.go`'s helper in a comment as the pattern being mirrored and state that the two must stay behaviorally identical on the one property that matters: an existing destination is an error, never an overwrite. Pin that property in BOTH packages' tests so a future change to either is caught.

Order matters: copy BOTH halves before removing anything, so a mid-operation failure never leaves the pair half-archived. On any error after a partial copy, remove the copies made so far before returning.

Choose the stamp convention explicitly and document it on the functions: the caller supplies it, and the production caller uses the same `UnixNano` decimal string `filewriter.backupExistingTarget` uses — one timestamp convention across the project, never the ISO-8601-with-dashes shape `tuikit.NewBackupPath` produces (that is a dummy-fixture display convention, per 05-RESEARCH.md's Don't Hand-Roll table). Because rotate and delete-everything share one directory, a same-nanosecond collision is possible in tests and theoretically possible in production: the exclusive-create makes it a hard error rather than an overwrite, and the composition root retries once with a monotonically bumped stamp before surfacing the failure.

**This plan owns only the PRIMITIVE half of that contract (review R2-05).** The retry lives in the production archive closure inside `buildIdentityDeps`, and this plan does not modify `cmd/gitid/wiring.go` at all — that closure is introduced by plan 05-03 Task 2. Cycle 1 nonetheless put a "composition-root stamp retry" acceptance criterion here, which no executor of this plan could satisfy without writing code from a later wave. So the split is explicit: THIS plan proves the collision is a hard error that touches nothing (the primitive's half), and plan 05-03 Task 2 proves the retry produces a second distinct path and that a second collision surfaces the error (the composition root's half). Say so on the functions' doc comments and name plan 05-03 as the retry's owner, so a reader of `archive.go` alone knows where the other half lives (review R-21 is satisfied across the two plans, not dropped).

In `internal/identity/inventory.go`: apply the reserved-path exclusion where it is CAUSAL, which is not inside the globber (review R-04). The current production glob `~/.ssh/id_*` is non-recursive, so an archived key is invisible to it even with no guard at all — a test that reverts a guard inside `listKeyFilesRealForHome` can never go red, and the plan must not claim otherwise. Instead:

  - Add `InventoryDeps.IsReservedKeyPath func(path string) bool` and apply it in `BuildInventory` to the RESULT of `deps.ListKeyFiles()`, dropping every path for which it reports true before the unused-key cross-reference runs. Wire it in `BuildInventoryDeps`/`InventoryDepsForHome` to `func(p string) bool { return sshconfig.IsReservedPath(sshDir, p) }`. Document that the predicate — not the shape of today's glob — is the durable registration, and that any future recursive enumerator inherits the guard for free.
  - Additionally keep the same filter inside `listKeyFilesRealForHome` as defense in depth, and say so in the comment.

In `internal/doctor/checks/orphans_test.go`: add the regression test AND its negative control. The test plants an archived key inside the archive directory of a fake home and builds the doctor deps from `BuildInventory` with an INJECTED `ListKeyFiles` seam that returns the archived path (that is what makes the assertion causal rather than incidentally satisfied by the non-recursive glob). It asserts zero findings mention the archived path. The negative control builds the same deps with `IsReservedKeyPath` set to `func(string) bool { return false }` and asserts an orphan finding IS produced for that path. Name both so their intent is obvious to a future reader who hits the destructive `--fix` loop again.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/sshconfig/... ./internal/keygen/... ./internal/identity/... ./internal/doctor/...</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `os.Stat` on the created archive directory reports `FileMode.Perm()` equal to 0700, the archived private key 0600, and the archived public key 0644.
    - A test pre-creates the archive directory at mode 0777, calls the copier, and asserts the directory's mode afterwards is 0700.
    - A test replaces the archive directory path with a symlink and asserts a non-nil error, that no file was copied, and that the symlink target's mode is unchanged.
    - A test asserts `CopyKeyPairToArchive` leaves both source paths present with unchanged bytes, and a sibling test asserts `MoveKeyPairToArchive` leaves neither present.
    - A test drives the unexported `moveKeyPairToArchive` with an injected `remove` that fails on the SECOND source and asserts the archive copies exist, the error names the surviving source, and the returned `ArchivedPair` carries both archive paths — deterministic on macOS and Linux alike, and unaffected by running as root (review R2-06).
    - A test asserts the exported `MoveKeyPairToArchive` wrapper really passes `os.Remove`, by moving a real pair and observing both sources gone.
    - A test pre-creates the destination path, calls each archiver, asserts a non-nil error, and asserts the SOURCE files still exist and the pre-existing destination is byte-unchanged — the primitive's half of the collision contract. (The composition-root retry that consumes this error is proven in plan 05-03 Task 2, which is where the closure is written; asserting it here would test code this plan does not create — review R2-05.)
    - A test asserts `internal/keygen`'s exclusive-open copy refuses an existing destination, and the mirrored assertion exists in `internal/filewriter`'s own tests, so the two implementations of the idiom cannot drift on the property that matters (review R2-07).
    - A test archives a pair whose `.pub` is absent and asserts `ArchivedPair.PublicPath` is the empty string with a nil error.
    - A test calls `RemoveArchivedPair` with a path outside the archive directory and asserts a non-nil error and that the path still exists; a sibling test asserts an already-absent archive path is tolerated.
    - `TestReservedPaths` and `TestIsReservedPath` are extended with a table covering: archive dir true, archive file true, archive file nested one directory deeper true, `~/.ssh/config` false, `~/.ssh/config.d/foo.txt` false, `~/.ssh/config.d/foo.config` true.
    - The orphans regression test asserts ZERO findings for a planted archive key when `IsReservedKeyPath` is wired, and its negative control asserts a finding IS produced when the predicate is replaced by one returning false. Record both observed results in the SUMMARY. Do NOT claim the production non-recursive glob proves the guard.
  </acceptance_criteria>
  <reversibility rating="costly">The archive path convention becomes user-visible on disk: once a user has rotated keys, changing the directory name or the filename shape strands their archived material at the old location. Mitigated by matching the project's single existing timestamp convention and registering the path in the one shared registry.</reversibility>
  <done>Archived key pairs land in a mode-0700 dedicated directory that the doctor is structurally blind to, proven by a regression test whose negative control goes red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append-not-replace allowed_signers writer (D-07)</name>
  <files>internal/keygen/signers.go, internal/keygen/signers_test.go</files>
  <read_first>
    - internal/keygen/signers.go (whole file — AllowedSignersLine's CR-18 comma guard, WriteAllowedSigners, WriteAllowedSignersReplacing, allowedSignersMode)
    - internal/filewriter/block.go (ReplaceBlock and ListBlocks — the opaque-body contract this task depends on)
    - internal/filewriter/block_list_test.go (how ListBlocks reports a block's body)
    - internal/gitconfig/reader.go lines 180-210 (RemoveAllowedSignersLine's exact-first-field match — the removal side that must keep working with two lines)
  </read_first>
  <behavior>
    - Appending to an identity with no existing block creates the block containing exactly the new line.
    - Appending to an identity whose block holds one line yields a block with two lines, oldest first, both preserved verbatim.
    - Appending a line already present in the block yields a byte-identical file and reports no error.
    - Foreign content outside the identity's block and other identities' blocks are preserved byte-for-byte.
    - An email containing a comma is rejected before any write, with the file left untouched.
    - The written file's mode is 0644.
    - After two appends for the same email, the existing block-keyed removal helper still removes the whole block including both lines.
  </behavior>
  <action>
Add `func AppendAllowedSigners(path, identity, email, pubLine string) (backupPath string, err error)` to `internal/keygen/signers.go`.

Build the new line through the existing `AllowedSignersLine` so the CR-18 comma-injection guard is enforced on this write path too — never construct the line string inline. 05-RESEARCH.md's Security Domain names this as a mandatory routing rule for any new allowed_signers writer.

Read the file (treating a missing file as empty), find the identity's current block body via `filewriter.ListBlocks`, and compose the new body as the existing body plus the new line. Dedupe by comparing trimmed lines: if the exact composed line is already present, return early with an empty backup path and a nil error rather than writing. Then write via `filewriter.Write` with `filewriter.ReplaceBlock(existing, identity, combinedBody)` at `allowedSignersMode` — the same chokepoint `WriteAllowedSigners` already uses. `ReplaceBlock`'s body is opaque text, so no block-format change is needed; only the caller-side compose step is new.

Document on the function that this is the KEY-CEREMONY writer used by BOTH rotate and repair (D-07 for rotate; plan 05-03 records the planner resolution extending the same rule to repair), and that create, update, and clone keep the single-line `WriteAllowedSigners`/`WriteAllowedSignersReplacing` semantics. State the reason in the comment: an `allowed_signers` entry verifies signatures from its PUBLIC key blob alone, so keeping the old line lets `git log --show-signature` keep verifying pre-rotation commits even after the old private key is archived or gone; pruning accumulated lines is deliberately deferred to Phase 8.

Extend `internal/keygen/signers_test.go` with the behaviors above. Include a test that appends twice and then calls the existing block-keyed removal helper, asserting all three lines disappear together — the removal side needs no change because it is block-keyed rather than line-keyed, and this pins that (review R-22, which flags rotate-twice accumulation as an undocumented consequence). Add a comment in the test naming the consequence explicitly: N rotations leave N+1 lines in one block; delete-everything removes the whole block regardless of line count, and Git-only delete keeps the whole block regardless of line count (D-10).
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/keygen/... ./internal/gitconfig/... -run 'AllowedSigners'</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the file after one append then a second append with a different public line contains exactly two lines whose first field is the email, in append order.
    - A test asserts a third append of an already-present line leaves the file bytes unchanged and returns an empty backup path.
    - A test asserts foreign text written above and below the managed block survives byte-for-byte across two appends.
    - A test passes an email containing a comma, asserts a non-nil error, and asserts the file was not created or modified.
    - A test asserts `os.Stat` reports mode 0644 on the written file.
  </acceptance_criteria>
  <done>Rotation can add a signing line without destroying the previous one, and the behavior is pinned by tests including the idempotent re-run case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RemoveProviderRewrite — the missing half of the per-provider insteadOf pair (D-09)</name>
  <files>internal/gitconfig/renderer.go, internal/gitconfig/renderer_test.go</files>
  <read_first>
    - internal/gitconfig/renderer.go lines 154-245 (providerRewritePrefix, ProviderRewriteBlockName, RenderProviderRewrite, WriteProviderRewrite, HasProviderRewrite, validProviderHostname)
    - internal/gitconfig/baseline.go lines 140-165 (RemoveURLRewritesBlock — the DIFFERENT, global block that must not be confused with the per-provider one)
    - internal/filewriter/block.go (RemoveBlock's idempotent contract)
    - recipes/gitconfig.recipe (the canonical insteadOf shape the block reproduces)
  </read_first>
  <behavior>
    - Removing the rewrite for a provider that has a block deletes exactly that block and returns a backup path.
    - Removing the rewrite for a provider with no block is a no-op that returns no error.
    - A different provider's rewrite block and the global baseline url-rewrites block both survive the removal.
    - An invalid provider hostname is rejected with an error before any read or write.
    - Removal is idempotent: a second call leaves the file byte-identical to the first result.
    - After removal, the presence check for that provider reports false and reports true for the surviving sibling provider.
  </behavior>
  <action>
Add `func RemoveProviderRewrite(gitconfigPath, provider string) (backupPath string, err error)` to `internal/gitconfig/renderer.go`, placed directly after `WriteProviderRewrite` so the writer and remover read as a pair.

Mirror `WriteProviderRewrite`'s exact shape: resolve the sentinel name through `ProviderRewriteBlockName` (so both halves are keyed by the same name and a hostname validation failure short-circuits identically), read the file tolerating a not-exist error, then write through `filewriter.Write` with `filewriter.RemoveBlock(existing, name)` at `gitconfigMode`. Take no `enabled` parameter and render no body.

Document on the function why it is NOT `RemoveURLRewritesBlock`: that function targets the GLOBAL baseline's static `url-rewrites` block, a Phase 7 concern, while this one targets the per-identity-provider `provider-rewrite:<host>` block. 05-RESEARCH.md Pitfall 4 records that confusing the two removes the wrong thing.

Add a test that writes rewrites for two distinct providers plus the global baseline block, removes one provider's, and asserts the other two survive — 05-RESEARCH.md warns that over-eager removal is as real a risk as under-removal.

Ref-counting itself is NOT this task: the decision of WHETHER to call this function belongs to the delete orchestration in plan 05-04. This task ships the mechanism only.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/gitconfig/... -run 'ProviderRewrite'</automated>
  </verify>
  <acceptance_criteria>
    - A test writes rewrites for two providers, removes one, and asserts `HasProviderRewrite` reports false for the removed provider and true for the other.
    - A test asserts the global baseline url-rewrites block written by the baseline writer is still present after a per-provider removal.
    - A test calls the remover twice and asserts the second call leaves the file bytes equal to the first result.
    - A test passes an invalid provider hostname and asserts a non-nil error with the gitconfig package prefix and an unmodified file.
    - A test asserts removing from a file with no such block returns a nil error.
  </acceptance_criteria>
  <done>The per-provider insteadOf block can be removed by the same sentinel name that created it, with sibling blocks provably untouched.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind | Notes |
|---|---|---|
| `sshconfig.ArchiveDirName` | exported const | value `gitid-archive` |
| `sshconfig.ArchiveDir(sshDir string) string` | exported func | single source of the archive location |
| `sshconfig.ReservedPaths` / `IsReservedPath` | extended funcs | archive dir + files directly inside it |
| `keygen.ArchivedPair{PrivatePath, PublicPath}` | exported struct | archive result |
| `keygen.CopyKeyPairToArchive(archiveDir, privPath, pubPath, stamp string)` | exported func | copy only — delete-everything's primitive |
| `keygen.MoveKeyPairToArchive(archiveDir, privPath, pubPath, stamp string)` | exported func | copy + remove sources — rotation's primitive |
| `keygen.RemoveArchivedPair(archiveDir string, pair ArchivedPair)` | exported func | rollback undo for archive entries |
| `identity.InventoryDeps.IsReservedKeyPath` | new seam field | causal reserved-path exclusion in `BuildInventory` |
| `keygen.AppendAllowedSigners(path, identity, email, pubLine string)` | exported func | D-07 key-ceremony writer (rotate + repair) |
| `gitconfig.RemoveProviderRewrite(gitconfigPath, provider string)` | exported func | D-09 mechanism |
| `internal/identity/inventory.go` archive-dir exclusion | behavior change | keeps doctor blind to archived keys |
| `internal/keygen/archive.go`, `internal/keygen/archive_test.go` | new files | |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| in-memory key material → archive directory | private key bytes cross onto disk at a new location |
| caller-supplied email → allowed_signers principals field | untrusted-ish text is interpolated into a trust file |
| provider hostname → managed block sentinel name | a name selects which block is deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-06 | Information Disclosure | archived private key | high | mitigate | Explicit 0600 on the archived private key and 0700 on the directory, set via `os.Chmod` after creation so the process umask cannot widen them; asserted by a mode test. |
| T-05-07 | Tampering | archive destination collision | medium | mitigate | Exclusive-create copy; an existing destination is a hard error and the source is left intact. |
| T-05-08 | Elevation of Privilege | comma injection into allowed_signers principals | high | mitigate | The append path builds its line only through `AllowedSignersLine`, which rejects a comma-bearing email (CR-18); a test asserts no file is written on rejection. |
| T-05-09 | Tampering | wrong insteadOf block removed | high | mitigate | Removal is keyed by `ProviderRewriteBlockName`, never by a substring or the global baseline name; a test asserts the sibling provider block and the global block survive. |
| T-05-10 | Denial of Service | doctor `--fix` deleting archived keys | high | mitigate | Archive directory registered in the single reserved-path registry AND excluded from key enumeration; a doctor regression test asserts zero findings. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
REQ | KEY-05 | Rotate re-points artifacts, re-runs test | 05-02, 05-03 | COVERED | Primitives here; orchestration in 05-03.
REQ | MGR-06 | Delete choice everything vs Git-only | 05-01, 05-02, 05-04 | COVERED | The insteadOf remover lands here; the ref-count decision in 05-04.
RESEARCH | Pitfall 2 | WriteAllowedSigners replaces, D-07 needs append | 05-02 | COVERED | Task 2 composes the combined body client-side.
RESEARCH | Pitfall 4 | No RemoveProviderRewrite exists | 05-02 | COVERED | Task 3.
RESEARCH | Pitfall 5 | BackupAndRemove writes a sibling .bak, not an archive dir | 05-02 | COVERED | Task 1's dedicated helper.
CONTEXT | D-06 | Archive dir 700/600 + MANDATORY doctor-reserved | 05-02 | COVERED | Task 1, including the doctor regression test.
CONTEXT | D-07 | allowed_signers APPENDS on rotate | 05-02 | COVERED | Task 2, scoped rotate-only in the doc comment.
CONTEXT | D-09 | Per-provider insteadOf removal mechanism | 05-02 | COVERED | Task 3 mechanism only; ref-count is 05-04.
REVIEW | R-03 | ArchiveKeyPair move-vs-copy contract conflict | 05-02 | COVERED | Split into Copy/Move/Remove primitives in Task 1.
REVIEW | R-04 | Doctor-reservation regression test is vacuous | 05-02 | COVERED | Predicate applied in BuildInventory + explicit negative control.
REVIEW | R-10 | No way to undo archive entries on rollback | 05-02 | COVERED | `RemoveArchivedPair` primitive.
REVIEW | R-19 | Reserved-path matching covers only direct children | 05-02 | COVERED | Prefix containment at any depth + nesting test.
REVIEW | R-20 | os.Chmod on an existing archive dir / symlink | 05-02 | COVERED | Symlink rejection + tighten-if-wider, both tested.
REVIEW | R-21 | Stamp collision across rotate and delete | 05-02, 05-03 | COVERED | Split by owner: exclusive-create hard error tested here; the composition-root monotonic retry tested in 05-03 Task 2, where the closure is written.
REVIEW | R-22 | Rotate-twice signer-line accumulation undocumented | 05-02 | COVERED | Task 2 test + comment pinning N+1 lines and block-keyed removal.
REVIEW | R2-05 | 05-02 tests a composition-root retry that does not exist until 05-03 | 05-02, 05-03 | COVERED | The criterion is removed here and added to 05-03 Task 2 with the closure it exercises.
REVIEW | R2-06 | Source-removal failure injection is underspecified and platform-dependent | 05-02 | COVERED | Unexported `moveKeyPairToArchive` takes a `remove func(string) error`; the exported wrapper passes `os.Remove` and is pinned by its own test.
REVIEW | R2-07 | Planned reuse of the unexported `filewriter.copyFileExclusive` | 05-02 | COVERED | `keygen` reproduces the four-line exclusive-open idiom rather than exporting from the write chokepoint; the shared property is asserted in both packages' tests.
</source_audit>

## Review resolutions (cycle 1)

| Finding | Severity | Resolution in this plan |
|---|---|---|
| `ArchiveKeyPair` defined as move but used as copy by 05-04 | HIGH | Task 1 ships `CopyKeyPairToArchive`, `MoveKeyPairToArchive` (a literal composition of the copy), and `RemoveArchivedPair`. 05-03 uses Move, 05-04 uses Copy. |
| Doctor-reservation regression test is vacuous | HIGH | The exclusion moves to `BuildInventory` over the `ListKeyFiles` result via an injected predicate, and the test injects the archived path so the guard is causal. A negative control asserts the finding appears without it. The old "revert the guard and observe red in production enumeration" criterion is deleted as unsatisfiable. |
| Reserved-path matching covers only direct children | MEDIUM | Containment is a cleaned separator-bounded prefix check, with a nested-path test case. |
| `os.Chmod` on an existing archive directory | MEDIUM | Symlink rejected before any chmod; a wider-than-0700 directory is tightened; both pinned by tests. |
| Stamp collision across rotate and delete sharing one dir | LOW | Exclusive-create is a hard error; the composition root retries once with a bumped stamp; both paths tested. |

## Review resolutions (cycle 2)

| Finding | Severity | Resolution in this plan |
|---|---|---|
| This plan's acceptance criteria test a "composition-root stamp retry" that does not exist until 05-03 wires the archive closure (R2-05) | MEDIUM | The contract is split by owner rather than dropped. This plan proves the primitive's half — an existing destination is a hard error that removes nothing — and the retry criterion moves to plan 05-03 Task 2, which is the task that actually writes the closure in `buildIdentityDeps`. The archive functions' doc comments name 05-03 as the retry's owner so a reader of `archive.go` alone can find the other half. Cycle 1's R-21 coverage now spans two plans and its audit row says so. |
| Archive source-removal failure injection is underspecified; permission-based failures are platform-dependent (R2-06) | MEDIUM | The move body moves into an unexported `moveKeyPairToArchive(..., remove func(string) error)` and the exported function becomes a one-line wrapper passing `os.Remove`. The second-source failure test injects a `remove` that fails on the second path — deterministic on macOS and Linux and unaffected by running as root, and it exercises the real control flow instead of a filesystem quirk. A package-level mutable seam is explicitly rejected because it breaks parallel tests; a wrapper test proves the exported form really passes `os.Remove`. |
| The archive helper cannot literally reuse `filewriter.copyFileExclusive`, which is unexported (R2-07) | MEDIUM | Resolved by NOT reusing it: `internal/keygen` writes its own four-line exclusive-open copy, and the plan says so explicitly so the executor does not detour into an import cycle or an unplanned API change. Exporting from `filewriter` is rejected with a reason — it is the project's write chokepoint and its copy helper carries backup semantics this caller does not want, so widening its surface for one consumer weakens the chokepoint. The one property that must not drift (an existing destination errors, never overwrites) is asserted in both packages' tests. |

<verification>
Run `make test` and `make lint`. Confirm `go test -race -count=1 ./internal/doctor/...` is green, and confirm no new file in `internal/keygen` imports `internal/sshconfig` (the archive directory is a parameter, not a lookup).
</verification>

<success_criteria>
- Three new primitives exist, each in the package that already owns its concern, each fully unit-tested.
- The archive directory is registered in the ONE reserved-path registry and proven invisible to the doctor.
- No orchestration logic leaked into these primitives — they compose, they do not decide.
</success_criteria>

<output>
Create `.planning/phases/05-identity-manager/05-02-SUMMARY.md` when done.
</output>
