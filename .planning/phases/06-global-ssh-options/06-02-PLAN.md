---
phase: 06-global-ssh-options
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/globalssh/classify.go
  - internal/globalssh/classify_test.go
  - internal/globalssh/policy.go
  - internal/globalssh/policy_test.go
  - internal/globalssh/peralias.go
  - internal/globalssh/peralias_test.go
  - internal/globalssh/version.go
  - internal/globalssh/version_test.go
  - internal/tuikit/design.go
  - internal/tuikit/design_test.go
  - internal/tuikit/globalssh.go
  - internal/tuikit/globalssh_test.go
  - internal/tuikit/views.go
  - internal/dummytui/fixturebackend.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - Makefile
autonomous: true
cross_ai: true
requirements: [GSSH-01]
estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All six D-10 options — StrictHostKeyChecking, ForwardAgent, HashKnownHosts, IdentitiesOnly, AddKeysToAgent, UseKeychain — render their real current value, real provenance and real state, in that frozen top-to-bottom order."
    - "Every row carries an explanation of the risk and the recommended value (GSSH-01's core requirement): the list row shows the risk chip and the current→recommended line, and the always-visible detail pane shows the provenance label, the recommended value and the option's explanation text."
    - "A row has exactly one of four states — needs-action, already-set, set-but-differs, not-applicable — and each renders with an EXISTING glyph and an EXISTING theme role plus a distinguishing WORD (D-12). No new glyph, no new colour, no new row is introduced."
    - "An explicitly-set non-recommended value reads as respected, not nagged: it keeps the warning glyph and hue because GSSH-01 is danger-aware, and its word names it as a deliberate choice (D-12, D-14)."
    - "gitid records NO decline state anywhere — no marker comment, no sidecar file (D-14). Row state is derived from the machine's files on every read, so a declined recommendation simply reappears as unset."
    - "UseKeychain is classified from the file parse alone, never from `ssh -G`, because Apple's OpenSSH never emits it — and its provenance wording is hedged to match (D-02)."
    - "On a non-darwin machine the UseKeychain row is still VISIBLE (the frozen 6-row contract holds) and renders the not-applicable state (D-11)."
    - "IdentitiesOnly's row is verify-only: it reports whether the per-alias managed Host blocks conform, and it is never selectable for a `Host *` write (D-10)."
    - "The frozen `GlobalSSHOptions` fixture matches D-10 exactly — StrictHostKeyChecking recommends the accept-new value and ForwardAgent carries the high risk level — and a test pins the fixture against `globalssh.Policy` field-for-field so live and dummy can never disagree again."
    - "StrictHostKeyChecking's detail pane carries a NON-contractual dynamic line naming the machine's OpenSSH version, and the accept-new fix is gated on that version (D-13). That dynamic line is explicitly excluded from the copy-freeze gate."
    - "Every user-visible string this plan introduces is registered in the copy-freeze gate, and every row stays fully legible with colour disabled."
    - statement: "The status line's need-action tally counts unset rows and set-but-differs rows together, and the pinned rule is stated in the code so the ceremony's 'N of M' can never disagree with the list."
      verification: automated
  artifacts:
    - path: "internal/globalssh/classify.go"
      provides: "the four-state classifier with the D-02 UseKeychain special case and the D-11 platform gate"
    - path: "internal/globalssh/peralias.go"
      provides: "the D-10 IdentitiesOnly per-alias conformance check (verify-only, never a Host * write)"
    - path: "internal/globalssh/version.go"
      provides: "the D-13 minimum-OpenSSH gate and the dynamic version note"
    - path: "internal/tuikit/design.go"
      provides: "the D-10-corrected frozen option fixture"
    - path: "Makefile"
      provides: "copy-freeze coverage for the provenance labels, the differs word state, the not-applicable note, and an explicit exclusion assertion for the dynamic version line"
  key_links:
    - from: "internal/tuikit/globalssh.go optionRow + renderOptions"
      to: "GlobalSSHOptionView.State"
      via: "the four-state glyph/word/tone selection"
      pattern: "GlobalSSHOptionState"
    - from: "cmd/gitid/wiring.go GlobalSSHOptionStates"
      to: "globalssh.Statuses + globalssh.PerAliasConformance + globalssh.VersionNote"
      via: "one conversion site producing the rendered labels"
      pattern: "GlobalSSHOptionStates"
---

<objective>
Expand the proven one-option slice to the full GSSH-01 set, with the state model, the platform and version gates, and the corrected data the frozen fixture has been carrying wrong.

Purpose: plan 06-01 proved the architecture on the easiest option. The remaining five each carry a real complication the tracer deliberately avoided — UseKeychain is invisible to `ssh -G` and is macOS-only, IdentitiesOnly must never be written globally, StrictHostKeyChecking's recommendation depends on the machine's OpenSSH version, and any of them may already be set to something the user chose on purpose. GSSH-01 requires every option to be explained with its risk and recommended value, so this plan is where the requirement is actually satisfied, not merely scaffolded.
Output: Six honest rows — each with a real value, a real provenance, one of four states, and copy that respects a deliberate choice without hiding the risk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@recipes/ssh-config.recipe
@.planning/phases/06-global-ssh-options/06-CONTEXT.md
@.planning/phases/06-global-ssh-options/06-UI-SPEC.md
@.planning/phases/06-global-ssh-options/06-01-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md

<authority>
Every bare D-NN citation means `06-CONTEXT.md`. `06-UI-SPEC.md`'s Spacing, Typography, Color and Copywriting tables are BINDING and every addition below is budgeted there against an existing slot. Where `06-UI-SPEC.md` marks a consideration unresolved (the three/four-state row model, the probe-failure state, the not-applicable state, the loading question), this plan's stated resolution is authoritative and MUST be recorded in the summary.
</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four-state classifier, the UseKeychain and IdentitiesOnly special cases, and the platform gate (D-02, D-10, D-11, D-12, D-14)</name>
  <files>internal/globalssh/classify.go, internal/globalssh/classify_test.go, internal/globalssh/policy.go, internal/globalssh/policy_test.go, internal/globalssh/peralias.go, internal/globalssh/peralias_test.go</files>
  <read_first>
    - internal/globalssh/classify.go and probe.go as landed by plan 06-01 (`OptionStatus`, `Provenance`, `OptionState`, `Statuses`, `Deps`)
    - internal/globalssh/policy.go as landed by plan 06-01 (the six-row D-10 table)
    - internal/sshconfig/reader.go lines 50-170 (`ParseManagedHosts`, `SSHHostInfo`, `parseHostBlockBody` — the per-alias source for the IdentitiesOnly conformance check)
    - internal/platform/platform.go `SupportsUseKeychain` (the darwin gate)
    - internal/tester/tester.go `ParseResolved` (the lowercase-prefix contract that explains why `usekeychain` never appears)
    - .planning/phases/06-global-ssh-options/06-CONTEXT.md decisions D-02, D-10, D-11, D-12, D-14 and the "Specific Ideas" section's two verified platform facts
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the "UI Considerations" table rows marked unresolved
  </read_first>
  <behavior>
    - An option whose effective value equals the policy's recommended value classifies as already-set.
    - An option with an explicit value that differs from the recommendation classifies as set-but-differs, regardless of where it was set.
    - An option with no explicit value anywhere classifies as needs-action.
    - On a non-darwin host, UseKeychain classifies as not-applicable regardless of what any probe returned.
    - UseKeychain's classification is derived from the file parse only; injecting an `ssh -G` output that contains a usekeychain line does not change its classification, because real OpenSSH never emits one.
    - IdentitiesOnly classifies as already-set when every managed Host block declares it affirmatively, and as needs-action when at least one managed block does not.
    - IdentitiesOnly with zero managed Host blocks on the machine classifies as already-set with a note that there is nothing to verify — an empty machine is not a failure.
    - No classification path reads or writes any persisted decline record; classifying the same machine twice with no file change returns identical results.
    - A probe error leaves the state at needs-action and populates the probe-error note; it never promotes a row to already-set.
  </behavior>
  <action>
Complete the state model plan 06-01 deliberately left half-built.

In `classify.go`, promote `Statuses` from the tracer's single-key correctness to the full six. The state rule is a pure function of the probe result and the policy — write it as `func stateFor(p OptionPolicy, effective string, hasExplicit bool, goos string) OptionState` so it is testable without a probe:
- policy `Platform` is darwin and `goos` is not darwin, return `StateNotApplicable` (D-11) — this branch comes FIRST, before any value comparison, because a value read on the wrong platform is meaningless;
- no explicit value anywhere, return `StateNeedsAction`;
- explicit value equal to `p.Recommended` (case-insensitive, since OpenSSH is case-insensitive on values), return `StateAlreadySet`;
- explicit value present and unequal, return `StateDiffers` (D-12).

Document on `stateFor` that D-14 forbids any persisted decline record: the states are DERIVED, and re-showing a declined recommendation is intentional and acceptable because the copy respects the choice rather than nagging. That comment is where a future contributor will be tempted to add a suppression file; say no there.

Give UseKeychain its own explicit branch in the probe path rather than letting it fall through the generic diff (D-02): its value comes from `fileValues` only, and its `Provenance` is the hedged form whenever a value was found, because Apple's `ssh -G` cannot corroborate it. Put the VERIFIED platform fact in the comment — `ssh -G` never emits a usekeychain line even when the directive is set — so it reads as a designed-in fact, not a bug someone later "fixes".

Create `peralias.go` for D-10's IdentitiesOnly rule. `func PerAliasConformance(deps Deps) (conforming int, total int, offenders []string)` reads the gitid-visible config through `deps.ReadConfig`, uses `sshconfig.ParseManagedHosts` to enumerate managed Host blocks, and reports how many declare IdentitiesOnly affirmatively plus the alias names of those that do not. The IdentitiesOnly row's state comes from this, NOT from the `Host *` probe. State the rule in the file's doc comment: the recipe scopes IdentitiesOnly per alias, a global setting would break every non-gitid host on the machine, and therefore this row is verify-only and must never be selectable for a wildcard write. Add `func (p OptionPolicy) WritableToHostStar() bool` returning false for the per-alias scope, so the selectability rule is a property of the policy row rather than a scattered condition — the apply path in plan 06-03 reads it.

`internal/globalssh` must stay UI-free and must not import `internal/tuikit`. It may import `internal/sshconfig` and `internal/platform`; keep the dependency direction one-way.

Test every branch table-driven with a fake `RunSSHG`/`ReadConfig`: each of the four states, the darwin gate with `goos` both ways, the UseKeychain fake-`ssh -G`-line case (asserting the injected line does NOT change the outcome), IdentitiesOnly all-conforming / one-offender / zero-blocks, determinism across two consecutive calls, and probe error never promoting a row.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/globalssh/... -run 'State|Classify|PerAlias|UseKeychain|Platform|Determinis'</automated>
  </verify>
  <acceptance_criteria>
    - A table test drives `stateFor` through all four returned states and asserts each expected constant.
    - A test asserts that with `goos` set to a non-darwin value, UseKeychain's state is `StateNotApplicable` even when an explicit recommended value is supplied.
    - A test supplies an `ssh -G` fixture containing a `usekeychain` line, asserts UseKeychain's `CurrentValue` comes from the file fixture and not from that line, and asserts its `Provenance` is the hedged constant when the two sources disagree.
    - A test asserts `PerAliasConformance` returns the offending alias names for a config with one non-conforming managed block, and returns zero offenders with `total == 0` for a config with no managed blocks.
    - A test asserts `PolicyFor("IdentitiesOnly").WritableToHostStar()` is false and that every other policy row returns true.
    - A test calls `Statuses` twice against an unchanged fake filesystem and asserts the two results are deeply equal (no hidden persisted state, D-14).
    - A test asserts a `RunSSHG` error leaves every row's state at `StateNeedsAction` with a non-empty `ProbeError`, and that no row is `StateAlreadySet`.
    - `go list -deps ./internal/globalssh` does not name `internal/tuikit`.
  </acceptance_criteria>
  <reversibility rating="reversible">The classifier is a pure derivation over files read at request time; nothing it produces is persisted, so changing the rule later changes only the next render.</reversibility>
  <done>Six options, four states, with the two verified platform facts designed in rather than discovered later.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the frozen fixture to D-10, pin it against the live policy, and add the version-gated dynamic line (D-10, D-13)</name>
  <files>internal/tuikit/design.go, internal/tuikit/design_test.go, internal/globalssh/version.go, internal/globalssh/version_test.go, internal/dummytui/fixturebackend.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, Makefile</files>
  <read_first>
    - internal/tuikit/design.go lines 193-233 (`GlobalSSHOption`, the six `GlobalSSHOptions` entries with their current values, `GlobalSSHDetailExplanation`, `GlobalSSHAdvisoryNote`)
    - internal/globalssh/policy.go as landed by plan 06-01 (the D-10 table it must agree with)
    - internal/platform/version.go (whole file — `ProbeSSHVersion`, `SSHVersion`, `parseSSHVersion` and the distro-suffix regex note)
    - Makefile lines 267-322 (`gate-copy-freeze` — the exact list shape and grep roots this task extends)
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the "Copywriting Contract" table, especially the "Required data correction" block naming the two wrong rows
    - .planning/phases/06-global-ssh-options/06-CONTEXT.md decisions D-10 and D-13
  </read_first>
  <behavior>
    - The frozen fixture's StrictHostKeyChecking entry recommends the D-10 value and its one-liner names that value together with the OpenSSH version it requires.
    - The frozen fixture's ForwardAgent entry carries the D-10 risk level.
    - A parity test fails if any fixture row's key, recommended value or risk level diverges from the corresponding `globalssh.Policy` row.
    - The version gate reports the accept-new recommendation as available on an OpenSSH version at or above the policy minimum, and as unavailable below it.
    - A malformed or unobtainable `ssh -V` result does not block the row: the recommendation is treated as available and the dynamic line is omitted rather than showing a wrong version.
    - StrictHostKeyChecking's detail pane shows the dynamic version line; no other option's does.
    - The dynamic version line is not present in the copy-freeze list, and a test proves the gate would not be satisfied by it.
  </behavior>
  <action>
Fix the data, then make the fix un-undoable by accident.

In `internal/tuikit/design.go` correct the two rows `06-UI-SPEC.md` identifies. StrictHostKeyChecking's `Recommended` becomes D-10's value, and its `OneLiner` is rewritten to name that value and the OpenSSH version it requires — the current sentence argues for stating the OpenSSH default explicitly, which is no longer the recommendation and would read as a contradiction next to the new value. ForwardAgent's `Risk` becomes D-10's level. Change NOTHING else in the fixture: the other four rows already match D-10 byte-for-byte, and the frozen order, the `GlobalSSHDetailExplanation` text and the `GlobalSSHAdvisoryNote` text are all untouched.

Add `internal/tuikit/design_test.go` (or extend the existing design test) with the parity assertion that prevents this drifting again. It cannot import `internal/globalssh` — that would break the backend boundary — so put the parity test in `cmd/gitid` instead, where both packages are already visible, and have it walk both tables in order asserting equal `Key`, equal `Recommended` and equal `Risk` for every index, plus equal length. Name it so its purpose is obvious from a failure line. In `internal/tuikit` keep only a self-contained test asserting the fixture's six keys and their frozen order.

Create `internal/globalssh/version.go`. `func VersionGate(v platform.SSHVersion, p OptionPolicy) (available bool, note string)` compares the parsed OpenSSH version against `p.MinOpenSSH` using a numeric component comparison (not string ordering — 7.10 must be greater than 7.6), returns available plus the dynamic note when the policy names a minimum, and returns available with an empty note when it does not. An unparsed version (empty `OpenSSHVersion`) returns available with an empty note: D-13's dynamic line is a helpful extra, and refusing a recommendation because `ssh -V` was unreadable would make a probe failure into a policy decision. Reuse `platform.ProbeSSHVersion` — do not re-parse `ssh -V` here.

The dynamic note's text is `06-UI-SPEC.md`'s DRAFT row: the machine's OpenSSH version, followed by whether the recommended value is available on it. Keep it a single line so it fits the one budgeted row.

In `cmd/gitid/wiring.go`'s `GlobalSSHOptionStates`, call `platform.ProbeSSHVersion` once per read (not once per option) and populate `GlobalSSHOptionView.VersionNote` only for the option whose policy names a minimum. When the gate reports unavailable, the row must still render and still explain itself — it simply must not be applicable for selection; carry that through as the not-applicable state so the row uses the SAME state vocabulary as D-11's platform case rather than inventing a fifth.

In `Makefile`'s `gate-copy-freeze`, add the strings this plan makes final. Add an explicit NEGATIVE assertion for D-13 in the same target: a short shell check asserting the gate's own frozen list does not contain the dynamic version line's prefix, so a future contributor cannot quietly freeze a string that changes with the user's machine. Put a comment above it naming D-13 as the reason.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... ./internal/globalssh/... ./cmd/gitid/... -run 'Fixture|Parity|Version|GlobalSSHOption' && make gate-copy-freeze</automated>
  </verify>
  <acceptance_criteria>
    - A `cmd/gitid` parity test asserts `len(tuikit.GlobalSSHOptions) == len(globalssh.Policy)` and, for every index, equal `Key`, equal `Recommended` and equal `Risk`; the test fails when either table is edited alone.
    - A test asserts `tuikit.GlobalSSHOptions` entry for StrictHostKeyChecking has `Recommended` equal to `globalssh.PolicyFor("StrictHostKeyChecking").Recommended`, and that its `OneLiner` mentions that same value.
    - A test asserts the ForwardAgent entry's `Risk` equals `globalssh.PolicyFor("ForwardAgent").Risk`.
    - A `tuikit` test asserts the six fixture keys appear in the frozen order.
    - A table test drives `VersionGate` with versions below, equal to and above the policy minimum (including a two-digit minor component) and asserts the returned availability for each.
    - A test asserts `VersionGate` with a zero-value `platform.SSHVersion` returns available and an empty note.
    - A `tuikit` render test asserts the dynamic version note appears in StrictHostKeyChecking's detail pane and is absent from another option's detail pane.
    - `make gate-copy-freeze` passes, and its negative assertion fails when the dynamic version line's prefix is added to the frozen list (verify by adding it, observing the failure, and reverting).
  </acceptance_criteria>
  <reversibility rating="reversible">Both edits are data corrections inside in-repo tables, pinned by a parity test that makes any future divergence a build failure rather than a silent drift.</reversibility>
  <done>The dummy and the live backend agree on what gitid recommends, and the one fact that legitimately varies per machine is rendered dynamically and deliberately kept out of the freeze.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render four states and real provenance inside the existing row budget (D-03, D-11, D-12, D-14)</name>
  <files>internal/tuikit/views.go, internal/tuikit/globalssh.go, internal/tuikit/globalssh_test.go, internal/dummytui/fixturebackend.go, cmd/gitid/wiring.go, Makefile</files>
  <read_first>
    - internal/tuikit/globalssh.go as landed by plan 06-01 (`optionRow`, `renderOptions`, `overlaidOptions`, `view`'s status/tone computation, `gssOptionsTopLines`, `handleClick`'s checkbox hit-test)
    - internal/tuikit/theme.go (the theme roles and the frozen glyph constants — `glyphCheckOn`/`glyphCheckOff`, the healthy and warning glyphs)
    - internal/tuikit/frame.go (`masterListWidth`, `masterDetailGutter`, `frameBodyRows`, `fitPane`, `joinMasterDetail`, `previewBlockClipped`)
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the Spacing/Typography/Color tables, the "Phase 6 additions" row-cost table, the Copywriting Contract DRAFT rows, and the Focal points list
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md §6 (copy freeze) and the glyph-plus-word rule
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md §4.4 (global-ssh states)
    - Makefile lines 267-322 (`gate-copy-freeze`)
  </read_first>
  <behavior>
    - A needs-action row renders the warning glyph, the warning tone and a word naming the recommendation as still open.
    - A set-but-differs row renders the SAME warning glyph and the SAME warning tone, with a different word naming it as the user's own choice.
    - An already-set row renders the healthy glyph and the healthy tone.
    - A not-applicable row renders neither a selection checkbox nor a recommendation, and its current-value text names it as a platform-only setting.
    - A not-applicable row and an already-set row are not selectable; pressing the toggle key on them does not change the selection set.
    - The IdentitiesOnly row is not selectable even when it needs action, and its detail pane explains that the fix is per-alias.
    - The detail pane shows the provenance label for the selected row, in the same block that already carries current, risk and recommended — no additional row is introduced.
    - A row whose probe failed renders its probe-error note in the detail pane and is not selectable.
    - Every state remains distinguishable when colour output is disabled.
    - The status line's tally counts needs-action rows and set-but-differs rows together, and excludes not-applicable rows.
    - The rendered frame stays within the fixed capture geometry for every combination of states.
  </behavior>
  <action>
Render the four states without spending a single new row, a new glyph or a new hue — `06-UI-SPEC.md` budgets every addition against a slot that already exists.

Replace the binary `NeedsAction` reasoning in `internal/tuikit/globalssh.go` with `GlobalSSHOptionView.State`. `optionRow`'s signature changes to take the state instead of the `needsAction bool`; keep its 2-line shape exactly (`optionRowLines` stays 2). Line 1 keeps marker, checkbox slot, tone glyph, key name and risk chip; line 2 keeps the indented current→recommended line, now fed by the real value. The tone glyph selection becomes: healthy glyph for already-set, warning glyph for BOTH needs-action and set-but-differs, and for not-applicable use the existing faint styling with no tone glyph and no checkbox — a blank selection slot is already what a non-selectable row renders today.

The distinguishing WORD goes on line 2, appended to the existing current→recommended text, because that line is already faint prose and has room: needs-action keeps the plain recommendation form, set-but-differs uses `06-UI-SPEC.md`'s DRAFT phrasing naming it as the user's choice, not-applicable replaces the whole current-value text with the DRAFT macOS-only phrasing and omits the arrow entirely (there is nothing to move toward). This is `02-UX-DIRECTION.md`'s glyph-plus-word rule applied to the new state: same glyph, same hue, different word — a user scanning by colour still sees that both flagged states want attention, and the word tells them which one is already decided.

Add `func (o GlobalSSHOptionView) Selectable() bool` in `views.go` returning true only for needs-action and set-but-differs rows that are writable to the wildcard block and carry no probe error. Route the toggle key, the checkbox click hit-test and the row's checkbox rendering through that ONE predicate, so there is exactly one definition of selectable and the click path can never disagree with the key path. The IdentitiesOnly exclusion arrives as a field on the view — add `WritableToHostStar bool` to `GlobalSSHOptionView` and populate it in `cmd/gitid/wiring.go` from the policy — rather than a key-name comparison inside the render package, which would put policy in the wrong layer.

In the detail pane, insert the provenance label into the existing detail block, between the key name heading and the explanation, and append the probe-error note when one is present. Keep `fitPane`'s wrap-and-visible-clip behavior: long provenance text must clip with the visible cue, never silently.

Update `view`'s status line: the tally counts rows whose state is needs-action or set-but-differs, and skips not-applicable rows. Pin the rule in a comment as this plan's resolution of the `06-CONTEXT.md` discretion item, so the ceremony's own count in plan 06-03 reads from the same predicate rather than re-deriving one.

Update `internal/dummytui/fixturebackend.go`'s projection so the frozen fixture's boolean maps onto the new states (its already-set rows to already-set, its needs-action rows to needs-action) and the dummy's rendering is unchanged.

Register the DRAFT strings this task makes real in `Makefile`'s `gate-copy-freeze`: the three provenance label forms, the hedge form, the differs word, and the not-applicable text.

Add a colour-disabled render test: render the Options sub-tab with each of the four states present and assert that with styling stripped the four rows are still distinguishable by their text alone. Add a geometry test asserting the rendered frame's line count and maximum line width stay within the fixed capture geometry with all four states present plus the findings banner.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... ./internal/dummytui/... -run 'GlobalSSH|OptionRow|Selectable|Provenance|NoColor|Geometry' && make gate-copy-freeze</automated>
  </verify>
  <acceptance_criteria>
    - A render test drives all four states through `optionRow` and asserts each rendered row is exactly 2 lines.
    - A render test asserts the needs-action row and the set-but-differs row use the SAME tone glyph and the same theme role, and that their line-2 texts differ.
    - A render test asserts the already-set row uses the healthy glyph.
    - A render test asserts the not-applicable row contains neither checkbox glyph and contains the not-applicable text.
    - A model test presses the toggle key on an already-set row, a not-applicable row, a probe-error row and the IdentitiesOnly row and asserts the selection set is unchanged in each case; the same test drives a checkbox CLICK at each of those rows' coordinates and asserts the selection set is likewise unchanged.
    - A model test presses the toggle key on a set-but-differs row and asserts the selection set gains that key (a deliberate non-recommended value is still fixable).
    - A render test asserts the detail pane for a selected row contains that row's provenance label text, and that a probe-error row's detail pane contains its error note.
    - A test renders the Options sub-tab with styling stripped and asserts the four states remain distinguishable by text alone.
    - A test asserts the status-line tally equals the count of needs-action plus set-but-differs rows for a fixture containing all four states.
    - A geometry test asserts the rendered frame stays within the fixed capture geometry with all four states plus the findings banner present.
    - `make gate-copy-freeze` passes with the new strings registered.
    - `go test ./internal/dummytui/ -run TestNoBackendAllowlist` passes.
  </acceptance_criteria>
  <reversibility rating="reversible">Render-only changes over an existing pane; the state vocabulary is internal and has two in-repo implementers.</reversibility>
  <done>Six rows, four states, every one explained — inside the same 100x30 frame the design was approved in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config files → classifier | Hand-written directives, including hostile values, drive the state and the rendered labels. |
| `ssh -V` / `ssh -G` output → rendered copy | Subprocess output reaches the screen as the dynamic version line and the provenance label. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Spoofing | rendered provenance label | high | mitigate | The label distinguishes user-set from system-set from default from the hedged form; when sources disagree the hedge is used rather than a confident claim, so gitid never tells the user a directive is theirs when it may not be. |
| T-06-09 | Tampering | probe output reaching the render layer | medium | mitigate | Values pass through `truncLine`/`fitPane` which clip to the pane; a config value containing control characters cannot break the frame. Add the assertion to the geometry test's fixture. |
| T-06-10 | Information Disclosure | provenance label paths | medium | mitigate | Every path in a label routes through `displayPath` in the one conversion site, so absolute sandbox paths are never surfaced raw. |
| T-06-11 | Repudiation | set-but-differs classification | medium | mitigate | The state is derived from the file on every read (D-14 stateless); there is no record gitid could get wrong, and the word explicitly attributes the value to the user rather than to gitid. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages. Every dependency is already in `go.mod`. |
</threat_model>

<verification>
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green.
- `make lint` reports 0 issues.
- `make test` green, including `gate-copy-freeze` with the new strings and the D-13 exclusion assertion.
- `go list -deps ./internal/tuikit` names no first-party backend package.
- `go list -deps ./internal/globalssh` does not name `internal/tuikit`.
</verification>

<success_criteria>
- All six GSSH-01 options render real values, real provenance and one of four states.
- The frozen fixture and the live policy agree, pinned by a failing-on-drift test.
- No new glyph, colour, row or screen was introduced.
- Nothing about a declined recommendation is persisted anywhere.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 06-02)

**`internal/globalssh`:** `stateFor`, `PerAliasConformance`, `(OptionPolicy).WritableToHostStar`, `VersionGate`.

**`internal/tuikit`:** `(GlobalSSHOptionView).Selectable`, `GlobalSSHOptionView.WritableToHostStar`, `GlobalSSHOptionView.VersionNote`; corrected `GlobalSSHOptions` fixture rows.

**`cmd/gitid`:** the fixture-versus-policy parity test; `GlobalSSHOptionStates` extended with provenance labels, per-alias conformance and the version gate.

**`Makefile`:** `gate-copy-freeze` extended with the provenance labels, the differs word, the not-applicable text, and a negative assertion excluding the dynamic version line.
</artifacts>

<output>
Create `.planning/phases/06-global-ssh-options/06-02-SUMMARY.md` when done. Record explicitly, as this plan's authoritative resolutions of `06-UI-SPEC.md`'s unresolved items: the Go type chosen for the state model, the probe-failure render decision, the not-applicable render decision, the measured cost of the three-probe read (and whether it needed a loading affordance), and the pinned tally rule.
</output>
