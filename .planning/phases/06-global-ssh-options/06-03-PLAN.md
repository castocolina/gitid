---
phase: 06-global-ssh-options
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - internal/globalssh/classify.go
  - internal/globalssh/classify_test.go
  - internal/globalssh/policy.go
  - internal/globalssh/policy_test.go
  - internal/globalssh/peralias.go
  - internal/globalssh/peralias_test.go
  - internal/globalssh/version.go
  - internal/globalssh/version_test.go
  - internal/tuikit/design.go
  - internal/tuikit/design_test.go
  - internal/tuikit/globalssh.go
  - internal/tuikit/globalssh_test.go
  - internal/tuikit/views.go
  - internal/dummytui/fixturebackend.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - Makefile
autonomous: true
cross_ai: true
requirements: [GSSH-01]
estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All six D-10 options — StrictHostKeyChecking, ForwardAgent, HashKnownHosts, IdentitiesOnly, AddKeysToAgent, UseKeychain — render their real current value, real provenance and real state, in that frozen top-to-bottom order."
    - "Every row carries an explanation of the risk and the recommended value (GSSH-01's core requirement): the list row shows the risk chip and the current→recommended line, and the always-visible detail pane shows the provenance label, the recommended value and the option's explanation text."
    - "A row has exactly one of four VISUAL states — needs-action, already-set, set-but-differs, not-applicable — and each renders with an EXISTING glyph and an EXISTING theme role plus a distinguishing WORD (D-12). No new glyph, no new colour, no new row is introduced."
    - "State and ATTRIBUTION are two different things and the copy keeps them apart: a value gitid parsed itself is worded as the user's own choice; a value that is merely effective from somewhere gitid does not read is worded as coming from outside their config. gitid never tells a user a directive is theirs when the evidence does not say so."
    - "STATE is decided by the VALUE, never by the source. An option whose effective value already equals the recommendation is ALREADY-SET even when nothing on the machine sets it — `ForwardAgent no` is OpenSSH's own default and equals the D-10 recommendation, so it is safe, not outstanding. The source class only decides the WORDS: an already-set-by-default row says so, and is never attributed to the user or to a file."
    - "Probe evidence is per-option and per-source: a failure of the generic resolution probe does NOT downgrade UseKeychain (file-only) or IdentitiesOnly (derived from managed blocks), because neither depends on that probe."
    - "A not-applicable row always carries a REASON — wrong platform, OpenSSH too old, compatibility unverified, or nothing on this machine to verify — and each reason has its own words, so the macOS-only sentence can never appear on a version-gated row."
    - "gitid records NO decline state anywhere — no marker comment, no sidecar file (D-14). Row state is derived from the machine's files on every read, so a declined recommendation simply reappears as unset."
    - "UseKeychain is classified from the file parse alone, never from `ssh -G`, because Apple's OpenSSH never emits it — and its provenance wording is hedged to match (D-02)."
    - "On a non-darwin machine the UseKeychain row is still VISIBLE (the frozen 6-row contract holds) and renders the not-applicable state with the platform reason (D-11)."
    - "IdentitiesOnly's row is verify-only: it reports whether the per-alias managed Host blocks conform, it is never selectable for a wildcard write (D-10), and a machine with no managed blocks reads as nothing-to-verify rather than as conformant."
    - "The accept-new fix is never written when gitid could not establish the machine's OpenSSH version (D-13): an unreadable version is an explicit compatibility-unverified state, not a silent assumption of availability."
    - "The frozen `GlobalSSHOptions` fixture matches D-10 exactly, and a test pins the fixture against `globalssh.Policy` field-for-field so live and dummy can never disagree again."
    - "Every user-visible string this plan introduces is registered in the copy-freeze gate, the dynamic version line is explicitly excluded from it, and every row stays fully legible with colour disabled."
    - statement: "The status line's need-action tally counts unset rows and set-but-differs rows together, and the pinned rule is stated in the code so the ceremony's 'N of M' can never disagree with the list."
      verification: automated
  artifacts:
    - path: "internal/globalssh/classify.go"
      provides: "the four-state classifier keyed on source class as well as value, with the D-02 UseKeychain special case, the D-11 platform gate and per-source probe-error handling"
    - path: "internal/globalssh/peralias.go"
      provides: "the D-10 IdentitiesOnly per-alias conformance check (verify-only, never a wildcard write)"
    - path: "internal/globalssh/version.go"
      provides: "the D-13 minimum-OpenSSH gate, its compatibility-unverified outcome, and the dynamic version note"
    - path: "internal/tuikit/views.go"
      provides: "GlobalSSHNotApplicableReason — the reason code that keeps four different non-applicability messages from drifting into one"
    - path: "Makefile"
      provides: "copy-freeze coverage for the provenance labels, the differs word state, the four not-applicable reasons, and an explicit exclusion assertion for the dynamic version line"
  key_links:
    - from: "internal/tuikit/globalssh.go optionRow + renderOptions"
      to: "GlobalSSHOptionView.State + GlobalSSHOptionView.NotApplicableReason"
      via: "the four-state glyph/word/tone selection plus the per-reason wording"
      pattern: "GlobalSSHOptionState"
    - from: "cmd/gitid/wiring.go GlobalSSHOptionStates"
      to: "globalssh.Statuses + globalssh.PerAliasConformance + globalssh.VersionGate"
      via: "one conversion site producing the rendered labels"
      pattern: "GlobalSSHOptionStates"
---

<objective>
Expand the proven one-option slice to the full GSSH-01 set, with a state model that never misattributes someone else's value to the user.

Purpose: plan 06-01 proved the architecture on the easiest option. The remaining five each carry a real complication the tracer deliberately avoided — UseKeychain is invisible to `ssh -G` and is macOS-only, IdentitiesOnly must never be written globally, StrictHostKeyChecking's recommendation depends on the machine's OpenSSH version, and any of them may already be set to something the user chose on purpose, or to something a machine-wide file chose for them. GSSH-01 requires every option to be explained with its risk and recommended value, so this plan is where the requirement is actually satisfied. `06-REVIEWS.md` found that the previous draft's state model collapsed "the user set this" and "this is effective from somewhere" into one word; that conflation is corrected here, at the source, before it reaches copy.
Output: Six honest rows — each with a real value, a real attribution, one of four visual states, and copy that respects a deliberate choice without hiding the risk or inventing an author.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@recipes/ssh-config.recipe
@.planning/phases/06-global-ssh-options/06-CONTEXT.md
@.planning/phases/06-global-ssh-options/06-UI-SPEC.md
@.planning/phases/06-global-ssh-options/06-REVIEWS.md
@.planning/phases/06-global-ssh-options/06-01-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md

<authority>
Every bare D-NN citation means `06-CONTEXT.md`. `06-UI-SPEC.md`'s Spacing, Typography, Color and Copywriting tables are BINDING and every addition below is budgeted there against an existing slot. Where `06-UI-SPEC.md` marks a consideration unresolved (the three/four-state row model, the probe-failure state, the not-applicable state, the loading question), this plan's stated resolution is authoritative and MUST be recorded in the summary.

`06-REVIEWS.md`'s findings on this plan's predecessor are BINDING: explicit is not the same as effective; a probe failure must not invalidate independent evidence; an empty IdentitiesOnly inventory is not conformance; an unknown OpenSSH version must not license an unverified write; and non-applicability needs a reason code.
</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four-state classifier keyed on source class, the UseKeychain and IdentitiesOnly special cases, and per-source probe-error handling (D-02, D-10, D-11, D-12, D-14)</name>
  <files>internal/globalssh/classify.go, internal/globalssh/classify_test.go, internal/globalssh/policy.go, internal/globalssh/policy_test.go, internal/globalssh/peralias.go, internal/globalssh/peralias_test.go</files>
  <read_first>
    - internal/globalssh/classify.go and probe.go as landed by plan 06-01 (`OptionStatus`, `SourceClass`, `OptionState`, `Statuses`, `Deps`, and the doc comment recording why a system file is never inferred)
    - internal/globalssh/policy.go as landed by plan 06-01 (the six-row D-10 table)
    - internal/sshconfig/reader.go lines 50-170 (`ParseManagedHosts`, `SSHHostInfo`, `parseHostBlockBody` — the per-alias source for the IdentitiesOnly conformance check)
    - internal/platform/platform.go `SupportsUseKeychain` (the darwin gate)
    - internal/tester/tester.go `ParseResolved` (the lowercase-prefix contract that explains why `usekeychain` never appears)
    - .planning/phases/06-global-ssh-options/06-CONTEXT.md decisions D-02, D-10, D-11, D-12, D-14 and the "Specific Ideas" section's two verified platform facts
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the "UI Considerations" table rows marked unresolved
    - .planning/phases/06-global-ssh-options/06-REVIEWS.md — the HIGH findings on explicit-versus-effective and on probe-error handling
  </read_first>
  <behavior>
    - An option whose effective value equals the policy's recommended value classifies as already-set, WHATEVER ITS SOURCE — including a value that is merely OpenSSH's own safe default. `ForwardAgent` resolving to `no` on a machine that sets it nowhere is ALREADY SET, not needing action: the recommendation is already true and telling the user otherwise is a false alarm.
    - An option whose value came from the baseline class (set in no file gitid or `ssh` reads) and does NOT equal the recommendation classifies as needs-action — there is nothing of the user's to change, the recommendation is simply still open.
    - An option with a value that differs from the recommendation AND was actually set somewhere classifies as set-but-differs, and its status records WHICH source class the value came from so the render layer can word it truthfully.
    - An option with no value from any source classifies as needs-action.
    - The equality check runs BEFORE any source-class branch, so no source class can force a value that already matches the recommendation into needs-action.
    - On a non-darwin host, UseKeychain classifies as not-applicable with the platform reason, regardless of what any probe returned.
    - UseKeychain's classification is derived from the file parse only; injecting an `ssh -G` output that contains a usekeychain line does not change its classification, because real OpenSSH never emits one.
    - A failure of the generic resolution probe leaves UseKeychain's classification unchanged, because UseKeychain never consults that probe.
    - A failure of the generic resolution probe leaves IdentitiesOnly's classification unchanged, because it is derived from the managed blocks in the parsed config.
    - A failure of the generic resolution probe leaves every option that DOES depend on it at needs-action with a per-option probe-error note, and promotes none of them to already-set.
    - A failure of the config READ, as distinct from the resolution probe, leaves the file-derived options with their own probe-error note.
    - IdentitiesOnly classifies as already-set when every managed Host block declares it affirmatively, and as needs-action when at least one managed block does not.
    - IdentitiesOnly with zero managed Host blocks classifies as not-applicable with the nothing-to-verify reason — an empty machine is neither a failure nor conformance.
    - No classification path reads or writes any persisted decline record; classifying the same machine twice with no file change returns identical results.
  </behavior>
  <action>
Complete the state model plan 06-01 deliberately left half-built, and correct the conflation the review found.

In `classify.go`, promote `Statuses` from the tracer's single-key correctness to the full six. The state rule is a pure function of the probe result, the source class and the policy — write it as `func stateFor(p OptionPolicy, effective string, src SourceClass, goos string) (OptionState, NotApplicableReason)` so it is testable without a probe:
**The branch ORDER is load-bearing and the previous revision had it wrong** — `06-REVIEWS.md`'s cycle-2 HIGH finding is that a baseline-sourced value was routed to `StateNeedsAction` BEFORE the equality check ever ran, so `ForwardAgent no` (OpenSSH's own default, and exactly what `internal/tuikit/design.go:214`'s fixture records) would have been flagged as needing action while already matching the D-10 recommendation of `no`. Write the branches in EXACTLY this order and put the reason on each:

1. policy `Platform` is darwin and `goos` is not darwin → `StateNotApplicable` with `ReasonPlatform` (D-11). FIRST, before any value comparison, because a value read on the wrong platform is meaningless.
2. there is no value at all, from any source → `StateNeedsAction` with `ReasonNone`. Nothing to compare.
3. **value equals `p.Recommended`** (case-insensitive, since OpenSSH is case-insensitive on values) → `StateAlreadySet`, **regardless of `src`**. This is the branch the review found misordered, and it must come before ANY source-class test. The recommendation being already true is a property of the VALUE; where the value came from is a separate question answered by `OptionStatus.Source`, which the render layer uses for wording and which `stateFor` must not consult to decide state. A machine on which the recommendation already holds by OpenSSH default is a machine with nothing to fix, and offering to write a directive that changes nothing is noise the advisory posture cannot afford.
4. `src` is the baseline class (unequal, and set in no file gitid or `ssh` reads) → `StateNeedsAction`. Reaching here means the value is BOTH not the recommendation AND not something anyone chose — the recommendation is still open and there is nothing of the user's to overwrite. This is what the baseline branch was actually for; putting it after equality is what makes it correct.
5. value present, unequal, and set somewhere → `StateDiffers` (D-12).

The state is deliberately the same in step 5 whether the value came from the user's own file or from somewhere outside it, because the VISUAL vocabulary is four states and `06-UI-SPEC.md` budgets exactly four. What must NOT collapse is the attribution: `OptionStatus.Source` already carries the class, and the render layer words the row from it. Say so in `stateFor`'s doc comment, naming the review finding: an explicitly present value is not evidence that the USER placed it there, and the word "your choice" is only correct for the gitid-parsed class.

Add a second sentence to that doc comment covering the mirror-image case introduced by step 3: an ALREADY-SET row whose source is the baseline class is already-set because OpenSSH's own default happens to be safe, not because anyone configured it. Task 3's wording rule follows from that and is stated there.

Document on `stateFor` that D-14 forbids any persisted decline record: the states are DERIVED, and re-showing a declined recommendation is intentional and acceptable because the copy respects the choice rather than nagging. That comment is where a future contributor will be tempted to add a suppression file; say no there.

Restructure probe-error handling per the review. `Statuses` currently has one failure mode for the whole read; split it by EVIDENCE SOURCE. Track three independent outcomes — the resolution probe, the isolated baseline probe, and the config read — and attach a probe-error note only to the options that actually depended on the source that failed. Write the dependency map explicitly next to the code so it cannot drift: UseKeychain depends on the config read alone (D-02); IdentitiesOnly depends on the config read alone (per-alias conformance); the other four depend on the resolution probe and, for the baseline class only, on the isolated probe. An option whose sources all succeeded keeps its real state even when an unrelated source failed. Add `OptionStatus.ProbeError` per row as today, but populate it only from the sources that row consulted.

Give UseKeychain its own explicit branch in the probe path rather than letting it fall through the generic diff (D-02): its value comes from the file scan only, and its source class is the gitid-parsed class when found and the baseline class when not — never a class derived from `ssh -G`, because Apple's OpenSSH cannot corroborate it. Put the VERIFIED platform fact in the comment — `ssh -G` never emits a usekeychain line even when the directive is set — so it reads as a designed-in fact, not a bug someone later "fixes".

Create `peralias.go` for D-10's IdentitiesOnly rule. `func PerAliasConformance(deps Deps) (conforming int, total int, offenders []string, err error)` reads the gitid-visible config through `deps.ReadConfig`, uses `sshconfig.ParseManagedHosts` to enumerate managed Host blocks, and reports how many declare IdentitiesOnly affirmatively plus the alias names of those that do not. `total == 0` is the nothing-to-verify signal the classifier maps to `StateNotApplicable` with `ReasonNothingToVerify` — the review is explicit that calling an empty inventory "already set" is inaccurate, because nothing was verified. State the rule in the file's doc comment: the recipe scopes IdentitiesOnly per alias, a global setting would break every non-gitid host on the machine, and therefore this row is verify-only and must never be selectable for a wildcard write. Add `func (p OptionPolicy) WritableToHostStar() bool` returning false for the per-alias scope, so the selectability rule is a property of the policy row rather than a scattered condition — plan 06-01's `runGlobalSSHApply` already rejects by name, and plan 06-04's ceremony reads the same predicate.

Declare `type NotApplicableReason int` in `internal/globalssh` with `ReasonNone`, `ReasonPlatform`, `ReasonVersionTooOld`, `ReasonVersionUnverified`, `ReasonNothingToVerify`, and carry it on `OptionStatus`. Four different situations were about to share one silent enum value and therefore one sentence; the review flagged it and this is the fix.

`internal/globalssh` must stay UI-free and must not import `internal/tuikit`. It may import `internal/sshconfig` and `internal/platform`; keep the dependency direction one-way.

Run the independent probes CONCURRENTLY under one overall bounded context rather than sequentially, so three three-second budgets cannot compose into a nine-second screen activation, and record the measured worst-case and typical latency in a test and in the summary. This is the review's LOW finding on activation latency, resolved here rather than deferred to implementation.

Test every branch table-driven with a fake resolution runner and a fake config reader: each of the four states; the darwin gate with `goos` both ways; the UseKeychain fake-`ssh -G`-line case asserting the injected line does NOT change the outcome; UseKeychain and IdentitiesOnly surviving a resolution-probe failure unchanged; the four resolution-dependent options degrading on that same failure; a config-read failure degrading the file-derived options; IdentitiesOnly all-conforming / one-offender / zero-blocks; determinism across two consecutive calls; and each `NotApplicableReason` reached by its own path.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/globalssh/... -run 'State|Classify|PerAlias|UseKeychain|Platform|Determinis|ProbeError|Reason|Latency'</automated>
  </verify>
  <acceptance_criteria>
    - A table test drives `stateFor` through all four returned states and asserts each expected constant and its accompanying reason.
    - **The cycle-2 HIGH, pinned by its real case:** a test calls `stateFor` for `ForwardAgent` with `effective` = `"no"` and `src` = the BASELINE class and asserts `StateAlreadySet` — NOT `StateNeedsAction`. This is the exact machine state `internal/tuikit/design.go:214`'s fixture records and the D-10 recommendation for `ForwardAgent` is `no`, so the previous branch order would have raised a false alarm on a safe default.
    - A test asserts `stateFor` returns `StateAlreadySet` for a value equal to the recommendation under EVERY `SourceClass` constant in turn (table-driven over the enum), proving no source class can override the equality branch.
    - A test asserts `stateFor` with a baseline-class value that does NOT equal the recommendation returns `StateNeedsAction`, and with a gitid-parsed value that does not equal it returns `StateDiffers` — the two unequal branches are distinguished by source, the equal branch is not.
    - A test asserts a value differing from the recommendation only by letter case classifies as `StateAlreadySet`, so the case-insensitivity is a tested property rather than an incidental one.
    - A test asserts that with `goos` set to a non-darwin value, UseKeychain's state is `StateNotApplicable` with `ReasonPlatform`, even when an explicit recommended value is supplied.
    - A test supplies an `ssh -G` fixture containing a `usekeychain` line, asserts UseKeychain's `CurrentValue` comes from the file fixture and not from that line, and asserts its source class is the gitid-parsed class.
    - A test makes the resolution probe fail and asserts UseKeychain's state and `ProbeError` are IDENTICAL to the all-probes-succeed run — the review's per-source finding, pinned.
    - A test makes the resolution probe fail and asserts IdentitiesOnly's state and `ProbeError` are likewise identical to the succeeding run.
    - A test makes the resolution probe fail and asserts each of the four resolution-dependent rows is `StateNeedsAction` with a non-empty `ProbeError`, and that none of the six rows is `StateAlreadySet`.
    - A test makes the config read fail and asserts UseKeychain and IdentitiesOnly carry a non-empty `ProbeError` while the resolution-dependent rows keep their real states.
    - A test asserts `PerAliasConformance` returns the offending alias names for a config with one non-conforming managed block, and returns `total == 0` with no offenders for a config with no managed blocks.
    - A test asserts IdentitiesOnly with zero managed blocks classifies as `StateNotApplicable` with `ReasonNothingToVerify` and NOT as `StateAlreadySet`.
    - A test asserts `PolicyFor("IdentitiesOnly").WritableToHostStar()` is false and that every other policy row returns true.
    - A test calls `Statuses` twice against an unchanged fake filesystem and asserts the two results are deeply equal (no hidden persisted state, D-14).
    - A test asserts a `StateDiffers` row sourced from the gitid-parsed class and one sourced from the outside-gitid class produce the SAME state and DIFFERENT source classes.
    - A test measures `Statuses` against a fake whose every probe sleeps close to the per-probe budget and asserts the total elapsed time is below the sum of the individual budgets, proving the probes run concurrently.
    - `go list -deps ./internal/globalssh` does not name `internal/tuikit`.
  </acceptance_criteria>
  <reversibility rating="reversible">The classifier is a pure derivation over files read at request time; nothing it produces is persisted, so changing the rule later changes only the next render.</reversibility>
  <done>Six options, four states, one honest attribution each, and a probe failure that only invalidates what actually depended on it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the frozen fixture to D-10, pin it against the live policy, and gate accept-new on a version gitid could actually read (D-10, D-13)</name>
  <files>internal/tuikit/design.go, internal/tuikit/design_test.go, internal/globalssh/version.go, internal/globalssh/version_test.go, internal/dummytui/fixturebackend.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, Makefile</files>
  <read_first>
    - internal/tuikit/design.go lines 193-233 (`GlobalSSHOption`, the six `GlobalSSHOptions` entries with their current values, `GlobalSSHDetailExplanation`, `GlobalSSHAdvisoryNote`)
    - internal/globalssh/policy.go as landed by plan 06-01 (the D-10 table it must agree with)
    - internal/platform/version.go (whole file — `ProbeSSHVersion`, `SSHVersion`, `parseSSHVersion` and the distro-suffix regex note; note that a malformed parse yields a ZERO `OpenSSHVersion` with a nil error)
    - Makefile lines 267-322 (`gate-copy-freeze` — the exact list shape and grep roots this task extends)
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the "Copywriting Contract" table, especially the "Required data correction" block naming the two wrong rows
    - .planning/phases/06-global-ssh-options/06-CONTEXT.md decisions D-10 and D-13
    - .planning/phases/06-global-ssh-options/06-REVIEWS.md — the finding that an unknown version treated as available conflicts with D-13
  </read_first>
  <behavior>
    - The frozen fixture's StrictHostKeyChecking entry recommends the D-10 value and its one-liner names that value together with the OpenSSH version it requires.
    - The frozen fixture's ForwardAgent entry carries the D-10 risk level.
    - A parity test fails if any fixture row's key, recommended value or risk level diverges from the corresponding `globalssh.Policy` row.
    - The version gate reports the recommendation as available on an OpenSSH version at or above the policy minimum, and as unavailable-too-old below it, comparing numeric components so a two-digit minor beats a one-digit one.
    - A malformed or unobtainable version reports the recommendation as UNVERIFIED — neither available nor too old — and the row is not selectable in that state.
    - The row is still rendered and still fully explained in every version outcome; only its selectability changes.
    - StrictHostKeyChecking's detail pane shows the dynamic version line when a version was readable, and a distinct compatibility-unverified line when it was not; no other option's detail pane shows either.
    - The dynamic version line is not present in the copy-freeze list, and a test proves the gate would not be satisfied by it.
  </behavior>
  <action>
Fix the data, then make the fix un-undoable by accident, then stop the version gate from guessing.

In `internal/tuikit/design.go` correct the two rows `06-UI-SPEC.md` identifies. StrictHostKeyChecking's `Recommended` becomes D-10's value, and its `OneLiner` is rewritten to name that value and the OpenSSH version it requires — the current sentence argues for stating the OpenSSH default explicitly, which is no longer the recommendation and would read as a contradiction next to the new value. ForwardAgent's `Risk` becomes D-10's level. Change NOTHING else in the fixture: the other four rows already match D-10 byte-for-byte, and the frozen order, the detail-explanation text and the advisory-note text are all untouched.

Add the parity assertion that prevents this drifting again. It cannot live in `internal/tuikit` — importing `internal/globalssh` there would break the backend boundary — so put it in `cmd/gitid`, where both packages are already visible, and have it walk both tables in order asserting equal `Key`, equal `Recommended` and equal `Risk` for every index, plus equal length. Name it so its purpose is obvious from a failure line. In `internal/tuikit` keep only a self-contained test asserting the fixture's six keys and their frozen order.

Create `internal/globalssh/version.go`. Declare `type VersionOutcome int` with `VersionAvailable`, `VersionTooOld`, `VersionUnverified`, and `func VersionGate(v platform.SSHVersion, p OptionPolicy) (VersionOutcome, string)` comparing the parsed OpenSSH version against `p.MinOpenSSH` using a numeric component comparison — not string ordering, because `7.10` must be greater than `7.6`. A policy with no minimum returns `VersionAvailable` with an empty note. A parsed version at or above the minimum returns `VersionAvailable` with the dynamic note; below it, `VersionTooOld` with the note.

An EMPTY `OpenSSHVersion` returns `VersionUnverified`. This reverses the predecessor draft, which returned available, and the reason is the review's: D-13 exists to gate the fix on OpenSSH at least 7.6, and treating an unreadable `ssh -V` as proof of compliance would write a directive whose compatibility gitid never established. The row still renders and still explains itself in full — the advisory posture is preserved for the EXPLANATION, which is what GSSH-01 requires — but the WRITE is withheld until the machine can be identified. Put the trade-off in the doc comment together with the escape hatch: `plan 06-06`'s CLI reports the same refusal with an error naming `ssh -V` as the command to check, so a user on an exotic build is told exactly what to fix rather than silently blocked. Reuse `platform.ProbeSSHVersion` — do not re-parse the version string here.

The dynamic note's text is `06-UI-SPEC.md`'s DRAFT row: the machine's OpenSSH version, followed by whether the recommended value is available on it. Keep it a single line so it fits the one budgeted row. The unverified case gets its OWN single line saying the version could not be read and naming the check command — a separate, frozen string, because the review's point about non-applicability needing distinct words applies here too.

In `cmd/gitid/wiring.go`'s `GlobalSSHOptionStates`, call `platform.ProbeSSHVersion` once per read (not once per option) and populate `GlobalSSHOptionView.VersionNote` only for the option whose policy names a minimum. Map `VersionTooOld` onto the not-applicable state with `ReasonVersionTooOld` and `VersionUnverified` onto the not-applicable state with `ReasonVersionUnverified`, so the row uses the SAME state vocabulary as D-11's platform case rather than inventing a fifth visual state — while the reason code keeps their words apart.

In `Makefile`'s `gate-copy-freeze`, add the strings this plan makes final. Add an explicit NEGATIVE assertion for D-13 in the same target: a short shell check asserting the gate's own frozen list does not contain the dynamic version line's prefix, so a future contributor cannot quietly freeze a string that changes with the user's machine. Put a comment above it naming D-13 as the reason.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... ./internal/globalssh/... ./cmd/gitid/... -run 'Fixture|Parity|Version|GlobalSSHOption' && make gate-copy-freeze</automated>
  </verify>
  <acceptance_criteria>
    - A `cmd/gitid` parity test asserts `len(tuikit.GlobalSSHOptions) == len(globalssh.Policy)` and, for every index, equal `Key`, equal `Recommended` and equal `Risk`; the test fails when either table is edited alone.
    - The same `cmd/gitid` parity test pins the NUMERIC values of the two `NotApplicableReason` enums against each other, constant by constant — `internal/globalssh`'s and `internal/tuikit`'s. They must be declared separately (the backend boundary forbids tuikit importing globalssh), and `cmd/gitid` is the only package that can see both, so this is where a silent renumbering is caught (`06-REVIEWS.md` cycle-2 LOW).
    - A test asserts the StrictHostKeyChecking fixture entry's `Recommended` equals `globalssh.PolicyFor("StrictHostKeyChecking").Recommended`, and that its `OneLiner` mentions that same value.
    - A test asserts the ForwardAgent entry's `Risk` equals `globalssh.PolicyFor("ForwardAgent").Risk`.
    - A `tuikit` test asserts the six fixture keys appear in the frozen order.
    - A table test drives `VersionGate` with versions below, equal to and above the policy minimum, including a two-digit minor component, and asserts the returned outcome for each.
    - A test asserts `VersionGate` with a zero-value `platform.SSHVersion` returns `VersionUnverified` and a non-empty note naming the version-check command — NOT `VersionAvailable`.
    - A `cmd/gitid` test asserts a `VersionUnverified` outcome produces a view whose state is not-applicable with `ReasonVersionUnverified`, whose explanation text is still non-empty, and that `runGlobalSSHApply` refuses that key with an error rather than writing.
    - A `cmd/gitid` test asserts a `VersionTooOld` outcome produces `ReasonVersionTooOld`, and that its rendered text differs from the platform-reason text.
    - A `tuikit` render test asserts the dynamic version note appears in StrictHostKeyChecking's detail pane and is absent from another option's detail pane.
    - `make gate-copy-freeze` passes, and its negative assertion fails when the dynamic version line's prefix is added to the frozen list (verify by adding it, observing the failure, and reverting).
  </acceptance_criteria>
  <reversibility rating="costly">Withholding the accept-new write on an unreadable version is a user-visible refusal, not just a data correction. It is the conservative reading of D-13 and it is reversible in one line if UAT shows it blocks real machines — record the observed frequency in the summary so that decision has evidence.</reversibility>
  <done>The dummy and the live backend agree on what gitid recommends, and the one fact that legitimately varies per machine is rendered dynamically, kept out of the freeze, and never assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render four states, four non-applicability reasons and truthful attribution inside the existing row budget (D-03, D-11, D-12, D-14)</name>
  <files>internal/tuikit/views.go, internal/tuikit/globalssh.go, internal/tuikit/globalssh_test.go, internal/dummytui/fixturebackend.go, cmd/gitid/wiring.go, Makefile</files>
  <read_first>
    - internal/tuikit/globalssh.go as landed by plan 06-01 (`optionRow`, `renderOptions`, `overlaidOptions`, `view`'s status/tone computation, `gssOptionsTopLines`, `handleClick`'s checkbox hit-test)
    - internal/tuikit/theme.go (the theme roles and the frozen glyph constants — the checkbox pair, the healthy and warning glyphs)
    - internal/tuikit/frame.go (`masterListWidth`, `masterDetailGutter`, `frameBodyRows`, `fitPane`, `joinMasterDetail`, `previewBlockClipped`)
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the Spacing/Typography/Color tables, the "Phase 6 additions" row-cost table, the Copywriting Contract DRAFT rows, and the Focal points list
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md §6 (copy freeze) and the glyph-plus-word rule
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md §4.4 (global-ssh states)
    - Makefile lines 267-322 (`gate-copy-freeze`)
  </read_first>
  <behavior>
    - A needs-action row renders the warning glyph, the warning tone and a word naming the recommendation as still open.
    - A set-but-differs row renders the SAME warning glyph and the SAME warning tone, with a different word.
    - A set-but-differs row whose value gitid parsed itself is worded as the user's own choice; one whose value came from outside gitid's files is worded as coming from outside their config, and never attributed to them.
    - An already-set row renders the healthy glyph and the healthy tone.
    - An already-set row whose source is the BASELINE class — the recommendation already holds because OpenSSH's own default is safe, not because anyone set it — is worded as safe-by-default and is never attributed to the user or to a file. It reuses the provenance slot that already carries the baseline label from plan 06-01; no new row and no new state.
    - An already-set row is not offered for selection whatever its source, so a value that is already correct is never proposed as a write.
    - A not-applicable row renders neither a selection checkbox nor a recommendation, and its current-value text is the sentence belonging to its reason: wrong platform, OpenSSH too old, compatibility unverified, or nothing to verify. No two reasons render the same sentence.
    - A not-applicable row and an already-set row are not selectable; pressing the toggle key on them does not change the selection set.
    - The IdentitiesOnly row is not selectable even when it needs action, and its detail pane explains that the fix is per-alias.
    - The detail pane shows the provenance label for the selected row, in the same block that already carries current, risk and recommended — no additional row is introduced.
    - A row whose relevant probe failed renders its probe-error note in the detail pane and is not selectable.
    - Every state remains distinguishable when colour output is disabled.
    - The status line's tally counts needs-action rows and set-but-differs rows together, and excludes not-applicable rows.
    - The rendered frame stays within the fixed capture geometry for every combination of states.
  </behavior>
  <action>
Render the four states without spending a single new row, a new glyph or a new hue — `06-UI-SPEC.md` budgets every addition against a slot that already exists.

Add `type GlobalSSHNotApplicableReason int` to `views.go` with a constant per reason mirroring the backend enum by VALUE ONLY, and add `NotApplicableReason` plus `AttributedToUser bool` to `GlobalSSHOptionView`. `AttributedToUser` is the render-layer projection of the source class: `cmd/gitid/wiring.go` sets it true only for the gitid-parsed class. tuikit must not learn the source-class enum itself — this single boolean is the whole of what the render layer needs, and it is what keeps the "your choice" wording off a value gitid cannot attribute.

Replace the binary needs-action reasoning in `internal/tuikit/globalssh.go` with `GlobalSSHOptionView.State`. `optionRow`'s signature changes to take the view instead of a boolean; keep its 2-line shape exactly (`optionRowLines` stays 2). Line 1 keeps marker, checkbox slot, tone glyph, key name and risk chip; line 2 keeps the indented current→recommended line, now fed by the real value. The tone glyph selection becomes: healthy glyph for already-set, warning glyph for BOTH needs-action and set-but-differs, and for not-applicable the existing faint styling with no tone glyph and no checkbox — a blank selection slot is already what a non-selectable row renders today.

The distinguishing WORD goes on line 2, appended to the existing current→recommended text, because that line is already faint prose and has room. Needs-action keeps the plain recommendation form. Set-but-differs takes ONE OF TWO forms depending on `AttributedToUser`: the user-choice phrasing when gitid parsed the value itself, and the outside-your-config phrasing when it did not. Already-set likewise takes ONE OF TWO forms: the normal already-set phrasing when the value was actually set somewhere, and a SAFE-BY-DEFAULT phrasing when the source is the baseline class — Task 1's corrected branch order makes a safe OpenSSH default (`ForwardAgent no`) an already-set row, and calling that "already configured" would credit the user with something they never did. Both forms reuse the same line-2 slot; there is no new row. Not-applicable replaces the whole current-value text with the sentence for its reason and omits the arrow entirely, since there is nothing to move toward. This is `02-UX-DIRECTION.md`'s glyph-plus-word rule applied to the new states: same glyph, same hue, different word — a user scanning by colour still sees that both flagged states want attention, and the word tells them which one is already decided and by whom.

Add `func (o GlobalSSHOptionView) Selectable() bool` in `views.go` returning true only for needs-action and set-but-differs rows that are writable to the wildcard block and carry no probe error. Route the toggle key, the checkbox click hit-test and the row's checkbox rendering through that ONE predicate, so there is exactly one definition of selectable and the click path can never disagree with the key path. The IdentitiesOnly exclusion arrives as `WritableToHostStar bool` on the view, populated in `cmd/gitid/wiring.go` from the policy — never a key-name comparison inside the render package, which would put policy in the wrong layer.

In the detail pane, insert the provenance label into the existing detail block, between the key name heading and the explanation, and append the probe-error note when one is present. Keep `fitPane`'s wrap-and-visible-clip behavior: long provenance text must clip with the visible cue, never silently.

Update `view`'s status line: the tally counts rows whose state is needs-action or set-but-differs, and skips not-applicable rows. Pin the rule in a comment as this plan's resolution of the `06-CONTEXT.md` discretion item, so the ceremony's own count in plan 06-04 reads from the same predicate rather than re-deriving one.

Update `internal/dummytui/fixturebackend.go`'s projection so the frozen fixture's boolean maps onto the new states and the dummy's rendering is unchanged.

Register the DRAFT strings this task makes real in `Makefile`'s `gate-copy-freeze`: the provenance label forms, the two differs words, the safe-by-default already-set phrasing, and the four not-applicable sentences.

Add a colour-disabled render test: render the Options sub-tab with each state present and assert that with styling stripped the rows are still distinguishable by their text alone. Add a geometry test asserting the rendered frame's line count and maximum line width stay within the fixed capture geometry with every state present plus the findings banner.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... ./internal/dummytui/... -run 'GlobalSSH|OptionRow|Selectable|Provenance|NoColor|Geometry|Reason' && make gate-copy-freeze</automated>
  </verify>
  <acceptance_criteria>
    - A render test drives all four states through `optionRow` and asserts each rendered row is exactly 2 lines.
    - A render test asserts the needs-action row and the set-but-differs row use the SAME tone glyph and the same theme role, and that their line-2 texts differ.
    - A render test drives two set-but-differs rows — one with `AttributedToUser` true, one false — and asserts their line-2 texts differ and that the false one's text does not contain the user-attribution wording.
    - A render test asserts the already-set row uses the healthy glyph.
    - A render test drives two already-set rows — one sourced from the gitid-parsed class, one from the BASELINE class — and asserts their provenance text differs and that the baseline one's text contains neither the user-attribution wording nor a file path. A safe OpenSSH default must not be reported as something the user configured.
    - A model test presses the toggle key on an already-set row sourced from the baseline class and asserts the selection set is unchanged, so a value that is already correct can never be proposed as a write.
    - A render test drives all four `NotApplicableReason` values and asserts four DISTINCT rendered sentences, and that the platform sentence never appears on a version-gated row.
    - A render test asserts the not-applicable row contains neither checkbox glyph.
    - A model test presses the toggle key on an already-set row, each not-applicable row, a probe-error row and the IdentitiesOnly row and asserts the selection set is unchanged in each case; the same test drives a checkbox CLICK at each of those rows' coordinates and asserts the selection set is likewise unchanged.
    - A model test presses the toggle key on a set-but-differs row and asserts the selection set gains that key (a deliberate non-recommended value is still fixable).
    - A render test asserts the detail pane for a selected row contains that row's provenance label text, and that a probe-error row's detail pane contains its error note.
    - A test renders the Options sub-tab with styling stripped and asserts the states remain distinguishable by text alone.
    - A test asserts the status-line tally equals the count of needs-action plus set-but-differs rows for a fixture containing every state.
    - A geometry test asserts the rendered frame stays within the fixed capture geometry with every state plus the findings banner present.
    - `make gate-copy-freeze` passes with the new strings registered.
    - `go test ./internal/dummytui/ -run TestNoBackendAllowlist` passes.
  </acceptance_criteria>
  <reversibility rating="reversible">Render-only changes over an existing pane; the state vocabulary is internal and has two in-repo implementers.</reversibility>
  <done>Six rows, four states, four distinct reasons, every value attributed only to whoever gitid can prove set it — inside the same frame the design was approved in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config files → classifier | Hand-written directives, including hostile values, drive the state and the rendered labels. |
| `ssh -V` / `ssh -G` output → rendered copy | Subprocess output reaches the screen as the dynamic version line and the provenance label. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Spoofing | rendered provenance label | high | mitigate | The label is driven by the source class, and `AttributedToUser` is true only for the class gitid parsed itself; a value effective from elsewhere never renders as the user's choice. Pinned by a dedicated render test. |
| T-06-09 | Tampering | probe output reaching the render layer | medium | mitigate | Values pass through `truncLine`/`fitPane` which clip to the pane; a config value containing control characters cannot break the frame. The geometry test's fixture includes such a value. |
| T-06-10 | Information Disclosure | provenance label paths | medium | mitigate | Every path in a label routes through `displayPath` in the one conversion site, so absolute sandbox paths are never surfaced raw. |
| T-06-11 | Repudiation | set-but-differs classification | medium | mitigate | The state is derived from the file on every read (D-14 stateless); there is no record gitid could get wrong, and the word attributes the value only when the source class supports it. |
| T-06-35 | Elevation of Privilege | writing `accept-new` on an unidentified OpenSSH | high | mitigate | `VersionUnverified` withholds the write and the row is non-selectable; the CLI refuses with an error naming the version-check command. Asserted by a `runGlobalSSHApply` refusal test. |
| T-06-36 | Spoofing | one non-applicability sentence covering four different situations | medium | mitigate | `NotApplicableReason` carries the situation; four distinct frozen sentences, asserted distinct by a render test. |
| T-06-51 | Spoofing | a safe machine reported as needing a fix (false alarm) | high | mitigate | `stateFor` checks value equality BEFORE any source-class branch, so a recommendation that already holds — including by OpenSSH default, e.g. `ForwardAgent no` — classifies as already-set and is not selectable. Pinned by a criterion driving equality through every `SourceClass` constant. A tool that cries wolf on a safe machine trains the user to dismiss the row that is not a false alarm, which is why this is high and not cosmetic. |
| T-06-52 | Spoofing | a safe OpenSSH default rendered as something the user configured | medium | mitigate | The already-set row takes a distinct safe-by-default wording for the baseline class, asserted by a render test to contain neither the user-attribution wording nor a file path — the same evidence discipline T-06-08 applies to the differs state. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages. Every dependency is already in `go.mod`. |
</threat_model>

<review_disposition>
## How `06-REVIEWS.md` findings are resolved in this plan

| Finding | Resolution |
|---|---|
| HIGH — "explicit value" is not "effective value"; the state model misattributes external values to the user | FIXED. `stateFor` now takes the `SourceClass`; the four VISUAL states are preserved (the UI-SPEC budgets exactly four) but ATTRIBUTION is carried separately via `AttributedToUser`, and the set-but-differs row has two wordings. Pinned by a render test asserting the non-attributed wording omits the user-attribution phrase. |
| HIGH — probe-error behavior invalidates independent per-option evidence | FIXED. Probe outcomes are tracked per evidence source with an explicit dependency map; UseKeychain and IdentitiesOnly are unaffected by a resolution-probe failure, asserted by two dedicated criteria comparing against the all-succeed run. |
| MEDIUM — zero managed identities as already-set is misleading | FIXED. `total == 0` maps to not-applicable with `ReasonNothingToVerify`; an acceptance criterion asserts it is NOT already-set. |
| MEDIUM — unknown SSH version treated as available, conflicting with D-13 | FIXED. `VersionUnverified` is a distinct outcome; the row renders and explains but is not selectable, and `runGlobalSSHApply` refuses the key. The CLI escape hatch (an error naming `ssh -V`) is specified in 06-06. Trade-off and reversal condition recorded in the reversibility note. |
| MEDIUM — two different meanings of not-applicable with no reason code | FIXED. `NotApplicableReason` with four values, four distinct frozen sentences, asserted distinct. |
| LOW — three sequential probes may produce visible latency | FIXED. Probes run concurrently under one overall bounded context; a latency test asserts total elapsed time is below the sum of individual budgets, and the measured typical/worst case goes in the summary. |
| xai-grok — copy must not say "macOS-only" for the version-gated row | FIXED by the same reason-code work; asserted by the "platform sentence never appears on a version-gated row" criterion. |

### Cycle-2 findings

| Finding | Resolution |
|---|---|
| **HIGH (cycle 2) — `stateFor` routes any baseline-sourced value to `StateNeedsAction` BEFORE checking value equality, contradicting the plan's own "already-set, whatever its source" rule and mis-flagging a safe OpenSSH default** | FIXED by reordering the algorithm, not by rewording the promise. The branches are now numbered explicitly in the action with a reason on each: (1) platform gate, (2) no value at all → needs-action, (3) **value equals the recommendation → already-set, regardless of `src`**, (4) baseline class AND unequal → needs-action, (5) set somewhere AND unequal → differs. Steps 3 and 4 are the swap: the equality test now runs before any source-class test, so `ForwardAgent no` (`internal/tuikit/design.go:214`'s fixture value, equal to the D-10 recommendation of `no`) classifies as already-set. Pinned by four criteria, including one driving equality through EVERY `SourceClass` constant in turn, so no future source class can be added on the wrong side of the branch. |
| MEDIUM (cycle 2) — "baseline" conflates a safe default with an unset recommendation | RESOLVED BY THE SAME REORDER, and it is the reason the reorder is correct rather than merely different. The baseline class now splits by value: baseline AND equal → already-set (safe default), baseline AND unequal → needs-action (recommendation still open). One class, two honest outcomes. |
| Follow-on (this planner, not raised by a reviewer) — an already-set-by-default row must not be worded as something the user configured | ADDRESSED in Task 3: already-set takes two wordings, one for a value actually set somewhere and a safe-by-default form for the baseline class, both in the existing line-2 slot. Pinned by a render test asserting the baseline form carries neither the user-attribution wording nor a file path, and by a model test asserting such a row is not selectable. Registered in `gate-copy-freeze`. |
| LOW (cycle 2) — dual `NotApplicableReason` enums (package + tuikit) can drift | ADDRESSED: the existing `NotApplicableReason` criteria are joined by the four-distinct-sentences render test; a numeric-value pin is added to the `cmd/gitid` parity test that already walks both tables, since that is the one package where both enums are visible. |
| LOW (cycle 2) — the concurrent-latency test may be flaky under `-race` | ACCEPTED AND BOUNDED, not removed. The assertion is a strict INEQUALITY against the SUM of the individual budgets, not a tight bound on the measured time, so it has roughly a full budget of slack — `-race` overhead would have to more than double the elapsed time to flip it. Recorded here so a future flake is triaged as a real regression rather than assumed to be the assertion's fault. |
</review_disposition>

<verification>
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green.
- `make lint` reports 0 issues.
- `make test` green, including `gate-copy-freeze` with the new strings and the D-13 exclusion assertion.
- `go list -deps ./internal/tuikit` names no first-party backend package.
- `go list -deps ./internal/globalssh` does not name `internal/tuikit`.
</verification>

<success_criteria>
- All six GSSH-01 options render real values, real attribution and one of four states.
- No row is ever worded as the user's choice unless gitid parsed the value itself.
- A probe failure degrades only what depended on it.
- An unreadable OpenSSH version withholds the write instead of assuming it.
- The frozen fixture and the live policy agree, pinned by a failing-on-drift test.
- No new glyph, colour, row or screen was introduced, and nothing about a declined recommendation is persisted anywhere.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 06-03)

**`internal/globalssh`:** `stateFor` keyed on source class, `NotApplicableReason` (`ReasonNone`, `ReasonPlatform`, `ReasonVersionTooOld`, `ReasonVersionUnverified`, `ReasonNothingToVerify`), per-source probe-error tracking with concurrent probe execution, `PerAliasConformance`, `(OptionPolicy).WritableToHostStar`, `VersionOutcome`, `VersionGate`.

**`internal/tuikit`:** `GlobalSSHNotApplicableReason`, `GlobalSSHOptionView.AttributedToUser`, `.NotApplicableReason`, `.WritableToHostStar`, `.VersionNote`, `(GlobalSSHOptionView).Selectable`; corrected `GlobalSSHOptions` fixture rows.

**`cmd/gitid`:** the fixture-versus-policy parity test; `GlobalSSHOptionStates` extended with attribution, per-alias conformance and the version gate.

**`Makefile`:** `gate-copy-freeze` extended with the provenance labels, both differs words, the four not-applicable sentences, and a negative assertion excluding the dynamic version line.
</artifacts>

<output>
Create `.planning/phases/06-global-ssh-options/06-03-SUMMARY.md` when done. Record explicitly, as this plan's authoritative resolutions of `06-UI-SPEC.md`'s unresolved items: the Go type chosen for the state model, the probe-failure render decision, the not-applicable render decision per reason, the MEASURED concurrent probe latency (typical and worst case) and whether it needed a loading affordance, and the pinned tally rule. Also record how often an unreadable OpenSSH version was observed, as evidence for or against the `VersionUnverified` refusal.
</output>
