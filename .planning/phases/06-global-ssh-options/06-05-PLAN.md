---
phase: 06-global-ssh-options
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - internal/filewriter/filewriter.go
  - internal/filewriter/filewriter_test.go
  - internal/sshconfig/migrate.go
  - internal/sshconfig/migrate_test.go
  - internal/tuikit/views.go
  - internal/tuikit/backend.go
  - internal/tuikit/backend_stub_test.go
  - internal/tuikit/globalssh.go
  - internal/tuikit/globalssh_test.go
  - internal/dummytui/fixturebackend.go
  - cmd/gitid/lifecycle.go
  - cmd/gitid/lifecycle_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - e2e/global_ssh_pty_e2e_test.go
  - .planning/phases/06-global-ssh-options/ui-frames/
autonomous: true
cross_ai: true
requirements: [GSSH-01]
estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Storage & preview sub-tab is REAL: its radio rows report the layout gitid actually resolved on this machine, its previews render the machine's ACTUAL resulting configuration computed by a pure planning function — not by reconstructing text the migration might not produce — and its migration ceremony performs a real backed-up, reversible migration instead of an in-memory state change."
    - "Preview and commit read the SAME plan: one exported, non-mutating `PlanMigration` produces the destination bytes, the source bytes and the diff, and the transaction writes exactly those bytes. What the user was shown is what gets written."
    - "The migration takes its backups WITHOUT rewriting the files: a dedicated backup-only seam replaces the previous mechanism of calling the writing path with the same bytes, so a backup can never overwrite an edit made after the snapshot."
    - "Concurrent external modification is detected BEFORE any content-changing operation — before the backups and again immediately before each write — not after the files have already been replaced."
    - "An abort caused by a detected external edit NEVER restores a file gitid has not written in this transaction. The externally edited file is left exactly as the other process left it; only gitid's own writes are rolled back."
    - "Migrating in one direction and then back leaves every managed block present — including the globals block plan 06-02 made movable, preserving D-06's one-wildcard-block invariant across the move — and every alias resolving to the same key it resolved to before. Reversibility is proven by resolution, not by byte comparison."
    - "A failed migration reports the concrete error and never renders a success receipt."
    - "After a migration, a subsequent global-SSH fix and a subsequent identity create both write into the NEW layout's resolved target: D-07's layout-follows-identities rule survives a layout change, and the choice is not stranded in one flow."
    - "The migrated globals block is still the LAST gitid block in its new file (D-09), so first-match-wins keeps per-alias values authoritative after the move."
    - "The demo banner is switched OFF for the Global SSH view, because both of its sub-tabs are now wired to live data. No other view's banner changes."
    - "The Storage sub-tab's visual contract is unchanged: same radio glyphs, same explainer text, same one-or-two-preview layout, same clip caps."
    - statement: "With the Include'd layout selected the right pane renders TWO stacked previews; with the sentinel layout it renders ONE."
      verification: automated
  artifacts:
    - path: "internal/filewriter/filewriter.go"
      provides: "Backup — the backup-only primitive that takes a timestamped copy without replacing the target"
    - path: "internal/sshconfig/migrate.go"
      provides: "PlanMigration (pure, non-mutating), the backup-only dependency, pre-backup and pre-write concurrent-modification detection, and abort handling that preserves external edits"
    - path: "internal/tuikit/backend.go"
      provides: "SSHStoragePlanner — the storage seam, deliberately separate from GlobalSSHPlanner"
    - path: "cmd/gitid/lifecycle.go"
      provides: "runSSHStorageMigrate — the ONE complete migration ceremony, UI-free, shared by the TUI seam and (in plan 06-06) the CLI verb"
  key_links:
    - from: "internal/tuikit/globalssh.go renderStorage + storageCeremonyFor"
      to: "Backend.SSHStorageMigrationPlan"
      via: "SSHStorageMigrationView carrying previews computed by PlanMigration, not reconstructed"
      pattern: "SSHStorageMigrationPlan"
    - from: "cmd/gitid/lifecycle.go runSSHStorageMigrate"
      to: "sshconfig.Migrate"
      via: "RealMigrateDeps over the resolved layout and the machine's managed aliases, with the engine's own rollback as the single restoration authority"
      pattern: "Migrate"
---

<objective>
Make the Global SSH view's second sub-tab real, harden the migration engine before a button can reach it, and retire the demo banner from the view as a whole.

Purpose: `cmd/gitid/wiring.go`'s `Persist` switch carries an explicit marker on the storage action naming this phase as its owner, and `06-UI-SPEC.md`'s second correction confirms the scope: view `2` has two sub-tabs and both fall through to the in-memory reducer in the real binary today. The underlying dual-storage and migration engine already exists and is test-proven — this plan wires the screen to it. It also discharges the precondition that engine's own author recorded against exactly this moment. `06-REVIEWS.md` then found that the previous draft's fix was placed too late to work and that its abort path could destroy the very edit it detected; both are corrected here, along with the engine's lack of a truthful preview API.
Output: A storage sub-tab that reports the truth about the machine, previews exactly what it will write, migrates safely and reversibly, and lets the whole view drop its demo banner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@recipes/ssh-config.recipe
@.planning/phases/06-global-ssh-options/06-CONTEXT.md
@.planning/phases/06-global-ssh-options/06-UI-SPEC.md
@.planning/phases/06-global-ssh-options/06-REVIEWS.md
@.planning/phases/06-global-ssh-options/06-02-SUMMARY.md
@.planning/phases/06-global-ssh-options/06-04-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md

<authority>
Every bare D-NN citation means `06-CONTEXT.md`. `06-UI-SPEC.md`'s "Second correction" section establishes this sub-tab as in scope for Phase 6 and states its visual contract is unchanged: this plan changes where the data comes from, never how it looks.

`06-REVIEWS.md`'s HIGH findings on this plan's predecessor are BINDING: the concurrency check must precede the backups; an abort must never restore a file gitid did not write; and the preview needs a pure planning API rather than a reconstruction.
</authority>

<scope_note>
The requirement this phase is keyed on is GSSH-01, and this plan's `requirements` field reflects the ROADMAP verbatim. For TRACEABILITY, and in answer to the review's scope-attribution finding: this plan RE-EXERCISES `STORE-01` (dual strategy), `STORE-03` (safe, backed-up, reversible migration) and `STORE-04` (safe writes), all of which are marked Complete in `REQUIREMENTS.md` from Phase 1. It does not re-open them. What is genuinely NEW here — and what belongs to GSSH-01 — is that the Global SSH view cannot drop its demo banner while half of it is a fixture, and the migration engine's own doc comment names an interactive caller as the precondition that must be discharged first. The storage hardening is therefore a prerequisite of this phase's deliverable, not scope creep, and the summary must say so.
</scope_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the migration engine a truthful preview, a backup-only seam, and concurrency detection that runs before it is too late</name>
  <files>internal/filewriter/filewriter.go, internal/filewriter/filewriter_test.go, internal/sshconfig/migrate.go, internal/sshconfig/migrate_test.go</files>
  <read_first>
    - internal/sshconfig/migrate.go lines 16-100 (`MigrateDirection`, `MigrateResult`, `MigrateDeps` and every injected seam including `ReadFile`, `WriteFile`, `RestoreFile`, `RemoveFile`, `afterStep`)
    - internal/sshconfig/migrate.go lines 194-345 (the `Migrate` doc comment — especially the PRECONDITION paragraph naming this exact wiring moment — and the five-step transaction body; note that step 2 obtains its backups by calling `WriteFile` with the snapshotted bytes, which REPLACES both targets)
    - internal/sshconfig/migrate.go lines 343-540 (`migratePaths`, `readOrEmpty`, `snapshotResolution`, `validateResolution`, `migrationClasses` as landed by plan 06-02, `composeDestination`, `composeSource`, `reorderGlobalLast`, `rollback`, `restoreSnapshot`, `backupSnapshot`)
    - internal/filewriter/filewriter.go lines 40-165 (`Write`, `WriteNoBackup`, `atomicReplace`, `backupExistingTarget`, `BackupAndRemove` — the unexported backup primitive this task exports)
    - internal/sshconfig/migrate_test.go (the existing transaction and rollback test shapes, including how `afterStep` injects failures)
    - .planning/phases/06-global-ssh-options/06-REVIEWS.md — the three HIGH findings on this engine
  </read_first>
  <behavior>
    - `PlanMigration` returns the destination bytes, the source bytes, both paths, the direction and a rendered diff, and modifies nothing on disk.
    - The bytes `Migrate` writes are byte-identical to the bytes `PlanMigration` returned for the same inputs.
    - `filewriter.Backup` copies an existing file to a timestamped sibling and leaves the original byte-identical, including its mode.
    - `filewriter.Backup` on a non-existent file returns an empty backup path and no error.
    - A migration over files that nobody else touches produces the same final content and the same backups as before this change.
    - When either file's on-disk content differs from its preflight snapshot at the moment BEFORE the backups are taken, the transaction aborts having written nothing at all.
    - When either file's on-disk content differs from its recorded digest immediately before a write step, the transaction aborts with an error naming the changed file.
    - An abort caused by a detected external edit restores only files gitid has already written in this transaction; a file gitid has not written is left exactly as it is on disk.
    - Detection is based on the file's content, not on a timestamp, so a modification within the same clock tick and a same-length different-content edit are both caught.
    - A file that did not exist at snapshot time and exists at write time is treated as a concurrent modification.
    - The check does not fire for the transaction's own writes: the source write step does not see the destination write step as external.
  </behavior>
  <action>
Three defects, one transaction. Fix them together because they interact.

**A backup-only seam (`internal/filewriter`).** Export `func Backup(path string) (backupPath string, err error)` wrapping the existing unexported backup primitive: it copies the file to its timestamped sibling and does NOT touch the original. A missing file yields an empty path and a nil error. Add its own tests asserting the original's bytes and mode are unchanged and that the backup's content matches.

**Use it in the transaction (`internal/sshconfig/migrate.go`).** Add `BackupFile func(path string) (string, error)` to `MigrateDeps`, bound to `filewriter.Backup` in `RealMigrateDeps`. Change step 2 to call it instead of calling `WriteFile` with the snapshotted bytes. The previous mechanism obtained a backup as a side effect of replacing the target with its own content, which meant the "backup" step was itself a content-changing write — so an external edit landing between preflight and step 2 was destroyed before any check could see it. That is the review's HIGH finding, and this is its root cause. Say so in the step's comment.

**A pure planning function.** Extract `func PlanMigration(direction MigrateDirection, deps MigrateDeps) (MigrationPlan, error)` where `MigrationPlan` carries `SourcePath`, `DestPath`, `SourceBefore`, `DestBefore`, `SourceAfter`, `DestAfter`, `MovedIdentities`, `MovedGlobals` and `Diff`. It performs the reads, the classification (`migrationClasses` from plan 06-02), `composeDestination`, `composeSource` and `reorderGlobalLast` — everything up to but not including any write — and returns the exact bytes the transaction would produce. It reads files and nothing else; it never writes, never backs up, and never shells out. `Migrate` then CALLS `PlanMigration` and writes precisely `plan.DestAfter` and `plan.SourceAfter`, so there is one composition path rather than two. Plan 06-05's backend uses `PlanMigration` for the screen's preview, which is what makes the preview truthful rather than a reconstruction — the review's MEDIUM finding, closed here rather than in the UI.

**Concurrency detection, early and per-file.** At preflight, alongside the content snapshot, record a content digest for each of the two files, with an explicit absent marker for a file that does not exist. Then check:
1. immediately BEFORE the backups, re-read both files and compare; a mismatch aborts having written nothing;
2. immediately before EACH content-changing write, re-read that file and compare.
After `Migrate` writes a file, update that file's recorded digest to the bytes it just wrote, so the source step's check compares against the destination step's result rather than against preflight. Getting this wrong makes every two-step migration abort, so assert it explicitly in a happy-path test rather than leaving it to chance.

Use a content digest, not a modification time. A same-tick edit is exactly the case an interactive flow makes likely — the user alt-tabs to an editor and saves while the ceremony is open — and timestamp granularity would miss it.

**Abort handling that preserves the external edit.** Track, per file, whether THIS transaction has written it. On abort, `rollback` restores only files with that flag set. A file gitid has not written is never restored, because restoring gitid's stale preflight snapshot over an edit another process just made would destroy exactly the data the detector was built to protect — the review's third HIGH finding. Make the error text say which file changed externally and that its current content was left alone, and, when gitid HAD already written the other file, that gitid's own write was rolled back. Add the invariant to `rollback`'s doc comment so a future refactor cannot quietly widen it back to "restore both".

Do not change the step ORDER or the add-to-destination-before-remove-from-source guarantee; that ordering is what makes every crash point safe, and it is not what this task is fixing.

Update the `Migrate` doc comment: replace the precondition paragraph with a statement of what is now detected, when it is checked, what abort does and does not restore, and what remains out of scope (a modification landing between the final validation and the caller observing success is still possible and is not a data-loss risk, because nothing further is written).

Test using the existing `afterStep` seam plus a `ReadFile` that returns different bytes on a chosen call: mutate a file between preflight and the pre-backup check, asserting NO backup was taken and neither file changed; mutate the source between the backup and the source write; mutate the destination between the backup and the destination write; create a file that was absent at preflight; and a happy-path case asserting the transaction's own writes never trigger the check. For the abort cases, assert specifically that the externally modified file still holds the EXTERNAL content afterwards — not the preflight snapshot — and that any file gitid did write is back at its pre-transaction bytes. Add a same-length-different-content case. Add a test asserting `Migrate`'s written bytes equal `PlanMigration`'s returned bytes for the same inputs.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/filewriter/... ./internal/sshconfig/... -run 'Migrate|PlanMigration|Backup|Concurrent'</automated>
  </verify>
  <acceptance_criteria>
    - Every pre-existing test in `internal/sshconfig/migrate_test.go` and `internal/filewriter/filewriter_test.go` still passes unmodified.
    - A test asserts `filewriter.Backup` leaves the original file's bytes and mode unchanged and produces a sibling whose content matches; a second asserts a missing file yields an empty path and a nil error.
    - A test asserts `PlanMigration` leaves both files' bytes unchanged on disk and returns non-empty `SourceAfter`/`DestAfter`.
    - A test asserts the bytes `Migrate` writes to each file are byte-identical to the corresponding `PlanMigration` field for the same inputs.
    - A test mutates a file between preflight and the PRE-BACKUP check and asserts `Migrate` returns an error, NO backup file was created, and both files' bytes are unchanged — the review's placement finding, pinned.
    - A test mutates the source file's bytes between the backup and the source write and asserts `Migrate` returns an error naming that file.
    - A test mutates the destination file's bytes between the backup and the destination write and asserts `Migrate` returns an error naming that file.
    - Each abort test asserts the externally modified file's final content equals the EXTERNAL bytes, not the preflight snapshot.
    - An abort test in which gitid had already written the other file asserts that other file is back at its pre-transaction bytes.
    - A test makes a file that was absent at preflight exist at write time and asserts the transaction aborts.
    - A happy-path test asserts a full two-step migration completes without the detection firing, proving the transaction's own writes are reconciled.
    - A test asserts detection fires for a same-content-length, different-content mutation, proving the check is content-based, not size- or time-based.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive safety machinery inside an existing transaction; the abort path reuses the engine's own rollback with a narrowed, documented scope.</reversibility>
  <done>The migration engine previews what it will actually write, backs up without writing, notices an external edit before it can destroy it, and never restores over one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the Storage & preview sub-tab to the real engine through one migration ceremony, and drop the view's demo banner</name>
  <files>internal/tuikit/views.go, internal/tuikit/backend.go, internal/tuikit/backend_stub_test.go, internal/tuikit/globalssh.go, internal/tuikit/globalssh_test.go, internal/dummytui/fixturebackend.go, cmd/gitid/lifecycle.go, cmd/gitid/lifecycle_test.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go</files>
  <read_first>
    - internal/tuikit/globalssh.go lines 184-260 and 609-649 (`sentinelPreview`, `includePreviewMain`, `includePreviewOwned`, `managedHostStar`, `storageCeremonyFor`, `renderStorage`) and the storage-ceremony branch of `handleKey` plus `handleStorageClick`
    - internal/tuikit/store.go lines 60-100, 190-200 and 440-450 (`SSHStorageLayout`, `DemoState.SSHStorage`, `SetSSHStorage`, its `Reduce` case)
    - cmd/gitid/wiring.go lines 592-640 (`InitialState`'s storage detection and `DemoBanner`), the `SetSSHStorage` demo-only arm this task replaces, and lines 2310-2415 (`storage()`, `hasIncludeLine`, `writeSSHBlock`)
    - cmd/gitid/lifecycle.go `runGlobalSSHApply` as landed by plans 06-01 and 06-04 (the per-verb ceremony shape to mirror) and `runDelete` (the canonical example)
    - cmd/gitid/wiring.go `CommitGlobalSSH` as landed by plan 06-01 (the async seam + scrubbing shape to mirror)
    - internal/sshconfig/migrate.go `RealMigrateDeps`, `PlanMigration` and `MigrateResult` (the constructor's required arguments: config path, include path, and the managed alias list)
    - internal/sshconfig/reader.go `ParseManagedHosts` (the source of the managed alias list the migration validates resolution for)
    - internal/tuikit/ceremony.go (`Async`, `commitSucceeded`, `commitFailed`)
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the Spacing table rows for the Storage sub-tab's panes and the Inherited Non-Negotiables entry on the demo banner
  </read_first>
  <precondition>An `ssh` binary must be resolvable on `PATH` for `sshconfig.Migrate`'s resolution validation to run; the tests supply the repository's fake `ssh` directory, and the real backend must surface a clear error rather than a panic when it is absent.</precondition>
  <behavior>
    - The radio rows report the layout the backend resolved for this machine, and the current-layout marker sits on that row.
    - With the sentinel layout selected the right pane renders one resulting-config preview; with the Include'd layout selected it renders two stacked previews.
    - Both previews render the bytes `PlanMigration` returned for the selected direction, not fixture text and not a reconstruction.
    - The migrate action is offered only when the selected layout differs from the resolved one.
    - Confirming the migration ceremony dispatches an asynchronous commit; the receipt is reachable only from an explicit success.
    - A failed migration renders the concrete error with retry and cancel, no receipt, and leaves both files at their pre-migration content.
    - A migration aborted by detected concurrent modification renders that specific error, distinguishable from a generic failure, and does not restore the externally edited file.
    - A successful migration's receipt carries the real backup paths for both files.
    - After a successful migration the screen re-reads and reports the new layout as current.
    - After a successful migration, a global-SSH fix writes into the new layout's resolved target.
    - The demo banner no longer renders on the Global SSH view, and still renders on the views that remain unwired.
  </behavior>
  <action>
Replace the fixture with the machine, keeping the pixels identical, and route the write through one more per-verb ceremony rather than a bespoke path.

**The seam.** Add `type SSHStoragePlanner interface { SSHStorageMigrationPlan(layout SSHStorageLayout) (SSHStorageMigrationView, error); CommitSSHStorage(layout SSHStorageLayout) tea.Cmd }` to `internal/tuikit/backend.go`, with a `NoopSSHStoragePlanner` and its sentinel error, and embed it in `Backend`. Keep it SEPARATE from `GlobalSSHPlanner` — plan 06-01's interface doc comment already states the split, and the dividing line is that one interface owns the option catalogue and its write while this one owns the layout and its migration. Two focused interfaces are what let a fixture or a test stub adopt one without hand-writing the other.

Add `type SSHStorageMigrationView struct { CurrentLayout SSHStorageLayout; TargetLayout SSHStorageLayout; Heading string; Targets []string; Backups []string; Diff string; MainPreview string; OwnedPreview string; SentinelPreview string }` to `views.go`. The three preview fields exist because the pane renders one or two blocks depending on the layout and the render package must not compose that text from backend knowledge.

**The ceremony (`cmd/gitid/lifecycle.go`).** Add `runSSHStorageMigrate(target tuikit.SSHStorageLayout, p lifecyclePolicy) (lifecycleResult, error)` following the same per-verb shape as `runGlobalSSHApply` and `runDelete`: take `txMu`, resolve the current layout, refuse when the target equals it, build `sshconfig.RealMigrateDeps` with the config path, the gitid-owned include path and the managed alias list from `sshconfig.ParseManagedHosts`, honour `p.DryRun` by stopping after `PlanMigration`, honour `p.Confirm` through `authorize`, then call `sshconfig.Migrate` and return the result's backup paths. Register its stages in `lifecycleStages`. Plan 06-06's CLI calls this same function — say so in the doc comment, so the CLI verb cannot grow a second migration path.

The engine owns rollback. Do NOT layer a `mutationJournal` over `Migrate`: one restoration authority per transaction, and the engine's is the one that knows which files it wrote. State that explicitly in the function's doc comment and keep it as an acceptance criterion.

**The backend (`cmd/gitid/wiring.go`).**
- `SSHStorageMigrationPlan(layout)` resolves the current layout with `b.storage()`, computes the direction, calls `sshconfig.PlanMigration`, and renders the diff plus the previews FROM THE PLAN's `SourceAfter`/`DestAfter` bytes. Return an error when the requested layout equals the current one, since there is nothing to plan. Scrub every path through `b.displayPath`.
- `CommitSSHStorage(layout)` is the async seam: a `tea.Cmd` closure that guards `b.initErr`, calls `runSSHStorageMigrate` with `confirmationAlreadyObtained`, scrubs paths and messages, and returns an `SSHStorageCommitMsg`. Mirror `CommitGlobalSSH` exactly.
- Replace the storage action's `Persist` arm with a real-owned arm: the write already happened in the commit, so `Persist` sets the persist error to nil and returns `b.InitialState()`, exactly like the other real-owned actions. Update the arm's comment: it currently names this phase as a future owner, and that sentence is now false.
- Change `DemoBanner` so the Global SSH view no longer raises it. Write the condition as an explicit set of still-unwired views rather than a negated single comparison, so the next phase removing its own banner edits one entry instead of restructuring the expression.

**The render.** In `renderStorage`, replace the calls to the three fixture preview helpers with the view's preview fields. Keep the pane widths, the radio glyphs, the current-layout marker, the explainer paragraph and the two clip caps EXACTLY as they are — `06-UI-SPEC.md` records all of them as unchanged, and this task is a data swap. Fetch the view in `activate` alongside the option states, and refetch after a successful migration so the current marker moves. On a fetch error render the error in the left pane and suppress the migrate action; a pane that cannot describe the machine must not offer to change it.

In `storageCeremonyFor`, replace the hand-written direction strings with the view's `Heading`, `Targets`, `Backups` and `Diff`, and set `Async`. Wire the confirmation to `CommitSSHStorage`, consume the commit message in `handleMsg`, and dispatch the storage action only on an explicit success — mirroring the apply ceremony's wiring rather than inventing a second shape. Add `type SSHStorageCommitMsg struct { Backups, Restored []string; Err string }` next to the existing commit messages.

Update `internal/dummytui/fixturebackend.go` to implement both new methods from its fixtures, returning exactly the strings the three preview helpers produce today, so the dummy's rendering is byte-identical to before. Those helpers move from being called by the render path to being called by the fixture backend; keep them, do not duplicate their text.

Prove it in `cmd/gitid/wiring_test.go` and `cmd/gitid/lifecycle_test.go` against isolated temp homes: a migration in each direction asserting the identity blocks AND the globals block moved and both backups exist; a round trip asserting every managed block is present at the end and that each alias resolves to the same key it did at the start (use the fake resolution seam, not the network); an injected failure asserting both files are byte-identical to their pre-migration content and no success is reported; a concurrent-modification abort asserting the specific error surfaces and the externally edited file keeps its external content; and a post-migration global-SSH apply asserting the write landed in the NEW layout's target file.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... ./internal/dummytui/... ./cmd/gitid/... -run 'Storage|Migrat|DemoBanner|runSSHStorageMigrate'</automated>
  </verify>
  <acceptance_criteria>
    - A `tuikit` render test with a stub returning the sentinel layout asserts the right pane contains exactly one preview block; with the Include'd layout it asserts exactly two.
    - A `tuikit` render test asserts the radio row carrying the current-layout marker is the one matching the stub's reported current layout.
    - A `tuikit` render test asserts the previews' text comes from the stub's view fields (distinctive sentinels) and not from the package's fixture preview helpers.
    - A `tuikit` model test asserts the migrate action is absent when the selected layout equals the current one and present when it differs.
    - A `tuikit` model test asserts confirming the ceremony leaves it in flight and emits no storage action until a success message arrives.
    - A `tuikit` model test delivers a failing commit message and asserts the error renders with retry and cancel and no storage action is emitted.
    - A `tuikit` model test with a stub whose plan call errors asserts the migrate action is suppressed and the error renders.
    - A `cmd/gitid` test asserts `SSHStorageMigrationPlan`'s preview strings contain content derived from `PlanMigration`'s returned bytes, and that calling it left both files unchanged on disk.
    - A `cmd/gitid` test migrates in each direction and asserts every managed identity block AND the globals block is present in the new target file, absent from the old one, and that a backup exists for both files.
    - A `cmd/gitid` round-trip test asserts, after migrating out and back, that the set of managed block names is unchanged and each alias's resolved identity file is unchanged.
    - A `cmd/gitid` test injects a migration failure and asserts both files' bytes equal their pre-migration content and the returned error is non-empty.
    - A `cmd/gitid` test triggers a concurrent-modification abort and asserts the returned error names the changed file and that the file's content on disk is still the EXTERNAL content.
    - A `cmd/gitid` test migrates, then applies a global-SSH fix, and asserts the managed block was written into the new layout's target file.
    - A `cmd/gitid` test asserts `DemoBanner` returns false for the Global SSH view and true for each view that remains unwired, enumerated explicitly.
    - A test asserts `runSSHStorageMigrate` does not open a `mutationJournal` — the engine's rollback is the single restoration authority (assert by construction: the function's body has no journal call site).
    - `go test ./internal/dummytui/ -run TestNoBackendAllowlist` passes and the dummy's Storage sub-tab render is byte-identical to its pre-change output (pin with a golden-text comparison inside the test).
  </acceptance_criteria>
  <reversibility rating="costly">The engineering decision — reuse the existing transactional migration engine and leave its own rollback as the single restoration authority — is reversible; nothing here is a one-way door for the executor, whose tests run against temp homes only. The RUNTIME write it enables is the phase's most consequential one (it rewrites where every managed block lives, across two files), which is why it is gated at runtime by the confirmation ceremony, unconditional backups of both files, the concurrency abort from Task 1, and a migrate-back path on the same screen.</reversibility>
  <done>Both sub-tabs of the Global SSH view are wired to the real machine through one migration ceremony, the preview shows exactly what the write produces, and the view's demo banner is gone because it is no longer true.</done>
</task>

<task type="auto">
  <name>Task 3: Raw-keystroke PTY coverage of the Storage & preview sub-tab (DLV-04, DLV-06)</name>
  <files>e2e/global_ssh_pty_e2e_test.go, .planning/phases/06-global-ssh-options/ui-frames/</files>
  <read_first>
    - e2e/global_ssh_pty_e2e_test.go as landed by plan 06-04 (the session helpers, the sandbox seeding, the extended fake and the frame-capture convention this file already establishes)
    - e2e/harness_test.go `SandboxHome`, `FakeSSHDir` including the mode plan 06-04 added, `writeFileT`
    - internal/tuikit/globalssh.go the Storage sub-tab's key bindings (sub-tab movement, layout movement, the migrate action) and `handleStorageClick`
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the Storage sub-tab rows of the Spacing table
  </read_first>
  <behavior>
    - Moving to the Storage sub-tab renders the radio rows, the explainer and the preview pane for the machine's resolved layout.
    - Moving the layout selection swaps the preview pane between the one-block and two-block forms.
    - The migrate action appears only once the selection differs from the current layout.
    - Cancelling the migration ceremony leaves both files unchanged on disk.
    - Confirming the migration moves the managed blocks — identities and the globals block — to the other layout, creates backups for both files, and the screen afterwards reports the new layout as current.
    - Migrating back restores the original layout with every managed block still present.
  </behavior>
  <action>
Extend `e2e/global_ssh_pty_e2e_test.go` with the Storage sub-tab's cases, using the same compiled real binary, the same sandbox seeding and the same session idiom the Options cases already use. Do not add a second helper set.

Seed each case's sandbox with at least two managed identity blocks AND a globals block so a migration has real content to move and the plan 06-02 globals-movability property is exercised end to end, plus the fake `ssh` directory so the migration engine's resolution validation resolves deterministically without the network. Cover, as separate test functions:
1. Browse — move to the Storage sub-tab, assert both radio rows and the explainer render, move the layout selection and assert the preview pane changes between the one-block and two-block forms while the sub-tab strip is unchanged.
2. Migrate and cancel — select the other layout, open the ceremony, assert the diff and both target paths render, cancel, and assert both files' bytes on disk are unchanged.
3. Migrate and confirm — select, open, confirm, assert the receipt renders with both backup paths, then assert on disk that the identity blocks AND the globals block moved and that the screen now marks the new layout as current.
4. Round trip — after case 3's migration, migrate back and assert every managed block name present at the start is present at the end.

Capture each case's final frame into the phase's `ui-frames/` directory alongside the Options frames, and enumerate the filenames in the summary.
  </action>
  <verify>
    <automated>make test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./e2e/ -run 'TestGlobalSSHStorage_RealPTY' -count=1` passes with the four named cases present.
    - The browse case asserts the preview pane's block count differs between the two layout selections within the same session.
    - The cancel case asserts both configuration files' bytes are identical before and after the cancelled ceremony.
    - The confirm case asserts each seeded managed block name — including the globals block — is present in the new layout's file and absent from the old one, and that a timestamped backup exists for both files.
    - The confirm case asserts the post-migration frame marks the newly selected layout as current.
    - The round-trip case asserts the set of managed block names after migrating back equals the set before migrating out.
    - Every captured frame is committed under the phase's `ui-frames/` directory and enumerated in the summary.
    - No case reaches the network.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only additions.</reversibility>
  <done>The Storage sub-tab's real migration is proven through real keystrokes against a real filesystem, in both directions, carrying the globals block with it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external editor → the two config files mid-transaction | A user or another process can modify either file while the migration is in flight. |
| migration transaction → both config files | The single write in this phase that touches two files at once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-18 | Tampering | concurrent external edit during migration | critical | mitigate | Content-digest detection at preflight, immediately before the backups, and before every write step; asserted by four dedicated tests including a same-length-different-content case. |
| T-06-39 | Tampering | the backup step itself overwriting an external edit | critical | mitigate | The backup is taken through the new `filewriter.Backup` seam, which never replaces the target; a dedicated test asserts no backup is created and no file changes when the pre-backup check fires. |
| T-06-40 | Tampering | rollback destroying the external edit it detected | critical | mitigate | `rollback` restores only files this transaction wrote, tracked per file; each abort test asserts the externally modified file still holds the EXTERNAL bytes afterwards. |
| T-06-19 | Denial of Service | managed block loss at a crash point | critical | mitigate | The engine's add-to-destination-before-remove-from-source ordering is preserved unchanged; the round-trip test asserts every block survives, at unit and PTY level. |
| T-06-20 | Repudiation | a failed migration reported as success | critical | mitigate | Async ceremony: the receipt is reachable only from an explicit success; the storage action is dispatched only then; an injected-failure test asserts byte-identity and no success. |
| T-06-41 | Spoofing | a preview that does not match what gets written | high | mitigate | `PlanMigration` is the single composition path; `Migrate` writes exactly its returned bytes, and a test asserts the byte-identity of the two. |
| T-06-21 | Information Disclosure | the gitid-owned include file's permissions after migration | medium | mitigate | Migration writes through the engine's restrictive file mode and creates the include directory at 0700, never relying on the umask. |
| T-06-22 | Spoofing | a stale demo banner or a stale current-layout marker | medium | mitigate | The banner condition is an explicit enumeration asserted per view; the current-layout marker is refetched after a successful migration and asserted by a PTY case. |
| T-06-23 | Denial of Service | migration resolution validation with no `ssh` on PATH | medium | mitigate | Declared as this plan's precondition; the backend surfaces a clear error rather than a panic and the tests supply the fake `ssh` directory. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages. |
</threat_model>

<review_disposition>
## How `06-REVIEWS.md` findings are resolved in this plan

| Finding | Resolution |
|---|---|
| HIGH — the concurrent-modification check runs too late; step 2 already replaces both files | FIXED. Verified against source: step 2 calls `deps.WriteFile` (bound to the writing path) with the snapshotted bytes, which backs up AND replaces. Root cause removed via `filewriter.Backup`; the check now runs at preflight, before the backups, and before each write. Pinned by a criterion asserting no backup is created and no file changes when the pre-backup check fires. |
| HIGH — rollback on a detected external edit could erase that edit | FIXED. Per-file written-by-us tracking; `rollback` restores only files gitid wrote. Every abort test asserts the externally modified file still holds the EXTERNAL bytes. The invariant is documented in `rollback`'s doc comment against future widening. |
| HIGH — the global block is excluded from migration (`migrate.go:419`) | FIXED IN PLAN 06-02, which now owns both the reserved-name registration and the migration classification. This plan's tests and PTY cases assert the globals block moves end to end. |
| MEDIUM — the "actual resulting preview" has no pure planning API | FIXED. `PlanMigration` is exported, non-mutating, and is the single composition path both the preview and the commit read; a test asserts `Migrate`'s written bytes equal its returned bytes. |
| MEDIUM — step 2 creates backups by rewriting unchanged content | FIXED by `filewriter.Backup` and the new `BackupFile` dependency. |
| LOW — scope attribution is muddled | ADDRESSED. A `<scope_note>` states that `STORE-01`/`STORE-03`/`STORE-04` are RE-EXERCISED, not re-opened, and explains why the hardening is a prerequisite of GSSH-01's deliverable rather than scope creep. The `requirements` field stays `[GSSH-01]` to match the ROADMAP and the requirement-coverage checker. |
| xai-grok MEDIUM — `CommitSSHStorage` must not also open `mutationJournal` | KEPT and PROMOTED to an acceptance criterion asserting no journal call site exists in the migration ceremony. |
| Suggestion — acceptance tests proving the global block moves in both directions | IMPLEMENTED at unit level (06-02) and end-to-end here (Task 2 criterion and Task 3 PTY case). |
</review_disposition>

<verification>
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green.
- `make lint` reports 0 issues.
- `make test` green.
- `make test-e2e` green with both the Options and the Storage PTY cases.
- `go list -deps ./internal/tuikit` names no first-party backend package.
</verification>

<success_criteria>
- Both Global SSH sub-tabs read and write the real machine.
- The preview is computed by the same function that produces the write.
- The migration backs up without writing, detects concurrent modification before it can destroy anything, and never restores over an external edit.
- A round-trip migration preserves every managed block, including the globals block, and every alias's resolution.
- The Global SSH demo banner is gone; every other view's banner is unchanged.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 06-05)

**`internal/filewriter`:** `Backup` — the backup-only primitive.

**`internal/sshconfig`:** `MigrationPlan`, `PlanMigration`; `MigrateDeps.BackupFile`; preflight/pre-backup/pre-write digest detection with self-write reconciliation; per-file written-by-us tracking narrowing `rollback`; the rewritten precondition doc comment.

**`internal/tuikit`:** `SSHStoragePlanner`, `NoopSSHStoragePlanner`, `ErrSSHStoragePlannerNotImplemented`, `SSHStorageMigrationView`, `SSHStorageCommitMsg`; `renderStorage` and `storageCeremonyFor` fed from the view.

**`cmd/gitid`:** `runSSHStorageMigrate` (lifecycle.go), `(*realBackend).SSHStorageMigrationPlan`, `(*realBackend).CommitSSHStorage`, the real storage `Persist` arm, and the enumerated `DemoBanner` condition with the Global SSH view removed.

**`e2e`:** four Storage sub-tab real-PTY cases and their captured frames.
</artifacts>

<output>
Create `.planning/phases/06-global-ssh-options/06-05-SUMMARY.md` when done. Record: the digest mechanism chosen for concurrent-modification detection; the exact per-file rollback scoping rule and the test output proving an external edit survives an abort; the exact `DemoBanner` enumeration that remains; confirmation that the dummy's Storage sub-tab render is byte-identical to its pre-change output; the enumerated `ui-frames/` filenames; and the `STORE-01`/`STORE-03`/`STORE-04` re-exercise note from the scope section.
</output>
