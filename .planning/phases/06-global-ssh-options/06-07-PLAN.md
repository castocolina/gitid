---
phase: 06-global-ssh-options
plan: 07
type: execute
wave: 7
depends_on: ["06-06"]
files_modified:
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - cmd/gitid/gate_visual_regression_test.go
  - .planning/design/global-ssh/visual-divergence-allowlist.txt
  - Makefile
  - .planning/phases/06-global-ssh-options/review-packet/
autonomous: true
cross_ai: true
requirements: [GSSH-01]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every Global SSH screen state named by the approved base-states table is registered in the visual-regression gate's inventory and compared between the real binary and the approved dummy, with every difference classified as an improvement or a defect against a checked-in allowlist."
    - "A state that cannot be captured truthfully in the in-process gate carries an explicit non-applicability record naming the PTY suite frame that carries that evidence instead — never a hollow frame and never a silent skip."
    - "The approved Bubble Tea dummy is the ONLY parity target for this surface. Historical HTML artifacts are explicitly recorded as non-applicable for every Phase 6 comparison, per the project's standing Phase 3-10 UI reference rule."
    - "The gate has working negative controls for this phase's screens: a deliberately missing state, a deliberately unclassified difference, a perturbed equal region and a cross-surface allowlist leak each fail it."
    - "The whole phase's exit battery is green and its real output is recorded: race tests, lint including the tagged builds, the copy-freeze gate, the no-backend gates, the e2e PTY and CLI suites, and the visual-regression gate."
    - "The cross-AI review packet contains the captured PTY frames, the classified divergence list and a manifest of every deviation and scoped divergence the six preceding summaries recorded, each attributed to the plan that recorded it."
    - statement: "Removing one allowlist entry makes the gate fail, verified by hand once and with the observed failure text recorded."
      verification: backstop
  artifacts:
    - path: ".planning/design/global-ssh/visual-divergence-allowlist.txt"
      provides: "the classified real-versus-dummy divergences for this surface"
    - path: "cmd/gitid/gate_visual_regression_test.go"
      provides: "the Global SSH capture merge, its deterministic fixture and its four negative controls"
    - path: ".planning/phases/06-global-ssh-options/review-packet/"
      provides: "the assembled cross-AI review evidence: frames, allowlist, manifest"
  key_links:
    - from: "cmd/gitid/gate_visual_regression_test.go"
      to: "internal/screenshot RequiredScreenSpecs"
      via: "the merged registry gaining this phase's screen specs"
      pattern: "RequiredScreenSpecs"
---

<objective>
Pin every Global SSH screen against its approved design, prove the gate can fail, and run the phase's exit battery.

Purpose: the ROADMAP's third success criterion for this phase is the UI-wave gate — PTY e2e driving every screen in the real binary plus an automated comparison against the approved dummy that classifies every difference as an improvement or a defect (DLV-04, DLV-06). Plans 06-04 and 06-05 supplied the PTY half; this plan supplies the comparison half, the classification, and the evidence a reviewer needs. It is deliberately SEPARATE from the CLI plan because `06-REVIEWS.md` found the combined closing wave too broad to attribute a failure in.
Output: A phase that is visually pinned against its approved design, green on every gate it owns, and packaged for review.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/06-global-ssh-options/06-CONTEXT.md
@.planning/phases/06-global-ssh-options/06-UI-SPEC.md
@.planning/phases/06-global-ssh-options/06-REVIEWS.md
@.planning/phases/06-global-ssh-options/06-04-SUMMARY.md
@.planning/phases/06-global-ssh-options/06-05-SUMMARY.md
@.planning/phases/06-global-ssh-options/06-06-SUMMARY.md

<authority>
Every bare D-NN citation means `06-CONTEXT.md`. `AGENTS.md`'s UI Reference rule is BINDING and settles the review's HTML question: Phase 2's approved Bubble Tea mockup is the SOLE UI/UX reference for Phases 3-10, and HTML artifacts are Phase-2 design history, not a later-phase parity target. This plan must record that explicitly for every Phase 6 spec rather than leaving it to the registry's defaults.
</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the Global SSH screens in the visual-regression gate with a classified divergence allowlist and working negative controls (DLV-04, DLV-06)</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, cmd/gitid/gate_visual_regression_test.go, .planning/design/global-ssh/visual-divergence-allowlist.txt, Makefile</files>
  <read_first>
    - internal/screenshot/createflow.go lines 520-600 (`RequiredScreenSpecs`, `ScreenSpec`, `ScreenAppliesToSurface`, `NonApplicabilityForSurface`, `ValidateCapturedState` and its region-bound marker enforcement; note the registry supports live, approved-TUI and approved-HTML surfaces)
    - internal/screenshot/createflow_regions.go (how a surface's named regions are declared and compared)
    - cmd/gitid/gate_visual_regression_test.go lines 60-160 and 240-440 (`deterministicGitIdentityFixture`, `predicateMatches`, `mergeGitScreenCaptures` and its separate-seeded-HOME rationale, the two-capture determinism assertion, the main gate test)
    - cmd/gitid/gate_visual_regression_test.go the Phase 5 identity-manager merge and its four negative controls, and the note explaining which states are DELIBERATELY excluded because they need a real probe
    - .planning/design/git-screen/visual-divergence-allowlist.txt and .planning/design/identity-manager/visual-divergence-allowlist.txt (the file format and the predicate vocabulary)
    - Makefile lines 415-470 (`gate-visual-regression`'s doc comment and its test-name filter — both need this phase's additions)
    - .planning/phases/06-global-ssh-options/06-01-SUMMARY.md through 06-06-SUMMARY.md (the real-versus-dummy divergences each recorded for this gate to classify)
    - .planning/phases/06-global-ssh-options/06-UI-SPEC.md the Approved Base States table (the states this registry must cover)
    - AGENTS.md the UI Reference section (the standing rule that the Bubble Tea dummy is the sole parity target for Phases 3-10)
  </read_first>
  <behavior>
    - The gate captures each Global SSH state from both the real backend and the approved dummy, from a deterministic seeded fixture, twice, and asserts within-surface determinism before comparing.
    - Every named region that differs between the two surfaces is either matched by an allowlist entry with a stated classification or fails the gate.
    - A state that a surface cannot truthfully render carries an explicit non-applicability record naming where its evidence lives instead.
    - Every Global SSH spec records the approved-HTML surface as non-applicable with the standing UI-reference rule as its reason.
    - Removing a required state from the capture fails the gate.
    - Introducing an unclassified difference in a compared region fails the gate.
    - Every region the gate reports as equal is mutation-sensitive: perturbing it makes the gate notice.
    - This surface's allowlist entry cannot classify another surface's region.
    - The Global SSH fixture's seeded home does not perturb any previously registered surface's capture.
    - The gate writes only to temporary directories.
  </behavior>
  <action>
Extend the existing merged registry rather than building a parallel gate — three surfaces already share this machinery and a fourth must join it the same way.

Add screen specs for the Global SSH states `06-UI-SPEC.md`'s Approved Base States table names: the Options sub-tab in browse mode, the Storage sub-tab in browse mode under each layout, the apply ceremony's preview state and its receipt state, and the storage migration ceremony's preview state. Give each a state marker that is region-bound the way `ValidateCapturedState` requires, so a duplicate of the text outside the focused pane cannot authorise a capture.

Record the approved-HTML surface as explicitly NON-APPLICABLE on every Global SSH spec, with the reason naming the standing UI-reference rule: the Bubble Tea dummy is the sole parity target for Phases 3-10 and the HTML artifacts are Phase-2 design history. The review's concern is that the registry SUPPORTS an HTML surface and a default could quietly make HTML parity a required outcome for a surface that never had an HTML mockup; making the non-applicability explicit closes that, and an acceptance criterion asserts it for every spec.

Add `deterministicGlobalSSHFixture` seeding its OWN home — two managed identities with known values and a known set of the six options present or absent, chosen so at least one row lands in each of the four states — and `mergeGlobalSSHCaptures` following `mergeGitScreenCaptures`' shape including the temporary home override. The separate-home isolation is not optional: the git-screen merge's doc comment records why a shared fixture shifts another surface's sidebar layout, and the same failure mode applies here.

Two states need an explicit decision rather than a default. The apply ceremony's RECEIPT and the storage migration's receipt both require a real write to have happened, and this gate runs in-process with no subprocess. Follow the Phase 5 precedent exactly: if a state cannot be captured truthfully here, register a non-applicability record naming the specific PTY frame from plan 06-04 or 06-05 that carries that evidence instead, rather than capturing a hollow frame. Name the frame FILE, not just "the PTY suite", so the pointer is checkable. Say which states you excluded and why in the summary.

Create `.planning/design/global-ssh/visual-divergence-allowlist.txt` in the established format, and populate it from the divergences plans 06-01 through 06-06 recorded — at minimum the block-render indentation and marker differences between the real renderer and the frozen dummy text, the resolved-target-file ceremony heading (plan 06-01's D-07 scoped divergence), and the real provenance, source-attribution and current-value strings which have no dummy equivalent by construction. Every entry states its classification as an improvement or a defect. Entries must use the specific-needle predicate forms the existing allowlists use; a broad predicate that would match unrelated text is a defect in the gate, not a shortcut.

Add the four negative controls this phase's registry needs, mirroring the Phase 4 and Phase 5 sets by name and by shape: a missing-state control, an unclassified-difference control, an all-equal-regions-are-mutation-sensitive control, and a cross-registry-leakage control asserting this surface's allowlist cannot classify another surface's region.

Update `Makefile`'s `gate-visual-regression` doc comment to describe the now four-way merged registry, and confirm its test-name filter still selects the new controls — the filter matches a prefix, so a control named outside that prefix would silently never run. Assert that explicitly rather than assuming it.
  </action>
  <verify>
    <automated>make gate-visual-regression</automated>
  </verify>
  <acceptance_criteria>
    - `make gate-visual-regression` passes and its output reports a frame count that increased by exactly the number of Global SSH specs registered.
    - A test asserts every registered Global SSH spec was captured non-empty for every surface it declares itself applicable to.
    - A test asserts every Global SSH spec records the approved-HTML surface as non-applicable with a non-empty reason.
    - A test asserts every Global SSH spec that declares any other surface non-applicable carries a reason naming a specific PTY frame file where that evidence lives instead, and that the named file exists under the phase's `ui-frames/` directory.
    - A test asserts the deterministic fixture produces at least one row in each of the four option states, so the comparison is not exercising a single state.
    - The missing-state negative control fails when a Global SSH spec's capture is removed.
    - The unclassified-difference negative control fails when a difference is introduced in a compared Global SSH region with no allowlist entry.
    - The mutation-sensitivity negative control fails when an equal Global SSH region is perturbed.
    - The cross-registry-leakage negative control fails when this surface's allowlist entry is applied to another surface's region.
    - A test asserts the Makefile's gate test-name filter selects all four new controls (assert by name-prefix comparison in the test, so a rename cannot silently orphan a control).
    - The fail path is hand-verified once: remove one allowlist entry, observe a red failure, restore it, and record the observed failure text in the summary.
    - A test asserts the Global SSH fixture's captures are byte-identical across two independent runs in the same test process.
    - A test asserts previously registered surfaces' captures are unchanged by the addition of this fixture.
    - `.planning/design/global-ssh/visual-divergence-allowlist.txt` exists, every entry states a classification, and no entry uses a predicate broader than a specific needle.
  </acceptance_criteria>
  <reversibility rating="reversible">Gate and fixture additions; the allowlist is a checked-in document that can be tightened at any time.</reversibility>
  <done>Every Global SSH screen is compared against its approved design, every difference is a recorded decision, and the gate is proven able to fail.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase exit battery and assemble the cross-AI review packet</name>
  <files>Makefile, .planning/phases/06-global-ssh-options/review-packet/</files>
  <read_first>
    - Makefile the `test`, `lint`, `lint-tagged`, `test-e2e`, `gate-copy-freeze`, `gate-no-backend-files`, `gate-visual-regression` and the review-packet generation targets
    - .planning/phases/03-create-flow-backend/03-06-SUMMARY.md the "Cross-AI visual-regression review" section (the packet's expected shape and the orchestrator/executor split)
    - .planning/phases/06-global-ssh-options/06-01-SUMMARY.md through 06-06-SUMMARY.md (every recorded divergence, deviation and carried-forward item that must appear in the packet)
    - .planning/STATE.md the Blockers/Concerns section (the standing rule that an executor cannot run subagent reviews, and that gate results must be independently re-verified at wave close)
    - project memory: the review-gate note that an executor reporting a pass without the race flag is not evidence
  </read_first>
  <behavior>
    - Every gate in the battery runs and its real output is recorded, including the exact command and its result.
    - A failing gate is fixed or explicitly recorded as a blocker; it is never reported as passing.
    - The review packet contains the captured PTY frames, the classified divergence list and a manifest of every deviation the phase's plans recorded, each attributed to its plan.
    - The manifest names the review findings this phase's plans resolved, so a reviewer can check the closure rather than re-derive it.
  </behavior>
  <action>
Run the battery yourself and record real output — a claimed pass is not a pass in this project, and the standing rule is that the race flag is part of the command, not an optional extra.

Run, in this order, recording the exact command and the exact result for each: the module build; the race test suite with the deterministic environment the project uses for reproducing continuous-integration behavior locally; the lint target including its tagged variants; the full test target with the copy-freeze gate; the end-to-end suite covering both the PTY and the headless CLI cases; the no-backend-files gate; the backend-import allowlist test; and the visual-regression gate. If any fails, fix it in this plan or record it as an explicit blocker in the summary with the failing output quoted — do not soften it.

Assemble the cross-AI review packet under the phase directory following the Phase 3 convention: the captured frames from plans 06-04 and 06-05, the classified divergence allowlist from Task 1, and a manifest listing every deviation, scoped divergence and carried-forward item the six preceding summaries recorded, each with the plan that recorded it.

Add one section to the manifest that the previous phases did not need: a closure table for `06-REVIEWS.md`. For each of the eight HIGH findings and six actionable non-HIGH findings that cross-AI review raised, name the plan and the artifact that closed it and the acceptance criterion that pins it. A reviewer re-reading this phase should be able to check the closure in one pass rather than re-deriving it from six summaries. The per-plan `<review_disposition>` sections are the source; this is their index.

Note in the summary that RUNNING the reviews is an orchestrator obligation, not an executor one — the executor assembles the packet and cannot spawn the reviewers.
  </action>
  <verify>
    <automated>make build && TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./... && make lint && make test && make test-e2e && make gate-no-backend-files && make gate-visual-regression</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the verify line above exits zero, and the summary quotes each command with its result.
    - `go test ./internal/dummytui/ -run TestNoBackendAllowlist` passes and `go list -deps ./internal/tuikit` output is recorded showing no first-party backend package.
    - The review packet directory exists under the phase directory containing the captured frames, the allowlist and a manifest.
    - The manifest lists every deviation and scoped divergence recorded across the six preceding summaries, each with the plan that recorded it.
    - The manifest contains a closure table covering all eight HIGH and six actionable non-HIGH findings from `06-REVIEWS.md`, each naming the plan, the artifact and the acceptance criterion that closed it.
    - All four parity-matrix tests pass and `docs/cli-parity-matrix.md` contains no deferred row for this phase's noun.
    - The summary states explicitly that the cross-AI reviews have NOT been run and are owed by the orchestrator.
  </acceptance_criteria>
  <reversibility rating="reversible">Verification and documentation only.</reversibility>
  <done>The phase is green on every gate it owns, and the evidence a reviewer needs — including proof that the last review's findings were closed — is assembled and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the visual gate's fixture homes → previously registered surfaces | A shared or leaking fixture can silently change another surface's capture and mask a regression. |
| a green gate → the reviewer's confidence | A gate that cannot fail is worse than no gate, because it is believed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-27 | Spoofing | a green gate that proves nothing | high | mitigate | Four negative controls plus a hand-verified fail path; the mutation-sensitivity control specifically prevents a vacuously-equal comparison, and a test asserts the Makefile filter actually selects all four controls. |
| T-06-44 | Spoofing | a hollow capture standing in for a state that needs a real write | high | mitigate | Uncapturable states carry a non-applicability record naming a SPECIFIC PTY frame file, and a test asserts that file exists. |
| T-06-45 | Spoofing | HTML parity silently becoming a required outcome | medium | mitigate | Every Global SSH spec records the approved-HTML surface as explicitly non-applicable with the standing UI-reference rule as its reason; asserted for every spec. |
| T-06-46 | Tampering | a new fixture perturbing an existing surface's capture | high | mitigate | The fixture seeds its own home following the documented separate-home rationale; a test asserts previously registered surfaces' captures are unchanged. |
| T-06-47 | Repudiation | a gate reported as passing without being run under the race flag | high | mitigate | The battery's exact commands are quoted with their real output in the summary; the standing project rule that an unraced pass is not evidence is named in the action. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages. |
</threat_model>

<review_disposition>
## How `06-REVIEWS.md` findings are resolved in this plan

| Finding | Resolution |
|---|---|
| MEDIUM — visual-gate scope risks mixing historical HTML back into the target | FIXED. Every Global SSH spec records the approved-HTML surface as explicitly non-applicable, citing `AGENTS.md`'s standing Phase 3-10 UI-reference rule; asserted per spec. |
| MEDIUM — the final wave is too broad; failures hard to attribute | FIXED by SPLITTING the predecessor plan into 06-06 (CLI + matrix) and this plan (gate + battery + packet), each independently verified. |
| xai-grok MEDIUM — receipt states may be non-applicable in the in-process gate; the ROADMAP criterion still needs the PTY frames named as the evidence surface | FIXED and STRENGTHENED: a non-applicability record must name a SPECIFIC PTY frame FILE, and a test asserts that file exists under the phase's `ui-frames/` directory. |
| LOW — legacy `grep` in acceptance criteria | FIXED across the revised plan set; no criterion in this plan shells a grep at all. |
| Cross-plan — review findings must be checkably closed | ADDRESSED beyond the original ask: the manifest carries a closure table for all fourteen actionable findings, indexed from the per-plan `<review_disposition>` sections. |
</review_disposition>

<verification>
- `make build` exits zero.
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green.
- `make lint` reports 0 issues, including the tagged variants.
- `make test` green including `gate-copy-freeze`.
- `make test-e2e` green with the PTY and headless CLI cases.
- `make gate-no-backend-files` green; `TestNoBackendAllowlist` passes.
- `make gate-visual-regression` green with the Global SSH specs and negative controls included.
- All four parity-matrix tests pass.
</verification>

<success_criteria>
- Every Global SSH screen state is registered in the visual gate, with every difference classified.
- Uncapturable states point at a named, existing PTY frame rather than a hollow capture.
- HTML is explicitly not a parity target for this surface.
- The negative controls fail when they should, verified by hand once and recorded.
- The exit battery is green with real output quoted, and the review packet — including the review-closure table — is committed.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 06-07)

**Visual gate:** Global SSH `ScreenSpec` entries in `internal/screenshot` with explicit HTML non-applicability, `deterministicGlobalSSHFixture`, `mergeGlobalSSHCaptures`, and four Global SSH negative controls in `cmd/gitid/gate_visual_regression_test.go`; the `gate-visual-regression` doc comment and filter assertion in `Makefile`.

**Documents:** `.planning/design/global-ssh/visual-divergence-allowlist.txt`; the cross-AI review packet under `.planning/phases/06-global-ssh-options/review-packet/` containing the frames, the allowlist and a manifest with the `06-REVIEWS.md` closure table.
</artifacts>

<output>
Create `.planning/phases/06-global-ssh-options/06-07-SUMMARY.md` when done. Record: which visual-gate states were registered non-applicable and the specific PTY frame file each points at; the hand-verified gate fail-path output; every exit-battery command with its real result; the review-closure table's location; and an explicit statement that the cross-AI reviews remain owed by the orchestrator.
</output>
