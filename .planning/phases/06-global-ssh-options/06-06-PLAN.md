---
phase: 06-global-ssh-options
plan: 06
type: execute
wave: 6
depends_on: ["06-05"]
files_modified:
  - cmd/gitid/main.go
  - cmd/gitid/ssh.go
  - cmd/gitid/ssh_test.go
  - cmd/gitid/identity_test.go
  - docs/cli-parity-matrix.md
  - docs/gitid-ssh-json-schema.md
  - e2e/global_ssh_cli_e2e_test.go
autonomous: true
cross_ai: true
requirements: [GSSH-01]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The command tree is FROZEN and stated in this plan before implementation: `gitid ssh options list [--json]`, `gitid ssh options apply <key>... [--dry-run] [--yes] [--fail-on-advisory]`, `gitid ssh storage show [--json]`, `gitid ssh storage migrate --to <include|in-file> [--dry-run] [--yes]`. No other verb is added, and the machine-checked parity matrix names every one of them."
    - "The JSON envelope is FROZEN and versioned: a schema identifier, a stable field set, stable enum strings and a documented ordering, recorded in a checked-in schema document and asserted by a test — the same rigour the identity read commands' frozen document already carries."
    - "The exit-status contract is FROZEN and consistent with the phase's advisory posture: a successful backed-up write exits zero even when the post-write verification reports shadowing, because D-04 and D-14 make shadowing advisory and never blocking. The advisory is carried in the output and in the JSON envelope, and a script that wants the stricter contract opts in explicitly."
    - "The CLI and the TUI produce the same outcome for the same input because both call the SAME UI-free per-verb ceremony — `runGlobalSSHApply` and `runSSHStorageMigrate` — not a `tea.Cmd` and not a reimplementation. There is no second write path."
    - "The write command takes a timestamped backup unconditionally: no flag can skip it, and the dry-run flag stops after the plan, simulation and shadow-report stages without writing."
    - "The write command refuses, by name, an option that is not writable to the wildcard block and an option whose OpenSSH version gate could not be verified, telling the user which command to run to check."
    - "Every runnable command in the tree is named by a parity-matrix row, and this phase's requirement id appears in the checker's required list."
    - statement: "Running the write command twice with the same arguments leaves the configuration byte-identical the second time and still records a second backup."
      verification: automated
  artifacts:
    - path: "cmd/gitid/ssh.go"
      provides: "the real gitid ssh command group replacing the reserved placeholder, wired to the shared per-verb ceremonies"
    - path: "docs/gitid-ssh-json-schema.md"
      provides: "the frozen, versioned machine-readable output contract for both read verbs"
    - path: "docs/cli-parity-matrix.md"
      provides: "the requirement-keyed rows for this phase's outcomes, machine-checked against the command tree, plus the dry-run contract rows"
  key_links:
    - from: "cmd/gitid/ssh.go apply verb"
      to: "cmd/gitid/lifecycle.go runGlobalSSHApply"
      via: "the same UI-free ceremony the TUI's CommitGlobalSSH calls — one chokepoint, no CLI-only write path"
      pattern: "runGlobalSSHApply"
    - from: "cmd/gitid/ssh.go migrate verb"
      to: "cmd/gitid/lifecycle.go runSSHStorageMigrate"
      via: "the same UI-free ceremony the TUI's CommitSSHStorage calls"
      pattern: "runSSHStorageMigrate"
---

<objective>
Put every Global SSH outcome on the command line, through the same ceremonies the screen uses, with a contract that is frozen rather than discovered.

Purpose: `docs/cli-parity-matrix.md` carries a deferred row stating in so many words that this phase replaces the reserved `ssh` noun with real verbs and adds its rows — and a machine-checked test enforces the matrix against the actual command tree, so leaving it undone is a build failure, not an oversight. `06-REVIEWS.md` found the previous draft's CLI under-specified in three externally visible ways: the verb names were described rather than named, the JSON output was listed rather than frozen, and the exit-status behavior for an advisory was contradictory with the phase's own advisory posture. Scripts consume all three. This plan pins them.
Output: Four commands, one frozen JSON document, one frozen exit-status table, and no second write path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/06-global-ssh-options/06-CONTEXT.md
@.planning/phases/06-global-ssh-options/06-REVIEWS.md
@.planning/phases/06-global-ssh-options/06-03-SUMMARY.md
@.planning/phases/06-global-ssh-options/06-04-SUMMARY.md
@.planning/phases/06-global-ssh-options/06-05-SUMMARY.md
@.planning/phases/05-identity-manager/05-CONTEXT.md

<authority>
Every bare D-NN citation means `06-CONTEXT.md`, EXCEPT where the CLI conventions are cited from `05-CONTEXT.md` decisions D-01 through D-04 (the noun-verb taxonomy, the adaptive non-interactive depth, the machine-readable read output, and the outcome-parity matrix obligation). `06-CONTEXT.md` lists the `gitid ssh` command shape under Claude's Discretion; the shape frozen below is this plan's exercise of that discretion, and it must follow the Phase 5 conventions rather than invent new ones.

The `<frozen_contract>` section below is BINDING on implementation. `06-REVIEWS.md` flagged each of its three parts as insufficiently pinned; they are pinned here, before implementation, and any deviation must be raised rather than improvised.
</authority>

<frozen_contract>
## Command tree (frozen)

```
gitid ssh options list    [--json]
gitid ssh options apply   <key>... [--dry-run] [--yes] [--fail-on-advisory]
gitid ssh storage show    [--json]
gitid ssh storage migrate --to <include|in-file> [--dry-run] [--yes]
```

No flat root-level aliases for this noun: `05-CONTEXT.md` D-01 reserves flat aliases for the identity verbs, and adding `gitid list` collisions here would defeat the reservation the `ssh` placeholder exists to protect.

## JSON envelope (frozen, versioned)

`gitid ssh options list --json`:

```
{
  "schema": "gitid.ssh.options/v1",
  "options": [
    {
      "key": "<policy key, in Policy declaration order>",
      "current_value": "<string, empty when unset>",
      "recommended_value": "<string>",
      "risk": "low|medium|high",
      "scope": "global|per-alias",
      "state": "needs-action|already-set|differs|not-applicable",
      "source": "gitid-parsed|outside-gitid|system-file|baseline|inconclusive",
      "source_file": "<display path, empty when unknown>",
      "source_line": <integer, 0 when unknown>,
      "not_applicable_reason": "none|platform|version-too-old|version-unverified|nothing-to-verify",
      "version_note": "<string, empty when none>",
      "probe_error": "<string, empty when none>"
    }
  ]
}
```

`gitid ssh storage show --json`:

```
{
  "schema": "gitid.ssh.storage/v1",
  "layout": "include|in-file",
  "target_path": "<display path>",
  "main_config_path": "<display path>",
  "include_line_present": true
}
```

Rules: the `options` array is ordered by `globalssh.Policy` declaration order, never sorted or filtered; every enum string above is part of the contract and is lower-case kebab; every path is a display path, never a raw absolute sandbox path; a field is emitted even when empty, so a consumer can rely on its presence; and the `schema` value changes only on a breaking change, at which point the old value must be retired in the same commit as the document.

## Exit-status contract (frozen)

| Code | Meaning |
|---|---|
| 0 | The command succeeded. For a write, this includes a successful backed-up write whose post-write verification reported an advisory (for example, the fix landed but is shadowed). |
| 1 | Usage, validation or refusal: an unknown or non-writable option key, a version gate that could not be verified, a missing confirmation off a terminal, an unknown layout. Nothing was written. |
| 2 | The write or the migration failed and was rolled back. |
| 3 | Reserved for `--fail-on-advisory` only: the write succeeded AND an advisory was reported. Never returned without that flag. |

The zero-on-advisory default is the direct consequence of D-04 and D-14: shadowing is ADVISORY and never blocking, so a script that pipes gitid into a provisioning run must not fail because gitid was honest about a pre-existing directive. `--fail-on-advisory` gives a stricter script the opt-in it needs without making honesty a failure by default. Record this reasoning in the parity matrix's notes column so a future reader does not "fix" it toward a nonzero default.
</frozen_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the reserved `ssh` noun with the frozen verb tree, wired to the shared ceremonies</name>
  <files>cmd/gitid/main.go, cmd/gitid/ssh.go, cmd/gitid/ssh_test.go, docs/gitid-ssh-json-schema.md, e2e/global_ssh_cli_e2e_test.go</files>
  <read_first>
    - cmd/gitid/main.go lines 82-110 (`newRootCmd`, the flat aliases, and `newReservedNounCmd`'s registration of the `ssh` placeholder this task replaces — leave the other three reserved nouns untouched)
    - cmd/gitid/identity.go (the noun-group construction, the per-verb command builders, and how a verb wires flags to a writer function)
    - cmd/gitid/identity_delete.go and cmd/gitid/identity_key.go (how a CLI verb calls `runDelete`/`runRotate`/`runRepair` with a `lifecyclePolicy` — the pattern this task follows exactly)
    - cmd/gitid/lifecycle.go lines 30-150 (`confirmationMode`, `lifecyclePolicy`, `lifecycleResult`, `authorize`) and `runGlobalSSHApply` / `runSSHStorageMigrate` as landed by plans 06-01, 06-04 and 06-05
    - cmd/gitid/wiring.go `GlobalSSHOptionStates` and `SSHStorageMigrationPlan` as landed by plans 06-03 and 06-05 (the read seams)
    - the frozen identity read-command JSON document referenced by `docs/cli-parity-matrix.md` (the format and rigour this schema document mirrors)
    - e2e/identity_cli_e2e_test.go (the headless CLI e2e idiom and how machine-readable output is asserted without a PTY)
    - .planning/phases/05-identity-manager/05-CONTEXT.md decisions D-01 through D-04
    - .planning/phases/06-global-ssh-options/06-REVIEWS.md — the findings on ambiguous syntax, unfrozen schema, advisory exit semantics, and "same function" versus "same transactional core"
  </read_first>
  <behavior>
    - The reserved placeholder error is gone for this noun: invoking the noun group with no verb prints help listing the real sub-nouns and verbs; the other reserved nouns still return their phase errors.
    - The options read verb prints an aligned table on a terminal, a tab-delimited form when piped, and the frozen envelope under its flag.
    - The options read verb's machine-readable form emits every field in the frozen envelope for every one of the six options, in policy declaration order, with the frozen enum strings.
    - The storage read verb emits the frozen storage envelope.
    - The apply verb applies only the options named on the command line, and only toward their recommended values.
    - The apply verb refuses, by name and with exit code 1, an option that is not writable to the wildcard block, an option whose version gate is unverified or too old, and an unknown key.
    - The apply verb with the dry-run flag prints the preview and any shadowing warning or inconclusive note and exits zero without writing.
    - The apply verb without the confirmation flag on a non-terminal errors with exit code 1 rather than writing unattended; with a complete argument set and the confirmation flag it runs headless; with an incomplete set on a terminal it opens the TUI on this view.
    - The apply verb takes a timestamped backup on every real write; no flag suppresses it.
    - The apply verb exits zero on a successful write that reported an advisory, and exits 3 for the same outcome when the opt-in flag is present.
    - The apply verb exits 2 when the write failed and was rolled back.
    - Running the apply verb twice with the same arguments leaves the configuration byte-identical after the second run and records a second backup.
    - The storage migrate verb reports the current layout, refuses a target equal to it, and otherwise migrates through the same engine the screen uses.
    - Both write verbs reach disk only through the shared per-verb ceremonies; neither constructs a candidate or calls a writer itself.
  </behavior>
  <action>
Give the noun group real verbs, wired to the ceremonies the screen already calls, and freeze the contract before writing the flags.

Create `cmd/gitid/ssh.go` and remove the reserved-placeholder registration for THIS noun from `newRootCmd`, leaving the other three untouched. Build the group with the same helpers `identity.go` uses so the flag parsing, the terminal detection and the error shaping are shared, not re-implemented. Build exactly the tree in this plan's `<frozen_contract>` — four commands, no more, with those exact paths and flag names.

Each write verb constructs a `lifecyclePolicy` and calls the SAME function the TUI's async seam calls: the apply verb calls `runGlobalSSHApply`, the migrate verb calls `runSSHStorageMigrate`. This is the review's "same transactional core" point and it is already how `identity_delete.go` and `identity_key.go` work: the TUI wraps the result in a `tea.Cmd`, Cobra calls the core synchronously. Do NOT have Cobra invoke a `tea.Cmd`, and do NOT add a CLI-only write path — `05-CONTEXT.md` D-02 is explicit that both surfaces route through one chokepoint, and a second path is how the two drift.

The dry-run flag maps to `lifecyclePolicy.DryRun`, the confirmation flag maps to `confirmationNotRequired`, and its absence on a terminal maps to `confirmationRequired` with a prompt. No flag maps to anything that could suppress a backup; the backup is taken inside the ceremony where no CLI argument reaches it.

Implement the exit-status contract from `<frozen_contract>` in ONE place — a small helper that maps a `lifecycleResult` plus its error plus the opt-in flag to a code — so the two write verbs cannot diverge. The advisory list comes from the ceremony's post-write verify stage (plan 06-04's `lifecycleResult.Advisories`). Put the D-04/D-14 reasoning for the zero-on-advisory default in that helper's doc comment, not only in the matrix, because that is where someone will be tempted to change it.

Write `docs/gitid-ssh-json-schema.md` from `<frozen_contract>`'s envelope section verbatim, in the shape and rigour the identity read commands' frozen document uses: the schema identifier, every field with its type and its meaning, every enum's complete value list, the ordering rule, the empty-field rule, and the versioning rule. Add a test that marshals a fixture through the real encoder and asserts the emitted key set for each object EXACTLY equals the documented key set — not a subset — so an added field is a deliberate, documented change rather than a silent one. Assert the enum strings against the document's list too, so renaming a Go constant cannot silently rename a wire value.

The machine-readable read output is also the assertion surface for the headless e2e, which is why it must carry source and state and not only the current value — a documented CI pain point is exactly the terminal-scraping this replaces.

Add `cmd/gitid/ssh_test.go` covering: flag parsing for all four commands; the refusal of the per-alias option with its exit code; the refusal of a version-unverified option with an error naming the version-check command; the refusal of an unknown key; the non-terminal-without-confirmation refusal; the dry-run no-write property; the exact JSON key sets and enum values; the exit code for each row of the frozen table including the opt-in flag's effect; and an assertion BY CONSTRUCTION that each write verb calls the shared ceremony function rather than duplicating behavioral assertions.

Add `e2e/global_ssh_cli_e2e_test.go` following `identity_cli_e2e_test.go`'s idiom: run the compiled binary against a sandbox home with the fake `ssh` directory on `PATH`, assert the machine-readable read output's fields against the schema document, apply an option, assert the managed block and the timestamped backup on disk, run the identical command again and assert the configuration bytes are unchanged and a second backup exists, and assert the exit code in the advisory and non-advisory cases.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./cmd/gitid/... -run 'SSHCmd|SSHJSON|SSHExit|Completion' && go test ./e2e/ -run 'TestGlobalSSHCLI' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the built command tree contains exactly the four command paths named in `<frozen_contract>` under this noun, and no others.
    - A test asserts invoking the noun group with no verb does not return the reserved-placeholder error and does list the real sub-nouns; a second test asserts the other three reserved nouns still return their phase errors.
    - A test marshals the options read output and asserts the top-level key set equals `{schema, options}` exactly, that `schema` equals the frozen identifier, and that each option object's key set equals the documented set exactly.
    - A test asserts the options array's order equals `globalssh.Policy`'s declaration order.
    - A test asserts every emitted `state`, `source`, `risk`, `scope` and `not_applicable_reason` value is a member of the documented enum list for that field.
    - A test marshals the storage read output and asserts its key set and `schema` value against the document.
    - A test asserts the apply verb given the per-alias option returns exit code 1 with an error naming that option and performs no write.
    - A test asserts the apply verb given a version-unverified option returns exit code 1 with an error naming the version-check command and performs no write.
    - A test asserts the apply verb with the dry-run flag leaves the configuration file's bytes unchanged, prints the preview, and exits zero.
    - A test asserts the apply verb off a terminal without the confirmation flag returns exit code 1 and performs no write.
    - A test asserts a successful write reporting an advisory exits 0 by default and 3 with the opt-in flag, and that a rolled-back failure exits 2.
    - A test asserts each write verb's implementation calls the shared ceremony function (assert by construction — the command holds the backend and calls the named method).
    - An e2e case applies an option, asserts the managed block contains it and a timestamped backup exists, then runs the identical command again and asserts the configuration bytes are unchanged and a second backup exists.
    - Shell completion still generates for the documented shells with the new verbs present.
  </acceptance_criteria>
  <reversibility rating="costly">The command paths, the JSON schema identifier and the exit codes are a public contract the moment they ship; changing any of them later is a breaking change for scripts. That is exactly why they are frozen in this plan before implementation rather than settled during it.</reversibility>
  <done>Four commands, one frozen wire format, one frozen exit-status table, and both write verbs reaching disk only through the ceremonies the screen uses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the machine-checked parity matrix and its dry-run contract table</name>
  <files>docs/cli-parity-matrix.md, cmd/gitid/identity_test.go</files>
  <read_first>
    - docs/cli-parity-matrix.md (both tables; the deferred row naming this phase, the tooling-exclusion note, and the dry-run contract table's current verb set)
    - cmd/gitid/identity_test.go lines 1700-1960 (`parseParityMatrix`, `matrixRow`, `checkParityMatrix`, `reservedNoun`, and all four parity tests including the dry-run contract table's exact-set assertion)
    - cmd/gitid/ssh.go as landed by Task 1 (the exact command paths and flags the rows must name)
    - .planning/phases/06-global-ssh-options/06-REVIEWS.md — the scope-attribution finding on requirement traceability
  </read_first>
  <behavior>
    - Every runnable command in the built tree is named by at least one matrix row.
    - The deferred row for this noun no longer exists, and its shipped rows are present.
    - Each shipped row names the exact command path and its selecting flags.
    - The requirement-coverage checker's required list contains this phase's requirement id and passes.
    - The dry-run contract table names both new write verbs, and the checker's expected verb set matches the table exactly.
  </behavior>
  <action>
Update the matrix and expect the checker to push back — that is what it is for.

Delete the deferred row for this noun and add one shipped row per outcome: listing the options, applying options, showing the storage layout, and migrating it. Key each row on `GSSH-01` and `SHELL-03`, and name the exact command path and flags from `<frozen_contract>`. In the notes column of the apply row, record the exit-status contract in one sentence including the zero-on-advisory default and its D-04/D-14 reasoning, and note that `--fail-on-advisory` is the opt-in for a stricter script. In the notes column of the storage rows, record that these outcomes RE-EXERCISE `STORE-01`/`STORE-03` rather than re-opening them, so the traceability question the review raised is answered where a reader will look for it.

`checkParityMatrix` walks the whole tree and reports any runnable command no row names, so every new command needs its own row. `TestParityMatrixDryRunContractTable` asserts the dry-run table's verb set EXACTLY equals its current contents, so adding two write verbs that honour the dry-run flag requires adding both rows to that table AND both verbs to the test's expected set, in the same commit. Read that test before editing the matrix so the change lands as one coherent edit rather than a red-then-patch cycle.

For each new write verb, the dry-run table row must say what its dry run DOES and what it does NOT do, in the table's established two-column form: the apply verb's dry run runs the plan, the simulation and the shadow report and prints them, and does not write or back up; the migrate verb's dry run runs the migration plan and prints the resulting configuration for both files, and does not back up, write or trim.

Extend `TestParityMatrixRequirementCoverage`'s required list with this phase's requirement id.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./cmd/gitid/... -run 'Parity'</automated>
  </verify>
  <acceptance_criteria>
    - `TestParityMatrixResolvesAndCoversTree` passes with the deferred row removed and every new command named by a shipped row.
    - `TestParityMatrixRequirementCoverage` passes with this phase's requirement id in its required list.
    - `TestParityMatrixDryRunContractTable` passes with both new write verbs present in the document table and in the test's expected set.
    - A check confirms `docs/cli-parity-matrix.md` contains no deferred row for this phase's noun and still contains the deferred rows for the three nouns later phases own.
    - The apply row's notes state the exit-status contract including the zero-on-advisory default and the opt-in flag.
    - The storage rows' notes state the re-exercise relationship to the earlier storage requirements.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation plus a checker list; both are machine-verified, so a mistake fails a test rather than shipping.</reversibility>
  <done>The matrix is a complete map of the product surface again, and the checker proves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command-line arguments → the write path | User-supplied option names and layout names reach the ceremony. |
| headless invocation → the user's config with no interactive confirmation | The confirmation flag replaces a human. |
| the JSON envelope → a downstream script | A consumer makes decisions from these strings; a silent field or enum change breaks it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-24 | Elevation of Privilege | an unrecognised, per-alias, or version-unverified option name on the command line | high | mitigate | Names resolve against the policy table inside the shared ceremony; anything not writable to the wildcard block, unknown, or version-unverified is refused by name with exit code 1. Three dedicated tests. |
| T-06-25 | Repudiation | an unattended write with no backup | critical | mitigate | The backup is taken inside the ceremony where no CLI argument reaches it; the e2e case asserts a backup exists after each of two runs. |
| T-06-26 | Tampering | a CLI-only write path drifting from the screen's | high | mitigate | Both surfaces call the same UI-free per-verb ceremony; a test asserts by construction that each verb calls the named function, and no verb constructs a candidate itself. |
| T-06-28 | Denial of Service | a headless write on a non-terminal without confirmation | medium | mitigate | Refused with exit code 1 and an error naming what is missing, per the adaptive-depth convention; asserted by a test. |
| T-06-42 | Spoofing | a silently changed JSON field or enum breaking a consumer | high | mitigate | The schema document is checked in and versioned; a test asserts the emitted key set EXACTLY equals the documented set and every enum value is a documented member, so an addition is a deliberate, documented change. |
| T-06-43 | Repudiation | an advisory turning a successful backed-up write into a script failure, or a shadowed fix silently exiting zero with no signal | medium | mitigate | The frozen exit table makes zero-on-advisory explicit and carries the advisory in the output AND the JSON; `--fail-on-advisory` is the opt-in for the stricter contract. Both directions asserted. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages. |
</threat_model>

<review_disposition>
## How `06-REVIEWS.md` findings are resolved in this plan

| Finding | Resolution |
|---|---|
| HIGH — exact CLI syntax ambiguous despite being declared contractual | FIXED. The `<frozen_contract>` section names all four command paths and every flag, before implementation, and an acceptance criterion asserts the built tree contains exactly those paths and no others. |
| HIGH — advisory shadowing exit semantics conflict with the phase posture | FIXED. The frozen exit table sets zero-on-advisory as the default, with the D-04/D-14 reasoning recorded in the helper's doc comment and in the matrix notes; `--fail-on-advisory` is the explicit opt-in for a stricter script. Both directions asserted. |
| MEDIUM — "same function" parity is weaker than "same transactional core"; Cobra should not invoke a `tea.Cmd` | FIXED. Both write verbs call the UI-free `runGlobalSSHApply`/`runSSHStorageMigrate` directly with a `lifecyclePolicy`, exactly as `identity_delete.go` and `identity_key.go` already do. No `tea.Cmd` crosses into Cobra. |
| MEDIUM — machine-readable schema not frozen | FIXED. `docs/gitid-ssh-json-schema.md` is a new checked-in, versioned document; a test asserts the emitted key set EXACTLY equals the documented set and every enum value is a documented member. |
| MEDIUM — the final wave is too broad | FIXED by SPLITTING. The predecessor plan's three tasks are now two plans: this one (CLI + matrix) and 06-07 (visual gate + exit battery + review packet), each with independent verification, so a failure is attributable and each commit stays coherent. |
| MEDIUM — scope materially exceeds GSSH-01; traceability should reflect it | ADDRESSED. Matrix rows are keyed on `GSSH-01` and `SHELL-03`; the storage rows' notes record the `STORE-01`/`STORE-03` re-exercise relationship; 06-05 carries the full scope note. The `requirements` field stays `[GSSH-01]` to match the ROADMAP and the requirement-coverage checker. |
| LOW — acceptance criteria use legacy `grep` despite the repository preferring `rg` | FIXED across the revised plan set: every grep-shaped criterion now uses `rg`. |
| xai-grok — assert the CLI holds the real backend and calls the commit by construction, not by duplicating disk assertions | IMPLEMENTED as a by-construction acceptance criterion, with the disk assertions living once in the e2e case. |
</review_disposition>

<verification>
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green.
- `make lint` reports 0 issues, including the tagged variants.
- `make test` green including `gate-copy-freeze`.
- `make test-e2e` green with the new headless CLI case.
- All four parity-matrix tests pass.
</verification>

<success_criteria>
- The reserved `ssh` noun is replaced by exactly the four frozen commands, and the matrix names each of them.
- The machine-readable output matches a checked-in, versioned schema document, key set for key set.
- The exit-status contract is frozen, implemented in one place, and consistent with the phase's advisory posture.
- Both write verbs reach disk only through the ceremonies the TUI uses.
</success_criteria>

<artifacts>
## Artifacts this phase produces (plan 06-06)

**`cmd/gitid`:** the `ssh` command group in `ssh.go` (`options list`, `options apply`, `storage show`, `storage migrate`), replacing the reserved placeholder registration in `main.go`; the single exit-code mapping helper; `ssh_test.go`.

**Documents:** `docs/gitid-ssh-json-schema.md` (frozen, versioned); `docs/cli-parity-matrix.md` shipped rows keyed on this phase's requirement plus `SHELL-03`, with the exit-status and re-exercise notes, and both new dry-run contract rows.

**`e2e`:** `e2e/global_ssh_cli_e2e_test.go` headless CLI coverage including idempotence, backups and exit codes.
</artifacts>

<output>
Create `.planning/phases/06-global-ssh-options/06-06-SUMMARY.md` when done. Record: the final `gitid ssh` command shape as the exercise of the `06-CONTEXT.md` discretion item, confirming it matches `<frozen_contract>` exactly or naming any deviation and why it was raised; the schema document's version identifier; the exit codes observed for each row of the frozen table; and confirmation that no CLI-only write path exists.
</output>
