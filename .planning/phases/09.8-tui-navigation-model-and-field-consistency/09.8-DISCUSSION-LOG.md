# Phase 09.8: TUI Navigation Model and Field Consistency - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-22
**Phase:** 09.8-tui-navigation-model-and-field-consistency
**Areas discussed:** App-wide key model (pre-phase), Options-row edit & save flow, Wizard step navigation, Chosen-value status semantics, Letter shortcuts / footer / proof

---

## App-wide key model (decided before phase insertion, same session)

| Option | Description | Selected |
|--------|-------------|----------|
| A: lazygit/huh style | 1-5 global jump, [ ] subviews, Tab/↑↓ fields, ←/→ values only, Enter picker w/o second confirm, Esc back, Ctrl+S save | ✓ |
| C: picker-only enums | Enums always open a picker; ←/→ free for subviews | |
| B: Tab switches subviews | Tab = subview, ↑↓ fields, modal text editing | |

| Option | Description | Selected |
|--------|-------------|----------|
| 1-5 anywhere, Esc to menu | Arrows inside screens never jump top-level views | ✓ |
| Arrows also jump when free | ←/→ switch top-level when the focus doesn't use them | |

| Option | Description | Selected |
|--------|-------------|----------|
| Modeless | Focused text field takes typing; letters inactive while focused | ✓ |
| Modal (Enter to edit) | Enter starts editing, Enter commits, Esc reverts | |

| Option | Description | Selected |
|--------|-------------|----------|
| Quick CI fix, then phase 09.8 | Green main first (quick 260922-brh), then this phase | ✓ |
| Everything in phase 09.8 | Main stays red until the phase lands | |

## Options-row edit & save flow

| Question | Options | Selected |
|----------|---------|----------|
| How to change a row's value | Direct on focus / Keep `e` to edit | Direct on focus |
| Change vs write relation | Batch, Ctrl+S once / Per-row write | Batch, Ctrl+S once |
| After confirming a write | Inline result, re-read from disk / Receipt screen | Inline result |
| Leaving with unsaved changes | Keep + warn on quit / Ask on leave / Discard silently | Keep + warn on quit |

## Wizard step navigation

| Question | Options | Selected |
|----------|---------|----------|
| Moving between steps | Enter/Esc + [ ] / Keep Shift+←/→ too | Enter/Esc + [ ] |
| Forward past invalid step | Only when valid / Free, validate at Review | Only when valid |
| Multi-option look everywhere | Radio row (≤5) + picker if long / Always compact ‹ value › | Radio row + picker if long |

## Chosen-value status semantics

| Question | Options | Selected |
|----------|---------|----------|
| Deliberate non-recommended value set via gitid | Settled "set by you" / Soft informational note | Settled "set by you" |
| Differing value set outside gitid | Same neutral treatment / Soft note for hand-set only | Same neutral treatment |
| Unset, built-in default differs | Orange ! needs action / Neutral suggestion only | Orange ! needs action |

## Letter shortcuts, footer & proof

| Question | Options | Selected |
|----------|---------|----------|
| Letter verbs | One verb per letter / Minimal letters | One verb per letter |
| Footer | Keys valid for current focus (generated) / Fixed per screen | Keys valid for current focus |
| Proof before merge (multi) | Real-binary PTY tests / UAT checklist you run / Recorded demos | PTY tests + Recorded demos |

## Claude's Discretion

- How `e` exposes identity git name/email (one pane vs actions-menu entry), picker/marker visuals, footer wording, `[ ]` strip drawing, Ctrl+S with nothing pending, wave structure.

## Deferred Ideas

- globalssh shadow simulation / `ssh -G` process-home resolution (read-only) unless on D-20's path.
- Phase 10 formal completion and STATE.md narrative reconciliation (tracked separately).
