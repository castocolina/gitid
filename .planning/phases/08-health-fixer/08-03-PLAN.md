---
phase: 08-health-fixer
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - internal/doctor/checks/orphans.go
  - internal/doctor/checks/orphans_test.go
  - internal/doctor/checks/files.go
  - internal/doctor/checks/files_test.go
  - internal/doctor/doctor.go
  - internal/doctor/doctor_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - cmd/gitid/health.go
  - cmd/gitid/health_test.go
  - internal/tuikit/health_screen.go
  - internal/tuikit/health_screen_test.go
  - internal/tuikit/fixer_screen.go
autonomous: true
requirements: [HLTH-02, HLTH-06]
cross_ai: true

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An SSH Host block whose gitconfig partner was removed via identity delete --git-only never triggers Orphans' destructive remove-block fix -- it is info-severity, report-only."
    - "A populated ~/.ssh/gitid-archive/ directory never produces an unused-key finding, no matter how Deps.KeyPaths is populated."
    - "A ~/.ssh/config or ~/.gitconfig (or any fragment) that fails to parse produces a critical Files-family finding, and that file's section renders a dedicated parse-error frame instead of an ordinary (possibly misleading) finding list -- and gitid health --json's output for that section is likewise suppressed to just the Files finding."
    - "doctor.SeverityCritical now documents TWO conditions (key/secret exposure via Permissions, and total parse failure via Files) both mapping to ExitCode 3; every render/dispatch decision distinguishes them by Family, never by Severity alone -- a Perms-critical finding never triggers the parse-error frame."
  artifacts:
    - internal/doctor/checks/files.go
    - internal/doctor/checks/orphans.go (Class-1 downgrade, corrected copy)
    - internal/doctor/doctor.go (SeverityCritical doc-comment amendment)
  key_links:
    - "cmd/gitid/wiring.go's Deps.KeyPaths population -> sshconfig.IsReservedPath filter -> CheckOrphans Class 3 (mirrors internal/identity/inventory.go's filterReservedKeyPaths precedent)"
    - "CheckFiles critical finding -> health_screen.go/fixer_screen.go's parse-error render branch AND cmd/gitid/health.go's --json array filter (suppresses ordinary findings for that section in both)"
---

<objective>
Close this phase's own governing precedent -- the false-positive-loop class
(project memory: "Doctor reserved-block false-positive loop") -- before
adding any new checks that could reintroduce it, and ship HLTH-02's parse
gates: the one condition Health can only report (a config file that will not
parse), which also PAUSES downstream findings for that file so a user is
never shown a misleading ordinary finding list computed from unparseable
data.

Purpose: D-06 in 08-CONTEXT.md is explicit that "code that fights upstream
decisions gets fixed in this phase" -- Phase 5's `--git-only` delete and
Phase 5's key-archive directory both currently have a live path to a
destructive false positive in `CheckOrphans`, now that Wave 1/2 wired
`doctor.Run()` into the real backend for the first time.
Output: `internal/doctor/checks/files.go`; `CheckOrphans` Class-1 downgrade;
a reserved-path-aware `Deps.KeyPaths`; the parse-error render frame.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-health-fixer/08-CONTEXT.md
@.planning/phases/08-health-fixer/08-RESEARCH.md
@.planning/phases/08-health-fixer/08-UI-SPEC.md
@.planning/design/health/FIELDS.md
@internal/doctor/checks/orphans.go
@internal/sshconfig/include.go
@internal/identity/inventory.go

# Prior plans
@.planning/phases/08-health-fixer/08-01-SUMMARY.md
@.planning/phases/08-health-fixer/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-06.1 -- CheckOrphans Class-1 downgrade (the false-positive-loop fix)</name>
  <files>internal/doctor/checks/orphans.go, internal/doctor/checks/orphans_test.go</files>
  <behavior>
    - A structurally well-formed, currently-referenced SSH Host block with no
      gitconfig counterpart (Class 1, orphans.go:52-87) now produces a
      SeverityInfo finding with Fix: nil -- never the current
      SeverityWarning plus destructive RemoveBlock fix.
    - identity delete --git-only NAME followed by a fresh doctor scan shows
      the surviving SSH block as an info-only row, never offering to remove
      it.
  </behavior>
  <action>
Per 08-RESEARCH.md's Pitfall 4/Assumption A1: downgrade Class 1
(orphans.go:50-87) broadly -- for ANY SSH Host block that is structurally
well-formed (parses, key exists per the existing keyExists-style check
already available via deps.Stat) and currently referenced (its IdentityFile
resolves to a real key on disk), change the finding to doctor.SeverityInfo
with Fix: nil and CORRECTED Title/Explanation/SuggestedFix text -- NOT
08-UI-SPEC.md's DRAFT copy verbatim ("This SSH block's Git identity was
deliberately removed (git-only delete) -- informational only, no action
offered."), which is factually wrong for a block that was CREATED SSH-only
and never had a git-only delete happen at all (a genuine, common case this
same downgrade also covers). Use copy that names BOTH possibilities without
asserting a specific history: e.g. "This SSH block has no matching Git
identity -- created SSH-only, or its Git side was removed intentionally.
Informational only, no action offered." This is a deliberate, documented
divergence from 08-UI-SPEC.md's DRAFT sentence (same scoped-divergence
discipline as D-11's FIELDS.md amendment) -- record it in the SUMMARY so
Wave 8's copy-freeze review (08-08 Task 3) inherits the CORRECTED copy, not
the original DRAFT string. Do NOT attempt to detect "was
this specifically a --git-only delete" via a new persisted marker --
08-RESEARCH.md's Open Question 1 explicitly recommends against inventing
state that does not exist; the safest reading matching this project's
severity discipline is that a destructive auto-offered removal of a
healthy, in-use, well-formed SSH block is the bug regardless of its
history -- info is the correct ceiling here regardless of how
SeverityCritical's own meaning is scoped elsewhere (see Task 3's pinned
decision: critical covers key/secret exposure AND total parse failure,
neither of which describes this well-formed, in-use block). A genuinely broken/unreferenced orphan (key
missing, or the block does not resolve) still falls through to a different,
unaffected Coherence finding (key-missing is already Coherence's job, not
Orphans') -- do not change that path.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/doctor/checks/... -run TestCheckOrphans</automated>
  </verify>
  <done>
A fixture built via the real identity-delete-git-only path (or an equivalent
hand-built fixture matching its on-disk shape) produces an info-severity,
no-fix Orphans finding for the surviving SSH block -- never the destructive
warning it produces today. Existing genuinely-broken-orphan test cases in
orphans_test.go still pass unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-06.2 -- reserved-path registry applied to Deps.KeyPaths</name>
  <files>cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/doctor/checks/orphans_test.go</files>
  <behavior>
    - Whatever function populates internal/doctor.Deps.KeyPaths for the real
      backend (built in Wave 1/2) excludes every path under
      sshconfig.ArchiveDir(sshDir) before CheckOrphans' Class-3 unused-key
      cross-reference runs.
    - A populated ~/.ssh/gitid-archive/ directory (containing retired key
      material from a real rotate/delete-everything operation) never
      produces an unused-key finding.
  </behavior>
  <action>
internal/sshconfig/include.go already exports ReservedPaths(sshDir) []string
and IsReservedPath (include.go:127-138+) -- the D-06.2 registry 08-CONTEXT.md
describes as not-yet-existing is, in fact, already built at the sshconfig
layer (verify this directly against current source before assuming
otherwise; 08-RESEARCH.md's own citation of this file predates a possible
intervening change). Apply the SAME causal-exclusion discipline
internal/identity/inventory.go's filterReservedKeyPaths/IsReservedKeyPath
already uses (inventory.go:113-129) to wherever cmd/gitid/wiring.go
populates internal/doctor.Deps.KeyPaths: filter the enumerated key-file list
through sshconfig.IsReservedPath (or an equivalent predicate built from
ReservedPaths) at the point the list is collected -- never rely on the
enumeration glob happening to miss the archive directory. If
internal/doctor.Deps has no dedicated field/hook for this predicate, thread
it the same way InventoryDeps.IsReservedKeyPath is threaded (a nil-tolerant
injected function field).
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid/... ./internal/doctor/checks/... -run 'TestDeps|TestArchive|TestCheckOrphans'</automated>
  </verify>
  <done>
A regression test with a real, populated ~/.ssh/gitid-archive/ directory
(retired key files present on disk) proves zero unused-key findings are
produced for those archived paths, mirroring the populated-archive
regression test 08-CONTEXT.md D-06.2 names.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HLTH-02 -- Files family (parse gates) + parse-error render frame + SeverityCritical decision</name>
  <files>internal/doctor/checks/files.go, internal/doctor/checks/files_test.go, internal/doctor/doctor.go, internal/doctor/doctor_test.go, internal/tuikit/health_screen.go, internal/tuikit/health_screen_test.go, internal/tuikit/fixer_screen.go, cmd/gitid/health.go, cmd/gitid/health_test.go</files>
  <behavior>
    - ~/.ssh/config missing or failing to parse -> one critical Files/SSH
      finding, and the Health SSH section renders the dedicated parse-error
      frame (file, raw error, offending snippet, "checks paused" copy --
      see health/FIELDS.md's parse-error screen and data.go's already-frozen
      HealthParseErrorFile/Raw/Snippet/Explanation constants) instead of an
      ordinary finding list.
    - ~/.gitconfig or any per-identity fragment failing `git config --file
      <f> --list`'s exit code -> the same critical-severity treatment,
      scoped to the Git section.
    - No downstream Coherence/Baseline/etc. finding for that SAME file's
      derived data is shown alongside the parse-error frame (they would be
      based on unparseable/zero-value content and would mislead) -- this
      applies to `gitid health --json`'s output too, not only the TUI
      render: the flat finding array (Wave 1/2's provisional shape) excludes
      the SAME suppressed downstream findings for that section, so a script
      parsing `--json` never sees findings computed from unparseable data.
    - `doctor.ExitCode` returns 3 for BOTH a Files-critical fixture AND an
      (already-shipped, pre-Phase-8) Perms-critical fixture
      (`internal/doctor/checks/perms.go:37`'s private-key-exposure check,
      which already emits `doctor.SeverityCritical` today) -- same tier, by
      design, per the amended enum meaning below. A render-dispatch test
      proves a Perms-critical finding does NOT trigger the parse-error
      frame -- only `Family == doctor.FamilyFiles` does; `Severity` alone
      can no longer disambiguate which critical condition fired.
  </behavior>
  <action>
Create internal/doctor/checks/files.go implementing HLTH-02: a CheckFiles
function checking (a) ~/.ssh/config exists and parses (reuse this project's
existing sshconfig parse path -- do not hand-roll a second parser) and (b)
~/.gitconfig plus every fragment under ~/.gitconfig.d/ parses via `git
config --file <f> --list` (reuse the injected RunGitConfigGet-style seam
this package already uses elsewhere in doctor.Deps, per Pattern 1 -- no
direct os/exec call inside the check function itself). A parse failure on
either produces doctor.SeverityCritical, Family: a new doctor.FamilyFiles
constant, Target set explicitly ("SSH" or "Git" depending on which file
failed). Add FamilyFiles to doctor.go's Family constants and append it to
Families() AFTER FamilyRedundancy (D-07 -- display-order only; a critical
parse finding replaces the section's finding list entirely, so its position
in the family list does not affect rendering order).

PINNED DECISION -- SeverityCritical's meaning (resolves a real conflict:
`internal/doctor/checks/perms.go:37` already emits
`doctor.SeverityCritical` for a private-key-exposure permissions problem,
and `doctor.go`'s existing doc comment says SeverityCritical means "key/
secret exposure"; 08-CONTEXT.md's D-05 table separately and explicitly
locks "parse fail = critical"). Reconcile by AMENDING
`doctor.go`'s `SeverityCritical` doc comment (do not touch its numeric
value or `ExitCode`'s tiered mapping) to read: "SeverityCritical means
either key/secret exposure (Permissions family) or a config file that
failed to parse entirely (Files family) -- both are immediate-action,
exit-code-3 conditions." Do NOT invent a second severity level or change
`perms.go`'s existing usage. Because `Severity` alone can no longer
disambiguate WHICH critical condition fired, every render/dispatch
decision keyed off SeverityCritical -- health_screen.go/fixer_screen.go's
parse-error-frame trigger and the `--json` suppression logic below -- MUST
branch on `Family == doctor.FamilyFiles`, never on `Severity ==
SeverityCritical` alone: a real Perms-critical finding (genuine
private-key exposure) must NEVER trigger the parse-error frame or suppress
other findings.

Wire the "downstream checks paused" behavior at the CONSUMPTION layer
(health_screen.go, fixer_screen.go, and cmd/gitid/health.go's --json path):
when a FamilyFiles finding at SeverityCritical exists for a given section
(SSH or Git), that section renders ONLY the dedicated parse-error frame --
every OTHER finding scoped to that same section is suppressed from the
rendered list for that render pass (they are potentially computed from
unparseable/zero-value data and would mislead the user). `gitid health
--json`'s flat finding array applies the SAME suppression before
marshaling -- filter the array itself (drop every finding whose Target
matches the FamilyFiles-critical section, except the FamilyFiles finding),
not just the TUI's rendered list, so a script parsing `--json` never sees
the misleading downstream findings either. doctor.Run(deps)
itself still executes every family unconditionally (do not special-case the
Run() call order); verify each of the 9 existing check functions tolerates
a zero-value/empty Deps.Identities, Deps.ManagedHosts, etc. derived from a
failed parse without panicking -- add a fixture test proving this if any
function does not already handle it gracefully.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/doctor/... ./internal/tuikit/... ./cmd/gitid/... -run 'TestCheckFiles|TestParseError|TestFamilyFiles|TestExitCodeCriticalBothTiers|TestHealthJSONParseErrorSuppression'</automated>
  </verify>
  <done>
A fixture ~/.gitconfig.d/work fragment with a deliberate git-config syntax
error produces exactly one critical Files/Git finding; the Health screen's
Git section shows the parse-error frame (file path, raw error, offending
snippet, "checks paused" explanation) instead of an ordinary finding list,
and no other Git-section finding renders alongside it in that pass.
`gitid health --json` against the SAME fixture omits every other Git-section
finding from its array, keeping only the FamilyFiles critical finding.
`doctor.ExitCode` returns 3 for both this fixture and an existing
Perms-critical (private-key-exposure) fixture; a Perms-critical-only
fixture's Health render does NOT show the parse-error frame.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Deps.KeyPaths population -> CheckOrphans Class 3 | An enumeration function whose output must never include gitid's own retained-but-inactive material |
| Malformed config file -> CheckFiles | Adversarial/corrupted file content must be reported, never crash the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08 | Denial of Service | CheckFiles on a malformed/huge config file | low | mitigate | Reuses the existing bounded sshconfig/git-config parse paths already exercised project-wide; no new unbounded read introduced |
| T-08-09 | Information Disclosure | Raw parse error text rendered verbatim | low | accept | The raw error is the user's own file's own syntax error, never secret material -- same posture as every other diagnostic string this project already surfaces |
</threat_model>

<verification>
`go build ./...` succeeds. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green. `make lint` clean. Manual: run `identity delete --git-only` on a real throwaway fixture identity, then `gitid health --json`, confirm the surviving SSH block shows info-severity with no fix.
</verification>

<success_criteria>
The false-positive-loop class is structurally closed (Class-1 downgrade + reserved-path exclusion, both regression-tested); HLTH-02's parse gates ship with a dedicated, non-misleading render frame.
</success_criteria>

<output>
Create `.planning/phases/08-health-fixer/08-03-SUMMARY.md` when done
</output>
