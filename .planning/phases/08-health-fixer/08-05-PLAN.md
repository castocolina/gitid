---
phase: 08-health-fixer
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - internal/doctor/checks/coherence.go
  - internal/doctor/checks/coherence_test.go
  - internal/doctor/checks/orphans.go
  - internal/doctor/checks/orphans_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
autonomous: true
requirements: [HLTH-03, HLTH-04, HLTH-06]
cross_ai: true

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An SSH managed-block option applied but shadowed by an earlier hand-written directive surfaces as a Coherence/SSH warning naming the shadowing file:line, report-only -- reusing Phase 6's own shadow-simulation probe, not a reimplementation."
    - "An identity's author resolution (matched-dir -> fragment, unmatched-dir -> fallback) is re-verified as a doctor check, reusing Phase 7's own probe -- error severity only when the invariant actually breaks."
    - "A user directive placed above gitid's managed SSH block is reported, report-only, never touched outside the D-09 ceremony."
    - "The includeIf-missing-fragment row (D-05's table) is confirmed to be the SAME existing Orphans-family fixture-matching finding, not a duplicate -- OR, if genuinely distinct, is deduplicated against it."
  artifacts:
    - internal/doctor/checks/coherence.go (shadowed-option, author-resolution, directive-above-block checks)
  key_links:
    - "new shadowed-option check -> internal/globalssh.Verify (Phase 6 probe, reused unchanged)"
    - "new author-resolution check -> internal/globalgit.VerifyAuthorResolution (Phase 7 probe, reused unchanged)"
---

<objective>
Ship the remaining D-05 Coherence-family rows, every one of which reuses a
probe or detector Phases 6/7 already built and tested -- this plan wraps
existing, proven detection logic as ordinary `doctor.Finding` values; it does
not invent new detection algorithms. Also resolve 08-RESEARCH.md's Pitfall 6
(the Family conflict between the frozen `git-includeif-missing-fragment`
dummy fixture, tagged `Family: "Orphans"`, and D-05's table, which glosses it
as "Coherence / Git").

Purpose: complete the D-05 new-checks queue's Coherence-family rows without
fragmenting this project's probe/detector vocabulary (08-RESEARCH.md's Don't
Hand-Roll table is explicit: reuse `internal/globalssh.Verify` and
`internal/globalgit.VerifyAuthorResolution`, never reimplement).
Output: three new Coherence checks; a resolved, tested Family assignment for
the includeIf-missing-fragment finding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-health-fixer/08-CONTEXT.md
@.planning/phases/08-health-fixer/08-RESEARCH.md
@internal/doctor/checks/coherence.go
@internal/doctor/checks/orphans.go
@internal/globalssh/shadow.go
@internal/globalgit/authorresolve.go

# Prior plans
@.planning/phases/08-health-fixer/08-01-SUMMARY.md
@.planning/phases/08-health-fixer/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shadowed-option check + directive-above-block check (Coherence/SSH, both report-only)</name>
  <files>internal/doctor/checks/coherence.go, internal/doctor/checks/coherence_test.go</files>
  <behavior>
    - A managed SSH option shadowed by an earlier hand-written directive (as
      internal/globalssh.Verify/ShadowResult already detects) produces one
      Coherence/SSH warning finding, Fix: nil, SuggestedFix naming the
      shadowing file:line in the sentence shape
      "<option> is shadowed by an earlier directive at <file>:<line> --
      available on the Fixer screen." (mirrors every existing SuggestedFix
      sentence shape, per 08-UI-SPEC.md Copywriting Contract).
    - A user directive placed above gitid's managed SSH block produces a
      report-only Coherence/SSH warning, Fix: nil (D-09: user content above
      the managed block is untouchable outside the ceremony).
  </behavior>
  <action>
Add a shadowed-option check to internal/doctor/checks/coherence.go that
calls internal/globalssh.Verify(deps, keys) (shadow.go:293) with the SAME
Deps the real backend already builds for Phase 6's own Options screen --
thread globalssh.Deps into doctor.Deps as an injected field (or a small
adapter constructing it from what doctor.Deps already carries) so the
probe is reused verbatim, not reimplemented. For every ShadowResult.Finding
returned, emit one doctor.Finding{Family: FamilyCoherence, Severity:
SeverityWarning, Target: "SSH", Fix: nil, SuggestedFix: <the file:line
sentence above>}. When ShadowResult.Inconclusive is true, degrade
gracefully (no finding, or an advisory note per Phase 6's own
never-block precedent) -- never treat inconclusive as a false positive.

For the directive-above-block check: detect a hand-written directive
appearing, in file order, BEFORE gitid's managed SSH block's opening
sentinel comment within the same Host stanza's scope -- reuse whatever
line-position data the Wave 2 `AllHostBlocks`/rewrite-target scan already
collects (it already needs line numbers for the surgical rewrite) rather
than writing a second raw scanner. Emit doctor.SeverityWarning,
Family: FamilyCoherence, Target: "SSH", Fix: nil, explaining that user
content above a managed block is left untouched by design.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/doctor/checks/... -run 'TestCheckCoherenceShadowed|TestCheckCoherenceDirectiveAbove'</automated>
  </verify>
  <done>
A fixture reproducing Phase 6's own shadow-simulation test fixture (an
earlier hand-written directive shadowing a managed option) produces exactly
one Coherence/SSH warning naming the correct file:line, via
internal/globalssh.Verify called with real Deps -- no reimplemented
shadow-detection logic exists in coherence.go.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author-resolution check (Coherence/Git)</name>
  <files>internal/doctor/checks/coherence.go, internal/doctor/checks/coherence_test.go, cmd/gitid/wiring.go</files>
  <behavior>
    - For each identity with a matched-directory gitdir: pattern this
      machine can test, internal/globalgit.VerifyAuthorResolution's result
      is wrapped as a doctor.Finding -- error severity ONLY when the
      resolved author does not match the expected fragment/fallback
      (the invariant actually breaks); MatchedNotVerifiable is a graceful
      no-finding state, never a false positive.
  </behavior>
  <action>
Add an author-resolution check calling internal/globalgit.VerifyAuthorResolution
(authorresolve.go:54) with the SAME globalgit.Deps shape Phase 7's own Global
Git screen already builds -- thread it through doctor.Deps the same way as
Task 1's globalssh adapter. For each managed identity with a resolvable
gitdir: match, run the verification from that identity's matched directory
against a representative unmatched directory (reuse Phase 7's own
directory-selection logic if it already exists as a reusable function --
do not invent a new "pick a representative unmatched dir" heuristic). When
the resolved user.email/user.name does not match the expected value for
that context, emit doctor.SeverityError, Family: FamilyCoherence,
Target: "Git", IdentityName: <the identity>, Fix: nil (SuggestedFix routes
to the D9 ceremony / git-screen repair, per D-05's table -- report-only
here).
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/doctor/checks/... ./cmd/gitid/... -run TestCheckCoherenceAuthorResolution</automated>
  </verify>
  <done>
A fixture where an identity's matched-directory author resolution does not
match its own fragment (simulated via a test double) produces exactly one
Coherence/Git error finding, scoped to that identity; a healthy fixture
produces none.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve the includeIf-missing-fragment Family conflict (Pitfall 6)</name>
  <files>internal/doctor/checks/orphans.go, internal/doctor/checks/orphans_test.go, internal/doctor/checks/coherence_test.go</files>
  <action>
Determine definitively whether D-05's "includeIf -> missing fragment" table
row is the SAME check as the existing git-includeif-missing-fragment
fixture (Family: "Orphans", per internal/dummytui/data.go:392) or a
genuinely distinct new Coherence check. Read internal/doctor/checks/orphans.go's
Class 2 (gitconfig blocks with no SSH partner) and coherence.go's existing
Check 2 (includeIf fragment existence, coherence.go:87-102) side by side: if
Check 2 ALREADY covers "an includeIf block's fragment path does not exist
on disk" for every gitid-managed identity, then D-05's row IS Check 2, and
D-05's table's "Coherence / Git" label is confirmed as the correct home
(coherence.go, not orphans.go) -- in which case, verify no separate
Orphans-family finding ALSO fires for the same identity in the same
fixture (add a regression test proving exactly one finding, not two, for a
fixture matching the frozen git-includeif-missing-fragment dummy scenario).
If instead a genuinely distinct condition exists that only Orphans currently
catches, add an explicit dedup guard (skip emitting the Coherence version
when the Orphans version already covers the identical identity+condition)
rather than leaving both to fire.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/doctor/checks/... -run 'TestMissingFragmentNoDuplicate'</automated>
  </verify>
  <done>
A fixture matching the frozen git-includeif-missing-fragment dummy scenario
(identity "legacy", `includeIf` targeting a missing `~/.gitconfig.d/legacy`)
produces EXACTLY ONE finding through the real doctor.Run() path, and its
Family assignment is documented (in the test's own comment or the SUMMARY)
as the resolved answer to Pitfall 6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| doctor.Deps -> internal/globalssh.Verify / internal/globalgit.VerifyAuthorResolution | Reused probes that already run `ssh -G` / `git config --show-origin` via arg-slice exec, no shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-11 | Tampering | os/exec calls inside the reused Phase 6/7 probes | low | accept | Already arg-slice `exec.Command` form (never shell string interpolation), inherited unchanged from Phase 6/7's own hardened wiring -- no new exec surface introduced by this plan |
</threat_model>

<verification>
`go build ./...` succeeds. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green. `make lint` clean.
</verification>

<success_criteria>
Every D-05 Coherence-family row ships by wrapping an existing, tested Phase 6/7 probe -- zero reimplemented detection logic; the includeIf-missing-fragment Family question is resolved and regression-tested against duplication.
</success_criteria>

<output>
Create `.planning/phases/08-health-fixer/08-05-SUMMARY.md` when done
</output>
