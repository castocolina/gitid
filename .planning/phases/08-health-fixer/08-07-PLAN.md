---
phase: 08-health-fixer
plan: 07
type: execute
wave: 7
depends_on: ["08-06"]
files_modified:
  - cmd/gitid/main.go
  - cmd/gitid/health.go
  - cmd/gitid/health_test.go
  - cmd/gitid/fix.go
  - cmd/gitid/fix_test.go
  - cmd/gitid/doctor_alias.go
  - cmd/gitid/doctor_alias_test.go
  - docs/cli-parity-matrix.md
  - e2e/health_fix_cli_e2e_test.go
autonomous: true
requirements: [FIX-01, HLTH-05]
cross_ai: true

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`gitid doctor` runs exactly like `gitid health`, and `gitid doctor --fix` forwards to `gitid fix` -- both hidden aliases (never listed in --help) matching the docker `ps` precedent D-03 names."
    - "`gitid health --json` emits a versioned JSON envelope; the process exit code follows doctor.ExitCode's tiered contract (0/1/2/3)."
    - "Every Phase-8 CLI action has a corresponding parity-matrix row alongside every TUI action, matching this project's requirement-keyed parity discipline (SHELL-03)."
  artifacts:
    - cmd/gitid/doctor_alias.go
    - docs/cli-parity-matrix.md (Health/Fixer rows)
    - e2e/health_fix_cli_e2e_test.go
  key_links:
    - "gitid doctor -> newHealthCmd's RunE (same code path, hidden alias); gitid doctor --fix -> newFixCmd's RunE"
    - "doctor.ExitCode(findings) -> process exit status (cmd/gitid/ssh.go's exitCodeError pattern, reused)"
---

<objective>
Complete the CLI half of Phase 8, mirroring this project's own convention
(Phase 5's 05-08, Phase 6's 06-06, Phase 7's 07-05): the hidden `doctor`
alias (D-03, "docker `ps` precedent"), a versioned JSON envelope and the
tiered exit-status contract, the parity-matrix rows, and a headless CLI e2e
proof exercising `health`/`fix`/`--identity`/`--json`/`--yes`/`--dry-run`/the
`doctor` alias together.

Purpose: SHELL-03 requires every product action reachable from both TUI and
CLI with no gap; this plan closes that gap for Phase 8's two new commands and
retires the `health`/`fix` reserved-noun stubs completely.
Output: `gitid doctor` (hidden) + `gitid doctor --fix` (hidden, forwarding);
a versioned `--json` schema; parity-matrix rows; a headless e2e test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-health-fixer/08-CONTEXT.md
@docs/cli-parity-matrix.md
@cmd/gitid/main.go
@internal/doctor/doctor.go

# Prior plans
@.planning/phases/08-health-fixer/08-01-SUMMARY.md
@.planning/phases/08-health-fixer/08-02-SUMMARY.md
@.planning/phases/08-health-fixer/08-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `doctor` hidden alias + `doctor --fix` forwarding shim</name>
  <files>cmd/gitid/doctor_alias.go, cmd/gitid/doctor_alias_test.go, cmd/gitid/main.go</files>
  <behavior>
    - `gitid doctor` runs the same RunE as `gitid health` (same flags:
      `--json`, `--identity`), never appears in `gitid --help`'s command
      list (Hidden: true).
    - `gitid doctor --fix` forwards to the SAME RunE `gitid fix` uses.
      PINNED flag mapping (D-03's "Claude's discretion" note resolved, not
      left open): `--fix` alone selects FIX MODE only -- it is equivalent
      to plain `gitid fix` (interactive, per-finding stdin confirm unless
      `--yes` is ALSO passed); `--fix --yes` is equivalent to `gitid fix
      --yes` (unattended, full D-10 verification loop still runs
      unconditionally -- there is no flag that skips it). `--fix` never
      implies blanket auto-apply by itself; `--yes` is what removes the
      interactive per-finding confirm, exactly as it already does for
      `fix` directly.
  </behavior>
  <action>
Create cmd/gitid/doctor_alias.go implementing a hidden `doctor` Cobra command
(`Hidden: true`, matching this project's existing hidden-command precedent
if one exists, or Cobra's `Hidden` field directly) whose RunE delegates to
the SAME function `newHealthCmd`'s RunE calls -- do not duplicate the
health-reading logic, share it via an exported/internal helper both commands
call. Add `--fix` and `--yes`/`--dry-run` bool flags on the `doctor` command
specifically (not on `health`) that, when `--fix` is set, delegate to the
SAME function `newFixCmd`'s RunE calls instead, threading `--yes`/
`--dry-run` through UNCHANGED (never re-interpreted at the alias layer) --
this SAME RunE already handles the `Fix.Interactive`-vs-`Fix.Fn` dispatch
08-02 Task 3 built, so `doctor --fix` shares it verbatim, no duplicate
apply-gate logic. Register `newDoctorAliasCmd()` in cmd/gitid/main.go's
`newRootCmd` alongside the existing `health`/`fix`/`ssh`/`git` registrations.
Document the exact flag-mapping decision (pinned above, no longer
discretionary) in a doc comment on the command constructor.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid/... -run TestDoctorAlias</automated>
  </verify>
  <done>
`gitid doctor` and `gitid health` produce byte-identical output against the
same fixture; `gitid doctor --fix --yes` applies the same fix `gitid fix
--yes` would; `gitid doctor --fix` (no `--yes`) against a scripted stdin
confirm behaves identically to plain `gitid fix` (no `--yes`) against the
SAME fixture -- it does NOT silently auto-apply; `gitid --help`'s output
does not list `doctor`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Versioned JSON envelope + tiered exit codes</name>
  <files>cmd/gitid/health.go, cmd/gitid/health_test.go, cmd/gitid/fix.go, cmd/gitid/fix_test.go</files>
  <behavior>
    - `gitid health --json`'s output is a versioned envelope (mirror this
      project's existing versioned-JSON-envelope precedent from Phase
      6/7's own `ssh`/`git` command groups -- read one of those command
      files for the exact envelope shape convention before inventing a new
      one) rather than a bare array. This SUPERSEDES 08-01 Task 1's
      provisional flat-array `--json` shape (that array was explicitly
      marked non-contractual in 08-01) -- an intentional, planned breaking
      change from tracer-wave shape to final contract, not a regression;
      update or replace any Wave 1/3 JSON-shape test that still asserts the
      old array shape.
    - The process exit status after `gitid health`/`gitid fix` matches
      `doctor.ExitCode(findings)`'s tiered contract: 0 no findings, 1
      highest is warning/info, 2 highest is error, 3 highest is critical.
      Per 08-03's pinned SeverityCritical decision, tier 3 fires for EITHER
      a Files-family parse failure OR a Permissions-family key/secret
      exposure -- both are genuinely "stop everything" conditions and
      intentionally share the SAME exit code; `ExitCode` itself stays
      Family-agnostic (only render/copy dispatch needs Family).
  </behavior>
  <action>
Read one of Phase 6's (`cmd/gitid/ssh.go`) or Phase 7's git-command JSON
envelope implementations to find this project's existing versioned-envelope
convention (a schema/version field, a top-level shape) and apply the SAME
convention to `gitid health --json`'s output -- do not invent a new
envelope shape. The versioned envelope's per-finding grouping (ssh/git-keyed)
must still carry the SAME parse-failure suppression 08-03 Task 3 wired into
the (now-superseded) flat array -- do not regress that behavior while
changing the shape. Wire `doctor.ExitCode(findings)` (already implemented,
`doctor.go:280-297`) to the process exit status via the SAME
`*exitCodeError` pattern `cmd/gitid/ssh.go:71-77` already establishes (an
error implementing `ExitCode() int`, translated by `exitStatusOf` in
`main.go`) -- reuse this mechanism, do not build a parallel one.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid/... -run 'TestHealthJSONEnvelope|TestHealthExitCode|TestFixExitCode|TestExitCodeCriticalBothTiers'</automated>
  </verify>
  <done>
`gitid health --json` output validates against the same envelope shape
convention `ssh`/`git`'s JSON commands already use; a fixture with only a
warning-level finding exits 1; a fixture with a critical Files parse-error
finding exits 3; a fixture with only a critical Permissions
(key/secret-exposure) finding also exits 3, confirming the tier is shared
by design across both critical triggers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Parity-matrix rows + headless CLI e2e</name>
  <files>docs/cli-parity-matrix.md, e2e/health_fix_cli_e2e_test.go</files>
  <action>
Add Health/Fixer rows to docs/cli-parity-matrix.md following this project's
existing requirement-keyed format (read the file first to match its exact
column shape) -- one row per distinct product action: view findings
(TUI key 4 / `gitid health`), scope to one identity (deep-link / `gitid
health --identity`), apply one fix (`f` ceremony / `gitid fix`), apply all
fixable findings (`F` batch / `gitid fix --yes`), preview without writing
(N/A in TUI / `gitid fix --dry-run`), and the `doctor` alias row. Add
`e2e/health_fix_cli_e2e_test.go` mirroring this project's existing headless
CLI e2e pattern (Phase 5/6/7's own parity e2e tests) driving a real built
binary over a hermetic fake home: `health --json`, `health --identity`,
`fix --dry-run`, `fix --yes`, and `doctor`/`doctor --fix` all exercised
end-to-end against a real fixture.
  </action>
  <verify>
    <automated>make test-e2e</automated>
  </verify>
  <done>
`make test-e2e` is green including the new Health/Fixer CLI e2e cases; the
parity matrix has no missing Phase 8 row (verified by whatever automated
completeness check this project's existing parity-matrix tooling already
runs, if one exists -- otherwise a manual completeness pass against this
plan's own must_haves).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `gitid fix --yes` (unattended) | The only CLI path that can apply a destructive hand-written-directive rewrite without a live human confirming a ceremony keystroke |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-13 | Repudiation | `gitid fix --yes` applying the D-09 flagship rewrite unattended | medium | mitigate | `--yes` still runs the FULL D-10 verification loop (parse-render-re-parse + ssh -G re-check + auto-restore) -- there is no "skip verification" flag; `--yes` only skips the interactive per-finding confirm prompt, never the safety loop itself |
</threat_model>

<verification>
`go build ./...` succeeds. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green. `make lint` clean. `make test-e2e` green.
</verification>

<success_criteria>
`gitid doctor`/`gitid doctor --fix` work as hidden aliases; `--json` is versioned; exit codes are tiered correctly; the parity matrix and a headless e2e test cover every Phase 8 CLI action.
</success_criteria>

<output>
Create `.planning/phases/08-health-fixer/08-07-SUMMARY.md` when done
</output>
