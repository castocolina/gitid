---
phase: 08-health-fixer
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - internal/doctor/checks/baseline.go
  - internal/doctor/checks/baseline_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
autonomous: true
requirements: [HLTH-03, HLTH-04, FIX-01]
cross_ai: true

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A missing global gitignore pair (core.excludesfile unset, or set but the file missing) surfaces as a Baseline/Git finding at the correct severity (warning for the fully-missing pair, error when the key is set but the file is absent)."
    - "Fixing it writes BOTH core.excludesfile and the managed pattern file in one fix -- never a key-only fold-in that leaves git tolerating a dangling excludesfile."
    - "A baseline key the user has deliberately overridden to a different value ('set, differs') is reported at info severity, is test-asserted to never escalate to warning/error, and is excluded from any needs-action tally."
  artifacts:
    - internal/doctor/checks/baseline.go (gitignore-pair check + fix, set-differs info cap)
  key_links:
    - "CheckBaseline's new gitignore finding's Fix.Fn -> internal/gitconfig.WriteGlobalGitignore + DefaultGitignorePatterns (dormant substrate, D-11 obligation from Phase 7)"
---

<objective>
Ship D-05's two Baseline-family checks: the global-gitignore pair (a Phase 7
D-11 obligation explicitly deferred to this phase's fixer, per
REQUIREMENTS.md §J's own note) and the "set, differs" informational hard cap
(Phase 7 D-02's concept, re-run here as a doctor check so it appears
alongside every other baseline fact in one place).

Purpose: complete the D-05 new-checks queue's Baseline-family rows using the
DORMANT substrate Phase 7 already built (`WriteGlobalGitignore`,
`DefaultGitignorePatterns`) but never wired to any check or CLI surface.
Output: two new `CheckBaseline` findings, one of them fixable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-health-fixer/08-CONTEXT.md
@.planning/phases/08-health-fixer/08-RESEARCH.md
@internal/doctor/checks/baseline.go
@internal/gitconfig/baseline.go

# Prior plans
@.planning/phases/08-health-fixer/08-01-SUMMARY.md
@.planning/phases/08-health-fixer/08-02-SUMMARY.md
@.planning/phases/08-health-fixer/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Global-gitignore pair check + fix</name>
  <files>internal/doctor/checks/baseline.go, internal/doctor/checks/baseline_test.go</files>
  <behavior>
    - core.excludesfile unset AND no gitid-managed pattern file present ->
      warning finding, Family: Baseline, Target: "Git".
    - core.excludesfile SET but the file it points at is missing -> error
      finding (a dangling excludesfile git silently tolerates).
    - core.excludesfile set AND the file exists AND its content matches
      internal/gitconfig.DefaultGitignorePatterns -> no finding.
    - The Fix writes BOTH core.excludesfile (git config) AND the managed
      pattern file content in one FixDescriptor.Fn call -- never a
      key-only write.
  </behavior>
  <action>
Read internal/doctor/checks/baseline.go's existing shape first (it already
implements other Baseline checks) and add a new check function in the same
file, following its existing per-key pattern. Query the current
core.excludesfile value + file existence through the SAME injected
RunGitConfigGet-style Deps seam every other Baseline/Coherence check already
uses (never a direct exec.Command inside the check). Build the fix's
FixDescriptor.Fn to call BOTH gitconfig-key-set (git config
--global core.excludesfile <path>) AND
internal/gitconfig.WriteGlobalGitignore(path,
gitconfig.DefaultGitignorePatterns) in sequence inside the SAME Fn closure
-- a partial failure (key set but file write fails, or vice versa) must
surface as a single failed fix, not a half-applied state (wrap both calls
so an error from either aborts before the other, or make the ordering
tolerant of re-running: WriteGlobalGitignore is itself idempotent per its
own doc comment, "content byte-identical to the existing file, skip write").
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/doctor/checks/... -run TestCheckBaselineGitignore</automated>
  </verify>
  <done>
Three fixture states (unset, key-set-file-missing, correctly-set) each
produce the expected finding (none/warning/error). Applying the fix on the
key-set-file-missing fixture leaves BOTH core.excludesfile correctly set
AND the pattern file present with DefaultGitignorePatterns' content.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: "Set, differs" informational hard cap</name>
  <files>internal/doctor/checks/baseline.go, internal/doctor/checks/baseline_test.go</files>
  <behavior>
    - A baseline key (e.g. init.defaultBranch, core.autocrlf) the user has
      explicitly set to a value different from gitid's recommended baseline
      value is reported at doctor.SeverityInfo, Fix: nil.
    - A property-based/table-driven test asserts this finding's severity can
      NEVER be anything other than SeverityInfo across every baseline key
      this check covers -- the "hard cap" from D-05.
  </behavior>
  <action>
Check whether internal/doctor/checks/baseline.go (or a Phase 7 successor
package) already implements an equivalent "set, differs" concept for the
Global Git options screen (Phase 7 D-02) -- if a comparable classification
function already exists there, REUSE it (do not duplicate the comparison
logic) and wrap its result as a doctor.Finding here; if it does not exist at
the doctor-check layer, implement it fresh in baseline.go following the same
per-key pattern as Task 1. The finding's Explanation states plainly that
this is the user's own deliberate choice, never a problem to fix (D-05:
"provably a no-op"). Exclude this finding from any severity tally / exit-code
contribution beyond what SeverityInfo already implies (doctor.ExitCode
already treats info as the lowest tier -- verify no special-casing is
needed beyond using SeverityInfo correctly).
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/doctor/checks/... -run 'TestCheckBaselineSetDiffers|TestSeverityNeverEscalates'</automated>
  </verify>
  <done>
A fixture with init.defaultBranch overridden to a non-default value produces
exactly one SeverityInfo finding, Fix: nil; the table-driven hard-cap test
covers every baseline key this check inspects and fails loudly if any
produces a non-info severity.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire both checks into real Deps and regression-test the pairing</name>
  <files>cmd/gitid/wiring.go, cmd/gitid/wiring_test.go</files>
  <action>
Confirm both new checks run through the SAME CheckBaseline function already
wired into doctor.Deps.CheckBaseline (Wave 1 Task 2's full-family wiring) --
no new Deps field should be needed unless a genuinely new injected effect
(e.g. a dedicated RunGitConfigSet for the excludesfile key) is required;
thread it the same way this project's existing injected-function-field
pattern works. Add an end-to-end regression test against a real fixture home
proving both findings surface through gitid health --json in the SAME pass
as every other Baseline finding, and that applying the gitignore fix via
gitid fix --yes leaves the fixture's global gitconfig and pattern file in
the expected final state.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid/... -run 'TestBaselineGitignoreRealWiring|TestSetDiffersRealWiring' && make lint</automated>
  </verify>
  <done>
`go test -race ./...` is green with no regression; both findings appear
correctly through the full real-backend path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| gitignore-pair fix -> global gitconfig + a new managed pattern file | A write path touching a file outside ~/.ssh/~/.gitconfig.d for the first time this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-10 | Tampering | WriteGlobalGitignore's managed-block write | low | mitigate | Reuses the existing filewriter chokepoint (backup + idempotent managed-block replace, unchanged since Phase 1/7) -- no new write mechanism introduced |
</threat_model>

<verification>
`go build ./...` succeeds. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green. `make lint` clean.
</verification>

<success_criteria>
Both Baseline-family D-05 rows ship, wired to real Deps, with the gitignore fix using the dormant Phase 7 substrate and the set-differs check test-proven to never escalate.
</success_criteria>

<output>
Create `.planning/phases/08-health-fixer/08-04-SUMMARY.md` when done
</output>
