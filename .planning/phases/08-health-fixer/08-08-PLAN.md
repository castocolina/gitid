---
phase: 08-health-fixer
plan: 08
type: execute
wave: 8
depends_on: ["08-07"]
files_modified:
  - e2e/health_fixer_pty_e2e_test.go
  - internal/screenshot/healthfixer.go
  - internal/tuikit/health_screen.go
  - internal/tuikit/fixer_screen.go
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-health-fixer/08-REVIEWS.md
autonomous: false
requirements: [HLTH-01, HLTH-02, HLTH-03, HLTH-04, HLTH-05, HLTH-06, FIX-01, FIX-02, DLV-04, DLV-06]
cross_ai: true

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every Health/Fixer screen state (health-with-findings, health-all-green, per-identity-health, parse-error; fixer-list, ceremony states A/B, batch walk, nothing-to-fix) has at least one raw-keystroke PTY e2e test driving the REAL compiled binary."
    - "The real TUI's Health/Fixer tabs are compared against the approved Bubble Tea mockup via the golden-text visual-regression gate, with every difference classified as an improvement or a defect -- Known Divergence #1 (tab split) and #2 (compressed ceremony) recorded as approved, documented divergences."
    - "The full exit battery (go test -race, make lint, make test-e2e, make gate-visual-regression) passes, and HLTH-01..06/FIX-01/02 are marked Complete in REQUIREMENTS.md against real, executed evidence."
  artifacts:
    - e2e/health_fixer_pty_e2e_test.go
    - .planning/phases/08-health-fixer/08-REVIEWS.md
  key_links:
    - "PTY e2e drives the real cmd/gitid binary's TabHealth/TabFixer, keys 4/5, exactly as a human would"
    - "gate-visual-regression compares the real TUI's rendered text against cmd/gitid-dummy's frozen Phase-2 goldens for the two new tabs"
---

<objective>
Close DLV-04/DLV-06 for Phase 8, the same two-part UI-wave gate every prior
UI-bearing phase (3, 4, 5, 6, 7) has closed: (1) a raw-keystroke PTY e2e test
per screen state driving the real compiled binary (closes the project's
documented injected-seam wiring blindspot), and (2) an automated
golden-text visual-regression comparison against the approved
`cmd/gitid-dummy` mockup, with every real-vs-dummy difference explicitly
classified. This is the phase's final wave: it also runs the full exit
battery and marks HLTH-01..06/FIX-01/02 Complete in REQUIREMENTS.md.

Purpose: no Phase 8 screen ships without the SAME real-binary proof and
design-parity discipline every earlier phase already required (project
convention, Phase 3 D-24/D-25).
Output: `e2e/health_fixer_pty_e2e_test.go`; a registered visual-regression
allowlist for both new tabs; `08-REVIEWS.md`; REQUIREMENTS.md closure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-health-fixer/08-UI-SPEC.md
@.planning/design/health/FIELDS.md
@.planning/design/fixer/FIELDS.md
@e2e/git_configuration_pty_e2e_test.go
@.planning/REQUIREMENTS.md

# Every prior Phase 8 plan
@.planning/phases/08-health-fixer/08-01-SUMMARY.md
@.planning/phases/08-health-fixer/08-02-SUMMARY.md
@.planning/phases/08-health-fixer/08-03-SUMMARY.md
@.planning/phases/08-health-fixer/08-04-SUMMARY.md
@.planning/phases/08-health-fixer/08-05-SUMMARY.md
@.planning/phases/08-health-fixer/08-06-SUMMARY.md
@.planning/phases/08-health-fixer/08-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raw-keystroke PTY e2e per Health/Fixer screen state (DLV-06)</name>
  <files>e2e/health_fixer_pty_e2e_test.go</files>
  <behavior>
    - Each named state from health/FIELDS.md (health-with-findings,
      health-all-green, finding-detail [the always-visible inline pane, per
      Known Divergence #1], per-identity-health, parse-error) and
      fixer/FIELDS.md (fixer-list, ceremony state A, ceremony state B,
      batch walk, nothing-to-fix) has at least one PTY test case sending
      REAL keystrokes to the compiled `cmd/gitid` binary and asserting on
      its real rendered output.
  </behavior>
  <action>
Create e2e/health_fixer_pty_e2e_test.go mirroring
e2e/git_configuration_pty_e2e_test.go's structure (raw-keystroke PTY
session against the real built binary, over a hermetic fake home fixture
per case). Drive key `4` into Health and key `5` into Fixer; exercise every
state listed above, including the flagship fix-in-place ceremony (typed
confirm "clientb.github.com", real backup, real result receipt) and the
batch walk's halt-on-failure path (Wave 6). Reuse this project's existing
PTY-session helper/harness (the same one `git_configuration_pty_e2e_test.go`
and Phase 5/6/7's own PTY suites already use) -- do not build a new harness.
  </action>
  <verify>
    <automated>make test-e2e</automated>
  </verify>
  <done>
`make test-e2e` passes with every Health/Fixer state covered by at least one
raw-keystroke PTY case; the flagship fix ceremony's real byte-for-byte
before/after diff is asserted against real file content, not a mock.
  </done>
</task>

<task type="auto">
  <name>Task 2: Visual-regression gate registration with classified divergence allowlist (DLV-04)</name>
  <files>internal/screenshot/healthfixer.go, internal/tuikit/health_screen.go, internal/tuikit/fixer_screen.go</files>
  <action>
Register both new tabs in this project's existing golden-text
visual-regression gate (the same in-process render-stack driver
`internal/screenshot/createflow.go` established for prior surfaces, mirrored
here as `internal/screenshot/healthfixer.go` or extending the existing
comparison driver if it is surface-agnostic already -- check before creating
a new file). Add divergence-allowlist entries for the two KNOWN, ALREADY-
APPROVED divergences 08-UI-SPEC.md documents: Known Divergence #1 (the tab
split itself -- `TabDoctor` -> `TabHealth`+`TabFixer`, a strict correction
of a stale `frame.go` comment, not a design regression) and Known Divergence
#2 (the compressed 2-state ceremony vs. FIELDS.md's nominal 4-screen chain --
already the SAME compaction Phases 3-7 all carry). Add negative controls
(mirroring Phase 6/7's own negative-control precedent) proving the gate
actually fails when a real divergence is introduced -- do not ship a gate
that only ever passes.
  </action>
  <verify>
    <automated>make gate-visual-regression</automated>
  </verify>
  <done>
`make gate-visual-regression` passes for both new tabs with the two
documented divergences allowlisted by name; a deliberately introduced
undocumented divergence (removed in the same commit after verifying) proves
the gate fails when it should.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Cross-AI review packet, full exit battery, REQUIREMENTS.md closure</name>
  <files>.planning/phases/08-health-fixer/08-REVIEWS.md, .planning/REQUIREMENTS.md</files>
  <what-built>
Claude assembled a review packet (mirroring Phase 3's 03-06 review-packet
convention -- text diffs + MANIFEST.md) covering every Health/Fixer render
difference this phase's visual-regression gate found, for
`agent-ui-ux-designer` + Codex review (the same D-24/D-25 convention every
prior UI-bearing phase used), ran the full exit battery
(`TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...`, `make lint`,
`make test-e2e`, `make gate-visual-regression`), and drafted
`.planning/phases/08-health-fixer/08-REVIEWS.md` with a findings/disposition
table plus a DRAFT REQUIREMENTS.md closure edit for HLTH-01..06/FIX-01/02.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/08-health-fixer/08-REVIEWS.md`'s findings
       table -- confirm the exit battery's four commands are all reported
       green with real (not asserted) output evidence.
    2. Confirm zero CRITICAL/HIGH findings remain open from the
       agent-ui-ux-designer + Codex packet review; any MEDIUM/LOW findings
       are explicitly dispositioned (fixed, deferred, or accepted with
       rationale), not silently dropped.
    3. Confirm the DRAFT `.planning/REQUIREMENTS.md` edit marks HLTH-01
       through HLTH-06 and FIX-01/FIX-02 Complete, Phase 8, citing this
       plan's evidence -- and that no OTHER phase's rows were touched.
  </how-to-verify>
  <resume-signal>Type "approved" to apply the REQUIREMENTS.md closure edit, or describe the outstanding CRITICAL/HIGH finding(s) to address first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PTY e2e / visual-regression fixtures -> real filesystem | Test fixtures write/read real (throwaway, hermetic) SSH/Git config files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-14 | Tampering | PTY e2e fixtures accidentally touching the DEVELOPER's real ~/.ssh or ~/.gitconfig | high | mitigate | Every fixture runs over a hermetic fake HOME (this project's existing PTY-harness convention, reused unchanged from Phase 3-7's own PTY suites) -- never the real environment |
</threat_model>

<verification>
`go build ./...` succeeds. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green. `make lint` clean. `make test-e2e` green. `make gate-visual-regression` green.
</verification>

<success_criteria>
Every Health/Fixer screen state is PTY-proven against the real binary; the visual-regression gate covers both new tabs with a classified, tested divergence allowlist; the cross-AI review closes with no outstanding CRITICAL/HIGH finding; HLTH-01..06/FIX-01/02 are Complete in REQUIREMENTS.md.
</success_criteria>

<output>
Create `.planning/phases/08-health-fixer/08-08-SUMMARY.md` when done
</output>
