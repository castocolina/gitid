---
phase: 08-health-fixer
plan: 06
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - internal/tuikit/health_screen.go
  - internal/tuikit/health_screen_test.go
  - internal/tuikit/fixer_screen.go
  - internal/tuikit/fixer_screen_test.go
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - internal/tuikit/store.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - cmd/gitid/health.go
  - cmd/gitid/health_test.go
autonomous: true
requirements: [HLTH-01, HLTH-05, FIX-01, FIX-02]
cross_ai: true

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No confirm/backup/apply write-ceremony marker string appears anywhere in Health's rendered output, including any inherited render path -- negatively asserted by a grep-style test, not just documented."
    - "The Fixer's detail pane offers f (Fix this...) for EVERY fixable finding produced by Waves 2-5, not only the flagship."
    - "A specific identity's health (HLTH-05) is reachable from the Identity Manager's own detail/action affordance, scoped to that identity's SSH + Git findings via FindingsFor."
    - "A fix that fails mid-batch-walk restores its own backup, the batch halts naming the failure, and lists what already succeeded -- earlier completed and re-verified fixes stand."
  artifacts:
    - internal/tuikit/health_screen.go (negative-assertion grep test)
    - internal/tuikit/identities.go (D-04 per-identity health deep-link)
    - internal/tuikit/fixer_screen.go (D-16 batch queue-halt message)
  key_links:
    - "Identity Manager row's detail affordance -> Health's per-identity-health screen -> store.FindingsFor(state, identityName)"
    - "Fixer's F batch walk -> ceremony commitFailed -> D-16 halt message naming the failed fix and the already-succeeded list"
---

<objective>
Close the render-layer gaps 08-UI-SPEC.md flags as unresolved or
backstop-only: Health's read-only negative assertion (currently only
documented, never negatively tested), the Fixer's fixable-set completeness
now that Waves 2-5 shipped several new fixable findings, D-04's per-identity
health entry point (left as "Claude's discretion" in 08-CONTEXT.md, with
08-UI-SPEC.md recommending the Identity-Manager deep-link), and D-16's
single-fix rollback scope + queue-halt message for the `F` batch walk.

Purpose: these are exactly the kind of finding-count-growth and
completeness risks 08-UI-SPEC.md's own "UI Considerations" table marks
"backstop" (unverified until this point) -- verify them now that the real
finding set is no longer just the flagship.
Output: a negative-assertion test for Health; the Identity-Manager
per-identity health deep-link; the batch-walk halt/rollback behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-health-fixer/08-CONTEXT.md
@.planning/phases/08-health-fixer/08-UI-SPEC.md
@.planning/design/health/FIELDS.md
@internal/tuikit/store.go
@internal/tuikit/identities.go

# Prior plans
@.planning/phases/08-health-fixer/08-01-SUMMARY.md
@.planning/phases/08-health-fixer/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Health negative-assertion grep gate + Fixer's complete fixable set</name>
  <files>internal/tuikit/health_screen.go, internal/tuikit/health_screen_test.go, internal/tuikit/fixer_screen.go, internal/tuikit/fixer_screen_test.go</files>
  <behavior>
    - Rendering Health across every state (with-findings, all-green,
      per-identity, parse-error) never contains any of: "Apply fix",
      "Confirm write", "Backed up ->", "Wrote ->", or an "f dot" prefix --
      same negative-assertion discipline health/FIELDS.md already mandates
      for the design layer, now enforced in Go.
    - Every fixable finding introduced by Waves 2-5 (the flagship, the
      gitignore pair, any Baseline/Coherence fix with a non-nil Fix) appears
      in the Fixer's fixableFindings-filtered list and its detail pane shows
      the "Fix this" affordance.
  </behavior>
  <action>
Add a table-driven grep-style test over health_screen.go's rendered View()
output across every reachable Health state (drive it the same way this
project's existing golden-text tests drive other screens -- via the shared
tuikit render stack in-process, no PTY needed for this check) asserting NONE
of the forbidden marker strings appear, matching health/FIELDS.md's own
negatively-asserted contract. If any inherited code path (from the
doctor.go split in Wave 1) still carries a stray fix-trigger key handler or
a "Fix this" line, remove it now -- this is exactly the copy-paste risk
08-UI-SPEC.md's own "primary focus" warning names.

Add a fixture exercising every fixable finding this phase has shipped
through Wave 5 (flagship IdentitiesOnly rewrite, gitignore pair,
allowed_signers corrections, etc.) and assert fixerModel's fixableFindings
list contains all of them, each showing the "Fix this" affordance when
selected.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run TestHealthNegativeAssertion</automated>
  </verify>
  <done>
The negative-assertion test passes across every Health state; the
complete-fixable-set test passes with every Wave 2-5 fixable finding
represented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04 per-identity health entry point (deep-link from Identity Manager)</name>
  <files>internal/tuikit/identities.go, internal/tuikit/identities_test.go, cmd/gitid/health.go, cmd/gitid/health_test.go</files>
  <behavior>
    - From an Identity Manager row's existing detail/action affordance, a
      new action opens Health's per-identity-health view scoped to that
      identity (the SAME slice HealthPerIdentityMgrHandoff copy already
      names).
    - `gitid health --identity NAME` filters via Finding.IdentityName, with
      identity-name shell completion.
  </behavior>
  <action>
Per 08-UI-SPEC.md's own recommendation (Known Divergence discussion, "UI
Considerations" table, per-identity health entry point row): wire a
deep-link from the Identity Manager's detail pane (internal/tuikit/identities.go)
into the Health screen's per-identity slice, using the existing
store.FindingsFor(state, identityName) function unchanged. This is an
ADDITIVE affordance on the existing detail pane -- do not restructure the
Identity Manager's own layout or key allocation beyond adding this one
action. Wire `gitid health --identity <name>` (cmd/gitid/health.go, built in
Wave 1) to filter doctor.Run(deps)'s output by IdentityName, with Cobra
shell-completion registered against the real identity list (mirror this
project's existing identity-name completion pattern from the identity noun
group). Per D-04's Claude's-discretion note: decide and document whether
global (empty-IdentityName) findings appear labeled inline
("(global, not identity-specific)", per 08-UI-SPEC.md's DRAFT copy) or are
excluded entirely from the scoped view -- either is acceptable, but the
choice must be consistent between the TUI deep-link and the CLI flag.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... ./cmd/gitid/... -run TestIdentityHealthDeepLink</automated>
  </verify>
  <done>
Selecting an identity in the Identity Manager and triggering the new action
opens Health scoped to that identity's own findings only (plus/minus global
findings per the documented consistent choice); `gitid health --identity
legacy` returns the same scoped set the TUI deep-link shows.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-16 batch queue-halt message + single-fix rollback scope</name>
  <files>internal/tuikit/fixer_screen.go, internal/tuikit/fixer_screen_test.go</files>
  <behavior>
    - During an `F` batch walk, if one fix's apply fails (verification
      mismatch, write error, etc.), the walk HALTS: it does not silently
      skip to the next queued finding.
    - The halt message names the failed fix and lists every fix that
      already succeeded in this walk (Wave 2's D-10 auto-restore already
      guarantees the FAILED fix's own file is back to its pre-fix state;
      this task only adds the user-facing halt behavior and message).
    - Earlier, already-applied-and-verified fixes in the SAME batch walk are
      never rolled back -- they stand.
  </behavior>
  <action>
Extend fixer_screen.go's batch-walk handling (the `doctorBatch`
queue-advance logic ported from the former doctorModel in Wave 1) so that a
`ceremonyFailed`/`commitFailed` outcome during a batch walk stops advancing
the queue and renders the D-16 halt message (08-UI-SPEC.md Copywriting
Contract's DRAFT string: "Fix N of M failed and was rolled back from its own
backup -- the first N-1 fixes already applied stand. Nothing else in this
batch was attempted.", with N/M interpolated) instead of silently continuing
to the next finding or silently stopping. The existing ceremony
`commitFailed`/retry-state render (ceremony.go:280-287, 350-362) already
covers the SINGLE-fix failure visual; this task's new code is the
BATCH-level halt decision and message, not a new ceremony render state.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run TestBatchWalkHalt && make lint</automated>
  </verify>
  <done>
A batch-walk fixture where the second of three fixes fails: fix 1 stands
applied, fix 2 shows the halt message naming itself and listing fix 1 as
already-succeeded, and fix 3 is never attempted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Identity Manager deep-link -> Health per-identity slice | Read-only navigation, no new write surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Tampering | Batch-walk halt logic silently continuing past a failure | medium | mitigate | Explicit halt-on-failure behavior + regression test (Task 3); mirrors the project's existing all-or-nothing-per-artifact discipline (Phase 4 D-11) applied at the per-fix granularity D-16 specifies |
</threat_model>

<verification>
`go build ./...` succeeds. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green. `make lint` clean.
</verification>

<success_criteria>
Health's read-only posture is negatively proven in Go, not just documented; every shipped fixable finding is reachable through the Fixer; per-identity health has a real entry point; a mid-batch failure halts honestly instead of silently continuing or stopping.
</success_criteria>

<output>
Create `.planning/phases/08-health-fixer/08-06-SUMMARY.md` when done
</output>
