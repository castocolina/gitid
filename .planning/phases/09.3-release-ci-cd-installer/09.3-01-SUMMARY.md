---
phase: 09.3-release-ci-cd-installer
plan: 01
subsystem: release-ci-cd-installer
tags: [ci, ldflags, checksums, install-script, e2e]
key-files:
  - cmd/gitid/main.go
  - Makefile
  - .github/workflows/ci.yml
  - scripts/install.sh
  - e2e/release_e2e_test.go
metrics:
  tasks: 3
  commits: 3
  test-e2e-seconds: 941.4
---

# Phase 09.3 Plan 01 — Summary

## Commits

| Task | Commit | Description |
|------|--------|--------------|
| 1 | `6b88419` | Stamp binaries with `-X main.version/commit/buildDate` via optional-default `VERSION`/`COMMIT`/`DATE`/`LDFLAGS` on `build`/`build-cross`/`install`; `make checksums` target hashing from inside `bin/`; `scripts/install.sh` (curl\|sh installer, `shasum` fallback); `e2e/release_e2e_test.go` proving stamped build → manifest → fixture-served download → verify → install → stamp round-trip. `test-e2e` timeout raised 900s → 1200s. |
| 2 | `0b17f35` | `release:` GitHub Actions job (`v*` tag scoped, job-scoped `contents: write`, `needs: [check, build-cross]`, SHA-pinned `softprops/action-gh-release@efb3536… # v3.0.3`). `relver` step classifies `prerelease`/`make_latest` from the tag's SemVer suffix and wires both as inputs to the publish action — the release's publication state is decided atomically at publish time, never corrected afterward. `cmd/gitid/release_plumbing_test.go`, an untagged test pinning the workflow invariants on every `make test` run. |
| 3 | `fa583c0` | Widened installer/e2e coverage: full 8-spelling OS/arch matrix, unsupported-OS/arch refusal (zero requests before download), missing-checksum-entry refusal, asset/checksums 404, overwrite of an existing install, install-dir creation, piped-into-`sh` delivery, `shasum` fallback when `sha256sum` is absent, and a refused install leaving nothing behind. `scripts/install.sh` needed no behavior change — Task 1's allowlists already covered every case; verified against a deliberately reverted script that the new tests do fail. |

## Review Findings Incorporated

All 4 HIGH + 5 actionable non-HIGH findings from `09.3-REVIEWS.md` cycle 1 (codex) were incorporated during the replan (`3e305db`, before this plan's execution) — see each plan's `<review_response>` table. Notably the prerelease-atomicity fix (HIGH #1) changed Task 2's design: the publish-then-correct approach originally planned was replaced with publish-once-correct, removing the need for any post-publication `gh release edit` step.

## Deviations

None beyond the design change already captured as a review response in Task 2.

## Verification

- `go test -tags e2e -count=1 -timeout 180s -run 'TestInstallScript_'` — 27/27 pass (includes the 8-way OS/arch matrix as one parent test with 8 subtests).
- `make test-e2e` — full suite, 941.4s, PASS (`-race`, 1200s timeout).
- `make lint` — 0 issues.
- `sh -n scripts/install.sh` — syntax clean.

## Self-Check

PASSED — all 3 tasks committed, every acceptance_criteria in the plan verified by running the actual command, no `--no-verify` used, all pre-commit hooks green.

## Note on Execution Path

Tasks 1-2 (and Task 3's test/script authoring) were delegated to the cross-AI executor (`opencode run --model local-llm-env/my-coding`) per this project's Rule 12 (cross_ai: true). The cross-AI run hit its 3600s budget mid-way through Task 3's `make test-e2e` verification (exit 124) — code was already committed for Tasks 1-2 and Task 3's test/script edits were complete and passing their scoped run + lint, just uncommitted. The orchestrator (this session) finished the remaining pure-verification step (`make test-e2e`, already proven clean) and committed Task 3 directly, since no further code generation was needed.
