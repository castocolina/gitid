---
phase: 10-linux-validation-release-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - PLATFORM-NOTES.md
  - .planning/phases/10-linux-validation-release-pipeline/bazzite-uat-checklist.md
autonomous: true
requirements: [PLAT-03]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A per-distro portability ledger (PLATFORM-NOTES.md) exists at the repo root with real, sourced rows — not a blank template — covering both what the fedora container job already proves and what only a real Bazzite machine can verify."
    - "A concrete, runnable Bazzite UAT checklist exists, giving the human a per-item exact command and pass/fail expectation for every D-03 container-invisible risk (SELinux, /home symlink, real-terminal TUI rendering, wl-clipboard presence, ssh-add graceful degradation)."
  artifacts:
    - PLATFORM-NOTES.md
    - .planning/phases/10-linux-validation-release-pipeline/bazzite-uat-checklist.md
  key_links:
    - "bazzite-uat-checklist.md's per-item findings -> PLATFORM-NOTES.md rows (D-04's explicit routing: checklist runs the check, ledger records the outcome)"
---

<objective>
Create the D-04 portability ledger (`PLATFORM-NOTES.md`) and the D-03 Bazzite manual-UAT checklist that together close the second half of D-01's two-part Linux validation vehicle — the container-invisible residue Plan 10-02's automated fedora job structurally cannot exercise (no SELinux enforcement, no real Wayland/desktop session, no gcr-ssh-agent).

Purpose: PLAT-03 requires portability gaps to be "fixed or logged as accepted limitations" — this plan gives that requirement a concrete, sourced artifact instead of leaving it implicit. A real live Bazzite UAT run is a recurring, per-release human action outside this autonomous execution's reach (no real Bazzite hardware is available here); this plan ships the mechanism and the currently-known findings, with the Bazzite-only rows explicitly logged as "pending manual UAT" — itself a legitimate, PLAT-03-compliant accepted-limitation state until the human runs the checklist.
Output: PLATFORM-NOTES.md (root-level ledger) and bazzite-uat-checklist.md (a phase-scoped runbook).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md
@.planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PLATFORM-NOTES.md — the per-distro portability ledger (D-04)</name>
  <files>PLATFORM-NOTES.md</files>
  <read_first>
    .planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md D-01, D-03, D-04
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "user_constraints" D-03 and the "Supporting" stack table (the real, verified fedora:latest dnf/ssh -V findings)
  </read_first>
  <action>
    Create PLATFORM-NOTES.md at the repo root: a short intro naming the D-01 two-part validation vehicle (the fedora:latest container job in CI, plus the per-release manual Bazzite UAT this ledger and bazzite-uat-checklist.md exist to support — Bazzite's userland IS Fedora's, so this pair is covering), then a "Distro | Aspect | Status | Workaround | Issue" table (D-04's exact column set) pre-filled with real, sourced rows: (1) Fedora | ssh -V version-string parsing | Verified: no distro suffix on OpenSSH_10.2p1, unlike Debian/Ubuntu's suffixed form; the existing parser already handles both | n/a | none; (2) Fedora (container) | CI prerequisites | Verified: the base image ships none of git/openssh-clients/make/nodejs by default; installed via dnf in the CI job | dnf install -y --setopt=install_weak_deps=False git openssh-clients make nodejs tar gzip | none; (3) Bazzite | wl-clipboard presence (KDE/GNOME Wayland) | Pending manual UAT | none yet | none; (4) Bazzite | ssh-add / no-agent graceful degradation (Fedora KDE ships none by default; GNOME uses the gcr-ssh-agent systemd user socket) | Pending manual UAT | none yet | none; (5) Bazzite | SELinux spot-check on ~/.ssh writes | Pending manual UAT, verify-once-and-log | none yet | none; (6) Bazzite | /home to /var/home symlink includeIf resolution | Pending manual UAT | none yet | none; (7) Bazzite | Real-terminal TUI rendering (Ptyxis, Konsole, Wayland) | Pending manual UAT | none yet | none. Close with a short "How this ledger is updated" note: every Bazzite UAT finding (see bazzite-uat-checklist.md) updates a row here — a fixed gap gets an Issue link, an accepted limitation gets a Workaround note, once per release.
  </action>
  <verify>
    <automated>test -f PLATFORM-NOTES.md &amp;&amp; grep -c '^| Fedora\|^| Bazzite' PLATFORM-NOTES.md</automated>
    <fails_when>the file does not exist, or the row count is less than 7 (the file was created without all seven sourced rows)</fails_when>
  </verify>
  <done>PLATFORM-NOTES.md exists at the repo root with the D-04 five-column table and all seven sourced rows (two Verified, five Pending manual UAT).</done>
</task>

<task type="auto">
  <name>Task 2: bazzite-uat-checklist.md — the runnable manual UAT runbook (D-03)</name>
  <files>.planning/phases/10-linux-validation-release-pipeline/bazzite-uat-checklist.md</files>
  <read_first>
    .planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md D-03
    PLATFORM-NOTES.md (the five "Pending manual UAT" rows this checklist exists to close)
  </read_first>
  <action>
    Create .planning/phases/10-linux-validation-release-pipeline/bazzite-uat-checklist.md: a runbook with one section per D-03 UAT-only risk item, each giving the exact command to run, what a PASS looks like, and an instruction to update the matching PLATFORM-NOTES.md row afterward. Sections: (1) SELinux spot-check — run `ls -Z ~/.ssh` immediately after gitid writes a config file; PASS is an unchanged or `ssh_home_t`-labeled context (the documented SELinux failure mode is server-side `authorized_keys`, not user-context writes — a denial here would be a genuine, previously-unseen finding); (2) /home to /var/home symlink includeIf resolution — create an identity with gitid, then confirm `git config --show-origin --get-regexp .` from inside a real clone under the symlinked home resolves the expected includeIf-selected fragment; PASS is the correct identity's user.name/user.email resolving; (3) Real-terminal TUI rendering — launch `gitid` interactively in both Ptyxis and Konsole under a real Wayland session; PASS is no glyph corruption, no color-scheme breakage, and mouse click-to-focus working exactly as it does on macOS; (4) wl-clipboard presence — from the Identity Manager, use gitid's copy-public-key action, then paste outside the terminal; PASS is the key landing in the system clipboard via wl-copy (or the GNOME/KDE equivalent) with no error; (5) ssh-add graceful degradation — on a machine with no running SSH agent (Fedora KDE ships none by default), confirm gitid does not error or hang anywhere it optionally shells to ssh-add; PASS is a clean advisory/skip, never a crash or a hang. Close with an instruction: after running all five, update the corresponding rows in ../../../../PLATFORM-NOTES.md (relative to this file) from "Pending manual UAT" to either "Verified" (with a one-line note) or "Accepted limitation" (with the workaround), and redact any real hostname/username/local path from what gets committed.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-linux-validation-release-pipeline/bazzite-uat-checklist.md &amp;&amp; grep -c '^## \|^### ' .planning/phases/10-linux-validation-release-pipeline/bazzite-uat-checklist.md</automated>
    <fails_when>the file does not exist, or it has fewer than 5 numbered/headed sections (one of the five D-03 UAT-only risk items is missing)</fails_when>
  </verify>
  <done>bazzite-uat-checklist.md exists with all five D-03 UAT-only risk items, each carrying a concrete command and pass criterion, and an explicit instruction routing findings back into PLATFORM-NOTES.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Committed UAT findings | PLATFORM-NOTES.md and bazzite-uat-checklist.md are public, repo-committed documents; a human logging a real UAT finding could paste machine-identifying detail into them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-01 | Information Disclosure | bazzite-uat-checklist.md findings routed into PLATFORM-NOTES.md | low | mitigate | The checklist's closing instruction explicitly tells the human to redact real hostnames/usernames/local paths before committing a logged finding. |
</threat_model>

<verification>
- `test -f PLATFORM-NOTES.md` and the table has all 7 rows.
- `test -f .planning/phases/10-linux-validation-release-pipeline/bazzite-uat-checklist.md` and it has all 5 sections.
- No file in this plan writes outside PLATFORM-NOTES.md / the phase directory (both are new, isolated files).
</verification>

<success_criteria>
D-04's ledger and D-03's Bazzite UAT checklist both exist, sourced from RESEARCH's real, verified findings rather than blank placeholders; PLAT-03's "portability gaps are fixed or logged as accepted limitations" clause is satisfied for every D-03 risk item — either closed (the two container-verified rows) or explicitly logged as pending, with a concrete mechanism to close it.
</success_criteria>

<output>
Create `.planning/phases/10-linux-validation-release-pipeline/10-03-SUMMARY.md` when done
</output>
