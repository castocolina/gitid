---
phase: 10-linux-validation-release-pipeline
plan: 06
type: execute
wave: 4
depends_on: ["10-05", "10-03"]
files_modified:
  - README.md
autonomous: true
requirements: [BUILD-03, PLAT-03]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "README.md documents every real install path (install.sh, Homebrew tap, manual ~/.local/bin, go install with its version-fallback caveat), leads with the manual/inspect-first path per D-14's security framing, and shows the exact D-15 per-OS checksum-verify commands."
    - "README.md links to PLATFORM-NOTES.md so a Linux user can see gitid's current portability status before installing."
  artifacts:
    - README.md (updated)
  key_links:
    - "README's documented install commands -> the ACTUAL commands Plan 10-04/10-05 built (goreleaser artifact names, install.sh's real env vars, the real tap formula path) — not aspirational text"
---

<objective>
Close Phase 10 by refreshing README.md through the user's README-crafting skill (D-16) — the phase's own explicit closing requirement: every install path documented, the D-15 verify commands shown verbatim, and PLATFORM-NOTES.md linked.

Purpose: D-16 explicitly requires this refresh to be executed VIA the README-crafting skill, not hand-written, and to be the phase's last deliverable — by this point every fact it documents (goreleaser artifact names, install.sh's real behavior, the tap formula, PLATFORM-NOTES.md) is real and built, not aspirational.
Output: an updated README.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md
@.planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md
@README.md
@scripts/install.sh
@.goreleaser.yaml
@PLATFORM-NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md refresh via the README-crafting skill (D-16)</name>
  <files>README.md</files>
  <read_first>
    README.md (current install/usage sections, whatever they currently document from Phase 9.3)
    scripts/install.sh (Plan 10-05's rewritten script — its real env vars: GITID_VERSION, GITID_INSTALL_DIR, GITID_INSTALL_BASE_URL)
    .goreleaser.yaml (Plan 10-04's real brews: stanza — the exact `castocolina/homebrew-tap` formula name/path)
    PLATFORM-NOTES.md (Plan 10-03's ledger — the exact link target)
    .planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md D-14, D-15, D-16
  </read_first>
  <action>
    Invoke the crafting-effective-readmes skill to update README.md — do not hand-write this section from scratch outside the skill, per D-16's explicit instruction. Direct the skill to cover: (1) every real install path — the curl|sh one-liner running the rewritten scripts/install.sh, `brew install castocolina/homebrew-tap/gitid`, the manual `~/.local/bin` download+verify+chmod path, and `go install github.com/castocolina/gitid/cmd/gitid@latest` with its version-fallback caveat (an unstamped, buildinfo-derived version — see internal/version.Resolve()'s D-09 fallback, not a stamped release build); (2) lead with the manual/inspect-first path and "download, inspect, then run" phrasing BEFORE the curl|sh one-liner (D-14's security-conscious ordering for a tool whose whole pitch is safe ~/.ssh mutation); (3) the D-15 per-OS checksum-verify commands verbatim — Linux `sha256sum --ignore-missing -c gitid_&lt;v&gt;_checksums.txt`, macOS `shasum -a 256 --ignore-missing -c gitid_&lt;v&gt;_checksums.txt`; (4) a link to PLATFORM-NOTES.md so a Linux user can see gitid's current per-distro portability status before installing; (5) GITID_VERSION and GITID_INSTALL_DIR documented as install.sh's pin/override env vars. Every command and path named must be the REAL one Plan 10-04/10-05 built — verify each against the actual .goreleaser.yaml/install.sh content read above, not copied from the retired Phase 9.3 shape.
  </action>
  <verify>
    <automated>grep -c 'install.sh\|homebrew-tap\|GITID_VERSION\|GITID_INSTALL_DIR\|sha256sum --ignore-missing\|shasum -a 256 --ignore-missing\|PLATFORM-NOTES.md' README.md</automated>
    <fails_when>the count is 0 for any of the required tokens above — a value of exactly 0 means that specific D-14/D-15/D-16 element never made it into the README</fails_when>
  </verify>
  <done>README.md documents install.sh, the Homebrew tap, the manual path, and go install (with its version-fallback caveat); it leads with inspect-first phrasing; it shows both D-15 verify commands verbatim; it links PLATFORM-NOTES.md; every command matches what Plan 10-04/10-05 actually built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| README's install instructions -> a stranger's first trust decision about the project | Documentation is a social-engineering surface: what a README asks a user to run first shapes their security posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06-01 | Tampering (social engineering via install guidance) | README.md install section ordering | low | mitigate | The manual, inspect-first path is documented BEFORE the curl\|sh one-liner (D-14), matching security-conscious install guidance for a tool whose core value proposition is safe local config mutation. |
</threat_model>

<verification>
- README.md contains every required token (install.sh, homebrew-tap, GITID_VERSION, GITID_INSTALL_DIR, both checksum-verify commands, PLATFORM-NOTES.md link).
- Manual read-through confirms the manual/inspect-first path appears before the curl|sh one-liner.
</verification>

<success_criteria>
D-16 is fully satisfied: README.md is refreshed via the README-crafting skill, documents every real install path this phase built, shows the D-15 verify commands verbatim, and links PLATFORM-NOTES.md — closing Phase 10.
</success_criteria>

<output>
Create `.planning/phases/10-linux-validation-release-pipeline/10-06-SUMMARY.md` when done
</output>
