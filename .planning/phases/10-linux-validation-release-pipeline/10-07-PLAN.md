---
phase: 10-linux-validation-release-pipeline
plan: 07
type: execute
wave: 1
depends_on: ["10-04", "10-05"]
cross_ai: true
files_modified:
  - .goreleaser.yaml
  - Makefile
  - .github/workflows/nightly.yml
  - scripts/install.sh
  - cmd/gitid/goreleaser_config_test.go
  - cmd/gitid/release_yml_test.go
  - cmd/gitid/nightly_yml_test.go
  - e2e/release_e2e_test.go
autonomous: true
requirements: [BUILD-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "A real `goreleaser release` invocation (release mode, not --snapshot) with HOMEBREW_TAP_GITHUB_TOKEN unset does NOT fail on the missing tap repo/token — the `release`/`release-nightly` make targets pass `--skip=homebrew` whenever the token is empty."
    - "make release-nightly creates a fresh v0.0.0-nightly.<ts>.<sha> tag and runs the ordinary goreleaser release command against it (no --nightly flag — that does not exist in the pinned OSS v2.18.0 binary; see D-19), ALWAYS skipping homebrew (nightly never touches the stable tap formula, token present or not)."
    - "scripts/install.sh's existing headless behavior (GITID_VERSION pin or /releases/latest redirect) is byte-for-byte unchanged when GITID_CHANNEL is unset and no usable /dev/tty exists."
    - "A usable-tty probe that actually opens /dev/tty (not test -t 0) gates the new interactive menu; a container with a non-openable /dev/tty node falls back to headless resolution."
  artifacts:
    - .goreleaser.yaml
    - Makefile
    - .github/workflows/nightly.yml
    - scripts/install.sh
    - cmd/gitid/nightly_yml_test.go
  key_links:
    - "Makefile release/release-nightly targets -> `--skip=homebrew` conditional on HOMEBREW_TAP_GITHUB_TOKEN emptiness"
    - "nightly.yml -> make release-nightly -> a fresh v0.0.0-nightly.* tag + ordinary `goreleaser release --clean --skip=homebrew,announce` (never touches release.yml's tag-triggered job — disjoint tag namespace)"
    - "install.sh GITID_CHANNEL=nightly -> GitHub /repos/castocolina/gitid/releases API (curl, no gh) -> newest prerelease tag matching nightly naming"
---

<objective>
Gate the D-13 Homebrew `brews:` publish leg so a real tag release succeeds without the tap repo/PAT (deferred, not deleted); add a nightly release path wired through a new `make release-nightly` target and `.github/workflows/nightly.yml` — using goreleaser's ORDINARY `release` command against a freshly created `v0.0.0-nightly.<ts>.<sha>` tag, since the originally-intended native `--nightly` mode/`nightly:` config turned out (verified empirically, see D-19) to be GoReleaser-Pro-only and unavailable in this project's pinned OSS binary; add `GITID_CHANNEL=stable|nightly` resolution plus a real usable-`/dev/tty` interactive menu to `scripts/install.sh`, reusing the `castocolina/wezterm-setup` `/dev/tty`-revival technique without copying its asset/repo mechanics.

Purpose: 10-CONTEXT.md addendum D-18/D-19/D-20 (2026-09-05 revision) — closes the human_needed Homebrew-tap blocker in 10-VERIFICATION.md by making it non-blocking, and adds nightly + channel-select install UX the user requested after reviewing their own wezterm-setup project.
Output: a real `make release-snapshot`-adjacent proof that release mode tolerates a missing HOMEBREW_TAP_GITHUB_TOKEN; `make release-nightly`; `nightly.yml`; rewritten `scripts/install.sh` with channel + TTY-menu support and full e2e coverage.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md
@.goreleaser.yaml
@Makefile
@.github/workflows/release.yml
@scripts/install.sh
@e2e/release_e2e_test.go
@cmd/gitid/goreleaser_config_test.go
@cmd/gitid/release_yml_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the brews: publish leg + add native nightly release (D-18, D-19)</name>
  <files>.goreleaser.yaml, Makefile, .github/workflows/nightly.yml, cmd/gitid/goreleaser_config_test.go, cmd/gitid/nightly_yml_test.go, e2e/release_e2e_test.go</files>
  <read_first>
    .goreleaser.yaml (brews: stanza, release: stanza)
    Makefile (release/release-snapshot targets, GORELEASER var, setup-env-release)
    .github/workflows/release.yml (full file — nightly.yml mirrors its structure with fewer permissions)
    cmd/gitid/release_yml_test.go (jobBlock/readRepoFile helpers, permissionsBlockEntries pattern to reuse for nightly_yml_test.go)
  </read_first>
  <action>
    codegraph_explore first for any symbol touched (Makefile/YAML are not Go source but the e2e test helpers are — search `stampedArtifacts`, `readRepoFile`, `jobBlock` before editing).

    IMPORTANT deviation from the pre-planned mechanism, discovered empirically during this
    task and already reconciled in 10-CONTEXT.md's D-19 (read it before proceeding):
    `goreleaser release --help` (pinned v2.18.0, this repo's exact binary per D-05) has NO
    `--nightly` flag, and `goreleaser jsonschema` contains zero occurrences of `nightly` —
    GoReleaser's native nightly-builds feature is GoReleaser-Pro-only and is NOT adopted here.
    Implement D-19's revised, empirically-grounded mechanism instead (below) — do not attempt
    to add a `nightly:` block to `.goreleaser.yaml` or a `--nightly` flag anywhere; both would
    be silently ignored or rejected by the pinned OSS binary.

    Add a code comment above `.goreleaser.yaml`'s `brews:` stanza documenting D-18: the
    stanza is INTENTIONALLY left exactly as D-13 specified (never deleted) — gating happens
    at the Makefile/CLI level via `--skip=homebrew`, not by editing this file, so the day
    HOMEBREW_TAP_GITHUB_TOKEN exists the publish leg activates with zero config changes.
    Do not add any `nightly:` block to this file.

    Makefile: change the `release` target so it computes
    `SKIP_HOMEBREW := $(if $(HOMEBREW_TAP_GITHUB_TOKEN),,--skip=homebrew)` and passes
    `$(SKIP_HOMEBREW)` to the goreleaser invocation. Add a new `release-nightly` target that:
    (1) computes `NIGHTLY_TAG := v0.0.0-nightly.$(shell date -u +%Y%m%d%H%M%S).$(shell git rev-parse --short HEAD)`;
    (2) creates it with `git tag -a "$(NIGHTLY_TAG)" -m "nightly release"` and pushes it
    (`git push origin "$(NIGHTLY_TAG)"` — needed so goreleaser's GitHub release/changelog
    steps have a real remote ref to describe against); (3) best-effort deletes prior
    `v0.0.0-nightly.*` tags/releases first, via `gh release list`/`gh release delete`/
    `git push --delete origin` filtered to that exact pattern, wrapped so a missing/
    unauthenticated `gh` logs a note and continues rather than failing the build (approximates
    goreleaser-Pro's `keep_single_release` with ordinary tooling, since that key is also
    Pro-only); (4) invokes `$(GORELEASER) release --clean --skip=homebrew,announce` (NOT
    `--nightly` — that flag does not exist in this pinned version) against the freshly
    pushed tag, exporting the SAME VERSION/COMMIT/DATE pattern `release`/`release-snapshot`
    already use (VERSION should reflect the nightly tag with its leading `v` stripped, matching
    the Makefile's existing v-prefix-stripping convention). Add `release-nightly` to `.PHONY`.
    Do not change `release-snapshot` (snapshot mode already skips ALL publish steps including
    brews, so it needs no new flag).

    Create .github/workflows/nightly.yml: `name: Nightly`; `on: { schedule: [{cron: "17 4 * * *"}], workflow_dispatch: {} }`
    (a non-round-number cron minute avoids the top-of-hour GitHub Actions scheduling
    congestion documented in their own docs); one job `nightly` on ubuntu-latest,
    `permissions: { contents: write }` only (no id-token/attestations — nightly skips
    provenance attestation to keep the workflow narrow, documented in a comment); steps:
    checkout (same SHA pin as release.yml, `fetch-depth: 0`), setup-go (same SHA pin),
    `make setup-env-release`, `make test`, `make lint`, then `make release-nightly` with
    `env: { GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} }` only (no HOMEBREW_TAP_GITHUB_TOKEN —
    release-nightly never reads it; GITHUB_TOKEN covers both goreleaser's release publish and
    the `gh release`/`git push` tag-cleanup calls). Add a header comment cross-referencing
    D-19 and stating explicitly this workflow can never collide with release.yml's
    tag-triggered job: disjoint tag namespaces (`v0.0.0-nightly.*` vs. real `vMAJOR.MINOR.PATCH`
    tags) and a different trigger.

    Extend cmd/gitid/goreleaser_config_test.go with a test asserting `.goreleaser.yaml`
    contains NO `nightly:` key (regression guard against accidentally adding the Pro-only,
    silently-ignored block) and that the existing `brews:` stanza is still present unchanged
    (D-18: deferred, not deleted — assert `token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"` is
    still there).

    Create cmd/gitid/nightly_yml_test.go (package main) mirroring release_yml_test.go's
    helpers (reuse jobBlock/readRepoFile/permissionsBlockEntries from that package) asserting:
    nightly.yml exists; triggers on `schedule` AND `workflow_dispatch`; the `nightly` job's
    permissions block has EXACTLY `contents: write` (no id-token/attestations); every `uses:`
    line is SHA-pinned; the step sequence is setup-env-release -> test -> lint -> release-nightly.
    Also assert the Makefile's `release-nightly` target text contains `--skip=homebrew` and
    does NOT contain the literal flag `--nightly` (regression guard for the D-19 deviation).

    Extend e2e/release_e2e_test.go (or add a new e2e file if cleaner) with a REAL functional
    proof of D-18 (REVIEW cycle-1 finding #4 fix: exactly TWO runs, each with ONE unambiguous
    boolean assertion — do not conflate them). Build goreleaser's release-mode invocation
    against a scratch git repo with an annotated semver-prerelease tag at HEAD (mirroring
    stampedArtifacts's pattern but in `release` mode, not `--snapshot`), with
    HOMEBREW_TAP_GITHUB_TOKEN deliberately UNSET in both runs:
    - Run A: `--skip=homebrew,publish,announce,validate` (homebrew IS skipped, alongside the
      network-dependent legs a scratch repo with a fake remote cannot perform). ASSERT: exit 0.
      This is the exact flag set the Makefile's gated `release`/`release-nightly` targets use
      when the token is absent — proves the gated path is genuinely safe with no token.
    - Run B: `--skip=publish,announce,validate` only (homebrew is NOT skipped this time — the
      one deliberate difference from Run A). Record whichever real outcome occurs (exit 0 or
      non-zero) as the test's finding, asserted as a fact, not guessed: if it fails, that
      proves the gate is strictly load-bearing (an ungated release would break without the
      token); if it also exits 0, document in a code comment that the token render is
      publish-network-time-only, so the Makefile-level `--skip=homebrew` gate is a defense-in-
      depth safeguard that still remains the correct place to keep the fix. Either outcome is
      an acceptable, valid test result — write the assertion to match whichever the real run
      produces, never asserted in advance of running it once to observe.
  </action>
  <verify>
    <automated>go test -count=1 ./cmd/gitid/... -run 'TestGoreleaserConfig|TestNightly' -v</automated>
    <fails_when>a `nightly:` key was added to .goreleaser.yaml (Pro-only, regression), brews: stanza was deleted instead of gated, or nightly.yml's structural assertions fail</fails_when>
    <automated>HOMEBREW_TAP_GITHUB_TOKEN= make -n release | grep -q -- '--skip=homebrew'</automated>
    <fails_when>the release target's dry-run command line does not include --skip=homebrew when the token is empty</fails_when>
    <automated>make -n release-nightly | grep -q -- '--skip=homebrew' && ! (make -n release-nightly | grep -q -- '--nightly ')</automated>
    <fails_when>release-nightly's dry-run command line does not unconditionally skip homebrew, or (regression guard) it contains a literal --nightly flag that does not exist in the pinned goreleaser binary</fails_when>
  </verify>
  <done>A real goreleaser release-mode run against a fresh scratch tag, skipping homebrew alongside the legs a scratch repo cannot perform, does not fail on a missing HOMEBREW_TAP_GITHUB_TOKEN; the negative-control run (homebrew not skipped) has its real observed outcome documented, not assumed; make release-nightly creates a fresh v0.0.0-nightly.* tag, always skips homebrew, and never passes a --nightly flag; nightly.yml is structurally verified; brews: stanza is untouched in .goreleaser.yaml.</done>
</task>

<task type="auto">
  <name>Task 2: install.sh GITID_CHANNEL resolution + real usable-/dev/tty interactive menu (D-20)</name>
  <files>scripts/install.sh, e2e/release_e2e_test.go</files>
  <read_first>
    scripts/install.sh (full file — version resolution block, asset/checksums naming, existing GITID_INSTALL_BASE_URL seam)
    e2e/release_e2e_test.go lines 1-430 (fixture server helpers: startGitHubRedirectFixtureServer, runInstallScript/runInstallScriptPiped, populateReleaseDownloadDir)
  </read_first>
  <action>
    codegraph_explore first for install.sh's existing test helpers (startGitHubRedirectFixtureServer, runInstallScript, runInstallScriptPiped) before editing either file.

    scripts/install.sh: preserve every existing headless code path byte-for-byte when
    GITID_CHANNEL is unset (GITID_VERSION pin, else /releases/latest redirect — do not
    reorder or rewrap this branch). Add:
    (1) A `GITID_CHANNEL` env var. When `GITID_CHANNEL=stable` and GITID_VERSION is unset,
        behave exactly like the current unset-channel default (latest non-prerelease via the
        /releases/latest redirect) — do not add a second code path for the same behavior.
    REVIEW cycle-1 finding #2 fix: production `${GITHUB_ORIGIN}` (default
    `https://github.com`) serves download/redirect URLs, but the GitHub *releases API*
    (`/repos/${REPO}/releases`) lives at a DIFFERENT real host, `api.github.com` — one
    origin cannot correctly serve both in production. Add a SECOND, independently-defaulted
    seam: `API_ORIGIN="${GITID_INSTALL_API_BASE_URL:-https://api.github.com}"`. Production
    callers never set either override and get the two real distinct hosts; e2e tests set
    BOTH `GITID_INSTALL_BASE_URL` and `GITID_INSTALL_API_BASE_URL` to the SAME fixture
    server URL (which serves both path shapes), so this remains a pure origin-substitution
    seam exactly like the existing one, just doubled for the one new host family.

    (2) When `GITID_CHANNEL=nightly`, resolve the latest nightly via
        `curl -fsSL "${API_ORIGIN}/repos/${REPO}/releases"`, parse the JSON response with
        POSIX tools only (grep/sed/awk — no jq dependency, matching the script's existing
        curl+tar-only constraint) to find the newest entry (the API returns them newest-first)
        whose `"tag_name"` matches the pattern `v0.0.0-nightly.` prefix (the exact tag shape
        Task 1's `release-nightly` Makefile target creates per-run — NOT a fixed literal tag
        name; there is no goreleaser-side single-release guarantee, since `keep_single_release`
        is GoReleaser-Pro-only, so the script must pick the newest matching entry itself).
    (3) A real usable-tty probe: `if [ -z "${GITID_VERSION:-}" ] && [ -z "${GITID_CHANNEL:-}" ] && { : < /dev/tty; } 2>/dev/null; then` — enters an interactive branch that fetches
        `${API_ORIGIN}/repos/${REPO}/releases` (same endpoint as (2)), lists the most
        recent 5-10 entries (tag_name + prerelease flag) as a numbered menu printed to
        stderr/stdout, then `read choice < /dev/tty` to get the user's pick, resolving
        `tag` from the chosen entry — reusing wezterm-setup's exact `{ : < /dev/tty; } 2>/dev/null`
        open-probe technique (not `test -t 0`, not `[ -e /dev/tty ]`) but NOT its
        repo-tarball-fetch or asset-naming mechanics (this script keeps its own tar.gz
        asset/checksums construction unchanged, still built from `${GITHUB_ORIGIN}` download
        URLs once a tag is chosen). Add a `GITID_ASSUME_HEADLESS=1` test seam
        (mirroring wezterm-setup's WEZ_ASSUME_HEADLESS) that forces the headless branch
        deterministically in CI regardless of whether /dev/tty exists — the real detection
        stays the open-probe.
    Preserve the header-comment invariant "never reads from stdin directly" — the new
    `read choice < /dev/tty` explicitly redirects from /dev/tty, never bare stdin, so this
    still holds. Remain POSIX sh: no `[[`, no arrays, no `local` outside functions already
    using it in this file's existing style, no `pipefail`.

    Extend e2e/release_e2e_test.go: add a fixture server (or extend
    startGitHubRedirectFixtureServer) that also serves
    `/repos/${releaseRepoPath}/releases` returning a JSON array (a handful of stable +
    one nightly entry, prerelease flags set correctly) so tests can exercise (2) and (3)
    without hitting real GitHub. Add tests:
    - TestInstallScript_ChannelNightlyResolvesLatestNightly: GITID_CHANNEL=nightly resolves
      the nightly tag and downloads/verifies/installs its archive.
    - TestInstallScript_ChannelUnsetPreservesExistingHeadlessBehavior: run the script with
      GITID_CHANNEL unset against the existing /releases/latest redirect fixture and assert
      IDENTICAL behavior/output to the pre-existing TestInstallScript_InstallsVerifiedHostBinary
      path (regression guard for the "byte-for-byte unchanged" must_have).
    - TestInstallScript_UsableTTYShowsInteractiveMenu: use github.com/creack/pty (already a
      module dependency, used by the PTY e2e suite) to open a real pty, run install.sh with
      its stdin attached to the pty's slave end and GITID_ASSUME_HEADLESS unset/0, feed a
      numeric choice through the pty's master end, assert the numbered menu was printed and
      the chosen release was installed.
    - TestInstallScript_UnusableTTYFallsBackToHeadless: run with GITID_ASSUME_HEADLESS=1 (or,
      if feasible in this sandbox, a /dev/tty substitute that fails to open) and assert the
      script falls back to the existing headless resolution with no menu printed and no hang
      (bounded by a context timeout matching this file's existing ciTimeoutMultiplier pattern).
  </action>
  <verify>
    <automated>go test -tags e2e -race -run 'TestInstallScript_' ./e2e/... -v</automated>
    <fails_when>any TestInstallScript_* test fails, including the 4 new ones and every pre-existing one (regression)</fails_when>
    <automated>grep -n '/dev/tty' scripts/install.sh | grep -q '{ : < /dev/tty'</automated>
    <fails_when>the usable-tty probe is not the real open-attempt form (e.g. reverted to test -t 0)</fails_when>
  </verify>
  <done>scripts/install.sh supports GITID_CHANNEL=stable|nightly, resolves nightly via the GitHub releases API with no jq/gh/git dependency, gates a real numbered interactive menu behind an actual /dev/tty open-probe (not test -t 0), preserves the existing headless behavior byte-for-byte when unset, and every path — nightly resolution, interactive menu via a real pty, and headless fallback — is proven by a passing e2e test, not asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub releases API response (untrusted network data) -> install.sh's channel/menu resolution | The script parses tag_name/prerelease fields from a JSON response before any checksum verification occurs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07-01 | Tampering | GitHub releases API JSON parsing (grep/sed/awk, no jq) | medium | mitigate | The parsed tag_name is used ONLY to construct the same allowlisted asset/checksums filename shape install.sh already builds (gitid_<version>_<os>_<arch>.tar.gz) — checksum verification (existing, unchanged) still runs strictly before extraction regardless of channel/menu path. A malformed or hostile tag_name that fails to match the expected archive naming fails the existing "no entry for asset" refusal, not a new bypass. |
| T-10-07-02 | Elevation of Privilege | nightly.yml permissions | low | mitigate | contents: write only, no id-token/attestations — narrower than release.yml, job-scoped. |
| T-10-07-03 | Denial of Service | /dev/tty open-probe hangs | medium | mitigate | `{ : < /dev/tty; } 2>/dev/null` is a non-blocking open attempt (open() either succeeds or fails immediately with ENXIO/ENOENT — it does not block waiting for input), matching the proven wezterm-setup precedent; the interactive `read` that follows only executes once open succeeded, and every e2e test bounds execution with a context timeout. |
</threat_model>

<verification>
- `go test -count=1 ./cmd/gitid/...` passes including new goreleaser_config_test.go/nightly_yml_test.go assertions.
- `go test -tags e2e -race ./e2e/...` passes including the 4+ new install.sh tests and every pre-existing release/install e2e test (no regression).
- `make -n release` (token unset) and `make -n release-nightly` show the expected --skip=homebrew flag.
- `make test`, `make lint`, `make test-e2e` all green at wave close (run independently by the orchestrator, not trusted from this plan's own execution).
</verification>

<success_criteria>
D-18/D-19/D-20 are fully implemented and proven: a real tag release can succeed without the Homebrew tap existing (gated, not deleted, per D-18); goreleaser's native --nightly mode is wired through make release-nightly and a dedicated nightly.yml that can never collide with release.yml; scripts/install.sh gains GITID_CHANNEL=stable|nightly resolution and a real usable-/dev/tty-gated interactive menu adapted from wezterm-setup's proven technique, with the existing headless/curl|sh behavior fully preserved and every new path covered by a real e2e test (fixture server + real pty), not merely asserted.
</success_criteria>

<output>
Create `.planning/phases/10-linux-validation-release-pipeline/10-07-SUMMARY.md` when done
</output>
