---
phase: 10-linux-validation-release-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - .goreleaser.yaml
  - Makefile
  - .github/workflows/release.yml
  - .github/workflows/ci.yml
  - cmd/gitid/release_plumbing_test.go
  - cmd/gitid/release_yml_test.go
autonomous: true
requirements: [BUILD-03]

user_setup:
  - service: github (homebrew-tap repo + fine-grained PAT)
    why: "D-13's goreleaser brews: pipe pushes a Formula file to a SEPARATE castocolina/homebrew-tap repo on every real release — Claude cannot create a new GitHub repository or mint a personal access token on the user's account."
    env_vars:
      - name: HOMEBREW_TAP_GITHUB_TOKEN
        source: "A fine-grained PAT scoped to ONLY the castocolina/homebrew-tap repo (contents: write), added as a repository secret on castocolina/gitid at GitHub -> Settings -> Secrets and variables -> Actions"
    dashboard_config:
      - task: "Create the castocolina/homebrew-tap GitHub repository (can be empty/initialized with a README)"
        location: "https://github.com/new"
      - task: "Add the HOMEBREW_TAP_GITHUB_TOKEN repository secret to castocolina/gitid"
        location: "castocolina/gitid -> Settings -> Secrets and variables -> Actions -> New repository secret"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`make release-snapshot` (no secrets, no real tag needed) reproduces the exact D-07 artifact set locally: 4 platform tar.gz archives plus one checksums.txt, all correctly named, all internally checksum-consistent."
    - "The goreleaser build's ldflags reference the SAME Makefile-computed VERSION/COMMIT/DATE that `make build`/`make build-cross` use — no drift between two independent version descriptions (D-05)."
    - "A real v* tag push, once the tap repo/PAT exist, triggers release.yml — NOT ci.yml's old release job (retired in the same commit to avoid a double-publish race) — which re-verifies test+lint before publishing (D-06)."
  artifacts:
    - .goreleaser.yaml
    - .github/workflows/release.yml
  key_links:
    - ".goreleaser.yaml builds.ldflags {{.Env.VERSION}}/{{.Env.COMMIT}}/{{.Env.DATE}} -> the Makefile's release target exporting the SAME VERSION/COMMIT/DATE make variables build/build-cross already use"
    - "release.yml's re-run make test + make lint (BLOCKING) -> make release (only reached on a green gate) -> actions/attest-build-provenance (D-08)"
    - "release.yml permissions: contents:write/id-token:write/attestations:write are JOB-scoped only; ci.yml stays contents: read workflow-wide"
---

<objective>
Replace Phase 9.3's already-shipped raw-binary release pipeline with a goreleaser-driven one: `.goreleaser.yaml` defines the D-07 tar.gz archive format, D-08 provenance attestation, and D-13 Homebrew tap publish, all invoked through new `make release`/`make release-snapshot` targets (D-05) — never raw in CI YAML. A new, tag-triggered `.github/workflows/release.yml` (D-06) replaces `ci.yml`'s current `release:` job, which is retired in the same commit to avoid a double-publish race on the same tag push.

Purpose: BUILD-03 requires checksummed, provenance-attested release artifacts published on a version tag; D-05 through D-08 and D-13 specify exactly how, replacing a working but now-superseded raw-binary pipeline.
Output: `.goreleaser.yaml`, `make release`/`make release-snapshot`, `.github/workflows/release.yml`, `ci.yml` with its old release job removed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md
@.planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md
@Makefile
@.github/workflows/ci.yml
@cmd/gitid/release_plumbing_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: .goreleaser.yaml + make release / make release-snapshot, sharing the Makefile's version vars (D-05, D-07, D-08, D-13)</name>
  <files>.goreleaser.yaml, Makefile</files>
  <read_first>
    Makefile (VERSION/COMMIT/DATE/LDFLAGS block, setup-env's pinned-tool-install pattern, the build-cross/checksums targets)
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "Code Examples" (archives/checksum/brews stanzas) and "Pattern 1" (env-var-shared ldflags) and "Pitfall 2" and "Pitfall 4"
    internal/version/version.go (from Plan 10-01 — the exact -X target path this config's ldflags must match)
  </read_first>
  <action>
    Create .goreleaser.yaml at the repo root: `project_name: gitid`; `dist: bin` (redirects goreleaser's output into the SAME $(BIN_DIR) the Makefile already uses, rather than goreleaser's own default `dist/`); one `builds:` entry (id gitid, main ./cmd/gitid, binary gitid, env [CGO_ENABLED=0], goos [darwin, linux], goarch [amd64, arm64], flags [-trimpath], ldflags ["-s -w -X github.com/castocolina/gitid/internal/version.version={{.Env.VERSION}} -X github.com/castocolina/gitid/internal/version.commit={{.Env.COMMIT}} -X github.com/castocolina/gitid/internal/version.buildDate={{.Env.DATE}}"] — the SAME -X path Plan 10-01 wired the Makefile's own LDFLAGS to, referencing {{.Env.*}} rather than goreleaser's own internal git-describe so there is exactly ONE version computation, per D-05; D-12's explicit static-link flags "-s -w" plus CGO_ENABLED=0 are made EXPLICIT here rather than left accidental); `archives:` formats [tar.gz], files [LICENSE*, README*] (the default name_template `{{.ProjectName}}_{{.Version}}_{{.Os}}_{{.Arch}}` already matches D-07's literal `gitid_&lt;version&gt;_&lt;os&gt;_&lt;arch&gt;.tar.gz` — no override needed); `checksum:` algorithm sha256 (default name_template `{{.ProjectName}}_{{.Version}}_checksums.txt` already matches D-15's literal naming — no override needed); `git: { ignore_tags: ["poc-*", "backup/*"] }` as defense-in-depth (belt-and-suspenders alongside the {{.Env.*}} ldflags approach — cheap, closes Pitfall 2 even if a future config path ever reads goreleaser's own {{.Version}}); `release:` prerelease auto, plus a short curated header line naming the project (D-08's "curated header line" requirement — this repo merges fast-forward, so auto-generated PR-based notes carry little story; the header supplies it); `brews:` one entry — repository owner castocolina name homebrew-tap, token "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}", directory Formula, homepage/description naming gitid, install `bin.install "gitid"`, test `system "#{bin}/gitid", "--version"` — with a code comment noting `brews:` is soft-deprecated in favor of `homebrew_casks:` since goreleaser v2.10 but D-13 explicitly locks `brews:`, remains fully functional through v2.18.0, and is honored literally (Pitfall 4). Add to the Makefile: a pinned `GORELEASER := $(GOPATH_BIN)/goreleaser` binary path variable (mirrors GOLANGCI_LINT's own pattern); a `release` target that exports VERSION/COMMIT/DATE (the SAME make variables build/build-cross already read) into the goreleaser subprocess's environment and runs `$(GORELEASER) release --clean`; a `release-snapshot` target running `$(GORELEASER) release --snapshot --clean` (snapshot mode skips git-tag validation and ALL publish steps including release: and brews: — safe to run locally with zero secrets, the phase's own dry-run verification vehicle). Add both to the .PHONY line. In setup-env, add `go install github.com/goreleaser/goreleaser/v2@v2.18.0` to the tool-install block, mirroring the existing golangci-lint/gosec/freeze pattern (pinned version, never @latest — Package Legitimacy Audit Approved verdict). Do NOT touch the existing `checksums`/`build-cross` targets or their e2e coverage in this task — they stay a fully working, if now-redundant, local convenience until Plan 10-05 retires them together with the install.sh/e2e artifact-shape migration (keeping this task's own change set additive-only, with zero risk of leaving `make test-e2e` red between this plan and the next).
  </action>
  <verify>
    <automated>make release-snapshot &amp;&amp; ls bin/*.tar.gz bin/*_checksums.txt</automated>
    <fails_when>`make release-snapshot` exits non-zero, or no *.tar.gz / *_checksums.txt files appear under bin/ — either means the archives:/checksum:/dist: configuration is broken</fails_when>
    <automated>cd bin &amp;&amp; (sha256sum -c gitid_*_checksums.txt --ignore-missing || shasum -a 256 -c gitid_*_checksums.txt --ignore-missing) &amp;&amp; tar -tzf gitid_*_darwin_arm64.tar.gz | grep -c '^gitid$$\|/gitid$$'</automated>
    <fails_when>the checksum verification reports any FAILED line, or the darwin_arm64 archive's file listing does not contain a `gitid` entry</fails_when>
  </verify>
  <done>`make release-snapshot` (no secrets, no real tag) produces exactly 4 platform tar.gz archives named gitid_&lt;version&gt;_&lt;os&gt;_&lt;arch&gt;.tar.gz plus one gitid_&lt;version&gt;_checksums.txt under bin/, every archive's checksum verifies, and each archive contains a runnable gitid binary plus LICENSE/README.</done>
</task>

<task type="auto">
  <name>Task 2: release.yml (D-06, D-08) — retire ci.yml's release job in the same commit</name>
  <files>.github/workflows/release.yml, .github/workflows/ci.yml, cmd/gitid/release_plumbing_test.go, cmd/gitid/release_yml_test.go</files>
  <read_first>
    .github/workflows/ci.yml (the CURRENT release job block in full — it is being deleted)
    cmd/gitid/release_plumbing_test.go (the 9 tests scoped to ci.yml's release job block — TestWorkflowReleaseJobIsTagScoped through TestWorkflowPrereleaseIsDerivedFromTheTag — all become obsolete the instant that block is deleted)
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "Code Examples" (attest-build-provenance step) and "Open Questions" #1 (why the old job must be deleted, not left running alongside)
  </read_first>
  <action>
    Delete ci.yml's entire `release:` job (and its preceding comment block) in this SAME commit as adding release.yml — both would otherwise trigger on the identical `push: tags: v*` event and race/double-publish onto the same GitHub Release object (RESEARCH's Open Question 1). Keep ci.yml's own `on: push: { branches: [main], tags: ["v*"] }` trigger and top-level `permissions: contents: read` exactly as they are — `check`/`build-cross` still need to run on a tag push, independent of which workflow actually publishes. Create .github/workflows/release.yml: `name: Release`; `on: push: tags: ['v*']`; no top-level `permissions:` key; one job `release` on `ubuntu-latest` with `permissions: { contents: write, id-token: write, attestations: write }` (D-06, job-scoped only). Steps: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0 with `fetch-depth: 0` (D-10's guardrail — tags must be available for git describe); actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0 with go-version "1.26.x"; `make setup-env`; `make test`; `make lint` (D-06's BLOCKING re-run — a tag on an untested commit cannot publish; this workflow has no cross-workflow `needs:` on ci.yml's own jobs, since GitHub Actions cannot express that — these two steps ARE this workflow's own equivalent blocking gate); a "Compute release version metadata" step writing `version=${GITHUB_REF_NAME#v}`, `commit=$(git rev-parse --short HEAD)`, `date=$(date -u +%Y-%m-%d)` to `$GITHUB_OUTPUT` (relocating ci.yml's retired job's own pattern, unchanged); `make release VERSION=${{ steps.relver.outputs.version }} COMMIT=${{ steps.relver.outputs.commit }} DATE=${{ steps.relver.outputs.date }}` with `env: { GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}, HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }} }` (goreleaser's own release: pipe reads GITHUB_TOKEN, its brews: pipe reads the tap token — both internally, never echoed); actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2 with `subject-path: "bin/*.tar.gz"` (D-08). D-08's `--verify-tag` clause (a `gh release create` CLI flag verifying the pushed tag's annotated-tag signature/object against the checked-out commit before publishing): goreleaser's `release:` pipe has no equivalent flag (10-RESEARCH.md Open Question 3), so implement the SAME guarantee structurally instead of leaving it silently unaddressed — add a code comment directly above the `release:` job's `on:` trigger stating explicitly: "D-08 --verify-tag equivalence: this job only ever runs from a real `push: tags: v*` event against the exact commit GitHub resolved that tag to (no local/forged tag path exists in CI), and `actions/checkout@...fetch-depth: 0` (above) checks out that same resolved commit — together these make a separate tag-verification step redundant; `gh release create --verify-tag` exists to protect a LOCAL release flow against a stale/incorrectly-resolved local tag, a class of risk this server-side, trigger-bound job cannot experience." This comment is the plan's explicit, recorded resolution of D-08's `--verify-tag` clause — not a silent omission. Add a short code comment above the job's permissions: block cross-referencing D-06/D-08/D-13, matching this repo's existing documentation-heavy YAML style. In cmd/gitid/release_plumbing_test.go, DELETE the 9 tests scoped to ci.yml's now-gone release job block: TestWorkflowReleaseJobIsTagScoped, TestWorkflowReleaseJobHasScopedWritePermission, TestWorkflowReleaseJobWaitsForTheGates, TestWorkflowReleasePublishesExactlyTheFiveAssets, TestWorkflowReleaseStepsAreTheExpectedSequence, TestWorkflowReleaseBuildsThroughTheMakeTarget, TestWorkflowReleaseDeclaresItsStateAtPublication, TestWorkflowReleaseStateIsNotCorrectedAfterPublication, TestWorkflowPrereleaseIsDerivedFromTheTag — leave TestWorkflowPinsEveryActionToACommitSHA and TestWorkflowTopLevelPermissionsStayReadOnly untouched (both still apply to ci.yml as-is). Create cmd/gitid/release_yml_test.go (package main) with a `releaseWorkflowPath(t) string` helper returning ".github/workflows/release.yml" (parallel to release_plumbing_test.go's workflowPath), reusing jobBlock/readRepoFile from the same package, asserting the NEW equivalents: the release job is tag-scoped (`on: push: tags:` contains "v*"), its permissions block has exactly contents:write/id-token:write/attestations:write, its step sequence runs make setup-env then make test then make lint then make release then the attest-build-provenance step (in that order), every `uses:` line in the file is pinned to a 40-hex commit SHA with a trailing `# vX.Y.Z` comment (mirroring TestWorkflowPinsEveryActionToACommitSHA's own regex), the file contains no post-publish `gh release` amendment call, and the file contains the string "verify-tag" (asserting the D-08 `--verify-tag`-equivalence comment is present, not silently dropped).
  </action>
  <verify>
    <automated>go test -count=1 ./cmd/gitid/... -run 'TestWorkflow|TestRelease' -v &amp;&amp; grep -qi "verify-tag" .github/workflows/release.yml</automated>
    <fails_when>any TestRelease* (release_yml_test.go) or remaining TestWorkflow* assertion fails, `grep -c "^  release:$" .github/workflows/ci.yml` is non-zero (the old job was not fully removed), or the verify-tag grep exits non-zero (D-08's --verify-tag resolution comment is missing from release.yml)</fails_when>
  </verify>
  <done>ci.yml no longer contains a release job; release.yml exists, is tag-scoped, carries job-scoped permissions only, re-runs test+lint before `make release`, attests provenance over bin/*.tar.gz, documents D-08's `--verify-tag` structural-equivalence resolution in a code comment, and every action in it is SHA-pinned; the 9 obsolete tests are gone and their release.yml-scoped replacements pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Tag push (developer-controlled) -> release.yml -> public GitHub Release + castocolina/homebrew-tap push | A `v*` tag push is the human release gate; from there, an automated job runs with elevated, secret-bearing permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04-01 | Tampering / Elevation of Privilege | HOMEBREW_TAP_GITHUB_TOKEN PAT | high | mitigate | PAT is fine-grained, scoped to ONLY the castocolina/homebrew-tap repo (user_setup), stored exclusively as a GitHub Actions repository secret, referenced only via `{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}` inside .goreleaser.yaml — never echoed or logged. |
| T-10-04-02 | Tampering (supply chain) | goreleaser / actions/attest-build-provenance | high | mitigate | goreleaser is pinned to an exact, `git ls-remote`-verified version (v2.18.0) via `go install ...@v2.18.0`, never `@latest`; attest-build-provenance is SHA-pinned (`4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2`), matching this repo's existing SHA-pin discipline. |
| T-10-04-03 | Tampering / Denial of Service (downstream) | A tag pushed on a red/untested commit | high | mitigate | release.yml re-runs `make test` + `make lint` as a BLOCKING step before `make release` ever executes — a red gate fails the job before any artifact is built or published. |
| T-10-04-04 | Elevation of Privilege | Workflow permissions scope | medium | mitigate | `contents: write`/`id-token: write`/`attestations: write` are declared at the JOB level only (release.yml's single job), never workflow-wide; ci.yml's own workflow-wide permissions stay `contents: read`. |
| T-10-04-SC | Tampering | npm/pip/cargo installs | n/a | n/a | Not applicable — this phase adds no npm/pip/cargo dependency; goreleaser is a Go dev-tool binary, already covered by 10-RESEARCH.md's Package Legitimacy Audit (Approved, first-party, verified via git ls-remote). |
</threat_model>

<verification>
- `make release-snapshot` produces 4 correctly-named tar.gz archives + 1 checksums.txt under bin/, checksums verify.
- `go test -count=1 ./cmd/gitid/...` passes (old release-job tests removed, new release.yml tests added and green).
- `git diff .github/workflows/ci.yml` shows only the release job's removal.
- `.github/workflows/release.yml` exists, every `uses:` line SHA-pinned.
</verification>

<success_criteria>
D-05 through D-08 and D-13 are fully implemented: a single, make-wrapped goreleaser configuration produces the D-07 artifact shape, is locally dry-runnable with zero secrets via `make release-snapshot`, and a real tag push (once the tap repo/PAT exist per user_setup) will re-verify green gates before publishing checksummed, provenance-attested archives plus a Homebrew tap update — replacing, not duplicating, Phase 9.3's retired raw-binary pipeline. D-08's `--verify-tag` clause has no goreleaser equivalent (10-RESEARCH.md Open Question 3); it is resolved by an explicit, tested code comment in release.yml documenting why the trigger-bound, server-side job structurally satisfies the same guarantee (see Task 2) rather than by a literal CLI flag.
</success_criteria>

<output>
Create `.planning/phases/10-linux-validation-release-pipeline/10-04-SUMMARY.md` when done
</output>
