---
phase: 10-linux-validation-release-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - .goreleaser.yaml
  - Makefile
  - .gitignore
  - .github/workflows/release.yml
  - .github/workflows/ci.yml
  - cmd/gitid/release_plumbing_test.go
  - cmd/gitid/release_yml_test.go
  - cmd/gitid/goreleaser_config_test.go
autonomous: true
requirements: [BUILD-03]

user_setup:
  - service: github (homebrew-tap repo + fine-grained PAT)
    why: "D-13's goreleaser brews: pipe pushes a Formula file to a SEPARATE castocolina/homebrew-tap repo on every real release — Claude cannot create a new GitHub repository or mint a personal access token on the user's account."
    env_vars:
      - name: HOMEBREW_TAP_GITHUB_TOKEN
        source: "A fine-grained PAT scoped to ONLY the castocolina/homebrew-tap repo (contents: write), added as a repository secret on castocolina/gitid at GitHub -> Settings -> Secrets and variables -> Actions"
    dashboard_config:
      - task: "Create the castocolina/homebrew-tap GitHub repository (can be empty/initialized with a README)"
        location: "https://github.com/new"
      - task: "Add the HOMEBREW_TAP_GITHUB_TOKEN repository secret to castocolina/gitid"
        location: "castocolina/gitid -> Settings -> Secrets and variables -> Actions -> New repository secret"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`make release-snapshot` (no secrets, no real tag needed) reproduces the exact D-07 artifact set locally: 4 platform tar.gz archives plus one checksums.txt, all correctly named, all internally checksum-consistent, written to dist/ — never bin/, so it never collides with or deletes the Makefile's own build/build-cross output (REVIEW C-4)."
    - "The goreleaser build's ldflags reference the SAME Makefile-computed VERSION/COMMIT/DATE that `make build`/`make build-cross` use — no drift between two independent version descriptions (D-05)."
    - "A real v* tag push, once the tap repo/PAT exist, triggers release.yml — NOT ci.yml's old release job (retired in the same commit to avoid a double-publish race) — which re-verifies test+lint before publishing (D-06)."
    - "A prerelease tag (this repo's ENTIRE tag history to date) is never marked GitHub's 'latest' release — goreleaser's `make_latest: \"{{ not .Prerelease }}\"` restores the exact guarantee the retired ci.yml case-statement provided (REVIEW C-3)."
  artifacts:
    - .goreleaser.yaml
    - .github/workflows/release.yml
    - cmd/gitid/goreleaser_config_test.go
  key_links:
    - ".goreleaser.yaml builds.ldflags {{.Env.VERSION}}/{{.Env.COMMIT}}/{{.Env.DATE}} -> the Makefile's release target exporting the SAME VERSION/COMMIT/DATE make variables build/build-cross already use"
    - "release.yml's re-run make test + make lint (BLOCKING, via the narrower make setup-env-release bootstrap, REVIEW C-7) -> make release (only reached on a green gate) -> actions/attest-build-provenance over BOTH the archives and the checksums manifest (D-08, REVIEW C-8)"
    - "release.yml permissions: contents:write/id-token:write/attestations:write are JOB-scoped only; ci.yml stays contents: read workflow-wide"
    - ".goreleaser.yaml's dist: dist -> a dedicated directory, gitignored, distinct from the Makefile's own bin/ output (REVIEW C-4) — goreleaser's --clean never touches bin/"
---

<!-- REVIEW NOTE (planner, replan pass): C-1 (version v-prefix leak, fixed in Plan
10-01's Makefile) and C-4 (dist:/bin/ collision, fixed above) were both checked
against this plan's threat_model. C-1 changes nothing here — release.yml already
computed `version=${GITHUB_REF_NAME#v}` correctly and this plan's own version
consumption is unchanged. C-4's dist relocation moves WHERE artifacts land, not
WHAT trust boundary they cross — no threat_model edit was needed for either. C-3
(make_latest) DOES change the trust surface (a dropped Spoofing/Tampering
mitigation is restored) and gets a new STRIDE row below (T-10-04-05). -->


<objective>
Replace Phase 9.3's already-shipped raw-binary release pipeline with a goreleaser-driven one: `.goreleaser.yaml` defines the D-07 tar.gz archive format, D-08 provenance attestation, and D-13 Homebrew tap publish, all invoked through new `make release`/`make release-snapshot` targets (D-05) — never raw in CI YAML. A new, tag-triggered `.github/workflows/release.yml` (D-06) replaces `ci.yml`'s current `release:` job, which is retired in the same commit to avoid a double-publish race on the same tag push.

Purpose: BUILD-03 requires checksummed, provenance-attested release artifacts published on a version tag; D-05 through D-08 and D-13 specify exactly how, replacing a working but now-superseded raw-binary pipeline.
Output: `.goreleaser.yaml`, `make release`/`make release-snapshot`, `.github/workflows/release.yml`, `ci.yml` with its old release job removed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md
@.planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md
@Makefile
@.github/workflows/ci.yml
@cmd/gitid/release_plumbing_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: .goreleaser.yaml + make release / make release-snapshot, sharing the Makefile's version vars (D-05, D-07, D-08, D-13)</name>
  <files>.goreleaser.yaml, Makefile, .gitignore, cmd/gitid/goreleaser_config_test.go</files>
  <read_first>
    Makefile (VERSION/COMMIT/DATE/LDFLAGS block, setup-env's pinned-tool-install pattern, the build-cross/checksums targets)
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "Code Examples" (archives/checksum/brews stanzas) and "Pattern 1" (env-var-shared ldflags) and "Pitfall 2" and "Pitfall 4"
    internal/version/version.go (from Plan 10-01 — the exact -X target path this config's ldflags must match)
  </read_first>
  <action>
    Create .goreleaser.yaml at the repo root: `project_name: gitid`; `dist: dist` (REVIEW C-4 FIX: goreleaser's OWN default directory name, kept explicit here for documentation rather than overridden to `bin` — `$(BIN_DIR)` is NOT goreleaser-private: `Makefile:68-69` writes `bin/gitid` and `build-cross` writes `bin/gitid-<os>-<arch>` into that SAME directory, and both `make release`/`make release-snapshot` invoke goreleaser with `--clean`, which deletes its `dist:` directory before every build — pointing `dist:` at `bin` would silently delete the Makefile's own build/build-cross output the moment a release build runs in the same session, a real flaky-test hazard once Plan 10-05's e2e helpers call `make release-snapshot` alongside other `make build`/`make build-cross` invocations. Add `/dist/` to .gitignore in this task — D-10's own guardrail already requires `bin/` to be gitignored for the same untracked-build-output-flips-vcs.modified reason, and `dist/` needs the identical treatment); one `builds:` entry (id gitid, main ./cmd/gitid, binary gitid, env [CGO_ENABLED=0], goos [darwin, linux], goarch [amd64, arm64], flags [-trimpath], ldflags ["-s -w -X github.com/castocolina/gitid/internal/version.version={{.Env.VERSION}} -X github.com/castocolina/gitid/internal/version.commit={{.Env.COMMIT}} -X github.com/castocolina/gitid/internal/version.buildDate={{.Env.DATE}}"] — the SAME -X path Plan 10-01 wired the Makefile's own LDFLAGS to, referencing {{.Env.*}} rather than goreleaser's own internal git-describe so there is exactly ONE version computation, per D-05; D-12's explicit static-link flags "-s -w" plus CGO_ENABLED=0 are made EXPLICIT here rather than left accidental); `archives:` formats [tar.gz], files [LICENSE*, README*] (the default name_template `{{.ProjectName}}_{{.Version}}_{{.Os}}_{{.Arch}}` already matches D-07's literal `gitid_&lt;version&gt;_&lt;os&gt;_&lt;arch&gt;.tar.gz` — no override needed); `checksum:` algorithm sha256 (default name_template `{{.ProjectName}}_{{.Version}}_checksums.txt` already matches D-15's literal naming — no override needed); `git: { ignore_tags: ["poc-*", "backup/*"] }` as defense-in-depth (belt-and-suspenders alongside the {{.Env.*}} ldflags approach — cheap, closes Pitfall 2 even if a future config path ever reads goreleaser's own {{.Version}}); `release:` prerelease auto, `make_latest: "{{ not .Prerelease }}"` (REVIEW C-3 FIX: goreleaser's `release.make_latest` key — default `true`, template-allowed — restores the EXACT guarantee the retired `ci.yml` release job's hand-rolled `case`/`make_latest` output pair provided ["a prerelease briefly presented as the repository's latest stable download is exactly the failure a checksum-verifying installer cannot protect anyone from"]. goreleaser's own `.Prerelease` template field is the non-empty prerelease-suffix STRING off the parsed semver tag (e.g. "rc.9" for `v0.1.0-rc.9`, empty for a stable `v1.0.0`), so `{{ not .Prerelease }}` evaluates `false` for a prerelease tag and `true` for a stable one — the SAME tag-suffix signal `prerelease: auto` already reads, so the two settings can never disagree by construction. This repo's ENTIRE tag history to date is 9 consecutive `-rc.N` prereleases and zero stable releases, so this is the NORMAL path here, not an edge case), plus a short curated header line naming the project (D-08's "curated header line" requirement — this repo merges fast-forward, so auto-generated PR-based notes carry little story; the header supplies it); `brews:` one entry — repository owner castocolina name homebrew-tap, token "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}", directory Formula, homepage/description naming gitid, install `bin.install "gitid"`, test `system "#{bin}/gitid", "--version"` — with a code comment noting `brews:` is soft-deprecated in favor of `homebrew_casks:` since goreleaser v2.10 but D-13 explicitly locks `brews:`, remains fully functional through v2.18.0, and is honored literally (Pitfall 4). Add to the Makefile: a pinned `GORELEASER := $(GOPATH_BIN)/goreleaser` binary path variable (mirrors GOLANGCI_LINT's own pattern); a `release` target that exports VERSION/COMMIT/DATE (the SAME make variables build/build-cross already read) into the goreleaser subprocess's environment and runs `$(GORELEASER) release --clean`; a `release-snapshot` target running `$(GORELEASER) release --snapshot --clean` (snapshot mode skips git-tag validation and ALL publish steps including release: and brews: — safe to run locally with zero secrets, the phase's own dry-run verification vehicle). Add both to the .PHONY line. Add a `GORELEASER_VERSION := v2.18.0` Makefile variable (mirrors `GOLANGCI_LINT_VERSION`'s own pattern). In setup-env, add `go install github.com/goreleaser/goreleaser/v2@$(GORELEASER_VERSION)` to the tool-install block, mirroring the existing golangci-lint/gosec/freeze pattern (pinned version, never @latest — Package Legitimacy Audit Approved verdict). REVIEW C-7 FIX: also add a new, narrower `setup-env-release` target (and its `.PHONY` entry) that installs ONLY golangci-lint, gosec, and goreleaser — the three tools `make test`/`make lint`/`make release` actually need — explicitly SKIPPING goimports, pre-commit/install-hooks, freeze, and the pinned-Chromium provisioning step `setup-env`'s full bootstrap performs; none of those are needed by `make test`/`make lint`/`make release` (golangci-lint's own `--build-tags screenshot` run is static analysis only, never test execution, and `make test`'s own screenshot-tagged line already `-skip`s the Chromium-dependent tests), and downloading Chromium on the one workflow where a failure means a pushed tag doesn't publish adds avoidable minutes and a network-flake failure mode for zero benefit — Plan 10-04 Task 2's release.yml calls `make setup-env-release`, not the full `make setup-env`. Do NOT touch the existing `checksums`/`build-cross` targets or their e2e coverage in this task — they stay a fully working, if now-redundant, local convenience until Plan 10-05 retires them together with the install.sh/e2e artifact-shape migration (keeping this task's own change set additive-only, with zero risk of leaving `make test-e2e` red between this plan and the next). Create cmd/gitid/goreleaser_config_test.go (package main) reusing readRepoFile from cmd/gitid/release_plumbing_test.go (same package) with tests asserting: .goreleaser.yaml contains the literal `dist: dist` (never `dist: bin`, REVIEW C-4); it contains `prerelease: auto`; it contains the literal `make_latest: "{{ not .Prerelease }}"` (REVIEW C-3); and it contains the ldflags -X path `github.com/castocolina/gitid/internal/version.` (proving the SAME internal/version symbol path Plan 10-01 wired the Makefile to, not a drifted duplicate).
  </action>
  <verify>
    <automated>mkdir -p bin &amp;&amp; touch bin/.pre-existing-marker &amp;&amp; make release-snapshot &amp;&amp; ls dist/*.tar.gz dist/*_checksums.txt &amp;&amp; test -f bin/.pre-existing-marker &amp;&amp; rm -f bin/.pre-existing-marker</automated>
    <fails_when>`make release-snapshot` exits non-zero, no *.tar.gz / *_checksums.txt files appear under dist/ (either means the archives:/checksum:/dist: configuration is broken), or bin/.pre-existing-marker was deleted — REVIEW C-4: a deleted marker means goreleaser's `--clean` reached into `bin/` and the dist-directory collision was not actually fixed</fails_when>
    <automated>cd dist &amp;&amp; (sha256sum -c gitid_*_checksums.txt --ignore-missing || shasum -a 256 -c gitid_*_checksums.txt --ignore-missing) &amp;&amp; tar -tzf gitid_*_darwin_arm64.tar.gz | grep -c '^gitid$$\|/gitid$$'</automated>
    <fails_when>the checksum verification reports any FAILED line, or the darwin_arm64 archive's file listing does not contain a `gitid` entry</fails_when>
    <automated>go test -count=1 ./cmd/gitid/... -run 'TestGoreleaserConfig' -v</automated>
    <fails_when>any TestGoreleaserConfig* assertion fails (missing `dist: dist`, missing `prerelease: auto`, missing the literal `make_latest: "{{ not .Prerelease }}"` line, or a drifted ldflags -X path)</fails_when>
  </verify>
  <done>`make release-snapshot` (no secrets, no real tag) produces exactly 4 platform tar.gz archives named gitid_&lt;version&gt;_&lt;os&gt;_&lt;arch&gt;.tar.gz plus one gitid_&lt;version&gt;_checksums.txt under dist/ — never bin/ (REVIEW C-4: a pre-existing bin/ marker file survives the run) — every archive's checksum verifies, each archive contains a runnable gitid binary plus LICENSE/README, and cmd/gitid/goreleaser_config_test.go confirms the dist/prerelease/make_latest/ldflags contract.</done>
</task>

<task type="auto">
  <name>Task 2: release.yml (D-06, D-08) — retire ci.yml's release job in the same commit</name>
  <files>.github/workflows/release.yml, .github/workflows/ci.yml, cmd/gitid/release_plumbing_test.go, cmd/gitid/release_yml_test.go</files>
  <read_first>
    .github/workflows/ci.yml (the CURRENT release job block in full — it is being deleted)
    cmd/gitid/release_plumbing_test.go (the 9 tests scoped to ci.yml's release job block — TestWorkflowReleaseJobIsTagScoped through TestWorkflowPrereleaseIsDerivedFromTheTag — all become obsolete the instant that block is deleted)
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "Code Examples" (attest-build-provenance step) and "Open Questions" #1 (why the old job must be deleted, not left running alongside)
  </read_first>
  <action>
    Delete ci.yml's entire `release:` job (and its preceding comment block) in this SAME commit as adding release.yml — both would otherwise trigger on the identical `push: tags: v*` event and race/double-publish onto the same GitHub Release object (RESEARCH's Open Question 1). Keep ci.yml's own `on: push: { branches: [main], tags: ["v*"] }` trigger and top-level `permissions: contents: read` exactly as they are — `check`/`build-cross` still need to run on a tag push, independent of which workflow actually publishes. Create .github/workflows/release.yml: `name: Release`; `on: push: tags: ['v*']`; no top-level `permissions:` key; one job `release` on `ubuntu-latest` with `permissions: { contents: write, id-token: write, attestations: write }` (D-06, job-scoped only). Steps: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0 with `fetch-depth: 0` (D-10's guardrail — tags must be available for git describe); actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0 with go-version "1.26.x"; `make setup-env-release` (REVIEW C-7 FIX: NOT the full `make setup-env` — Task 1 added this narrower target specifically so this workflow never provisions Chromium/goimports/pre-commit hooks it does not need, avoiding the extra minutes and network-flake surface on the one workflow where a failure means a pushed tag doesn't publish); `make test`; `make lint` (D-06's BLOCKING re-run — a tag on an untested commit cannot publish; this workflow has no cross-workflow `needs:` on ci.yml's own jobs, since GitHub Actions cannot express that — these two steps ARE this workflow's own equivalent blocking gate); a "Compute release version metadata" step writing `version=${GITHUB_REF_NAME#v}`, `commit=$(git rev-parse --short HEAD)`, `date=$(date -u +%Y-%m-%d)` to `$GITHUB_OUTPUT` (relocating ci.yml's retired job's own pattern, unchanged — REVIEW C-1 note: this already strips the tag's leading `v`, so it was already consistent with Plan 10-01's Makefile fix; no change needed here); `make release VERSION=${{ steps.relver.outputs.version }} COMMIT=${{ steps.relver.outputs.commit }} DATE=${{ steps.relver.outputs.date }}` with `env: { GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}, HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }} }` (goreleaser's own release: pipe reads GITHUB_TOKEN, its brews: pipe reads the tap token — both internally, never echoed); actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2 with `subject-path` covering BOTH the archives AND the checksums manifest (REVIEW C-8 FIX: the originally-proposed `subject-path: "dist/*.tar.gz"` attests the four archives but not `gitid_<version>_checksums.txt` — the exact file the installer, D-14/D-15, actually trusts to validate an archive's integrity; attesting only the archives leaves the manifest that VALIDATES them outside the provenance chain, an odd trust boundary the action's own multi-pattern `subject-path` support removes for free — use a newline-delimited value: `subject-path: |` followed by `dist/*.tar.gz` and `dist/*_checksums.txt` on their own indented lines) (D-08, D-15). D-08's `--verify-tag` clause (a `gh release create` CLI flag verifying the pushed tag's annotated-tag signature/object against the checked-out commit before publishing): goreleaser's `release:` pipe has no equivalent flag (10-RESEARCH.md Open Question 3), so implement the SAME guarantee structurally instead of leaving it silently unaddressed — add a code comment directly above the `release:` job's `on:` trigger stating explicitly: "D-08 --verify-tag equivalence: this job only ever runs from a real `push: tags: v*` event against the exact commit GitHub resolved that tag to (no local/forged tag path exists in CI), and `actions/checkout@...fetch-depth: 0` (above) checks out that same resolved commit — together these make a separate tag-verification step redundant; `gh release create --verify-tag` exists to protect a LOCAL release flow against a stale/incorrectly-resolved local tag, a class of risk this server-side, trigger-bound job cannot experience." This comment is the plan's explicit, recorded resolution of D-08's `--verify-tag` clause — not a silent omission. Add a short code comment above the job's permissions: block cross-referencing D-06/D-08/D-13, matching this repo's existing documentation-heavy YAML style. In cmd/gitid/release_plumbing_test.go, DELETE the 9 tests scoped to ci.yml's now-gone release job block: TestWorkflowReleaseJobIsTagScoped, TestWorkflowReleaseJobHasScopedWritePermission, TestWorkflowReleaseJobWaitsForTheGates, TestWorkflowReleasePublishesExactlyTheFiveAssets, TestWorkflowReleaseStepsAreTheExpectedSequence, TestWorkflowReleaseBuildsThroughTheMakeTarget, TestWorkflowReleaseDeclaresItsStateAtPublication, TestWorkflowReleaseStateIsNotCorrectedAfterPublication, TestWorkflowPrereleaseIsDerivedFromTheTag — leave TestWorkflowPinsEveryActionToACommitSHA and TestWorkflowTopLevelPermissionsStayReadOnly untouched (both still apply to ci.yml as-is). Create cmd/gitid/release_yml_test.go (package main) with a `releaseWorkflowPath(t) string` helper returning ".github/workflows/release.yml" (parallel to release_plumbing_test.go's workflowPath), reusing jobBlock/readRepoFile from the same package, asserting the NEW equivalents: the release job is tag-scoped (`on: push: tags:` contains "v*"), its permissions block has exactly contents:write/id-token:write/attestations:write, its step sequence runs make setup-env-release then make test then make lint then make release then the attest-build-provenance step (in that order — REVIEW C-7: asserting `setup-env-release`, NOT `setup-env`, is itself the regression guard that the narrower target is actually wired in), every `uses:` line in the file is pinned to a 40-hex commit SHA with a trailing `# vX.Y.Z` comment (mirroring TestWorkflowPinsEveryActionToACommitSHA's own regex), the file contains no post-publish `gh release` amendment call, the attest-build-provenance step's `subject-path` value contains BOTH `dist/*.tar.gz` and `dist/*_checksums.txt` (REVIEW C-8), and the file contains the string "verify-tag" (asserting the D-08 `--verify-tag`-equivalence comment is present, not silently dropped).
  </action>
  <verify>
    <automated>go test -count=1 ./cmd/gitid/... -run 'TestWorkflow|TestRelease' -v &amp;&amp; grep -qi "verify-tag" .github/workflows/release.yml</automated>
    <fails_when>any TestRelease* (release_yml_test.go) or remaining TestWorkflow* assertion fails, `grep -c "^  release:$" .github/workflows/ci.yml` is non-zero (the old job was not fully removed), or the verify-tag grep exits non-zero (D-08's --verify-tag resolution comment is missing from release.yml)</fails_when>
  </verify>
  <done>ci.yml no longer contains a release job; release.yml exists, is tag-scoped, carries job-scoped permissions only, bootstraps via the narrower `make setup-env-release` (REVIEW C-7), re-runs test+lint before `make release`, attests provenance over BOTH dist/*.tar.gz and dist/*_checksums.txt (REVIEW C-8), documents D-08's `--verify-tag` structural-equivalence resolution in a code comment, and every action in it is SHA-pinned; the 9 obsolete tests are gone and their release.yml-scoped replacements pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Tag push (developer-controlled) -> release.yml -> public GitHub Release + castocolina/homebrew-tap push | A `v*` tag push is the human release gate; from there, an automated job runs with elevated, secret-bearing permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04-01 | Tampering / Elevation of Privilege | HOMEBREW_TAP_GITHUB_TOKEN PAT | high | mitigate | PAT is fine-grained, scoped to ONLY the castocolina/homebrew-tap repo (user_setup), stored exclusively as a GitHub Actions repository secret, referenced only via `{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}` inside .goreleaser.yaml — never echoed or logged. |
| T-10-04-02 | Tampering (supply chain) | goreleaser / actions/attest-build-provenance | high | mitigate | goreleaser is pinned to an exact, `git ls-remote`-verified version (v2.18.0) via `go install ...@v2.18.0`, never `@latest`; attest-build-provenance is SHA-pinned (`4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2`), matching this repo's existing SHA-pin discipline. |
| T-10-04-03 | Tampering / Denial of Service (downstream) | A tag pushed on a red/untested commit | high | mitigate | release.yml re-runs `make test` + `make lint` as a BLOCKING step before `make release` ever executes — a red gate fails the job before any artifact is built or published. |
| T-10-04-04 | Elevation of Privilege | Workflow permissions scope | medium | mitigate | `contents: write`/`id-token: write`/`attestations: write` are declared at the JOB level only (release.yml's single job), never workflow-wide; ci.yml's own workflow-wide permissions stay `contents: read`. |
| T-10-04-05 | Spoofing | `.goreleaser.yaml` `release.make_latest` | medium | mitigate | REVIEW C-3: without `make_latest: "{{ not .Prerelease }}"`, goreleaser's own default (`make_latest: true`) would mark EVERY release "latest" including a prerelease — a prerelease briefly presented as the repository's latest stable download is exactly the failure a checksum-verifying installer cannot protect anyone from. The template ties `make_latest` to the SAME tag-suffix signal `prerelease: auto` already reads, so the two can never disagree; `cmd/gitid/goreleaser_config_test.go` (Task 1) asserts the literal template string is present. |
| T-10-04-SC | Tampering | npm/pip/cargo installs | n/a | n/a | Not applicable — this phase adds no npm/pip/cargo dependency; goreleaser is a Go dev-tool binary, already covered by 10-RESEARCH.md's Package Legitimacy Audit (Approved, first-party, verified via git ls-remote). |
</threat_model>

<verification>
- `make release-snapshot` produces 4 correctly-named tar.gz archives + 1 checksums.txt under dist/ (never bin/, REVIEW C-4), checksums verify.
- `go test -count=1 ./cmd/gitid/...` passes (old release-job tests removed, new release.yml/goreleaser-config tests added and green).
- `git diff .github/workflows/ci.yml` shows only the release job's removal.
- `.github/workflows/release.yml` exists, every `uses:` line SHA-pinned, bootstraps via `make setup-env-release` (REVIEW C-7), and attests both the archives and the checksums manifest (REVIEW C-8).
- A pre-existing `bin/` marker file survives a `make release-snapshot` run (REVIEW C-4 regression guard).
</verification>

<success_criteria>
D-05 through D-08 and D-13 are fully implemented: a single, make-wrapped goreleaser configuration produces the D-07 artifact shape into a dedicated, gitignored `dist/` directory that never collides with the Makefile's own `bin/` output (REVIEW C-4), is locally dry-runnable with zero secrets via `make release-snapshot`, and a real tag push (once the tap repo/PAT exist per user_setup) will re-verify green gates via a scoped `make setup-env-release` bootstrap (REVIEW C-7) before publishing checksummed, provenance-attested archives (REVIEW C-8 widens attestation to the checksums manifest too) plus a Homebrew tap update — replacing, not duplicating, Phase 9.3's retired raw-binary pipeline. A prerelease tag is never marked GitHub's "latest" release, restoring the retired ci.yml job's `make_latest` guarantee via goreleaser's own templated `make_latest` key (REVIEW C-3). D-08's `--verify-tag` clause has no goreleaser equivalent (10-RESEARCH.md Open Question 3); it is resolved by an explicit, tested code comment in release.yml documenting why the trigger-bound, server-side job structurally satisfies the same guarantee (see Task 2) rather than by a literal CLI flag.
</success_criteria>

<output>
Create `.planning/phases/10-linux-validation-release-pipeline/10-04-SUMMARY.md` when done
</output>
