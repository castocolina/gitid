---
phase: 10-linux-validation-release-pipeline
plan: 05
type: tdd
wave: 3
depends_on: ["10-04"]
files_modified:
  - e2e/release_e2e_test.go
  - scripts/install.sh
  - Makefile
autonomous: true
requirements: [BUILD-03]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/install.sh downloads and verifies the D-07 tar.gz archive (never the old raw-binary shape) BEFORE extracting, then installs only the known gitid binary out of the extracted contents."
    - "A GITID_VERSION pin and a custom install-dir override both work — neither existed in the Phase 9.3 script."
    - "Every install.sh behavior is proven against real, locally-built goreleaser snapshot archives — not hand-rolled fixtures that could silently diverge from the real artifact shape."
  artifacts:
    - scripts/install.sh (rewritten)
    - e2e/release_e2e_test.go (rewritten)
  key_links:
    - "scripts/install.sh's asset-name construction -> goreleaser's default archive name_template (gitid_<version>_<os>_<arch>.tar.gz), proven by testing against `make release-snapshot` output, not a guessed string"
    - "e2e/release_e2e_test.go's fixture server -> the SAME checksums.txt format goreleaser's checksum: pipe produces"
---

<objective>
Migrate `scripts/install.sh` and its entire e2e test suite from Phase 9.3's raw-binary artifact shape to Plan 10-04's goreleaser-produced tar.gz archives — the single highest-risk item this phase's own research flagged: a breaking change to already-shipped, working code. Write the new contract into the tests FIRST (RED against the current script), then rewrite the script to satisfy it (GREEN), in one coordinated commit-set so the artifact-shape migration never lands half-done.

Purpose: D-14/D-15 require install.sh to work against the D-07 archive format with verify-before-extract, a version pin, and a custom install dir; D-07 itself already broke the OLD script's raw-binary assumption in Plan 10-04.
Output: a rewritten scripts/install.sh and a rewritten e2e/release_e2e_test.go, both proven against real `make release-snapshot` archives.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md
@.planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md
@scripts/install.sh
@e2e/release_e2e_test.go
@Makefile
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): rewrite e2e/release_e2e_test.go for the D-07 tar.gz contract</name>
  <files>e2e/release_e2e_test.go</files>
  <read_first>
    e2e/release_e2e_test.go (the FULL file — stampedArtifacts/hostAsset/fixtureServer/runInstallScript helpers, and all 17 existing TestRelease_*/TestInstallScript_* functions)
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "Pattern 4" (the exact required changes to install.sh) and "Pitfall 1"
    .goreleaser.yaml (from Plan 10-04 — the exact archive/checksum naming this test suite must assert against)
  </read_first>
  <behavior>
    - stampedArtifacts() (or its replacement) calls `make release-snapshot` (not the retired `make checksums`) with VERSION/COMMIT/DATE overrides, and resolves the produced archive/checksums filenames from goreleaser's actual naming (gitid_&lt;version&gt;_&lt;os&gt;_&lt;arch&gt;.tar.gz, gitid_&lt;version&gt;_checksums.txt) rather than a hardcoded raw-binary name.
    - TestRelease_SnapshotBuildProducesArchivesForEveryTarget (was TestRelease_BuildCrossStampsEveryTarget): asserts all 4 platform archives + checksums.txt exist and are non-empty; extracts the HOST's own archive to a temp dir and runs the extracted binary's --version, asserting the D-11 stamp.
    - TestRelease_UnstampedBuildKeepsDevDefaults: unchanged from Plan 10-01's already-updated regex-based assertion (this test exercises `make build`, not the release archive — no further change needed here beyond confirming it still compiles/passes in this file's new shape).
    - TestRelease_ChecksumsManifestMatchesArchives (was ...MatchesBinaries): reads the checksums.txt goreleaser produced, verifies each line's hash against its corresponding .tar.gz file (not a raw binary).
    - Every TestInstallScript_* function (13 total) is rewritten for the tar.gz flow: the fixture server serves goreleaser-snapshot-built archives + checksums.txt (not renamed raw binaries); "installed" means the EXTRACTED gitid binary landed at the install dir, not the raw downloaded file; checksum verification happens against the DOWNLOADED ARCHIVE before any `tar -xzf` runs (never after).
    - Two NET-NEW cases: TestInstallScript_GITIDVersionPinsToASpecificVersion (setting GITID_VERSION to a fixed tag-shaped string makes the script fetch that exact version's asset instead of resolving "latest" — the fixture server can be pointed with GITID_INSTALL_BASE_URL exactly as today, bypassing latest-resolution entirely, since the override IS the resolved origin) and TestInstallScript_CustomInstallDirOverride (an env var override relocates the install target away from the hardcoded ~/.local/bin default).
  </behavior>
  <action>
    Rewrite e2e/release_e2e_test.go's fixture/build machinery and all 17 existing test functions per the &lt;behavior&gt; block above, plus the 2 net-new cases. Keep the file's existing structural conventions (t.Helper(), sync.Once-memoized artifact builds, httptest.NewServer-backed fixtureServer, the installPathParts PATH-substitution helper) — this is a migration of WHAT is asserted and WHAT fixtures are served, not a rewrite of the test harness's own plumbing. Update publishedAssets and any host-asset-resolution logic to the new gitid_&lt;version&gt;_&lt;os&gt;_&lt;arch&gt;.tar.gz naming. This task intentionally leaves scripts/install.sh UNCHANGED — every rewritten TestInstallScript_* case is expected to FAIL (RED) against the current raw-binary script; Task 2 makes them pass.
  </action>
  <verify>
    <automated>go vet -tags e2e ./e2e/... &amp;&amp; go test -tags e2e -count=1 -run 'TestRelease_|TestInstallScript_' ./e2e/... 2&gt;&amp;1 | tail -40</automated>
    <fails_when>`go vet -tags e2e` reports a compile error (the rewrite must compile even though it is expected to fail at runtime), or the TestInstallScript_* failures are NOT all attributable to the old raw-binary script (e.g. a fixture-server bug unrelated to install.sh's own behavior would be a false RED, not the intended one)</fails_when>
  </verify>
  <done>e2e/release_e2e_test.go compiles under the e2e build tag and asserts the FULL new D-07 tar.gz contract, including both net-new GITID_VERSION/custom-install-dir cases; every TestInstallScript_* case fails for the expected reason (the still-unmodified install.sh only understands the old raw-binary shape).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): rewrite scripts/install.sh for the tar.gz archive, GITID_VERSION pin, and custom install dir (D-14, D-15)</name>
  <files>scripts/install.sh, Makefile</files>
  <read_first>
    scripts/install.sh (the current file in full — the OS/arch allowlist, the checksum-then-move sequence, the trap-based tmp cleanup)
    e2e/release_e2e_test.go (Task 1's rewritten TestInstallScript_* expectations — this task's contract)
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "Pattern 4" and the V5/Tampering rows of the "Security Domain" section
  </read_first>
  <action>
    Rewrite scripts/install.sh, keeping the existing OS/arch ALLOWLIST pattern (never a convenience mapping — an unexpected `uname` output must still refuse before any URL is built) and the `set -eu` / no-stdin-read / POSIX-only constraints untouched. Asset name construction changes to "gitid_${version_num}_${os_tag}_${arch_tag}.tar.gz" (underscore-separated, matching goreleaser's default name_template — never the old hyphenated raw-binary name). Version resolution: when GITID_VERSION is unset (the default, real-world "latest" case) AND GITID_INSTALL_BASE_URL is unset (the real-world case, as opposed to the existing test-only override seam), resolve the latest release tag via GitHub's redirect (curl -fsSL -o /dev/null -w '%{url_effective}' "https://github.com/${REPO}/releases/latest", then strip everything up to and including "/tag/" to get the tag string) — GitHub's `/releases/latest/download/&lt;asset&gt;` endpoint cannot be used directly anymore because the archive filename now EMBEDS the version number, so the exact tag must be known before any asset URL can be built. When GITID_VERSION IS set, treat it as the exact git tag (including its "v" prefix, e.g. "v1.0.0") and skip the redirect-resolution step entirely. Derive version_num by stripping a leading "v" from whichever tag string was resolved (matches goreleaser's {{.Version}} template, which strips it). Download from "https://github.com/${REPO}/releases/download/${tag}/${asset}" (the versioned, not the "latest", endpoint — always reachable once the tag is known). When GITID_INSTALL_BASE_URL IS set (the existing test-only seam), preserve today's simpler behavior: skip BOTH the redirect-resolution and the versioned-path construction, fetching "${BASE_URL}/${asset}" and "${BASE_URL}/checksums.txt" directly, exactly as the current script does — this keeps the e2e fixture-server tests simple (a flat directory of pre-named files, no redirect simulation needed) while only the real, unoverridden github.com path pays the "latest" resolution cost. Checksum verification: download the ARCHIVE plus the checksums manifest, verify the archive's SHA-256 against the manifest's anchored `^[0-9a-f]{64}  &lt;asset&gt;$` entry BEFORE any extraction (D-14's ceremony, now verifying the single archive file rather than a bare binary — arguably simpler than before). Extraction: `tar -xzf` the verified archive into the SAME tmp dir, then move ONLY the known, exact relative path `gitid` out of it (never trust arbitrary archive entries — this is the tar path-traversal mitigation) via `chmod 0755` + `mv` into the install directory. Install directory: default stays `${HOME}/.local/bin`, now overridable via a new `GITID_INSTALL_DIR` env var (currently absent — this is the D-14 "custom install dir" requirement Phase 9.3 never implemented); validate it is non-empty before use, never interpolate it into anything beyond `mkdir -p`/`mv` target paths. Keep the existing trap-based tmp cleanup, the sha256sum-or-shasum fallback, and the final PATH-membership hint unchanged in spirit, adapted only where the archive flow requires it. In the Makefile, remove the now fully-superseded `checksums` target (goreleaser's checksum: pipe replaces it; its own e2e coverage moved to Task 1's rewrite) and its .PHONY entry; leave `build-cross` untouched (still a valid local unstamped cross-build convenience, BUILD-01).
  </action>
  <verify>
    <automated>make lint-shell &amp;&amp; go test -tags e2e -race -count=1 -timeout 900s -run 'TestRelease_|TestInstallScript_' ./e2e/...</automated>
    <fails_when>`sh -n scripts/install.sh` reports a parse error, or ANY TestRelease_*/TestInstallScript_* case (including the two net-new GITID_VERSION/custom-install-dir cases) fails</fails_when>
  </verify>
  <done>`make lint-shell` passes; every test Task 1 wrote (17 rewritten + 2 net-new) passes against the real rewritten script; the Makefile's `checksums` target is gone with no remaining reference to it anywhere in the tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Untrusted internet download (curl \| sh) -> user's local machine execution | scripts/install.sh runs with the invoking user's shell privileges, fetching binary content from GitHub Releases over the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-01 | Tampering (MITM / compromised download) | scripts/install.sh archive download | high | mitigate | SHA-256 verify-BEFORE-extract against checksums.txt is preserved through the tar.gz rewrite (D-14); a mismatch refuses to install anything, before `tar` ever runs. |
| T-10-05-02 | Injection | GITID_VERSION / GITID_INSTALL_DIR env vars | medium | mitigate | GITID_VERSION only ever appears as an opaque path segment in a `curl` URL (never shell-evaluated); GITID_INSTALL_DIR is only ever passed to `mkdir -p`/`mv` as a literal target path argument, never interpolated into a constructed command string. |
| T-10-05-03 | Tampering (archive path traversal) | `tar -xzf` of the downloaded archive | medium | mitigate | The script extracts into an isolated tmp dir, then moves out ONLY the exact known relative path `gitid` — it never trusts or globs arbitrary paths from inside the archive. |
</threat_model>

<verification>
- `make lint-shell` passes (POSIX parse check).
- `go test -tags e2e -race -count=1 ./e2e/... -run 'TestRelease_|TestInstallScript_'` — all 19 functions (17 rewritten + 2 net-new) pass.
- `make release-snapshot && ./scripts/install.sh` (with GITID_INSTALL_BASE_URL pointed at a local `bin/` fileserver) installs a working `gitid` binary end-to-end, manually confirmed once by the executor.
</verification>

<success_criteria>
D-07's breaking artifact-shape change is fully absorbed: scripts/install.sh and its entire e2e suite agree on the tar.gz contract, GITID_VERSION and a custom install dir both work (net-new, closing gaps Phase 9.3 never had), and the coordinated RED-then-GREEN commit-set leaves no half-migrated state in the tree.
</success_criteria>

<output>
Create `.planning/phases/10-linux-validation-release-pipeline/10-05-SUMMARY.md` when done
</output>
