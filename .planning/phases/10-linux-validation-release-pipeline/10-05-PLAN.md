---
phase: 10-linux-validation-release-pipeline
plan: 05
type: tdd
wave: 3
depends_on: ["10-04"]
files_modified:
  - e2e/release_e2e_test.go
  - scripts/install.sh
  - Makefile
autonomous: true
requirements: [BUILD-03, BUILD-05]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/install.sh downloads and verifies the D-07 tar.gz archive (never the old raw-binary shape) BEFORE extracting, then installs only the known gitid binary out of the extracted contents."
    - "A GITID_VERSION pin and a custom install-dir override both work — neither existed in the Phase 9.3 script."
    - "Every install.sh behavior is proven against real, locally-built goreleaser snapshot archives — not hand-rolled fixtures that could silently diverge from the real artifact shape."
    - "REVIEW C-2: the e2e fixture server mimics GitHub's REAL /releases/latest redirect and /releases/download/<tag>/<asset> URL shape — every test drives install.sh's REAL versioned-asset-name AND versioned-checksums-filename construction code, and at least one dedicated test drives the REAL redirect-resolution/tag-stripping code too. No test-only bypassed/simplified code path is left unexercised."
  artifacts:
    - scripts/install.sh (rewritten)
    - e2e/release_e2e_test.go (rewritten)
  key_links:
    - "scripts/install.sh's asset-name construction -> goreleaser's default archive name_template (gitid_<version>_<os>_<arch>.tar.gz), proven by testing against `make release-snapshot` output, not a guessed string"
    - "e2e/release_e2e_test.go's fixture server -> mirrors GitHub's real /releases/latest + /releases/download/<tag>/<asset> URL shape (REVIEW C-2), serving the SAME versioned gitid_<version>_checksums.txt filename goreleaser's checksum: pipe produces — never a flat/bypassed layout"
---

<objective>
Migrate `scripts/install.sh` and its entire e2e test suite from Phase 9.3's raw-binary artifact shape to Plan 10-04's goreleaser-produced tar.gz archives — the single highest-risk item this phase's own research flagged: a breaking change to already-shipped, working code. Write the new contract into the tests FIRST (RED against the current script), then rewrite the script to satisfy it (GREEN), in one coordinated commit-set so the artifact-shape migration never lands half-done.

Purpose: D-14/D-15 require install.sh to work against the D-07 archive format with verify-before-extract, a version pin, and a custom install dir; D-07 itself already broke the OLD script's raw-binary assumption in Plan 10-04.
Output: a rewritten scripts/install.sh and a rewritten e2e/release_e2e_test.go, both proven against real `make release-snapshot` archives.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-linux-validation-release-pipeline/10-CONTEXT.md
@.planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md
@scripts/install.sh
@e2e/release_e2e_test.go
@Makefile
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): rewrite e2e/release_e2e_test.go for the D-07 tar.gz contract</name>
  <files>e2e/release_e2e_test.go</files>
  <read_first>
    e2e/release_e2e_test.go (the FULL file — stampedArtifacts/hostAsset/fixtureServer/runInstallScript helpers, and all 17 existing TestRelease_*/TestInstallScript_* functions)
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "Pattern 4" (the exact required changes to install.sh) and "Pitfall 1"
    .goreleaser.yaml (from Plan 10-04 — the exact archive/checksum naming this test suite must assert against)
  </read_first>
  <behavior>
    - stampedArtifacts() (or its replacement) calls `make release-snapshot` (not the retired `make checksums`) with VERSION/COMMIT/DATE overrides, and resolves the produced archive/checksums filenames from goreleaser's actual naming (gitid_&lt;version&gt;_&lt;os&gt;_&lt;arch&gt;.tar.gz, gitid_&lt;version&gt;_checksums.txt) rather than a hardcoded raw-binary name.
    - TestRelease_SnapshotBuildProducesArchivesForEveryTarget (was TestRelease_BuildCrossStampsEveryTarget): asserts all 4 platform archives + checksums.txt exist and are non-empty; extracts the HOST's own archive to a temp dir and runs the extracted binary's --version, asserting the D-11 stamp.
    - TestRelease_UnstampedBuildKeepsDevDefaults: unchanged from Plan 10-01's already-updated regex-based assertion (this test exercises `make build`, not the release archive — no further change needed here beyond confirming it still compiles/passes in this file's new shape).
    - TestRelease_ChecksumsManifestMatchesArchives (was ...MatchesBinaries): reads the checksums.txt goreleaser produced, verifies each line's hash against its corresponding .tar.gz file (not a raw binary).
    - REVIEW C-2 FIX — the fixture server mimics GitHub's REAL URL shape instead of a flat/bypassed layout: a new helper (e.g. `startReleaseFixtureServer(t, artifactsDir, tag string) *fixtureServer`) roots a temp directory as `<tmp>/castocolina/gitid/releases/download/<tag>/` containing the goreleaser-snapshot-built archive + the REAL `gitid_<version>_checksums.txt` (never a flat `checksums.txt`), then serves it with `http.FileServer` so a request for `/castocolina/gitid/releases/download/<tag>/<asset>` resolves exactly as it would against real GitHub — the SAME path shape install.sh's own download-URL construction (Task 2) builds. Every existing TestInstallScript_* case sets `GITID_VERSION` to a tag string derived from the REAL version `stampedArtifacts()`/its replacement discovered from the actual produced filenames (never a hardcoded literal) — this is the real, already-specified "pinned install" production path, never a test-only shortcut — so each one drives install.sh's REAL versioned asset-name AND versioned-checksums-filename construction code against the fixture server, closing the "untested real checksums filename" half of the review's finding.
    - Every TestInstallScript_* function (14 total) is rewritten for the tar.gz flow on top of the fixture-shape change above: "installed" means the EXTRACTED gitid binary landed at the install dir, not the raw downloaded file; checksum verification happens against the DOWNLOADED ARCHIVE before any `tar -xzf` runs (never after).
    - Three NET-NEW cases (REVIEW C-2 adds the third): TestInstallScript_GITIDVersionPinsToASpecificVersion (setting GITID_VERSION to a fixed tag-shaped string makes the script fetch that exact version's asset via the versioned download path instead of resolving "latest"); TestInstallScript_CustomInstallDirOverride (an env var override relocates the install target away from the hardcoded ~/.local/bin default); and TestInstallScript_ResolvesLatestViaGitHubRedirect (GITID_VERSION deliberately UNSET; GITID_INSTALL_BASE_URL points at a small dedicated handler — built on the existing `startRecordingHandler` pattern, not the flat `startFixtureServer` — that serves `/castocolina/gitid/releases/latest` with a real `http.StatusFound` redirect to `/castocolina/gitid/releases/tag/<tag>`, plus the versioned download path from the same nested layout above, exactly mirroring GitHub's real redirect shape). This third case is the ONE test in the suite that exercises install.sh's "resolve latest via redirect, then strip everything up to and including /tag/" logic — the brand-new, string-munging, network-shaped code RESEARCH's Pattern 4 flags as previously untested — closing the second half of the review's finding.
  </behavior>
  <action>
    Rewrite e2e/release_e2e_test.go's fixture/build machinery and all 17 existing test functions per the &lt;behavior&gt; block above, plus the 3 net-new cases (REVIEW C-2 adds the redirect-resolution case). Keep the file's existing structural conventions (t.Helper(), sync.Once-memoized artifact builds, httptest.NewServer-backed fixtureServer, the installPathParts PATH-substitution helper, the startRecordingHandler wrapper) — this is a migration of WHAT is asserted and WHAT fixtures are served, not a rewrite of the test harness's own plumbing; the new `startReleaseFixtureServer` helper is additive, built on the same `httptest.NewServer`/`t.Cleanup` pattern the existing `startFixtureServer`/`startRecordingHandler` already use. Update publishedAssets and any host-asset-resolution logic to the new gitid_&lt;version&gt;_&lt;os&gt;_&lt;arch&gt;.tar.gz naming, discovered dynamically from the real `make release-snapshot` output (never hardcoded), per REVIEW C-2's fixture-shape change above. This task intentionally leaves scripts/install.sh UNCHANGED — every rewritten TestInstallScript_* case is expected to FAIL (RED) against the current raw-binary script; Task 2 makes them pass.
  </action>
  <verify>
    <automated>go vet -tags e2e ./e2e/... &amp;&amp; go test -tags e2e -count=1 -run 'TestRelease_|TestInstallScript_' ./e2e/... 2&gt;&amp;1 | tail -40</automated>
    <fails_when>`go vet -tags e2e` reports a compile error (the rewrite must compile even though it is expected to fail at runtime), or the TestInstallScript_* failures are NOT all attributable to the old raw-binary script (e.g. a fixture-server bug unrelated to install.sh's own behavior would be a false RED, not the intended one)</fails_when>
  </verify>
  <done>e2e/release_e2e_test.go compiles under the e2e build tag and asserts the FULL new D-07 tar.gz contract via a fixture server that mirrors GitHub's real /releases/latest + /releases/download/&lt;tag&gt;/&lt;asset&gt; URL shape (REVIEW C-2), including all three net-new cases (GITID_VERSION pin, custom install dir, and the GitHub-redirect resolution case); every TestInstallScript_* case fails for the expected reason (the still-unmodified install.sh only understands the old raw-binary shape).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): rewrite scripts/install.sh for the tar.gz archive, GITID_VERSION pin, and custom install dir (D-14, D-15)</name>
  <files>scripts/install.sh, Makefile</files>
  <read_first>
    scripts/install.sh (the current file in full — the OS/arch allowlist, the checksum-then-move sequence, the trap-based tmp cleanup)
    e2e/release_e2e_test.go (Task 1's rewritten TestInstallScript_* expectations — this task's contract)
    .planning/phases/10-linux-validation-release-pipeline/10-RESEARCH.md "Pattern 4" and the V5/Tampering rows of the "Security Domain" section
  </read_first>
  <action>
    Rewrite scripts/install.sh, keeping the existing OS/arch ALLOWLIST pattern (never a convenience mapping — an unexpected `uname` output must still refuse before any URL is built) and the `set -eu` / no-stdin-read / POSIX-only constraints untouched. Asset name construction changes to "gitid_${version_num}_${os_tag}_${arch_tag}.tar.gz" (underscore-separated, matching goreleaser's default name_template — never the old hyphenated raw-binary name). Version resolution (REVIEW C-2 FIX — GITID_INSTALL_BASE_URL is now a pure origin substitution, never a logic bypass): define `GITHUB_ORIGIN="${GITID_INSTALL_BASE_URL:-https://github.com}"` as the ONE place the fixture-server test seam plugs in — every other line of logic below runs UNCONDITIONALLY, whether GITHUB_ORIGIN is the real github.com or Task 1's fixture server. When GITID_VERSION is unset (the default, real-world "latest" case), resolve the latest release tag via GitHub's redirect: `curl -fsSL -o /dev/null -w '%{url_effective}' "${GITHUB_ORIGIN}/${REPO}/releases/latest"`, then strip everything up to and including "/tag/" to get the tag string — GitHub's `/releases/latest/download/&lt;asset&gt;` endpoint cannot be used directly anymore because the archive filename now EMBEDS the version number, so the exact tag must be known before any asset URL can be built. When GITID_VERSION IS set, treat it as the exact git tag (including its "v" prefix, e.g. "v1.0.0") and skip the redirect-resolution step entirely — this is the REAL pinned-install production path, not a test shortcut. Derive version_num by stripping a leading "v" from whichever tag string was resolved (matches goreleaser's {{.Version}} template, which strips it). Download from "${GITHUB_ORIGIN}/${REPO}/releases/download/${tag}/${asset}" and "${GITHUB_ORIGIN}/${REPO}/releases/download/${tag}/gitid_${version_num}_checksums.txt" (the versioned, not the "latest", endpoint — always reachable once the tag is known) — this is the SAME path shape whether GITHUB_ORIGIN is the real github.com or Task 1's fixture server, so every e2e test drives install.sh's REAL download-URL AND REAL versioned-checksums-filename construction code, never a simplified/bypassed one (closing REVIEW C-2's "untested real checksums filename" finding for every test, not just one). Task 1's fixture server must therefore be rooted so a request for "/${REPO}/releases/download/${tag}/${asset}" resolves to the real goreleaser-built archive, and (for the one dedicated redirect test) a request for "/${REPO}/releases/latest" 302-redirects to "/${REPO}/releases/tag/${tag}" exactly as real GitHub does (closing REVIEW C-2's "untested redirect-resolution" finding). Checksum verification: download the ARCHIVE plus the VERSIONED checksums manifest (gitid_${version_num}_checksums.txt — never a flat checksums.txt), verify the archive's SHA-256 against the manifest's anchored `^[0-9a-f]{64}  &lt;asset&gt;$` entry BEFORE any extraction (D-14's ceremony, now verifying the single archive file rather than a bare binary — arguably simpler than before). Extraction: `tar -xzf` the verified archive into the SAME tmp dir, then move ONLY the known, exact relative path `gitid` out of it (never trust arbitrary archive entries — this is the tar path-traversal mitigation) via `chmod 0755` + `mv` into the install directory. Install directory: default stays `${HOME}/.local/bin`, now overridable via a new `GITID_INSTALL_DIR` env var (currently absent — this is the D-14 "custom install dir" requirement Phase 9.3 never implemented); validate it is non-empty before use, never interpolate it into anything beyond `mkdir -p`/`mv` target paths. Keep the existing trap-based tmp cleanup, the sha256sum-or-shasum fallback, and the final PATH-membership hint unchanged in spirit, adapted only where the archive flow requires it. In the Makefile, remove the now fully-superseded `checksums` target (goreleaser's checksum: pipe replaces it; its own e2e coverage moved to Task 1's rewrite) and its .PHONY entry; leave `build-cross` untouched (still a valid local unstamped cross-build convenience, BUILD-01).
  </action>
  <verify>
    <automated>make lint-shell &amp;&amp; go test -tags e2e -race -count=1 -timeout 900s -run 'TestRelease_|TestInstallScript_' ./e2e/...</automated>
    <fails_when>`sh -n scripts/install.sh` reports a parse error, or ANY TestRelease_*/TestInstallScript_* case (including the three net-new GITID_VERSION/custom-install-dir/GitHub-redirect cases, REVIEW C-2) fails</fails_when>
  </verify>
  <done>`make lint-shell` passes; every test Task 1 wrote (17 rewritten + 3 net-new, REVIEW C-2) passes against the real rewritten script; the Makefile's `checksums` target is gone with no remaining reference to it anywhere in the tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Untrusted internet download (curl \| sh) -> user's local machine execution | scripts/install.sh runs with the invoking user's shell privileges, fetching binary content from GitHub Releases over the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-01 | Tampering (MITM / compromised download) | scripts/install.sh archive download | high | mitigate | SHA-256 verify-BEFORE-extract against the versioned gitid_&lt;version&gt;_checksums.txt manifest is preserved through the tar.gz rewrite (D-14); a mismatch refuses to install anything, before `tar` ever runs. REVIEW C-2: this mitigation is now exercised by every e2e test against the REAL versioned filename, not a flat test-only stand-in. |
| T-10-05-02 | Injection | GITID_VERSION / GITID_INSTALL_DIR env vars | medium | mitigate | GITID_VERSION only ever appears as an opaque path segment in a `curl` URL (never shell-evaluated); GITID_INSTALL_DIR is only ever passed to `mkdir -p`/`mv` as a literal target path argument, never interpolated into a constructed command string. |
| T-10-05-03 | Tampering (archive path traversal) | `tar -xzf` of the downloaded archive | medium | mitigate | The script extracts into an isolated tmp dir, then moves out ONLY the exact known relative path `gitid` — it never trusts or globs arbitrary paths from inside the archive. |
</threat_model>

<verification>
- `make lint-shell` passes (POSIX parse check).
- `go test -tags e2e -race -count=1 ./e2e/... -run 'TestRelease_|TestInstallScript_'` — all 20 functions (17 rewritten + 3 net-new, REVIEW C-2) pass.
- `make release-snapshot && GITID_VERSION=v<discovered-version> GITID_INSTALL_BASE_URL=<local fixture server URL> ./scripts/install.sh` installs a working `gitid` binary end-to-end via the REAL versioned download path (dist/, not bin/, per Plan 10-04's REVIEW C-4 fix), manually confirmed once by the executor.
</verification>

<success_criteria>
D-07's breaking artifact-shape change is fully absorbed: scripts/install.sh and its entire e2e suite agree on the tar.gz contract, GITID_VERSION and a custom install dir both work (net-new, closing gaps Phase 9.3 never had), the e2e fixture server mirrors GitHub's real /releases/latest + /releases/download/&lt;tag&gt;/&lt;asset&gt; URL shape so no test-only bypassed code path is left unexercised (REVIEW C-2), and the coordinated RED-then-GREEN commit-set leaves no half-migrated state in the tree.
</success_criteria>

<output>
Create `.planning/phases/10-linux-validation-release-pipeline/10-05-SUMMARY.md` when done
</output>
