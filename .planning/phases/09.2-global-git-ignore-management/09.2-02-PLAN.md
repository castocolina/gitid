---
phase: 09.2-global-git-ignore-management
plan: 02
type: execute
wave: 2
depends_on: ["09.2-01"]
cross_ai: true
files_modified:
  - internal/gitconfig/baseline.go
  - internal/gitconfig/baseline_test.go
  - internal/doctor/checks/baseline.go
  - internal/doctor/checks/baseline_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - e2e/health_fixer_pty_e2e_test.go
  - e2e/gitignore_pty_e2e_test.go
autonomous: true
requirements: [GIGN-01]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The curated default seed contains the venv, env-file and negation patterns GIGN-01 names by name (.venv/, venv/, .env, .env.*, !.env.example) plus common temp/OS/editor/cache patterns, grouped under comment headers (D-01, D-02, D-03)."
    - "The default seed is a strict superset of the previous 13-pattern set — nothing a user's machine already ignored stops being ignored."
    - "Doctor's excludesfile fix and the new Global Git Ignore screen seed from the SAME curated list — there is no second catalog (D-01)."
    - "A user-edited but populated managed gitignore block is reported by doctor as configured, not as missing or broken."
    - "An ABSENT or EMPTY managed gitignore block still produces a warning that carries the excludesfile pair fix — relaxing the exact-match check never silences a genuinely unconfigured machine."
    - "core.excludesfile pointing at an EXISTING file other than the gitid-managed target is surfaced (informational, no automatic override), so a wired-but-wrong path can no longer pass silently."
    - "core.excludesfile pointing at a path that is NEITHER the managed target NOR an existing file keeps today's ERROR severity, names the configured path, and carries no automatic fix — the branch restructure never turns a diagnosis the shipping doctor already makes into silence, and never repairs a dangling custom path by overwriting the user's chosen value."
    - "core.excludesfile written in the recipe's own tilde form (~/.gitignore_global) against an absolute managed-target path is classified as correctly wired — the branch matrix compares expanded paths, so a gitid-configured machine never lands on a wrong-target branch."
    - "Doctor's fix for the excludesfile pair never overwrites an existing populated managed gitignore block with the defaults."
    - "A failed two-file repair leaves the home exactly as it was: a pre-existing first target is restored from its backup, and a first target the repair itself created is removed — never left behind wired to nothing."
    - "Doctor's excludesfile fix refuses a malformed or duplicate managed baseline fragment and writes NOTHING — the Fixer write path is guarded by the SAME InspectManagedBlockFile inspector as the screen's three seams, so gitid can never classify a finding from one baseline block and patch another."
  artifacts:
    - "internal/gitconfig/baseline.go: extended DefaultGitignorePatterns + DefaultGitignoreEntries"
    - "internal/doctor/checks/baseline.go: comment-aware, edit-tolerant, six-leaf baseline gitignore decision tree (A/B/B2/C/D/E)"
    - "cmd/gitid/wiring.go: non-destructive, shape-guarded fixExcludesfile"
  key_links:
    - "DefaultGitignorePatterns (render lines, comments included) -> RenderGitignoreBlock -> ComposeGlobalGitignore -> WriteGlobalGitignore"
    - "DefaultGitignoreEntries (comment-free) -> doctor CheckBaseline comparisons -> parseGitignoreBlockBody output"
    - "excludesFileWiredToManagedTarget (tilde-expanded against filepath.Dir(d.GitignorePath)) + excludesFileExists + managedGitignorePopulated -> the SIX-leaf doctor decision tree (A unset / B wrong target present / B2 wrong target missing / C managed target missing / D managed target present but block empty / E silent) -> gitignorePairFinding's FixExcludesfile descriptor on A, C and D only"
    - "fixExcludesfile -> existing block detection -> preserve-or-seed decision"
    - "fixExcludesfile -> gitconfig.InspectManagedBlockFile(baselineBytes, \"baseline\") -> the SAME body the subsequent filewriter.ReplaceBlock rewrites (the Fixer's copy of the guard plan 09.2-01 put on the screen's three seams; the ListBlocks first-match loop is deleted, not merely wrapped)"
    - "fixExcludesfile -> gitconfig.InspectGitignoreFile(gitignoreBytes) -> preserve-or-seed decision"
---

<objective>
Make the curated catalog the one the user actually asked for, and make the rest of gitid
coexist with the fact that this content is now user-owned — without ever letting "user-owned"
become a euphemism for "gitid stopped checking".

Two halves, one commit boundary — they cannot ship apart. Extending
`DefaultGitignorePatterns()` immediately changes what doctor's excludesfile fix writes on
every machine (D-01's own "costly" reversibility note), and doctor currently demands a
byte-exact, ordered, comment-free match against that list. Ship the extension alone and every
existing user gets a false "not configured" error plus a "curated entries missing" warning,
AND the health/fixer e2e fixture — which hand-copies the old thirteen patterns — goes red
until Task 3 re-greens it. Ship the screen's editing without this and doctor's `--fix` reverts
the user's reviewed edits behind their back: the destructive false-positive loop this project
has already hit twice.

Purpose: close GIGN-01's literal pattern requirement and remove the doctor/edit conflict
BEFORE editing becomes reachable in plan 09.2-03.
Output: one curated catalog, honest doctor findings that still catch a broken machine, and a
fix that preserves user content.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.2-global-git-ignore-management/09.2-CONTEXT.md
@.planning/phases/09.2-global-git-ignore-management/09.2-REVIEWS.md
@.planning/phases/09.2-global-git-ignore-management/09.2-01-SUMMARY.md
@CLAUDE.md
@recipes/gitconfig.recipe
</context>

<decisions_implemented>
- **D-01** — one catalog, not two: `DefaultGitignorePatterns()` is EXTENDED in place so
  doctor's excludesfile fix and the new screen seed from it.
- **D-02 / D-03** — the seed carries comment-header groups, with the `.env` family
  (`.env`, `.env.*`, `!.env.example`) seeded together under one header. Grouping is a
  readability/seeding concern only; nothing enforces the grouping after seeding.
- Claude's discretion (the extended list beyond the names GIGN-01 gives): see Task 1's
  exact ordered seed, and its explicit exclusion of project-scoped build-output patterns.
</decisions_implemented>

<source_correction>
## Correction carried from cross-AI review: `gitid baseline setup` is not a current command

The previous revision of this plan asserted, as a must-have truth, that "`gitid baseline
setup`, doctor's excludesfile fix, and the new screen all seed from the SAME curated list."
codex-sol traced that `gitid baseline setup` does NOT exist in this binary: `newRootCmd`'s
own doc comment records that D-14 archived the 0.0.1 POC command surface including
`baseline` (`cmd/gitid/main.go:69-83`), and `buildDoctorDeps` deliberately leaves
`SetupBaseline` nil for exactly that reason (`cmd/gitid/wiring.go:4064-4069`). The truth is
corrected above to name only the two real callers. Restoring a retired CLI command is
unrelated scope and is NOT in this phase.

A consequence the review also surfaced: `CheckBaseline` still tells the user to "run 'gitid
baseline setup'" in `SuggestedFix` text — advice naming a command that does not exist.

**Scope of the copy fix, narrowed after cycle-3 re-verification against source** (opencode-grok
LOW): the previous revision said "the gitignore-pair findings and Check 4". That was too broad.
`gitignorePairFinding`'s `SuggestedFix` is ALREADY current — it reads `"configure the managed
global gitignore"` (`internal/doctor/checks/baseline.go:219`) — and needs no edit. The only
finding this phase owns whose copy is genuinely stale is **Check 4** (`baseline.go:162-163`,
`"run 'gitid baseline setup' to restore the managed gitignore block"`, plus the comment above it
at `:154-157` that repeats the claim). Task 2 fixes exactly that one, and does not touch
`gitignorePairFinding`'s already-correct string.

The baseline-INCLUDE missing finding's own suggested-fix text (`baseline.go:92`) has the same
staleness but belongs to the baseline/include surface, not to GIGN-01; it is explicitly DEFERRED
here with that rationale rather than silently dropped, so a later phase (or a `/gsd-quick`) can
close it as a one-line copy fix.
</source_correction>

<review_findings_addressed>
## Cycle 2 findings (09.2-REVIEWS.md "Cycle 2 (verification pass)")

| Finding | Severity | Resolution |
|---------|----------|------------|
| Only the Check-2 call site (`baseline.go:116`) of `matchesDefaultGitignorePatterns` was named; Check-1's call site (`baseline.go:95`) was left unnamed, inviting an invented fourth doctor branch (opencode-grok MEDIUM) | MEDIUM | Task 2's `<action>` now enumerates all THREE occurrences by line — the Check-1 guard at `:95`, the Check-2 binding at `:116`, and the declaration at `:224` — and states exactly what happens to each. `<behavior>` gains a dedicated Check-1 case (populated edited block under a missing baseline include must not also emit the "not configured" pair finding), and the `grep -c ... == 0` criterion now names all three sites |
| The doctor-fix-then-reopen PTY case did not say HOW the fix is triggered — Fixer `f` key, CLI flag, or a direct `fixExcludesfile` call (opencode-grok MEDIUM) | MEDIUM | Task 3's `<behavior>` now specifies the exact PTY recipe through the real Fixer screen (`5` → assert the finding row → `f` → Enter → Enter → `6`), names the case, cites the analog at `e2e/health_fixer_pty_e2e_test.go:180-204`, and explicitly rules out `typeFixerConfirm` because this finding routes to `PlanFor`'s non-destructive default branch (`internal/tuikit/fixplans.go:60-66`). A new acceptance criterion greps to prove no typed-confirm helper was copied in |

## Cycle 3 findings (09.2-REVIEWS.md "Cycle 3 (verification pass)")

| Finding | Severity | Disposition |
|---------|----------|-------------|
| Rollback cannot restore a newly created first file from a backup that does not exist (codex-sol HIGH, `09.2-02-PLAN.md:490`) | HIGH | **CONFIRMED — remediation specified in this plan (implementation pending execution).** `filewriter.Write`'s own doc comment states it plainly: "backupPath is non-empty only when the target pre-existed" (`internal/filewriter/filewriter.go:46-52`). `fixExcludesfile` writes `.gitignore_global` FIRST, and on the fresh machine this fix most exists to serve that file does not exist — so the previous "restore the first from that backup" instruction was a no-op exactly when it mattered, leaving a half-applied repair. Task 3 now requires a `{path, preimage, existed}` record per write and branches the undo on `existed`: restore when the target pre-existed, REMOVE when the transaction created it. The identical rule is applied to plan 09.2-01 Task 2's two-write commit, so both write paths undo the same way. Separate tests for both variants |
| The planned Check-1 "populated edited block" outcome is unreachable through the production reader (codex-sol MEDIUM AND opencode-grok MEDIUM — the two lanes agree here) | MEDIUM | **Accepted, expectation corrected rather than the code widened.** `ReadBaselineState` returns zero state when the baseline/include pair is absent or incomplete (`internal/gitconfig/baseline.go:82-97`), BEFORE `GitignorePatterns` is parsed (`:110-113`). So on Check 1's branch the reader never supplies gitignore data and the predicate swap is a no-op in production. Both lanes warn against "fixing" this by reordering `ReadBaselineState` — that is an unspecified fourth branch and out of scope. Task 2's `<action>` now states the honest contract (the swap is a compile-consistency change; Check 1 keeps appending the pair finding because patterns are always empty there) and its `<behavior>` labels the case as a hand-built-state unit test that documents the predicate, not a production outcome |
| The Fixer PTY ceremony preview is a dummy: `FixPlanFor` falls back to `PlanFor`'s `~/.ssh/config` default for the excludesfile finding, so disk assertions can pass while the ceremony display is wrong (opencode-grok MEDIUM) | MEDIUM | **Accepted, folded in.** Task 3's `<behavior>` now says so explicitly, forbids inventing a real `FixPlanFor` for this finding (out of scope), and forbids asserting the ceremony's target or diff. It also tightens the row assertion: `mustSee` searches the whole frame (`e2e/dummy_demo_e2e_test.go:144`), so the case must assert the finding is the SELECTED row, not merely present |
| The stale `gitid baseline setup` SuggestedFix claim is over-broad — the pair finding's copy is already current (opencode-grok LOW) | LOW | **Accepted, narrowed.** Verified against source: `gitignorePairFinding`'s `SuggestedFix` is already `"configure the managed global gitignore"` (`internal/doctor/checks/baseline.go:219`); only Check 4 (`:162-163`) still names the retired command. Task 2's copy fix is narrowed to Check 4 alone, and this plan's `<source_correction>` is corrected to match |

## Cycle 5 findings (09.2-REVIEWS.md "Cycle 5 (verification pass)")

Cycle 5's ONE corroborated HIGH — both lanes found it independently, on the same root cause as
cycle-3's confirmed baseline-duplicate bug but on the OTHER write path this phase owns — belongs
to this plan.

| Finding | Severity | Disposition |
|---------|----------|-------------|
| `fixExcludesfile` reads the FIRST `"baseline"` block from `filewriter.ListBlocks` and then calls `filewriter.ReplaceBlock` with no shape check, while doctor's classification reads the LAST duplicate through `indexBlocks` — the exact preview/commit divergence plan 09.2-01's inspector was invented to close, reproduced through the Fixer instead of the screen (codex-sol HIGH **and** opencode-grok HIGH, corroborated; orchestrator independently re-confirmed both citations against `cmd/gitid/wiring.go:4355-4382`) | HIGH | **CONFIRMED — remediation specified in this plan's Task 3 (implementation pending execution).** Re-derived from live source: `fixExcludesfile` (declared at `cmd/gitid/wiring.go:4355`) loops `for _, blk := range filewriter.ListBlocks(content)` and `break`s on the first `blk.Name == "baseline"`, then hands the patched body to `filewriter.ReplaceBlock(content, "baseline", newBody)`. `ReadBaselineState`'s `indexBlocks` (`internal/gitconfig/baseline.go:129-135`) keeps the LAST block of a name, so the finding that TRIGGERS the fix can be classified from one block while the fix patches another — it then reports success and leaves the finding unresolved. An orphan `baseline` BEGIN is worse: `ListBlocks` returns nothing, `body` stays empty, and `ReplaceBlock` appends a freshly rendered block into already-malformed content. Fixed the same way plan 09.2-01 fixed the screen path and for the same blast-radius reason: fail closed on the fragment's SHAPE via `gitconfig.InspectManagedBlockFile(content, "baseline")` before the FIRST mutation and again immediately before the fragment's own write, and take the body from the inspector rather than from the `ListBlocks` first-match loop. `internal/filewriter/block.go` stays byte-unchanged. Task 3's `<behavior>` adds the duplicate-baseline and orphan-BEGIN refusal cases, mirroring the screen-side tests |
| The Fixer ceremony preview is knowingly inaccurate for this finding — it may show `~/.ssh/config` rather than the files actually repaired (codex-sol MEDIUM, re-raised from cycle 3) | MEDIUM | **Already dispositioned in cycle 3, unchanged.** Task 3's `<behavior>` step 8 records the caveat in the test's own comment, forbids asserting on the ceremony's target or diff, and forbids inventing a real `FixPlanFor` branch (Fixer-surface scope, not GIGN-01). Recording it as a formal deferred defect on the Fixer surface is the right follow-up and is noted here so a later phase can pick it up; it is not re-litigated inside GIGN-01 |

## Cycle 7 findings (09.2-REVIEWS.md "Cycle 7 (verification pass)")

| Finding | Severity | Disposition |
|---------|----------|-------------|
| The five-branch doctor matrix omits a dangling WRONG-target path. Live `CheckBaseline` fires an ERROR via `else if !fileExists` for ANY configured path that does not exist, managed target or not (`internal/doctor/checks/baseline.go:117-135`), but the proposed matrix covered only "wrong target that EXISTS" (Branch B) and "MANAGED target that is missing" (Branch C). A value like `/some/deleted/custom-ignore` satisfied neither, so an executor following the enumerated five-branch list could regress a currently diagnosed broken machine into silence (codex-sol HIGH) | HIGH | **CONFIRMED — remediation specified in this task (implementation pending execution).** The orchestrator independently re-verified the citation against live source before this revision: the `!fileExists` arm at `baseline.go:124` is reached for every non-empty `excludesFile` value and is not target-scoped. Task 2 now specifies the check as an explicit DECISION TREE rather than an ordered branch list — both the "not the managed target" arm and the "managed target" arm split on file existence — and adds **Branch B2** (wrong target, file missing) as its own named, separately tested leaf: ERROR severity, preserving today's diagnosis; its OWN title naming the CONFIGURED path rather than reusing Branch C's managed-target wording; and `Fix` nil, because the only repair `FixExcludesfile` can perform is to retarget `core.excludesfile` at gitid's managed path, silently overwriting the value the user deliberately chose — exactly what Branch B's `setDiffersFindings`-modelled policy refuses. `SuggestedFix` names the two honest options (create that file, or adopt the managed target via the Global Git Ignore screen). The `Fix == nil` disposition is a recorded departure from live behavior, pinned by a test and called out in the task's `<reversibility>` so it cannot be reverted by accident. Acceptance now also requires an exhaustive combination test, so no future leaf can go missing silently, and both wrong-target permutations are driven through the real `ReadBaselineState` |
| Doctor's path-equality predicates must keep comparing against the live tilde fixture (`internal/doctor/checks/baseline_test.go:45-54` seeds `"core.excludesfile": "~/.gitignore_global"`); `internal/doctor/checks` cannot import `cmd/gitid.expandTildeForHome`, so expansion there must use `filepath.Dir(d.GitignorePath)` — and the fixture must not be "fixed" to an absolute path (opencode-grok MEDIUM) | MEDIUM | **Accepted, folded in.** Same root cause as the cycle-7 HIGH on plan 09.2-01: `RenderBaselineBlock` writes the literal tilde (`internal/gitconfig/baseline.go:318`), so a string-equal predicate routes every gitid-configured machine onto Branch B or B2. Task 2's `<action>` now names `filepath.Dir(d.GitignorePath)` as the expansion base and forbids `os.UserHomeDir()`/`$HOME`; `<behavior>` gains a tilde-spelling case asserting Branch E; and acceptance requires `git diff internal/doctor/checks/baseline_test.go` to show `fullyInstalledState`'s tilde value UNCHANGED plus `TestBaselineAllPass` still green at zero findings |
| The second baseline inspect (immediately before its own write, after the fragment is re-read) has no rollback path if it fails AFTER `.gitignore_global` was already written (opencode-grok MEDIUM) | MEDIUM | **Not re-planned in this pass — scoped out by the orchestrator's revision brief, which authorised exactly the three corroborated-and-verified cycle-7 items.** Recorded here rather than silently dropped so the next convergence pass can pick it up: the `{path, preimage, existed}` undo Task 3 already specifies for a failed second WRITE should also fire on a failed second INSPECT, since an inspect error is not a write error and TOCTOU malformation would otherwise leave a seeded-but-unwired ignore file — the half-applied state T-09.2-20 exists to prevent |
</review_findings_addressed>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the curated catalog to the grouped seed, and add the comment-free entry view</name>

  <read_first>
    - internal/gitconfig/baseline.go lines 13-35 (BaselineState.GitignorePatterns), 56-120 (ReadBaselineState), 190-210 (parseGitignoreBlockBody), 261-280 (DefaultGitignorePatterns), 405-420 (RenderGitignoreBlock), 466-492 (WriteGlobalGitignore and the ComposeGlobalGitignore extraction landed by plan 09.2-01)
    - internal/gitconfig/baseline_test.go lines 240-290 and 365-430 and 520-610 (every existing assertion that consumes DefaultGitignorePatterns — these must be updated, not deleted)
    - internal/doctor/checks/baseline.go lines 140-165 (Check 4's loop over DefaultGitignorePatterns — the reason a comment header would be permanently "missing")
    - e2e/health_fixer_pty_e2e_test.go lines 60-95 (the all-green fixture that hand-copies the old thirteen patterns and goes red the moment this task lands)
    - .planning/phases/09.2-global-git-ignore-management/09.2-CONTEXT.md `<specifics>` (the user's verbatim ask)
    - .planning/REQUIREMENTS.md section Q (GIGN-01's literal pattern list)
  </read_first>

  <files>
    internal/gitconfig/baseline.go,
    internal/gitconfig/baseline_test.go
  </files>

  <behavior>
    Write these tests first.

    - `DefaultGitignorePatterns()` contains every one of the previous thirteen entries
      (.DS_Store, Thumbs.db, *.log, *.bak, *.tmp, *.swp, *.swo, .idea/, .vscode/,
      node_modules/, __pycache__/, *.pyc, .env) — a strict-superset assertion driven from a
      literal list in the test, so a future edit that drops one fails loudly.
    - `DefaultGitignorePatterns()` contains each pattern GIGN-01 names explicitly.
    - The negation entry appears strictly AFTER the wildcard env entry it re-includes
      (gitignore semantics: a later negation wins).
    - `DefaultGitignorePatterns()` contains at least one comment-header line, and
      `DefaultGitignoreEntries()` contains none.
    - `DefaultGitignoreEntries()` equals the ordered, comment-and-blank-free filtering of
      `DefaultGitignorePatterns()`.
    - Round trip through the real read path: rendering the defaults into a block, then reading
      them back with `ReadBaselineState`, yields `GitignorePatterns` equal to
      `DefaultGitignoreEntries()`. (This is the invariant that stops comment headers from
      making every doctor comparison unsatisfiable.)
    - `WriteGlobalGitignore` with the extended defaults is still idempotent (second call
      returns an empty backup path and identical bytes) and still preserves foreign content
      outside the block.
    - `ComposeGlobalGitignore` with the extended defaults still produces bytes identical to
      what `WriteGlobalGitignore` writes (plan 09.2-01's preview/commit contract survives the
      catalog change).
  </behavior>

  <action>
Replace the body of `DefaultGitignorePatterns()` with the curated seed below, in exactly this
order. Every element is one rendered LINE — comment headers included, because
`RenderGitignoreBlock` writes the slice one entry per line and a comment line is valid
gitignore syntax. Keep the existing doc comment's byte-stability note and extend it to say the
slice now carries comment headers and that `DefaultGitignoreEntries` is the comment-free view.

Seed order (group header, then that group's entries):
OS artifacts group: `.DS_Store`, `Thumbs.db`, `desktop.ini`.
Editors and IDEs group: `.idea/`, `.vscode/`, `*.swp`, `*.swo`, `*~`.
Logs, temp and scratch group: `*.log`, `*.bak`, `*.tmp`, `tmp/`, `.tmp/`.
Environment files group (D-03 — these three seeded together under ONE header, and the header's
text should say committed examples stay tracked): `.env`, `.env.*`, `!.env.example`.
Python group: `__pycache__/`, `*.pyc`, `.venv/`, `venv/`.
Node group: `node_modules/`.
Tooling caches group: `.direnv/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`.

Deliberately NOT seeded, and say so in the doc comment: project-scoped build output such as a
distribution or build directory. A global ignore that hides those would silently un-track them
in repositories that legitimately commit them; the content is user-editable now, so anyone who
wants them adds them on the screen.

The scratch-directory entries in the logs/temp group are the deliberate, narrower exception to
that same rule, and the doc comment must say why rather than leaving the inconsistency for a
reader to find: the user asked for global scratch-directory ignoring by name in this phase's
CONTEXT, and a directory of that name is conventionally machine-local scratch, whereas a
distribution or build directory is routinely committed. Addresses review concern: the seed was
otherwise inconsistent with its own stated exclusion rationale (xai-grok MEDIUM).

Add `DefaultGitignoreEntries() []string` returning the ordered subset of
`DefaultGitignorePatterns()` with blank lines and lines whose trimmed form starts with a
comment marker removed — the same filtering `parseGitignoreBlockBody` already performs when
reading a block back, so the two can never disagree. Document that callers comparing against
what `ReadBaselineState` returns must use THIS function, never the render list.

Update every existing assertion in `internal/gitconfig/baseline_test.go` that assumed the
thirteen-entry, comment-free list. Do not delete coverage — restate it against the new seed.

Execution note (addresses review concern: split-commit hazard, xai-grok HIGH): the moment this
task lands, `e2e/health_fixer_pty_e2e_test.go`'s all-green fixture and `cmd/gitid`'s
default-pattern assertion are red, because both hand-copy the old catalog and doctor's Check 4
compares against the new one. Tasks 1, 2 and 3 of this plan are therefore ONE commit. This
task's own `<verify>` deliberately runs the doctor, cmd and e2e suites so that a commit
containing only Task 1 cannot be reported green.
  </action>

  <verify>
    <automated>go test ./internal/gitconfig/ -count=1</automated>
    <automated>go test -race -count=1 ./internal/gitconfig/ ./internal/doctor/... ./cmd/gitid/...</automated>
    <automated>make test-e2e</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/gitconfig/ -run 'TestDefaultGitignore' -count=1` passes with the superset, GIGN-01-names, negation-ordering, comment-header and round-trip cases all present.
    - `go build ./...` exits 0.
    - The doc comment on `DefaultGitignorePatterns` names `DefaultGitignoreEntries` as the comparison view, states the build-output exclusion, and states the scratch-directory exception with its reason.
    - `grep -c "DefaultGitignoreEntries" internal/gitconfig/baseline.go` returns at least 2 (declaration plus doc reference).
    - `make test-e2e` passes at this task's boundary — proving Tasks 1-3 landed as one buildable, green commit.
  </acceptance_criteria>

  <reversibility rating="costly">
    Per CONTEXT D-01: `DefaultGitignorePatterns()` is called by doctor's excludesfile fix and
    (from plan 09.2-01) by the new screen's default seed, so this changes what every existing
    caller writes on every user machine, not just what the new screen seeds. Reverting after
    users have written the new block leaves their files carrying patterns the code no longer
    knows about — harmless but no longer self-consistent.
  </reversibility>

  <done>
    One curated catalog exists, it is a strict superset of the old one, it names every pattern
    GIGN-01 requires, and reading a rendered block back produces exactly
    `DefaultGitignoreEntries()`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make doctor honest about a user-owned gitignore block — without going blind</name>

  <read_first>
    - internal/doctor/checks/baseline.go (whole file — the file header comment, the four checks, gitignorePairFinding, matchesDefaultGitignorePatterns, excludesFileExists, and setDiffersFindings, which is the in-repo precedent for "the user deviated deliberately")
    - internal/doctor/checks/baseline_test.go lines 30-90 (how the fixture seeds patterns)
    - internal/gitconfig/baseline.go lines 56-116 and 190-210 (ReadBaselineState, parseGitignoreBlockBody — what state.GitignorePatterns actually contains, and the early return when the baseline/include setup is absent or incomplete)
    - Task 1's `<action>` in this file (DefaultGitignoreEntries' contract)
    - .planning/phases/09.2-global-git-ignore-management/09.2-REVIEWS.md — the "Doctor predicate correctness" consensus concern, which both reviewers derived independently from this file's exact check numbering
    - .planning/STATE.md "Doctor reserved-block false-positive loop" note in the memory-backed blockers context
  </read_first>

  <files>
    internal/doctor/checks/baseline.go,
    internal/doctor/checks/baseline_test.go
  </files>

  <behavior>
    Write these tests first; each must fail against the current check code.

    Predicate-level:
    - A managed gitignore block whose entries are exactly `DefaultGitignoreEntries()` produces
      no baseline gitignore finding at any severity above informational.
    - Check 1's own branch (baseline include block missing, `baseline.go:95`), driven with a
      HAND-BUILT `BaselineState` carrying a populated but user-edited block, produces the
      include-missing error and does NOT also append the "not configured" pair finding — proving
      the call site was converted to the block-populated predicate rather than left on an exact
      match. **Label this test, in its own name and a comment, as a predicate-level unit test
      that documents the converted call site — NOT a production outcome.** Both cycle-3 reviewers
      independently established that the real reader never produces this state:
      `ReadBaselineState` returns zero state whenever the baseline/include pair is absent or
      incomplete (`internal/gitconfig/baseline.go:82-97`), which is BEFORE `GitignorePatterns` is
      parsed (`:110-113`), so on Check 1's branch `state.GitignorePatterns` is always empty and
      the pair finding is always appended in production. The predicate swap there is a
      compile-consistency change, deliberately not a behavior change. Do NOT "make this real" by
      moving gitignore parsing above `ReadBaselineState`'s early return — that is an unspecified
      change to a shared reader, outside this phase's scope, and both reviewers warned against
      exactly that drive-by.
    - A managed gitignore block the user has EDITED — one default entry removed, two personal
      entries added — is treated as configured: no error or warning saying the excludesfile and
      global gitignore are not configured.
    - The same edited block produces at most an informational finding about entries the user
      removed, worded as a deliberate user choice that gitid will not override (matching
      `setDiffersFindings`' established framing), carrying no executable fix, and naming the
      Global Git Ignore screen's Reset action as the way to adopt the current catalog.
    - A block containing comment-header lines is not penalised: the comparison operates on the
      comment-free entry view on both sides.

    Branch-level — these are the cases the review proved a "populated at both call sites"
    substitution would silence, and each is a separate test:
    - Branch A: `core.excludesfile` unset, block populated. Still produces the existing
      not-configured WARNING, and that finding still carries the excludesfile pair fix.
    - Branch B: `core.excludesfile` set to an EXISTING file that is NOT the gitid-managed
      target, block populated. Produces an INFORMATIONAL finding naming both the configured
      path and the managed target, with no executable fix (a deliberate user choice gitid does
      not override) — and produces no false "configured" silence.
    - Branch B2: `core.excludesfile` set to a path that is NEITHER the gitid-managed target NOR
      an existing file — a dangling pointer at a custom location, e.g.
      `/some/deleted/custom-ignore`. Produces an ERROR whose title NAMES the configured path
      and says it does not exist, distinct from Branch C's managed-target wording, and whose
      `Fix` is nil. Assert BOTH halves: the severity is error (never info, never warning) AND
      the fix descriptor is nil.
      **This branch exists because the five-branch matrix silently dropped a diagnosis the
      shipping doctor already makes** (cycle-7 codex-sol HIGH): live `CheckBaseline` reaches
      `else if !fileExists` for ANY configured-but-missing path, managed target or not
      (`internal/doctor/checks/baseline.go:124-135`), so a machine pointing at a deleted custom
      ignore file is diagnosed today and would have gone silent under a matrix whose only
      wrong-target branch required the file to exist. The nil `Fix` is a deliberate, recorded
      change from live behavior — see the `<action>`'s rationale; the test must pin it so the
      choice cannot be quietly reverted to `gitignorePairFinding`'s retargeting fix.
    - Branch C: `core.excludesfile` set to the managed target but the file does not exist.
      Keeps its existing ERROR severity and its fix descriptor, unchanged. Its test asserts
      `Fix` is NON-nil, which is what stops an executor from collapsing C and B2 into one
      branch: they differ in disposition, not only in wording.
    - Branch D: `core.excludesfile` set to the managed target, the file EXISTS, and the managed
      block is absent or has no non-blank non-comment line. Produces a WARNING that names the
      empty/absent managed block, and that finding CARRIES the excludesfile pair fix
      descriptor. This is the branch that would otherwise emit nothing at all.
    - Branch E: `core.excludesfile` set to the managed target, file exists, block populated.
      Produces no warning and no error — only the possible informational curated-entries note.

    Path-spelling — the branch matrix hinges on "is this value the managed target?", and the
    value real machines carry is a tilde:
    - `core.excludesfile` set to the LITERAL `~/.gitignore_global` — the exact bytes
      `RenderBaselineBlock` writes (`internal/gitconfig/baseline.go:318`) and the form the live
      fixture already uses (`internal/doctor/checks/baseline_test.go:45-54`) — with the file
      present and the block populated lands on Branch E: NO error, NO warning, and NO
      wrong-target informational finding. A string-equal predicate would send every
      gitid-configured machine to Branch B or B2 instead. Keep the existing fixture's tilde
      spelling; do NOT "fix" it to an absolute path to make a naive comparison pass.
      `TestBaselineAllPass` must still report zero findings after the restructure.

    Integration-level (addresses codex-sol MEDIUM: synthetic doctor states can disagree with
    the real reader):
    - At least one case drives the real `gitconfig.ReadBaselineState` over a temp home rather
      than constructing `BaselineState` by hand, and asserts the same branch outcome, so a
      test cannot pass against a state shape the real reader never produces.
    - Run that real-reader case for BOTH wrong-target permutations — an existing custom file
      (Branch B) and a missing one (Branch B2) — so neither wrong-target disposition rests on a
      hand-built state alone (cycle-7 codex-sol suggestion).
  </behavior>

  <action>
Split the single exact-match predicate into the three independent conditions the call sites
actually need. Addresses the review's second consensus finding: substituting a bare
"populated" predicate at both call sites — as the previous revision literally specified —
would make a wired key plus an existing file plus an empty managed block emit no pair finding
AND no Check 4 finding, dropping the only `FixExcludesfile` descriptor for that machine (both
reviewers, HIGH).

Introduce three predicates and remove `matchesDefaultGitignorePatterns`:
one reporting whether the managed gitignore block has at least one non-blank, non-comment line;
one reporting whether `state.BaselineKeys["core.excludesfile"]` is set at all;
one reporting whether that value, after tilde expansion, resolves to the gitid-managed target
path the doctor deps already know.

**Expand the tilde inside `internal/doctor/checks`, against `filepath.Dir(d.GitignorePath)`.**
That package cannot import `cmd/gitid`'s own `expandTildeForHome`, and it must not reach for
`os.UserHomeDir()`/`$HOME` (the hermeticity rule that helper's doc comment records). This is
load-bearing, not cosmetic: `RenderBaselineBlock` writes the LITERAL
`excludesfile = ~/.gitignore_global` (`internal/gitconfig/baseline.go:318`), and the live
fixture `fullyInstalledState` already carries that spelling against an absolute
`GitignorePath` (`internal/doctor/checks/baseline_test.go:45-54`). A raw `==` would route every
correctly configured machine into Branch B or B2. Keep that fixture's tilde form as it is.

`matchesDefaultGitignorePatterns` has THREE occurrences in
`internal/doctor/checks/baseline.go` and this task must account for every one of them
explicitly — do not invent an unspecified fourth doctor branch to absorb a leftover:

- **`baseline.go:95`** — Check 1's early-return path (the "baseline [include] block missing
  from ~/.gitconfig" branch), where `if !matchesDefaultGitignorePatterns(state.GitignorePatterns)`
  guards an appended `gitignorePairFinding` before the function returns. Replace that guard
  with the negation of the block-populated predicate. Severity, title, explanation and the
  pair fix descriptor on that finding are UNCHANGED.

  **State the honest scope of this one in a code comment at the call site**, because both
  cycle-3 reviewers independently flagged that a reader will otherwise over-read it: in
  PRODUCTION this swap changes nothing. `ReadBaselineState` returns zero state whenever the
  baseline/include pair is absent or incomplete (`internal/gitconfig/baseline.go:82-97`), before
  `GitignorePatterns` is ever parsed (`:110-113`), so on this branch the patterns slice is always
  empty, the predicate is always false, and the pair finding is always appended — exactly as
  today. The swap exists so the file has ONE predicate vocabulary after
  `matchesDefaultGitignorePatterns` is deleted, not to change this branch's behavior. Do NOT
  reorder `ReadBaselineState` to "make it reachable": that is an unspecified change to a shared
  reader and is outside this phase.
- **`baseline.go:116`** — Check 2's `gitignorePresent := matchesDefaultGitignorePatterns(...)`
  binding, which feeds the pair check. This is the site the six-branch restructure below
  replaces wholesale.
- **`baseline.go:224`** — the function declaration itself. Delete it once both call sites are
  converted; the `grep -c` acceptance criterion below is what proves nothing was left behind.

Addresses cycle-2 review concern: only the Check-2 call site was previously named, so the
Check-1 site was left to the executor's improvisation (opencode-grok MEDIUM).

Restructure the pair check as a DECISION TREE over the three predicates — not as a flat list of
`else if`s an executor can quietly drop a leaf from. Write it in exactly this shape so every
combination of (key set?) x (points at the managed target?) x (file exists?) x (block populated?)
lands on exactly one named branch:

    key unset                                  -> Branch A
    key set, NOT the managed target,  missing  -> Branch B2
    key set, NOT the managed target,  exists   -> Branch B
    key set,     the managed target,  missing  -> Branch C
    key set,     the managed target,  exists, block empty/absent -> Branch D
    key set,     the managed target,  exists, block populated    -> Branch E (silence)

**SIX branches, and B2 is the one the previous revision was missing.** Both the "not the managed
target" arm and the "managed target" arm split on file existence; a wrong-target arm that only
handles the existing case is the cycle-7 codex-sol HIGH — it regresses a diagnosis the shipping
doctor already makes into silence.

Branch A keeps the existing not-configured warning text and severity and keeps the pair fix.
Branch B is new and informational with no fix, worded on the `setDiffersFindings` model —
gitid names what it found and what it manages, and does not override a value the user chose.

Branch B2 is new and is an ERROR, matching the severity the live code already produces for this
machine (`internal/doctor/checks/baseline.go:124-135` fires on `!fileExists` for ANY configured
path). Give it its OWN title, naming the configured path and saying that file does not exist —
do not reuse Branch C's managed-target wording, which would tell the user the wrong file is
missing. Build the finding directly rather than through `gitignorePairFinding`, because
**`Fix` on this branch is nil, and that is a deliberate departure from live behavior that must be
recorded in a code comment at the branch:** the only repair `FixExcludesfile` can perform is to
point `core.excludesfile` at gitid's managed target, which would silently overwrite the custom
path the user deliberately chose — precisely what Branch B's policy (and the `setDiffersFindings`
precedent this whole task follows) refuses to do. gitid cannot create a file at a path it does
not manage either. So the honest disposition is: report the breakage at full ERROR severity, name
the path, and offer the user the two things that ARE true — create that file themselves, or use
the Global Git Ignore screen to adopt the managed target. Put those two options in
`SuggestedFix`. Do not "restore parity" by attaching `gitignorePairFinding`'s fix here; the test
pinning `Fix == nil` is what makes that regression loud.

Branch C is unchanged, fix descriptor and all. Branch D is the empty-block branch: severity
warning, a title naming the empty or absent managed block inside the configured file, an
explanation that Git has a configured ignore file with no gitid-managed patterns in it, and the
SAME `gitignorePairFinding` fix descriptor Branch A uses, so the machine remains repairable.
Branch E emits nothing here.

Rewrite Check 4 (curated entries). Compare `state.GitignorePatterns` against
`gitconfig.DefaultGitignoreEntries()` rather than the render list, so comment headers can never
make it unsatisfiable. Change its disposition: when the block is populated but some curated
entries are absent, emit an INFORMATIONAL finding, not a warning, worded like the existing
per-key deviation finding — state that the managed gitignore is user-editable, name how many
curated entries are currently absent, name the screen's Reset action as the way to adopt the
current catalog, and keep `Fix` nil. When the block is entirely absent or empty, Branch D above
already fires and Check 4 must add nothing.

Correct the stale suggested-fix advice on the ONE finding this phase owns that actually carries
it. Verified against source in cycle 3 (opencode-grok LOW): `gitignorePairFinding`'s
`SuggestedFix` already reads `"configure the managed global gitignore"`
(`internal/doctor/checks/baseline.go:219`) and must be left ALONE. Only **Check 4**
(`baseline.go:162-163`) still tells the user to `run 'gitid baseline setup'`, a command this
binary no longer has (see this plan's `<source_correction>`). Repoint Check 4's copy at what does
exist — the Global Git Ignore screen and its Reset action — and update the stale comment above it
at `:154-157` which asserts the same retired command. Leave the baseline-INCLUDE missing
finding's own suggested-fix text (`baseline.go:92`) alone — it belongs to the include surface,
not to GIGN-01, and is deferred with that rationale rather than silently changed.

Update the file's header doc comment. It currently describes Check 4 as requiring all default
patterns to be present, and (a separate staleness the review found) still claims Check 4 has a
fix descriptor when the code sets it to nil. Describe the new, edit-tolerant, six-branch
contract — reproducing the decision table above so the file documents its own exhaustiveness —
and cite GIGN-01 as the reason the content became user-owned.

Update `internal/doctor/checks/baseline_test.go`'s fixtures to seed from
`gitconfig.DefaultGitignoreEntries()` instead of a hand-copied literal list, so the fixture can
never drift from the catalog again.
  </action>

  <verify>
    <automated>go test ./internal/doctor/... -count=1</automated>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/...</automated>
    <automated>make lint</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/doctor/checks/ -run 'TestCheckBaseline' -count=1` passes with a distinct test for each of the SIX branches — A, B, B2, C, D and E — plus the real-`ReadBaselineState` integration case run for both wrong-target permutations.
    - Branch B2's test asserts the finding's severity is ERROR, that its `Fix` is nil, and that its title contains the configured path (e.g. the seeded `/some/deleted/custom-ignore`) rather than the managed target's path — the cycle-7 codex-sol HIGH. Branch C's test asserts its `Fix` is NON-nil, so the two dangling-pointer branches cannot be collapsed into one.
    - No configuration falls through the tree silently: a table-driven test enumerates every combination of (key unset / key at the managed target / key elsewhere) x (file exists / missing) x (block populated / empty) and asserts each case produces the branch named in the `<action>`'s decision table, with at least one finding for every combination except Branch E.
    - The tilde spelling is pinned: a test seeds `core.excludesfile` as the literal `~/.gitignore_global` against an absolute `GitignorePath` and asserts Branch E (no findings). `git diff internal/doctor/checks/baseline_test.go` shows `fullyInstalledState`'s `"core.excludesfile": "~/.gitignore_global"` value UNCHANGED — the fixture was not "fixed" to an absolute path to accommodate a string-equal predicate (cycle-7 opencode-grok MEDIUM).
    - The tilde expansion stays hermetic: `grep -v '^\s*//' internal/doctor/checks/baseline.go | grep -c 'os.UserHomeDir\|Getenv("HOME")'` returns 0 (the file has zero such calls today, so this gate is about not introducing one), and `grep -n 'filepath.Dir(d.GitignorePath)' internal/doctor/checks/baseline.go` matches inside the managed-target predicate — the comment-stripping is deliberate so a future explanatory comment cannot invalidate the gate.
    - `go test ./internal/doctor/checks/ -run 'TestBaselineAllPass' -count=1` still passes with zero findings after the restructure.
    - `grep -c "matchesDefaultGitignorePatterns" internal/doctor/checks/baseline.go` returns 0 — all three occurrences (the Check-1 call at `:95`, the Check-2 binding at `:116`, and the declaration at `:224`) are gone.
    - `grep -n "DefaultGitignoreEntries" internal/doctor/checks/baseline.go` shows the comparison uses the comment-free view.
    - Branch D's test asserts BOTH that a warning-severity finding is produced AND that its fix descriptor is non-nil — the specific regression the cross-AI review identified.
    - Branch B's test asserts the finding is informational and its fix descriptor is nil.
    - The edited-block test asserts the finding's severity is the informational level and that its fix descriptor is nil.
    - The file header comment no longer describes Check 4 as carrying a fix descriptor.
    - `grep -n "gitid baseline setup" internal/doctor/checks/baseline.go` matches ONLY the deliberately-deferred baseline-INCLUDE finding's `SuggestedFix` — Check 4's `SuggestedFix` and the comment above it no longer name the retired command. Verify by reading which lines match, not by a bare count: this is an inspection criterion precisely so a future explanatory comment cannot invalidate it.
    - `git diff internal/doctor/checks/baseline.go` shows NO change to `gitignorePairFinding`'s `SuggestedFix` string, which was already current — the copy fix is narrowed to Check 4 (cycle-3 opencode-grok LOW).
    - The Check-1 test is named and commented as a predicate-level unit test over a hand-built `BaselineState`, and `git diff --stat internal/gitconfig/baseline.go` shows no reordering of `ReadBaselineState`'s early return.
    - `make lint` reports 0 issues.
  </acceptance_criteria>

  <reversibility rating="costly">
    This relaxes a health contract established in Phase 8. It follows the in-repo precedent
    (`setDiffersFindings`: a deliberate user value is informational and never overridden)
    rather than inventing a new policy, but a machine that has been running the relaxed check
    will no longer be nagged about entries it dropped. The six-branch split is what keeps the
    relaxation from becoming blindness; collapsing it back to one predicate would reintroduce
    the exact defect the cross-AI review caught.

    One narrower reversal is worth calling out separately: Branch B2 keeps today's ERROR but
    DROPS the executable fix the live code currently offers for a configured-but-missing path.
    That is reversible in one line if the project later decides a confirmed retarget ceremony is
    the better answer — the branch is already isolated and its disposition is pinned by a test —
    but restoring `gitignorePairFinding`'s fix as-is would make `--fix` silently overwrite a
    user-chosen `core.excludesfile` value, which is the behavior this task exists to stop.
  </reversibility>

  <done>
    Doctor reports a populated, user-edited gitignore block as configured, informs rather than
    warns about absent curated entries, still catches an unset key, a wrong target that exists,
    a wrong target that does NOT exist, a dangling managed target and an empty managed block,
    and still hands the user a fix for every one of those that gitid can repair without
    overwriting a value the user deliberately chose.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stop the excludesfile fix from reverting user content or patching the wrong baseline block, and re-green the suites that seed the old catalog</name>

  <read_first>
    - cmd/gitid/wiring.go lines 4320-4400 (fixExcludesfile and patchExcludesfileInBaselineBody, including the doc comment explaining why the fix patches the block body directly, and the fact that the gitignore write happens FIRST and its backup is never used for rollback)
    - cmd/gitid/wiring_test.go lines 4480-4520 (the existing assertion that iterates the default patterns)
    - internal/filewriter/block.go lines 26-62 and 160-215 (ListBlocks / ReplaceBlock semantics the fix relies on)
    - internal/filewriter/block.go lines 117-155 (replaceBlockWith: FIRST complete pair wins, then break) and internal/gitconfig/baseline.go lines 125-136 (indexBlocks: duplicates collapse into a name-keyed map, LAST one wins) — read these two TOGETHER. Their disagreement is the cycle-5 HIGH this task's baseline-fragment shape guard closes, and it is the SAME disagreement plan 09.2-01 Task 2 guards on the screen's three seams
    - internal/gitconfig/baseline.go (the `InspectManagedBlockFile` / `InspectGitignoreFile` pair landed by plan 09.2-01 Task 1 §1 — its five refused shapes, its requested-name-only counting rule, and its returned body)
    - internal/filewriter/filewriter.go lines 24-61 (Write's returned backup path — the rollback source — INCLUDING its doc comment's explicit "backupPath is non-empty only when the target pre-existed" at :46, which is why the undo must branch on a recorded `existed` flag and remove rather than restore a file this fix created)
    - e2e/health_fixer_pty_e2e_test.go lines 60-95 (the all-green fixture that hand-copies the old thirteen patterns) and lines 100-212 (startHealthFixerPTY, openFixer's `5` + "every fix is previewed" wait, typeFixerConfirm, and TestHealthFixer_RealPTYFixerCeremonyWritesAndBacksUp's `f` → Enter → Enter recipe including its comment on why the SECOND Enter is what dispatches the write)
    - internal/tuikit/fixplans.go lines 30-67 (PlanFor — the excludesfile pair finding falls to the default branch, so its ceremony has no Destructive typed-confirm word)
    - cmd/gitid/wiring.go line 4184 (FixExcludesfile wired into the doctor deps — the production path the PTY case must exercise) and internal/doctor/checks/baseline.go lines 201-212 (gitignorePairFinding's fix descriptor)
    - e2e/gitignore_pty_e2e_test.go (this phase's own PTY suite from plan 09.2-01 — its seed helper and assertions)
    - Task 1 and Task 2 `<action>` blocks in this file
  </read_first>

  <files>
    cmd/gitid/wiring.go,
    cmd/gitid/wiring_test.go,
    e2e/health_fixer_pty_e2e_test.go,
    e2e/gitignore_pty_e2e_test.go
  </files>

  <behavior>
    Write these tests first.

    cmd/gitid/wiring_test.go:
    - `fixExcludesfile` against a home whose gitignore file has NO managed block writes the
      full curated seed and wires the excludesfile key — the pre-existing behavior, unchanged.
    - `fixExcludesfile` against a home whose gitignore file ALREADY has a populated managed
      block with user-edited content leaves that block's body byte-identical, while still
      wiring the excludesfile key in the baseline fragment. This is the anti-revert case.
    - `fixExcludesfile` against a home whose managed block exists but is empty re-seeds the
      curated defaults.
    - `fixExcludesfile` against a home whose gitignore file is MALFORMED (orphan sentinel)
      returns an error naming the file and writes nothing, rather than appending a second block
      into already-broken content.
    - `fixExcludesfile` against a home whose BASELINE FRAGMENT (`~/.gitconfig.d/00-baseline`)
      carries TWO complete `baseline` blocks returns the inspector's duplicate-block error naming
      that fragment, writes NOTHING — not the gitignore file either — and leaves every file under
      the hermetic home byte-identical. Seed the two blocks with DISAGREEING bodies (the first
      with no `excludesfile` line, the last with one) so the test fails loudly if the guard is
      dropped: without it, doctor classifies the finding from the LAST block
      (`indexBlocks`, `internal/gitconfig/baseline.go:129-135`) while `ReplaceBlock` patches the
      FIRST (`internal/filewriter/block.go:126-145`), so the fix reports success and leaves the
      finding unresolved. This is the cycle-5 HIGH both reviewers corroborated.
    - `fixExcludesfile` against a home whose baseline fragment carries an ORPHAN `baseline` BEGIN
      sentinel with no matching END returns the inspector's error and writes nothing. Without the
      guard `ListBlocks` returns no block, `body` stays empty, and `ReplaceBlock` APPENDS a freshly
      rendered baseline block into already-malformed content — assert no second `# BEGIN gitid
      managed: baseline` line exists in the fragment afterwards.
    - Refusal ordering is asserted, not incidental: in BOTH baseline-fragment refusal cases the
      `.gitignore_global` file is byte-identical too (and absent if it was absent), proving the
      fragment is inspected BEFORE the first write rather than after it — otherwise the fix would
      leave a written-but-unwired gitignore file behind on every refusal.
    - No test for cross-NAME nested markers is added here. That parser edge case was raised again
      in cycle 5 (codex-sol HIGH), re-traced against `internal/filewriter/block.go`, and downgraded
      to MEDIUM for this phase's safety property; it is covered by exactly ONE defensive test, at
      the inspector level, in plan 09.2-01 Task 1's `<behavior>`. The rationale lives in
      `09.2-01-PLAN.md`'s `<downgraded_findings>` section — do not duplicate the test here and do
      not redesign the inspector or the block scanner to reject the shape.
    - When the baseline-fragment write fails after the gitignore file was already rewritten AND
      that file PRE-EXISTED, the gitignore file is restored from its own timestamped backup and
      the returned error says so.
    - When the baseline-fragment write fails after the gitignore file was CREATED by this fix
      (it did not exist beforehand, so `filewriter.Write` returned an EMPTY backup path), the
      created file is REMOVED — not "restored from" a backup that never existed — the returned
      error says so, and the home is byte-identical to its pre-fix state including the absence
      of that file. This is the common fresh-machine shape and the case codex-sol's cycle-3 HIGH
      identified as unhandled.
    - Both rollback cases additionally assert that no stray `.bak.*` sibling is left behind.
    - Foreign content outside the block survives every case byte-identically.

    e2e/gitignore_pty_e2e_test.go:
    - `TestGitIgnore_RealPTYDoctorFixPreservesEditedBlock`: the fix is triggered through the
      REAL Fixer screen over the PTY — not by calling `fixExcludesfile` directly from the test,
      and not through a CLI flag. Exact mechanism, mirroring
      `TestHealthFixer_RealPTYFixerCeremonyWritesAndBacksUp`
      (`e2e/health_fixer_pty_e2e_test.go:180-204`):
      1. Seed a home whose ONLY fixable finding is the excludesfile pair finding — a managed
         baseline block present but carrying NO `core.excludesfile` line (Branch A from Task 2),
         and a `~/.gitignore_global` whose managed block is POPULATED with user-edited content
         (a default entry removed and a distinctive personal entry added). Seeding a single
         fixable finding is what makes the Fixer's first row deterministic.
      2. Send key `5` and wait for `"every fix is previewed"` — the suite's existing
         `openFixer` helper does exactly this; reuse it rather than re-implementing it.
      3. Assert the excludesfile pair finding is the SELECTED row BEFORE pressing anything, so a
         wrong row fails loudly instead of silently fixing something else. `mustSee` searches the
         WHOLE frame (`e2e/dummy_demo_e2e_test.go:144`), so `mustSee` on the finding's title
         alone proves only that it is on screen — assert the selection marker and the title
         together on the same rendered line (cycle-3 opencode-grok MEDIUM).
      4. Send `f` to open the fix ceremony. This finding routes to `PlanFor`'s default branch
         (`internal/tuikit/fixplans.go:60-66`), so `Destructive` is nil and there is NO typed
         confirm word — do NOT call `typeFixerConfirm`, which is specific to the SSH
         `clientb.github.com` contradiction.
      5. Send the carriage-return byte to confirm (ceremony state A → receipt), then a SECOND
         carriage-return byte to acknowledge the receipt, which is what actually dispatches the
         write under the Fixer's optimistic-receipt contract (documented at
         `e2e/health_fixer_pty_e2e_test.go:195-198`).
      6. Send key `6` to re-enter the Git Ignore screen and assert its `activate` re-read shows
         the user's distinctive entry and still does NOT show the default entry the user
         removed.
      7. Assert on real file bytes that the managed gitignore block body is byte-identical to
         its pre-fix content and that the baseline fragment now carries the excludesfile line.
      8. **Assert on DISK bytes and on the Git Ignore screen's re-read only — never on the fix
         ceremony's displayed target or diff.** `realBackend.FixPlanFor` falls back to `PlanFor`
         when the finding has no `Rewrite` (`cmd/gitid/wiring.go:1834-1836`), and this finding
         carries a `Fix` function rather than a `Rewrite`, so `PlanFor`'s default names
         `~/.ssh/config` (`internal/tuikit/fixplans.go:60-65`) while `Fix.Fn` still performs the
         real gitconfig repair. The ceremony's own target text is therefore knowingly wrong for
         this finding and is NOT this case's subject. Do NOT "fix" that by building a real
         `FixPlanFor` branch for the excludesfile finding — that is Fixer-surface scope, not
         GIGN-01, and would expand this phase. Record the caveat in the test's own comment so a
         future reader does not mistake the omission for a gap (cycle-3 opencode-grok MEDIUM).
  </behavior>

  <action>
Change `fixExcludesfile` so its gitignore half is conditional, its BOTH files are shape-guarded,
and its two writes are recoverable.

**Guard BOTH files' shape BEFORE the first mutation.** The function currently mutates two files
and inspects neither. Read them both up front — the gitignore target and the baseline fragment —
and run each through the inspector plan 09.2-01 Task 1 §1 landed, returning its error verbatim
before anything is written:

- `gitconfig.InspectGitignoreFile(gitignoreBytes)` for the target file. A malformed file is an
  error, not something to write over.
- `gitconfig.InspectManagedBlockFile(baselineBytes, "baseline")` for
  `~/.gitconfig.d/00-baseline`, and again immediately before that fragment's own
  `filewriter.Write`, after the fragment has been re-read under whatever ordering the final code
  uses. Two inspections, same call, same fail-closed contract as the screen's three seams.

Then **delete the first-match block-body loop entirely** and take the block body from the
inspector's returned body instead. This is the load-bearing half of the fix, not a refactor. Today
the function ranges over `filewriter.ListBlocks(content)`, assigns `body` from the first element
whose `Name` is `baseline`, and `break`s — see the live code at the `<read_first>` citation. That
is a name-agnostic scanner's FIRST match, while the doctor finding that triggered this fix
was classified from `ReadBaselineState`'s `indexBlocks` — a name-keyed map where the LAST
duplicate wins (`internal/gitconfig/baseline.go:129-135`) — and the write that follows uses
`replaceBlockWith`, which claims the FIRST complete pair and `break`s
(`internal/filewriter/block.go:126-145`). A duplicate-`baseline` fragment can therefore produce a
finding from one block, patch a different one, report success, and leave the finding unresolved;
an orphan `baseline` BEGIN leaves `body` empty and makes `ReplaceBlock` APPEND a fresh block into
already-malformed content. Refusing on shape first, then reading the body through the same
name-scoped inspector the write is guarded by, makes the read and the write mean the same bytes.

Do NOT "fix" this by changing `replaceBlockWith`'s selection rule: it is the shared core behind
every managed-block write in this repository and `internal/filewriter/block.go` must be
byte-unchanged by this plan — the identical constraint and rationale plan 09.2-01 records for the
screen path. Addresses the cycle-5 HIGH both reviewers corroborated (codex-sol Plan 09.2-02
Concerns; opencode-grok "New finding"), whose citations the orchestrator independently
re-confirmed against `cmd/gitid/wiring.go:4355-4382`.

The refusals must precede BOTH writes, not sit between them: a fragment refusal after the
gitignore write would leave a written-but-unwired `.gitignore_global` behind, which is the same
half-applied state the rollback rules below exist to prevent.

When the gitignore file's managed block exists and its body has at least one non-blank,
non-comment line, skip the gitignore write entirely and proceed to the excludesfile-key half
only. Otherwise seed `gitconfig.DefaultGitignorePatterns()` exactly as today.

Make the two writes recoverable in BOTH directions. Before each write, record
`{path, preimage bytes, existed bool}` — `existed` from a stat, `preimage` from the read the fix
already performs. Then, if the second write fails, undo the first from that record before
returning an error naming both the original failure and the undo.

**The undo branches on `existed`, never on whether a backup path came back.** `filewriter.Write`
returns a backup path only when the target pre-existed — its own doc comment says so in those
words (`internal/filewriter/filewriter.go:46-52`), and the stat-then-`backupExistingTarget`
sequence at `:47-55` is where that happens. `fixExcludesfile` writes `.gitignore_global` FIRST,
and on the fresh, never-configured machine this fix exists to repair, that file does not exist —
so the previous instruction ("restore the first from that backup") was a silent no-op in exactly
the most common case, leaving a created-but-unwired `.gitignore_global` behind after a failed
repair. The two shapes:

- **existed = true** → restore the file from the timestamped backup `filewriter.Write` returned.
- **existed = false** → the fix CREATED this file; there is no backup and nothing to restore.
  REMOVE the file so the home returns to its genuine pre-fix state.

Use the identical rule as plan 09.2-01 Task 2's `CommitGlobalGitIgnore`, so the two write paths
in this phase undo the same way and a reader does not have to check which is which. Addresses
review concerns: the fix is a non-atomic two-file operation whose first mutation is never rolled
back (codex-sol cycle-1 MEDIUM) and whose stated rollback is impossible for a newly created file
(codex-sol cycle-3 HIGH).

Extend the function's doc comment to record four things: that both files are shape-inspected
before the first mutation and that the baseline body comes from the inspector rather than a
first-match `ListBlocks` scan, because the doctor finding that triggers this fix is classified
last-duplicate-wins while the writer is first-duplicate-wins and the two must be made to mean the
same block; that the managed gitignore content is
user-owned as of GIGN-01, and that a fix which re-seeded it would silently discard a write the
user had already reviewed and confirmed — the exact class of destructive fix loop this codebase
has hit before; that the undo-on-second-failure exists so a failed repair never leaves a wired
key pointing at content that was not written, and that it has TWO shapes (restore a pre-existing
target, remove one this repair created) because `filewriter.Write` returns no backup path for a
file that did not exist; and — explicitly, because it is a
deliberate trade-off and not an oversight — that this preservation means a machine whose
managed block was seeded from the OLD thirteen-pattern catalog will never be upgraded to the
new curated set by `doctor --fix`. The upgrade path for those machines is the Reset action on
the Global Git Ignore screen (plan 09.2-03), and doctor's Check 4 informational finding names
it. Addresses review concern: preserve-if-populated freezes old machines on the old catalog and
that was not written down anywhere (xai-grok MEDIUM).

Update `cmd/gitid/wiring_test.go`'s existing default-pattern assertion for the extended
catalog, comparing against the comment-free entry view where it asserts on parsed content.

Update `e2e/health_fixer_pty_e2e_test.go`'s all-green fixture so it seeds the managed gitignore
block from the catalog instead of the hand-copied thirteen-entry literal, keeping the fixture
green by construction as the catalog evolves.

Add the doctor-fix-then-reopen case to this phase's own PTY suite, driving the fix through the
real Fixer screen with the exact key sequence enumerated in `<behavior>` (`5` → assert the row
→ `f` → Enter → Enter → `6`). Addresses cycle-2 review concern: the previous revision did not
say HOW the fix was triggered, leaving the executor to choose between the Fixer key, a CLI
flag and a direct `fixExcludesfile` call (opencode-grok MEDIUM). Only the Fixer-key path
exercises the production wiring at `cmd/gitid/wiring.go:4184`, which is the point of the case.
  </action>

  <verify>
    <automated>go test ./cmd/gitid/... -count=1</automated>
    <automated>make test-e2e</automated>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./...</automated>
    <automated>make lint</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./cmd/gitid/ -run 'FixExcludesfile' -count=1` passes with every case from `<behavior>`, including the two baseline-fragment refusal cases.
    - The anti-revert test asserts on the block body's exact bytes before and after the fix.
    - There are TWO rollback tests, one per undo shape: the pre-existing-file case asserts the first-written file's bytes are back to their pre-fix state, and the newly-created-file case asserts the file does NOT exist afterwards. Neither leaves a stray `.bak.*` sibling.
    - `grep -n "existed" cmd/gitid/wiring.go` shows the undo branches on whether the target pre-existed, not on whether `filewriter.Write` returned a non-empty backup path — the latter is empty for a new file (`internal/filewriter/filewriter.go:46-52`).
    - The doctor-fix PTY case's assertions are on disk bytes and the re-read screen only. Prove no assertion was written against the dummy `PlanFor` ceremony target with a COMMENT-FILTERED count — `grep -v '^[[:space:]]*//' e2e/gitignore_pty_e2e_test.go | grep -c 'ssh/config'` returns 0 — so the explanatory comment this case is required to carry cannot invalidate its own gate.
    - `grep -n "InspectGitignoreFile" cmd/gitid/wiring.go` shows the fix refuses a malformed file through the same inspector the screen uses.
    - The Fixer write path is shape-guarded on the baseline fragment too: `awk '/^func fixExcludesfile/,/^}/' cmd/gitid/wiring.go | grep -v '^[[:space:]]*//' | grep -c 'InspectManagedBlockFile'` returns at least 2 — real calls before the first mutation and again immediately before the fragment's own write, with comment lines filtered so explanatory prose cannot satisfy the gate. A repo-wide `grep -c` would be satisfied by plan 09.2-01's three SCREEN seams alone, which is exactly the gap cycle 5 caught, so this criterion is function-scoped by construction.
    - The first-match scan is GONE, not merely wrapped: `awk '/^func fixExcludesfile/,/^}/' cmd/gitid/wiring.go | grep -v '^[[:space:]]*//' | grep -c 'ListBlocks'` returns 0 — the block body comes from the inspector, which is what makes the read and the write mean the same block. Comment lines are filtered because the doc comment this task requires is allowed to explain what was removed and must not invalidate its own gate; the `awk` range starts at the `func` line, so the function's own doc comment is outside it either way.
    - `git diff --stat internal/filewriter/block.go` is empty: the duplicate-block hazard is fixed by failing closed on file shape, never by changing the shared writer's first-match selection rule.
    - The two baseline-fragment refusal tests assert that `.gitignore_global` is byte-identical (or still absent) afterwards, proving the fragment is inspected BEFORE the first write.
    - `TestGitIgnore_RealPTYDoctorFixPreservesEditedBlock` triggers the fix through the Fixer screen's `f` key (not a direct `fixExcludesfile` call and not a CLI flag), and `grep -n "typeFixerConfirm" e2e/gitignore_pty_e2e_test.go` returns nothing — this finding is non-destructive and has no typed confirm word.
    - `make test-e2e` passes, including the health/fixer suite and this phase's suite.
    - `TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./...` passes.
    - `make lint` reports 0 issues.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A guarded branch and a backup-restore path inside one fix function; removing them restores
    the previous behavior exactly.
  </reversibility>

  <done>
    Doctor's excludesfile fix seeds a missing block, preserves an existing one, refuses a
    malformed one — the gitignore file AND the baseline fragment, through the same inspector, before
    it mutates anything — patches the same baseline block it read, and undoes a half-applied repair
    in both directions, restoring a file that already existed and removing one it created itself;
    and every suite that seeded the old thirteen-entry catalog is green against the new one.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

| Kind | Symbol / path | Where |
|------|---------------|-------|
| Func | `gitconfig.DefaultGitignoreEntries() []string` | internal/gitconfig/baseline.go |
| Func (changed) | `gitconfig.DefaultGitignorePatterns()` — grouped curated seed incl. comment headers | internal/gitconfig/baseline.go |
| Func | managed-block-populated / excludesfile-set / excludesfile-points-at-managed-target predicates (replacing `matchesDefaultGitignorePatterns`) | internal/doctor/checks/baseline.go |
| Behavior | The gitignore pair check becomes five explicit branches; the empty-block branch keeps a warning AND the pair fix | internal/doctor/checks/baseline.go |
| Behavior | Baseline check 4 becomes informational, edit-tolerant, and compares the comment-free view | internal/doctor/checks/baseline.go |
| Copy fix | The gitignore-pair and Check 4 suggested-fix texts stop naming a retired CLI command | internal/doctor/checks/baseline.go |
| Behavior | `fixExcludesfile` preserves an existing populated managed block, refuses a malformed file, and restores the first write when the second fails | cmd/gitid/wiring.go |
| Behavior | `fixExcludesfile` shape-inspects the baseline fragment through `InspectManagedBlockFile(..., "baseline")` before the first mutation and again before its own write, and sources the block body from the inspector instead of a `ListBlocks` first-match scan | cmd/gitid/wiring.go |
| File path | `~/.gitignore_global` seeded content (new curated set) | user's home |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor fix -> user's home | An automated fix rewrites a file the user has already curated |
| catalog -> every write caller | One list drives `doctor --fix` and the new screen |
| doctor findings -> user's belief about their machine | A relaxed check can make a broken machine look healthy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.2-06 | Tampering | `fixExcludesfile` | high | mitigate | The fix skips the gitignore write when a populated managed block already exists, so an automated repair can never discard a user's reviewed content; proven by a byte-exact before/after test |
| T-09.2-07 | Denial of Service | doctor findings | medium | mitigate | The exact-match predicate that turned every user edit into a permanent error/warning pair is removed; a populated block reports as configured, so the machine cannot be pushed into a permanent unhealthy state by a legitimate edit |
| T-09.2-08 | Information Disclosure | curated seed contents | medium | mitigate | The env-file group ignores `.env` and `.env.*` globally so secrets are not committed by accident, with the negation scoped to the example file only; ordering asserted by test |
| T-09.2-09 | Tampering | over-broad seed | medium | mitigate | Project-scoped build-output patterns are deliberately excluded from a MACHINE-wide ignore file so repositories that track those paths are not silently broken; the narrower scratch-directory exception and its reason are recorded in the catalog's doc comment |
| T-09.2-18 | Spoofing | relaxed doctor predicate certifying a nonfunctional configuration | high | mitigate | The pair check is split into five explicit branches so an unset key, a wrong target, a dangling pointer and an empty managed block each keep their own finding — and the empty-block branch keeps the executable pair fix; each branch has its own test (both reviewers, HIGH) |
| T-09.2-20 | Tampering | non-atomic two-file repair | medium | mitigate | `fixExcludesfile` records `{path, preimage, existed}` before each write and undoes the first when the second fails — RESTORING a pre-existing target from its timestamped backup, or REMOVING a target the repair itself created. Branching on `existed` rather than on a non-empty backup path is required: `filewriter.Write` returns an empty backup for a new file (`internal/filewriter/filewriter.go:46-52`), which is the common fresh-machine shape for `.gitignore_global` and the case the previous revision silently left half-applied (codex-sol cycle-3 HIGH) |
| T-09.2-25 | Tampering | `fixExcludesfile`'s baseline-fragment write | high | mitigate | The Fixer path is guarded by the SAME `gitconfig.InspectManagedBlockFile(content, "baseline")` inspector plan 09.2-01 put on the screen's three seams — called before the first mutation and again immediately before the fragment's write — and its block body now comes from the inspector instead of a `ListBlocks` first-match scan. Without it, a duplicate fragment lets doctor classify a finding from the LAST block (`indexBlocks`, `internal/gitconfig/baseline.go:129-135`) while `ReplaceBlock` patches the FIRST (`internal/filewriter/block.go:126-145`), reporting success while leaving the finding unresolved; an orphan BEGIN makes `ReplaceBlock` append a fresh block into malformed content. Fail-closed, byte-identical-home tests for both shapes; `internal/filewriter/block.go` unchanged (cycle-5 HIGH, corroborated by both reviewers) |
| T-09.2-SC | Tampering | dependencies | low | accept | No package-manager install occurs in this plan; no new module is added |
</threat_model>

<verification>
- `TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./...` passes.
- `make lint` reports 0 issues.
- `make test-e2e` passes.
- `make gate-visual-regression` passes. (Note, corrected from the previous revision: plan
  09.2-01 DID change every screen's header nav strip, so the claim that this plan touches no
  approved screen's rendered text is only true relative to plan 09.2-01's baseline. Real and
  dummy render through the same `tuikit` frame, so the change lands identically on both sides
  of every region comparison — run the gate to prove it rather than assuming it.)
</verification>

<success_criteria>
- The curated seed contains every pattern GIGN-01 names and is a strict superset of the
  previous set.
- A user who removes a default pattern and confirms the write is not told their machine is
  misconfigured, and nothing silently puts the pattern back.
- A machine with no global ignore file, an empty managed block, a wrong excludesfile target, or
  a dangling pointer is still diagnosed and — where gitid can repair it — still fixable.
- A machine whose baseline fragment carries a duplicate or malformed managed block is refused by
  the automatic repair with nothing written, on the same terms and through the same inspector as
  the screen's own write path.
</success_criteria>

<output>
Create `.planning/phases/09.2-global-git-ignore-management/09.2-02-SUMMARY.md` when done.
</output>
