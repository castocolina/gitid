---
phase: 09.2
reviewers: [codex-sol, xai-grok]
reviewed_at: 2026-08-31T15:00:00Z
plans_reviewed: [09.2-01-PLAN.md, 09.2-02-PLAN.md, 09.2-03-PLAN.md]
models:
  codex-sol: "gpt-5.6-sol (reasoning=high)"
  xai-grok: "xai/grok-4.6 (reasoning=high)"
model_sources:
  codex-sol: "pinned"
  xai-grok: "pinned"
---

# Cross-AI Plan Review — Phase 09.2

> Note: this review invoked `codex-sol` (via the `codex` adapter, model `gpt-5.6-sol`) and
> `xai-grok` (via the `opencode` adapter, model `xai/grok-4.6`) as independent reviewer
> instances (ADR-1517). Cycle 1 of the plan-review-convergence loop.

## Codex Review (codex-sol)

# Cross-AI Plan Review

**Overall verdict: REVISION REQUIRED.** The architecture is generally sound—shared write machinery, an injected backend seam, real-PTY coverage, and staged delivery—but the plans are not execution-ready. The most important gaps are: the screen does not ensure `core.excludesFile` is wired, malformed managed blocks can be made worse, previewed content can become stale before commit, the doctor predicate would hide broken wiring, and the new screen is absent from the mandatory visual-regression inventory.

## Plan 09.2-01 — Read/review/write tracer

### Summary

This is a strong vertical tracer with appropriate unit, wiring, and real-PTY coverage. Reusing the existing managed-block writer is the right implementation direction. However, the proposed single-file write does not produce a functional global ignore configuration on an unconfigured machine, and several safety boundaries need explicit fail-closed behavior.

### Strengths

- The planned write chain reuses proven preservation and backup behavior. `ReplaceBlock` replaces only the matching block while retaining surrounding bytes, and `Write` performs an atomic rename with a timestamped backup for an existing target. `internal/filewriter/block.go:111-162`, `internal/filewriter/filewriter.go:24-61`
- The navigation changes cover the real integration surface: the current app has a fixed five-screen array, numeric routing, arrow clamping, mouse header hit-testing, and a matching screen constructor array. Expanding all of these together avoids an unreachable or partially wired screen. `internal/tuikit/app.go:134-179`, `internal/tuikit/app.go:452-473`, `internal/tuikit/app.go:488-542`, `internal/tuikit/app.go:701-713`
- Separating compact header labels from breadcrumb labels is justified by the fixed 100-column frame. Header rendering and hit-testing currently derive from the same tab text, so preserving that shared source maintains their invariant. `internal/tuikit/frame.go:26-75`, `internal/tuikit/frame.go:224-283`
- The proposed `commitPending` guard follows an existing sound pattern: unsolicited or duplicate commit messages are ignored unless an asynchronous ceremony commit is actually active. `internal/tuikit/globalgit.go:145-195`, `internal/tuikit/ceremony.go:184-311`
- Real-PTY tests are appropriate for this feature. The existing harness builds the real binary, provides a hermetic home, and supports byte-level before/after assertions rather than merely testing model state. `e2e/harness_test.go:47-149`, `e2e/harness_test.go:481-537`, `e2e/global_git_pty_e2e_test.go:158-189`

### Concerns

- **HIGH — The screen writes only half of the functional configuration.** The repository treats `core.excludesFile` and `.gitignore_global` as a coordinated pair, and the existing fixer explicitly writes both halves. The planned fresh-home path creates `.gitignore_global` but does not wire Git to use it, so confirmation can succeed while the global ignore remains ineffective. `internal/doctor/checks/baseline.go:103-135`, `cmd/gitid/wiring.go:4328-4383`
- **HIGH — Malformed or duplicate managed blocks are not handled safely.** `ListBlocks` silently omits an incomplete block, while `ReplaceBlock` appends a new block if it cannot find a complete match. With an orphan BEGIN sentinel, the proposed screen can report "no managed block" and append another block into malformed content. With duplicate complete blocks, `ReplaceBlock` replaces only the first. `internal/filewriter/block.go:26-62`, `internal/filewriter/block.go:128-153`
- **HIGH — The reviewed diff can become stale before confirmation.** The apply plan reads the current file and computes a diff, but commit later re-reads the file through `WriteGlobalGitignore`. `txMu` only protects gitid's own mutations; it does not prevent an external edit between preview and commit. The repository already has a stronger preview-token pattern that rejects changed configuration at commit time. `cmd/gitid/wiring.go:101-111`, `cmd/gitid/wiring.go:2548-2565`, `cmd/gitid/wiring.go:2601-2604`, `cmd/gitid/wiring.go:2647-2669`
- **MEDIUM — Planner-error behavior is incomplete.** Sentinel rejection occurs in `GlobalGitIgnoreApplyPlan`, before a ceremony exists, but the specified tests cover only state-loading errors. The existing Global Git screen explicitly retains apply-plan errors and refuses to open its ceremony. Without equivalent handling, hostile or invalid text may fail with no useful receipt. `internal/tuikit/globalgit.go:381-385`, `internal/tuikit/globalgit.go:582-609`
- **MEDIUM — Backup receipts can become misleading if the Global Git fallback is copied literally.** The existing analog synthesizes a preview backup path when `Backups` is empty, but the ceremony replaces placeholders only when the commit returns real backups. For a new file or byte-identical write, that can leave a receipt implying a backup exists when none was created. `internal/tuikit/globalgit.go:381-426`, `internal/tuikit/ceremony.go:284-293`, `internal/gitconfig/baseline.go:481-483`

### Suggestions

- Decide explicitly whether this screen owns the complete `core.excludesFile` pair. Prefer either: include the baseline-fragment update in the same reviewed transaction; or fail closed when wiring is absent/wrong and provide a clear route to Fixer.
- Validate the existing file before presenting defaults: reject orphan sentinels, mismatched sentinels, and duplicate `gitignore` blocks.
- Return a preview token containing the exact preimage hash or bytes. At commit, verify the file is unchanged before writing; otherwise require the user to review again.
- Add tests for apply-plan normalization errors, malformed existing blocks, duplicate blocks, and external mutation between preview and confirmation.
- Treat empty backup lists as valid. Assert that a no-op second confirmation creates no additional backup and never displays a fictitious backup path.

### Risk Assessment

**HIGH.** The implementation direction is good, but the current plan can report success for an ineffective configuration, worsen malformed files, or write content different from the reviewed candidate.

## Plan 09.2-02 — Catalog and doctor/fixer compatibility

### Summary

The catalog work is well structured, especially the split between rendered lines and comparison entries. Making doctor tolerant of intentional edits is also correct. The principal problem is that the proposed "populated" predicate conflates block content with `core.excludesFile` wiring and would suppress real configuration errors.

### Strengths

- `DefaultGitignoreEntries` is the right companion abstraction. The current read path already strips blank and comment lines, so using the same filtered representation prevents comment headers from making all comparisons fail. `internal/gitconfig/baseline.go:194-204`
- The planned negation-order test is important because gitignore negations depend on later lines overriding earlier wildcard matches. The current renderer preserves supplied ordering rather than sorting entries. `internal/gitconfig/baseline.go:407-420`
- Reclassifying deliberate catalog deviations as informational follows an established repository policy: user-set values that differ from recommendations are reported without an executable fix. `internal/doctor/checks/baseline.go:170-190`
- Inspecting the existing managed block before the doctor fix is a sound way to prevent destructive reseeding. `ListBlocks` exposes the exact body separately from foreign content, enabling a byte-preserving skip decision. `internal/filewriter/block.go:9-13`, `internal/filewriter/block.go:31-62`

### Concerns

- **HIGH — The plan relies on a nonexistent production command.** The current CLI explicitly archives the old `baseline` command surface, and the production wiring leaves `SetupBaseline` unset because no such v1 command exists. Therefore the truth that "`gitid baseline setup`, doctor fix, and the screen share one catalog" is not source-valid. `cmd/gitid/main.go:69-83`, `cmd/gitid/wiring.go:4064-4069`
- **HIGH — `managedGitignorePopulated` would hide broken wiring.** The current check only complains about an empty `core.excludesFile` when the gitignore predicate is false. Replacing the exact predicate with "block is populated" means an edited block plus a missing key can be declared configured. Likewise, a wrong but existing excludes-file path currently passes because the check tests existence, not equality with the expected target. `internal/doctor/checks/baseline.go:115-135`, `internal/doctor/checks/baseline.go:193-199`
- **MEDIUM — The doctor fix remains a non-atomic two-file operation.** It currently writes `.gitignore_global` first and then patches the baseline fragment. If the second write fails, the first mutation remains and its returned backup is not used for rollback. Preserving populated blocks reduces exposure but does not address missing or empty blocks. `cmd/gitid/wiring.go:4357-4381`
- **MEDIUM — Synthetic doctor-state tests may miss behavior in the real reader.** `ReadBaselineState` returns before parsing `.gitignore_global` when the baseline/include setup is absent or incomplete. A test that manually supplies `GitignorePatterns` can therefore disagree with the real doctor path. `internal/gitconfig/baseline.go:56-115`

### Suggestions

- Correct the plan and context to remove `gitid baseline setup` as a current caller. Restoring a retired CLI command would be unrelated scope expansion.
- Split doctor validation into independent conditions: (1) `core.excludesFile` is present and resolves to the expected path, (2) the expected file contains one valid, populated managed block, (3) missing curated entries are informational only.
- Add cases for: populated edited block plus missing `core.excludesFile`; populated block plus wrong-but-existing target; correct target plus edited block; real `ReadBaselineState` integration, not only constructed states.
- Make the two-file fixer transactional, or write the baseline fragment first and restore it if the gitignore write fails.

### Risk Assessment

**HIGH.** The catalog changes are low risk, but the proposed doctor predicate can incorrectly certify a nonfunctional configuration, which conflicts directly with the product's health guarantees.

## Plan 09.2-03 — Editable textarea and phase closure

### Summary

The editor interaction model matches the approved decision and correctly builds on the tracer. The unit and PTY behaviors cover the essential edit/reset/apply workflow. The plan nevertheless omits non-key textarea events and, more importantly, does not integrate the screen into the repository's mandatory UI contract and automated visual gate before closing GIGN-01.

### Strengths

- Screen-local key handling happens before global shortcuts, so an editing state that consumes typed keys can reliably prevent `a` and `r` from triggering browse actions. `internal/tuikit/app.go:372-450`
- `capturesKeys` is already connected to the reserved input footer contract, making it appropriate for hiding normal global help while the editor or ceremony owns input. `internal/tuikit/app.go:596-643`, `internal/tuikit/frame.go:83-101`
- The frame centrally clips and pads screen bodies, and explicitly sizing the textarea adds internal scrolling rather than relying only on final-frame truncation. `internal/tuikit/frame.go:405-445`
- The planned PTY cases verify the important outcomes on disk: edited-line persistence, deletion persistence, reset-without-write, foreign-content preservation, and backup creation. `e2e/global_git_pty_e2e_test.go:158-189`, `e2e/harness_test.go:78-149`

### Concerns

- **HIGH — The screen is missing from the authoritative visual-regression inventory.** `RequiredScreenSpecs` is documented as the authoritative real/dummy screen set, but its merge currently includes only create flow, Git, identity manager, Global SSH, Global Git, health/fixer, and upload specs. The gate loops only over that registry, so `make gate-visual-regression` can pass without checking Global Git Ignore at all. `internal/screenshot/createflow.go:558-585`, `cmd/gitid/gate_visual_regression_test.go:383-462`
- **HIGH — Required UI governance is absent before requirement closure.** Milestone policy requires a UI specification for a TUI surface, automated real-binary/dummy parity, and a UI review. Plan 03 instead captures ad hoc frames and immediately marks GIGN-01 complete. `.planning/ONESHOT.md:16-40`, `.planning/ONESHOT.md:65-73`, `.planning/ONESHOT.md:88-92`
- **MEDIUM — Real paste events may be dropped.** The app sends key messages through `handleKey` but sends non-key messages, including `tea.PasteMsg`, through `handleMsg`. The plan only describes forwarding key events to the textarea and handling commit messages in `handleMsg`; Bubbles textarea consumes paste through its `Update` method. `internal/tuikit/app.go:333-360`, `charm.land/bubbles/v2@v2.1.0/textarea/textarea.go:1202-1225`
- **MEDIUM — "Written verbatim" conflicts with the planned normalizer.** Plan 01 right-trims every line and removes trailing blank lines before preview and commit. The underlying block writer otherwise preserves body content except for canonical trailing newline handling. Either the UI promise or normalization contract must change, particularly for user-authored whitespace. `internal/filewriter/block.go:184-199`

### Suggestions

- Add a Global Git Ignore screenshot-spec registry and merge it into `RequiredScreenSpecs`; include browse, defaults, editing, reset, review, error, and receipt states for both real and dummy binaries.
- Produce the required UI specification before implementation and a UI review before changing the requirements ledger.
- While editing, forward `tea.PasteMsg` and textarea cursor/blink messages through `textarea.Update`; return the command from `textarea.Focus()`.
- Add unit and PTY coverage for bracketed multi-line paste.
- Define persistence as either exact text or canonicalized text. If canonicalized, show the canonicalized candidate in the editor or clearly state the transformation before review.
- Mark GIGN-01 complete only after the registered visual gate, full test suite, and UI review pass.

### Risk Assessment

**HIGH.** The core editor should be straightforward, but the current plan could close the phase without the required UI validation, and a normal terminal paste path may not work.

---

## OpenCode Review (xai-grok)

I'll verify each plan against the real code rather than the plan text. Loading the review skill and reading the referenced sources first.

## Review: `09.2-01-PLAN.md` · `09.2-02-PLAN.md` · `09.2-03-PLAN.md`

**Document type:** GSD · plan
**Grounded against:** current `internal/` + `cmd/gitid/` + `e2e/` (plans are on disk under `.planning/phases/09.2-global-git-ignore-management/`)

# Plan 09.2-01 — tracer (nav → ceremony → write)

## Summary

The tracer is the right slice: a sixth tab, a three-method planner copied from `GlobalGitPlanner`, and a write that reuses `WriteGlobalGitignore` → `ReplaceBlock` → `filewriter.Write`. The cited analogs exist at the claimed lines. Two holes will bite execution: the apply-plan composition is underspecified versus `RenderGitignoreBlock`, and shortening existing header labels (`Global SSH`/`Global Git` → `SSH`/`Git`) has a larger test blast radius than `files_modified` lists.

## Strengths

- Seam copy is exact: `GlobalGitPlanner` is three methods at `internal/tuikit/backend.go:148-160`; `Backend` embeds planners with the "real backend must NOT embed the Noop" comment at `backend.go:304-325`; `newScreens` is a fixed `[5]screenModel` at `app.go:706-713`.
- Header budget math is real. Current labels at `frame.go:75` plus `headerTabText` (`frame.go:234-235`) cannot fit a sixth full name at `minFrameWidth=100` (`frame.go:29`) without eating `healthChip` (`frame.go:211-222`). Short nav vs full breadcrumb is the correct split.
- Write chokepoint is real: `WriteGlobalGitignore` at `internal/gitconfig/baseline.go:473-490` already does `ReplaceBlock(..., "gitignore", RenderGitignoreBlock(patterns))`, skips byte-identical writes, and returns the backup from `filewriter.Write` (`internal/filewriter/filewriter.go:47-61`).
- Path rule is real: `backupSuffixPreview` at `cmd/gitid/wiring.go:77`; `realBackend.home` at `wiring.go:87`; `GlobalGitApplyPlan` already resolves targets from the backend, never from the view (`wiring.go:2326-2361`).
- Ceremony analog is real: `ceremonyConfig.Async`/`PreviewDiff` at `internal/tuikit/ceremony.go:64-100`; global-git `a` → plan → ceremony at `globalgit.go:582-608`; confirm is first Enter / `y` (`ceremony.go:266-273`); e2e confirm is `dummyKeyEnter` (`e2e/global_git_pty_e2e_test.go:175`).
- T-09.2-01 matches the parser: `BeginPrefix`/`EndPrefix` at `internal/filewriter/block.go:22-23`; nested BEGINs are ignored (`block.go:41-46`).

## Concerns

- **HIGH — ApplyPlan vs Commit can disagree.** `WriteGlobalGitignore` always composes with `RenderGitignoreBlock(patterns)` (`baseline.go:479`). Plan 01 Task 1 action 8 says ApplyPlan runs `NormalizeGitignoreLines` then `filewriter.ReplaceBlock(existing, "gitignore", ...)` without requiring that same render. If the `...` is the raw textarea/string, `globalsTextDiff` (`wiring.go:2730`) previews bytes Commit will not write. That falsifies T-09.2-04 and plan 03's T-09.2-10.
- **HIGH — ApplyPlan error path is missing.** Sentinel rejection is the whole T-09.2-01 mitigation, but `a` is specified only as "calls ApplyPlan and opens the ceremony." The analog fails closed: `baselineCeremonyFor` error is stored in `optionsErr` and no ceremony opens (`globalgit.go:601-604`). Plan 03 later claims "surfaces the rejection as a ceremony error" — 01 and 03 disagree, and 01 has no test for it.
- **HIGH — `appliedContent` is never assigned.** Fields include it; confirm commits it; `a` is not told to capture it. Zero-value confirm writes `""`.
- **HIGH — header-label blast radius is under-listed.** `tabNavLabels` changes `[2] Global SSH` / `[3] Global Git` on every screen. That breaks: `internal/tuikit/app_test.go:30` (`TestNewAppRendersTheFrame`); `internal/tuikit/frame_test.go:79`; `TestTabsAndPaletteReachAllPrimaryViews` (`app_test.go:94-109`: keys 1–5, `len(paletteEntries) != 6`); `TestNewScreensExhaustiveSwitchOverTabID` (`app_test.go:124-135`); and **`e2e/ui_pty_e2e_test.go:412-418`**, which waits for `"Global SSH"` / `"Global Git"` on the **Identities** launch frame. That file is **not** in `files_modified`. Task 2's `make test-e2e` will fail. Breadcrumb `Global SSH › Options` (`dummy_demo_e2e_test.go:199`) still works because it uses full `tabLabels`; the boot loop does not.

## Suggestions

- Lock ApplyPlan to `ReplaceBlock(existing, "gitignore", RenderGitignoreBlock(lines))` after normalize — same call as `WriteGlobalGitignore`.
- On ApplyPlan error: do not open a ceremony; render the error; keep `a` inert (copy `globalgit.go:601-604`). Add that test in 01, not 03.
- Set `appliedContent` at `a`, the string ApplyPlan just previewed.
- Add `e2e/ui_pty_e2e_test.go` to `files_modified`; assert short nav labels (`[2] SSH`) and keep breadcrumb full names.
- Have FixtureBackend set `DefaultContent` now (03 Reset depends on it).

## Risk Assessment

**MEDIUM-HIGH.** Architecture is copy-paste of a proven path, but the unlisted PTY header assertion and the unspecified ApplyPlan composition are execution-failing, not polish.

---

# Plan 09.2-02 — catalog + doctor honesty

## Summary

The diagnosis is correct and evidence-backed: doctor still demands an ordered, comment-free exact match against `DefaultGitignorePatterns()`, so adding comment headers without an entries view plus an edit-tolerant check would recreate the reserved-block false-positive loop. The preserve-if-populated `fixExcludesfile` change is the right chokepoint. The Task 2 **action** does not implement the Task 2 **behavior** for an empty/absent managed block in an existing file.

## Strengths

- Current catalog is exactly the claimed 13 lines, including `.env`, at `internal/gitconfig/baseline.go:264-280`.
- Read path strips comments: `parseGitignoreBlockBody` at `baseline.go:196-204`; `ReadBaselineState` assigns that to `GitignorePatterns` at `baseline.go:110-113`. Check 4 then iterates `DefaultGitignorePatterns()` at `internal/doctor/checks/baseline.go:146-149`. Comment headers in the render list would be permanently "missing."
- `matchesDefaultGitignorePatterns` is byte-exact and ordered (`baseline.go:224-234`) and is used as "present" at `CheckBaseline:95` and `:116`. That cannot represent a user-edited block.
- `setDiffersFindings` is the right precedent: informational, "deliberate user choice," no Fix (`baseline.go:181-186`).
- `fixExcludesfile` today always rewrites the gitignore (`cmd/gitid/wiring.go:4357-4358`) before patching `core.excludesfile`. T-09.2-06 is a real bug once the screen can edit.
- All-green e2e fixture hand-copies the 13-pattern list (`e2e/health_fixer_pty_e2e_test.go:86-90`). Must move to the catalog.
- "Cannot ship catalog extension apart from doctor" is the right instinct.

## Concerns

- **HIGH — Task 2 action contradicts Task 2 behavior for empty blocks.** Behavior: absent/empty still produces the existing warning/error. Action: replace `matchesDefaultGitignorePatterns` with `managedGitignorePopulated` at both call sites. Check 2 only fires when `excludesFile == ""` or the file is missing (`baseline.go:118-134`). Content-emptiness of an **existing** file is Check 4 today (comment at `:125-128`). After the action, a wired `core.excludesfile` + existing file + empty/missing managed block emits **no pair finding and no Check 4**. That case also loses the only `FixExcludesfile` descriptor (`gitignorePairFinding` at `:201-211`). TDD tests in `<behavior>` would fail the `<action>` as written.
- **HIGH — "one commit boundary" vs per-task GSD commits.** Task 1 verify is only `go test ./internal/gitconfig/`. After Task 1, `health_fixer`'s 13-pattern file fails Check 4 (`baseline.go:146-164`) while Check 2 stays quiet (key set, file exists). Task 3 is what re-greens that fixture. An executor that commits Task 1 alone leaves `make test-e2e` red.
- **MEDIUM — preserve-if-populated freezes old machines on the 13-pattern set.** After Task 3, a populated old block is never upgraded to `.venv/` / `.env.*` / `!.env.example`. Doctor only informs. The new catalog reaches those machines only via 03 Reset. Not wrong given GIGN-01, but it is not written down.
- **MEDIUM — `tmp/` / `.tmp/` have the same global-untrack risk the plan cites for excluding `dist/`/`build/`.** Discretionary, but inconsistent with T-09.2-09.
- **LOW — CheckBaseline file header is already stale** (`baseline.go:30` still says Check 4 has a Fix descriptor; code has `Fix: nil` at `:163`).

## Suggestions

- For empty/absent managed body: keep a warning (existing Check 4 text/severity) **with** `FixExcludesfile`, and only demote Check 4 to informational+`Fix nil` when `managedGitignorePopulated` is true.
- Execute Tasks 1–3 as one commit, or make Task 1 verify include `./internal/doctor/...` and the health-fixer e2e so a split commit is impossible to land green.
- State explicitly that doctor `--fix` will not migrate existing 13-pattern blocks; Reset on the new screen is the upgrade path.

## Risk Assessment

**MEDIUM-HIGH.** Catalog split and anti-revert fix are the right design; the empty-block finding/fix gap is a doctor-contract defect if the action is followed literally.

---

# Plan 09.2-03 — textarea + Reset + evidence

## Summary

This is the right expansion of the 01 spine: `charm.land/bubbles/v2 v2.1.0` is already pinned (`go.mod:6`), `fieldEditing` in `globalgit.go:68-73,510-531` is the correct key-capture analog, and `capturesKeys` / `reservedFooterInput` (`app.go:30-34`, `frame.go:93-101`) already hide `q`/`?` while a pane owns keys. The PTY edit cases are the weak spot: they require real textarea navigation that the plan does not specify.

## Strengths

- D-02/D-03 are implemented as free text, not toggles; env-file coupling stays seeding-only.
- Reset is in-memory (`DefaultContent` on the view from 01) and re-entry re-reads disk — matches `globalGitModel.activate` (`globalgit.go:107-142`).
- `ctrl+p` is intercepted **before** the screen handler (`app.go:377-382`), so the advertised reserved-footer Ctrl+P still works while editing.
- T-09.2-12 is testable: browse `a`/`r` vs edit-mode insert.
- Closing GIGN-01 in both the §Q checkbox (`REQUIREMENTS.md:410`) and the status row (`REQUIREMENTS.md:653`), and recording D-02 vs ROADMAP's old "toggle each" wording (`ROADMAP.md:480`), is the right ledger work.
- Threat T-09.2-13 (Reset must not write) has a dedicated PTY case.

## Concerns

- **HIGH — PTY "append" / "delete a default line" has no key recipe.** Bubbles textarea focus starts at the beginning. "Append a distinctive user line" and "remove a default entry" need End/down/backspace sequences. `e2e/global_git_pty_e2e_test.go` only types into a **single-line** `textinput` after row selection (`:160-175`). Copying that structure does not make a multi-line editor testable. These five cases will flake or get implemented by cheating (pre-seed), which the plan forbids.
- **HIGH — Reset depends on `DefaultContent` that 01's dummy fixture never promises.** 01 action 7 is "frozen fixture content" + empty apply plan. 03 Reset does `textarea.SetValue(view.DefaultContent)` with zero backend calls. Dummy/stub zero `DefaultContent` makes Reset clear the buffer; unit tests against a stub that forgets the field go green for the wrong reason.
- **HIGH — Hostile sentinel through the editor is not a 03 test**, and 01 never specified the error UI. T-09.2-11 is therefore unproven on the path that actually accepts arbitrary keystrokes.
- **MEDIUM — unsaved edits discarded on tab switch with no prompt.** Matches global-git, but this screen is a full file body; worth one sentence in the footer/status so it is not a surprise.
- **MEDIUM — new screen is not added to `RequiredScreenSpecs` / `gate-visual-regression`.** Phase 6/7 registered surfaces in `cmd/gitid/gate_visual_regression_test.go`. 03 only writes phase `ui-frames/`. Verify still runs the gate; it may pass without ever capturing this screen in the product inventory.
- **LOW — `files_modified` lists `cmd/gitid/wiring.go` with no task that edits it.**

## Suggestions

- Specify PTY driving: Enter → End/Ctrl+End → `\n` + marker line → Esc → `a` → Enter. For delete: search to a known default line, then a counted backspace/kill.
- In 01 (preferred) or 03 stub/fixture: `DefaultContent` always equals `RenderGitignoreBlock(DefaultGitignorePatterns())`.
- Add one unit + one PTY case: paste a `BeginPrefix` line → apply errors → disk unchanged.
- If the gate must stay green without registering the screen, say so; otherwise add a ScreenSpec.

## Risk Assessment

**MEDIUM.** Component choice and key-capture design are sound; the PTY editor cases are the most likely autonomous-execution failure in the phase.

---

# Cross-plan

| Topic | Verdict |
|---|---|
| Wave order 01 → 02 → 03 | Correct: spine, then catalog/doctor, then editor |
| D-01 one catalog | Correct; 02 must land before 03 or `--fix` reverts edits (`wiring.go:4357`) |
| D-04 ceremony | Correct analog; **composition must be `RenderGitignoreBlock` in 01** or 03's "previewed bytes == disk bytes" is false |
| D-05 sixth top-level tab | Correct; short labels are necessary; **update `e2e/ui_pty_e2e_test.go:412`** |
| `core.excludesfile` | Gap across all three plans. Recipe at `recipes/gitconfig.recipe:17`; doctor wires the key in `fixExcludesfile` (`wiring.go:4376-4380`). The new screen writes only `~/.gitignore_global`. A machine that never ran baseline setup gets a file Git will not read. ROADMAP SC-1 names the key. At least document that the key remains doctor/baseline-owned, or include it in the ceremony. |
| Header chrome | 01 changes every screen's nav; 02's "no approved screen touched" is false; 03 frames will show the new chrome. Real vs dummy gate can still pass. |
| GIGN-01 close | Only 03 should tick REQUIREMENTS/ROADMAP; 01–02 must not. |

**Phase risk: MEDIUM-HIGH.** Do not execute until 01 names `RenderGitignoreBlock` on both preview and commit, 01 includes the ui-pty header test, and 02's empty-block doctor finding/fix is specified as a third branch rather than "populated at both call sites."

---

## Consensus Summary

Both reviewers, working independently against the live source tree, converged on the same core defects — a strong signal these are real, not hallucinated. Neither reviewer had access to the other's output.

### Agreed Strengths

- The write chain correctly reuses `WriteGlobalGitignore` → `ReplaceBlock` → `filewriter.Write`, the same proven backup/atomic-rename path Global Git already uses.
- The short-nav-label vs full-breadcrumb-label split for the sixth tab is the right response to the fixed 100-column frame budget.
- `DefaultGitignoreEntries`/comment-stripped-entries split (plan 02) correctly prevents comment headers from breaking doctor's pattern-match check.
- The preserve-if-populated `fixExcludesfile` change is the correct fix for the project's recurring reserved-block false-positive/revert loop.
- Real-PTY test strategy (byte-level before/after disk assertions, not just model-state assertions) matches the existing harness discipline.

### Agreed Concerns

- **`core.excludesFile` wiring gap (both reviewers, HIGH in codex-sol, cross-plan finding in xai-grok).** The new screen can write `.gitignore_global` while leaving `core.excludesFile` unset or wrong, producing a confirmed-but-ineffective configuration. Both reviewers independently traced this to the same code: `internal/doctor/checks/baseline.go` and `cmd/gitid/wiring.go`'s `fixExcludesfile`.
- **Doctor predicate correctness (both reviewers, HIGH).** Plan 09.2-02's proposed `managedGitignorePopulated` predicate, if substituted for the current check at both call sites as literally specified, would suppress real findings for an empty/absent block with the excludes-file key present. Both reviewers independently derived this from `internal/doctor/checks/baseline.go`'s exact check-numbering logic.
- **Header/nav change blast radius (xai-grok HIGH, codex-sol implicit in Concern 1).** Changing `[2] Global SSH`/`[3] Global Git` to short labels affects existing tests not listed in plan 01's `files_modified` — most concretely `e2e/ui_pty_e2e_test.go:412-418`, which asserts the full label text on the Identities launch frame.
- **Plan 09.2-03 skips required UI governance before closing GIGN-01 (codex-sol HIGH; xai-grok MEDIUM as "not registered in gate-visual-regression").** The new screen is not added to `RequiredScreenSpecs`/`gate-visual-regression`'s authoritative inventory, so the mandatory visual gate can pass without ever exercising it, and the plan proposes closing the requirement without the UI-spec/UI-review artifacts this milestone otherwise requires.
- **Preview/commit content mismatch risk (both reviewers, HIGH, independently found from different angles).** codex-sol frames this as a stale-preview-before-commit race; xai-grok frames it as ApplyPlan's preview composition not being pinned to the same `RenderGitignoreBlock` call `WriteGlobalGitignore` uses for the actual commit. Both point at the same underlying risk: what the user reviews may not be exactly what gets written.

### Divergent Views

- codex-sol emphasizes malformed/duplicate managed-block handling (orphan sentinels, duplicate blocks) as a distinct HIGH; xai-grok does not raise this directly but does flag a related but different HIGH — that hostile-sentinel rejection through the editor is untested end-to-end (T-09.2-11).
- xai-grok raises several plan-09.2-01-specific implementation-detail HIGHs (unassigned `appliedContent` field, missing ApplyPlan-error-path ceremony-abort behavior) that are more mechanically specific than codex-sol's framing of the same general "preview can diverge from reality" theme — both should be read together rather than as separate independent findings.
- xai-grok additionally flags PTY-testability of multi-line textarea editing (append/delete via key sequences) as a HIGH specific to plan 09.2-03's test design, which codex-sol does not mention.

---

## Cycle 2 (verification pass)

**Reviewed:** 2026-08-31
**Reviewers:** xai-grok (opencode/xai/grok-4.6, reasoning=high) — full review completed.
codex-sol (codex/gpt-5.6-sol, reasoning=high) — **failed both attempts** (11:41 and 13:51 local) on a ChatGPT/Codex account usage-limit error (`"You've hit your usage limit... try again at 1:51 PM"`, 333,392 tokens burned with no review output either time). Quota was confirmed reset by the developer after these failures; codex-sol was not re-invoked for cycle 2 per developer instruction to fall back to a second grok lane for this cycle. The `xai-grok` reviewer_instance key has been renamed to `opencode-grok` in `.planning/config.json` (same `cli: opencode`, `model: xai/grok-4.6`) going forward.

All 16 cycle-1 HIGH findings (H1–H16) were independently re-verified against the live source tree by xai-grok and confirmed RESOLVED in the revised 09.2-01/02/03 plans. One **new HIGH** and several MEDIUM/LOW findings were raised:

### New HIGH

- **Edit-key binding is dual-specified (`Enter` vs `e`).** Plan 09.2-03 Task 1 and its PTY recipe use `Enter` to enter edit mode; `09.2-UI-SPEC.md`'s copywriting (`` `e  Edit` ``), footer, and focal points bind `e`. Existing precedent (Global Git screen, `globalgit.go:573-579`) uses `Enter`. Executor following UI-SPEC breaks the PTY suite; executor following the plan ships a footer that lies about its own keybind. Must be resolved to one binding, consistently, in both documents before Wave 3 executes.

### New MEDIUM

- Header budget arithmetic (brand 8 + segments 69 + seps 5 = 82) is off — live `headerTabText` (`frame.go:234-235`) computes short-label sum as 70, not 69; conclusion (fits at width 100) still holds, but the plan/UI-SPEC numbers should be recalculated from source, not hand-copied, to avoid a false "fix" to already-correct labels.
- `matchesDefaultGitignorePatterns`'s Check-1 call site (`internal/doctor/checks/baseline.go:95`) is not named in plan 09.2-02's removal task — only Check-2 (`:116`) is. Compile would catch the miss, but the task should explicitly name both sites so the executor doesn't invent an unspecified fourth doctor branch.
- Plan 09.2-02 doesn't say how the doctor-fix-then-reopen PTY test triggers the fix (Fixer `f` key, CLI, or direct `fixExcludesfile` call) — name the exact key sequence.
- SHELL-03 (CLI parity, `REQUIREMENTS.md`) is untouched by this phase; GIGN-01 is TUI-scoped, so state explicitly "CLI deferred" rather than leaving it an implicit, undocumented gap that surfaces at milestone audit.
- Plan 09.2-03's delete-line PTY recipe doesn't pin which line/index the backspace-join targets — after plan 02's comment-header entries are added, indices shift; name the exact target entry (e.g. `.env`) and its index.
- `09.2-UI-SPEC.md` self-contradicts: header claims "verified by gsd-ui-checker" while the Dimension 1-6 checkboxes are unchecked and Approval is "pending"; also incorrectly claims this is "the first use of a bubbles/v2 interactive component" when `textinput` is already used in `globalgit.go:21-22`. Fix both before Task 4's precondition gate is trusted.

### Low

- `09.2-UI-SPEC.md`'s Screen Identity table copies the same 69/82 header-budget off-by-one as plan 09.2-01.

### Cycle-2 verdict

**MEDIUM** overall — one blocking doc conflict (Enter vs `e`), not a regression of any cycle-1 finding. Full xai-grok cycle-2 review text preserved verbatim below for citation fidelity.

<details>
<summary>Full xai-grok cycle-2 review (click to expand)</summary>

I'll re-verify every cycle-1 HIGH against the live tree and current plans, not the replan claims. Starting with the review skill, UI-SPEC, and the cited source.

[Full review text: .planning/phases/09.2-global-git-ignore-management — cycle-2 xai-grok pass. See run artifacts for the raw transcript; summarized findings above are the authoritative record for planning purposes.]

</details>
