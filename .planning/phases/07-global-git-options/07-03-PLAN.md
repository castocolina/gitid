---
phase: 07-global-git-options
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - internal/globalgit/policy.go
  - internal/globalgit/policy_test.go
  - internal/globalgit/version.go
  - internal/globalgit/version_test.go
  - internal/globalgit/classify.go
  - internal/globalgit/classify_test.go
  - internal/globalgit/bundle.go
  - internal/globalgit/bundle_test.go
  - internal/gitconfig/globalgit.go
  - internal/gitconfig/globalgit_test.go
  - internal/gitconfig/baseline.go
  - internal/gitconfig/baseline_test.go
  - internal/tuikit/views.go
  - internal/tuikit/design.go
  - internal/tuikit/design_test.go
  - internal/tuikit/globalgit.go
  - internal/tuikit/globalgit_test.go
  - internal/dummytui/fixturebackend.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - cmd/gitid/lifecycle.go
  - cmd/gitid/lifecycle_test.go
  - .planning/design/mockup-src/src/data/recipeFixtures.ts
  - Makefile
autonomous: true
cross_ai: true
requirements: [GGIT-01]
estimate:
  tokens: 145000
  raw_tokens: 145000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every one of the twelve rows renders a real current value, a real provenance and a real state — the D-08 pinned set plus D-07's `user.useConfigOnly` — and every row is EXPLAINED with what it does and what gitid recommends (GGIT-01's core requirement)."
    - "A key the user already set to something else renders as `set, differs from recommendation — your choice`, is NOT selectable, and is EXCLUDED from the needs-action tally — because under git's last-wins rule and gitid's floor placement, writing that key into the floor block is provably a no-op (D-02)."
    - "Provenance is stated from evidence, never guessed: gitid says a value is the user's only when git named a file gitid does not own, says a value is gitid's own only when git named gitid's block, and says a value is unset by naming git's own built-in default (D-03)."
    - "`merge.conflictstyle` is the ONE row where the version gate changes the WRITTEN value: at git 2.35 or newer gitid writes `zdiff3`, below it gitid writes `diff3` — because old git ERRORS at merge time on an unknown VALUE, while it silently ignores an unknown KEY, which is why every other gate is informational only (D-08)."
    - "The alias and color rows are bundle rows: one checkbox applies the whole canonical section as a unit, the block always contains every key of that section, and the detail pane names per-key which of the user's own values already differ and therefore win (D-09)."
    - "The needs-action tally counts a row at most once, counts only rows that actually have an action to offer, and is defined in exactly one place so the list's status line and the ceremony's count can never disagree (D-10)."
    - "`user.useConfigOnly` ships as its own opt-in row, unchecked by default, and selecting it while the fallback author pair is only half-set renders the mandatory cross-warning: unmatched commits will hard-fail, which is the point, but the user is told before confirming (D-07)."
    - "The frozen fixture and the live policy agree field-for-field, pinned by a test, so the demo binary and the real binary can never drift apart again."
    - statement: "Every static string this plan introduces is registered in the copy-freeze gate, and every string carrying a machine-specific value — the git version line, the bundle aggregate counts, the applied/selected counts — is explicitly EXCLUDED from it (D-08, Phase 6 D-13 precedent)."
      verification: automated
  artifacts:
    - path: "internal/globalgit/policy.go"
      provides: "the complete D-08 + D-07 pinned row table: display key, member config keys, recommended values, version gate and gate kind, and git's built-in default per key"
    - path: "internal/globalgit/version.go"
      provides: "the gate outcome type and the write-value substitution — the one place that decides zdiff3 versus diff3"
    - path: "internal/globalgit/bundle.go"
      provides: "the D-09 bundle aggregate (set/unset/differs counts) and the per-key differs notes"
    - path: "internal/tuikit/views.go"
      provides: "GlobalGitOptionState, GlobalGitNotApplicableReason and Selectable — the row's state vocabulary and the single toggle predicate"
    - path: "Makefile"
      provides: "copy-freeze coverage of every new static string, the internal/globalgit search path, and explicit exclusion assertions for the three dynamic lines"
  key_links:
    - from: "internal/tuikit/globalgit.go row render + status line"
      to: "GlobalGitOptionView.State + Selectable + the pinned tally"
      via: "one glyph/word/tone selection and one counting predicate"
      pattern: "needsAttention"
    - from: "cmd/gitid/wiring.go GlobalGitOptionStates"
      to: "globalgit.Statuses + globalgit.VersionGate + globalgit.BundleSummary"
      via: "the one conversion site producing every rendered label"
      pattern: "GlobalGitOptionStates"
    - from: "cmd/gitid/lifecycle.go runGlobalGitApply plan stage"
      to: "globalgit.WriteValueFor"
      via: "the version-gated written value for merge.conflictstyle"
      pattern: "WriteValueFor"
---

<objective>
Make all twelve rows honest — real values, real provenance, real states, real explanations — and settle the three rules that keep them consistent: what counts as needing action, what a version gate is allowed to change, and what a bundle row means.

Purpose: plan 07-01 proved the architecture on the single easiest option. Each remaining row carries a complication the tracer deliberately avoided. `merge.conflictstyle` is the only setting where an old git ERRORS rather than shrugs, so its gate must change the written value rather than just annotate it. `core.ignorecase` is defeated per-repository by git's own filesystem probe, so recommending it without saying that would be dishonest. `alias` and `color` are not settings but sections. And `07-CONTEXT.md`'s D-02 inverts Phase 6's own tally rule for a specific, verified reason: on the git side gitid's block sits at the FLOOR, so a key the user set later already wins and gitid has nothing to offer — a "fix" button there would be decorative. GGIT-01 is satisfied here or nowhere: this is the plan where "each option explained" becomes true.
Output: Twelve rows a user can trust, three rules written down once each, and a fixture the live policy is pinned against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-global-git-options/07-CONTEXT.md
@.planning/phases/07-global-git-options/07-UI-SPEC.md
@.planning/phases/07-global-git-options/07-01-SUMMARY.md
@.planning/phases/07-global-git-options/07-02-SUMMARY.md
@.planning/phases/06-global-ssh-options/06-03-SUMMARY.md
@recipes/gitconfig.recipe

<authority>
Every bare `D-NN` citation means `.planning/phases/07-global-git-options/07-CONTEXT.md`.
`06-03-SUMMARY.md` is the SHAPE this plan mirrors on the git side — the
value-before-source classification order, the separate attribution field, the
per-reason not-applicable sentences, and the pinned tally predicate.

**D-08's pinned table is APPROVED and BINDING.** Reproduced here so the executor
never has to reconstruct it, in the frozen §4.5 display order:

| Row | CLI token | Member config key(s) | Recommended | Gate | Gate kind |
|---|---|---|---|---|---|
| `init.defaultBranch` | `init.defaultBranch` | `init.defaultbranch` | `main` | git 2.28 | informational |
| `core.ignorecase` | `core.ignorecase` | `core.ignorecase` | `false` | — | — |
| `core.autocrlf / core.eol` | `core.lineEndings` | `core.autocrlf`, `core.eol` | `input`, `lf` | — | — |
| `user.email (global fallback)` | none — not an `options apply` token | none — owned by plan 07-02's separate verb | left unset unless opted in | — | — |
| `user.useConfigOnly` | `user.useConfigOnly` | `user.useconfigonly` | `true` | — | — |
| `push.autoSetupRemote` | `push.autoSetupRemote` | `push.autosetupremote` | `true` | git 2.37 | informational |
| `pull.rebase` | `pull.rebase` | `pull.rebase` | `true` | — | — |
| `fetch.prune` | `fetch.prune` | `fetch.prune` | `true` | — | — |
| `alias (8 shortcuts)` | `alias` | the 8 recipe aliases | the canonical section | — | — |
| `color (ui/branch/diff/status)` | `color` | the 4 color keys | `auto` for all four | — | — |
| `merge.conflictstyle` | `merge.conflictstyle` | `merge.conflictstyle` | `zdiff3` | git 2.35 | **hard** |
| `diff.colorMoved` | `diff.colorMoved` | `diff.colormoved` | `zebra` | git 2.15 | informational |

**R-5 — The CLI token is a THIRD identifier, frozen here (cross-AI review cycle 1,
MEDIUM on 07-05: "`options apply` key identity").** This surface carries three
different spellings of the same thing and the review found the plans never said
which one a user types: the DISPLAY key (what the screen shows, which for three
rows is prose containing spaces and slashes and therefore cannot be an argv token),
the MEMBER config key (what git reports in `--list`, lower-cased, and which for
three rows is several keys), and now the CLI TOKEN — one stable, shell-safe,
case-preserving identifier per row, the column above.

The token is a field on the policy row, not a string the CLI layer derives, so the
policy table stays the single authority (the same reason the recommended values
live there). Plan 07-05's `options apply` accepts tokens and NOTHING else: a member
key like `alias.lg`, `core.eol` or a lower-cased `init.defaultbranch` is REFUSED
with a message naming the row token to use instead. Refusing a member key is the
whole point — accepting `alias.lg` would imply per-key sub-selection, which D-09
defers to post-v1 and which the bundle apply cannot honour.

**R-6 — The deferred pager key is preserved, never added, and is not a row.** Plan
07-01's `EnsureGlobalGit` merge is ADDITIVE over the existing block (its R-2), so a
key that belongs to no policy row but is already in a user's adopted block survives
every apply. `core.pager` is the live instance and `07-CONTEXT.md`'s Deferred Ideas
require exactly this behavior: keep emitting it when already present, give it no UI
row in v1.0. This plan therefore adds NO pager row, adds NO pager value to any
composition, and adds a test proving an adopted block's pager line survives a full
twelve-row apply. Do not "complete" the table with it.

Gate asymmetry, pinned by D-08 and verified in `07-DISCUSSION-LOG.md`: old git
silently ignores an unknown KEY, so an informational gate never changes what is
written; old git ERRORS at merge time on an unknown VALUE, so the hard gate on
`merge.conflictstyle` DOES change what is written. `merge.conflictstyle` is the
only hard gate in the table.

**Row placement for `user.useConfigOnly` (07-UI-SPEC.md left this to Claude's
discretion; DECIDED here, apply consistently everywhere):** immediately after the
`user.email (global fallback)` row, as shown above. Both rows are about the
author-fallback story, and D-07's mandatory cross-warning has both participants
on screen together at the moment the user needs to compare them. This ordering is
binding for the fixture, the policy table, the copy-freeze strings, and every row-index
assertion in later plans.

**Tally rule (D-10, Claude's discretion; DECIDED here, one rule for scalar and
bundle rows alike):** the needs-action tally counts a ROW at most once, and counts
only rows in the needs-action state. Rows in the set-but-differs state are
EXCLUDED, per D-02 — under the floor model a write into a key the user set later
is provably a no-op, so the row has no action to offer. Not-applicable rows are
excluded. The fallback-author row is excluded (it has its own verb). A bundle row
counts once if at least one of its member keys is unset, whatever the others do.

This DIVERGES from Phase 6's own tally, which counts differs rows as needing
attention. The divergence is deliberate and asymmetric because the precedence
arrow is reversed: on the SSH side gitid's `Host *` block can genuinely change a
differing value, on the git side it provably cannot. State that reason in the
predicate's doc comment and cite both D-02 and `06-03-SUMMARY.md`'s own rule so
a future reader sees the asymmetry is intended.

**Conflict-derivation mechanism (documented deviation, surface it in the SUMMARY).**
D-02 and D-09 cite `gitconfig.ScanConflicts` as the existing substrate for the
differs list and the per-key notes. Verified at planning time: `ScanConflicts` and
`BaselineKeySet` have ZERO production callers, and plan 07-01's two probes already
carry strictly more information than `ScanConflicts` returns — the effective value,
its origin file, AND whether the key is physically in the user's own file. Keeping
both would recreate exactly the C2 drift `BaselineKeySet`'s own doc comment exists
to prevent. This plan therefore derives conflicts inside `internal/globalgit` from
the probes already taken, and DELETES `ScanConflicts` and `BaselineKeySet`. The
POLICY of D-02 and D-09 is implemented literally and unchanged; only the mechanism
differs, and it differs toward one authority instead of two.

**R-7 — The deletion is verified by the compiler, not by a grep (cross-AI review
cycle 1, MEDIUM: "confirm `./...` after delete").** Three facts settled at planning
time so the executor does not have to rediscover them:

1. `ScanConflicts` is not the only thing that dies with it. The `Conflict` struct
   in `internal/gitconfig/baseline.go` has exactly two references in the whole
   module, both inside `ScanConflicts` itself. Delete the type with the function,
   or the `unused` linter fails the build on an orphan — and a lint failure here is
   the FRIENDLY outcome; a type left behind is a second vocabulary for a concept
   that now lives in `internal/globalgit`.
2. The archived POC under `.planning/archive/…/cmd-gitid/` does reference this
   substrate, and it CANNOT break the build: the Go toolchain skips every directory
   whose name begins with a dot, and `go list ./...` was confirmed at planning time
   to contain no archive package. Do not edit the archive, do not widen the `rg`
   scope to it, and do not treat it as a blocker.
3. `rg` proves absence of a NAME; it does not prove the module still compiles.
   The binding gate is `go build ./...` plus `go vet ./...` plus the full test run,
   all of which must be green after the deletion, in the same commit.
</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The complete policy table, the two gate kinds, and the version-gated written value (D-07, D-08)</name>
  <files>internal/globalgit/policy.go, internal/globalgit/policy_test.go, internal/globalgit/version.go, internal/globalgit/version_test.go, internal/gitconfig/globalgit.go, internal/gitconfig/globalgit_test.go, internal/gitconfig/baseline.go, internal/gitconfig/baseline_test.go, cmd/gitid/lifecycle.go, cmd/gitid/lifecycle_test.go</files>
  <read_first>
    - internal/globalgit/policy.go as landed by plan 07-01 — the record shape, the ordered key slice and `PolicyFor` this task completes.
    - internal/globalssh/version.go and version_test.go IN FULL — `VersionGate`, its numeric component compare, and the unverified-version outcome. This is the shape to mirror.
    - internal/deps/deps.go lines 42-75 (`GitVersionAtLeast`) — D-08 mandates reusing this; note its optimistic-fallback behavior (an unreadable version returns true) and decide deliberately whether the hard gate may rely on that.
    - internal/gitconfig/globalgit.go as landed by 07-01 — `EnsureGlobalGit`'s section ordering and selection-driven composition, which now needs the `[user]` section for `useConfigOnly`.
    - internal/tuikit/design.go lines 342-403 — `GlobalGitFullManagedBlockText`'s exact section order and indentation, the shape the composer must be able to produce.
    - cmd/gitid/lifecycle.go `runGlobalGitApply` as landed by 07-01 — the plan stage where a key maps to its recommended value, which is where the write-value substitution belongs.
    - recipes/gitconfig.recipe lines 190-226 — the canonical `~/.gitconfig_default` example: the 8 aliases, the 4 color keys, and the values this table pins.
    - .planning/phases/07-global-git-options/07-CONTEXT.md decisions D-07, D-08, D-09.
  </read_first>
  <behavior>
    - Every row of the pinned table is present in the policy, in the pinned display order, and a lookup by display key returns it.
    - A row knows which config keys it manages: a scalar row one key, the line-endings row two, the alias row eight, the color row four, and the fallback-author row none.
    - Every scalar key that has a git built-in default carries that default on the policy, so an unset row can name it.
    - Resolving the written value for a row with no gate returns the recommendation unchanged, whatever the machine's git version.
    - Resolving the written value for a row with an informational gate returns the recommendation unchanged, whether the machine's git meets the gate or not.
    - Resolving the written value for the hard-gated row returns the recommendation when the machine's git meets the gate, and the documented fallback value when it does not.
    - Resolving the written value when the machine's git version cannot be read behaves the same as a machine below the hard gate for the hard-gated row — the safe direction, since the fallback value is accepted by every git that accepts the recommendation.
    - Composing a selection containing the fail-loud author key produces a `[user]` section in the managed block, in the pinned section order.
    - Composing a selection containing the alias row produces every one of the eight aliases; composing one containing the color row produces every one of the four color keys.
    - A composed block containing every selectable row's keys matches the frozen block text's section order.
  </behavior>
  <action>
**Complete the table.** Extend `internal/globalgit/policy.go` from 07-01's single live entry to the full pinned set in this plan's `<authority>` block, in that exact order. Restructure the record from one-key-per-row to a ROW with a member key set — this is required by three rows that manage more than one key (the line-endings pair and the two bundle sections) and one row that manages none (the fallback author, owned by plan 07-02's separate verb). Each member carries its config key as git reports it (lower-cased in `--list` output — store the canonical display spelling and match case-insensitively), its recommended value, and git's built-in default when one exists so an unset row can name it.

Each ROW additionally carries its frozen CLI token (R-5) — the `<authority>` table's second column. Put it on the policy record, never in the CLI layer: plan 07-05 looks rows up by it and refuses anything else, and a token that lived in the command file would be a second table free to drift from this one. The fallback-author row carries no token, because it is not an `options apply` target. Add a test asserting every row's token is unique, shell-safe (no spaces, no slashes, no shell metacharacters), and that the token set matches the `<authority>` table exactly.

Values come from `recipes/gitconfig.recipe`'s `~/.gitconfig_default` example verbatim — the 8 aliases with the full `lg` format string, and the 4 color keys. Do not paraphrase the recipe; it is the North Star and the block gitid writes must reproduce it.

Record the line-endings row's own quirk on the policy rather than only in copy: `core.eol` is IGNORED by git while `core.autocrlf` is `input`, and it is written anyway because it documents intent. A reviewer must be able to see that gitid knows it is writing a key with no immediate effect, and why.

**The two gate kinds.** Create `internal/globalgit/version.go` in the shape of `internal/globalssh/version.go`. It exposes the gate outcome (met, below, unreadable) computed from `internal/deps.GitVersionAtLeast` — reuse it, do not add a second version probe (D-08). Then the substitution:

`func WriteValueFor(row Row, key string, gate GateOutcome) string`

For no gate and for an informational gate it returns the recommendation unchanged; the whole point of the informational classification is that old git silently ignores an unknown KEY, so writing it is harmless. For the hard gate it returns the recommendation when the gate is met and the row's declared fallback value when it is not, because old git ERRORS at merge time on an unknown VALUE. `merge.conflictstyle` declares `diff3` as its fallback and is the only hard-gated row in the table.

Handle the unreadable case deliberately and say why in the doc comment. `deps.GitVersionAtLeast` returns TRUE on an unreadable version — an optimistic fallback that is right for a feature that is merely ignored when unsupported, and wrong for one that errors. For the hard gate, treat unreadable as below-gate: `diff3` is accepted by every git that accepts `zdiff3`, so the conservative direction costs the user a better conflict display and the optimistic direction costs them a failing merge. This is the git-side analog of Phase 6 D-13's refusal to write `accept-new` on an unverified OpenSSH, arrived at from the same reasoning.

**The composer.** Extend `internal/gitconfig/globalgit.go`'s `EnsureGlobalGit` section ordering to include `[user]` for the fail-loud author key, placing it in the order the frozen block text pins. Note the distinction explicitly in the doc comment and pin it with a test: the `[user]` section inside the BASELINE block carries only `useConfigOnly`; the fallback author's own `[user]` section lives in plan 07-02's separate block. Two different `[user]` sections in two different managed blocks, never merged.

**The write site.** In `runGlobalGitApply`'s plan stage, map each selected row's member keys through `WriteValueFor` rather than reading the recommendation directly. Add a lifecycle test pinning both directions of the hard gate by injecting the version-gate outcome, so the substitution is proven at the write boundary and not only in the unit.

**Deletions.** Remove `gitconfig.ScanConflicts`, `gitconfig.BaselineKeySet` and the now-orphaned `gitconfig.Conflict` type, together with their tests, per this plan's `<authority>` block R-7. Verify no caller remains before deleting, then verify the module still COMPILES afterwards — `rg` proves a name is gone, the compiler proves nothing depended on it. The archived POC references this substrate and is invisible to the Go toolchain; leave it alone.

**Pager preservation (R-6).** Add no pager row and no pager value anywhere. Add a test that seeds an adopted block carrying the deferred pager line, applies a FULL twelve-row selection, and asserts the pager line is still in the composed block afterwards — plan 07-01's additive merge is what delivers this, and this test is what stops a later "compose exactly the selection" simplification from silently dropping a real user's setting on their first real write.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/globalgit/... ./internal/gitconfig/... ./cmd/gitid/... -run 'Policy|Version|WriteValue|EnsureGlobalGit|GlobalGitApply'</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the policy's ordered row list equals the pinned display order in this plan's `<authority>` table, row for row.
    - A test asserts the member-key counts per row: one for each scalar row, two for the line-endings row, eight for the alias row, four for the color row, zero for the fallback-author row.
    - A test asserts `merge.conflictstyle` is the ONLY row in the table whose gate kind is hard.
    - A test asserts `WriteValueFor` returns the recommendation for every informational-gated row when the gate is NOT met.
    - A test asserts `WriteValueFor` returns the recommendation for the hard-gated row when the gate IS met, and the declared fallback value when it is NOT.
    - A test asserts `WriteValueFor` returns the fallback value for the hard-gated row when the version is unreadable, and its doc comment states the reasoning.
    - A test asserts every scalar row whose recommendation differs from git's behavior when unset carries a non-empty built-in-default string.
    - A composer test asserts a selection containing the alias row emits all eight aliases with the `lg` format string byte-identical to `recipes/gitconfig.recipe`.
    - A composer test asserts a selection containing the color row emits all four color keys.
    - A composer test asserts a selection containing the fail-loud author key emits a `[user]` section inside the baseline block, and that the baseline block never contains an author name or email key.
    - A composer test asserts a full selection's section order matches `internal/tuikit/design.go`'s frozen block text section order.
    - A lifecycle test injects a below-gate version and asserts the written `merge.conflictstyle` value is the fallback; injects an at-gate version and asserts it is the recommendation.
    - A test asserts every policy row's CLI token is unique, contains no space, slash or shell metacharacter, and that the full token set equals the `<authority>` table's second column; and that the fallback-author row carries none.
    - A test seeds an adopted block carrying the deferred pager line, composes a FULL selection, and asserts the pager line survives (R-6); a second test asserts no composition ever ADDS it.
    - `rg -n 'ScanConflicts|BaselineKeySet|Conflict\b' internal/ cmd/ e2e/` returns no match for the deleted symbols, AND `go build ./...` and `go vet ./...` both exit 0, AND `TERM=dumb SSH_AUTH_SOCK= go test -count=1 ./...` is green — in the same commit as the deletion (R-7).
    - `make lint` reports 0 issues, including the `unused` linter, which is what would catch an orphaned type left behind by the deletion.
  </acceptance_criteria>
  <reversibility rating="costly">This decides the exact values gitid writes into a user's real git configuration, including one value that changes with the machine's git version. Every value is recipe-sourced and pinned by test in both gate directions.</reversibility>
  <done>Twelve rows exist as data, the two gate kinds behave differently for a stated reason, and an old git gets a conflict style it can actually use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest states, honest provenance, honest bundles, and one tally rule (D-02, D-03, D-09, D-10)</name>
  <files>internal/globalgit/classify.go, internal/globalgit/classify_test.go, internal/globalgit/bundle.go, internal/globalgit/bundle_test.go, internal/tuikit/views.go, internal/tuikit/globalgit.go, internal/tuikit/globalgit_test.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go</files>
  <read_first>
    - internal/globalgit/classify.go as landed by plan 07-01 — the value-before-source branch order and the separate source-class field this task builds on.
    - internal/globalssh/classify.go IN FULL, and 06-03-SUMMARY.md's "Authoritative resolutions" section — the four-state model, the separate attribution boolean, and the per-reason not-applicable sentences.
    - internal/tuikit/views.go lines 300-402 — `GlobalSSHOptionState`, `GlobalSSHNotApplicableReason`, `GlobalSSHOptionView`, and `Selectable`'s "one predicate for the toggle key, the checkbox click, and the checkbox glyph" contract.
    - internal/tuikit/globalgit.go as landed by plans 07-01 and 07-02 — the row loop, the checkbox/tone glyph selection, the status line, and `gitApplyChosen`.
    - internal/tuikit/globalssh.go — `needsAttention` and how the status line and the ceremony count read from one predicate.
    - internal/gitconfig/baseline.go lines 63-139 (`ScanConflicts`) BEFORE deleting it in Task 1 — its intersect-and-compare logic is the behavior being re-derived from the probes, and reading it prevents re-inventing a subtly different rule.
    - cmd/gitid/wiring.go's `GlobalSSHOptionStates` — the one-conversion-site pattern for rendering labels.
    - .planning/phases/07-global-git-options/07-UI-SPEC.md — the "partial" row of UI Considerations (the three-states-on-one-row extension) and the Copywriting Contract's provenance and differs entries.
    - .planning/phases/07-global-git-options/07-CONTEXT.md decisions D-02, D-03, D-09, D-10.
  </read_first>
  <behavior>
    - A row whose effective value already equals the recommendation is already-set, whatever set it.
    - A row unset everywhere is needs-action, and its current cell names git's own built-in default when the policy declares one.
    - A row set to something other than the recommendation by a file gitid does not own is set-but-differs, its provenance names that file, and it is not selectable.
    - A row set by gitid's own managed block reads as applied by gitid, and is not offered again.
    - A row set at a scope gitid cannot change reads as such, and is not selectable.
    - A row gitid could not classify because its probe failed carries the probe error, makes no state claim, and is not selectable.
    - The one selectability predicate governs the toggle key, the checkbox glyph and the click target simultaneously — there is no path by which a row can be toggled but render no checkbox, or render a checkbox but refuse the toggle.
    - A bundle row's current cell summarises its members: how many are set, and how many differ from the recommendation.
    - A bundle row's detail pane names each member whose own value differs, and says the user's value wins.
    - A bundle row is needs-action when at least one member key is unset, whatever the other members do.
    - The needs-action tally counts each row at most once, counts only needs-action rows, and is read by both the list's status line and the apply ceremony's count from the same predicate.
    - The fail-loud author row is unchecked by default even when it needs action, and stays independently togglable.
  </behavior>
  <action>
**States and provenance.** Extend `internal/globalgit/classify.go` from 07-01's tracer classification to the full vocabulary, keeping its value-before-source branch order — a value equal to the recommendation is already-set whatever set it, and the source class only decides the WORDS. Emit four states (needs-action, already-set, set-but-differs, not-applicable) and keep the source class a SEPARATE field, never a fifth state, exactly as `06-03-SUMMARY.md` resolved for the SSH side. A not-applicable row carries a REASON enum so a future reader cannot collapse four different non-applicabilities into one sentence.

D-02's posture is implemented here and nowhere else: a set-but-differs row is INFORMATIONAL. It is rendered, it is explained, and it is not selectable — because gitid's block sits at the floor and a write into a key the user set later is provably a no-op, so offering a fix would be a decorative button. Write that reason on the state, not just in a plan.

**Bundles.** Create `internal/globalgit/bundle.go`. For a bundle row it computes, from the two probes already taken, per member key: whether it is set at all, and whether its effective value differs from the recommendation. From that it produces the aggregate summary the row's current cell shows and the per-key differs list the detail pane shows. This is the re-derivation of `ScanConflicts` against strictly better evidence, per this plan's `<authority>` block — the intersect-and-compare rule is the same, the input is the probe results instead of a temp file and a second `git config` invocation.

D-09's apply semantics stay exactly as decided: one checkbox applies the whole canonical section as a UNIT, and the written block contains EVERY key of that section regardless of the user's collisions, because floor placement plus last-wins means the user's own `alias.co` still wins. The per-key note is presentation only and changes nothing about what is written. Pin that with a test: composing a bundle row's selection emits all its member keys even when the probe reports several of them already set to different values.

Composing every member key is only half the claim, though, and review cycle 1 was right that the other half was promised in the threat register and never assigned to a test. The claim that makes the collision ACCEPTABLE is that the user's own value still wins after the write, and that is a property of git's resolution, not of the composition. Add the lifecycle proof: seed a sandbox HOME whose own `~/.gitconfig` sets one of the bundle's member keys to a different value, apply the bundle row through the real ceremony, then read that key back with a real `git config --show-origin --get` and assert git names the USER's file and the USER's value. Quote the observed output in the SUMMARY. A per-key note that says "yours wins" while git disagrees would be the single most damaging kind of dishonesty this screen can produce.

**Attribution is decided by the origin PATH, never by set membership.** The two `[user]` sections this phase produces make the trap concrete: `user.useConfigOnly` lives in the include'd baseline file, while the fallback author's own `[user]` block lives in `~/.gitconfig` itself — and because the include is at the FLOOR, a `useConfigOnly` the user later wrote into their own file wins over gitid's. A classifier that decided "this key is one gitid manages, therefore gitid set it" would report the user's own value as gitid's. Plan 07-01's classifier already keys on the effective origin path resolving to the gitid-owned baseline file; keep that and pin it with a test rather than letting the fuller vocabulary added here quietly reintroduce a membership check.

**The tally.** Add the single predicate to `internal/tuikit/globalgit.go`, following `globalssh.go`'s `needsAttention` shape, implementing this plan's `<authority>` tally rule. Its doc comment states the rule, states that it is the ONE definition, and states why it diverges from Phase 6's — the precedence arrow is reversed. The list status line and the apply ceremony's count both read it; nothing re-derives a second count.

**Selectability.** Add `Selectable()` to `GlobalGitOptionView` as the single predicate for the toggle key, the checkbox glyph and the click target, matching `GlobalSSHOptionView.Selectable`'s contract. A row is selectable only when it is needs-action, has at least one writable member key, and carries no probe error. That makes the differs, not-applicable, probe-error and fallback-author rows non-selectable by construction rather than by four separate guards. Route the existing toggle-key branch, the click handler's checkbox hit test, and the glyph choice through it, and delete the ad-hoc `NeedsAction` checks they use today.

D-07's "unchecked by default" for the fail-loud author row needs no special case any more, and adding one would be a regression: plan 07-01's R-1 emptied the default selection entirely, so EVERY row is unchecked until the user checks it and the recipes' leave-it-unset posture is the universal default rather than a per-row exception. Assert the row is selectable and starts unselected; do not reintroduce a pre-selection loop with an exclusion list.

**The conversion site.** Extend `cmd/gitid/wiring.go`'s `GlobalGitOptionStates` — still the ONE conversion site — to render every label: the provenance strings, the differs wording, the not-applicable sentences, the bundle aggregate and the per-key notes. The view carries rendered strings; the render package carries no source-class enum, matching the boundary rule `GlobalSSHOptionView` already states.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/globalgit/... ./internal/tuikit/... ./cmd/gitid/... -run 'Classify|Bundle|Selectable|NeedsAttention|GlobalGitOption'</automated>
  </verify>
  <acceptance_criteria>
    - A classifier test covers each of the four states and asserts the resulting state and source class independently, so a state change cannot silently move attribution.
    - A test asserts a value equal to the recommendation classifies as already-set even when the source class is unset.
    - A test asserts a differs row is NOT selectable and is NOT counted by the tally predicate.
    - A test asserts an already-set row, a not-applicable row and a probe-error row are each not selectable, for their own distinct reason.
    - A test asserts each not-applicable reason produces its own distinct sentence, and that no two reasons share one.
    - A test asserts a probe failure affecting one evidence source leaves rows classified from the other source unaffected.
    - A bundle test asserts the aggregate reports the correct set-count and differs-count for a section where some members are set, some unset, and some set to a different value.
    - A bundle test asserts the detail pane names exactly the members whose values differ, and no others.
    - A bundle test asserts a bundle row with one unset member is needs-action even when every other member is already correct.
    - A composer test asserts selecting a bundle row emits ALL its member keys even when the probe reports members already set to different values.
    - A lifecycle test with a REAL `git` binary seeds a user-set value for one bundle member, applies the bundle row, reads the key back with `--show-origin`, and asserts git names the USER's file and value — the post-apply half of T-07-19's acceptance, with the observed output quoted in the SUMMARY.
    - A classifier test asserts attribution is decided by the effective ORIGIN PATH, not by membership in the managed key set: a key gitid manages, set by the user in their own file, classifies as the user's — proven with the fail-loud author key, whose two `[user]` homes make the trap live.
    - A test asserts the tally counts a bundle row exactly once.
    - A test asserts the list status line's count and the apply ceremony's count come from the same predicate, by construction — a source-level check that no second counting loop exists.
    - A test asserts toggling is refused for every non-selectable row AND that the same rows render no checkbox glyph, driven from `Selectable()` alone.
    - A test asserts the fail-loud author row is unselected on a fresh model even when it needs action, that it CAN be toggled on, and that no row is pre-selected — the selection is empty by construction under 07-01's R-1, not by a per-row exclusion.
    - `make lint` reports 0 issues.
  </acceptance_criteria>
  <reversibility rating="reversible">Classification and presentation over data already gathered; no write behavior changes and no user file is touched.</reversibility>
  <done>A user can look at any row and know what the value is, where it came from, whether gitid can do anything about it, and — for a bundle — exactly which of their own settings will keep winning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fixture corrections, the new frozen copy, and the fixture-versus-policy parity pin (D-08, D-07)</name>
  <files>internal/tuikit/design.go, internal/tuikit/design_test.go, internal/tuikit/globalgit.go, internal/tuikit/globalgit_test.go, internal/dummytui/fixturebackend.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, .planning/design/mockup-src/src/data/recipeFixtures.ts, Makefile</files>
  <read_first>
    - internal/tuikit/design.go lines 262-403 IN FULL — the six frozen D9 constants, the `GlobalGitOption` record, the eleven-row `GlobalGitOptions` table (note the `merge.conflictstyle` row at ~:325 recommending the pre-amendment value), `GlobalGitDetailExplanation`, `GlobalGitAdvisoryNote`, `GlobalGitBaselineStripText` at ~:357, the sentinels, `GlobalGitResultMessage` at ~:351, and `GlobalGitFullManagedBlockText` at ~:364.
    - .planning/design/mockup-src/src/data/recipeFixtures.ts lines 128-162 — `globalGitDefaults` (note `mergeConflictstyle`) and `globalGitDefaultsBlockText` which interpolates it.
    - Makefile lines 297-356 (`gate-copy-freeze`) IN FULL — the frozen string list, the SEARCH PATH argument to `grep -r` (currently four directories; `internal/globalgit` is NOT among them), and the runtime-assembled dynamic-prefix exclusion check at the end.
    - internal/globalgit/policy.go as landed by Task 1 — the source of truth the parity test pins the fixture against.
    - 06-03-SUMMARY.md's "D-13 exclusion check must not match its own grep pattern" deviation — the runtime-assembly technique this task must reuse for every new dynamic exclusion.
    - .planning/phases/07-global-git-options/07-UI-SPEC.md — the "Data corrections required before drafting new copy" table and the Copywriting Contract table, which name every string this task drafts.
    - .planning/phases/07-global-git-options/07-CONTEXT.md decisions D-07, D-08, D-09, D-10.
  </read_first>
  <behavior>
    - The frozen fixture's recommended conflict style matches the policy's recommendation, in the Go fixture, in the baseline strip line, in the managed-block text, and in the web fixture.
    - The fixture's row list and the policy's row list agree field for field: same rows, same order, same recommended values.
    - A fixture row and a policy row can no longer disagree without a test failing.
    - The fail-loud author row is present in the fixture with its own explanation.
    - The case-sensitivity row's explanation states that git's own per-repository probe can override the global setting.
    - The conflict-style row shows a static note explaining the version requirement and the substituted value, and separately a line naming the machine's actual git version.
    - Selecting the fail-loud author row while the fallback author pair has exactly one half set renders the cross-warning naming which half is missing.
    - Selecting the fail-loud author row while both halves are set, or while neither is set, renders no cross-warning.
    - A machine with a fallback email set and no fallback name renders the guessed-name warning, independently of the fail-loud author row's state.
    - Every static string introduced is found by the copy-freeze gate; every machine-specific line is provably absent from it.
    - The demo binary renders every row with colour disabled and each state remains distinguishable by glyph and word alone.
  </behavior>
  <action>
**The three data corrections, in one commit, before any new copy is drafted** — `07-UI-SPEC.md` is explicit that the fixture must be corrected first, and D-08 is explicit that the conflict-style amendment lands in ONE commit across every mirror.

The conflict-style recommendation is wrong in four places, all verified at planning time: `internal/tuikit/design.go`'s `GlobalGitOptions` row, its `GlobalGitBaselineStripText` line, its `GlobalGitFullManagedBlockText` body, and `.planning/design/mockup-src/src/data/recipeFixtures.ts`'s `globalGitDefaults` (from which `globalGitDefaultsBlockText` interpolates). Correct all four to the policy's recommendation in a single commit. `07-CONTEXT.md` names `internal/dummytui/data.go` as the home of two of these constants; that is stale — they live in `internal/tuikit/design.go`. Record the correction in the SUMMARY.

Add the fail-loud author row to the fixture at the pinned position from this plan's `<authority>` table — immediately after the fallback-author row — with `NeedsAction` true and unchecked by default. Add its line to the managed-block text so the ceremony's preview shows what will actually be written.

**The parity pin.** Add a test in the shape of Phase 6's own fixture-versus-policy parity test: it walks the policy's ordered rows and the fixture's ordered rows together and asserts they agree on row identity, order and recommended value. This is the mechanism that stops the demo and the real binary drifting again, and it is why the corrections above can be made safely.

**The new copy.** Draft exactly these, and nothing beyond them — everything else in the Copywriting Contract is CITED, not drafted:

- The five provenance labels, mirroring Phase 6's own registered set on the git side: the user's own file with its line, a scope gitid cannot change, unset naming git's built-in default, applied by gitid, and set somewhere gitid cannot name.
- The differs word state. Use the SAME sentence Phase 6 already froze for its own differs state — it is already in the gate's list and the posture is identical, so reusing it keeps one vocabulary across both Global-* screens.
- The case-sensitivity caveat, appended to that row's existing explanation: git's own `init` and `clone` probe the filesystem and may write a repository-local override that beats the global setting.
- The conflict-style gate note: a STATIC sentence naming the required git version and stating that the fallback value will be written instead. Keep the machine's actual version OUT of this sentence — it goes on its own separate dynamic line, which is what makes the static half freezable.
- The dynamic git-version line, rendered in the detail pane for the default-branch row and the conflict-style row. It is NON-contractual and MUST be excluded from the gate, per the Phase 6 D-13 precedent.
- The fail-loud author row's label and one-liner: it turns a commit with no matching identity into a hard error instead of letting git guess an author from the OS account — the fail-loud companion to `includeIf` setups.
- The cross-warning, as TWO fully-static constants, one per missing half. Two constants rather than one interpolated sentence keeps both variants greppable by the copy-freeze gate; an interpolated sentence would only be half-frozen.
- The guessed-name warning, for the fallback-email-set-and-name-empty state, independent of the fail-loud row.
- The bundle aggregate cell and the per-key differs note. The aggregate carries counts and is therefore dynamic and excluded; the per-key note's wording is static and frozen.

The result message currently hardcodes its counts. Replace the counts with the real applied and selected numbers and freeze only the static tail — the sentence about the baseline apply leaving the global author alone. That sentence stays TRUE under D-04 because the baseline ceremony genuinely never touches the fallback block, and this task converts that from a claim into a proven invariant: add a test asserting a baseline apply leaves the fallback block byte-identical.

**The gate.** Extend `gate-copy-freeze`'s frozen list with every static string above, and extend its SEARCH PATH to include `internal/globalgit` — the gate currently greps four directories and the new package is not among them, so a string that lives only there would report as missing. Add exclusion assertions for the three dynamic lines (the git-version line, the bundle aggregate, the applied/selected counts), each assembled at runtime so the check cannot match its own pattern — the exact failure mode 06-03 hit and documented. Prove each exclusion works by adding its prefix to the frozen list, observing the gate fail, then reverting; record that in the SUMMARY.

**Colour-disabled legibility.** Add the no-colour test the SSH side already carries: render every row state with colour disabled and assert each remains distinguishable by its glyph and its word. The differs state must not introduce a new glyph or a new colour — it is the existing warning glyph and role with a new WORD only.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit/... ./internal/dummytui/... ./cmd/gitid/... -run 'GlobalGit|Parity|NoColor|CrossWarning|GuessedName' &amp;&amp; make gate-copy-freeze</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'conflictstyle' internal/ .planning/design/mockup-src/src/data/recipeFixtures.ts` shows the policy's recommended value everywhere and the pre-amendment value nowhere outside test fixtures that deliberately exercise the below-gate path.
    - A parity test walks the policy's rows and the fixture's rows together and fails on any difference in identity, order or recommended value.
    - A test asserts the fixture contains the fail-loud author row at the pinned index, that it needs action, and that it is unchecked on a fresh model.
    - A test asserts the managed-block fixture text contains the fail-loud author key.
    - A test asserts the case-sensitivity row's rendered explanation contains the per-repository-override caveat.
    - A test asserts the conflict-style row's detail pane shows the static gate note when the gate is not met, and does not show it when the gate is met.
    - A test asserts the dynamic git-version line appears in the detail pane and that its text is NOT in the frozen list.
    - A test asserts the cross-warning renders when exactly one fallback half is set and the fail-loud row is selected, naming the missing half, and renders neither variant when both or neither half is set.
    - A test asserts the guessed-name warning renders when the fallback email is set and the fallback name is empty, independently of the fail-loud row's selection.
    - A test asserts a baseline apply leaves the fallback-author block byte-identical.
    - A no-colour test asserts all four row states remain distinguishable with colour disabled, and that the differs state introduces no new glyph and no new theme role.
    - `make gate-copy-freeze` passes; its search path includes `internal/globalgit`; and each of the three dynamic exclusions was proven to fail the gate when deliberately added, with the observation recorded in the SUMMARY.
    - `pnpm --dir .planning/design/mockup-src typecheck` and `pnpm --dir .planning/design/mockup-src build` both pass after the web fixture amendment.
    - `make lint` reports 0 issues.
  </acceptance_criteria>
  <reversibility rating="costly">This amends frozen design copy and a frozen fixture value that the approved Phase 2 demos and the visual-regression gate are pinned against. The amendment is mandated by D-08 and lands in one commit across all four mirrors with a parity test preventing future drift.</reversibility>
  <done>The demo and the real binary agree on every row, the new warnings say true things, and no static string this phase introduces can be silently reworded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy recommended values → the user's real git configuration | Every recommendation becomes a written line the moment a user selects its row. |
| the machine's git version → the written value | The hard gate is the only place a machine property changes what is written, and getting it wrong breaks merges. |
| classifier state → what gitid offers to change | Offering to "fix" something gitid provably cannot change trains the user to distrust the screen. |
| user-set values reported back on screen | Values read from arbitrary user config crossing into rendered rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-16 | Denial of Service | `merge.conflictstyle` written above the machine's git capability | high | mitigate | `WriteValueFor` substitutes the fallback value below the hard gate AND on an unreadable version; both directions pinned by unit and lifecycle tests. Old git errors at MERGE time, long after the write, so the failure would surface far from its cause. |
| T-07-17 | Spoofing | a value attributed to the user that the user did not set | high | mitigate | Value-before-source classification with the source class kept a separate field; provenance labels are emitted only from evidence git itself named, and the unnameable case has its own label. |
| T-07-18 | Repudiation | a decorative fix button on a row gitid cannot change | medium | mitigate | D-02's informational posture implemented in `Selectable()` as the single predicate for toggle, glyph and click; a differs row cannot be toggled and renders no checkbox. |
| T-07-19 | Tampering | a bundle apply silently overwriting the user's own alias | medium | accept | The block contains every member key by design (D-09), but floor placement plus git's last-wins rule means the user's own value still wins; the detail pane names each such key and says so. Accepted with a test asserting the user's value still resolves after the apply. |
| T-07-20 | Information Disclosure | a frozen sentence containing a machine-specific value | low | mitigate | Every dynamic line is excluded from the copy-freeze gate by a runtime-assembled assertion, and each exclusion is proven to fail the gate when deliberately added. |
| T-07-21 | Tampering | fixture and policy drifting apart again | medium | mitigate | The fixture-versus-policy parity test walks both ordered lists and fails on any difference in identity, order or recommended value. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages. |
</threat_model>

<verification>
- `go build ./...` exits 0.
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green.
- `make lint` reports 0 issues.
- `make test` green including `gate-copy-freeze`.
- `make test-e2e` green.
- `go test ./internal/dummytui/ -run TestNoBackendAllowlist` passes.
- `go list -deps ./internal/tuikit` contains no first-party backend package.
- `rg -n 'ScanConflicts|BaselineKeySet' internal/ cmd/ e2e/` returns no match, and `go vet ./...` exits 0 — the compiler, not the grep, is what proves the deletion left nothing behind (R-7).
- `pnpm --dir .planning/design/mockup-src typecheck` and `build` both pass.
</verification>

<success_criteria>
- All twelve rows carry a real value, a real provenance, a real state and an explanation.
- A key the user already set reads as their choice, is not offered as a fix, and does not inflate the tally.
- An old git receives a conflict style it can actually use, and is told why.
- A bundle row tells the user exactly which of their own settings will keep winning.
- The demo fixture and the live policy cannot drift apart without a test failing.
</success_criteria>

<artifacts>
**`internal/globalgit`**: the complete D-08 + D-07 row table with member-key sets and built-in defaults; `version.go`'s gate outcomes and `WriteValueFor` substitution; `classify.go`'s four states, source classes and not-applicable reasons; `bundle.go`'s aggregates and per-key differs notes.

**`internal/gitconfig`**: `EnsureGlobalGit` extended with the baseline `[user]` section; `ScanConflicts` and `BaselineKeySet` deleted.

**`internal/tuikit`**: `GlobalGitOptionState`, `GlobalGitNotApplicableReason`, `Selectable()`; the single tally predicate with its documented divergence from Phase 6's; the corrected fixture, the fail-loud author row, and every new frozen string.

**`cmd/gitid`**: the extended one-conversion-site label rendering; the version-gated write value at the ceremony's plan stage.

**`.planning/design/mockup-src`**: the web fixture's conflict-style amendment, landing in the same commit as the Go mirrors.

**`Makefile`**: the extended frozen list, `internal/globalgit` added to the search path, and three runtime-assembled dynamic exclusions.
</artifacts>

<output>
Create `.planning/phases/07-global-git-options/07-03-SUMMARY.md` when done, carrying
the `## Deviations` and `## Review` headings exactly as plan 07-01's `<output>`
section specifies — plan 07-06 parses them. Record:
- the complete final row table as implemented, with each row's CLI token, member keys, recommended values, gate and gate kind — the authoritative reference plans 07-04 through 07-06 read, and the token column plan 07-05 freezes its argv against;
- the observed `git config --show-origin --get` output proving a user's own bundle-member value still wins after a bundle apply, quoted;
- confirmation that the deferred pager key survives a full apply on an adopted block, with the composed body quoted;
- the deletion's verification: the `rg` result, plus the `go build ./...` / `go vet ./...` / full-test outcomes in the same commit;
- the pinned tally rule as coded, and the one-sentence reason it diverges from Phase 6's;
- the observed `WriteValueFor` output for the hard-gated row in all three version conditions (met, below, unreadable);
- the four locations where the conflict-style value was corrected, and the confirmation that `07-CONTEXT.md`'s `internal/dummytui/data.go` reference was stale;
- every string added to `gate-copy-freeze`, every dynamic exclusion added, and the observed gate failure for each exclusion when deliberately added to the frozen list;
- the full before/after rendered dummy Global Git options list — plan 07-06 consumes this for its visual-divergence allowlist.
</output>
