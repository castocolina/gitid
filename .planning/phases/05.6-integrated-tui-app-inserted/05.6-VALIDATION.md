---
phase: 5.6
slug: integrated-tui-app-inserted
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-15
---

# Phase 5.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 05.6-RESEARCH.md § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go standard `testing` package (white-box `package tui` tests; no teatest dependency) |
| **Config file** | none — standard `go test` runner |
| **Quick run command** | `go test ./tui/... -count=1` |
| **Full suite command** | `make test` (runs `go test ./...`) |
| **Lint command** | `make lint` (golangci-lint v2) |
| **Estimated runtime** | ~15–30 seconds (tui package); full suite ~1–2 min |

---

## Sampling Rate

- **After every task commit:** Run `go test ./tui/... -count=1`
- **After every plan wave:** Run `make test` (full suite incl. `internal/`)
- **Before `/gsd-verify-work`:** `make test` + `make lint` must be green
- **Max feedback latency:** ~30 seconds (tui quick run)

---

## Per-Task Verification Map

> Test names from RESEARCH.md § "Phase Requirements → Test Map". Plan/Wave/Task IDs
> are filled by the planner; rows below are keyed by requirement + behavior so the
> planner can attach each to its task.

| Behavior | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|----------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| Two-pane layout renders without panic at 80×24 and 120×40 | TUI-03 | — | N/A | unit | `go test ./tui/... -run TestRootModel` | ❌ W0 | ⬜ pending |
| WindowSizeMsg < 80 cols → "Terminal too small" plain text | TUI-03 | — | N/A | unit | `go test ./tui/... -run TestSmallTerminal` | ❌ W0 | ⬜ pending |
| Sidebar always visible when width ≥ 80 | TUI-03 | — | N/A | unit | `go test ./tui/... -run TestSidebarVisible` | ❌ W0 | ⬜ pending |
| Sidebar collapses below 80 cols | TUI-03 | — | N/A | unit | `go test ./tui/... -run TestSidebarCollapse` | ❌ W0 | ⬜ pending |
| `1`/`2`/`3` key switches active view | TUI-04 | — | N/A | unit | `go test ./tui/... -run TestViewSwitch` | ❌ W0 | ⬜ pending |
| Ctrl+P opens palette modal | TUI-04 | — | N/A | unit | `go test ./tui/... -run TestPaletteOpen` | ❌ W0 | ⬜ pending |
| `a` → wizard modal opens (step 1 form renders) | TUI-05 | — | N/A | unit | `go test ./tui/... -run TestWizardOpen` | ❌ W0 | ⬜ pending |
| prove-before-write: phase1 fail → retry path | TUI-05 | T-write-gate | persist only after authenticated PASS | unit | `go test ./tui/... -run TestWizardProveRetry` | ❌ W0 (port prove_test.go) | ⬜ pending |
| inline edit: `e` → field focused → Enter commits → update called | TUI-05 | — | structural edit re-runs prove loop | unit | `go test ./tui/... -run TestInlineEdit` | ❌ W0 | ⬜ pending |
| `d` → delete confirm modal opens | TUI-06 | T-destructive | confirm gate before removal | unit | `go test ./tui/... -run TestDeleteModal` | ❌ W0 | ⬜ pending |
| Health view: 8 families emit familyResultMsg | TUI-06 | — | N/A | unit | `go test ./tui/... -run TestHealthFamilies` | ❌ W0 (port dashboard_test.go) | ⬜ pending |
| `x` on fixable finding → fix confirm modal | TUI-06 | T-destructive | confirm gate before fix write | unit | `go test ./tui/... -run TestFixModal` | ❌ W0 | ⬜ pending |
| footer always rendered in every view | TUI-06 | — | N/A | unit | `go test ./tui/... -run TestFooterAlwaysRendered` | ❌ W0 | ⬜ pending |
| `?` opens help overlay | TUI-06 | — | N/A | unit | `go test ./tui/... -run TestHelpOverlay` | ❌ W0 | ⬜ pending |
| Real wiring: `buildTUIDeps()` has no nil CheckFn or identity dep field (incl. new Delete) | D-16 | T-nil-seam | all live seams non-nil | unit | `go test ./tui/... -run TestBuildTUIDepsNilGuard` | ❌ W0 (extend wiring_test.go) | ⬜ pending |
| Real entry: `gitid` no-args + TTY launches TUI | D-16 | — | live wiring exercised | **manual** | N/A — see Manual-Only | ❌ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tui/model_test.go` — extend with two-pane layout tests: `TestRootModelTwoPaneRenders`, `TestSmallTerminal`, `TestSidebarVisible`, `TestSidebarCollapse`, `TestViewSwitch`
- [ ] `tui/sidebar_test.go` — new file for sidebar model tests (managed + unmanaged rows)
- [ ] `tui/health_test.go` — new file (port from `tui/dashboard_test.go`)
- [ ] `tui/wizard_test.go` — new file (port from `tui/prove_test.go` + form tests)
- [ ] `tui/confirm_test.go` — new file for delete/fix/rotate confirm + palette/help modal tests
- [ ] `tui/wiring_test.go` — EXTEND existing file to cover new `DeleteDeps` wiring (`TestBuildTUIDepsNilGuard`)
- [ ] Wave 0 spike: confirm `lipgloss.PlaceOverlay` exists at v2.0.3 (`go doc charm.land/lipgloss/v2 PlaceOverlay`); if absent, implement the documented line-by-line string-overlay fallback before any modal work.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real wired entry launches and is navigable | D-16, TUI-03..06 | Requires a real TTY; unit tests use injected fakes and cannot catch a nil live seam or alt-screen failure (recurring Phase 4/5 blindspot) | Build `make build`; run `./bin/gitid` (no args) on a real terminal → TUI launches in alt-screen; sidebar lists identities + the read-only unmanaged section; `Tab` cycles pane focus; `1`/`2`/`3` switch Identities/Health/Global Options; `a` opens the create wizard; `e` enters inline edit; `d` opens delete confirm; `?` opens help overlay; `q` quits cleanly restoring the terminal. |
| Color downsampling over SSH | TUI-03 | Needs real 256/16/no-color terminals + `NO_COLOR` to observe degradation | Run over SSH and with `NO_COLOR=1`; confirm severity glyphs (`✗`/`!`/`~`/`✓`) remain distinguishable without color (glyph carries meaning, not color alone). |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (incl. the PlaceOverlay spike)
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter
- [ ] `wave_0_complete: true` — set after Wave 0 (Plan 05.6-01) executes

**Approval:** approved 2026-06-15 (plan-checker verified; sampling continuity + coverage confirmed)
