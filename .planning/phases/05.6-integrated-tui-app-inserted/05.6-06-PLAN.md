---
phase: 05.6-integrated-tui-app-inserted
plan: 06
type: tdd
wave: 5
depends_on: ["05.6-05"]
files_modified:
  - tui/deps.go
  - tui/tui.go
  - tui/model.go
  - tui/confirm.go
  - tui/sidebar.go
  - tui/wiring_test.go
  - tui/confirm_test.go
  - tui/deps_test.go
autonomous: false
requirements: [TUI-04, TUI-06]
must_haves:
  truths:
    - "Pressing `d` opens a delete confirm modal stating consequences; on confirm the four artifacts are removed in-app via identity.Delete (no CLI handoff) with backup (TUI-06, D-09, SAFE-01)"
    - "Pressing `R` opens a rotate confirm modal; on confirm a new key is generated and the wizard test loop re-runs before writing (TUI-06, D-09, KEY-01)"
    - "buildTUIDeps wires identity.DeleteDeps; TestBuildTUIDepsNilGuard proves all 8 doctor CheckFn fields, FixPerm/RemoveBlock/AddWiring, identity.Generate, and every DeleteDeps field are non-nil on the REAL builder (D-16)"
    - "Unmanaged-section entries expose reveal-path / copy-pubkey / open-location only; no edit/health/management (D-13)"
    - "The real `gitid` no-args TTY entry launches and is fully navigable; the manual D-16 smoke step passes"
  artifacts:
    - path: "tui/deps.go"
      provides: "buildTUIDeleteDeps() + delete field on tuiDeps + 5-return buildTUIDeps"
      contains: "buildTUIDeleteDeps"
    - path: "tui/confirm.go"
      provides: "delete + rotate confirm flows wired through identity.Delete / identity.Rotate"
      contains: "identity.Delete"
  key_links:
    - from: "tui/deps.go buildTUIDeleteDeps"
      to: "filewriter.BackupAndRemove + gitconfig.RemoveAllowedSignersBlock"
      via: "mirror cmd/gitid/delete.go buildDeleteDeps"
      pattern: "BackupAndRemove|RemoveAllowedSignersBlock"
    - from: "tui/confirm.go rotate"
      to: "identity.Rotate → wizardProveModel"
      via: "rotate generates a new key then re-runs the test loop"
      pattern: "identity.Rotate"
    - from: "tui/wiring_test.go"
      to: "buildTUIDeps() real builder"
      via: "nil-guard over all live seams (D-16)"
      pattern: "TestBuildTUIDepsNilGuard"
---

<objective>
Final vertical slice: **in-app delete + rotate**, the **unmanaged-section affordances**,
and the **D-16 anti-blindspot closeout**. Wire `identity.DeleteDeps` into the TUI
(currently unwired), add delete and rotate confirm flows (no CLI handoff, D-09),
give unmanaged entries their read-only affordances (D-13), and lock the recurring
green-tests-over-injected-seams blindspot with `TestBuildTUIDepsNilGuard` over the REAL
builder plus the manual real-entry smoke test (D-16).

After this plan, the integrated app is feature-complete: delete and rotate run in-app
with confirm gates; unmanaged entries reveal/copy/open; and the live wiring is proven
non-nil on the real program entry. This is the capability that makes the app whole.

Purpose: Deliver delete/rotate (TUI-06), close D-16, and finalize the phase.
Output: `buildTUIDeleteDeps()` + 5-return `buildTUIDeps` in `tui/deps.go`, updated
`tui/tui.go` call site, delete/rotate flows in `tui/confirm.go`, unmanaged affordances
in `tui/sidebar.go`, and the wiring nil-guard + manual smoke.

This plan is NOT autonomous: it ends with a blocking human-verify checkpoint driving
the real `gitid` entry on a TTY (D-16 mandate).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-RESEARCH.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-PATTERNS.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-UI-SPEC.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-VALIDATION.md
@.planning/phases/05.6-integrated-tui-app-inserted/05.6-05-SUMMARY.md
@cmd/gitid/delete.go
@tui/deps.go
@tui/tui.go
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from drift checks):
- `buildTUIDeleteDeps() identity.DeleteDeps`, `delete identity.DeleteDeps` field on `tuiDeps`, 5-return `buildTUIDeps`
- delete/rotate driving in `confirmModel`: `runDeleteCmd`, `runRotateCmd`, `deleteConfirm`/`rotateConfirm` kinds (declared in Plan 03), `keepKey` toggle
- new modalKind values `deleteConfirmModal`, `rotateConfirmModal`
- unmanaged affordances in sidebar: `revealPath`, `copyUnmanagedPubkey`, `openLocation`
- `TestBuildTUIDepsNilGuard` (flipped from skip to active)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Wire identity.DeleteDeps and add the delete + rotate confirm flows (in-app, no CLI handoff)</name>
  <files>tui/deps.go, tui/tui.go, tui/model.go, tui/confirm.go, tui/confirm_test.go</files>
  <read_first>
    - cmd/gitid/delete.go (buildDeleteDeps at line 172 — the CANONICAL analog: RemoveFragment = filewriter.BackupAndRemove; RemoveAllowedSigners = gitconfig.RemoveAllowedSignersBlock; RemoveKeyFiles routes BOTH key + .pub through filewriter.BackupAndRemove; ReadSSH/ReadGitconfig/WriteSSH/WriteGitconfig)
    - internal/identity/delete.go (Delete signature + DeleteDeps + DeleteResult fields, verified by planner)
    - internal/identity/modes.go (Rotate(existing Account, deps Deps) (CreateResult, error) — reuses identity.Deps, already wired)
    - tui/deps.go (buildTUIUpdateDeps as the wiring template; buildIdentityDeps; current 4-return buildTUIDeps)
    - tui/confirm.go (Plan 03 confirmModel + confirmKind with deleteConfirm/rotateConfirm declared)
    - tui/wizard.go (Plan 04/05 wizardProveModel — rotate reuses its steps 2-4)
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-PATTERNS.md § "tui/deps.go", § "tui/confirm.go", § "tui/tui.go"
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-UI-SPEC.md § "Delete / Rotate Confirm Modals", § "Destructive Action Copy"
  </read_first>
  <behavior>
    - Test TestDeleteModal: `d` on a selected identity opens deleteConfirmModal; the body lists the artifacts to be removed and a "Delete key files too? [y/N]" toggle BEFORE the confirm prompt (Accessibility item 5); Esc cancels without removal.
    - Test TestDeleteDispatchesCore: Enter (after the keep-key choice) dispatches runDeleteCmd → identity.Delete(acct, keepKey, deps.delete); on success the modal closes, the sidebar drops the row (refreshSidebarMsg), and the detail pane shows empty state; on error shows "✗ delete failed: <err> [critical]".
    - Test TestRotateModal: `R` opens rotateConfirmModal stating the current key will be replaced; on confirm it transitions into the wizard test loop (steps 2-4) generating a new key via identity.Rotate, then writes only after PASS.
    - Test TestBuildTUIDeleteDepsWiring: buildTUIDeleteDeps() returns DeleteDeps with all 7 fields non-nil (ReadSSH, ReadGitconfig, WriteSSH, WriteGitconfig, RemoveFragment, RemoveAllowedSigners, RemoveKeyFiles); RemoveKeyFiles on a missing file is a no-op (mirrors cmd/gitid/delete.go semantics).
  </behavior>
  <action>
    Extend `tui/deps.go`: add `buildTUIDeleteDeps() identity.DeleteDeps` mirroring `cmd/gitid/delete.go` `buildDeleteDeps` (line 172) — the CANONICAL wiring (the PATTERNS.md sketch that writes empty bytes for RemoveFragment is WRONG; use the real helpers). Wire: `ReadSSH`/`ReadGitconfig` = os.ReadFile of the trusted paths; `WriteSSH`/`WriteGitconfig` = `filewriter.Write(path, content, 0o600)`; `RemoveFragment` = `filewriter.BackupAndRemove`; `RemoveAllowedSigners` = `gitconfig.RemoveAllowedSignersBlock`; `RemoveKeyFiles` = route BOTH key and .pub through `filewriter.BackupAndRemove` (a missing file is a no-op, empty backup, no error). Add a `delete identity.DeleteDeps` field to the `tuiDeps` struct (in model.go — this is the documented TODO from Plan 02). Change `buildTUIDeps` to return 5 values `(doctor.Deps, identity.Deps, identity.UpdateDeps, identity.DeleteDeps, error)` and update `newRootModel` to accept and store the delete deps. Update the `tui/tui.go` Run() call site to the 5-return signature and pass deleteDeps to `newRootModel` (PATTERNS § tui/tui.go).

    Extend `tui/confirm.go` (Plan 03): drive the `deleteConfirm` and `rotateConfirm` kinds. Delete: render the consequence statement (UI-SPEC Destructive Action Copy) + a `keepKey` toggle (default keep = safe) BEFORE the confirm; Enter (after the toggle choice) dispatches `runDeleteCmd(acct, keepKey, deps)` → `identity.Delete` in a recover()-wrapped goroutine → `deleteResultMsg`; success closes the modal, emits `refreshSidebarMsg`, and shows the delete-success toast; error keeps the modal open. Rotate: confirm modal → on confirm transition into the shared `wizardProveModel` steps 2-4, generating the new key via `identity.Rotate(acct, deps.identity)` (reuses identity.Deps) then re-running the upload+test loop and writing only after PASS. Wire `d`/`R` keys (Identities view) and the palette delete/rotate items to open these modals.
  </action>
  <verify>
    <automated>go test ./tui/... -run 'TestDeleteModal|TestDeleteDispatchesCore|TestRotateModal|TestBuildTUIDeleteDepsWiring' -count=1</automated>
  </verify>
  <done>
    Delete + rotate tests pass; `TestDeleteModal` flips from Plan 01 RED skip to green; buildTUIDeleteDeps mirrors cmd/gitid/delete.go; buildTUIDeps is 5-return; tui.go updated; delete/rotate run in-app with confirm gates; `make lint` clean.
  </done>
  <acceptance_criteria>
    - Delete + rotate tests pass; `TestDeleteModal` flips from Plan 01 RED skip to green.
    - buildTUIDeleteDeps mirrors cmd/gitid/delete.go (filewriter.BackupAndRemove for fragment + key files); buildTUIDeps is 5-return; tui.go call site updated.
    - Delete/rotate run in-app via identity.Delete/identity.Rotate (no CLI handoff); confirm gate before any removal/write.
    - `make lint` clean; English-only.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Unmanaged-section affordances + D-16 wiring nil-guard test</name>
  <files>tui/sidebar.go, tui/wiring_test.go, tui/deps_test.go</files>
  <read_first>
    - tui/sidebar.go (Plan 02 unmanagedEntry + unmanaged section render)
    - tui/copy.go (Plan 05 copyPubkeyModel — reused for unmanaged copy)
    - tui/wiring_test.go (Plan 01 TestBuildTUIDepsNilGuard skipped stub — flip to active; TestDepsThreadedEndToEnd pattern)
    - internal/doctor (Deps CheckFn fields: CheckDeps/CheckPerms/CheckCoherence/CheckOrphans/CheckSigning/CheckAgent/CheckBaseline/CheckOverlap — all 8; FixPerm/RemoveBlock/AddWiring)
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-RESEARCH.md § "The Recurring D-16 Blindspot — Anti-Pattern" + "Pattern to USE — wiring verification tests"
    - .planning/phases/05.6-integrated-tui-app-inserted/05.6-CONTEXT.md D-12, D-13
  </read_first>
  <behavior>
    - Test TestUnmanagedAffordances: with an unmanaged entry selected (sidebar selected == -1 section), the available actions are reveal-path, copy-pubkey, open-location ONLY; no edit/delete/health action is exposed (D-13).
    - Test TestUnmanagedCopyPubkeyOnly: copy-pubkey on an unmanaged key uses the copyPubkeyModel feeding only the .pub line (never the private key).
    - Test TestBuildTUIDepsNilGuard: buildTUIDeps() (real builder, 5-return) yields docDeps with all 8 CheckFn fields non-nil (CheckDeps, CheckPerms, CheckCoherence, CheckOrphans, CheckSigning, CheckAgent, CheckBaseline, CheckOverlap), FixPerm/RemoveBlock/AddWiring non-nil, idDeps.Generate non-nil, and deleteDeps all 7 fields non-nil (D-16).
  </behavior>
  <action>
    Extend `tui/sidebar.go` with the unmanaged-section affordances (D-13, read-only): when an `unmanagedEntry` is selected, expose `revealPath` (show the full path in the detail/toast area), `copyUnmanagedPubkey` (open the Plan 05 `copyPubkeyModel` with the entry's .pub line — public key only), and `openLocation` (reveal the file's directory; on macOS shell out via `open`, dispatched safely without shell interpolation; best-effort). Expose NO edit/health/management actions for unmanaged entries — "adopt into gitid" is explicitly deferred (CONTEXT Deferred Ideas). The footer/help for an unmanaged selection lists only these affordances.

    Flip `TestBuildTUIDepsNilGuard` in `tui/wiring_test.go` from the Plan 01 `t.Skip` to active, now that `buildTUIDeps` is 5-return. Assert against the REAL builder (not fakes — this is the whole point of D-16): all 8 `doctor.Deps` CheckFn fields non-nil, `FixPerm`/`RemoveBlock`/`AddWiring` non-nil, `identity.Deps.Generate` non-nil, and every `identity.DeleteDeps` field non-nil. This is the recurring-blindspot guard (Phase 4 doctor + Phase 5 TUI both shipped green tests over injected fakes that hid unwired live paths — MEMORY: doctor-injected-seam-wiring-blindspot). Add a `tui/deps_test.go` exercising `buildTUIDeleteDeps()` RemoveKeyFiles missing-file no-op (mirror cmd/gitid/delete_test.go).
  </action>
  <verify>
    <automated>go test ./tui/... -run 'TestUnmanagedAffordances|TestUnmanagedCopyPubkeyOnly|TestBuildTUIDepsNilGuard' -count=1 && make test</automated>
  </verify>
  <done>
    Unmanaged affordances limited to reveal/copy(pubkey-only)/open; `TestBuildTUIDepsNilGuard` is ACTIVE and green against the real builder covering all 8 CheckFn + fix fns + Generate + all DeleteDeps fields; `make test` exits 0; `make lint` clean.
  </done>
  <acceptance_criteria>
    - Unmanaged affordances limited to reveal/copy/open (D-13); copy is pubkey-only.
    - `TestBuildTUIDepsNilGuard` is ACTIVE (not skipped) and green against the real builder, covering all 8 CheckFn + fix fns + Generate + all DeleteDeps fields.
    - `make test` exits 0; `make lint` clean; English-only.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: D-16 manual smoke test — drive the REAL `gitid` no-args entry on a TTY</name>
  <files>none (manual verification — no code change)</files>
  <action>
    CHECKPOINT (no autonomous code). After Tasks 1-2 are green, the executor pauses and the human drives the real program per the steps in how-to-verify. This is the D-16 anti-blindspot gate: green unit tests over injected fakes do NOT satisfy it (Phase 4 doctor and Phase 5 TUI both shipped green-but-unwired). The executor must NOT mark the phase complete until the human reports the result.
  </action>
  <what-built>
    The complete integrated TUI app, wired through the REAL `gitid` no-args entry: two-pane shell, sidebar (managed + unmanaged), Identities/Health/Global Options views, create/add wizard, inline edit, delete, rotate, copy, in-app fix, palette, and help. All automated tui tests are green and `TestBuildTUIDepsNilGuard` passes against the real builder.
  </what-built>
  <how-to-verify>
    This is the D-16 manual smoke test (RESEARCH Pitfall 7; VALIDATION.md § Manual-Only). Unit tests over injected fakes do NOT satisfy it — drive the real program.

    1. `make build` (must succeed; honest install per CLAUDE.md).
    2. Run `./bin/gitid` (no args) on a REAL terminal (≥80×24). Expect: the TUI launches in alt-screen; the sidebar lists your managed identities (name · provider · (N)) plus, if present, the read-only Unmanaged section; the footer shows bold key hints.
    3. Press `tab` — focus cycles sidebar ↔ detail. Press `↑/↓` — selection moves; the detail pane fills for the selected identity (the sidebar does NOT disappear).
    4. Press `1`/`2`/`3` — views switch to Identities / Health / Global Options. In Health, families stream with spinners; a warning shows `!` (yellow) and an error shows `✗` (red) — visibly different.
    5. Press `Ctrl+P` — the palette opens and filters; `?` — the help overlay opens; `Esc` closes each.
    6. Press `a` — the create wizard opens; walk it (you may `Esc` out before writing). Press `e` on a selected identity — a field becomes editable; focus the match strategy and confirm the live includeIf preview updates.
    7. Press `d` — the delete confirm modal opens stating consequences (Esc to cancel, do NOT confirm unless on a throwaway identity). Press `R` — the rotate confirm opens (Esc to cancel).
    8. Press `c` — the copy modal opens; confirm it says the PUBLIC key was copied and shows the private key path as "(never copied)".
    9. Press `q` — the app quits cleanly and the terminal is restored (no garbled output).
    10. Resize the terminal below 80 cols — the sidebar collapses; `\` toggles it back. Below 80×24 — a plain "Terminal too small" message renders.
    11. Optional: run over SSH and with `NO_COLOR=1` — severity glyphs (`✓`/`✗`/`!`/`~`) remain distinguishable without color.

    Report any step where the real app diverges from the unit-test behavior (the exact divergence the D-16 mandate exists to catch).
  </how-to-verify>
  <verify>
    <human-check>All 11 smoke steps behave as described when driving the real `gitid` no-args entry on a TTY.</human-check>
  </verify>
  <done>The human has driven the real `gitid` no-args entry and confirmed every smoke step, or reported divergences that are then closed.</done>
  <resume-signal>Type "approved" if every step behaves as described, or describe the divergence(s) observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delete action → removal of four artifacts + key files | Destructive; must be confirm-gated with backup; key files removed only on explicit opt-in |
| rotate action → new key + re-test + write | Generates a new key and re-points artifacts; must re-run the prove loop before writing |
| unmanaged open-location → OS shell | Revealing a file location must not execute arbitrary paths |
| real builder seams → live program | The nil-guard boundary D-16 exists to protect (green fakes hid unwired live paths in Phases 4/5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.6-19 | Tampering | destructive delete without confirm | mitigate | Delete runs only after Enter on the confirm modal; consequence statement + keepKey toggle shown first; Esc is the safe default (D-09) |
| T-05.6-20 | Tampering | delete/rotate bypass backup | mitigate | buildTUIDeleteDeps routes every removal through filewriter.BackupAndRemove / gitconfig.RemoveAllowedSignersBlock (backup); rotate writes via identity.Deps after the prove loop (SAFE-01) |
| T-05.6-21 | Tampering | rotate writes for an un-authenticated new key | mitigate | Rotate reuses the wizard prove loop; write fires only after PASS + confirm (KEY-01, FIX-CREATE-01 semantics) |
| T-05.6-22 | Information Disclosure | unmanaged copy leaks the private key | mitigate | Unmanaged copy uses copyPubkeyModel feeding only the .pub line (D-13); TestUnmanagedCopyPubkeyOnly asserts it |
| T-05.6-23 | Tampering/Execution | open-location executes an arbitrary path | mitigate | open-location dispatches the directory path via os/exec without a shell (no `sh -c`); best-effort, never interpolated |
| T-05.6-24 | Spoofing | nil live seam hidden by green fakes (D-16) | mitigate | TestBuildTUIDepsNilGuard over the REAL builder (all 8 CheckFn + fix fns + Generate + all DeleteDeps fields) + the blocking manual smoke step driving `gitid` no-args |
| T-05.6-SC | Tampering | go installs | accept | No new dependencies (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go test ./tui/... -count=1` and `make test` green; `TestBuildTUIDepsNilGuard` ACTIVE and passing against the real builder.
- The blocking manual smoke step (Task 3) is approved: the real `gitid` no-args entry launches and every capability behaves as the unit tests claim (D-16).
- `make lint` green over the whole module.
</verification>

<success_criteria>
- In-app delete + rotate with confirm gates and backups; no CLI handoff (TUI-06, D-09, KEY-01).
- DeleteDeps wired; buildTUIDeps 5-return; nil-guard proves all live seams non-nil (D-16).
- Unmanaged entries expose reveal/copy(pubkey-only)/open only (D-13).
- Manual D-16 smoke approved — the integrated app is feature-complete and the live wiring matches the tests.
</success_criteria>

<output>
Create `.planning/phases/05.6-integrated-tui-app-inserted/05.6-06-SUMMARY.md` when done.
</output>
