---
phase: 09-upload-credentials-assist
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - internal/tuikit/views.go
  - internal/tuikit/identities.go
  - internal/tuikit/upload_section_test.go
  - internal/dummytui/fixturebackend.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
autonomous: true
requirements: [UP-01, UP-02, UP-03]
cross_ai: true

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "When `gh`/`glab` is absent or unauthenticated, the create wizard falls back to a manual step and never gates create — the wizard still reaches the Git step and the write still happens. (UP-02, UP-03, ROADMAP success criterion 3)"
    - "The manual-fallback block rendered in the wizard is byte-identical to `internal/upload.Instructions(provider)` — the same text the CLI prints and the same text UP-01 specifies (GitHub = two registrations, GitLab = one)."
    - "A GitHub identity whose key is already registered for authentication but not signing runs exactly ONE command (the signing registration) and renders two rows: one already-registered, one registered. (D-15 dedupe feeding D-16 per-type ensure)"
    - "A GitHub identity whose key is already registered for BOTH types runs zero commands and renders one collapsed already-registered line."
    - "Upload failure never changes what the wizard does next: every failure, degraded, and skipped path still auto-advances into the existing two-stage gate. (D-03, D-11)"
    - "gitid persists no upload state anywhere — no new file, no new field in any written artifact; the live tester's reachable-not-uploaded classification remains the only health signal. (D-18)"
  artifacts:
    - internal/tuikit/identities.go (the complete testUpload render branch, all four checkbox states, the manual-fallback slot, the row-budget guard)
    - internal/tuikit/views.go (UploadRunView gains AlreadyComplete; UploadEligibilityView gains the disabled/unauth render inputs)
    - cmd/gitid/wiring.go (realBackend.RunUpload's inventory-driven missing-type diff, classifier mapping, D-17 post-upload confirmation with one bounded retry)
    - internal/dummytui/fixturebackend.go (deterministic fixtures for every upload-section state)
  key_links:
    - "uploader.Inventory -> uploader.MissingRegistrations -> uploader.UploadKeys(only the missing set) -> uploader.ClassifyUploadFailure -> toUploadRunView -> UploadResultRow.Reason rendered through the frozen remediation constants"
    - "UploadEligibilityView.State -> the step-0 checkbox row's four render branches AND the step-1 decision to run, skip-to-manual-fallback, or omit the section entirely"
    - "every testUpload terminal state -> UploadRunMsg -> testRunning1 -> Backend.TestStage1 (one auto-advance path shared by success, partial failure, total failure, degraded, skipped)"
---

<objective>
Complete the shared upload-section component the tracer proved on one path: all four
eligibility states, both providers, the inventory-driven missing-type diff, the
classified failure reasons, the manual-fallback block, and D-17's post-upload
confirmation — all inside the create wizard's `testUpload` sub-beat and all within
the unchanged 100x30 frame.

Purpose: this is the plan that makes ROADMAP success criteria 1 and 3 true. Criterion
2 (autonomous when authenticated) already holds from the tracer; criterion 3
(absent/unauthenticated falls back to manual and never gates) and criterion 1
(concrete auth-and-signing steps) are delivered here, on every path the user can
actually reach.

Output: a create wizard whose upload section behaves correctly for every combination
of provider, tool presence, auth state, and prior registration — and which never,
under any of them, prevents the identity from being created.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-upload-credentials-assist/09-CONTEXT.md
@.planning/phases/09-upload-credentials-assist/09-UI-SPEC.md
@.planning/phases/09-upload-credentials-assist/09-RESEARCH.md
@.planning/phases/09-upload-credentials-assist/09-02-SUMMARY.md
@.planning/phases/09-upload-credentials-assist/09-03-SUMMARY.md
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: all four D-01 checkbox states plus the D-13 omitted branch, both providers</name>
  <files>internal/tuikit/identities.go, internal/tuikit/views.go, internal/tuikit/upload_section_test.go, internal/dummytui/fixturebackend.go</files>
  <behavior>
    - Provider `github.com` with `gh` present and authenticated: the row renders checked, using the on-glyph and the ready label, and is toggleable.
    - Provider `github.com` with `gh` present and NOT authenticated: the row renders unchecked, still toggleable, carrying the unauth label including the tool's own login-command hint. The user may check it anyway.
    - Provider `gitlab.com` with neither CLI on PATH: the row renders unchecked and dimmed with the disabled label, and toggling it — by `space`, by `u`, by arrow, or by click — leaves it unchecked.
    - Provider `git.internal.example` (or any host outside the two main domains): the row does NOT render at all, and the step-0 focus order does not contain the checkbox slot.
    - Every one of the three rendered states occupies exactly ONE physical row at the wizard's detail-pane width — the reason text is inline on the same row, never a second line.
    - Each state is legible with color disabled: the state is carried by the glyph plus the words, and removing color leaves every distinction intact.
    - `FixtureBackend.UploadEligibility` answers deterministically from the hostname alone, never from the machine's PATH.
  </behavior>
  <read_first>
    - `internal/tuikit/identities.go` — the checkbox row render and `wizardStep0FocusOrder` as plan 09-02 left them; `sshForm.view` (470-529) and its focused-only-helper row-budget discipline; `renderWizard`'s step-0 branch (4148-4161).
    - `internal/tuikit/identities.go` lines 4192-4223 — the documented earlier row-budget regression at this exact pane ("any line naming it — or any other line in this pane long enough to wrap — silently costs an EXTRA physical row"), and the `glyphCheckOff`/`glyphCheckOn` composition.
    - `internal/tuikit/design.go` — `UploadCheckboxLabelReadyFmt`, `UploadCheckboxLabelUnauthFmt`, `UploadCheckboxLabelDisabledFmt`. The unauth label takes THREE format arguments (provider, tool, tool); read plan 09-01's SUMMARY for the exact argument order it froze.
    - `.planning/phases/09-upload-credentials-assist/09-UI-SPEC.md` lines 113-135 — the states table; lines 139-168 — "Focal points", whose FIRST bullet is this row's binding constraint ("must read correctly as ONE line in all three states ... the disabled/hint reason is inline on the same row, never a stacked line underneath").
    - `.planning/phases/09-upload-credentials-assist/09-UI-SPEC.md` lines 186-241 — the Typography and Color role tables: Warning for the disabled reason and the unauth hint, Accent only for the focus highlight, and the non-negotiable glyph-plus-word rule.
    - `.planning/phases/09-upload-credentials-assist/09-CONTEXT.md` D-01 and D-13 — the three-scenario derivation and the main-domain-only gate.
    - `internal/tuikit/theme.go` — `styleWarning`, `styleFaint`, `styleSelected`, and how existing rows compose them; Phase 9 adds no new theme role.
    - `internal/dummytui/fixturebackend.go` — the `UploadEligibility` fixture plan 09-02 added, which this task extends to answer the GitLab and unknown-host cases too.
    - `.planning/design/create-flow/FIELDS.md` — the `auto_upload_checkbox` row plan 09-01 added; its Notes column is the contract this render satisfies.
  </read_first>
  <action>
Complete the step-0 checkbox row so all four `UploadEligibilityState` values render
correctly for both providers.

In `internal/tuikit/identities.go`:
- Replace the tracer's ready-only render branch with a small
  `renderUploadCheckboxRow(view UploadEligibilityView, checked, focused bool, width int) string`
  helper next to `sshForm.view`'s other row builders. It returns the empty string for
  `UploadEligibilityOmitted`, and otherwise composes: the glyph
  (`glyphCheckOn` when checked, `glyphCheckOff` otherwise), a space, and the state's
  frozen label rendered with the role from the UI-SPEC Typography table — plain for
  ready, `styleWarning` for unauth and disabled, plus `styleFaint` over the whole row
  for disabled. When `focused` is true, apply the existing focus treatment the other
  form rows use (`styleSelected`), never a new hue.
- Hard-bound the row to one physical line: truncate the composed row at `width` with
  the same no-ellipsis `ansi.Truncate` call `renderStageEvidence` uses at
  `identities.go:1540`, so the wizard's row budget can never be blown by a long
  provider or tool name. Add a code comment citing the Focal-points constraint.
- Gate toggling on the state: `space`, `left`/`right`, `u`, and the click handler all
  route through ONE `toggleUploadCheckbox()` method on `wizardModel` that returns the
  model unchanged when the state is `UploadEligibilityDisabled` or
  `UploadEligibilityOmitted`. Four affordances must not be able to disagree about
  whether a disabled row can be checked.
- Make `wizardStep0FocusOrder`'s `uploadRowVisible` argument derive from the resolved
  state (visible for disabled/unauth/ready, absent for omitted) at both call sites,
  and add a guard so that if the resolved state transitions to omitted while the
  checkbox slot holds focus, focus falls back to the first slot rather than being
  stranded on a slot no longer in the order.
- Resolve the eligibility view from `w.form.providerHost()` — the same accessor the
  existing `unknownProvider`/`altSSHHint` logic at `identities.go:513-518` uses — so
  the checkbox and the alt-SSH port hint agree about what the provider is.

In `internal/tuikit/views.go`: add a `String()`-free doc note on
`UploadEligibilityState` recording that Omitted must render nothing at all (not an
empty row), because a blank row still costs a line in the 100x30 budget.

In `internal/dummytui/fixturebackend.go`: extend `UploadEligibility` to answer
`Ready` for a github host, `Unauth` for a gitlab host, and `Omitted` for anything
else, purely from the hostname string. Record in a comment that these three fixed
answers exist so the demo can show three of the four states without probing the
machine, that the fourth (disabled) is exercised by unit tests and by the PTY suite's
no-CLI-on-PATH case, and that the real binary's machine-dependent answer is a
divergence plan 09-07 classifies.

Tests in `internal/tuikit/upload_section_test.go`:
`TestUploadCheckboxRendersAllFourStates` (table-driven; assert the rendered row
contains the expected frozen label for ready/unauth/disabled and is empty for
omitted), `TestUploadCheckboxRowIsExactlyOnePhysicalLine` (assert the rendered row
contains no newline and its display width is at most the pane width, for all three
visible states, using the longest realistic provider and tool names),
`TestDisabledUploadCheckboxCannotBeToggled` (drive `space`, `u`, `right`, and a
synthetic click; assert `uploadChecked` stays false in all four),
`TestOmittedUploadStateRemovesTheFocusSlot` (assert `wizardStep0FocusOrder` output
and that Tab from Port lands on the key-source toggle),
`TestUploadCheckboxIsLegibleWithoutColor` (render with the no-color path the
project's existing tests use — locate it by grepping the tuikit tests for the
existing NO_COLOR-style assertion helper and reuse it, do not invent a second one —
and assert each state's distinguishing WORDS are present).
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... ./internal/dummytui/... -run 'TestUploadCheckbox|TestDisabledUploadCheckbox|TestOmittedUploadState|TestWizardStep0FocusOrder'</automated>
  </verify>
  <acceptance_criteria>
    - `TestUploadCheckboxRendersAllFourStates` covers exactly four cases and asserts the omitted case renders the empty string.
    - `TestUploadCheckboxRowIsExactlyOnePhysicalLine` passes for all three visible states using provider and tool name inputs at least as long as the longest real values.
    - `TestDisabledUploadCheckboxCannotBeToggled` exercises all four toggle affordances and asserts the state is unchanged after each.
    - `TestOmittedUploadStateRemovesTheFocusSlot` asserts the returned focus order does not contain `wizardFocusUploadCheckbox` and that a Tab from the Port slot lands on `wizardFocusKeySource`.
    - `TestUploadCheckboxIsLegibleWithoutColor` asserts the distinguishing words of each state survive with color disabled.
    - `go build ./...` succeeds and `var _ tuikit.Backend = FixtureBackend{}` still compiles.
    - `make gate-copy-freeze` passes — every label rendered comes from a plan-09-01 constant.
    - `make lint` reports zero issues.
  </acceptance_criteria>
  <done>
The auto-upload checkbox reads correctly and stays on one line in every state, a
disabled row cannot be checked through any of its four affordances, an
out-of-scope provider costs zero rows and zero focus slots, and the dummy answers
deterministically.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the complete testUpload beat — inventory diff, per-type rows, classified reasons, degraded note, manual fallback</name>
  <files>cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/tuikit/identities.go, internal/tuikit/views.go, internal/tuikit/upload_section_test.go, internal/dummytui/fixturebackend.go</files>
  <behavior>
    - GitHub, nothing registered: two announce lines and two result rows (authentication, signing), in that order.
    - GitHub, authentication already registered: ONE announce line (signing) and TWO rows — an already-registered row for authentication and a registered row for signing.
    - GitHub, both already registered: ZERO announce lines, ZERO commands executed, and one collapsed already-complete line.
    - GitLab, nothing registered: ONE announce line and ONE combined-registration row.
    - GitHub with the signing scope missing: two rows — authentication registered, signing failed with the signing-scope remediation reason. The authentication attempt is NOT rolled back and the signing failure does not suppress it.
    - GitLab already-taken response: one failed row whose reason is the cross-account-conflict copy, plus the manual-fallback block.
    - Inventory read fails: the degraded note renders, the upload still runs for every requested registration, and the outcome is otherwise normal.
    - Checkbox unchecked, or eligibility unauth, or every attempted registration failed: the manual-fallback block renders — byte-identical to `Backend.UploadInstructions(provider)` — under the frozen manual heading.
    - Every one of the above paths ends by auto-advancing into the existing `testRunning1` stage, with no keystroke and no change to the wizard's own gate.
    - The whole step-1 pane still fits the 100x30 frame in the worst realistic case (two announce lines plus two result rows plus the multi-line GitHub instructions block).
  </behavior>
  <read_first>
    - `cmd/gitid/wiring.go` — `realBackend.RunUpload` and `toUploadRunView` as plans 09-02/09-03 left them; `stagedKeyFor` (4281) and its cache doc comment; `toTestResultView` and the DTO-conversion-in-one-place rule.
    - `internal/uploader/inventory.go` and `classify.go` (plan 09-03) — `Inventory`, `MissingRegistrations`, `HasRegistration`, `ClassifyUploadFailure`, `ClassifyGHDuplicate`, `RedactCLIOutput`, and their exact signatures.
    - `internal/upload/upload.go` lines 31-54 — `Instructions(provider)`: the GitHub branch is 5 lines, the GitLab branch is 2. These are the row costs the fallback slot must absorb.
    - `internal/tuikit/design.go` — `UploadRunningLineFmt`, `UploadResultOKFmt`, `UploadResultSkippedFmt`, `UploadResultFailedFmt`, `UploadScopeRemediationAuthFmt`, `UploadScopeRemediationSigningFmt`, `UploadCrossAccountConflict`, `UploadInventoryDegradedFmt`, `UploadAlreadyCompleteFmt`, `UploadManualHeading`, `UploadRegistrationLabelAuth`/`Signing`/`Combined`.
    - `.planning/phases/09-upload-credentials-assist/09-UI-SPEC.md` lines 113-135 (the states table, including the `announcing`/`per-key-results`/`manual-fallback` triple), lines 172-183 (the row-cost table: 1 row per announce line, 1 per result row, up to 5 rows for the GitHub instructions), and lines 321-338 (UI Considerations — the two `🧪 backstop` rows for the loading transition and the overflow case, and the `unresolved` row this plan closes as continue-on-failure).
    - `.planning/phases/09-upload-credentials-assist/09-UI-SPEC.md` line 334 — the `zero-one-many` row, whose 0-command copy plan 09-01 froze as `UploadAlreadyCompleteFmt`.
    - `internal/tuikit/identities.go` lines 1495-1552 — `renderStageOutcome`/`renderStageEvidence`: the exact command/PASS-row treatment the upload rows must visually match (09-UI-SPEC.md's second Focal point) and the no-ellipsis truncation rule.
    - `internal/tuikit/identities.go` lines 4162-4245 — `renderWizard`'s step-1 branch and its documented row-budget history.
    - `internal/tuikit/identities.go` line 4188 — `w.renderProof(width)`, the existing viewport; and `internal/tuikit/viewport`-style helpers used elsewhere for the Phase-7 scrolling-window cue, which is the documented fallback if the fallback block overflows.
    - `cmd/gitid/wiring_test.go` — the fake-`Deps` construction pattern used by the existing backend tests, so the new tests drive `realBackend.RunUpload` without a real `gh`.
  </read_first>
  <action>
Complete `realBackend.RunUpload` and the `testUpload` render branch.

In `cmd/gitid/wiring.go`, restructure `RunUpload`'s closure into these ordered steps,
each of which must be individually observable in the returned `UploadRunView`:
1. Resolve eligibility. `Omitted` or `Disabled` -> return a view with `Skipped: true`
   and `ManualFallback` set from `upload.Instructions(provider)` for the
   non-omitted case; omitted returns a view with no rows and no fallback (the section
   is absent entirely).
2. Stage the key through the SAME `b.stagedKeyFor` call `TestStage1` uses; derive the
   public-key path from the staged result's public-key field only.
3. Read the `.pub` line from disk and compute the desired registration set:
   `[RegistrationAuthentication, RegistrationSigning]` for gh,
   `[RegistrationCombined]` for glab.
4. Call `uploader.Inventory`. On error, set `InventoryDegraded: true` and treat the
   missing set as the FULL desired set (degrade to a plain upload — D-15's never-gates
   rule, and the reason the field exists). On success, call
   `uploader.MissingRegistrations`.
5. If the missing set is empty, return a view with `AlreadyComplete: true` and no
   rows. Add `AlreadyComplete bool` to `UploadRunView` in `views.go` in this task.
6. Otherwise call `uploader.UploadKeys` with ONLY the missing set, then convert each
   `RegistrationResult` in `toUploadRunView`: `OutcomeUploaded` -> an uploaded row;
   `ClassifyGHDuplicate` true -> an already-present row; `OutcomeFailed` -> a failed
   row whose `Reason` comes from mapping `ClassifyUploadFailure`'s kind onto the
   frozen remediation constants, falling back to
   `RedactCLIOutput(res.Output, b.home, <the pane width budget>)` for the unknown
   kind. Rows for registrations the inventory reported as already present are
   appended too, so a partially-registered GitHub identity still shows both types.
7. Set `ManualFallback` when the checkbox path did not run, or when EVERY attempted
   row failed. A partial success does not print the whole fallback block — the
   per-row remediation reason is the actionable instruction there, and printing five
   more lines would push step 1 out of its row budget for no added information.
   Record that reasoning as a code comment.
Wrap the whole closure so that a panic or an unexpected error still yields a
`UploadRunMsg` — the create flow must reach `testRunning1` from every path.

In `internal/tuikit/identities.go`, complete the `case testUpload:` render branch:
- Before rows arrive: one `UploadRunningLineFmt` line per command about to run,
  using the same faint command styling `renderStageEvidence` applies, so the beat
  reads as one more stage in the same test loop (09-UI-SPEC.md's second Focal point).
- After rows arrive: one row per `UploadResultRow` using `UploadResultOKFmt` +
  `styleHealthy`, `UploadResultSkippedFmt` + `styleInfo`, or `UploadResultFailedFmt`
  + `styleError`, each carrying its glyph AND its word.
- `InventoryDegraded` -> one `UploadInventoryDegradedFmt` line above the rows.
- `AlreadyComplete` -> the single `UploadAlreadyCompleteFmt` line instead of rows.
- `ManualFallback != ""` -> the frozen `UploadManualHeading` followed by the block
  verbatim. Do NOT reformat, re-indent, or re-wrap it: UP-02's shown==run contract
  and 09-UI-SPEC.md's third Focal point both require byte-identical reuse.
- Guard the total row cost: compute the rows this branch is about to emit and, if it
  would exceed the pane's remaining budget, route the fallback block through the
  SAME bounded viewport mechanism the wizard already uses for its proof text
  (`w.renderProof`'s viewport, or the Phase-7 scrolling-window cue) rather than
  letting it overflow the frame. 09-UI-SPEC.md's `overflow` row names this
  explicitly as a backstop to verify at render time; make it a real branch with a
  test, not an assumption.

In `internal/dummytui/fixturebackend.go`, extend `RunUpload` so the demo can show the
full set of states deterministically — drive the returned view from the hostname the
spec carries (github -> two rows both uploaded; gitlab -> one combined row; a
seeded fixture host -> a partial-failure pair with the signing remediation reason).
Keep it in-memory with the same tick delay the existing fixture stages use.

Tests:
`cmd/gitid/wiring_test.go` (fake `uploader.Deps`, real `realBackend`):
`TestRunUploadUsesOnlyTheMissingRegistrations` (inventory reports the authentication
type present; assert exactly one `ssh-key add` invocation and that its argv carries
the signing type), `TestRunUploadZeroCommandsWhenFullyRegistered` (assert zero
`ssh-key add` invocations and `AlreadyComplete` true),
`TestRunUploadDegradesWhenInventoryFails` (assert `InventoryDegraded` true and that
the full desired set was still attempted),
`TestRunUploadPartialScopeFailureReportsBothTypes` (assert two rows, one uploaded
and one failed, and that the failed row's Reason is the signing remediation string),
`TestRunUploadGLabTakenIsAConflictWithFallback` (assert the row's Reason is the
cross-account copy and `ManualFallback` is non-empty),
`TestRunUploadNeverReturnsAnErrorMsg` (drive staging failure, detect failure, auth
failure, and upload failure; assert every case yields a `UploadRunMsg`),
`TestRunUploadSharesTheStagedKeyWithTestStage1` (assert the public-key path
`RunUpload` passed to the uploader equals the one `TestStage1` stages for the same
spec).
`internal/tuikit/upload_section_test.go`:
`TestUploadResultRowsRenderGlyphAndWord` (all three outcomes),
`TestManualFallbackIsByteIdenticalToInstructions` (compare the rendered block
against `upload.Instructions` output obtained through the fixture's
`UploadInstructions`, asserting exact equality after stripping the heading),
`TestUploadSectionFitsTheFrameInTheWorstCase` (two announce lines + two result rows
+ the GitHub instructions block; assert the rendered step-1 pane's line count is
within `frameBodyRows(30)`), `TestEveryUploadTerminalStateAutoAdvances` (table over
success, partial, total failure, degraded, already-complete, and skipped; assert
each yields `testRunning1` and a non-nil command).
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid/... ./internal/tuikit/... ./internal/dummytui/... -run 'TestRunUpload|TestUploadResultRows|TestManualFallbackIsByteIdentical|TestUploadSectionFitsTheFrame|TestEveryUploadTerminalStateAutoAdvances'</automated>
  </verify>
  <acceptance_criteria>
    - `TestRunUploadUsesOnlyTheMissingRegistrations` asserts exactly one recorded `ssh-key add` invocation and that its argv contains the signing key-type value.
    - `TestRunUploadZeroCommandsWhenFullyRegistered` asserts zero recorded `ssh-key add` invocations.
    - `TestRunUploadPartialScopeFailureReportsBothTypes` asserts `len(view.Rows) == 2` with one uploaded outcome and one failed outcome, and that the failed row's Reason equals the frozen signing-remediation string rendered for the identity's host.
    - `TestRunUploadNeverReturnsAnErrorMsg` drives four distinct failure injections and asserts all four return a `UploadRunMsg`.
    - `TestRunUploadSharesTheStagedKeyWithTestStage1` asserts path equality, proving one staged result is shared rather than two independently staged.
    - `TestManualFallbackIsByteIdenticalToInstructions` asserts exact string equality against `upload.Instructions(provider)` output — not a substring match.
    - `TestUploadSectionFitsTheFrameInTheWorstCase` asserts the rendered pane's line count is at most `frameBodyRows(30)`, and the overflow branch is exercised by at least one case that triggers it.
    - `TestEveryUploadTerminalStateAutoAdvances` covers six states and asserts `testRunning1` plus a non-nil command for each.
    - `make gate-copy-freeze` passes; `make lint` reports zero issues; `TERM=dumb SSH_AUTH_SOCK= go test -race ./...` passes.
  </acceptance_criteria>
  <done>
Every combination of provider, tool state, and prior registration produces the
correct announce lines, the correct result rows, the correct classified reason, and
the correct fallback — and every one of them still hands the wizard to its existing
two-stage gate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-17 post-upload verification with one bounded retry, and the D-18 no-persisted-state assertion</name>
  <files>cmd/gitid/wiring.go, cmd/gitid/wiring_test.go, internal/tuikit/upload_section_test.go</files>
  <behavior>
    - After a successful upload, the backend performs ONE post-upload inventory read to confirm the registrations it just created are actually present — including the signing registration, which the `ssh -T` probe structurally cannot see.
    - When that confirmation read shows a registration still missing, the backend waits a bounded interval and re-reads exactly once. A second miss is reported as an unconfirmed row, not retried again and not escalated to a failure that gates.
    - The `ssh -T` verification D-17 describes is the wizard's EXISTING stage-1/stage-2 gate, unchanged: no new probe function is added, and the reachable-not-uploaded classification remains `internal/tester`'s job.
    - Nothing about an upload is written to disk: no new file, no new key in any managed block, no new field in any written artifact.
  </behavior>
  <read_first>
    - `.planning/phases/09-upload-credentials-assist/09-CONTEXT.md` D-17 and D-18 — the verification requirement (re-run the existing `ssh -T` expecting reachable-not-uploaded to become PASS, PLUS one post-upload inventory read to confirm the signing registration the probe cannot see, PLUS one bounded retry for propagation lag) and the no-persisted-state rule with its stated reason (derived-state files are this repo's known doctor false-positive-loop footgun).
    - `.planning/phases/09-upload-credentials-assist/09-RESEARCH.md` "Don't Hand-Roll" third row — `internal/tester.PreWrite`/`ResolvedVia` are the SAME functions the wizard's own stages call; no bespoke post-upload probe may be added.
    - `internal/tester/tester.go` lines 10-23 — the three-way `Outcome` classification D-17 reuses unmodified.
    - `cmd/gitid/wiring.go` — `TestStage1`/`TestStage2` (1069-1130), `recordOutcomeFor`, and the `mu`/`txMu` lock contract at lines 98-128. The confirmation read must not hold `txMu` (it performs no file mutation) and must not block the Bubble Tea update loop — it runs inside `RunUpload`'s existing `tea.Cmd` goroutine.
    - `cmd/gitid/wiring.go` — how other backend methods obtain a clock or a delay, and whether an injectable clock/sleep seam already exists (grep for `archiveClockNow`, the test-only clock override at line 173). Reuse that pattern for the retry interval so the test does not sleep in real time.
    - `.planning/phases/08-health-fixer/08-CONTEXT.md` and the project MEMORY note "Doctor reserved-block false-positive loop" — the concrete reason D-18 forbids persisted upload state.
  </read_first>
  <action>
Add the D-17 confirmation to `realBackend.RunUpload`'s closure, after the upload
step and before the `UploadRunMsg` is returned.

- Perform ONE `uploader.Inventory` read and check, with `HasRegistration`, that each
  registration this run reported as uploaded is now present. Registrations the run
  reported as already-present are checked too — a confirmation that contradicts the
  pre-upload inventory is worth surfacing.
- If any expected registration is absent, wait one bounded interval and re-read
  exactly once. Take the interval from an injectable field on `realBackend`
  following the existing test-only-override precedent (`archiveClockNow` at
  `wiring.go:173`) so the unit test can drive it to zero; production uses a small
  fixed value chosen to cover ordinary propagation lag without making the wizard
  feel stalled. Record the chosen production value and its rationale in the SUMMARY.
- After at most those two reads, mark any still-absent registration's row as
  unconfirmed. Represent this on the existing `UploadResultRow` by setting the row's
  `Reason` and leaving its `Outcome` as uploaded — do NOT invent a fourth outcome
  value. The registration genuinely was accepted by the provider; only the
  confirmation is pending, and turning that into a failure would misreport a
  successful upload. Add a code comment stating that reasoning.
- If the confirmation read itself errors, set `InventoryDegraded` and skip
  confirmation entirely. Never gate, never retry a failing read a second time.
- Add a doc comment on `RunUpload` stating explicitly that the `ssh -T` half of
  D-17's verification is the wizard's existing stage-1/stage-2 gate, which runs
  immediately after this command's message auto-advances the wizard — that is WHY
  D-05 places upload before the test loop, and why no new probe belongs here.

Add `TestNoPersistedUploadState` in `cmd/gitid/wiring_test.go`: run a full
`RunUpload` against a seeded temporary HOME with a fake `Deps`, snapshot the
recursive file listing plus each file's content hash before and after, and assert
they are identical. This is the mechanical enforcement of D-18 and the direct
countermeasure to this repository's documented false-positive-loop footgun; a
future plan that quietly caches an upload receipt fails here by name.

Add `TestPostUploadConfirmationRetriesExactlyOnce` in `cmd/gitid/wiring_test.go`:
a fake whose inventory returns the missing registration on the first two reads;
assert the recorded inventory invocation count for the confirmation phase is exactly
two (never three), and that the resulting row is still an uploaded outcome carrying
an unconfirmed reason.

Add `TestPostUploadConfirmationSucceedsOnFirstRead`: a fake whose confirmation read
shows both registrations; assert exactly one confirmation read and no unconfirmed
reason on any row.

Add `TestConfirmationFailureDegradesInsteadOfGating`: a fake whose confirmation read
errors; assert `InventoryDegraded` is true, exactly one confirmation read occurred,
and the returned message still auto-advances the wizard.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid/... -run 'TestNoPersistedUploadState|TestPostUploadConfirmation|TestConfirmationFailureDegrades|TestRunUpload'</automated>
  </verify>
  <acceptance_criteria>
    - `TestNoPersistedUploadState` compares a recursive path+content-hash snapshot of the seeded HOME before and after a full `RunUpload` and asserts they are byte-identical.
    - `TestPostUploadConfirmationRetriesExactlyOnce` asserts the confirmation-phase inventory invocation count equals exactly 2.
    - `TestPostUploadConfirmationSucceedsOnFirstRead` asserts the count equals exactly 1 and that no row carries an unconfirmed reason.
    - `TestConfirmationFailureDegradesInsteadOfGating` asserts `InventoryDegraded` is true and the returned message is a `UploadRunMsg`.
    - No new `tester`-layer function was added: `git diff --stat internal/tester/` shows zero changes.
    - `UploadRowOutcome` still has exactly three values (no fourth was added for the unconfirmed case); assert the constant count in a test.
    - The retry interval is reachable through a test-only override field on `realBackend`, following the `archiveClockNow` precedent, and the test does not sleep in real time (the whole test file still completes well inside the package's normal runtime).
    - `make lint` reports zero issues; `TERM=dumb SSH_AUTH_SOCK= go test -race ./...` passes.
  </acceptance_criteria>
  <done>
The signing registration `ssh -T` cannot see is confirmed by one inventory read with
exactly one bounded retry, an unconfirmed registration is reported honestly without
being misreported as a failure, the confirmation never gates, and a snapshot test
proves gitid persists nothing about uploads.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Go symbols this PLAN creates:
`tuikit.renderUploadCheckboxRow`, `wizardModel.toggleUploadCheckbox`,
`tuikit.UploadRunView.AlreadyComplete` (new field);
`realBackend`'s post-upload confirmation step and its test-only retry-interval
override field; the extended `FixtureBackend.UploadEligibility` and
`FixtureBackend.RunUpload` fixture branches.
New tests: `TestUploadCheckboxRendersAllFourStates`,
`TestUploadCheckboxRowIsExactlyOnePhysicalLine`,
`TestDisabledUploadCheckboxCannotBeToggled`,
`TestOmittedUploadStateRemovesTheFocusSlot`,
`TestUploadCheckboxIsLegibleWithoutColor`,
`TestRunUploadUsesOnlyTheMissingRegistrations`,
`TestRunUploadZeroCommandsWhenFullyRegistered`,
`TestRunUploadDegradesWhenInventoryFails`,
`TestRunUploadPartialScopeFailureReportsBothTypes`,
`TestRunUploadGLabTakenIsAConflictWithFallback`,
`TestRunUploadNeverReturnsAnErrorMsg`,
`TestRunUploadSharesTheStagedKeyWithTestStage1`,
`TestUploadResultRowsRenderGlyphAndWord`,
`TestManualFallbackIsByteIdenticalToInstructions`,
`TestUploadSectionFitsTheFrameInTheWorstCase`,
`TestEveryUploadTerminalStateAutoAdvances`, `TestNoPersistedUploadState`,
`TestPostUploadConfirmationRetriesExactlyOnce`,
`TestPostUploadConfirmationSucceedsOnFirstRead`,
`TestConfirmationFailureDegradesInsteadOfGating`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider CLI output → rendered failure reason | Untrusted external text reaches the user's terminal. |
| upload outcome → create-flow control flow | An upload result must never be able to change whether the identity gets created. |
| upload outcome → disk | The boundary D-18 forbids crossing at all. |
| eligibility state → toggle affordances | Four independent affordances (key, arrow, hotkey, mouse) must agree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Denial of Service | `RunUpload` control flow | high | mitigate | Every path — omitted, disabled, unauth, skipped, degraded, partial failure, total failure, panic — returns a `UploadRunMsg` that auto-advances into the existing gate. `TestRunUploadNeverReturnsAnErrorMsg` and `TestEveryUploadTerminalStateAutoAdvances` assert this across ten combined cases. Upload never gates (D-03/D-11) is the single most load-bearing invariant in this phase. |
| T-09-04-02 | Information Disclosure | unknown-kind failure reason | medium | mitigate | The unknown-kind reason is routed through plan 09-03's `RedactCLIOutput` with the real home directory passed in, so an absolute path or a token-shaped substring cannot reach the terminal. |
| T-09-04-03 | Tampering | persisted upload state | high | mitigate | `TestNoPersistedUploadState` snapshots the seeded HOME's recursive path+content hashes before and after a full run and asserts equality. D-18 exists because derived-state files caused this repository's documented destructive doctor false-positive loop; a snapshot test is the only mitigation that survives a future well-intentioned cache. |
| T-09-04-04 | Elevation of Privilege | disabled-checkbox bypass | medium | mitigate | All four toggle affordances route through one `toggleUploadCheckbox` method that refuses the disabled and omitted states; `TestDisabledUploadCheckboxCannotBeToggled` drives all four. A UI that lets a user check a box for a tool that is not installed would then attempt an upload with an unresolved binary path. |
| T-09-04-05 | Repudiation | unconfirmed registration reporting | medium | mitigate | A registration the provider accepted but whose confirmation read has not yet caught up is reported as uploaded WITH an unconfirmed reason, not as a failure and not silently as a plain success. No fourth outcome value is introduced, asserted by a constant-count test. |
| T-09-04-06 | Denial of Service | row-budget overflow | low | mitigate | The worst realistic case (two announce lines, two result rows, the five-line GitHub instructions block) is asserted to fit `frameBodyRows(30)`, with the existing bounded-viewport mechanism as the tested overflow branch — closing 09-UI-SPEC.md's `overflow` backstop row with a real branch rather than an assumption. |
| T-09-04-SC | Tampering | npm/pip/cargo installs | high | accept | Not applicable — zero new module dependencies in this plan or anywhere in Phase 9. |
</threat_model>

<verification>
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` passes.
- `go test -tags e2e -race -timeout 900s ./e2e/...` passes — the tracer's PTY tests still hold with the completed section.
- `make lint` reports zero issues.
- `make gate-copy-freeze` passes.
- `make test` passes.
- `git status --porcelain` after `TestNoPersistedUploadState` shows no stray artifacts.
</verification>

<success_criteria>
- Absent or unauthenticated `gh`/`glab` falls back to a manual step and never gates
  create. (ROADMAP success criterion 3, UP-02, UP-03)
- The manual-fallback block is byte-identical to `internal/upload.Instructions`,
  delivering UP-01's auth-and-signing steps inside the wizard.
- The inventory-driven missing-type diff means an already-registered key is skipped
  and a partially-registered GitHub identity converges on the next run. (D-15, D-16)
- GitLab's cross-account conflict surfaces as a finding with manual instructions.
- D-17's confirmation covers the signing registration `ssh -T` cannot see, with
  exactly one bounded retry.
- D-18 holds mechanically: a before/after disk snapshot proves nothing was persisted.
</success_criteria>

<output>
Create `.planning/phases/09-upload-credentials-assist/09-04-SUMMARY.md` when done.
</output>
</content>
