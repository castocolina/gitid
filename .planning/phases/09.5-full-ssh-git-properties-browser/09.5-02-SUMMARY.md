---
phase: 09.5-full-ssh-git-properties-browser
plan: 02
subsystem: ui
tags: [bubbletea, lipgloss, git-config, tui, sub-tabs]

# Dependency graph
requires:
  - phase: 09.5-full-ssh-git-properties-browser
    provides: "plan 09.5-01's proven stack shape (sub-tab strip pattern, filterable master-detail list, filter keyboard-capture contract, copy-freeze double gate) — merged onto the worktree base (f9a04ce)"
provides:
  - "internal/tuikit's shared subTabStripRows()/renderSubTabStrip() — the ONE bordered sub-tab strip renderer used by both Global SSH and Global Git (D-D)"
  - "internal/globalgit.AllSetKeys(deps) — every git config key actually SET on the machine, with scope and origin"
  - "tuikit.GitPropertiesBrowser seam (AllGitSetKeys) with both realBackend and FixtureBackend implementations"
  - "Global Git's 'Set keys' sub-tab: the same flat filterable master-detail shape plan 09.5-01 proved for SSH, now honestly scoped to git's open-ended key space (D-01/D-02)"
affects: [09.5-03, 09.5-04, 09.5-05]

actuals:
  tokens: 26400
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Shared chrome primitives live in frame.go (subTabStripRows/renderSubTabStrip joins frameBodyRows/masterListWidth/joinMasterDetail/fitPane/hitNeedle/spliceTitleIntoTopBorder) — a screen NEVER owns its own copy of cross-screen UI plumbing"
    - "A screen's row-budget consumers (gitTopLines here) are enumerated and updated together whenever the budget's cost changes — partial updates desync the click hit-test from the render"
    - "'Set keys' vs 'All keys': a git-config-style browser that can only prove what is SET must never claim exhaustiveness its own probe can't back — the SSH side's 'All directives' label does not transfer"

key-files:
  created:
    - internal/globalgit/properties.go
    - internal/globalgit/properties_test.go
  modified:
    - internal/tuikit/frame.go
    - internal/tuikit/globalgit.go
    - internal/tuikit/globalssh.go
    - internal/tuikit/views.go
    - internal/tuikit/backend.go
    - internal/tuikit/design.go
    - internal/tuikit/app.go
    - internal/tuikit/globalgit_test.go
    - internal/tuikit/frame_test.go
    - internal/tuikit/backend_stub_test.go
    - internal/tuikit/properties_copy_test.go
    - internal/dummytui/fixturebackend.go
    - cmd/gitid/wiring.go
    - cmd/gitid/wiring_test.go
    - e2e/global_git_pty_e2e_test.go
    - Makefile

key-decisions:
  - "D-D held: the sub-tab strip is ONE shared implementation in frame.go, not a second copy in globalgit.go — grep -rc 'strings.Repeat(\"╌\"' internal/tuikit/ returns exactly 1 (frame.go)."
  - "D-E held: AllSetKeys reuses effectiveProbe verbatim (sed extraction shows exactly 1 call), no --global narrowing, system scope stays visible."
  - "D-05 held: renderSetKeys stays flat, no section grouping despite git keys carrying a native section prefix."
  - "A genuinely empty git configuration is not achievable in this sandbox — the system scope (/usr/local/etc/gitconfig, credential.helper=osxkeychain) leaks into every seeded HOME. The zero-keys-set empty state is proven in-package only (TestSetKeysTwoDistinctEmptyStates); the probe-failure state IS e2e-proven."

patterns-established:
  - "Coordinated-reword gate-fail-path proof: rewording a frozen constant in design.go ALONE never trips gate-copy-freeze, because the copy test's own 'want' literal (same grep roots) masks it — the fail path must reword BOTH occurrences together to simulate a genuine forgotten-Makefile-update rename."

requirements-completed: [PROP-02]

coverage:
  - id: D1
    description: "Global Git gains its first sub-tab strip, shared with Global SSH via one renderer (D-D)"
    requirement: "PROP-02"
    verification:
      - kind: unit
        ref: "internal/tuikit/frame_test.go#TestSharedSubTabStripRendersBorderedBox"
        status: pass
      - kind: unit
        ref: "internal/tuikit/frame_test.go#TestGlobalSSHStripUnchangedAfterExtraction"
        status: pass
      - kind: e2e
        ref: "e2e/global_git_pty_e2e_test.go#TestGlobalGit_RealPTYSubTabStripClick"
        status: pass
    human_judgment: false
  - id: D2
    description: "Global Git's existing Options content is re-homed byte-identically below the new strip, for the populated, optionsErr, and zero-options states"
    requirement: "PROP-02"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalgit_test.go#TestGlobalGitOptionsSubTabContentUnchanged"
        status: pass
    human_judgment: false
  - id: D3
    description: "Global Git's ←/→ now switches sub-tabs and no longer reaches the app's main-tab switcher; the top-level arrow hint is suppressed on this screen"
    requirement: "PROP-02"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalgit_test.go#TestGlobalGitSubTabSwitching"
        status: pass
      - kind: unit
        ref: "internal/tuikit/globalgit_test.go#TestTopLevelArrowHintSuppressedOnGlobalGit"
        status: pass
    human_judgment: false
  - id: D4
    description: "globalgit.AllSetKeys returns every SET git config key with its scope and origin, delegating to the existing effectiveProbe, sorted, NUL-safe, error-propagating"
    requirement: "PROP-02"
    verification:
      - kind: unit
        ref: "internal/globalgit/properties_test.go#TestAllSetKeysReturnsEverySetKeyWithProvenance"
        status: pass
      - kind: unit
        ref: "internal/globalgit/properties_test.go#TestAllSetKeysIsSortedByKey"
        status: pass
      - kind: unit
        ref: "internal/globalgit/properties_test.go#TestAllSetKeysUsesTheListForm"
        status: pass
      - kind: unit
        ref: "internal/globalgit/properties_test.go#TestAllSetKeysPreservesValuesContainingSeparators"
        status: pass
      - kind: unit
        ref: "internal/globalgit/properties_test.go#TestAllSetKeysPropagatesProbeError"
        status: pass
    human_judgment: false
  - id: D5
    description: "The 'Set keys' sub-tab renders the flat filterable master-detail list (origin + scope in the detail pane), honestly scoped, never 'All keys'"
    requirement: "PROP-02"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalgit_test.go#TestSetKeysListRendersOriginAndScope"
        status: pass
      - kind: unit
        ref: "internal/tuikit/properties_copy_test.go#TestFrozenPropertiesCopy"
        status: pass
      - kind: e2e
        ref: "e2e/global_git_pty_e2e_test.go#TestGlobalGit_RealPTYSetKeysBrowse"
        status: pass
    human_judgment: false
  - id: D6
    description: "The filter's D-B keyboard-capture contract holds on the Git side: a digit typed while focused lands in the field, esc blurs without clearing, the same digit then switches tabs"
    requirement: "PROP-02"
    verification:
      - kind: e2e
        ref: "e2e/global_git_pty_e2e_test.go#TestGlobalGit_RealPTYSetKeysFilter"
        status: pass
    human_judgment: false
  - id: D7
    description: "Two distinct Set-keys empty/error states never interchangeable: probe failure (warning glyph/style) vs. zero-keys-set (faint, no glyph)"
    requirement: "PROP-02"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalgit_test.go#TestSetKeysTwoDistinctEmptyStates"
        status: pass
      - kind: e2e
        ref: "e2e/global_git_pty_e2e_test.go#TestGlobalGit_RealPTYSetKeysProbeFailure"
        status: pass
    human_judgment: true
    rationale: "The zero-keys-set state's e2e leg is documented as not achievable on this machine's sandbox (system-scope credential.helper always leaks in) — a human should confirm the in-package coverage plus the documented evidence is an acceptable substitute for a live e2e assertion of that specific branch."
  - id: D8
    description: "Every visible Git string is frozen twice (byte-exact test + grep-presence gate), with the gate's fail path hand-verified"
    requirement: "PROP-02"
    verification:
      - kind: unit
        ref: "internal/tuikit/properties_copy_test.go#TestFrozenPropertiesCopy"
        status: pass
      - kind: other
        ref: "make gate-copy-freeze"
        status: pass
    human_judgment: false

duration: ~52min across commits (Task 1 commit 22:38 to Task 3 commit 23:30 local), plus earlier Task 2 TUI-wiring work before a context compaction
completed: 2026-09-03
status: complete
---

# Phase 09.5 Plan 02: Full Git Properties Browser (Set Keys Sub-Tab) Summary

**Global Git gains its first sub-tab strip — shared with Global SSH via one renderer — and a new "Set keys" sub-tab listing every git config key actually set on the machine (via `git config --show-origin --show-scope --list -z`), with scope, origin, and a type-to-filter master-detail pane, wired end-to-end from the real `git` binary through the compiled TUI and proven in a real terminal.**

## Performance

- **Duration:** ~52min across this session's commits (Task 1 commit 22:38:40 to Task 3 commit 23:30:38 local), plus earlier Task 2 TUI-wiring work (view()/handleClick completion, stub backend, wiring tests) carried over from before a context compaction
- **Tasks:** 3
- **Files modified:** 16 (2 new, 14 modified)
- **Commits:** 3 task commits + this docs commit

## Accomplishments

- Extracted Global SSH's sub-tab strip into `internal/tuikit/frame.go`'s `subTabStripRows()`/`renderSubTabStrip(labels, active)` — the ONE shared bordered-strip renderer both Global screens now use (D-D); Global SSH's own rendered bytes proven unchanged.
- Gave Global Git its first sub-tab strip: `Options` (existing content, re-homed byte-identically) and the new `Set keys` sub-tab, with `←`/`→` cycling between them and the top-level arrow-hint exclusion extended to this screen.
- `internal/globalgit.AllSetKeys(deps)` — every SET git config key with its scope and origin, delegating to the existing `effectiveProbe` unchanged (D-E: no `--global` narrowing, system scope stays visible).
- The `GitPropertiesBrowser` seam (`AllGitSetKeys`), wired for both `*realBackend` (via `globalgit.AllSetKeys(BuildProbeDeps(b.fragmentDir))`, the SAME non-repository probe directory the Options sub-tab already uses) and `FixtureBackend`.
- The `Set keys` sub-tab: the SAME flat filterable master-detail shape plan 09.5-01 proved for SSH — filter row with live match count, `truncLine`d master list through the existing `gitScrollWindow` machinery, a detail pane with the full value, origin, scope, and the `PolicyBacked` cross-reference note (reused, not re-declared).
- Every new visible string frozen twice (`TestFrozenPropertiesCopy` + `gate-copy-freeze`), with the gate's fail path hand-verified via a coordinated reword.
- Discovered and fixed a scroll-budget regression in three PRE-EXISTING Global Git PTY tests, surfaced only when the FULL `make test-e2e` suite ran (Tasks 1/2's own verify commands never exercised e2e).

## Task Commits

1. **Task 1: Extract the shared sub-tab strip, give Global Git its first one** — `b129fb0` (feat, tdd)
2. **Task 2: `globalgit.AllSetKeys` through the seam into the Set keys flat filterable list** — `7c57922` (feat, tdd)
3. **Task 3: Register the Git copy in both gates, prove the strip and the list through the compiled binary** — `98e97dd` (feat)

**Plan metadata:** this commit (docs: complete plan)

## Files Created/Modified

- `internal/tuikit/frame.go` — `subTabStripRows()` + `renderSubTabStrip(labels, active)`, extracted verbatim from Global SSH's original.
- `internal/tuikit/globalssh.go` — `subTabStrip()`/`gssSubTabStripRows()` reduced to one-line delegations to the shared helper.
- `internal/tuikit/globalgit.go` — `ggitSubTab` enum, the strip in every non-ceremony body, `←`/`→` cycling, strip click routing, `gitTopLines` accounting for the strip's rows, `renderSetKeys`/`ggitFilteredKeys`/`ggitSetKeysTopLines`/`handleSetKeysClick` and the rest of the Set-keys stack.
- `internal/tuikit/app.go` — Global Git joins the top-level `←→ switch view` footer-hint exclusion.
- `internal/globalgit/properties.go` (new) — `SetKey{Key,Value,Scope,Origin}` + `AllSetKeys(deps) ([]SetKey, error)`.
- `internal/globalgit/properties_test.go` (new) — 5 behavior tests.
- `internal/tuikit/views.go` — `GitSetKeyView` DTO.
- `internal/tuikit/backend.go` — `GitPropertiesBrowser`, `NoopGitPropertiesBrowser`, `ErrGitPropertiesBrowserNotImplemented`, embedded in `Backend`.
- `internal/tuikit/backend_stub_test.go` — `stubBackend` gets the noop plus `gitSetKeys`/`gitSetKeysErr` override fields (empty-success default).
- `internal/tuikit/design.go` — 5 frozen Git copy constants.
- `internal/dummytui/fixturebackend.go` — `AllGitSetKeys()` returning a frozen 20-row slice.
- `cmd/gitid/wiring.go` — `(*realBackend).AllGitSetKeys()`.
- `cmd/gitid/wiring_test.go` — reflection nil-guard (`TestRealBackendDoesNotEmbedNoopGitPropertiesBrowser`) + wiring test (`TestBuildTUIDepsWiresGitPropertiesBrowser`).
- `internal/tuikit/globalgit_test.go` — 6 new Task 1/2 behavior tests plus fixes to 2 pre-existing scroll-sensitive tests.
- `internal/tuikit/frame_test.go` — `TestSharedSubTabStripRendersBorderedBox`, `TestGlobalSSHStripUnchangedAfterExtraction`.
- `internal/tuikit/properties_copy_test.go` — 5 new rows, `wantCount` 8 → 13.
- `Makefile` — 5 new frozen literals in `gate-copy-freeze`.
- `e2e/global_git_pty_e2e_test.go` — 4 new PTY cases plus fixes to 3 pre-existing scroll-sensitive cases.

## Decisions Made

- **D-D held.** `grep -rc 'strings.Repeat("╌"' internal/tuikit/` → `frame.go:1` only; no second strip implementation.
- **D-E held.** `sed -n '/func AllSetKeys/,/^}/p' internal/globalgit/properties.go | grep -c 'effectiveProbe(deps)'` → `1`. `git diff internal/globalgit/probe.go internal/globalgit/authorresolve.go` → empty (no second origin parser or probe call added).
- **D-05 held.** `sed -n '/renderSetKeys/,/^}/p' internal/tuikit/globalgit.go | grep -iE 'section|group'` → empty; the list stays flat.
- **The row-budget fallback was not needed** for Task 1's Options re-homing (measured `available=25 used=25 diff=0` — fits using the existing scroll mechanism when needed), but Task 3's full e2e run showed that mechanism genuinely engages now for the real 12-row table, which three pre-existing PTY tests had assumed would never happen. Documented as a deviation below.
- **A genuinely empty git configuration is not achievable in this sandbox.** `git config --system --list --show-origin` on the dev/CI machine used for this session returns `file:/usr/local/etc/gitconfig credential.helper=osxkeychain` — confirmed leaking into every seeded e2e HOME (visible in the captured `global-git-set-keys-browse.txt` frame). The zero-keys-set empty state is proven in-package only (`TestSetKeysTwoDistinctEmptyStates`); the probe-failure state is proven both in-package and e2e.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Three pre-existing Global Git PTY tests broken by Task 1's strip row-budget cost**

- **Found during:** Task 3, running `make test-e2e` (the FULL suite — Tasks 1 and 2's own `<verify>` commands scoped to `./internal/tuikit/...` / `./cmd/gitid/...` and never exercised the `e2e` build tag, so this regression was invisible until Task 3's mandatory full-suite run).
- **Issue:** Task 1's sub-tab strip costs `gitTopLines(s)` an extra 3 rows (4 with the findings banner) versus its pre-plan value. The real 12-row D9 policy table (24 lines) fit the OLD ~24-25-row list budget without scrolling; against the NEW ~21-row budget it does not. Three pre-existing tests assumed all 12 rows were simultaneously visible: `TestGlobalGit_RealPTYBrowse` (checked `merge.conflictstyle` in the initial frame), `TestGlobalGit_RealPTYOptionFocusPlaceholderAndColumns` (checked column alignment across all 12 labels in one frame), and `TestGlobalGit_RealPTYScrollBothDirections` (asserted NO scroll cue ever appears against the 12-row fixture).
- **Fix:** Updated all three to account for the now-genuine need to scroll: `TestGlobalGit_RealPTYBrowse` and the column-alignment test now scroll to the last row (`moveGlobalGitRow(t, s, 11)`) to check the two now-hidden rows separately; `TestGlobalGit_RealPTYScrollBothDirections` was rewritten (with its doc comment) to EXPECT the up-cue at the bottom boundary and the down-cue at the top boundary, proving the boundary is stable UNDER scrolling rather than asserting scrolling never happens. This is content re-homed behind the screen's existing, already-proven scroll mechanism — not shrunk.
- **Files modified:** `e2e/global_git_pty_e2e_test.go`.
- **Verification:** `go test -tags e2e -count=1 ./e2e/... -run 'TestGlobalGit_RealPTY' -v` → all 17 Global Git PTY cases pass; full `make test-e2e` (1044.478s) exit 0.
- **Committed in:** `98e97dd` (Task 3 commit).

---

**Total deviations:** 1 auto-fixed (1 bug, Rule 1)
**Impact on plan:** In-scope fix for Task 1's own row-budget change, surfaced only by Task 3's mandatory full-suite verification. No scope creep — no new behavior was added, only test expectations corrected to match the measured, approved consequence of the strip.

## Issues Encountered

- **Genuinely empty git config not achievable in this sandbox.** `git config --system --list --show-origin` reports `file:/usr/local/etc/gitconfig credential.helper=osxkeychain` on this machine — a system-scope entry that survives every seeded e2e `HOME`. Per the plan's own contingency ("If a genuinely empty git configuration is not achievable in the sandbox... say so explicitly... cover that state in-package only"), the zero-keys-set empty state is proven only via `TestSetKeysTwoDistinctEmptyStates` (in-package, `stubBackend`); the probe-failure empty state IS proven live via `TestGlobalGit_RealPTYSetKeysProbeFailure`.
- **The gate-copy-freeze fail-path proof required a coordinated two-file reword**, exactly mirroring plan 09.5-01's own finding: rewording `PropsGitNoKeysSet` alone in `design.go` does not trip the grep gate, because `properties_copy_test.go`'s own `want` literal (inside the same grep roots) masks the change. The genuine RED required rewording both `design.go`'s constant value AND the test's `want` field together (simulating a coordinated rename that forgot the Makefile), which produced the expected `MISSING`/exit 2, then both were reverted with zero residual diff.

## Verification Evidence

**Task 1 RED note:** the compile-based RED technique (temporarily moving/renaming a not-yet-written file to observe the resulting undefined-symbol errors) was used for `internal/globalgit/properties.go` in Task 2 (see below, fully reproduced this session). Task 1's own strip-extraction and sub-tab-plumbing RED transcripts were produced in an earlier portion of this session that predates a context compaction; the exact terminal output was not preserved verbatim into this continuation. Task 1's commit (`b129fb0`) documents the same methodology (byte-identical literal comparisons captured from `git show f9a04ce:...`, not from a re-render of the new code path) and all listed `<behavior>` tests are present and passing at HEAD.

**Task 2 RED (reproduced this session, verbatim):** the 6 source files Task 2 touches (`cmd/gitid/wiring.go`, `internal/dummytui/fixturebackend.go`, `internal/tuikit/backend.go`, `internal/tuikit/design.go`, `internal/tuikit/globalgit.go`, `internal/tuikit/views.go`) were reverted to their pre-Task-2 commit (`b129fb0`) while every Task 2 TEST file (`backend_stub_test.go`, `globalgit_test.go`, `wiring_test.go`) was left at its CURRENT (finished) content, then:

```
$ go vet ./cmd/gitid/... ./internal/tuikit/... ./internal/dummytui/...
# github.com/castocolina/gitid/internal/tuikit
vet: internal/tuikit/backend_stub_test.go:173:2: undefined: NoopGitPropertiesBrowser
# github.com/castocolina/gitid/cmd/gitid
vet: cmd/gitid/wiring_test.go:122:15: undefined: tuikit.GitPropertiesBrowser
```

The 6 files were then restored to their GREEN content (byte-for-byte, verified via `git status --short` matching pre-revert exactly) and the full Task 2 verify command re-run — all 107 tests pass.

**`TestAllSetKeysUsesTheListForm` argv (D-E):**
```go
want := []string{"config", "--show-origin", "--show-scope", "--list", "-z"}
```
Recorded and asserted via `reflect.DeepEqual` against the fake `RunGitConfig`'s captured `args`.

**Set keys label (byte-exact, `PropsGitSubTabLabel`):** `"Set keys"`.

**Two Git empty states, both captured (real PTY frames):**

Probe-failure (`TestGlobalGit_RealPTYSetKeysProbeFailure`, `tmp/ui-frames/global-git-set-keys-probe-failure.txt`):
```
 Global Git › Set keys
╭╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╮
┊   Options   Set keys  ┊
╰╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╯
 ! Git config could not be read.

 git config --list --show-origin failed — re-enter the screen to retry.
```

Zero-keys-set (in-package only, `TestSetKeysTwoDistinctEmptyStates/zero_keys_set,_no_error`): asserts `PropsGitNoKeysSet` ("No git config keys are set yet.") renders with NO warning glyph and NO `PropsGitProbeFailedHeading` present — the faint, non-alarming sentence, structurally distinct from the probe-failure body above.

**Row budget (Task 1, `TestGlobalGitFitsFixedGeometryWithStrip`):**
```
globalgit_test.go:2409: Options sub-tab, tallest variant: available=25 used=25 difference=0
```
The three-row bordered box ships; the named one-row fallback (`spliceTitleIntoTopBorder`) was not needed.

**`gitTopLines` consumers (all four updated together):** `func gitTopLines(s DemoState) int` at line 1020; `gitVisibleRowCount` at line 1063; `handleClick`'s entry guard at line 1516 and its row-math offset at line 1528; `view`'s `bodyRows` computation at line 1661. Enumerated via `grep -n "gitTopLines(" internal/tuikit/globalgit.go`.

**`view()` strip placement (all three non-ceremony early returns):** the hoisted `strip := renderSubTabStrip(...)` at line 1567 is reused at line 1614 (`optionsErr`), line 1645 (zero-options), and line 1805 (populated).

**gate-copy-freeze — 5 new Git lines, all `ok`:**
```
    ok   Set keys
    ok   ! Git config could not be read.
    ok   git config --list --show-origin failed — re-enter the screen to retry.
    ok   No git config keys are set yet.
    ok   No keys match \"%s\".
```

**gate-copy-freeze fail path (hand-verified on `PropsGitNoKeysSet`, coordinated reword):**
```
    MISSING  No git config keys are set yet.
gate-copy-freeze: FROZEN COPY MISSING (02-STYLE-SPEC.md §6)
```
exit code 2. Both `design.go` and `properties_copy_test.go` reverted; `git diff` showed zero residual diff; `make gate-copy-freeze` re-run GREEN.

**`t.Skip` count:** `grep -c 't\.Skip' e2e/global_git_pty_e2e_test.go` → `0`.

**No-backend import gate:** `go list -deps ./internal/tuikit | grep -E "castocolina/gitid/internal/(globalssh|globalgit|identity|sshconfig|gitconfig|keygen|doctor|adopter|uploader|clipboard|deps|platform|filewriter)"` → no matches (exit 1). `go test ./internal/dummytui/ -run TestNoBackendAllowlist` → PASS.

**Full verification (all green):**
```
$ TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...
2630 tests passed in 22 packages

$ make lint
0 issues.

$ make test  (includes gate-copy-freeze)
exit 0

$ make test-e2e
go test -tags e2e -race -timeout 2400s ./e2e/...
ok  	github.com/castocolina/gitid/e2e	1044.478s
```
Includes all 17 Global Git PTY cases (13 pre-existing + 4 new) and all 15 Global SSH PTY cases (plan 09.5-01's own, unaffected by the strip extraction).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- PROP-02's "Set keys" browser is real, wired, and proven end-to-end. Plan 09.5-03 can now add the custom-key WRITE methods (explicitly deferred here per the plan's own scope note) on top of this read path.
- The shared `renderSubTabStrip`/`subTabStripRows` home in `frame.go` is available for any future third sub-tab screen without a second implementation.
- No blockers. The sandbox's system-scope git config leak (documented above) is worth carrying forward as a known constraint for any future e2e test that assumes a truly empty git configuration is reachable.

---
*Phase: 09.5-full-ssh-git-properties-browser*
*Completed: 2026-09-03*
