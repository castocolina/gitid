---
phase: 09.5-full-ssh-git-properties-browser
plan: 01
subsystem: ui
tags: [bubbletea, lipgloss, tuikit, ssh-config, textinput, master-detail, golangci-lint]

# Dependency graph
requires:
  - phase: 06-global-ssh-options
    provides: "globalssh's effective(deps)/parseResolvedOptions/ProbeHost wildcard-only probe, the gssSubTab scaffold, the Backend seam pattern, the scroll-window machinery (gitScrollWindow/scrollWindowFor/gitCueLine, screen-agnostic despite git* naming)"
provides:
  - "globalssh.AllDirectives(deps) — the full, sorted ssh -G resolved directive set for Host * (PROP-01)"
  - "tuikit.SSHDirectiveView DTO + SSHPropertiesBrowser seam (Noop + realBackend + FixtureBackend)"
  - "Global SSH's third gssProperties sub-tab: flat, filterable, master-detail, with a frozen empty/error-state contract"
  - "e2e/harness_test.go's globalssh-probe-unresolvable fixture mode (a genuinely new capability: fails the wildcard-only ssh -G probe unconditionally, distinct from the pre-existing globalssh-inconclusive mode which only fails the isolated shadow-check's real -F <config> call)"
affects: ["09.5-02", "09.5-03", "09.5-04", "09.5-05"]

# Actuals (#2632)
actuals:
  tokens: 19841
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Vertical-slice tracer: engine -> DTO -> seam (Noop + both real implementations) -> sub-tab -> real-PTY proof, committed as ONE buildable-boundary commit"
    - "gssNextSubTab/gssPrevSubTab opposite-direction 3-way cycle helper, replacing a hardcoded two-way toggle at all four call sites"
    - "Reuse gitScrollWindow/scrollWindowFor/gitCueLine verbatim for a NEW list rather than reimplementing pagination — the machinery is screen-agnostic despite its git* naming"
    - "Frozen copy pinned twice: TestFrozenXxxCopy (byte-exact, PRIMARY) + gate-copy-freeze grep (source-presence, SECONDARY) — and when the copy constant is ALSO restated as a test's `want` literal, the fail-path proof must reword BOTH occurrences together, since the test file itself lives inside the grep roots"
    - "Fixture-mode fake-shell extension for a genuinely new failure class (globalssh-probe-unresolvable) rather than repurposing an existing mode whose failure condition does not fire for the code path under test"

key-files:
  created:
    - internal/globalssh/properties.go
    - internal/globalssh/properties_test.go
    - internal/tuikit/properties_copy_test.go
  modified:
    - internal/tuikit/views.go
    - internal/tuikit/backend.go
    - internal/tuikit/backend_stub_test.go
    - internal/tuikit/globalssh.go
    - internal/tuikit/globalssh_test.go
    - internal/tuikit/design.go
    - internal/dummytui/fixturebackend.go
    - cmd/gitid/wiring.go
    - cmd/gitid/wiring_test.go
    - e2e/harness_test.go
    - e2e/global_ssh_pty_e2e_test.go
    - Makefile

key-decisions:
  - "D-A (plan-authored): AllDirectives is a thin sorted-map wrapper over the EXISTING unexported effective(deps) — no second ssh -G parser. Reused, not touched: internal/globalssh/probe.go (git diff empty)."
  - "D-B (plan-authored): the filter textinput owns the keyboard while focused — every key but esc routes into it; esc blurs without clearing. Proven in a real PTY by typing a digit into the focused filter and observing it land in the field text, not switch main tabs."
  - "D-C (plan-authored): filter-text change resets selection to the first row of the newly filtered set."
  - "Deviation (Rule 2, Task 1): stubBackend needed sshDirectives/sshDirectivesErr override fields with an empty-success DEFAULT (mirroring the existing sshOptions/sshOptionsErr pattern), not the literal plan wording of 'add the noop to the embed list' alone — a bare embed left every plain stubBackend{} erroring on the new sub-tab, breaking unrelated tests."
  - "Deviation (Rule 3, Task 3): added a new e2e/harness_test.go fixture mode, globalssh-probe-unresolvable. The plan's read_first pointed at the existing globalssh-inconclusive mode as 'the existing way a probe failure is forced through the compiled binary', but that mode only fails the ISOLATED shadow-check call (shadow.go's real -F <config> call, apply-preview only) — it never fails the wildcard-only effective(deps) call AllDirectives (and GlobalSSHOptionStates) both run on activate(), so it could not exercise the properties sub-tab's top-level probe-failed state at all. Verified empirically before writing the real test."
  - "Deviation (Rule 2, Task 3): added an in-package unit click assertion for the third sub-tab label to internal/tuikit/globalssh_test.go's TestSubTabStripClickSwitchesSubTabs, complementing the real-PTY SGR click test — the plan's Task 3 read_first named globalssh_test.go's click-hit-test helpers explicitly for 'third-label click coverage' but the action text only specified the PTY-level case."

patterns-established:
  - "Master-detail sub-tab body: masterListWidth/masterDetailGutter/joinMasterDetail/fitPane, reused verbatim from renderOptions — no second implementation."
  - "Empty/error state trio for a probe-backed list: probe-failure (frozen heading+body, fail-open), filter-zero-match (frozen Fmt with the filter text), zero-rows-no-error (faint line, no index-into-empty-slice)."

requirements-completed: [PROP-01]

coverage:
  - id: D1
    description: "Global SSH gains a third sub-tab, 'All directives', reachable via ←/→ or a mouse click, showing every ssh -G directive resolved for Host * (not just the six curated policy rows)"
    requirement: "PROP-01"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalssh_test.go#TestGlobalSSHArrowsCycleThreeSubTabsInOppositeDirections"
        status: pass
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYAllDirectivesBrowse"
        status: pass
    human_judgment: false
  - id: D2
    description: "The list is flat, scrollable, and type-to-filter — case-insensitive match on key and value, match count right-aligned, selection resets to the first match on filter change"
    requirement: "PROP-01"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalssh_test.go#TestPropertiesFilterNarrowsTheList"
        status: pass
      - kind: unit
        ref: "internal/tuikit/globalssh_test.go#TestPropertiesFilterResetsSelectionToFirstMatch"
        status: pass
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYAllDirectivesFilter"
        status: pass
    human_judgment: false
  - id: D3
    description: "The filter owns the keyboard while focused (D-B): every key but esc routes into it, esc blurs without clearing, and app.go's 1-5 main-tab globals are genuinely bypassed while focused"
    requirement: "PROP-01"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalssh_test.go#TestPropertiesFilterCapturesKeys"
        status: pass
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYAllDirectivesFilter"
        status: pass
    human_judgment: false
  - id: D4
    description: "Detail pane shows the full unclipped value + provenance line + cross-reference note for PolicyBacked rows; master row truncates with a visible cue"
    requirement: "PROP-01"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalssh_test.go#TestPropertiesDetailPaneShowsFullValueAndSource"
        status: pass
      - kind: unit
        ref: "internal/tuikit/globalssh_test.go#TestPropertiesCrossReferenceNoteOnlyForPolicyBackedRows"
        status: pass
    human_judgment: false
  - id: D5
    description: "Probe-failure fail-open state: frozen warning renders, navigation keys still reach app globals"
    requirement: "PROP-01"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalssh_test.go#TestPropertiesEmptyStates"
        status: pass
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYAllDirectivesProbeFailure"
        status: pass
    human_judgment: false
  - id: D6
    description: "Every visible string is frozen twice: byte-exactly by a dedicated test, and by presence in the gate-copy-freeze grep gate; the gate's fail path was hand-verified"
    requirement: "PROP-01"
    verification:
      - kind: unit
        ref: "internal/tuikit/properties_copy_test.go#TestFrozenPropertiesCopy"
        status: pass
      - kind: other
        ref: "make gate-copy-freeze"
        status: pass
    human_judgment: false
  - id: D7
    description: "A raw SGR mouse click on the third sub-tab label switches to it, with coordinates derived from the decoded frame"
    requirement: "PROP-01"
    verification:
      - kind: unit
        ref: "internal/tuikit/globalssh_test.go#TestSubTabStripClickSwitchesSubTabs"
        status: pass
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYAllDirectivesLabelMouseClick"
        status: pass
    human_judgment: false

duration: ~1h across sessions (Task 1 commit 21:07 to Task 3 commit 22:03 local, plus earlier tracer/planning work before a context compaction)
completed: 2026-09-03
status: complete
---

# Phase 09.5 Plan 01: Full SSH Properties Browser Tracer Summary

**Global SSH gains a third "All directives" sub-tab showing every `ssh -G`-resolved directive (27 in the PTY fixture, not just the six curated policy rows), with a case-insensitive type-to-filter, a master-detail pane, and a probe-failure fail-open state — wired end-to-end from the real `ssh` binary through the compiled TUI and proven in a real terminal.**

## Performance

- **Tasks:** 3/3 completed
- **Commits:** 3 (one per task, each a coherent buildable-boundary group per CLAUDE.md)
- **Files modified:** 15 (3 created, 12 modified)

## Accomplishments

- `globalssh.AllDirectives(deps)` exposes the FULL resolved directive set (not the six-row policy subset), delegating to the existing `effective(deps)`/`parseResolvedOptions`/`ProbeHost` machinery with zero changes to `probe.go`.
- Global SSH's `gssProperties` sub-tab: a flat, filterable, master-detail list reusing the screen's existing scroll-window machinery, with match-count, detail pane (full value + provenance line + cross-reference note), and three distinct empty/error states.
- All four "two-sub-tab assumption" sites in `globalssh.go` were found and fixed: `subTabStrip()`, `handleKey`'s `←`/`→` cycle, the zero-options fail-open branch, and `handleClick`'s strip hit-test — now a proper 3-way opposite-direction cycle (`gssNextSubTab`/`gssPrevSubTab`).
- Every visible string is frozen twice — `TestFrozenPropertiesCopy` (byte-exact) and `gate-copy-freeze` (grep presence) — with the gate's fail path hand-verified.
- The whole stack is proven through the COMPILED binary in a real PTY: browse, filter + D-B keyboard-capture contract, probe-failure fail-open, and a raw SGR mouse click on the new label — closing this project's documented injected-seam wiring blindspot for the new seam.

## Task Commits

1. **Task 1: End-to-end "read every resolved SSH directive"** — `77bcfab` (feat, tracer)
2. **Task 2: The flat filterable master-detail body** — `36351c5` (feat)
3. **Task 3: Register the frozen copy in both gates + real-terminal proof** — `d1ba222` (feat)

**Plan metadata:** this commit (docs: complete plan)

## Files Created/Modified

- `internal/globalssh/properties.go` — `Directive{Key,Value}` + `AllDirectives(deps) ([]Directive, error)`, sorted, delegating to `effective(deps)`.
- `internal/globalssh/properties_test.go` — 5 behavior tests (sorted, camelCase-skip, wildcard-sentinel argv, error propagation).
- `internal/tuikit/views.go` — `SSHDirectiveView{Key,Value,PolicyBacked}` DTO.
- `internal/tuikit/backend.go` — `SSHPropertiesBrowser` (one method), `NoopSSHPropertiesBrowser`, embedded in `Backend`.
- `internal/tuikit/backend_stub_test.go` — `stubBackend` gets the noop plus `sshDirectives`/`sshDirectivesErr` override fields (empty-success default).
- `internal/tuikit/globalssh.go` — the `gssProperties` sub-tab: fields, `activate()` fetch, the 3-way cycle helpers, filter textinput + D-B/D-C, master-detail render, empty/error states, click routing, footer/crumb updates.
- `internal/tuikit/globalssh_test.go` — 8 new behavior tests plus the 3-way-cycle regression and the third-label click assertion.
- `internal/tuikit/design.go` — 8 frozen copy constants (`PropsSSH*`, `PropsFilter*`, `PropsMatchCountFmt`, `PropsCrossReferenceNote`).
- `internal/tuikit/properties_copy_test.go` (new) — `TestFrozenPropertiesCopy`, byte-exact, `wantCount = 8`.
- `internal/dummytui/fixturebackend.go` — `AllSSHDirectives()` returning a frozen 22-row slice.
- `cmd/gitid/wiring.go` — `(*realBackend).AllSSHDirectives()`, calling `globalssh.AllDirectives` + `globalssh.PolicyFor`.
- `cmd/gitid/wiring_test.go` — reflection nil-guard + wiring test for the new seam.
- `e2e/harness_test.go` — extends the `globalssh`/`globalssh-inconclusive` fixture with 21 additional directive lines (Task 1); adds the new `globalssh-probe-unresolvable` mode (Task 3).
- `e2e/global_ssh_pty_e2e_test.go` — `TestGlobalSSH_RealPTYAllDirectivesBrowse` (Task 1); `TestGlobalSSH_RealPTYAllDirectivesFilter`, `TestGlobalSSH_RealPTYAllDirectivesProbeFailure`, `TestGlobalSSH_RealPTYAllDirectivesLabelMouseClick` (Task 3).
- `Makefile` — 8 new frozen literals registered in `gate-copy-freeze`.

## Decisions Made

See `key-decisions` in frontmatter — D-A/D-B/D-C were plan-authored (CONTEXT.md left them to discretion, resolved in the plan itself); the stubBackend override, the new fixture mode, and the unit click assertion were executor-time Rule 2/3 deviations (documented below with evidence).

## Task 1 Evidence — Tracer

**Acceptance criteria, re-verified against the current commit:**

```
$ grep -c 'func AllDirectives' internal/globalssh/properties.go
1
$ sed -n '/func AllDirectives/,/^}/p' internal/globalssh/properties.go | grep -c 'effective(deps)'
1
$ git log --oneline -1 -- internal/globalssh/probe.go
4bec436 fix(06): WR-15 read the per-user config once in Statuses instead of two racing reads
```
(`probe.go`'s last-touching commit predates this phase entirely — confirmed untouched by Task 1, D-A.)

```
$ grep -c 'SSHPropertiesBrowser' internal/tuikit/backend.go
15
$ grep -v '^\s*//' cmd/gitid/wiring.go | grep -c 'NoopSSHPropertiesBrowser'
0
$ sed -n '/func gssSubTabStripRows/,/^}/p' internal/tuikit/globalssh.go | grep -c 'return 3'
1
```

**The four two-sub-tab assumption sites, re-derived directly from the current file (not copied from the plan's own list):**

| # | Site | Location (globalssh.go) | Fix |
|---|------|--------------------------|-----|
| 1 | `subTabStrip()` label composition | line 951 | composes three labels via `switch m.subTab`, reversing only the active one |
| 2 | `handleKey`'s `left`/`right` case | line 851 | `gssNextSubTab`/`gssPrevSubTab` (defined lines 702/718), opposite directions, wraps at both ends |
| 3 | zero-options fail-open branch | line 836 | routes through the SAME `gssNextSubTab`/`gssPrevSubTab` helper (was a hardcoded two-way toggle) |
| 4 | `handleClick`'s strip hit-test | line 1089 | third `hitNeedle(body, x, y, gssTabPropertiesLabel)` case added alongside the two existing ones (1081, 1084) |

The `crumb` switch (line 1318) and the `←→` footer label (`gssFooterCycleLabel`) also name all three sub-tabs identically across every branch.

**`left`/`right` opposite-direction cycle, proven directly:** `internal/tuikit/globalssh_test.go#TestGlobalSSHArrowsCycleThreeSubTabsInOppositeDirections` starts on Options, presses `left`, and asserts the frame lands on the THIRD sub-tab ("Global SSH › All directives") — not Storage. Currently PASSING.

**Extended `FakeSSHDir` fixture (harness_test.go, `globalssh`/`globalssh-inconclusive` mode) — the new fixed block emitted immediately after the six existing policy lines:**

```
addressfamily any
batchmode no
canonicalizehostname false
checkhostip no
ciphers chacha20-poly1305@openssh.com,aes128-ctr
clearallforwardings no
compression no
connectionattempts 1
connecttimeout none
controlmaster false
dynamicforward none
escapechar ~
exitonforwardfailure no
gatewayports no
hostbasedauthentication no
loglevel INFO
pubkeyauthentication yes
serveraliveinterval 0
streamlocalbindmask 0177
userknownhostsfile ~/.ssh/known_hosts ~/.ssh/known_hosts2
visualhostkey no
```

(21 lines; combined with the 6 existing policy lines, the fixture answers 27 total directives — genuinely more than the six-row policy set. `TestFakeSSHDirGlobalSSHProbesReadTheirConfig` still passes byte-identically, confirmed in this session's re-run of the full `e2e` suite.)

**`TestGlobalSSH_RealPTYAllDirectivesBrowse` — captured frame** (`tmp/ui-frames/global-ssh-all-directives-browse.txt`, re-captured this session):

```
 gitid   [1] Identities · [2] SSH · [3] Git · [4] Doctor · [5] Ignore               0 ids · ! 3 ✗ 2
 Global SSH › All directives
╭╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╮
┊   Options   Storage & preview   All directives  ┊
╰╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╯
 addkeystoagent  no
 addressfamily  any
 batchmode  no
 canonicalizehostname  false
 checkhostip  no
 ciphers  chacha20-poly1305@openssh.com,aes128-ctr
 clearallforwardings  no
 compression  no
 connectionattempts  1
 connecttimeout  none
 controlmaster  false
 dynamicforward  none
 escapechar  ~
 exitonforwardfailure  no
 forwardagent  no
 gatewayports  no
 hashknownhosts  no
 hostbasedauthentication  no
 identitiesonly  no
 loglevel  INFO
 pubkeyauthentication  yes
 ↓ (+6 more options)
 4 of 6 options need action — Recommended, not required -- you can leave any option unchanged. Thi…
 ←→ Options / Storage / Directives
 Enter activate · Esc back · ? help · Ctrl+P palette · q quit
```

The assertion names 8 candidate non-policy keys and requires ≥3 present; `addressfamily`, `batchmode`, `canonicalizehostname`, `checkhostip`, `ciphers`, `clearallforwardings`, `compression`, and `connectionattempts` are ALL visible in this frame.

**RED-before-GREEN note (Task 1):** the six `<behavior>` tests (`TestAllDirectivesReturnsEveryResolvedKey`, `TestAllDirectivesIsSortedByKey`, `TestAllDirectivesSkipsCamelCaseLines`, `TestAllDirectivesProbesTheWildcardSentinel`, `TestAllDirectivesPropagatesProbeError`, `TestBuildTUIDepsWiresSSHPropertiesBrowser`) were written before `properties.go`/the wiring method existed and observed failing to compile (`undefined: AllDirectives`) during the portion of this session that ran before a context compaction; the literal terminal transcript from that portion is not available to quote verbatim in this SUMMARY (it predates the compaction boundary), but the RED→GREEN discipline was followed and all six tests are currently GREEN, re-confirmed in this session's full-suite runs below.

## Task 2 Evidence — Filter, Match Count, Detail Pane, Empty/Error States

**All seven `<behavior>` tests, re-run this session with `-v`:**

```
$ TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run 'TestProperties|TestGlobalSSH|TestSubTabStrip' -v
...
--- PASS: TestPropertiesFilterNarrowsTheList (0.02s)
--- PASS: TestPropertiesFilterResetsSelectionToFirstMatch (0.00s)
--- PASS: TestPropertiesFilterCapturesKeys (0.02s)
--- PASS: TestPropertiesDetailPaneShowsFullValueAndSource (0.01s)
--- PASS: TestPropertiesCrossReferenceNoteOnlyForPolicyBackedRows (0.02s)
--- PASS: TestPropertiesEmptyStates (0.03s)
--- PASS: TestPropertiesFitsFixedGeometry (0.02s)
PASS
ok  	github.com/castocolina/gitid/internal/tuikit	1.594s
```

**`TestPropertiesFitsFixedGeometry` — full `-v` output, available/used/diff per case:**

```
=== RUN   TestPropertiesFitsFixedGeometry
    globalssh_test.go:2497: 91-row unfiltered list: available=25 used=25 diff=0
    globalssh_test.go:2501: filtered set (1 match): available=25 used=25 diff=0
    globalssh_test.go:2504: probe-failure empty state: available=25 used=6 diff=19
    globalssh_test.go:2508: filter-zero-match empty state: available=25 used=5 diff=20
    globalssh_test.go:2511: zero-rows-no-error empty state: available=25 used=5 diff=20
--- PASS: TestPropertiesFitsFixedGeometry (0.03s)
```

The 90-row (91 including the extreme-value row) case's headroom is **0** — the list exactly fills the available body rows at the fixed 100×30 geometry, the tightest case in the phase.

**Copy-freeze source count:**

```
$ grep -c 'PropsSSHSubTabLabel\|PropsFilterPlaceholder\|PropsMatchCountFmt\|PropsSSHProbeFailedHeading\|PropsSSHProbeFailedBody\|PropsSSHNoFilterMatchFmt\|PropsCrossReferenceNote\|PropsSSHSourceLine' internal/tuikit/design.go
16
```
(8 constant declarations + 8 doc-comment references — all 8 constants present.)

**Label derivation (not a second literal):**

```
$ sed -n '/gssTabPropertiesLabel/,+2p' internal/tuikit/globalssh.go
	// gssTabPropertiesLabel derives from design.go's frozen
	// PropsSSHSubTabLabel constant (padding added around it) — ONE source
	// for the label text, never restated.
	properties := gssTabPropertiesLabel
```

**Flat-list check (D-05 LOCKED — no grouping):**

```
$ git diff internal/tuikit/globalssh.go | grep -iE 'category|group'
NONE FOUND
```

**Scroll-machinery reuse (not reimplemented):**

```
$ sed -n '/^func (m globalSSHModel) renderProperties/,/^}/p' internal/tuikit/globalssh.go | grep -c 'gitScrollWindow\|scrollWindowFor\|gitCueLine\|gssPropertiesComputeScrollWindow'
3
```

**Master-row-truncated / detail-row-complete, captured for the SAME selected key** (`identityfile`, a synthetic 106-character value):

```
 / T                                                                                   2 of 2 shown
 ▸ identityfile              alpha bravo ch…│  identityfile
   stricthostkeychecking     ask            │
                                            │  alpha bravo charlie delta echo foxtrot golf hotel
                                            │ india juliet kilo lima mike november oscar papa quebec
                                            │
                                            │  Resolved via ssh -G — reflects Include/Match
                                            │ precedence already applied.
```

The master row truncates to `alpha bravo ch…` (visible `…` cue); the detail pane wraps and shows the FULL value across two lines. The value is never rendered twice side by side.

**Findings-banner decision (`gssPropertiesTopLines`):** the Options sub-tab's findings banner (`! The doctor found N SSH findings...`) is chrome ABOVE the sub-tab strip, shared by all three sub-tabs, not something `gssPropertiesTopLines` needs to account for separately — confirmed by the captured PTY frames above, where the banner (when present) occupies the SAME row above the strip on every sub-tab. `gssPropertiesTopLines(s)` derives only from `gssSubTabStripRows()` (3) plus the one filter row.

## Task 3 Evidence — Both Gates + Real-Terminal Proof

**`TestFrozenPropertiesCopy`:**

```
$ TERM=dumb SSH_AUTH_SOCK= go test -count=1 ./internal/tuikit/... -run 'TestFrozenPropertiesCopy' -v
=== RUN   TestFrozenPropertiesCopy
--- PASS: TestFrozenPropertiesCopy (0.00s)
PASS
```
Covers all 8 constants (`wantCount = 8`), the two format-string constants assert the FORMATTED result (`"23 of 91 shown"`, `No directives match "stricthost".`).

**`make gate-copy-freeze` — the 8 newly registered lines, all `ok`:**

```
    ok   All directives
    ok   Type to filter…
    ok   %d of %d shown
    ok   ! The SSH configuration could not be resolved.
    ok   ssh -G could not be run against this host — re-enter the screen to retry.
    ok   No directives match \"%s\".
    ok   Also tracked as a recommended option — see the Options tab for gitid's guidance.
    ok   Resolved via ssh -G — reflects Include/Match precedence already applied.
```

**Fail path hand-verified.** Rewording `PropsCrossReferenceNote` alone in `design.go` did NOT go red — the literal string was still present via `properties_copy_test.go`'s own `want` field (which lives inside the same grep roots). The experiment was corrected to reword BOTH occurrences together (simulating a genuine coordinated rename that forgot to update the Makefile), which produced:

```
    MISSING  Also tracked as a recommended option — see the Options tab for gitid's guidance.
gate-copy-freeze: FROZEN COPY MISSING (02-STYLE-SPEC.md §6)
```
exit code 2. Both files were reverted; `git diff internal/tuikit/design.go` and `internal/tuikit/properties_copy_test.go` post-revert showed no residual diff (the copy test file remained untracked/new as expected, `design.go` showed zero diff from its Task 2 committed state). `make gate-copy-freeze` re-run GREEN afterward.

**Three new PTY cases, all pass, `t.Skip` count 0:**

```
$ grep -c 't\.Skip' e2e/global_ssh_pty_e2e_test.go
0
$ go test -tags e2e -count=1 ./e2e/... -run 'TestGlobalSSH_RealPTY' -v
...
--- PASS: TestGlobalSSH_RealPTYAllDirectivesBrowse (5.52s)
--- PASS: TestGlobalSSH_RealPTYAllDirectivesFilter (6.33s)
--- PASS: TestGlobalSSH_RealPTYAllDirectivesProbeFailure (5.61s)
--- PASS: TestGlobalSSH_RealPTYAllDirectivesLabelMouseClick (5.52s)
...(11 pre-existing Global SSH PTY cases also PASS, unaffected)
PASS
ok  	github.com/castocolina/gitid/e2e	88.531s
```

**D-B filter capture-contract proof, before/after frames (`TestGlobalSSH_RealPTYAllDirectivesFilter`):**

Before (filter focused, "strict" typed, narrowed to the one matching row):
```
 Global SSH › All directives
 / strict                                                                             1 of 27 shown
 ▸ stricthostkeychecking     no             │  stricthostkeychecking
                                            │
                                            │  no
                                            │
                                            │  Resolved via ssh -G — reflects Include/Match
                                            │ precedence already applied.
                                            │  Also tracked as a recommended option — see the
                                            │ Options tab for gitid's guidance.
```

After typing digit `1` (still focused — the digit landed IN the field, not app.go's tab-switch globals):
```
 Global SSH › All directives
 / strict1                                                                            0 of 27 shown
 No directives match "strict1".
```
The breadcrumb stays `Global SSH › All directives` throughout — proof the digit did NOT reach `app.go`'s `1`..`5` main-tab globals. After `esc` (blur without clear) and the SAME digit `1` again, the app switches to the Identities main tab (breadcrumb loses "Global SSH" entirely), confirmed by the test's `waitFor(!strings.Contains(frame, "Global SSH"))` assertion, currently PASSING.

**SGR click coordinate derivation:** `TestGlobalSSH_RealPTYAllDirectivesLabelMouseClick` calls the existing `clickLabelRow(t, s, "All directives")` helper (`e2e/create_flow_pty_e2e_test.go`), which scans the CURRENT rendered frame for the label substring and computes `col`/`row` from the matched line's index — no terminal cell is hardcoded. Confirmed by reading the helper's implementation (lines 128-146).

**Full verification suite, all green (re-run this session, post Task 3):**

```
$ TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...
ok all 20 packages (cmd/gitid, internal/*, screenshot has no test files)

$ make lint
0 issues.

$ make test
ok all packages + screenshot build-tag suite

$ make test-e2e
ok  	github.com/castocolina/gitid/e2e	1000.676s
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing test infrastructure] `stubBackend` needed an empty-success override, not a bare Noop embed**
- **Found during:** Task 1
- **Issue:** The plan's literal Task 1 instruction was "add the noop to `stubBackend`'s embed list". Doing only that meant every bare `stubBackend{}` (used throughout the existing Global SSH test suite) would answer `AllSSHDirectives()` with `ErrSSHPropertiesBrowserNotImplemented`, breaking the new `TestGlobalSSHArrowsCycleThreeSubTabsInOppositeDirections` regression test (pressing → into the Properties sub-tab hit the fail-open branch instead of the intended sub-tab switch).
- **Fix:** Added `sshDirectives []SSHDirectiveView` / `sshDirectivesErr error` override fields to `stubBackend`, defaulting to empty-success — mirroring the EXISTING `sshOptions`/`sshOptionsErr` pattern already used for `GlobalSSHOptionStates`, the established convention for every other seam on this stub.
- **Files modified:** `internal/tuikit/backend_stub_test.go`
- **Verification:** full `internal/tuikit` suite green; the new cycle regression test passes.
- **Committed in:** `77bcfab` (part of Task 1's single buildable-boundary commit)

**2. [Rule 3 - Blocking issue] Existing `globalssh-inconclusive` fixture mode could not exercise the properties sub-tab's probe-failure state**
- **Found during:** Task 3, while implementing `TestGlobalSSH_RealPTYAllDirectivesProbeFailure`
- **Issue:** Task 3's `<read_first>` pointed at `e2e/harness_test.go`'s existing `globalssh-inconclusive` mode as "the existing way a probe failure is forced through the compiled binary". Tracing `probe.go`/`shadow.go` showed that mode's failure condition only fires when the fake `ssh` is invoked WITH a real `-F <config>` argument — that call shape belongs to `shadow.go`'s isolated shadow-check, used only inside the apply-preview flow (`openGlobalSSHPreview`). `AllDirectives`/`GlobalSSHOptionStates` both call `effective(deps)`, which runs `ssh -G <ProbeHost>` with NO `-F` at all — a call shape `globalssh-inconclusive` never fails. Empirically confirmed with a throwaway probe test before committing to this diagnosis.
- **Fix:** Added a new `globalssh-probe-unresolvable` fixture mode to `e2e/harness_test.go`, failing every `-G` resolution unconditionally regardless of `-F`.
- **Files modified:** `e2e/harness_test.go`
- **Verification:** `TestGlobalSSH_RealPTYAllDirectivesProbeFailure` passes; empirically confirmed both the Options sub-tab (degrades per-row to "not applicable (could not be probed)") and the Properties sub-tab (full-screen frozen warning) respond to the new mode, matching the plan's expectation that they "share the same `effective(deps)` call".
- **Committed in:** `d1ba222`

**3. [Rule 2 - Missing test coverage] Third-label click coverage in the in-package unit suite**
- **Found during:** Task 3
- **Issue:** Task 3's `<read_first>` named `internal/tuikit/globalssh_test.go`'s click-hit-test helpers explicitly "for the third-label click coverage", but the `<action>` text only specified the real-PTY SGR click case, leaving the in-package assertion implicit.
- **Fix:** Extended `TestSubTabStripClickSwitchesSubTabs` with a `clickCell(t, a, gssTabPropertiesLabel, 0, 0)` assertion (and the return trip to Options), directly proving `handleClick`'s fourth `hitNeedle` case at the model level, complementing the PTY-level proof.
- **Files modified:** `internal/tuikit/globalssh_test.go`
- **Verification:** `TestSubTabStripClickSwitchesSubTabs` and `TestSubTabStripClickHitTestMatchesRenderedSpans` both pass; full suite re-run green.
- **Committed in:** `d1ba222`

---

**Total deviations:** 3 auto-fixed (2× Rule 2, 1× Rule 3).
**Impact on plan:** All three were necessary for correctness or genuine test coverage the plan's own acceptance criteria required; none expand scope beyond PROP-01.

## Issues Encountered

None beyond the deviations documented above.

## Known Stubs

None. `n · Add custom directive` was deliberately NOT advertised in the footer (belongs to plan 09.5-04), per the plan's explicit instruction — this is a documented scope boundary, not a stub.

## Threat Flags

None beyond the plan's own `<threat_model>` register — no new network endpoints, auth paths, or trust-boundary crossings were introduced beyond what T-09.5-01 through T-09.5-06 already cover. The new `globalssh-probe-unresolvable` fixture mode is test-only (`e2e/harness_test.go`, `//go:build e2e`), never compiled into the shipped binary.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

The phase's architectural proof is closed: engine seam, DTO, Backend interface (Noop + both real implementations), sub-tab plumbing (3-way cycle, filter/keys capture contract, master-detail render, empty/error states), both freeze gates, and real-PTY coverage are all established on this ONE path. Plans 09.5-02 through 09.5-05 (the Git browser and both custom-entry write flows) repeat this exact stack shape as expansion, not re-litigation, per the plan's own objective. No blockers.

## Self-Check: PASSED

All 16 files listed under `key-files` + this SUMMARY confirmed present on disk (`ls -la`); all three task commit hashes (`77bcfab`, `36351c5`, `d1ba222`) confirmed present in `git log --oneline --all`.

---
*Phase: 09.5-full-ssh-git-properties-browser*
*Completed: 2026-09-03*
