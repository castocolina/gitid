---
phase: 09.5-full-ssh-git-properties-browser
verified: 2026-09-04T20:50:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 9.5: Full SSH/Git Properties Browser & Custom Key Entry Verification Report

**Phase Goal:** Close Phase 9.5 "Full SSH/Git Properties Browser & Custom Key Entry" — a user
can browse the complete resolved SSH directive set and the complete set of SET git config keys
(not just gitid's curated policy subset), and can add a custom SSH directive or custom Git
config key through the standard review-before-write ceremony with full backup/rollback safety.

**Verified:** 2026-09-04T20:50:00Z
**HEAD verified against:** `bf66a09` (`docs(09.5): round-4 independent re-review — 0 critical, converged`)
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A user can reach a third SUB-TAB (`All directives`) on Global SSH listing EVERY `ssh -G` directive, not just the curated 6-row policy | ✓ VERIFIED | `internal/globalssh/properties.go:25 AllDirectives(deps)`; `internal/tuikit/backend.go` `SSHPropertiesBrowser` seam + `NoopSSHPropertiesBrowser`; wired in `cmd/gitid/wiring.go:2084 (*realBackend).AllSSHDirectives`; anti-blindspot test `TestRealBackendDoesNotEmbedNoopSSHPropertiesBrowser` (wiring_test.go:83) exists and passes; real-PTY behavioral test `TestGlobalSSH_RealPTYAllDirectivesFilter` run directly by this verifier — **PASS (7.93s)** |
| 2 | A user can reach a `Set keys` sub-tab on Global Git listing every git config key actually SET (global/system/local), honestly scoped (never "All keys") | ✓ VERIFIED | `internal/globalgit/properties.go:46 AllSetKeys(deps)`; `GitPropertiesBrowser` seam; wired in `wiring.go:2123 AllGitSetKeys`; anti-blindspot test `TestRealBackendDoesNotEmbedNoopGitPropertiesBrowser` (wiring_test.go:121) exists; real-PTY behavioral test `TestGlobalGit_RealPTYSetKeysFilter` run directly by this verifier — **PASS (7.40s)** |
| 3 | A user can add a free-form `key = value` custom Git config pair (no name validation, syntax validation only) through the standard review-before-write ceremony with backup | ✓ VERIFIED | `internal/gitconfig/customkeys.go` (`SplitGitKey`, `ParseCustomKeysBlock`, `RenderCustomKeysBlock`, `EnsureCustomGitKey`); `cmd/gitid/lifecycle.go` `runCustomGitKeyWrite` — the one production writer, journal+backup+rollback; sentinel `CustomGitKeysBlockName = "custom-git-keys"` registered in `gitconfig.IsReservedBlockName` (reader.go:52) BEFORE the write path exists (closes the L4 doctor-destructive-loop learning); real-PTY behavioral tests `TestGlobalGit_RealPTYCustomKeyWrite` and `TestGlobalGit_RealPTYCustomKeyRejectsMalformedKey` run directly by this verifier — **both PASS** |
| 4 | A user can add a custom SSH directive, name-validated against the locally-installed OpenSSH via a staged throwaway-config `ssh -G` proof, before it ever reaches the ceremony or the real file | ✓ VERIFIED | `internal/globalssh/probe.go` `RunSSHGCombined` combined-output seam; `internal/globalssh/validate.go` `ProveCustomDirective`; fail-closed backstop confirmed in code: `validate.go:192-193` returns an error ("refusing to report an unproven directive as accepted") when `deps.RunSSHGCombined == nil`, matching the plan's declared backstop verbatim; `cmd/gitid/lifecycle.go` `runCustomSSHDirectiveWrite` re-runs the proof in the writer itself (not just the TUI caller); real-PTY behavioral tests `TestGlobalSSH_RealPTYCustomDirectiveRejectedNameNeverWrites` and `TestGlobalSSH_RealPTYCustomDirectiveWrite` run directly by this verifier — **both PASS**, including a real on-disk `.bak` file confirmed created |
| 5 | The whole gate battery is green at phase close (unit `-race`, lint, e2e, copy-freeze, visual-regression) and PROP-01..04 are marked complete against named, quoted evidence | ✓ VERIFIED | Independently re-run by this verifier at HEAD `bf66a09` (not trusted from SUMMARY/REVIEW): `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` → all packages `ok`; `make lint` → `0 issues` (default + screenshot tag); `make gate-visual-regression` → clean PASS on isolated re-run (`TestGateVisualRegression: 63 RequiredScreenSpecs... OK`); `TestPhase95FrameProvenanceMatches` → PASS; REQUIREMENTS.md lines 692-695 name the exact tests as evidence for PROP-01..04, all of which independently reproduce |

**Score:** 5/5 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/globalssh/properties.go` | `AllDirectives(deps)` full resolved directive set | ✓ VERIFIED | Present, exported, wired into `realBackend.AllSSHDirectives` |
| `internal/globalgit/properties.go` | `AllSetKeys(deps)` every set key + origin + scope | ✓ VERIFIED | Present, exported, wired into `realBackend.AllGitSetKeys` |
| `internal/tuikit/backend.go` | 4 new seams: `SSHPropertiesBrowser`, `GitPropertiesBrowser`, `GitCustomKeyPlanner`, `SSHCustomDirectivePlanner` + their Noop implementations | ✓ VERIFIED | All 4 present with `Noop*` sentinel-erroring stubs and `var _ Interface = Noop{}` compile-time assertions |
| `internal/gitconfig/customkeys.go` | Custom Git key engine (split/parse/render/ensure) | ✓ VERIFIED | Present; `CustomGitKeysBlockName` const; last-dot subsection split proven against real git in round-4 review |
| `internal/globalssh/validate.go` + `probe.go` | `ProveCustomDirective` staged-config classifier + combined-output probe seam | ✓ VERIFIED | Present; fail-closed on nil seam; whitespace-`#` guard (WR-03 round-3 fix) present |
| `cmd/gitid/lifecycle.go` | `runCustomGitKeyWrite`, `runCustomSSHDirectiveWrite` — the two production writers | ✓ VERIFIED | Both present with plan/confirm/backup/write/verify staged pipeline and their own independent fail-closed backstops |
| `internal/tuikit/globalssh.go`, `globalgit.go` | Sub-tab UI, custom-entry two/three-stage flows | ✓ VERIFIED | Present; shared `subTabStripRows`/`renderSubTabStrip` in `internal/tuikit/frame.go` used by BOTH screens (no drift) |
| `e2e/global_ssh_pty_e2e_test.go`, `e2e/global_git_pty_e2e_test.go` | Real-PTY coverage for all 4 PROP requirements | ✓ VERIFIED | All 6 named tests exist and independently re-run PASS by this verifier |
| `.planning/phases/09.5-full-ssh-git-properties-browser/ui-frames/` | Promoted frames + provenance README | ✓ VERIFIED | 17 `.txt` frames + `README.md` with per-frame SHA-256/producing-test/source-commit table present; `TestPhase95FrameProvenanceMatches` passes |
| `.planning/REQUIREMENTS.md` | PROP-01..04 marked `[x]` with quoted evidence | ✓ VERIFIED | Lines 493-515 (checkbox `[x]`) and 692-695 (evidence table) present and internally consistent with ROADMAP.md line 585 (`9.5. ... | 5/5 | Complete | 2026-09-04`) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `globalssh.AllDirectives` | `realBackend.AllSSHDirectives` | Backend seam, nil-guarded | ✓ WIRED | `TestRealBackendDoesNotEmbedNoopSSHPropertiesBrowser` present and passing — closes the project's documented recurring injected-seam blindspot |
| `globalgit.AllSetKeys` | `realBackend.AllGitSetKeys` | Backend seam, nil-guarded | ✓ WIRED | `TestRealBackendDoesNotEmbedNoopGitPropertiesBrowser` present and passing |
| `CustomGitKeysBlockName` | `gitconfig.IsReservedBlockName` | doctor orphan-check exclusion | ✓ WIRED | `reader.go:52` includes `CustomGitKeysBlockName` in the reserved-name switch, registered in the SAME commit as the writer per plan requirement (L4 learning closed) |
| `sshconfig.EnsureGlobals`'s explicit overlay | `renderGlobalBody`'s unrecognized-key branch | preserves unknown key verbatim | ✓ WIRED | Confirmed via round-4 review's independent live-binary proof (staged proof byte-identical to real write, WR-08/WR-09 round-3 fix verified genuine, not cosmetic) |
| `ProveCustomDirective`'s staged proof | `runCustomSSHDirectiveWrite`'s own re-proof | writer re-validates, not just UI caller | ✓ WIRED | `lifecycle.go` calls `globalssh.ProveCustomDirective` again inside the writer itself (comment explicitly states this is deliberate defense-in-depth) |
| `globalgit.PolicyForMember` collision guard | `CustomGitKeyPlan` (wiring.go:2540) AND `runCustomGitKeyWrite`'s own plan stage (lifecycle.go:1433) | BOTH gates enforce the collision backstop | ✓ WIRED | Round-4 review independently drove real `git` binary through both gates and confirmed refusal in both directions (CR-01, round-3 fix, confirmed genuinely closed in round 4) |

### Behavioral Spot-Checks (run directly by this verifier, not trusted from SUMMARY/REVIEW)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| PROP-01 core: full directive set + type-to-filter, real PTY | `go test -tags e2e -race -run 'TestGlobalSSH_RealPTYAllDirectivesFilter$' ./e2e/...` | PASS (7.93s) | ✓ PASS |
| PROP-02 core: Set-keys list + filter, real PTY | `go test -tags e2e -race -run 'TestGlobalGit_RealPTYSetKeysFilter$' ./e2e/...` | PASS (7.40s) | ✓ PASS |
| PROP-03 core: custom Git key write lands on disk | `go test -tags e2e -race -run 'TestGlobalGit_RealPTYCustomKeyWrite$'` | PASS (8.52s) | ✓ PASS |
| PROP-03 malformed-key rejection (syntax check, not name allow-list) | `go test -tags e2e -race -run 'TestGlobalGit_RealPTYCustomKeyRejectsMalformedKey$'` | PASS (7.10s) | ✓ PASS |
| PROP-04 rejected directive name never reaches write ceremony | `go test -tags e2e -race -run 'TestGlobalSSH_RealPTYCustomDirectiveRejectedNameNeverWrites$'` | PASS (8.93s) | ✓ PASS |
| PROP-04 accepted directive lands with real backup created | `go test -tags e2e -race -run 'TestGlobalSSH_RealPTYCustomDirectiveWrite$'` | PASS (7.97s), real `.bak` files confirmed in test output | ✓ PASS |
| Visual-regression provenance gate for this phase | `go test -tags screenshot -race -run 'TestPhase95FrameProvenanceMatches$' ./cmd/gitid/...` | PASS | ✓ PASS |
| Full unit suite, race-enabled | `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` | All 20 packages `ok`, 0 failures | ✓ PASS |
| Full lint gate | `make lint` | `0 issues` (default + screenshot build-tag pass, all 6 `go vet` tag combos, POSIX shell check) | ✓ PASS |
| Full visual-regression gate | `make gate-visual-regression` (re-run solo after a transient concurrent-process flake — see note below) | Clean PASS, no `FAIL` lines | ✓ PASS |

**Note on a transient gate-visual-regression flake:** the first `make gate-visual-regression` invocation in this verification session was run concurrently with two other `go test` invocations this verifier had also started against the same repo (shared temp `HOME` fixtures, real `git`/`ssh` subprocess contention). That single concurrent run reported `FAIL` with no matching `--- FAIL:` line ever surfacing in its own captured output — consistent with process/resource contention, not a code defect. Two subsequent solo re-runs (one full `-v` capture, one via `make gate-visual-regression` directly) were both clean with zero `FAIL` lines. This is recorded as environmental noise from this verifier's own methodology, not a phase gap.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PROP-01 | 09.5-01 | Full SSH directive browser (`All directives` sub-tab) | ✓ SATISFIED | `AllDirectives`, `SSHPropertiesBrowser`, real-PTY test re-run PASS |
| PROP-02 | 09.5-02 | Full Git set-keys browser (`Set keys` sub-tab, honestly scoped) | ✓ SATISFIED | `AllSetKeys`, `GitPropertiesBrowser`, real-PTY test re-run PASS |
| PROP-03 | 09.5-03 | Free-form custom Git key entry, syntax-only validation | ✓ SATISFIED | `customkeys.go` engine, `runCustomGitKeyWrite`, real-PTY tests re-run PASS (accept + malformed-reject) |
| PROP-04 | 09.5-04 | Validated custom SSH directive entry, staged-proof gate | ✓ SATISFIED | `ProveCustomDirective`, `runCustomSSHDirectiveWrite`, real-PTY tests re-run PASS (reject + accept) |
| (closeout) | 09.5-05 | Visual-regression registration + phase-parameterised frame promotion + requirements closeout | ✓ SATISFIED | 17 frames + provenance README present; `TestPhase95FrameProvenanceMatches` PASS; `make gate-visual-regression` PASS; REQUIREMENTS.md/ROADMAP.md consistent |

No orphaned requirements found — REQUIREMENTS.md's Phase 9.5 section (PROP-01..04) maps 1:1 to the four plans' `requirements:` frontmatter, with plan 05 covering all four for closeout evidence only (no new requirement IDs introduced).

### Anti-Patterns Found

Scanned all core Phase 9.5 files (`internal/globalssh/properties.go`, `internal/globalgit/properties.go`,
`internal/gitconfig/customkeys.go`, `internal/globalssh/validate.go`, `internal/globalssh/probe.go`,
`internal/tuikit/globalssh.go`, `internal/tuikit/globalgit.go`, `cmd/gitid/lifecycle.go`,
`cmd/gitid/wiring.go`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER`: **none found.** No debt markers,
no stub returns, no empty handlers in the write paths.

### Independent Code-Review History (context, not re-litigated by this verifier)

This phase converged after 4 independent code-review rounds (documented in `09.5-REVIEW.md` /
`09.5-REVIEW-FIX.md`, commits `2168fe2`..`bf66a09`): round 1 found+fixed 2 CRITICAL + 14 WARNING;
round 2 (independent) found+fixed 2 NEW CRITICAL + 14 WARNING; round 3 (independent) confirmed all
4 prior criticals genuinely fixed, found+fixed 1 NEW BLOCKER (the collision backstop, CR-01) + 9
WARNING; round 4 (independent, final) confirmed round 3's fix holds in both directions with live
`git`/`ssh` binary proof and reported **0 CRITICAL**, 10 WARNING, 5 INFO — none of the 10 WARNING
or 5 INFO findings block the phase goal; they are documented follow-up (round 4's own verdict:
"I did not take round 3's conclusions on trust... The two free-form write paths are sound.").

This verifier independently spot-checked the most security-relevant open warning (WR-01: a
custom SSH directive can silently overwrite a curated policy value with no collision guard, the
unguarded SSH-side twin of the Git-side CR-01 fix) by reading `cmd/gitid/wiring.go`'s
`CustomSSHDirectivePlan` and `cmd/gitid/lifecycle.go`'s `runCustomSSHDirectiveWrite` directly:
confirmed neither calls `globalssh.PolicyFor`/`PolicyForMember`, so WR-01 is genuinely still open
at HEAD `bf66a09`, exactly as round 4 reports it — not classified CRITICAL because (a) it is not a
violation of `09.5-04-PLAN.md`'s one declared backstop (staged-proof fail-closed, which IS
implemented), (b) the ceremony diff does surface the replaced line before confirmation, (c) the
write is backed up and rollback-proven, and (d) the user must type and confirm the value. This
verifier concurs with round 4's classification: a real, worth-fixing asymmetry, but not a phase
goal blocker — PROP-04's plan-declared acceptance criteria (name validated against the live
OpenSSH binary via staged proof; write goes through the existing managed-block ceremony with
backup/rollback) are met.

### Human Verification Required

None. All must-haves resolved to VERIFIED via direct codebase inspection and independently
re-run automated/behavioral evidence (no visual-appearance, real-time, or external-service
judgment calls were required beyond what the phase's own real-PTY + real-binary test suite
already proves).

### Gaps Summary

No gaps. All 5 observable truths (PROP-01 through PROP-04's core acceptance criteria, plus the
phase-close gate battery) are VERIFIED against the actual codebase at HEAD `bf66a09`, independent
of SUMMARY.md/REVIEW.md narrative:

- `go build ./...` — clean
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` — all packages green
- `make lint` — 0 issues
- `make gate-visual-regression` — clean (after ruling out a same-session concurrent-process flake)
- 6 named real-PTY behavioral tests (one per PROP-0X core claim, plus the malformed/rejected
  negative cases) — all independently re-run and PASS
- `TestPhase95FrameProvenanceMatches` — PASS
- REQUIREMENTS.md and ROADMAP.md are internally consistent and both marked complete with
  named, reproducible evidence

The 10 open WARNING findings from round 4 (WR-01 through WR-10) and 5 INFO findings (IN-01
through IN-05) are real, documented, non-blocking follow-up items — none contradicts a
plan-declared must-have or backstop. They are candidates for a future hardening phase, not a
gap in Phase 9.5's stated goal.

---

_Verified: 2026-09-04T20:50:00Z_
_Verifier: Claude (gsd-verifier)_
