---
phase: 09.5-full-ssh-git-properties-browser
plan: 05
subsystem: testing
tags: [visual-regression, go, bubbletea, pty-e2e, frame-promotion, requirements-closeout]

# Dependency graph
requires:
  - phase: 09.5-01..04
    provides: "the SSH All-directives browser, the Global Git sub-tab strip + Set-keys browser, the free-form custom Git key flow, and the validated custom SSH directive flow (PROP-01..04)"
provides:
  - "A standing gate-visual-regression guard for all 6 new Phase 9.5 render states, with a hand-verified fail path"
  - "A phase-parameterised cmd/gitid-frame-promote, with Phase 9's default behavior byte-identical and Phase 9.5's 17 frames promoted with machine-checked provenance"
  - "PROP-01 through PROP-04 marked complete in REQUIREMENTS.md against named, quoted evidence"
  - "ROADMAP.md's Phase 9.5 entry closed out (5/5 plans, all four Success Criteria evidenced)"
affects: [phase-10, future-visual-regression-registrations, future-frame-promoter-phases]

# Actuals (#2632)
actuals:
  tokens: 10503
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "RegionDisposition split by capture state (unfiltered vs filtered) when the underlying data set's default-selected row differs between real and dummy backends — anchor on a structural cue (gitCueLine's overflow marker) rather than a specific row's key when no single key is guaranteed visible on both sides."
    - "Frame-promoter phase parameterisation via a registry map (phaseFrames) keyed by phase identifier, with promoteFrames's own signature untouched so pre-existing tests need no modification."
    - "Normalize an unstable capture BEFORE promotion (captureGlobalGitFrame saves a regex-normalized copy while returning the raw snapshot to in-test assertions) rather than after — keeps the promoted baseline stable without weakening the live assertion that needs the real path."

key-files:
  created:
    - .planning/phases/09.5-full-ssh-git-properties-browser/ui-frames/README.md
    - .planning/phases/09.5-full-ssh-git-properties-browser/ui-frames/*.txt (17 frames)
  modified:
    - internal/screenshot/createflow.go
    - internal/screenshot/createflow_regions.go
    - cmd/gitid/gate_visual_regression_test.go
    - .planning/design/global-ssh/visual-divergence-allowlist.txt
    - .planning/design/global-git/visual-divergence-allowlist.txt
    - cmd/gitid-frame-promote/main.go
    - cmd/gitid-frame-promote/main_test.go
    - e2e/global_git_pty_e2e_test.go
    - e2e/ui_pty_e2e_test.go
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md

key-decisions:
  - "gss-properties-list's StateMarker and RegionSidebar/RegionGSSPropertiesBrowse dispositions anchor on the gitCueLine overflow cue ('more options)'), not 'stricthostkeychecking' — the unfiltered landing state selects row 0 (the alphabetically-first key), which is a DIFFERENT literal key on the real (~90 items) vs dummy (22 items) backend, so no single directive name is guaranteed visible on both sides without navigation. The filtered state keeps the direct key-name anchor since filtering narrows both sides to exactly that one row."
  - "writeProvenance takes phase-specific title/summary text (resolveProvenanceText) rather than a hardcoded Phase 9 heading — found while reviewing the first Phase 9.5 promotion output, whose README was mislabeled with Phase 9's own prose."
  - "captureGlobalGitFrame normalizes ShortSandboxHome's absolute path in the SAVED frame copy only; the value it returns to callers stays raw, so no existing in-test assertion (e.g. the origin-path check in TestGlobalGit_RealPTYSetKeysBrowse) is weakened."

requirements-completed: [PROP-01, PROP-02, PROP-03, PROP-04]

coverage:
  - id: D1
    description: "Standing visual-regression guard for all 6 new Phase 9.5 render states (SSH All-directives populated/filtered/custom-form/custom-invalid, Git Set-keys populated, Git custom-key ceremony), with the fail path hand-verified"
    requirement: "PROP-01"
    verification:
      - kind: integration
        ref: "make gate-visual-regression — TestGateVisualRegression: '63 RequiredScreenSpecs frames checked as a classified real/dummy symmetric union'"
        status: pass
      - kind: integration
        ref: "hand-verified RED: deliberately broke ggit-custom-key-ceremony-preview's disposition predicate, observed TestGateVisualRegression FAIL, quoted output, reverted"
        status: pass
    human_judgment: false
  - id: D2
    description: "cmd/gitid-frame-promote parameterised by phase (-phase flag), Phase 9's default behavior byte-identical, Phase 9.5's 17 frames promoted with machine-checked provenance"
    verification:
      - kind: unit
        ref: "cmd/gitid-frame-promote/main_test.go#TestPromoteFramesPhase9DefaultUnchanged"
        status: pass
      - kind: unit
        ref: "cmd/gitid-frame-promote/main_test.go#TestPromoteFramesTargetsTheRequestedPhaseDirectory"
        status: pass
      - kind: unit
        ref: "cmd/gitid-frame-promote/main_test.go#TestPromoteFramesUnknownPhaseFailsClosed"
        status: pass
      - kind: unit
        ref: "cmd/gitid-frame-promote/main_test.go#TestPromoteFramesWritesNothingWhenAnyCaptureIsMissing (pre-existing, unmodified)"
        status: pass
      - kind: unit
        ref: "cmd/gitid-frame-promote/main_test.go#TestPromoteFramesWritesEveryFrameWhenAllCapturesArePresent (pre-existing, unmodified)"
        status: pass
    human_judgment: false
  - id: D3
    description: "PROP-01 (full SSH directive browser, all ~90 resolved directives, type-to-filter) marked complete"
    requirement: "PROP-01"
    verification:
      - kind: unit
        ref: "internal/globalssh/properties_test.go#TestAllDirectivesReturnsEveryResolvedKey"
        status: pass
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYAllDirectivesBrowse"
        status: pass
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYAllDirectivesFilter"
        status: pass
    human_judgment: false
  - id: D4
    description: "PROP-02 (full Git set-keys browser across all scopes with origin, type-to-filter) marked complete"
    requirement: "PROP-02"
    verification:
      - kind: unit
        ref: "internal/globalgit/properties_test.go#TestAllSetKeysReturnsEverySetKeyWithProvenance"
        status: pass
      - kind: e2e
        ref: "e2e/global_git_pty_e2e_test.go#TestGlobalGit_RealPTYSetKeysBrowse"
        status: pass
      - kind: e2e
        ref: "e2e/global_git_pty_e2e_test.go#TestGlobalGit_RealPTYSetKeysFilter"
        status: pass
    human_judgment: false
  - id: D5
    description: "PROP-03 (free-form custom Git key entry through the standard ceremony) marked complete"
    requirement: "PROP-03"
    verification:
      - kind: e2e
        ref: "e2e/global_git_pty_e2e_test.go#TestGlobalGit_RealPTYCustomKeyWrite"
        status: pass
      - kind: e2e
        ref: "e2e/global_git_pty_e2e_test.go#TestGlobalGit_RealPTYCustomKeyRejectsMalformedKey"
        status: pass
    human_judgment: false
  - id: D6
    description: "PROP-04 (validated custom SSH directive entry, name checked before write, ssh -G prove-before-write) marked complete"
    requirement: "PROP-04"
    verification:
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYCustomDirectiveRejectedNameNeverWrites"
        status: pass
      - kind: e2e
        ref: "e2e/global_ssh_pty_e2e_test.go#TestGlobalSSH_RealPTYCustomDirectiveWrite"
        status: pass
    human_judgment: false
  - id: D7
    description: "Full gate battery green, run fresh by this task: go test -race, make lint, make test, make test-e2e, make gate-visual-regression"
    verification:
      - kind: unit
        ref: "TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./... — 'Go test: 2708 passed in 22 packages'"
        status: pass
      - kind: other
        ref: "make lint — '0 issues.' (screenshot-tagged and default build)"
        status: pass
      - kind: other
        ref: "make test — gate-copy-freeze all 'ok', go test -race -coverprofile + go test -tags screenshot"
        status: pass
      - kind: e2e
        ref: "make test-e2e — 'ok github.com/castocolina/gitid/e2e 1058.796s'"
        status: pass
      - kind: integration
        ref: "make gate-visual-regression — 'gate-visual-regression: OK — 63 RequiredScreenSpecs frames checked as a classified real/dummy symmetric union'"
        status: pass
    human_judgment: false

duration: 128min
completed: 2026-09-04
status: complete
---

# Phase 9.5 Plan 05: Visual-Regression Closeout, Phase-Parameterised Frame Promotion, and Requirements Closeout Summary

**Registered a standing gate-visual-regression guard for all 6 new Phase 9.5 render states (with a hand-verified fail path), parameterised `cmd/gitid-frame-promote` by phase and promoted its 17 PTY frames with machine-checked provenance, and closed PROP-01 through PROP-04 in REQUIREMENTS.md against named, quoted evidence behind a full green gate battery.**

## Performance

- **Duration:** ~128 min (2026-09-04 07:03 → 09:11, spanning a mid-session context compaction)
- **Tasks:** 3
- **Files modified:** 11 modified, 18 created (17 promoted frames + 1 README)

## Accomplishments

- Registered `gss-properties-list`, `gss-properties-filter`, `gss-properties-custom-form`, `gss-properties-custom-invalid`, `ggit-set-keys-list`, and `ggit-custom-key-ceremony-preview` in the shared `internal/screenshot` visual-regression registry, with 5 new `RegionName`s and their extractors, and proved the gate can actually fail on a newly registered state (quoted RED, reverted).
- Parameterised `cmd/gitid-frame-promote` by phase (`-phase` flag, default `09` byte-identical to today's behavior) via a `phaseFrames` registry, keeping `promoteFrames`'s own signature and both pre-existing tests untouched, and promoted Phase 9.5's 17 PTY frames into the tracked tree with a generated provenance README.
- Closed PROP-01 through PROP-04 in `.planning/REQUIREMENTS.md` §S against a clause-by-clause re-read, each annotated with the closing plan and a named test asserting real behavior on disk — never "tests pass."
- Updated `.planning/ROADMAP.md`'s Phase 9.5 entry (checkbox, all 5 plan checkboxes, all 4 Success Criteria evidence pointers, Progress table row) via scoped, line-count-preserving edits (586 lines before and after).
- Ran the full gate battery fresh, itself: `go test -race` (2708 passed), `make lint` (0 issues), `make test` (copy-freeze + race + screenshot), `make test-e2e` (1058.8s, -race), `make gate-visual-regression` (63 specs green).

## Task Commits

Each task was committed atomically:

1. **Task 1: Register the new render states in the visual-regression inventory and prove the gate can fail on them** - `3fc9ec3` (feat)
2. **Task 2: Parameterise the frame promoter by phase and promote this phase's evidence with provenance** - `7ccd604` (feat, TDD RED→GREEN)
3. **Task 3: Close PROP-01 through PROP-04 against quoted evidence and run the full gate battery** - `a6a8f5e` (docs)

**Plan metadata:** commit pending immediately after this SUMMARY is written (docs: complete plan)

## Files Created/Modified

- `internal/screenshot/createflow.go` - 6 new `ScreenSpec` entries, 5 new `RegionDisposition` vars, extended `CaptureGlobalSSHScreens`/`CaptureGlobalGitScreens` capture scripts
- `internal/screenshot/createflow_regions.go` - 5 new `RegionName` consts + extractors (`extractSubTabStrip`, `bodyFromAnchorToEnd`, `extractGSSPropertiesBrowse`, `extractGSSCustomDirective`, `extractGGitSetKeysBrowse`, `extractGGitCustomKeyCeremony`), plus 2 pre-existing extractor false-positive fixes
- `cmd/gitid/gate_visual_regression_test.go` - registered 6 new ScreenIDs in `globalSSHScreenIDs`/`globalGitScreenIDs`, extended `deterministicGlobalGitFixture` with a deterministic `alias.co` probe key, fixed a missing `normalizeDisposableHome` call in `mergeGlobalGitCaptures` (a genuine CR-01 non-determinism bug)
- `.planning/design/global-ssh/visual-divergence-allowlist.txt` / `.planning/design/global-git/visual-divergence-allowlist.txt` - classified entries matching the new code-level dispositions
- `cmd/gitid-frame-promote/main.go` - `-phase` flag, `phaseFrames` registry (dir/title/summary/frames per phase), `resolvePhase`/`resolveProvenanceText`/`promoteForPhase`, `phase95Frames` (17 entries), phase-aware `writeProvenance`
- `cmd/gitid-frame-promote/main_test.go` - 3 new TDD-authored tests (104 insertions, 0 deletions to the pre-existing tests)
- `e2e/global_git_pty_e2e_test.go` - `normalizeShortSandboxHomePath` + `captureGlobalGitFrame` now saves a normalized copy
- `e2e/ui_pty_e2e_test.go` - `saveFrame` refactored to delegate to shared `saveFrameContent(t, name, content)`
- `.planning/phases/09.5-full-ssh-git-properties-browser/ui-frames/` - 17 promoted frames + generated `README.md`
- `.planning/REQUIREMENTS.md` - PROP-01..04 checkboxes ticked, traceability rows annotated with evidence
- `.planning/ROADMAP.md` - Phase 9.5 checkboxes ticked, Success Criteria evidenced, Progress table row set to 5/5 Complete

## Candidate States (09.5-UI-SPEC.md Approved Base States) — Registered/Omitted

| State | Decision | Reason |
|---|---|---|
| Global SSH sub-tab strip, 3 labels | Registered implicitly | Not its own `ScreenID`, but `RegionSubTabStrip` is compared on every Global SSH screen (architectural: the gate compares every region regardless of `RequiredRegions`) — `gss-options-list` already shows the 3-label strip and passes byte-identical with no disposition needed, proving both backends render it identically via shared code. |
| Global SSH All-directives list, populated | **Registered** (`gss-properties-list`) | Plan's explicit minimum-list item. |
| Global SSH All-directives list, probe error | Omitted | Not in the plan's minimum list; same structural non-applicability pattern as `gss-options-probe-error`/`ggit-options-probe-error` (dummy cannot fail a probe it never runs), needing a second differently-seeded HOME this task's scope did not extend to. |
| Global SSH All-directives list, filter active | **Registered** (`gss-properties-filter`) | Plan's explicit minimum-list item. |
| Global SSH custom-directive form (stage 1) | **Registered** (`gss-properties-custom-form`) | Plan's explicit minimum-list item. |
| Global SSH custom-directive validate (stage 2) | **Registered** (`gss-properties-custom-invalid`, the FAILURE path) | The plan explicitly names this the phase's focal point. The stage-2 SUCCESS proof-output pane was not separately registered — not in the minimum list, and its content transitions directly into the (already-covered) ceremony preview. |
| Global SSH custom-directive ceremony (stage 3) | Omitted | Not in the plan's minimum list. UI-SPEC states it "reuses `ceremonyConfig` unmodified" — the shared ceremony chrome is already exercised by the pre-existing `gss-apply-preview`/`ggit-apply-preview` specs' `RegionConfirmationPreview` coverage. |
| Global Git sub-tab strip, 2 labels | Registered implicitly | Same reasoning as the SSH strip above — `ggit-options-list` and `ggit-set-keys-list` both carry `RegionSubTabStrip` comparison and pass with no divergence. |
| Global Git Options sub-tab | Pre-existing, unchanged | Not a new Phase 9.5 state — already registered as `ggit-options-list` before this phase. |
| Global Git Set-keys list, populated | **Registered** (`ggit-set-keys-list`) | Plan's explicit minimum-list item. |
| Global Git Set-keys list, probe error | Omitted | Same structural non-applicability pattern as above; not in the minimum list. |
| Global Git Set-keys list, zero keys set | Omitted | Not in the minimum list; the deterministic fixture always seeds `alias.co` (needed as the shared real/dummy anchor for every other state), so a genuinely-zero-keys fixture needs a third, separate seed this task's scope did not extend to. |
| Global Git custom-key form (stage 1) | Omitted | Not in the plan's minimum list (unlike the SSH side, the plan's minimum list only names "the Global Git custom-key ceremony preview," not its stage-1 form) — deliberately mirrored the plan's own asymmetric emphasis. |
| Global Git custom-key ceremony (stage 2) | **Registered** (`ggit-custom-key-ceremony-preview`) | Plan's explicit minimum-list item. |

## Task 1 Acceptance-Criteria Evidence

**`make gate-visual-regression` `-v` output showing the new specs actually running, by ScreenID** (from `TestGateVisualRegression`'s frame-count preamble and the earlier per-run traces during authoring — the new `gss-properties-*`/`ggit-set-keys-list`/`ggit-custom-key-ceremony-preview` screens are captured by `CaptureGlobalSSHScreens`/`CaptureGlobalGitScreens` and iterated by `BuildRegionDiffs` over `RequiredScreenSpecs()`, which the final green run confirmed at "63 RequiredScreenSpecs frames checked as a classified real/dummy symmetric union" — 57 pre-existing + 6 new):

```
gate_visual_regression_test.go:468: gate-visual-regression: OK — 63 RequiredScreenSpecs frames checked as a classified real/dummy symmetric union
--- PASS: TestGateVisualRegression (15.46s)
```

**StateMarker verification — grep against the actual captured text (real AND dummy), for each of the 6 new specs:**

- `gss-properties-list` marker `"more options)"` — found in both real and dummy captures (dumped via a scratch harness and grep-verified): real capture line ` ↓ (+60 more options)`, dummy capture line ` ↓ (+2 more options)`.
- `gss-properties-filter` marker `"/ strict"` — found in both (1 match each) via the filter row `/ strict` text.
- `gss-properties-custom-form` marker `tuikit.PropsAddCustomDirectiveLabel` ("Add custom directive") — found in both (1 match each).
- `gss-properties-custom-invalid` marker (interpolated rejection sentence with the fixed name `"NotARealDirective"`) — found in both (1 match each): `'NotARealDirective' is not a recognized SSH directive — nothing was written.`
- `ggit-set-keys-list` marker `"alias.co"` — found in both (1 match each); the real capture showed only 2 seeded keys (`alias.co`, `credential.helper`) so `alias.co` is selected by default (no scroll-window issue on this surface).
- `ggit-custom-key-ceremony-preview` marker `"Write custom Git key to"` — found in both (1 match each).

**No new `StateMarker` contains an interpolated path, count, timestamp, or version string** — confirmed per spec: `"more options)"`, `"/ strict"`, `tuikit.PropsAddCustomDirectiveLabel`, `fmt.Sprintf(tuikit.PropsSSHUnknownDirectiveFmt, "NotARealDirective")` (a fixed literal name, not machine-varying), `"alias.co"`, `"Write custom Git key to"` — none embed a live path/count/timestamp/version.

**Allowlist outcome:** `git diff --stat` on both files is NON-empty (genuine divergences were found and classified, not the "shared code, no divergence" case):

```
.../global-git/visual-divergence-allowlist.txt     | 35 ++++++++++++++--
.../global-ssh/visual-divergence-allowlist.txt     | 48 ++++++++++++++++++++--
2 files changed, 76 insertions(+), 7 deletions(-)
```

Each added entry quotes the gate output that showed the divergence (see "Deviations from Plan" below for the full discovery trace), names its classification (`improvement`, class `DLV-4`), and gives its `contains:` anchor predicate — `T-06-PROPERTIES-*` (Global SSH) and `T-07-SET-KEYS-*`/`T-07-CUSTOM-KEY-CEREMONY-*` (Global Git).

**Fail path hand-verified on a NEW spec** — deliberately changed `ggitCustomKeyCeremonyDisposition`'s predicate to a needle guaranteed absent (`DELIBERATE-GATE-FAIL-PROOF-09.5-05-TASK1`), ran `go test -tags screenshot -run 'TestGateVisualRegression$' -v ./cmd/gitid/...`, observed:

```
gate_visual_regression_test.go:453: gate-visual-regression: building symmetric registry region evidence: screenshot: BuildRegionDiffs: frame "ggit-custom-key-ceremony-preview" region "ggit-custom-key-ceremony" disposition predicate "contains:\"DELIBERATE-GATE-FAIL-PROOF-09.5-05-TASK1\"" does not match the observed divergence (live: "\x1b[1mWrite custom Git key to ~/.gitconfig.d/00-baseline\x1b[m...
--- FAIL: TestGateVisualRegression (15.03s)
```

then reverted the predicate back to `contains:"Write custom Git key to"`. `git status` was clean of the experiment after the revert (`git diff internal/screenshot/createflow.go` showed 276 insertions and 0 deletions — a net-additive diff, confirming no residual mutation).

**`TestNegativeControl_*` gate controls unmodified** — `git diff cmd/gitid/gate_visual_regression_test.go` (37 insertions, 1 deletion; the deletion is the closing `}` of a map literal, restored identically with new entries before it) shows only additive registrations (new ScreenID map entries, `deterministicGlobalGitFixture`'s new fixture line, and the `normalizeDisposableHome` bug fix in `mergeGlobalGitCaptures`) — no existing `TestNegativeControl_*` function body was touched.

**Previously registered states unaffected** — screen count went from 57 (pre-existing) to 63 (57 + 6 new); no existing spec's `RegionDispositions` classification changed (only additions: 2 new sidebar dispositions for `gss-properties-list`/`gss-properties-filter`, split for filtered vs unfiltered state, and the 1 new `RegionConfirmationPreview` disposition on `ggit-custom-key-ceremony-preview`).

`TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` and `make lint` — both green (quoted under Task 3 below, run again fresh at phase close).

## Task 2 Acceptance-Criteria Evidence

**Three new `<behavior>` tests exist and pass; RED observed first:**

```
$ go test -count=1 ./cmd/gitid-frame-promote/...
gitid-frame-promote [build failed]
  cmd/gitid-frame-promote/main_test.go:116:29: undefined: phase95Frames
  cmd/gitid-frame-promote/main_test.go:118:23: undefined: promoteForPhase
  cmd/gitid-frame-promote/main_test.go:146:22: undefined: resolvePhase
  cmd/gitid-frame-promote/main_test.go:146:35: undefined: defaultPhase
  ... too many errors
```

GREEN after implementing `phase95Frames`, `defaultPhase`, `phaseFrames`, `resolvePhase`, `promoteForPhase`:

```
$ go run ... (rtk proxy go test -count=1 -race -v ./cmd/gitid-frame-promote/...)
=== RUN   TestPromoteFramesWritesNothingWhenAnyCaptureIsMissing
--- PASS: TestPromoteFramesWritesNothingWhenAnyCaptureIsMissing (0.00s)
=== RUN   TestPromoteFramesWritesEveryFrameWhenAllCapturesArePresent
--- PASS: TestPromoteFramesWritesEveryFrameWhenAllCapturesArePresent (0.00s)
=== RUN   TestPromoteFramesTargetsTheRequestedPhaseDirectory
--- PASS: TestPromoteFramesTargetsTheRequestedPhaseDirectory (0.01s)
=== RUN   TestPromoteFramesPhase9DefaultUnchanged
--- PASS: TestPromoteFramesPhase9DefaultUnchanged (0.00s)
=== RUN   TestPromoteFramesUnknownPhaseFailsClosed
--- PASS: TestPromoteFramesUnknownPhaseFailsClosed (0.00s)
PASS
ok  	github.com/castocolina/gitid/cmd/gitid-frame-promote	1.323s
```

**`git diff cmd/gitid-frame-promote/main_test.go` shows the two PRE-EXISTING test functions unmodified:**

```
cmd/gitid-frame-promote/main_test.go | 104 +++++++++++++++++++++++++++++++++++
1 file changed, 104 insertions(+)
```

Zero deletions — only additions after the two pre-existing test bodies.

**`TestPromoteFramesPhase9DefaultUnchanged` establishes byte-identical default behavior:** it calls `resolvePhase(defaultPhase)` and asserts `defaultPhase == "09"`, the returned directory equals the literal `"09-upload-credentials-assist"` string, and the returned frame slice has the same length and every entry `==` (Go struct equality, field-by-field) the pre-existing `phase9Frames` slice — proving the parameterisation introduced no drift in either the destination or the frame list for the no-argument invocation.

**Promoted frames, listed** (17 total, under `.planning/phases/09.5-full-ssh-git-properties-browser/ui-frames/`):
`global-git-custom-key-malformed.txt`, `global-git-custom-key-write.txt`, `global-git-set-keys-back-to-options.txt`, `global-git-set-keys-browse.txt`, `global-git-set-keys-filter-digit-captured.txt`, `global-git-set-keys-filter-narrowed.txt`, `global-git-set-keys-probe-failure.txt`, `global-git-strip-click-to-options.txt`, `global-git-strip-click-to-set-keys.txt`, `global-ssh-all-directives-browse.txt`, `global-ssh-all-directives-filter-digit-captured.txt`, `global-ssh-all-directives-filter-narrowed.txt`, `global-ssh-all-directives-label-click.txt`, `global-ssh-all-directives-probe-failure.txt`, `global-ssh-custom-directive-ceremony.txt`, `global-ssh-custom-directive-rejected-name.txt`, `global-ssh-custom-directive-write-receipt.txt`.

**Each promoted frame reviewed for an embedded absolute sandbox path or timestamp:**

```
$ grep -n "/tmp/\|/var/\|/private/\|/Users/" *.txt
global-git-set-keys-browse.txt:11:  /tmp/h3858340329/.gitconfig
global-git-set-keys-filter-narrowed.txt:11:  /tmp/h2806313330/.gitconfig
$ grep -nE "[0-9]{4}-[0-9]{2}-[0-9]{2}T|[0-9]{2}:[0-9]{2}:[0-9]{2}" *.txt
(no matches)
```

2 of 17 frames were unstable — the Set-keys detail pane's `git config --show-origin` provenance line renders `ShortSandboxHome`'s literal per-run path. **Fixed the capture, not the workaround**: added `normalizeShortSandboxHomePath` (a narrow `/tmp/h[0-9]+` regex, matching ONLY `ShortSandboxHome`'s own fixed prefix — see `e2e/harness_test.go`) so `captureGlobalGitFrame` saves a normalized copy while still RETURNING the raw snapshot to the caller, so every existing in-test assertion (including `TestGlobalGit_RealPTYSetKeysBrowse`'s own origin-path check, which specifically needs the real path visible) is unaffected. Re-ran, re-promoted, re-scanned — clean:

```
$ grep -n "/tmp/h[0-9]\|/tmp/\|/var/\|/private/\|/Users/" *.txt
global-git-set-keys-browse.txt:11:  /tmp/h<sandbox>/.gitconfig
global-git-set-keys-filter-narrowed.txt:11:  /tmp/h<sandbox>/.gitconfig
```

Reproducibility proven: re-ran `TestGlobalGit_RealPTYSetKeysBrowse`/`Filter` a second time and diffed both regenerated frames against the promoted copies — `diff` exit 0, byte-identical, for both files.

**Promotion was reproducible, not hand-assembled** — exact command and its output:

```
$ go run ./cmd/gitid-frame-promote -phase 09.5
promoted global-git-custom-key-malformed.txt           <- tmp/ui-frames/global-git-custom-key-malformed.txt
... (17 lines)
promoted 17 frame(s) for phase 09.5; provenance written to .../ui-frames/README.md
```

**`TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid-frame-promote/...`** — `Go test: 5 passed in 1 packages`.
**`make build`** — clean (`go build ... -o bin/gitid ./cmd/gitid`).
**`make test-e2e`** — `ok github.com/castocolina/gitid/e2e 1056.692s` (re-confirmed fresh at Task 3, see below).

## Task 3 Full Gate Battery (run fresh, by this task)

```
$ TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...
Go test: 2708 passed in 22 packages

$ make lint
/Users/ramon/go/bin/golangci-lint run --build-tags screenshot ./internal/screenshot/...
0 issues.
/Users/ramon/go/bin/golangci-lint run ./...
0 issues.

$ make test
==> gate-copy-freeze: 02-STYLE-SPEC.md §6 frozen copy
    ok   [... every frozen string ...]
(gate-copy-freeze + go test -race -coverprofile + go test -tags screenshot, exit 0)

$ make test-e2e
go test -tags e2e -race -timeout 2400s ./e2e/...
ok  	github.com/castocolina/gitid/e2e	1058.796s

$ make gate-visual-regression
gate_visual_regression_test.go:468: gate-visual-regression: OK — 63 RequiredScreenSpecs frames checked as a classified real/dummy symmetric union
--- PASS: TestGateVisualRegression (15.46s)
```

All five green; none red — no requirement was marked complete against a red gate.

## PROP-01..04 Clause-by-Clause Closeout

**PROP-01** (Full SSH directive browser): "a new sub-tab on the Global SSH screen" — MET (`gssProperties` sub-tab, `subTabStrip()` grown to 3 labels). "lists EVERY SSH directive `ssh -G <host>` resolves (the full, bounded ~90-directive set, set or default)" — MET: `internal/globalssh/properties.go`'s `AllDirectives` resolves the LIVE `ssh -G` output (not the curated `Policy` subset); `TestAllDirectivesReturnsEveryResolvedKey` asserts `len(got) > len(Policy)` (strictly more than the 6-key curated table), and the real-PTY capture during Task 1 observed ~90 directives with a `+60 more options` overflow cue against the dummy's frozen 22-row fixture. "not just gitid's curated `OptionPolicy` table" — MET (same evidence: the assertion is explicitly against `len(Policy)`). "as a flat, type-to-filter scrollable list" — MET: `TestGlobalSSH_RealPTYAllDirectivesFilter` proves the filter narrows the list and the D-B keyboard-capture contract (digits reach the filter, not the main-tab globals) through a real terminal.

**PROP-02** (Full Git set-keys browser): "a new sub-tab on the Global Git screen" — MET (`gssSubTab`-mirrored `Set keys` sub-tab, Global Git's FIRST sub-tab strip). "lists every git config key actually SET across all scopes (global/system/local)" — MET: `TestAllSetKeysReturnsEverySetKeyWithProvenance` asserts one `SetKey` per record spanning all three scope words git's `--list --show-origin --null` output can carry. "sourced from `git config --list --show-origin`, with its origin file" — MET: same test asserts the `Origin` field with its `file:` prefix stripped; the real-PTY `TestGlobalGit_RealPTYSetKeysBrowse` asserts the detail pane shows the literal origin file path. "not a catalog of all possible keys... honestly scoped to 'what is set'" — MET: `AllSetKeys` has no static key catalog — it parses only what `git config --list` actually returns. "as a flat, type-to-filter scrollable list" — MET: `TestGlobalGit_RealPTYSetKeysFilter`.

**PROP-03** (Custom Git key entry): "from the Git properties browser, the user can add an arbitrary global `key = value` pair" — MET: `TestGlobalGit_RealPTYCustomKeyWrite` drives the full flow and asserts the on-disk managed-block content. "free-form (no directive-name validation...)" — MET: `TestGlobalGit_RealPTYCustomKeyRejectsMalformedKey` proves the ONLY check is key-syntax (a dot-form `section.key`, rejecting `nodothere` for lacking a dot) — never an allow-list of recognized git keys. "through the same review-before-write ceremony every other gitid write path uses" — MET: both tests exercise the standard `ceremonyConfig` diff-preview/confirm/backup flow (the same `RegionConfirmationPreview`/"Exact change" shared code Task 1's disposition work confirmed).

**PROP-04** (Custom SSH directive entry, validated): "the user can add an arbitrary global SSH directive" — MET: `TestGlobalSSH_RealPTYCustomDirectiveWrite`. "the entered directive NAME is validated against OpenSSH's known-directive list before the write proceeds" — MET: `TestGlobalSSH_RealPTYCustomDirectiveRejectedNameNeverWrites` submits `NotARealDirective` (genuinely unrecognized by the REAL locally-installed OpenSSH, not a hardcoded keyword list gitid maintains) and asserts the managed target stays byte-identical — the write ceremony is never reached. "plus the existing prove-before-write `ssh -G` verification gitid already uses elsewhere" — MET: `ProveCustomDirective` (`internal/globalssh/validate.go`) stages a throwaway config and re-verifies via `ssh -G`, the same technique other gitid write paths already use; `TestGlobalSSH_RealPTYCustomDirectiveWrite` proves the accepted path lands on disk only after this verification.

No clause was found unmet. All four requirements ticked.

## Decisions Made

- Anchored `gss-properties-list`'s StateMarker and its `RegionSidebar`/`RegionGSSPropertiesBrowse` dispositions on the shared `gitCueLine` overflow cue (`"more options)"`) rather than `"stricthostkeychecking"` — the unfiltered landing state's row-0 default selection is a different literal key on each backend, so no single directive name is guaranteed visible without navigation; the filtered state correctly keeps the direct key-name anchor.
- Made `writeProvenance` phase-aware (title/summary sourced from `phaseFrames`) rather than hardcoding Phase 9's prose — found only by reviewing the actual promoted README output, not by static reasoning about the diff.
- Normalized `captureGlobalGitFrame`'s SAVED copy only, never its RETURNED value — preserves every existing in-test assertion that specifically needs the real sandbox path, while still producing a stable promoted baseline.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `extractSubTabStrip`'s border-glyph-only match false-positived against an unrelated `PreviewBlock`**
- **Found during:** Task 1, first `make gate-visual-regression` run after registering the new `RegionSubTabStrip`
- **Issue:** `frame.go`'s `previewDashedBorder` (the create-flow wizard's "Live Host-block preview" box) uses the IDENTICAL `╭╌…╮`/`┊…┊`/`╰╌╌…╯` glyph set as the sub-tab strip's own border, so the generic glyph-based extractor picked up the wrong content on an unrelated screen (`ssh-form-filled`), reporting a spurious real-vs-dummy divergence.
- **Fix:** Narrowed `extractSubTabStrip` to require BOTH the `┊` border glyph AND the label text ("Options" plus one of "All directives"/"Storage & preview"/"Set keys") before matching.
- **Files modified:** `internal/screenshot/createflow_regions.go`
- **Verification:** `make gate-visual-regression` no longer reports the `ssh-form-filled` false positive; the sub-tab strip states still classify correctly.
- **Committed in:** `3fc9ec3` (Task 1 commit)

**2. [Rule 1 - Bug] `extractConnectivityOutput`'s `"ssh -"` needle false-positived a SECOND time against `PropsSSHSourceLine`**
- **Found during:** Task 1, second `make gate-visual-regression` run
- **Issue:** `PropsSSHSourceLine`'s frozen text ("Resolved via ssh -G — reflects Include/Match precedence already applied.") contains the substring `"ssh -"`, the same over-broad needle the 04-04-PLAN.md Task 3 fix already had to exclude once for a different phrase — swallowing the rest of the `gss-properties-list` frame from that point on.
- **Fix:** Added an explicit `!strings.Contains(rpPlain, "Resolved via ssh -G")` exclusion, matching the same narrowing technique the prior fix used.
- **Files modified:** `internal/screenshot/createflow_regions.go`
- **Verification:** `make gate-visual-regression` no longer reports the `gss-properties-list`/`connectivity-output` false positive.
- **Committed in:** `3fc9ec3` (Task 1 commit)

**3. [Rule 1 - Bug] `mergeGlobalGitCaptures` was missing the `normalizeDisposableHome` call its sibling already has**
- **Found during:** Task 1, `TestGateVisualRegression` CR-01 non-determinism failure on `ggit-set-keys-list`
- **Issue:** `mergeGlobalSSHCaptures` normalizes the real capture's disposable-home path before comparison; `mergeGlobalGitCaptures` never did — harmless before Phase 9.5 (no prior Global Git region rendered an absolute path), but the new Set-keys detail pane's `git config --show-origin` origin path made this a genuine non-determinism bug across two `t.TempDir()` runs.
- **Fix:** Added the `normalizeDisposableHome` call to `mergeGlobalGitCaptures`, matching `mergeGlobalSSHCaptures`'s existing pattern exactly.
- **Files modified:** `cmd/gitid/gate_visual_regression_test.go`
- **Verification:** The CR-01 determinism check in `TestGateVisualRegression` passes for all Global Git screens across two independent runs.
- **Committed in:** `3fc9ec3` (Task 1 commit)

**4. [Rule 1 - Bug] `gss-properties-list`'s StateMarker/dispositions initially anchored on a key not guaranteed visible**
- **Found during:** Task 1, `make gate-visual-regression` predicate-mismatch failure on the `sidebar` and `gss-properties-browse` regions for `gss-properties-list`
- **Issue:** The original design anchored on `"stricthostkeychecking"`, assuming it would appear on both sides regardless of selection. It does NOT: the unfiltered landing state selects row 0 (the alphabetically-first key), which is a different literal key on the real (~90-item) vs dummy (22-item) list, and `stricthostkeychecking` sorts well past the visible ~20-row window on the real side.
- **Fix:** Re-anchored the unfiltered state's `StateMarker` and its `gss-properties-browse`/`sidebar` dispositions on the shared `gitCueLine` overflow cue (`"more options)"`), present on both sides because both lists exceed the visible window. Left the filtered state's dispositions on the direct `"stricthostkeychecking"` anchor, since filtering correctly narrows both sides to exactly that one row.
- **Files modified:** `internal/screenshot/createflow.go`, `.planning/design/global-ssh/visual-divergence-allowlist.txt`
- **Verification:** `make gate-visual-regression` — `63 RequiredScreenSpecs` green; grep-verified the new marker present in both real and dummy captures (quoted above).
- **Committed in:** `3fc9ec3` (Task 1 commit)

**5. [Rule 1 - Bug] `ggit-custom-key-ceremony-preview`'s `RegionConfirmationPreview` cross-screen contamination**
- **Found during:** Task 1, `make gate-visual-regression` run after fixing the sidebar issue above
- **Issue:** `internal/tuikit/ceremony.go`'s shared "Exact change" hint triggers `RegionConfirmationPreview`'s extraction on this new screen too (the same shared-code pattern `ggit-apply-preview` already has a disposition for), reported as an unclassified divergence.
- **Fix:** Added the same `uxRegionDifference(RegionConfirmationPreview, ...)` blanket disposition `ggit-apply-preview` already carries, for the same reason (shared ceremony code, no allowlist-schema region name for it).
- **Files modified:** `internal/screenshot/createflow.go`
- **Verification:** `make gate-visual-regression` fully green (`63 RequiredScreenSpecs`); this was also the disposition deliberately broken to prove the fail path (see Task 1 evidence above).
- **Committed in:** `3fc9ec3` (Task 1 commit)

**6. [Rule 3 - Blocking] Stale `golangci-lint` cache misattributed a sibling worktree's pre-existing findings**
- **Found during:** Task 1, first `make lint` run after the gofmt fix
- **Issue:** `make lint` failed with 19 gosec + 1 revive findings against files this task never touched (`internal/keygen/*`, `internal/globalgit/policy.go`, `internal/platform/platform.go`, `internal/sshconfig/*`, `internal/tester/*`, `internal/identity/state.go`), all displayed under a DIFFERENT worktree's path prefix (`agent-a8c692c111578303e`, not this task's own `agent-a83c4ba5f1e6f54a6`) — a stale `golangci-lint` result cache from a concurrent/prior run on a sibling worktree.
- **Fix:** `golangci-lint cache clean`, then re-ran `make lint` — fully green (`0 issues.` twice).
- **Files modified:** none (tooling cache only, not a code change)
- **Verification:** `make lint` clean, reproduced across two subsequent fresh runs (Task 2 and Task 3).
- **Committed in:** n/a (environment fix, not a source change)

**7. [Rule 1 - Bug] `writeProvenance` always wrote Phase 9's own prose regardless of which phase was promoted**
- **Found during:** Task 2, reviewing the FIRST `go run ./cmd/gitid-frame-promote -phase 09.5` output — the generated README's `# Phase 9 upload-surface approved PTY frames` heading mislabeled the Phase 9.5 directory.
- **Issue:** `writeProvenance` hardcoded the Phase 9 title/summary text, untouched by the phase parameterisation.
- **Fix:** Added `title`/`summary` fields to `phaseFrames`, a `resolveProvenanceText` accessor, and threaded both into `writeProvenance`'s signature.
- **Files modified:** `cmd/gitid-frame-promote/main.go`
- **Verification:** Re-promoted; the README now reads `# Phase 9.5 Global SSH/Git properties-browser approved PTY frames` with Phase 9.5's own summary text; Phase 9's own README (untouched, not re-promoted this run) still shows its original heading (`git status --short .planning/phases/09-upload-credentials-assist/` empty).
- **Committed in:** `7ccd604` (Task 2 commit)

**8. [Rule 1 - Bug] 2 of 17 promoted frames embedded `ShortSandboxHome`'s absolute per-run path**
- **Found during:** Task 2, the mandatory promoted-frame path/timestamp review
- **Issue:** `global-git-set-keys-browse.txt` and `global-git-set-keys-filter-narrowed.txt` embedded the literal `/tmp/h<digits>/.gitconfig` origin path — per-run, per-machine, not a stable baseline.
- **Fix:** Added `normalizeShortSandboxHomePath` (a narrow regex matching only `ShortSandboxHome`'s own `/tmp/h[0-9]+` prefix) and had `captureGlobalGitFrame` save a normalized copy while still returning the raw snapshot; refactored `saveFrame` to delegate to a new shared `saveFrameContent(t, name, content)` so the new call site does not duplicate its `MkdirAll`/`WriteFile` logic.
- **Files modified:** `e2e/global_git_pty_e2e_test.go`, `e2e/ui_pty_e2e_test.go`
- **Verification:** Re-ran the two affected PTY tests, re-promoted, re-scanned (clean, only the stable `/tmp/h<sandbox>` placeholder remains); re-ran a second time and diffed both regenerated frames byte-for-byte against the promoted copies (identical) to prove reproducibility.
- **Committed in:** `7ccd604` (Task 2 commit)

---

**Total deviations:** 8 auto-fixed (6 bugs found while authoring Task 1's registry, 1 blocking environment issue, 1 bug found while reviewing Task 2's promoted output) + 2 additional bugs found during the mandatory promoted-frame stability review (frame-path normalization).
**Impact on plan:** All fixes were necessary for correctness (a false-positive gate failure, a genuinely non-deterministic test, an unstable promoted baseline, a mislabeled README) or environmental (a stale lint cache). None represent scope creep — each was discovered only by actually running the gate/tests the plan required, exactly as CLAUDE.md's hypothesis→test→implementation discipline expects.

## Known Stubs

None — no hardcoded empty values, placeholder text, or unwired data sources were introduced by this plan. All 6 newly registered render states drive real production code paths (`globalssh.AllDirectives`, `globalgit.AllSetKeys`, `EnsureCustomGitKey`, `ProveCustomDirective`) through both the real and dummy backends.

## Threat Flags

None — this plan registered a test/tooling guard and closed out documentation bookkeeping; it introduced no new network endpoints, auth paths, file-access patterns, or schema changes at a trust boundary beyond what the plan's own `<threat_model>` (T-09.5-28..32) already anticipated and which the Task 1/2 acceptance criteria directly addressed (frame path/timestamp review, allowlist predicate discipline, scoped ROADMAP/REQUIREMENTS edits).

## Issues Encountered

None beyond the deviations documented above — every issue found had a direct, scoped fix within this plan's own execution.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 9.5 is fully closed: all 5 plans complete, all 4 requirements (PROP-01..04) marked complete against named evidence, the full gate battery green.
- Phase 10 (Linux Validation + Release Pipeline, PLAT-03) is the next phase in the roadmap's execution order — no blockers identified from this plan's work.
- The visual-regression registry pattern this plan established (split unfiltered/filtered dispositions when a data set's default selection differs by backend; anchor on structural cues like `gitCueLine`'s overflow marker when no single value is guaranteed visible) is reusable for any future phase adding a new master-detail browser.
- The frame-promoter's `phaseFrames` registry pattern is reusable for any future phase's PTY-evidence promotion — add one entry, no new promoter tool.

## Self-Check: PASSED

- `internal/screenshot/createflow.go`, `internal/screenshot/createflow_regions.go`, `cmd/gitid/gate_visual_regression_test.go`, `.planning/design/global-ssh/visual-divergence-allowlist.txt`, `.planning/design/global-git/visual-divergence-allowlist.txt`, `cmd/gitid-frame-promote/main.go`, `cmd/gitid-frame-promote/main_test.go`, `e2e/global_git_pty_e2e_test.go`, `e2e/ui_pty_e2e_test.go`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/phases/09.5-full-ssh-git-properties-browser/ui-frames/README.md` — all confirmed present via `git diff --stat`/`ls` during execution.
- `3fc9ec3`, `7ccd604`, `a6a8f5e` — all three task commits confirmed present via `git log --oneline --all`.

---
*Phase: 09.5-full-ssh-git-properties-browser*
*Completed: 2026-09-04*
