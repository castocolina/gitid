---
phase: 02-design-all-mockups-checkpoint-1
plan: 09
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/health/FIELDS.md
  - .planning/design/health/manifest.json
  - .planning/design/health/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/health/health-all-green.route.tsx
  - .planning/design/mockup-src/src/routes/health/health-with-findings.route.tsx
  - .planning/design/mockup-src/src/routes/health/finding-detail.route.tsx
  - .planning/design/mockup-src/src/routes/health/per-identity-health.route.tsx
  - .planning/design/mockup-src/src/routes/health/parse-error.route.tsx
  - internal/dummytui/surface_health.go
  - internal/dummytui/surface_health_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The health screen is mocked and dummied across all 5 named states from UX-DIRECTION §4.6, with SSH and Git sections"
    - "Findings detect redundancy/contradiction (multiple Host *; IdentitiesOnly no with a specific IdentityFile; an includeIf targeting a missing fragment) and cover per-identity + global health (HLTH-01..06)"
    - "Health is visibly READ-ONLY: it diagnoses, never mutates — NO write ceremony (no confirm/backup/apply) appears on this surface at all (§4.6, §5)"
    - "Both media captured (5 PNGs each); dummy-nav e2e reaches every screen; CRITIQUE.md 0 OPEN"
  artifacts:
    - path: ".planning/design/health/manifest.json"
      provides: "per-screen {htmlRoute, keys, signature} for 5 states"
      contains: "health-with-findings"
    - path: "internal/dummytui/surface_health.go"
      provides: "TUI dummy health surface (view 4, number key 4), read-only, backend-free"
      contains: "Register"
  key_links:
    - from: "src/routes/health/health-with-findings.route.tsx"
      to: "SSH + Git two-section findings (severity-sorted)"
      via: "diagnose only, no write ceremony"
      pattern: "IdentitiesOnly"
    - from: "internal/dummytui/surface_health.go"
      to: "dummytui.Register (number key 4)"
      via: "init()-registration, no model.go edit"
      pattern: "Register"
---

<objective>
Fan-out surface: **Health check** (view `4`, owned later by Phase 8). Repeat the proven per-surface pipeline (02-04) for all 5 states from UX-DIRECTION §4.6.

States (verbatim §4.6): `health-all-green` · `health-with-findings` (severity-sorted, split SSH / Git) · `finding-detail` (e.g. multiple `Host *`; `IdentitiesOnly no` with a specific `IdentityFile`; an `includeIf` targeting a missing fragment) · `per-identity-health` (the slice that feeds a Manager row) · `parse-error` (a config file that won't parse).

Highest-risk affordance (§4.6): **read-only integrity** — Health must clearly diagnose, NOT mutate; it hands off to the Fixer. NO write ceremony (no confirm/backup/apply beat) appears on this surface at all — the design must not blur "reported" with "fixed".

DLV-02: author the MUI mockup with `/mui`, the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/ssh-config.recipe
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: health FIELDS.md + manifest.json + MUI mockup (5 states)</name>
  <files>.planning/design/health/FIELDS.md, .planning/design/health/manifest.json, .planning/design/mockup-src/src/routes/health/health-all-green.route.tsx, .planning/design/mockup-src/src/routes/health/health-with-findings.route.tsx, .planning/design/mockup-src/src/routes/health/finding-detail.route.tsx, .planning/design/mockup-src/src/routes/health/per-identity-health.route.tsx, .planning/design/mockup-src/src/routes/health/parse-error.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.6 (5 states + read-only integrity affordance), § 2 (master-detail; SSH+Git sections; color+glyph+word), § 5 (Health never writes)
    - .planning/design/create-flow/FIELDS.md + a create-flow route (02-04 — proven format)
    - REQUIREMENTS.md HLTH-01..06 (two sections; files exist+parse; redundancy/override; contradictions; per-identity + global), .planning/design/mockup-src/src/data/recipeFixtures.ts
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` direction and the 02-04 format: author `FIELDS.md` (5-state table), `manifest.json` (5 entries), and 5 `*.route.tsx` under `src/routes/health/` inside `<Shell>` (master-detail) with SSH and Git SECTIONS: `health-all-green` (both sections healthy, green ✓ + word); `health-with-findings` (severity-sorted findings split SSH / Git); `finding-detail` showing concrete example findings (multiple `Host *`; `IdentitiesOnly no` with a specific `IdentityFile`; an `includeIf` targeting a missing fragment); `per-identity-health` (the slice feeding a Manager row); `parse-error` (a config file that won't parse). CRITICAL: NO write ceremony anywhere on this surface — no confirm/backup/apply beat; Health only diagnoses and points to the Fixer. Real recipe copy, no lorem. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/health/manifest.json'))))") -eq 5</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 5 screens: `test $(python3 -c "import json;print(len(json.load(open('.planning/design/health/manifest.json'))))") -eq 5` AND `grep -q 'health-with-findings' .planning/design/health/manifest.json`
    - 5 route files exist: `test $(ls .planning/design/mockup-src/src/routes/health/*.route.tsx | wc -l) -eq 5`
    - SSH+Git sections + contradiction examples present: `grep -rq 'IdentitiesOnly' .planning/design/mockup-src/src/routes/health/` AND `grep -rq 'Host \*' .planning/design/mockup-src/src/routes/health/`
    - Read-only integrity is explicit in FIELDS.md (no write ceremony on Health): `grep -qi 'read-only' .planning/design/health/FIELDS.md`
    - No lorem: `! grep -riq 'lorem' .planning/design/mockup-src/src/routes/health/`
    - `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The health MUI mockup renders all 5 states with SSH/Git sections and concrete redundancy/contradiction findings, visibly read-only (no write ceremony).</done>
</task>

<task type="auto">
  <name>Task 2: health TUI dummy surface (5 screens, number key 4, read-only, backend-free)</name>
  <files>internal/dummytui/surface_health.go, internal/dummytui/surface_health_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/registry.go, internal/dummytui/data.go (02-02 — the placeholder Health view on key 4 to replace)
    - .planning/design/health/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 4 = key 4), § 4.6 (read-only)
  </read_first>
  <action>
    Write `surface_health.go` in `package dummytui` mirroring 02-04: register health as the primary view on number key `4` (replacing the 02-02 placeholder). Define 5 `ScreenDef`s matching `manifest.json`; render health-all-green, health-with-findings (SSH+Git sections, severity-sorted, ✓/!/✗ glyph+word), finding-detail (concrete contradiction examples), per-identity-health, parse-error — hardcoded fixture values. NO write-ceremony screens (this surface never mutates). NO backend import. Write `surface_health_test.go` asserting 5 registered screens, each RenderScreen non-empty + contains its signature, connected Keys path, AND that no health screen's Render contains a confirm/backup/apply write-ceremony marker (read-only integrity).
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run Health && ! (go list -deps ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)')</automated>
  </verify>
  <acceptance_criteria>
    - `surface_health.go` registers 5 screens on number key 4: `grep -q 'Register' internal/dummytui/surface_health.go`; test asserts count 5
    - `go test -race ./internal/dummytui/... -run Health` passes, incl. the read-only assertion (no write-ceremony markers on any health screen)
    - Copy anchors: `grep -q 'IdentitiesOnly' internal/dummytui/surface_health.go`
    - No backend import: `! grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)' internal/dummytui/surface_health.go`
    - `make lint` passes
  </acceptance_criteria>
  <done>The health surface exists in the dummy as view 4 with all 5 read-only screens, mirroring the mockup, proven backend-free.</done>
</task>

<task type="auto">
  <name>Task 3: Capture health (both media) + agent-ui-ux-designer parity critique → CRITIQUE.md 0 OPEN</name>
  <files>.planning/design/health/CRITIQUE.md, .planning/design/health/FIELDS.md</files>
  <read_first>
    - .planning/design/create-flow/CRITIQUE.md (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.6, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/health' ./internal/screenshot/...` (5 HTML + 5 TUI PNGs); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNav/health' ./e2e/...`). Spawn `agent-ui-ux-designer` for `.planning/design/health/CRITIQUE.md` (aesthetic pass HTML + §3 parity matrix), giving the SSH/Git two-section split and the read-only integrity (no write ceremony) explicit parity rows. Fill FIELDS.md parity columns; resolve to 0 OPEN. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/health' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNav/health' ./e2e/... && test $(ls .planning/design/health/html/*.png | wc -l) -eq 5 && test $(ls .planning/design/health/tui/*.png | wc -l) -eq 5 && ! grep -q 'OPEN' .planning/design/health/CRITIQUE.md</automated>
  </verify>
  <acceptance_criteria>
    - 5 HTML + 5 TUI PNGs: `test $(ls .planning/design/health/html/*.png | wc -l) -eq 5` AND `test $(ls .planning/design/health/tui/*.png | wc -l) -eq 5`
    - The surface-scoped dummy-nav e2e passes
    - `CRITIQUE.md` 0 OPEN with both passes; SSH/Git two-section + read-only integrity are explicit parity rows
    - FIELDS.md parity columns filled for all 5 screens
    - `make lint` passes
  </acceptance_criteria>
  <done>health captured in both media (5+5), navigable, parity resolved to 0 OPEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| health mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB8 | Tampering / Elevation of Privilege | health dummy reaching a backend write path | mitigate | `surface_health.go` imports no backend package; inherits the 02-02 import-graph gate + 02-03 zero-write e2e |
| T-02-RDONLY | Tampering | Health surface blurring "reported" with "fixed" (implying a mutation it does not perform) | mitigate | read-only integrity is a MUST-match parity row; no write ceremony on any health screen; the dummy test asserts no write-ceremony markers |
| T-02-DIAG | Information Disclosure | contradiction/redundancy findings not surfaced accurately | mitigate | concrete example findings (multiple Host *; IdentitiesOnly no + IdentityFile; includeIf → missing fragment) are captured states and parity rows (HLTH-03/04) |
</threat_model>

## Artifacts this phase produces

- `.planning/design/health/{FIELDS.md, manifest.json, CRITIQUE.md, html/*.png (5), tui/*.png (5)}`
- `.planning/design/mockup-src/src/routes/health/*.route.tsx` — 5 MUI v7 route files
- `internal/dummytui/surface_health.go` (+ `_test.go`) — TUI dummy view-4 surface, 5 read-only screens, backend-free

<verification>
- 5 HTML + 5 TUI PNGs; surface-scoped dummy-nav e2e passes; CRITIQUE.md 0 OPEN
- SSH/Git two-section split + read-only integrity — parity rows
- Dummy imports zero backend packages; no lorem
</verification>

<success_criteria>
- DLV-01/DLV-05 for health: HTML mockup + navigable backend-free TUI dummy, both captured, parity resolved.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-09-SUMMARY.md` when done.
</output>
