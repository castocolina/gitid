---
phase: 02-design-all-mockups-checkpoint-1
plan: 09
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/health/FIELDS.md
  - .planning/design/health/manifest.json
  - .planning/design/health/parity.json
  - .planning/design/health/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/health/health-all-green.route.tsx
  - .planning/design/mockup-src/src/routes/health/health-with-findings.route.tsx
  - .planning/design/mockup-src/src/routes/health/finding-detail.route.tsx
  - .planning/design/mockup-src/src/routes/health/per-identity-health.route.tsx
  - .planning/design/mockup-src/src/routes/health/parse-error.route.tsx
  - internal/dummytui/surface_health.go
  - internal/dummytui/surface_health_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The health screen is mocked and dummied across all 5 named states from UX-DIRECTION §4.6, with SSH and Git sections"
    - "Findings detect redundancy/contradiction (multiple Host *; IdentitiesOnly no with a specific IdentityFile; an includeIf targeting a missing fragment) and cover per-identity + global health (HLTH-01..06)"
    - "Health is visibly READ-ONLY: it diagnoses, never mutates — NO write ceremony (no confirm/backup/apply) appears on this surface at all (§4.6, §5), asserted by a negative check in both media"
    - "Both media captured (5 PNGs each) with the breadcrumb gate; dummy-nav e2e reaches every screen; parity.json has no unresolved row; the surface claims number key 4 via RegisterOrReplace"
  artifacts:
    - path: ".planning/design/health/manifest.json"
      provides: "hardened per-screen {surface, screen, htmlRoute, keysFromHome, signature} for 5 states"
      contains: "health-with-findings"
    - path: ".planning/design/health/parity.json"
      provides: "machine-checkable §3 rows + SSH/Git-two-section + read-only-integrity rows (gate: no row status != resolved)"
      contains: "status"
    - path: "internal/dummytui/surface_health.go"
      provides: "TUI dummy health surface (view 4, RegisterOrReplace key 4), read-only, backend-free"
      contains: "RegisterOrReplace"
  key_links:
    - from: "src/routes/health/health-with-findings.route.tsx"
      to: "SSH + Git two-section findings (severity-sorted)"
      via: "diagnose only, no write ceremony"
      pattern: "IdentitiesOnly"
    - from: "internal/dummytui/surface_health.go"
      to: "dummytui.RegisterOrReplace (number key 4)"
      via: "init()-registration replacing the placeholder, no data.go edit"
      pattern: "RegisterOrReplace"
---

<objective>
Fan-out surface: **Health check** (view `4`, owned later by Phase 8). Repeat the proven per-surface pipeline (02-04) for all 5 states from UX-DIRECTION §4.6.

States (verbatim §4.6): `health-all-green` · `health-with-findings` (severity-sorted, split SSH / Git) · `finding-detail` (e.g. multiple `Host *`; `IdentitiesOnly no` with a specific `IdentityFile`; an `includeIf` targeting a missing fragment) · `per-identity-health` (the slice that feeds a Manager row) · `parse-error` (a config file that won't parse).

Highest-risk affordance (§4.6): **read-only integrity** — Health must clearly diagnose, NOT mutate; it hands off to the Fixer. NO write ceremony (no confirm/backup/apply beat) appears on this surface at all.

Review folded in (Codex 02-REVIEWS.md): HIGH-2 number-key view (4) → `RegisterOrReplace` (no data.go edit); HIGH-4 structured `parity.json` gate; HIGH-3(c) hardened manifest schema; HIGH-1 the ALLOWLIST no-backend proof; LOW-11 tighten the read-only assertion with a NEGATIVE check (no write-ceremony markers on any health screen, both media); MEDIUM-10 fan-out isolation — writes ONLY its own surface files; serialize BOTH `pnpm build` AND the whole-module `make lint` if wave-4 runs concurrently (review M2 — both are whole-module with no per-surface isolation).

DLV-02: author the MUI mockup with `/mui`, the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/ssh-config.recipe
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: health FIELDS.md + manifest.json (hardened) + parity.json seed + MUI mockup (5 states)</name>
  <files>.planning/design/health/FIELDS.md, .planning/design/health/manifest.json, .planning/design/health/parity.json, .planning/design/mockup-src/src/routes/health/health-all-green.route.tsx, .planning/design/mockup-src/src/routes/health/health-with-findings.route.tsx, .planning/design/mockup-src/src/routes/health/finding-detail.route.tsx, .planning/design/mockup-src/src/routes/health/per-identity-health.route.tsx, .planning/design/mockup-src/src/routes/health/parse-error.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.6 (5 states + read-only integrity affordance), § 2 (master-detail; SSH+Git sections; color+glyph+word), § 5 (Health never writes)
    - .planning/design/create-flow/{FIELDS.md, manifest.json, parity.json} + a create-flow route (02-04 — proven format)
    - REQUIREMENTS.md HLTH-01..06, .planning/design/mockup-src/src/data/recipeFixtures.ts, .planning/design/PARITY.template.json
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` and the 02-04 format: author `FIELDS.md` (5-state table), `manifest.json` (5 hardened `{surface:"health", screen, htmlRoute, keysFromHome, signature}` entries — unique screen/htmlRoute/signature, absolute keysFromHome; route titles `health/<screen>`), and copy `PARITY.template.json` to `parity.json` seeding the §3 rows + a `ssh-git-two-section` row + a `read-only-integrity` row. Author 5 `*.route.tsx` under `src/routes/health/` inside `<Shell>` (master-detail) with SSH and Git SECTIONS: `health-all-green` (both healthy, green ✓ + word); `health-with-findings` (severity-sorted findings split SSH / Git); `finding-detail` showing concrete example findings (multiple `Host *`; `IdentitiesOnly no` with a specific `IdentityFile`; an `includeIf` targeting a missing fragment); `per-identity-health`; `parse-error`. CRITICAL: NO write ceremony anywhere on this surface — no confirm/backup/apply beat; Health only diagnoses and points to the Fixer. Real recipe copy, no lorem. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/health/manifest.json'))))") -eq 5 && python3 -c "import json;m=json.load(open('.planning/design/health/manifest.json'));assert len(set(e['screen'] for e in m))==5 and len(set(e['signature'] for e in m))==5 and all(e.get('keysFromHome') for e in m)"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 5 hardened-schema screens (unique screen+signature, non-empty keysFromHome) AND `grep -q 'health-with-findings' .planning/design/health/manifest.json`
    - `parity.json` seeded with §3 + two-section + read-only rows: `python3 -c "import json;r=json.load(open('.planning/design/health/parity.json'));assert len(r)>=9"`
    - 5 route files exist: `test $(ls .planning/design/mockup-src/src/routes/health/*.route.tsx | wc -l) -eq 5`
    - SSH+Git sections + contradiction examples present: `grep -rq 'IdentitiesOnly' .../health/` AND `grep -rq 'Host \*' .../health/`
    - Read-only integrity is EXPLICIT and enforced by a negative check (LOW-11): `grep -qi 'read-only' .planning/design/health/FIELDS.md` AND no write-ceremony route exists — `! ls .planning/design/mockup-src/src/routes/health/ | grep -qE 'confirm-write|backup-notice|result-applied'` AND `! grep -riqE 'confirm & write|apply fix|backup created at' .planning/design/mockup-src/src/routes/health/`
    - No lorem: `! grep -riq 'lorem' .../health/`; `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The health MUI mockup renders all 5 states with SSH/Git sections and concrete redundancy/contradiction findings, visibly read-only (no write ceremony, negatively asserted), with a seeded parity.json.</done>
</task>

<task type="auto">
  <name>Task 2: health TUI dummy surface (5 screens, RegisterOrReplace key 4, read-only, backend-free)</name>
  <files>internal/dummytui/surface_health.go, internal/dummytui/surface_health_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/registry.go (RegisterOrReplace), internal/dummytui/data.go (02-02 — the placeholder Health view on key 4 to REPLACE)
    - .planning/design/health/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 4 = key 4; breadcrumb), § 4.6 (read-only)
  </read_first>
  <action>
    Per DLV-02 (this is a UI task): this dummy screen reproduces the `/mui`-built mockup (the parity source) and `agent-ui-ux-designer` validates the terminal-skin cross-media parity (UX-DIRECTION §2). Write `surface_health.go` in `package dummytui` mirroring 02-04: register health as the primary view on number key `4` via `RegisterOrReplace` (review HIGH-2 — replaces the 02-02 placeholder, no data.go edit). Define 5 `ScreenDef`s matching `manifest.json`; render health-all-green, health-with-findings (SSH+Git sections, severity-sorted, ✓/!/✗ glyph+word), finding-detail (concrete contradiction examples), per-identity-health, parse-error — hardcoded fixture values. NO write-ceremony screens (this surface never mutates). NO backend import. Write `surface_health_test.go` asserting 5 registered screens, health sole owner of key 4, each RenderScreen non-empty + contains its signature + the `health/<id>` breadcrumb, connected Keys path, AND (LOW-11) that no health screen's Render contains a confirm/backup/apply write-ceremony marker (read-only integrity, negative assertion).
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run Health && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `surface_health.go` claims key 4 via RegisterOrReplace: `grep -q 'RegisterOrReplace' internal/dummytui/surface_health.go`; test asserts count 5 AND sole ownership of key 4
    - `go test -race ./internal/dummytui/... -run Health` passes, incl. the read-only NEGATIVE assertion (no write-ceremony markers on any health screen) + breadcrumb per screen
    - Copy anchors: `grep -q 'IdentitiesOnly' internal/dummytui/surface_health.go`
    - No-backend ALLOWLIST holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - No shared-file edit — RegisterOrReplace claims key 4 without touching data.go (review H1 — exit-code-honest, matching 02-11's idiom): `[ -z "$(git diff --name-only -- internal/dummytui/data.go internal/dummytui/model.go .planning/design/mockup-src/src/App.tsx .planning/design/mockup-src/package.json .planning/design/mockup-src/pnpm-lock.yaml Makefile)" ]` (FAILS if any shared file was modified); `make lint` passes
  </acceptance_criteria>
  <done>The health surface exists in the dummy as view 4 (sole owner of key 4 via RegisterOrReplace) with all 5 read-only screens (negatively asserted to have no write ceremony), mirroring the mockup, proven backend-free, no data.go edit.</done>
</task>

<task type="auto">
  <name>Task 3: Capture health (both media) + agent-ui-ux-designer critique → parity.json 0-unresolved</name>
  <files>.planning/design/health/CRITIQUE.md, .planning/design/health/FIELDS.md, .planning/design/health/parity.json</files>
  <read_first>
    - .planning/design/create-flow/{CRITIQUE.md, parity.json} (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.6, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/health' ./internal/screenshot/...` (5 HTML + 5 TUI PNGs, breadcrumb-gated); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/health' ./e2e/...`). Spawn `agent-ui-ux-designer`: record the HTML-aesthetic pass in `CRITIQUE.md` and fill `.planning/design/health/parity.json` — §3 rows + `ssh-git-two-section` + `read-only-integrity` — resolving every divergence so NO row has `status != resolved`. Fill FIELDS.md columns. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/health' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/health' ./e2e/... && test $(ls .planning/design/health/html/*.png | wc -l) -eq 5 && test $(ls .planning/design/health/tui/*.png | wc -l) -eq 5 && python3 -c "import json;r=json.load(open('.planning/design/health/parity.json'));import sys;sys.exit(1 if (not r or [x for x in r if x.get('status')!='resolved']) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - 5 HTML + 5 TUI PNGs: `test $(ls .planning/design/health/html/*.png | wc -l) -eq 5` AND `test $(ls .planning/design/health/tui/*.png | wc -l) -eq 5`
    - The surface-scoped dummy-nav e2e passes (breadcrumb+signature asserted, zero writes)
    - `parity.json` has no unresolved row and includes the two-section + read-only-integrity rows: `python3 -c "import json;r=json.load(open('.planning/design/health/parity.json'));assert r and all(x['status']=='resolved' for x in r)"`
    - FIELDS.md columns filled for all 5 screens; `make lint` passes
  </acceptance_criteria>
  <done>health captured in both media (5+5), navigable, parity resolved in a machine-checkable parity.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| health mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB8 | Tampering / Elevation of Privilege | health dummy reaching a backend write path | mitigate | `surface_health.go` imports no backend package; inherits the 02-02 import-graph ALLOWLIST + 02-03 zero-write e2e |
| T-02-RDONLY | Tampering | Health surface blurring "reported" with "fixed" | mitigate | read-only integrity is a parity.json row; NO write ceremony on any health screen; the dummy test + the route-file check assert no write-ceremony markers (LOW-11 negative check) |
| T-02-DIAG | Information Disclosure | contradiction/redundancy findings not surfaced accurately | mitigate | concrete example findings (multiple Host *; IdentitiesOnly no + IdentityFile; includeIf → missing fragment) are captured states + parity.json rows (HLTH-03/04) |
| T-02-RG8 | Tampering | fan-out registration failing on a duplicate activation key | mitigate | `RegisterOrReplace` cleanly replaces the key-4 placeholder; the test asserts sole ownership of key 4 |
</threat_model>

## Artifacts this phase produces

- `.planning/design/health/{FIELDS.md, manifest.json (hardened), parity.json, CRITIQUE.md, html/*.png (5), tui/*.png (5)}`
- `.planning/design/mockup-src/src/routes/health/*.route.tsx` — 5 MUI v7 route files (titles `health/<screen>`)
- `internal/dummytui/surface_health.go` (+ `_test.go`) — TUI dummy view-4 surface (RegisterOrReplace key 4), 5 read-only screens, backend-free

<verification>
- 5 HTML + 5 TUI PNGs; surface-scoped dummy-nav e2e passes; parity.json no unresolved row
- SSH/Git two-section split + read-only integrity (negatively asserted) — parity.json rows
- Dummy passes the no-backend allowlist; sole owner of key 4 via RegisterOrReplace; no data.go edit; no lorem
</verification>

<success_criteria>
- DLV-01/DLV-05 for health: HTML mockup + navigable backend-free TUI dummy, both captured, parity resolved in parity.json.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-09-SUMMARY.md` when done.
</output>
