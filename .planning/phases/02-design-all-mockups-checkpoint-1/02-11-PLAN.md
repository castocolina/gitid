---
phase: 02-design-all-mockups-checkpoint-1
plan: 11
type: execute
wave: 5
depends_on: [02-04, 02-05, 02-06, 02-07, 02-08, 02-09, 02-10]
files_modified:
  - .planning/design/APPROVAL.md
  - .planning/design/REFERENCE-INDEX.md
autonomous: true
requirements: [DLV-01, DLV-05, DLV-02]
user_setup: []
must_haves:
  truths:
    - "The comprehensive dummy-nav PTY e2e drives the REAL cmd/gitid-dummy binary across EVERY screen of all 7 surfaces and asserts zero files are written under a sandboxed HOME — and it passes BEFORE the final reference set is assembled and presented (the user's explicit e2e-before-presenting ordering)"
    - "The full dual capture is green: exactly 50 HTML PNGs + 50 TUI PNGs across the 7 surfaces (12+7+8+6+6+5+6), matching the per-surface manifests"
    - "The DLV-05 no-backend import-graph gate still holds across the whole dummy (all surface files), and every surface CRITIQUE.md has 0 OPEN findings"
    - "A REFERENCE-INDEX.md enumerates the complete reference set (every .planning/design/<surface>/{html,tui}/*.png + each surface FIELDS.md + resolved CRITIQUE.md) and an APPROVAL.md scaffold lists it, WITHOUT the **APPROVED:** line (that line is added only by the human checkpoint in 02-12)"
    - "agent-ui-ux-designer runs a final cross-surface consistency pass (one shell, one color semantics, one mutation ceremony) with 0 OPEN"
  artifacts:
    - path: ".planning/design/REFERENCE-INDEX.md"
      provides: "the complete frozen-reference index (all screenshots + FIELDS + CRITIQUE per surface)"
      contains: "html"
    - path: ".planning/design/APPROVAL.md"
      provides: "DLV-08 approval record scaffold (no APPROVED line yet)"
      contains: "reference set"
  key_links:
    - from: "make dummy-nav-e2e (full)"
      to: "cmd/gitid-dummy real binary, sandboxed HOME"
      via: "every screen reachable + zero writes, BEFORE presentation"
      pattern: "TestDummyNav"
    - from: ".planning/design/REFERENCE-INDEX.md"
      to: ".planning/design/<surface>/{html,tui}/*.png"
      via: "the frozen reference set every later phase regression-tests against"
      pattern: "png"
---

<objective>
Wave-5 assembly (autonomous, no checkpoint): prove the whole dummy is navigable on the REAL binary, capture the complete canonical reference set, verify all gates, and assemble the reference index — everything that must be TRUE before the single human approval (02-12). This plan runs the **comprehensive dummy-nav PTY e2e across every screen of all 7 surfaces FIRST** (the user's explicit "e2e before presenting" ordering), THEN produces the final full dual-capture reference set, THEN assembles `REFERENCE-INDEX.md` + the `APPROVAL.md` scaffold (without the `**APPROVED:**` line). It splits the implementation (here) from the human checkpoint (02-12) per GSD checkpoint hygiene.

Purpose: DLV-01 (complete screenshot set) + DLV-05 (dummy proven navigable + backend-free before any presentation) — the phase-level gates the approval is taken against.
Output: the comprehensive e2e + full capture pass, `.planning/design/REFERENCE-INDEX.md`, `.planning/design/APPROVAL.md` scaffold.

DLV-02: the final cross-surface consistency pass is run by `agent-ui-ux-designer` against the `/mui`-built mockups + the dummy screenshots; name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-VALIDATION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-10-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive dummy-nav PTY e2e (all screens, zero writes) BEFORE presenting + full gate check</name>
  <files>.planning/design/REFERENCE-INDEX.md</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-VALIDATION.md (Per-Task Verification Map: the DLV-05 full-nav + no-backend + DLV-02 0-OPEN commands; Sampling "Before /gsd-verify-work" gate)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md (the full make targets: dummy-nav-e2e, screenshot-*-mockups)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Pitfall 8" (capture vs PTY are distinct paths — the e2e proves nav, it does not produce the PNGs), § Pattern 2 (the no-backend go list -deps loop)
    - each surface manifest .planning/design/*/manifest.json (the authoritative per-surface screen counts: 12+7+8+6+6+5+6 = 50)
  </read_first>
  <action>
    FIRST (before any final capture/presentation) run the COMPREHENSIVE dummy-nav proof: `make dummy-nav-e2e` — the manifest-driven `TestDummyNav` walker now sees all 7 surfaces' manifests and must drive the REAL `cmd/gitid-dummy` binary to every screen and assert zero files written under the sandboxed HOME. Then re-assert the whole-dummy DLV-05 no-backend import-graph gate over all surface files (`go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/...` must contain none of internal/{identity,keygen,sshconfig,gitconfig,filewriter,tester,doctor,adopter,uploader,repoclone,upload,platform,clipboard,deps}). Then confirm every surface CRITIQUE.md has 0 OPEN (`! grep -rq 'OPEN' .planning/design/*/CRITIQUE.md`). Then run the full dual capture `make screenshot-html-mockups && make screenshot-tui-mockups` and verify the totals: exactly 50 PNGs under `.planning/design/*/html` and 50 under `.planning/design/*/tui`, matching the summed manifest counts. Write `.planning/design/REFERENCE-INDEX.md` enumerating, per surface, its html/ PNGs, tui/ PNGs, FIELDS.md, and CRITIQUE.md (the complete frozen reference set). Record the observed html/tui counts in the index.
  </action>
  <verify>
    <automated>make dummy-nav-e2e && ! (go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader|repoclone|upload|platform|clipboard|deps)') && ! grep -rq 'OPEN' .planning/design/*/CRITIQUE.md && make screenshot-html-mockups && make screenshot-tui-mockups && test $(find .planning/design/*/html -name '*.png' | wc -l) -eq 50 && test $(find .planning/design/*/tui -name '*.png' | wc -l) -eq 50</automated>
  </verify>
  <acceptance_criteria>
    - The comprehensive `make dummy-nav-e2e` passes (every screen of all 7 surfaces reachable on the REAL binary, zero writes under sandbox HOME) — run BEFORE the final capture in this task's ordering
    - Whole-dummy no-backend gate holds: `go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader|repoclone|upload|platform|clipboard|deps)'` returns NON-zero
    - Every CRITIQUE.md is 0 OPEN: `! grep -rq 'OPEN' .planning/design/*/CRITIQUE.md`
    - Full dual capture totals: `test $(find .planning/design/*/html -name '*.png' | wc -l) -eq 50` AND `test $(find .planning/design/*/tui -name '*.png' | wc -l) -eq 50`
    - `REFERENCE-INDEX.md` enumerates all 7 surfaces' html/tui PNGs + FIELDS.md + CRITIQUE.md
    - `make lint` passes
  </acceptance_criteria>
  <done>The whole dummy is proven navigable on the real binary with zero writes BEFORE presentation, the full 50+50 reference set is captured, all gates (no-backend, 0-OPEN critiques, counts) are green, and REFERENCE-INDEX.md enumerates the frozen set.</done>
</task>

<task type="auto">
  <name>Task 2: agent-ui-ux-designer final cross-surface consistency pass + APPROVAL.md scaffold</name>
  <files>.planning/design/APPROVAL.md</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 6 (the DLV-08 approval checklist — A Shell&IA, B per-surface completeness, C copy/fields/options/defaults FREEZE, D safety affordances, E parity&accessibility, F explicit acknowledgment), § 2 (one shell, one color semantics, one mutation ceremony)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Approval Mechanics (DLV-08)" (APPROVAL.md lists every screenshot path + resolved critiques + a closing APPROVED line — added by the human, not here)
    - .planning/design/REFERENCE-INDEX.md (Task 1 — the enumerated reference set)
  </read_first>
  <action>
    Spawn `agent-ui-ux-designer` for a FINAL cross-surface consistency pass over the complete `/mui` mockup + dummy screenshot set: confirm ONE shared shell (header/body/status/keybar) reads identically across all 7 surfaces in both media, ONE color-semantics table, ONE four-beat mutation ceremony on every mutating surface, reserved keys consistent, and that Health is visibly read-only + advisory surfaces visibly non-blocking. Record any consistency findings and RESOLVE them to 0 OPEN (updating the offending surface's CRITIQUE.md if needed). Then write `.planning/design/APPROVAL.md` as the DLV-08 record SCAFFOLD following UX-DIRECTION §6: the full §6 approval checklist (A–F) as unchecked items, a link to `REFERENCE-INDEX.md` and each surface's FIELDS.md/CRITIQUE.md, and an explicit note that no backend logic is written for any surface until a `**APPROVED:** <date> by <user>` line is added by the human checkpoint (02-12) — DO NOT add that line here. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>test -f .planning/design/APPROVAL.md && grep -q 'reference set' .planning/design/APPROVAL.md && ! grep -q 'APPROVED:' .planning/design/APPROVAL.md && ! grep -rq 'OPEN' .planning/design/*/CRITIQUE.md</automated>
  </verify>
  <acceptance_criteria>
    - `agent-ui-ux-designer` final consistency pass is recorded and resolved: `! grep -rq 'OPEN' .planning/design/*/CRITIQUE.md` still holds after the pass
    - `APPROVAL.md` exists, contains the §6 A–F approval checklist and links the REFERENCE-INDEX + per-surface FIELDS/CRITIQUE, and states the no-backend-before-approval rule: `grep -q 'reference set' .planning/design/APPROVAL.md`
    - The `**APPROVED:**` line is ABSENT (it is the human's to add in 02-12): `! grep -q 'APPROVED:' .planning/design/APPROVAL.md`
    - `make lint` passes
  </acceptance_criteria>
  <done>A final agent-ui-ux-designer cross-surface consistency pass confirms one coherent product across all surfaces, and APPROVAL.md is scaffolded with the §6 checklist and the frozen reference index — ready for the single human approval, with the APPROVED line deliberately withheld.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whole dummy ← sandboxed HOME | the comprehensive e2e runs the real binary under an isolated HOME and asserts zero writes |
| reference set ← design artifacts | the assembled reference set is design-review data; loaded/captured from local file:// only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB-ALL | Tampering / Elevation of Privilege | any surface file having slipped a backend import into the dummy | mitigate | Whole-dummy `go list -deps` gate over all surface files; the comprehensive e2e asserts zero writes under sandbox HOME |
| T-02-PRESENT | Repudiation | presenting an unproven (non-navigable) design for approval | mitigate | The comprehensive dummy-nav e2e MUST pass BEFORE the final capture/assembly in this plan's ordering — the user's explicit e2e-before-presenting requirement |
| T-02-FREEZE | Repudiation | approving an incomplete set (missing screens) | mitigate | The 50+50 count check against summed manifest counts gates assembly; REFERENCE-INDEX enumerates the complete set |
</threat_model>

## Artifacts this phase produces

- `.planning/design/REFERENCE-INDEX.md` — the complete frozen-reference index (per surface: html/ PNGs, tui/ PNGs, FIELDS.md, CRITIQUE.md; observed 50+50 counts)
- `.planning/design/APPROVAL.md` — DLV-08 approval record scaffold (§6 A–F checklist + reference links + no-backend-before-approval rule; **APPROVED:** line deliberately absent)

<verification>
- `make dummy-nav-e2e` (comprehensive) passes BEFORE final capture; zero writes under sandbox HOME
- 50 HTML + 50 TUI PNGs; whole-dummy no-backend gate holds; every CRITIQUE.md 0 OPEN
- agent-ui-ux-designer final cross-surface consistency pass resolved; APPROVAL.md scaffolded without the APPROVED line
</verification>

<success_criteria>
- DLV-01: the complete screenshot reference set (50+50) exists and is indexed.
- DLV-05: the dummy is proven navigable on the real binary with zero writes and zero backend imports, BEFORE presentation.
- DLV-02: the final consistency pass is run by agent-ui-ux-designer over the /mui + dummy set; both named.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-11-SUMMARY.md` when done.
</output>
