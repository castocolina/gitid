---
phase: 02-design-all-mockups-checkpoint-1
plan: 11
type: execute
wave: 5
depends_on: [02-04, 02-05, 02-06, 02-07, 02-08, 02-09, 02-10]
files_modified:
  - internal/dummytui/keyowners_test.go
  - .planning/design/APPROVAL.md
  - .planning/design/REFERENCE-INDEX.md
autonomous: true
requirements: [DLV-01, DLV-05, DLV-02]
user_setup: []
must_haves:
  truths:
    - "The comprehensive dummy-nav PTY e2e drives the REAL cmd/gitid-dummy binary across EVERY screen of all 7 surfaces (re-homing per entry, asserting the breadcrumb screen-ID + signature) — including the ~19 KEYLESS modal screens reached through the 02-02 launch mechanism (launch key in keysFromHome) — and asserts zero writes under a sandboxed HOME, passing BEFORE the final reference set is assembled/presented"
    - "The full dual capture is green with counts COMPUTED from the per-surface manifests (not hard-coded): #HTML == #TUI == sum(len(manifest.json)) across the 7 surfaces, AND the 7 required surface dirs each exist"
    - "The DLV-05 no-backend ALLOWLIST holds across the whole dummy (fail on any first-party package != dummytui/gitid-dummy), the final owners of number keys 1–5 are exactly identity-manager/global-ssh/global-git/health/fixer, create-flow/git-screen are keyless modal surfaces that each declare a LaunchFrom/LaunchKey launch binding, and every surface parity.json has no row status != resolved"
    - "A NEW positive-space gate asserts Phase 2 changed ONLY .planning/design, internal/dummytui, cmd/gitid-dummy, internal/screenshot, e2e, and the Makefile — no backend files were touched before approval — using a concretely-bound base ref BASE=$(git merge-base main HEAD)"
    - "A REFERENCE-INDEX.md enumerates the complete reference set and an APPROVAL.md scaffold lists it WITHOUT the **APPROVED:** line (added only by the human checkpoint in 02-12)"
  artifacts:
    - path: "internal/dummytui/keyowners_test.go"
      provides: "final-owners assertion: keys 1–5 owned by identity-manager/global-ssh/global-git/health/fixer; create-flow/git-screen are keyless modal surfaces with a LaunchFrom/LaunchKey binding (no activation key)"
      contains: "identity-manager"
    - path: ".planning/design/REFERENCE-INDEX.md"
      provides: "the complete frozen-reference index (all screenshots + FIELDS + parity per surface; computed counts)"
      contains: "html"
    - path: ".planning/design/APPROVAL.md"
      provides: "DLV-08 approval record scaffold (no APPROVED line yet)"
      contains: "reference set"
  key_links:
    - from: "make dummy-nav-e2e (full)"
      to: "cmd/gitid-dummy real binary, sandboxed HOME"
      via: "every screen reachable (breadcrumb+signature; modal screens via the launch mechanism) + zero writes, BEFORE presentation"
      pattern: "TestDummyNav"
    - from: ".planning/design/REFERENCE-INDEX.md"
      to: ".planning/design/<surface>/{html,tui}/*.png"
      via: "the frozen reference set every later phase regression-tests against"
      pattern: "png"
---

<objective>
Wave-5 assembly (autonomous, no checkpoint): prove the whole dummy is navigable on the REAL binary, capture the complete canonical reference set, verify all gates, and assemble the reference index — everything that must be TRUE before the single human approval (02-12). Runs the **comprehensive dummy-nav PTY e2e across every screen of all 7 surfaces FIRST** (the user's explicit "e2e before presenting" ordering) — including the ~19 keyless modal screens reached through the 02-02 launch mechanism — THEN the final full dual-capture, THEN assembles `REFERENCE-INDEX.md` + the `APPROVAL.md` scaffold (without the `**APPROVED:**` line). Splits the implementation (here) from the human checkpoint (02-12) per GSD checkpoint hygiene.

Review folded in (Codex 02-REVIEWS.md): HIGH-1 the whole-dummy no-backend gate is the ALLOWLIST; HIGH-2 add the final-key-owners test (keys 1–5 owned by exactly the five real surfaces); HIGH-4 the whole-set parity gate reads `parity.json` (no row status != resolved), not a Markdown `grep OPEN`; MEDIUM-8 COMPUTE the expected screenshot counts from the manifests and SEPARATELY assert the 7 required surfaces exist (do not hard-code 50); NEW GATE (Codex) assert Phase 2 touched only the allowed directories — no backend files before approval. Review-spec iteration 2 folded in: C3 — the comprehensive e2e reaches the ~19 keyless modal screens through the launch mechanism (launch key in keysFromHome), and keyowners_test asserts create-flow/git-screen are keyless with a LaunchFrom/LaunchKey binding; H3 — the no-backend-files gate binds `$BASE` concretely (`BASE=$(git merge-base main HEAD)`) so the gate runs instead of erroring on an undefined variable.

Purpose: DLV-01 (complete screenshot set) + DLV-05 (dummy proven navigable + backend-free before any presentation) — the phase-level gates the approval is taken against.
Output: `internal/dummytui/keyowners_test.go`, the comprehensive e2e + full capture pass, `.planning/design/REFERENCE-INDEX.md`, `.planning/design/APPROVAL.md` scaffold.

DLV-02: the final cross-surface consistency pass is run by `agent-ui-ux-designer` against the `/mui`-built mockups + the dummy screenshots; name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-VALIDATION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-10-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive dummy-nav PTY e2e + final-key-owners test + all gates (allowlist, parity.json, computed counts, no-backend-files with a bound $BASE) BEFORE presenting</name>
  <files>internal/dummytui/keyowners_test.go, .planning/design/REFERENCE-INDEX.md</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-VALIDATION.md (Per-Task Verification Map: the DLV-05 full-nav + no-backend + DLV-02 parity commands; Sampling "Before /gsd-verify-work" gate)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md (the full make targets: dummy-nav-e2e, screenshot-*-mockups; the breadcrumb-asserting capture + re-homing e2e that reaches keyless modal surfaces via the launch key)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-02-SUMMARY.md (Surfaces() + activation-key ownership contract + the LaunchFrom/LaunchKey launch bindings for the final-owners test)
    - each surface manifest .planning/design/*/manifest.json (the authoritative per-surface screen counts — computed, not hard-coded)
  </read_first>
  <action>
    Write `internal/dummytui/keyowners_test.go` (plain package-dummytui test — sees every surface's init() registration; review HIGH-2): assert `Surfaces()` maps number keys "1".."5" to surface IDs EXACTLY identity-manager/global-ssh/global-git/health/fixer, and that create-flow + git-screen are registered as KEYLESS modal flows with NO activation key AND each declaring a LaunchFrom/LaunchKey launch binding (review C3 — so no view competes for a number key and both modal surfaces are launchable from Identities). Then, in ordering: FIRST run the COMPREHENSIVE dummy-nav proof `make dummy-nav-e2e` — the manifest-driven `TestDummyNav` walker now sees all 7 surfaces' manifests and must drive the REAL `cmd/gitid-dummy` binary to every screen (re-homing per entry, asserting the `<surface>/<screen>` breadcrumb + the screen-specific signature), reaching the ~19 keyless modal screens through the 02-02 launch mechanism (the launch key embedded in keysFromHome, not a direct RenderScreen call), and assert zero files written under the sandboxed HOME. Then re-assert the whole-dummy DLV-05 no-backend ALLOWLIST (review HIGH-1): `go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/...` filtered to `github.com/castocolina/gitid/*` must contain NOTHING other than internal/dummytui + cmd/gitid-dummy. Then confirm every surface `parity.json` has no unresolved row (review HIGH-4; whole-set). Then run the full dual capture `make screenshot-html-mockups && make screenshot-tui-mockups` and verify counts COMPUTED from the manifests (review MEDIUM-8): `expected = sum(len(json.load(open(p))) for p in glob('.planning/design/*/manifest.json'))`; assert `#png under */html == expected` AND `#png under */tui == expected`; AND separately assert the 7 required surface dirs each exist (create-flow, git-screen, identity-manager, global-ssh, global-git, health, fixer). Then run the NEW positive-space gate (Codex; review H3): bind the Phase-2 base ref concretely as `BASE=$(git merge-base main HEAD)` (the fork point of the phase branch from `main`), and assert the set of files changed since then — `git diff --name-only "$BASE"..HEAD` — all fall under `.planning/design/`, `internal/dummytui/`, `cmd/gitid-dummy/`, `internal/screenshot/`, `e2e/`, or `Makefile`; no backend file changed before approval. Write `.planning/design/REFERENCE-INDEX.md` enumerating, per surface, its html/ PNGs, tui/ PNGs, FIELDS.md, parity.json, and CRITIQUE.md, and record the COMPUTED counts.
  </action>
  <verify>
    <automated>go test -race ./internal/dummytui/... -run KeyOwners && make dummy-nav-e2e && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ] && python3 -c "import json,glob,sys;rows=[r for p in glob.glob('.planning/design/*/parity.json') for r in json.load(open(p))];sys.exit(1 if (not rows or [x for x in rows if x.get('status')!='resolved']) else 0)" && make screenshot-html-mockups && make screenshot-tui-mockups && python3 -c "import json,glob;exp=sum(len(json.load(open(p))) for p in glob.glob('.planning/design/*/manifest.json'));import subprocess as s;h=int(s.check_output('find .planning/design/*/html -name \'*.png\' | wc -l',shell=True));t=int(s.check_output('find .planning/design/*/tui -name \'*.png\' | wc -l',shell=True));assert h==exp and t==exp, (h,t,exp)" && BASE=$(git merge-base main HEAD) && [ -z "$(git diff --name-only "$BASE"..HEAD | grep -vE '^(\.planning/design/|internal/dummytui/|cmd/gitid-dummy/|internal/screenshot/|e2e/|Makefile$)')" ]</automated>
  </verify>
  <acceptance_criteria>
    - The final-owners test passes: `go test -race ./internal/dummytui/... -run KeyOwners` asserts keys 1–5 owned by exactly identity-manager/global-ssh/global-git/health/fixer AND create-flow/git-screen are keyless with a LaunchFrom/LaunchKey binding: `grep -q 'identity-manager' internal/dummytui/keyowners_test.go` AND `grep -q 'fixer' internal/dummytui/keyowners_test.go` AND `grep -q 'LaunchFrom' internal/dummytui/keyowners_test.go`
    - The comprehensive `make dummy-nav-e2e` passes (every screen of all 7 surfaces reachable on the REAL binary with re-homing + breadcrumb + signature, the keyless modal screens reached via the launch mechanism, zero writes) — run BEFORE the final capture
    - Whole-dummy no-backend ALLOWLIST holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - Every surface parity.json has no unresolved row (whole-set): `python3 -c "import json,glob;rows=[r for p in glob.glob('.planning/design/*/parity.json') for r in json.load(open(p))];assert rows and all(x['status']=='resolved' for x in rows)"`
    - Screenshot counts are COMPUTED from manifests (not hard-coded) and the 7 surfaces exist: the verify python asserts `#html==#tui==sum(manifest lengths)` AND `test $(ls -d .planning/design/{create-flow,git-screen,identity-manager,global-ssh,global-git,health,fixer}/manifest.json | wc -l) -eq 7`
    - The no-backend-files gate holds with a CONCRETELY-BOUND base ref (review H3): with `BASE=$(git merge-base main HEAD)` (the Phase-2 base commit — the fork point of the phase branch from `main`), `[ -z "$(git diff --name-only "$BASE"..HEAD | grep -vE '^(\.planning/design/|internal/dummytui/|cmd/gitid-dummy/|internal/screenshot/|e2e/|Makefile$)')" ]`
    - `REFERENCE-INDEX.md` enumerates all 7 surfaces' html/tui PNGs + FIELDS.md + parity.json + CRITIQUE.md with the computed counts
    - `make lint` passes
  </acceptance_criteria>
  <done>The whole dummy is proven navigable on the real binary (re-homing, screen-ID asserted, keyless modal screens reached via the launch mechanism, zero writes) BEFORE presentation; the full reference set is captured with counts computed from the manifests; all gates (allowlist, final key owners + launch bindings, whole-set parity.json, computed counts, no-backend-files with a bound $BASE) are green; REFERENCE-INDEX.md enumerates the frozen set.</done>
</task>

<task type="auto">
  <name>Task 2: agent-ui-ux-designer final cross-surface consistency pass + APPROVAL.md scaffold</name>
  <files>.planning/design/APPROVAL.md</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 6 (the DLV-08 approval checklist A–F), § 2 (one shell, one color semantics, one mutation ceremony)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Approval Mechanics (DLV-08)" (APPROVAL.md lists every screenshot path + resolved parity + a closing APPROVED line — added by the human, not here)
    - .planning/design/REFERENCE-INDEX.md (Task 1 — the enumerated reference set + computed counts)
  </read_first>
  <action>
    Spawn `agent-ui-ux-designer` for a FINAL cross-surface consistency pass over the complete `/mui` mockup + dummy screenshot set: confirm ONE shared shell (header incl. the `<surface>/<screen>` breadcrumb / body / status / keybar) reads identically across all 7 surfaces in both media, ONE color-semantics table, ONE four-beat mutation ceremony on every mutating surface, reserved keys consistent, Health visibly read-only + advisory surfaces visibly non-blocking. Record any consistency findings as parity.json rows and RESOLVE them to `status: resolved` (updating the offending surface's parity.json/CRITIQUE.md). Then write `.planning/design/APPROVAL.md` as the DLV-08 record SCAFFOLD following UX-DIRECTION §6: the full §6 approval checklist (A–F) as unchecked items, a link to `REFERENCE-INDEX.md` and each surface's FIELDS.md/parity.json/CRITIQUE.md, and an explicit note that no backend logic is written for any surface until a `**APPROVED:** <date> by <user-supplied approver>` line is added by the human checkpoint (02-12) — DO NOT add that line here, and DO NOT infer the approver name (02-12 requires a user-supplied string). Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>test -f .planning/design/APPROVAL.md && grep -q 'reference set' .planning/design/APPROVAL.md && ! grep -q 'APPROVED:' .planning/design/APPROVAL.md && python3 -c "import json,glob;rows=[r for p in glob.glob('.planning/design/*/parity.json') for r in json.load(open(p))];assert rows and all(x['status']=='resolved' for x in rows)"</automated>
  </verify>
  <acceptance_criteria>
    - `agent-ui-ux-designer` final consistency pass is recorded and resolved: every parity.json row is `resolved` after the pass (whole-set python assertion holds)
    - `APPROVAL.md` exists, contains the §6 A–F checklist, links the REFERENCE-INDEX + per-surface FIELDS/parity/CRITIQUE, and states the no-backend-before-approval rule + the user-supplied-approver rule: `grep -q 'reference set' .planning/design/APPROVAL.md`
    - The `**APPROVED:**` line is ABSENT (the human's to add in 02-12): `! grep -q 'APPROVED:' .planning/design/APPROVAL.md`
    - `make lint` passes
  </acceptance_criteria>
  <done>A final agent-ui-ux-designer cross-surface consistency pass confirms one coherent product across all surfaces (findings resolved as parity.json rows), and APPROVAL.md is scaffolded with the §6 checklist + the frozen reference index — ready for the single human approval, with the APPROVED line deliberately withheld and the approver name not inferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whole dummy ← sandboxed HOME | the comprehensive e2e runs the real binary under an isolated HOME and asserts zero writes |
| reference set ← design artifacts | the assembled reference set is design-review data; loaded/captured from local file:// only |
| Phase-2 change-set ← git working tree | the no-backend-files gate asserts Phase 2 touched only the allowed directories (base ref bound as git merge-base main HEAD) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB-ALL | Tampering / Elevation of Privilege | any surface file having slipped a NEW/renamed backend import into the dummy | mitigate | Whole-dummy `go list -deps` ALLOWLIST over all surface files (review HIGH-1); the comprehensive e2e asserts zero writes under sandbox HOME |
| T-02-KEYS | Tampering | a fan-out surface silently not owning its number key, or a modal surface silently unlaunchable (broken nav in the frozen reference) | mitigate | `keyowners_test.go` asserts keys 1–5 owned by exactly the five real surfaces (review HIGH-2) AND create-flow/git-screen keyless with a LaunchFrom/LaunchKey binding (review C3) |
| T-02-PRESENT | Repudiation | presenting an unproven (non-navigable) design for approval | mitigate | The comprehensive dummy-nav e2e MUST pass BEFORE the final capture/assembly (re-homing + breadcrumb + signature; keyless modal screens reached via the launch mechanism) — the user's explicit e2e-before-presenting requirement |
| T-02-FREEZE | Repudiation | approving an incomplete set (missing screens) | mitigate | Counts COMPUTED from summed manifest lengths (review MEDIUM-8) gate assembly + the 7 required surfaces asserted separately; REFERENCE-INDEX enumerates the complete set |
| T-02-BEGATE | Tampering | backend files changed before approval, OR the gate silently no-op-ing on an undefined base ref | mitigate | NEW positive-space gate with a CONCRETELY-BOUND base (`BASE=$(git merge-base main HEAD)`, review H3): git diff against that base must fall only under .planning/design, internal/dummytui, cmd/gitid-dummy, internal/screenshot, e2e, Makefile |
</threat_model>

## Artifacts this phase produces

- `internal/dummytui/keyowners_test.go` — final-owners assertion (keys 1–5 → identity-manager/global-ssh/global-git/health/fixer; create-flow/git-screen keyless modal with a LaunchFrom/LaunchKey binding)
- `.planning/design/REFERENCE-INDEX.md` — the complete frozen-reference index (per surface: html/ PNGs, tui/ PNGs, FIELDS.md, parity.json, CRITIQUE.md; COMPUTED counts)
- `.planning/design/APPROVAL.md` — DLV-08 approval record scaffold (§6 A–F checklist + reference links + no-backend-before-approval + user-supplied-approver rules; **APPROVED:** line deliberately absent)

<verification>
- `make dummy-nav-e2e` (comprehensive) passes BEFORE final capture; re-homing + breadcrumb + signature; keyless modal screens reached via the launch mechanism; zero writes under sandbox HOME
- Screenshot counts COMPUTED from manifests (#html==#tui==sum(manifests)); 7 required surfaces exist; whole-dummy no-backend ALLOWLIST holds; final key owners correct + modal launch bindings present; every parity.json 0-unresolved
- no-backend-files gate holds with a bound `$BASE`=`git merge-base main HEAD` (Phase 2 touched only allowed dirs); APPROVAL.md scaffolded without the APPROVED line
</verification>

<success_criteria>
- DLV-01: the complete screenshot reference set exists (counts computed from manifests) and is indexed.
- DLV-05: the dummy is proven navigable on the real binary with zero writes and the no-backend ALLOWLIST — including the keyless modal screens reached via the launch mechanism — final key owners + launch bindings correct, BEFORE presentation.
- DLV-02: the final consistency pass is run by agent-ui-ux-designer over the /mui + dummy set; both named.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-11-SUMMARY.md` when done.
</output>
</content>
