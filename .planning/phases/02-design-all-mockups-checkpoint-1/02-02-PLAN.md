---
phase: 02-design-all-mockups-checkpoint-1
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/dummytui/doc.go
  - internal/dummytui/registry.go
  - internal/dummytui/registry_test.go
  - internal/dummytui/model.go
  - internal/dummytui/model_test.go
  - internal/dummytui/shell.go
  - internal/dummytui/overlay.go
  - internal/dummytui/data.go
  - internal/dummytui/nobackend_test.go
  - cmd/gitid-dummy/main.go
autonomous: true
requirements: [DLV-05, DLV-02]
user_setup: []
must_haves:
  truths:
    - "A separate binary cmd/gitid-dummy launches a Bubble Tea v2 nav-only TUI that shares the same four-region shell (header/body/status/keybar) as the MUI mockup; the header renders the active surface/screen as a `<surface>/<screen>` breadcrumb screen-ID marker"
    - "The five primary views (1 Identities, 2 Global SSH, 3 Global Git, 4 Health, 5 Fixer) route on number keys; KEYLESS modal surfaces (create-flow, git-screen) carry NO activation key and are reachable ONLY via a TARGET-OWNED launch binding (SurfaceDef.LaunchFrom + LaunchKey) — reserved keys Esc/q/?/Enter behave identically everywhere"
    - "route() implements the modal-launch mechanism: while its source surface is active and no modal is open, a key equal to a registered keyless surface's LaunchKey whose LaunchFrom == the active surface ID PUSHES that keyless surface as a modal frame onto navState.modalStack (at the modal's entry screen); Esc POPS the top frame back to the parent; number keys 1–5 are ignored while a modal is active"
    - "The model View() composites the launched modal surface's active screen over the DIMMED parent via the backend-free placeOverlay, and its header breadcrumb reflects the ACTIVE MODAL screen `<surface>/<screen>` (the topmost modalStack frame) — so every keyless modal screen is reachable AND screen-ID-assertable on the real binary"
    - "internal/dummytui and cmd/gitid-dummy import ONLY an ALLOWLISTED graph — proven by an ALLOWLIST assertion on go list -deps that FAILS on ANY github.com/castocolina/gitid/* package other than internal/dummytui and cmd/gitid-dummy (strictly stronger than a denylist; DLV-05)"
    - "Surfaces register via the registry with explicit replacement semantics — Register rejects a duplicate NON-EMPTY activation key (an EMPTY/unset ActivationKey is EXEMPT, so keyless modal surfaces both register without collision), RegisterOrReplace claims/replaces a key — so fan-out surfaces add one file and never edit model.go/data.go; the five placeholder surfaces use the FINAL surface IDs (identity-manager/global-ssh/global-git/health/fixer) so launch bindings resolve both before and after replacement; RenderScreen(surface,screen) yields a deterministic FULL-SHELL View() string (incl. the breadcrumb screen-ID) for capture"
    - "A modal-overlay compositing helper exists inside the dummy (reimplemented, not imported from tui/) because lipgloss v2.0.3 has no PlaceOverlay"
    - "route() has an EXPLICIT key precedence (intra-surface ScreenDef.Keys then keyless LaunchKey then number-key view-switch) AND Register/RegisterOrReplace enforce a LAUNCHKEY COLLISION GUARD that rejects (a test-detectable error) a (LaunchFrom, LaunchKey) whose key is already claimed by ANY ScreenDef.Keys transition of the LaunchFrom surface, by another keyless LaunchKey with the same LaunchFrom, by a number key 1-5, or by a reserved key -- so a create-flow/git-screen launch key vs identity-manager ScreenDef.Keys clash fails at registration, never as a silent 02-11 e2e failure; doc.go mirrors the UX-DIRECTION section 2 key-allocation table as the single authority"
  artifacts:
    - path: "internal/dummytui/registry.go"
      provides: "Register + RegisterOrReplace + RenderScreen(surface,screen) + key→screen routing contract + SurfaceDef.LaunchFrom/LaunchKey target-owned modal-launch bindings + route() launch/pop resolution over navState.modalStack + explicit route() key precedence (ScreenDef.Keys then LaunchKey then number-key) + a LaunchKey collision guard in Register/RegisterOrReplace (review iter-3)"
      contains: "func RegisterOrReplace"
    - path: "internal/dummytui/doc.go"
      provides: "package doc: DLV-05 no-backend ALLOWLIST rule + modal-launch contract + the key-allocation table mirroring UX-DIRECTION section 2 (number keys 1-5; LaunchKeys n=create-flow, g=git-screen; identity-manager intra-surface a/c/d; reserved keys) as the single authority fan-out surfaces allocate keys against"
      contains: "key-allocation"
    - path: "internal/dummytui/model.go"
      provides: "tea.Model: number-key view routing + modal-stack dispatch (launch key opens a keyless modal via placeOverlay, Esc pops) + region layout + header breadcrumb reflects the active modal screen"
      contains: "tea.Model"
    - path: "internal/dummytui/overlay.go"
      provides: "backend-free placeOverlay line-compositing (lipgloss v2 has no PlaceOverlay)"
      contains: "func placeOverlay"
    - path: "cmd/gitid-dummy/main.go"
      provides: "nav-only dummy binary entry point (no backend deps)"
      contains: "dummytui"
    - path: "internal/dummytui/nobackend_test.go"
      provides: "go list -deps ALLOWLIST assertion — only dummytui/gitid-dummy among first-party packages"
      contains: "go list"
  key_links:
    - from: "cmd/gitid-dummy/main.go"
      to: "internal/dummytui NewModel()"
      via: "tea.NewProgram(dummytui.NewModel()).Run() — zero deps injected"
      pattern: "dummytui.NewModel"
    - from: "internal/dummytui/registry.go route()"
      to: "keyless modal surfaces (create-flow, git-screen)"
      via: "SurfaceDef.LaunchFrom/LaunchKey — a launch key on the active source surface pushes the keyless surface as a modal frame; Esc pops"
      pattern: "LaunchFrom"
    - from: "internal/dummytui/*"
      to: "allowlisted import graph only"
      via: "DLV-05 import-graph ALLOWLIST (fail on any first-party pkg != dummytui/gitid-dummy)"
      pattern: "go list -deps"
---

<objective>
Build the navigable Go TUI **dummy** skeleton (DLV-05 front half): `internal/dummytui` + a physically separate `cmd/gitid-dummy` binary that renders the SAME four-region shell as the MUI mockup and routes the five primary views on number keys `1`–`5` plus a **modal-launch mechanism** for the KEYLESS modal surfaces (create-flow, git-screen) — with ZERO backend logic, enforced as an **allowlist** import-graph property (`go list -deps`), not a denylist and not a promise. Lands the contracts surfaces plug into: a registry with `Register` (rejects duplicate activation keys) and `RegisterOrReplace` (claims/replaces a key so fan-out surfaces replace placeholders without a conflict, never editing model.go/data.go); a TARGET-OWNED launch binding (`SurfaceDef.LaunchFrom` + `LaunchKey`) so a keyless modal surface declares — in its own file — which source surface + key launches it (`route()` pushes it as a modal frame on `navState.modalStack`, Esc pops); a deterministic full-shell `RenderScreen` (incl. the `<surface>/<screen>` breadcrumb screen-ID) for capture; and a backend-free modal-overlay helper (lipgloss v2.0.3 has no `PlaceOverlay`). Ships only placeholder screens for the five views, using the FINAL surface IDs so launch bindings resolve pre- and post-replacement.

Purpose: DLV-05 fixes the per-surface order HTML→dummy→approval→backend and mandates no backend logic. The separate binary + import ALLOWLIST make "no backend" an enforced CI signal that catches NEW/renamed backend packages (a denylist cannot); the replacement semantics keep the Wave-4 fan-out conflict-free; the target-owned launch binding makes the keyless modal surfaces (~19 screens across create-flow/git-screen) reachable on the REAL binary without any fan-out plan editing the source surface, model.go, or data.go.
Output: `internal/dummytui/{doc,registry,model,shell,overlay,data}.go` + tests, `cmd/gitid-dummy/main.go`, the `nobackend` allowlist test.

Review folded in (Codex 02-REVIEWS.md): HIGH-1 flip no-backend from denylist → ALLOWLIST (fail on any first-party pkg != dummytui/gitid-dummy); HIGH-2 add `RegisterOrReplace` so fan-out 06–10 don't collide on keys 1–5; HIGH-3(b) RenderScreen renders the full shell incl. the `<surface>/<screen>` breadcrumb so tests assert the SCREEN, not just a signature. Second-layer review (review-spec) folded in: C2 — Task 1 ships a self-contained MINIMAL inline `RenderScreen` (breadcrumb + body + status + keybar) so `internal/dummytui` compiles and Task 1's tests pass STANDALONE, and Task 2 swaps in shell.go's full four-region composition + owns the full-shell assertion (each task independently buildable per CLAUDE.md's buildable-boundary rule); H2 — `Register` EXEMPTS an EMPTY/unset `ActivationKey` from the duplicate-key check so keyless modal surfaces (create-flow, git-screen) both register without collision. Review-spec iteration 2 folded in: C3 — the modal-launch keybinding is now a CONCRETE mechanism (target-owned `SurfaceDef.LaunchFrom`/`LaunchKey` → `route()` pushes a modal frame on `navState.modalStack`, `View()` composites the modal over the dimmed parent via `placeOverlay` with the header breadcrumb reflecting the active modal screen, Esc pops), keyless surfaces are reachable ONLY via this mechanism, and placeholders use the final surface IDs so launch bindings resolve before and after RegisterOrReplace. Review-spec iteration 3 folded in: HIGH — a keyless surface's LaunchKey could clash with its LaunchFrom surface's own intra-surface ScreenDef.Keys (both are picked by independent fan-out plans), a clash that would only surface as a confusing 02-11 e2e failure; the fix makes route() PRECEDENCE explicit (intra-surface ScreenDef.Keys resolve BEFORE launch keys) AND adds a REGISTRATION-TIME collision guard so Register/RegisterOrReplace reject (a test) any (LaunchFrom, LaunchKey) whose key is already claimed by ANY ScreenDef.Keys of the LaunchFrom surface (or by another keyless LaunchKey with the same LaunchFrom, a number key 1-5, or a reserved key), with doc.go mirroring the UX-DIRECTION §2 key-allocation table as the single authority the fan-out plans allocate against.

TDD: the nav state machine (screen enum + key routing; number-key → view; modal launch/pop over a modalStack; register vs register-or-replace) is real logic with defined I/O — Tasks 1–2 are RED→GREEN→REFACTOR. Shell rendering + overlay port are execute-style (static View()).

DLV-02: the dummy shell/screens/breadcrumb mirror the `/mui`-built HTML mockup (the parity source); `agent-ui-ux-designer` validates the terminal-skin cross-media shell parity (UX-DIRECTION §2). Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md
@CLAUDE.md
@tui/model.go
@tui/overlay.go
@tui/tui.go
@cmd/gitid/main.go
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Surface registry (Register + RegisterOrReplace) + navigation state machine (number-key views + modal-launch mechanism) + deterministic full-shell RenderScreen — TDD</name>
  <files>internal/dummytui/doc.go, internal/dummytui/registry.go, internal/dummytui/registry_test.go, internal/dummytui/data.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md § "internal/dummytui/model.go" (viewKind enum + switchTo-per-number-key pattern from tui/model.go; the renderContent dim-then-composite modal dispatch shape; deviation: 5 views 1-5 + a modal-launch mechanism, backend-free)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (five views on 1-5; create-flow/git-screen are modal flows launched FROM Identities; reserved keys Esc/q/?//Enter; header breadcrumb screen-ID; the KEY-ALLOCATION TABLE — single source of truth: number keys 1-5, LaunchKeys n=create-flow/g=git-screen, identity-manager intra-surface a/c/d, reserved keys — which doc.go MUST mirror and the registration guard enforces)
    - tui/model.go (viewKind/modalKind enums, switchTo, renderContent `activeModal`/dim-then-composite dispatch — copy the SHAPE: a key sets the active modal, Esc clears it; the dummy generalizes the single activeModal into a modalStack keyed by launch bindings, still backend-free)
  </read_first>
  <behavior>
    - route(state{view:identity-manager}, key "2") → state{view:global-ssh}; keys "1".."5" map to identity-manager/global-ssh/global-git/health/fixer (by ActivationKey)
    - an unregistered/out-of-range number key is a no-op (state unchanged)
    - within a surface a registered screen-transition key advances activeScreen; Esc with an empty modalStack returns to the surface entry screen
    - Register(SurfaceDef) rejects a duplicate NON-EMPTY ActivationKey (test-detectable error/panic)
    - Register(SurfaceDef) with an EMPTY/unset ActivationKey is EXEMPT from the uniqueness check (review H2): registering TWO keyless modal surfaces together BOTH succeed (create-flow + git-screen use plain keyless Register)
    - RegisterOrReplace(SurfaceDef) claims a key even if a placeholder owns it: afterward exactly ONE surface owns that key and it is the replacement (ID + screens); Surfaces() holds no stale placeholder for that key — this is what lets fan-out 06–10 replace the 02-02 placeholders
    - MODAL LAUNCH (review C3): a registered keyless surface may declare LaunchFrom (a source surface ID) + LaunchKey (a key). While that source surface is the active surface and modalStack is empty, route(state, key==LaunchKey) PUSHES a modal frame {that keyless surface, its entry screen} onto navState.modalStack; a keyless surface is reachable ONLY this way — NO number key ever reaches it
    - MODAL POP: route(state, Esc) with a non-empty modalStack pops the top frame (returning to the parent surface/screen); while a modal is active, number keys 1–5 are IGNORED (do not switch top-level views) and intra-modal LaunchKeys/Keys apply to the topmost modal surface's active screen
    - ROUTE PRECEDENCE (review iter-3): on the active surface with an empty modalStack a key resolves in a DEFINED order — intra-surface ScreenDef.Keys FIRST, then a keyless LaunchKey whose LaunchFrom == st.view, then a number-key ActivationKey view-switch — so behavior is deterministic even absent a collision; a launch key fires ONLY if the active screen's ScreenDef.Keys does not claim it
    - REGISTRATION COLLISION GUARD (review iter-3): Register AND RegisterOrReplace REJECT (test-detectable error/panic) a registration that creates a key collision on a source surface — registering a keyless surface whose (LaunchFrom, LaunchKey) key is already used by ANY ScreenDef.Keys of the LaunchFrom surface (or by another keyless surface's LaunchKey with the same LaunchFrom) is rejected; symmetrically a RegisterOrReplace that introduces a ScreenDef.Keys transition equal to an existing keyless LaunchKey targeting that surface is rejected — so a create-flow/git-screen LaunchKey vs identity-manager ScreenDef.Keys clash fails at registration, not at 02-11 e2e; a LaunchKey equal to a number key 1-5 or a reserved key is likewise rejected
    - RenderScreen(surfaceID, screenID) returns the exact deterministic FULL-SHELL View() string (header incl. `<surface>/<screen>` breadcrumb + body + status + keybar), byte-identical every call, and errors for an unknown (surface,screen) — in Task 1 this is a self-contained MINIMAL inline composition (so the package compiles + this test passes standalone); Task 2 swaps in shell.go's full four-region composition (review C2)
  </behavior>
  <action>
    DLV-02: the five-view IA, modal-launch model, and header breadcrumb come from `agent-ui-ux-designer` (UX-DIRECTION §2); routing mirrors the `/mui` mockup navigation + breadcrumb (the parity source). Write `doc.go` documenting the DLV-05 no-backend ALLOWLIST rule AND the modal-launch contract (keyless surfaces reachable only via LaunchFrom/LaunchKey) AND MIRRORING the UX-DIRECTION §2 key-allocation table (a compact table headed "key-allocation": number keys 1-5 = the five primary surfaces; LaunchKeys `n`=create-flow and `g`=git-screen launched FROM identity-manager; identity-manager intra-surface `ScreenDef.Keys` `a`=action-menu / `c`=clone-name-prompt / `d`=delete-choice; reserved keys Esc, q, ?, /, Enter) as the single authority the fan-out surfaces allocate keys against (review iter-3). Write `registry.go`: `ScreenDef{ID string; Keys map[string]string; Render func() string}` (Render returns the screen BODY), `SurfaceDef{ID; Title; ActivationKey string; LaunchFrom string; LaunchKey string; Screens []ScreenDef}` — `ActivationKey` is set ONLY for the five number-key surfaces; `LaunchFrom`+`LaunchKey` are set ONLY on keyless modal surfaces (TARGET-OWNED: the modal surface names the source surface + key that launches it, so a fan-out surface wires its own launch in its own file and NEVER edits the source surface owner, model.go, or data.go). Add a package registry with `func Register(SurfaceDef)` (rejects a duplicate NON-EMPTY ActivationKey; an EMPTY/unset ActivationKey is EXEMPT from the uniqueness check — review H2 — so keyless modal surfaces like create-flow + git-screen both register via plain `Register` without collision), `func RegisterOrReplace(SurfaceDef)` (review HIGH-2: replace the existing owner of an ActivationKey so exactly one remains; else add — surfaces call this from their file's `init()`), `func Surfaces() []SurfaceDef`, and `func RenderScreen(surfaceID, screenID string) (string, error)` that composes the FULL shell so the returned string carries the `<surface>/<screen>` breadcrumb screen-ID (review HIGH-3b). For C2 (each task independently buildable): in THIS task RenderScreen builds the full-shell string via a SELF-CONTAINED minimal inline composition (breadcrumb header + body + status + keybar joined directly in registry.go — NO shell.go dependency) so `internal/dummytui` compiles and the RenderScreen test passes standalone; Task 2 rewires RenderScreen to delegate to shell.go's full four-region composition. LAUNCHKEY COLLISION GUARD (review iter-3): BOTH `Register` and `RegisterOrReplace` run an internal collision check that REJECTS (error/panic) a registration whose keyless `(LaunchFrom, LaunchKey)` key is already claimed by ANY `ScreenDef.Keys` transition of the `LaunchFrom` surface, by another keyless surface's `LaunchKey` with the same `LaunchFrom`, by a number key `1`-`5`, or by a reserved key; symmetrically `RegisterOrReplace` RE-RUNS the guard so a replaced source surface that introduces a `ScreenDef.Keys` transition equal to an existing keyless `LaunchKey` targeting it is rejected (this is what stops identity-manager 02-06 from silently stealing create-flow/git-screen's launch key). The keys the fan-out surfaces use are allocated in the UX-DIRECTION §2 / doc.go key-allocation table (`n`, `g`, `a`, `c`, `d`), so the guard normally passes; it exists to FAIL LOUDLY at registration (a test) rather than as a confusing 02-11 e2e failure if a future surface picks a colliding key. Implement the nav state machine: `navState{view string; activeScreen string; modalStack []modalFrame}` where `modalFrame{Surface, Screen string}`, and a pure reducer `func route(st navState, msg tea.KeyMsg) navState` (review C3): (a) with an empty modalStack a key resolves in this DEFINED PRECEDENCE (review iter-3) — FIRST an intra-surface transition via the active screen's `ScreenDef.Keys`, ELSE a key equal to some registered keyless surface's `LaunchKey` whose `LaunchFrom == st.view` PUSHES `modalFrame{that surface, its entry screen}`, ELSE a number key 1-5 switches the top-level `view` (by ActivationKey) — so a launch key fires ONLY when the active screen's `ScreenDef.Keys` does not claim it; (b) with a non-empty modalStack — number keys are ignored, `ScreenDef.Keys`/nested `LaunchKey` apply to the topmost modal surface's active screen, and Esc POPS the top frame; (c) Esc on an empty stack returns `activeScreen` to the surface entry screen — NO backend imports (only `charm.land/bubbletea/v2` for `tea.KeyMsg`). Seed `data.go` with five placeholder SurfaceDefs using the FINAL surface IDs `identity-manager`/`global-ssh`/`global-git`/`health`/`fixer` on ActivationKeys 1-5 (so `LaunchFrom: "identity-manager"` bindings from create-flow/git-screen resolve against the placeholder in Wave 3 AND the RegisterOrReplace real surface in Wave 4), each one placeholder screen with a labeled placeholder body — surfaces REPLACE these via RegisterOrReplace in their own files, never editing data.go. Write `registry_test.go` (table-driven) covering every `<behavior>`: number-key routing to the five final IDs, no-op on unknown key, intra-surface transition, Esc return, duplicate NON-EMPTY-key rejection via Register, the EMPTY-key exemption (register TWO keyless surfaces together — BOTH succeed; review H2), RegisterOrReplace single-owner replacement, the MODAL-LAUNCH mechanism (review C3 — register a source surface + a test-local keyless surface with LaunchFrom=source ID and LaunchKey "x"; assert route on "x" while the source is active pushes the modal frame at the keyless surface's entry screen; assert Esc pops back to the parent; assert NO number key reaches the keyless surface), the LAUNCHKEY COLLISION GUARD (review iter-3 — register a source surface whose entry-screen `ScreenDef.Keys` claims key "x", then assert registering a keyless surface with `LaunchFrom`=that source + `LaunchKey`="x" is REJECTED; the REVERSE — a keyless `LaunchKey` "x" present, then a `RegisterOrReplace` of the source surface that adds a `ScreenDef.Keys` "x" is REJECTED; and a NON-colliding `LaunchKey` registers cleanly) plus the PRECEDENCE case (an active screen whose `ScreenDef.Keys` defines key "x" resolves the intra-surface transition, NOT a launch), and RenderScreen determinism + contains the `<surface>/<screen>` breadcrumb + unknown-key error. Author RED first (`_`-named-param stub per MEMORY tdd-red-stub so RED passes hard-fail lint without --no-verify), then GREEN.
  </action>
  <verify>
    <automated>go test -race ./internal/dummytui/... -run 'TestRoute|TestRegistry|TestRegisterOrReplace|TestModalLaunch|TestRenderScreen'</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/dummytui/... -run 'TestRoute|TestRegistry|TestRegisterOrReplace|TestModalLaunch|TestRenderScreen'` passes covering all `<behavior>` bullets
    - `grep -q 'func Register' internal/dummytui/registry.go` AND `grep -q 'func RegisterOrReplace' internal/dummytui/registry.go` AND `grep -q 'func RenderScreen' internal/dummytui/registry.go`
    - The modal-launch fields exist on the contract (review C3): `grep -q 'LaunchFrom' internal/dummytui/registry.go` AND `grep -q 'LaunchKey' internal/dummytui/registry.go` AND `grep -q 'modalStack' internal/dummytui/registry.go`
    - Routing test asserts keys "1".."5" reach identity-manager/global-ssh/global-git/health/fixer (the FINAL IDs, matching 02-11's keyowners assertion)
    - Register's empty-key exemption is proven (review H2): a test registers TWO keyless (empty ActivationKey) surfaces together and asserts BOTH succeed (no duplicate-key rejection for empty keys)
    - Modal launch is proven (review C3): a test registers a keyless surface with LaunchFrom/LaunchKey, asserts route on that key (while the source surface is active) pushes the modal frame at the keyless surface's entry screen and that Esc pops back to the parent; and asserts NO number key reaches the keyless surface
    - The LaunchKey collision guard is proven (review iter-3): a test asserts registering a keyless surface whose LaunchKey equals a ScreenDef.Keys key on its LaunchFrom surface (and the reverse — a RegisterOrReplace that introduces the colliding ScreenDef.Keys) is REJECTED, and a non-colliding LaunchKey registers cleanly; route() precedence is proven — intra-surface ScreenDef.Keys resolve before a launch key
    - doc.go mirrors the key-allocation table: `grep -q 'key-allocation' internal/dummytui/doc.go` AND `grep -q 'LaunchKey' internal/dummytui/doc.go`
    - A test registers a placeholder on key "1" then RegisterOrReplace on key "1" and asserts exactly one owner = the replacement
    - RenderScreen determinism + screen-ID: a test asserts two byte-identical calls whose string contains the `<surface>/<screen>` breadcrumb
    - Allowlist holds for these files: `[ -z "$(go list -deps ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '^github.com/castocolina/gitid/(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - `make lint` passes
  </acceptance_criteria>
  <done>Register + RegisterOrReplace + pure reducer route the five views on 1-5, launch keyless modal surfaces via LaunchFrom/LaunchKey onto a modalStack (Esc pops), support intra-surface transitions and Esc return, reject duplicate keys under Register while RegisterOrReplace cleanly replaces a placeholder, and expose a deterministic full-shell RenderScreen carrying the breadcrumb screen-ID — backend-free, TDD-proven.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Bubble Tea model + shell rendering (breadcrumb) + modal-stack overlay + dummy binary + no-backend ALLOWLIST gate</name>
  <files>internal/dummytui/registry.go, internal/dummytui/model.go, internal/dummytui/model_test.go, internal/dummytui/shell.go, internal/dummytui/overlay.go, internal/dummytui/nobackend_test.go, cmd/gitid-dummy/main.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md § "internal/dummytui/overlay.go" (copy tui/overlay.go placeOverlay/overlayLine/modalOrigin/boundModalToViewport verbatim, backend-free) and § "cmd/gitid-dummy/main.go" (tui/tui.go Run() shape + cmd/gitid/main.go isTTY branch; DROP Cobra) and § "No-backend-import boundary"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Pattern 2" (import-graph enforcement), § "Pitfall 1" (lipgloss v2.0.3 no PlaceOverlay)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (four regions; AltScreen; keybar shows only valid keys; header breadcrumb screen-ID)
    - tui/overlay.go (full 204 lines — the algorithm to reimplement), tui/model.go lines 954-999 (View()+AltScreen; renderPersistentLayout / dim-then-composite `activeModal` dispatch — the shape the dummy generalizes into a modalStack)
  </read_first>
  <behavior>
    - NewModel() returns a tea.Model seeded from the registry with identity-manager active and an empty modalStack
    - Update(tea.KeyMsg) applies route() from Task 1; View() composes header(incl. `<surface>/<screen>` breadcrumb)+body(active Render)+status+keybar and sets AltScreen=true
    - MODAL VIEW (review C3): when modalStack is non-empty, View() computes the active (surface,screen) as the TOPMOST modalStack frame, renders the header breadcrumb from THAT frame, and composites the modal surface's active-screen body over the DIMMED parent layout via placeOverlay; Esc pops the modal and the breadcrumb reverts to the parent
    - a launch key pressed while its source surface is active opens the modal (breadcrumb switches to the modal's `<surface>/<screen>`); Esc closes it
    - placeOverlay(x,y,fg,bg) splices fg into bg ANSI-width-safely and never panics when the modal exceeds the viewport (clamped)
    - the no-backend allowlist test fails if go list -deps contains ANY github.com/castocolina/gitid/* package other than internal/dummytui and cmd/gitid-dummy
  </behavior>
  <action>
    Write `overlay.go` reimplementing tui/overlay.go's `placeOverlay`/`overlayLine` (ANSI-width-safe via `lipgloss.Width` + `github.com/charmbracelet/x/ansi` Truncate/TruncateLeft)/`modalOrigin`/`boundModalToViewport` VERBATIM in `package dummytui` — do NOT import `tui/` (it pulls backend packages and breaks the allowlist). Write `shell.go` rendering the four regions — Header with `gitid` + active-view name + the `<surface>/<screen>` BREADCRUMB screen-ID (review HIGH-3b) + context chip; body = active screen Render(); StatusLine; always-visible Keybar (only valid keys) — using `charm.land/lipgloss/v2`, mirroring tui/model.go `renderPersistentLayout`, backend-free, matching the MUI shell + breadcrumb from 02-01 (the `/mui` parity source; `agent-ui-ux-designer` validates cross-media parity). Then REWIRE `RenderScreen` in registry.go (review C2) to delegate to shell.go's full four-region composition — REPLACING Task 1's minimal inline shell — so the capture string carries the full-shell breadcrumb; Task 2 OWNS the full-shell assertion (a test that RenderScreen's output shows the four regions + the `<surface>/<screen>` breadcrumb). Write `model.go`: `NewModel()` seeded from `Surfaces()` with identity-manager active + empty modalStack, `Update` delegating to `route()`, `View() tea.View` with `v.AltScreen = true`, and the modal-stack dispatch (review C3) — compute the active (surface,screen) as the topmost modalStack frame when present (else view/activeScreen), render the header breadcrumb from that active frame, and when a modal is active dim the parent layout and composite the modal surface's active-screen body via `placeOverlay` (the dummy's generalization of tui/model.go's single-`activeModal` dim-then-composite). Write `cmd/gitid-dummy/main.go` as a thin main: the isTTY-gate idiom from cmd/gitid/main.go (drop Cobra) calling `tea.NewProgram(dummytui.NewModel()).Run()` + `os.Exit`. Write `nobackend_test.go` (review HIGH-1) — a plain (untagged) test that shells `go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/...` and as an ALLOWLIST FAILS if any `github.com/castocolina/gitid/*` package other than exactly `internal/dummytui` and `cmd/gitid-dummy` appears (filter to first-party lines, subtract the two allowed, fail on any remainder) — strictly stronger than the old denylist because it catches NEW/renamed backend packages by construction (arg-slice exec.Command, `#nosec G204`). Write `model_test.go` covering the `<behavior>` incl. a placeOverlay clamp case, a View()-contains-breadcrumb case, the MODAL-LAUNCH-through-the-model case (review C3 — seed a source surface + a keyless surface with LaunchFrom/LaunchKey, drive NewModel().Update(launchKey), assert View() header breadcrumb shows the modal's `<surface>/<screen>`, then Update(Esc), assert View() reverts to the parent breadcrumb), and the FULL-SHELL RenderScreen assertion (four regions + breadcrumb) now that shell.go composes it (review C2). RED first, then GREEN.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... ./internal/dummytui/... && go test -race ./internal/dummytui/... && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/gitid-dummy/... ./internal/dummytui/...` exits 0
    - `go test -race ./internal/dummytui/...` passes (model Update/View, breadcrumb in View, modal-launch through the model + breadcrumb reflects the active modal screen + Esc reverts, modal overlay clamp, no-backend allowlist all green, AND — review C2 — RenderScreen now delegates to shell.go's full four-region composition with the full-shell assertion green)
    - Modal launch through the model is proven (review C3): a model_test presses a launch key and asserts View()'s header breadcrumb shows the launched modal's `<surface>/<screen>`, then Esc returns to the parent breadcrumb
    - DLV-05 ALLOWLIST holds (strictly stronger than a denylist): `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]` — any other first-party package FAILS
    - `nobackend_test.go` encodes the allowlist (not a denylist): `grep -q 'go list' internal/dummytui/nobackend_test.go` AND `grep -qi 'allowlist' internal/dummytui/nobackend_test.go`
    - overlay reimplemented, not imported from tui/: `grep -q 'func placeOverlay' internal/dummytui/overlay.go` AND `! grep -q 'castocolina/gitid/tui' internal/dummytui/overlay.go`
    - `cmd/gitid-dummy/main.go` calls the dummy model directly with no Cobra: `grep -q 'dummytui.NewModel' cmd/gitid-dummy/main.go` AND `! grep -q 'cobra' cmd/gitid-dummy/main.go`
    - `go build ./cmd/gitid` (the real product binary) still succeeds and is unaffected
    - `make lint` passes
  </acceptance_criteria>
  <done>cmd/gitid-dummy launches a navigable four-region TUI shell (header breadcrumb screen-ID, working modal-launch overlays keyed by LaunchFrom/LaunchKey with the breadcrumb reflecting the active modal screen, five number-key views), proven by a go list -deps ALLOWLIST to import zero backend packages; Register/RegisterOrReplace let surfaces plug in and replace placeholders without editing model.go/data.go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dummy TUI ← real dotfiles (~/.ssh, ~/.gitconfig) | the dummy MUST have no code path that can read/write real config — enforced by the import-graph allowlist |
| dev/test tooling ← go toolchain (go list) | nobackend_test shells `go list`; arg-slice only, no shell string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB | Tampering / Elevation of Privilege | dummy accidentally importing a NEW/renamed backend package that can touch ~/.ssh or ~/.gitconfig | mitigate | DLV-05 import-graph ALLOWLIST (review HIGH-1): `nobackend_test.go` fails the build if `go list -deps` contains ANY `github.com/castocolina/gitid/*` package other than internal/dummytui + cmd/gitid-dummy — catches new/renamed backend pkgs by construction, unlike the prior denylist; the dummy imports only charm.land/{bubbletea,lipgloss,bubbles} + x/ansi |
| T-02-EX | Tampering | exec.Command("go","list",…) in nobackend_test | mitigate | Arg-slice form only, `#nosec G204`; no shell interpolation of any surface/screen name |
| T-02-OV | Denial of Service | placeOverlay panic when modal exceeds viewport | mitigate | Port tui/overlay.go's `boundModalToViewport` clamp verbatim; model_test asserts a clamp case does not panic |
| T-02-RG | Tampering | fan-out surface silently failing to replace a placeholder (duplicate-key registration) | mitigate | RegisterOrReplace guarantees a single owner per activation key; a registry_test asserts the replacement is the sole owner |
| T-02-ML | Tampering / Repudiation | a keyless modal surface silently unreachable (broken launch binding) → a modal screen missing from the frozen reference | mitigate | Modal launch is a target-owned LaunchFrom/LaunchKey contract; a registry_test proves the launch pushes the modal + Esc pops, a model_test proves the launched breadcrumb; placeholders use the final surface IDs so bindings resolve pre- and post-RegisterOrReplace |
| T-02-KC | Tampering / Repudiation | a keyless surface's LaunchKey colliding with the LaunchFrom surface's intra-surface ScreenDef.Keys (independent fan-out plans picking keys), surfacing only as a confusing 02-11 e2e failure | mitigate | route() precedence (intra-surface ScreenDef.Keys before launch keys) + a REGISTRATION-TIME collision guard in Register/RegisterOrReplace that rejects a colliding (LaunchFrom, LaunchKey) (or the reverse ScreenDef.Keys on RegisterOrReplace) with a test-detectable error; the UX-DIRECTION §2 / doc.go key-allocation table gives the three fan-out plans ONE authority (n=create-flow, g=git-screen, a/c/d=identity-manager); a registry_test proves the rejection (review iter-3) |
</threat_model>

## Artifacts this phase produces

- `internal/dummytui/doc.go` — package + DLV-05 no-backend ALLOWLIST rule doc + modal-launch contract doc + the key-allocation table mirroring UX-DIRECTION §2 (1-5 views, `n`/`g` LaunchKeys, identity-manager `a`/`c`/`d` transitions, reserved keys) — the single key authority (review iter-3)
- `internal/dummytui/registry.go` — `Register`, `RegisterOrReplace`, `Surfaces`, `RenderScreen` (full-shell, breadcrumb), `SurfaceDef` (incl. `LaunchFrom`/`LaunchKey` target-owned modal-launch bindings)/`ScreenDef`, pure `route()` reducer over `navState.modalStack` (number-key views + modal launch/pop) with EXPLICIT key precedence (`ScreenDef.Keys` → `LaunchKey` → number-key) + a LaunchKey collision guard in `Register`/`RegisterOrReplace` (review iter-3)
- `internal/dummytui/model.go` — `NewModel()` tea.Model: number-key routing + modal-stack dispatch (launch key → placeOverlay composite over dimmed parent, Esc pops) + AltScreen View() with the breadcrumb reflecting the active modal screen
- `internal/dummytui/shell.go` — four-region shell rendering (header incl. `<surface>/<screen>` breadcrumb / body / status / keybar)
- `internal/dummytui/overlay.go` — backend-free `placeOverlay`/`overlayLine`/`modalOrigin`/`boundModalToViewport`
- `internal/dummytui/data.go` — five placeholder SurfaceDefs on the FINAL surface IDs identity-manager/global-ssh/global-git/health/fixer (replaced per surface via RegisterOrReplace; source of the LaunchFrom target IDs)
- `internal/dummytui/{registry,model,nobackend}_test.go` — nav state machine + modal launch/pop + replacement + no-backend ALLOWLIST tests
- `cmd/gitid-dummy/main.go` — nav-only dummy binary entry point (no backend deps, no Cobra)

<verification>
- `go build ./cmd/gitid-dummy/...` produces a runnable nav-only binary; `go build ./cmd/gitid` unaffected
- `go test -race ./internal/dummytui/...` green; nav routing + modal launch/pop + RegisterOrReplace + RenderScreen determinism/breadcrumb + modal clamp proven
- DLV-05 ALLOWLIST: `go list -deps` confirms zero first-party packages beyond dummytui/gitid-dummy
- Register/RegisterOrReplace + LaunchFrom/LaunchKey + model let surfaces register/replace and launch keyless modals in their own files without editing model.go/data.go
- route() precedence (ScreenDef.Keys → LaunchKey → number-key) + the LaunchKey collision guard reject a colliding (LaunchFrom, LaunchKey) at registration (registry_test), and doc.go mirrors the UX-DIRECTION §2 key-allocation table (review iter-3)
</verification>

<success_criteria>
- DLV-05 (front half + no-backend): a navigable Go TUI dummy with full shell navigation (five number-key views + keyless modal surfaces reachable via the launch mechanism) and ZERO backend logic, proven by an import-graph ALLOWLIST (strictly stronger than the prior denylist), not asserted.
- DLV-02: the dummy shell + breadcrumb mirror the `/mui` mockup and `agent-ui-ux-designer` validates cross-media parity — both named in every task.
- Register/RegisterOrReplace + LaunchFrom/LaunchKey + model contracts make the six-surface fan-out (Wave 4) file-conflict-free AND make the ~19 keyless modal screens reachable on the real binary.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
