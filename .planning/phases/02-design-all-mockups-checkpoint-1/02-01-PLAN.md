---
phase: 02-design-all-mockups-checkpoint-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/design/mockup-src/package.json
  - .planning/design/mockup-src/pnpm-lock.yaml
  - .planning/design/mockup-src/tsconfig.json
  - .planning/design/mockup-src/vite.config.ts
  - .planning/design/mockup-src/index.html
  - .planning/design/mockup-src/.gitignore
  - .planning/design/mockup-src/src/main.tsx
  - .planning/design/mockup-src/src/App.tsx
  - .planning/design/mockup-src/src/theme.ts
  - .planning/design/mockup-src/src/shell/Shell.tsx
  - .planning/design/mockup-src/src/shell/Header.tsx
  - .planning/design/mockup-src/src/shell/StatusLine.tsx
  - .planning/design/mockup-src/src/shell/Keybar.tsx
  - .planning/design/mockup-src/src/data/recipeFixtures.ts
  - .planning/design/mockup-src/src/routes/_shell/shell-demo.route.tsx
  - .planning/design/mockup-src/README.md
  - .planning/design/FIELDS.template.md
  - .planning/design/CRITIQUE.template.md
autonomous: true
requirements: [DLV-01, DLV-02]
user_setup: []
must_haves:
  truths:
    - "A Vite + React 19 + TypeScript + MUI v7.3.11 SPA builds to a static dist/ loadable via file:// with zero network dependency (self-hosted font, base './', HashRouter)"
    - "The MUI theme renders as a terminal skin (monospace, shadows:none, borderRadius:0, transitions 0ms) — the mockup reads as a screenshot of a terminal, not a SaaS dashboard (per DLV-02 agent-ui-ux-designer direction)"
    - "The shared app shell (header/context bar, body region, status line, always-on keybar) is implemented once and every later surface route renders inside it"
    - "New surfaces register via import.meta.glob route auto-discovery — adding a surface route file requires NO edit to App.tsx"
    - "Recipe-accurate canonical copy (Port 443, IdentitiesOnly yes, includeIf hasconfig:/gitdir:, insteadOf, allowed_signers) is available as typed fixtures for every surface — no lorem"
    - "npm supply-chain is pinned exactly (MUI 7.3.11 not @latest→9.x), committed lockfile, frozen-lockfile installs only"
  artifacts:
    - path: ".planning/design/mockup-src/package.json"
      provides: "pinned MUI v7 + React 19 + Vite workspace manifest"
      contains: "7.3.11"
    - path: ".planning/design/mockup-src/vite.config.ts"
      provides: "file:// compatible static build config"
      contains: "base"
    - path: ".planning/design/mockup-src/src/theme.ts"
      provides: "terminal-skin MUI theme"
      contains: "borderRadius"
    - path: ".planning/design/mockup-src/src/App.tsx"
      provides: "HashRouter + glob-based route auto-discovery"
      contains: "import.meta.glob"
    - path: ".planning/design/mockup-src/src/data/recipeFixtures.ts"
      provides: "typed recipe-accurate canonical config copy for all surfaces"
      contains: "IdentitiesOnly"
    - path: ".planning/design/FIELDS.template.md"
      provides: "per-surface semantic field-parity manifest template"
      contains: "HTML present"
  key_links:
    - from: "src/App.tsx"
      to: "src/routes/**/*.route.tsx"
      via: "import.meta.glob auto-discovery (surfaces add files, never edit App.tsx)"
      pattern: "import.meta.glob"
    - from: "src/theme.ts"
      to: "MUI ThemeProvider"
      via: "terminal-skin overrides (shadows none, borderRadius 0, transitions 0ms)"
      pattern: "shadows"
---

<objective>
Establish the HTML/MUI-v7 mockup foundation (DLV-01): a Vite + React 19 + TypeScript + MUI v7.3.11 SPA under `.planning/design/mockup-src/`, themed into a **terminal skin** so the HTML↔TUI parity gate is semantic (not pixel noise), plus the shared app shell (header / body / status line / keybar) every one of the seven surfaces renders inside, plus a typed recipe-accurate copy fixtures module, plus glob-based route auto-discovery so surface plans ADD route files without editing shared code. This plan builds ZERO surface screens — only the reusable foundation the pilot (02-04) and the six fan-out surfaces (02-05..02-10) build on. It also lands the per-surface `FIELDS.md` / `CRITIQUE.md` templates (the semantic-parity manifest each surface copies).

Purpose: DLV-01 requires an HTML mockup BEFORE any Go/TUI code for a surface; the mockup must lock content + flow (fields, labels, copy, order, states). A terminal-skinned shell + recipe fixtures make every later mockup fast, consistent, and recipe-faithful. Auto-discovery keeps the six-surface fan-out (Wave 4) conflict-free.
Output: the pnpm workspace, the terminal-skin theme, the shell components, the recipe fixtures module, glob route discovery, and the two design-doc templates.

PREFLIGHT: This phase depends on Phase 1. This plan itself has no Phase-1 code dependency (it is npm/Vite only), but the capture plan (02-03) consumes Phase 1's `internal/screenshot` — do not start the capture wave until Phase 1 Plan 05 has landed.

DLV-02: this is a UI task. Author the theme and shell WITH the `/mui` skill (MUI v7 conventions — `sx` prop, slots/slotProps, package-exports-only imports, `ThemeProvider`) and consult `agent-ui-ux-designer` for the terminal-skin direction (02-UX-DIRECTION §0 Risk 1, §1 anti-slop, §2 shared shell + ANSI-safe semantic color table). Name both explicitly in each task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md
@recipes/ssh-config.recipe
@recipes/gitconfig.recipe
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the pinned pnpm + Vite + MUI v7 workspace (supply-chain automated-verified)</name>
  <files>.planning/design/mockup-src/package.json, .planning/design/mockup-src/pnpm-lock.yaml, .planning/design/mockup-src/tsconfig.json, .planning/design/mockup-src/vite.config.ts, .planning/design/mockup-src/index.html, .planning/design/mockup-src/.gitignore, .planning/design/mockup-src/README.md</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Standard Stack" (exact pins: react 19.2.7, @mui/material 7.3.11, @mui/icons-material 7.3.11, @emotion/react 11.14.0, @emotion/styled 11.14.1, react-router-dom 7.18.1, vite 8.1.3, @vitejs/plugin-react 6.0.3, typescript 6.0.3, @types/react 19.2.17, @types/react-dom 19.2.3) and § "Package Legitimacy Audit" (all 10 packages slopcheck [OK]/Approved)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Pitfall 2" (base:'./'), § "Pitfall 4" (@mui/material@latest resolves to v9 — pin exactly)
    - .planning/phases/01-foundations-spikes-ci/01-05-PLAN.md § Task 1 (the established pattern for replacing a human supply-chain gate with AUTOMATED verification to preserve the one-checkpoint milestone)
    - CLAUDE.md (pnpm not npm; commit lockfiles; MEMORY note python-tooling-uv-not-pip-brew)
  </read_first>
  <action>
    From `.planning/design/mockup-src/`, create `package.json` with EXACT version pins (no caret across a major) using the `/mui` skill's v7 conventions: `@mui/material` and `@mui/icons-material` both `7.3.11` (NEVER `@latest` — resolves to v9), `@emotion/react` `11.14.0`, `@emotion/styled` `11.14.1`, `react`/`react-dom` `19.2.7`, `react-router-dom` `7.18.1`, and dev deps `vite` `8.1.3`, `@vitejs/plugin-react` `6.0.3`, `typescript` `6.0.3`, `@types/react` `19.2.17`, `@types/react-dom` `19.2.3`. Add a self-hosted font dependency `@fontsource/jetbrains-mono` (pin exact latest 5.x — RESEARCH Pitfall 6: no Google Fonts CDN, offline-deterministic). Run `pnpm install` to generate `pnpm-lock.yaml`, then confirm the resolved `@mui/material` is `7.3.11` (NOT 9.x). Write `vite.config.ts` with `base: './'` (Pitfall 2) and the react plugin. Write `tsconfig.json` (strict, react-jsx). Write `index.html` (root div + module script). Write `.gitignore` (ignore `node_modules/` and `dist/`, but the plan MUST commit `package.json` + `pnpm-lock.yaml` per RESEARCH supply-chain note). Write `README.md` documenting: this is a living design doc NOT a shipped web UI (REQUIREMENTS Out of Scope); build with `pnpm install --frozen-lockfile && pnpm build`; supply-chain is automated-verified (pinned + committed lockfile + slopcheck [OK] in 02-RESEARCH), mirroring Phase 1's Go-side automated-gate decision, so it adds NO second human checkpoint to this one-checkpoint milestone. Do NOT run a bare `pnpm install` in any CI/make step — only `--frozen-lockfile`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm install --frozen-lockfile && pnpm exec vite build && test -f dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/design/mockup-src/package.json` pins `@mui/material` and `@mui/icons-material` at exactly `7.3.11`: `grep -q '"@mui/material": "7.3.11"' .planning/design/mockup-src/package.json` AND `! grep -q '@latest' .planning/design/mockup-src/package.json`
    - `pnpm-lock.yaml` exists and is committed; the resolved MUI version is 7.3.11 not 9.x: `grep -q '7.3.11' .planning/design/mockup-src/pnpm-lock.yaml` AND `! grep -qE 'mui/material@9' .planning/design/mockup-src/pnpm-lock.yaml`
    - `vite.config.ts` sets `base: './'`: `grep -q "base: './'" .planning/design/mockup-src/vite.config.ts`
    - `pnpm install --frozen-lockfile && pnpm exec vite build` exits 0 and writes `.planning/design/mockup-src/dist/index.html`
    - `.gitignore` excludes `node_modules/` and `dist/`: `grep -q 'node_modules' .planning/design/mockup-src/.gitignore`
    - No Google Fonts CDN link anywhere: `! grep -rq 'fonts.googleapis.com' .planning/design/mockup-src/`
  </acceptance_criteria>
  <done>The pinned MUI-v7 Vite workspace installs from a committed frozen lockfile and builds a static file://-loadable dist/ with zero network dependency; supply-chain is automated-verified (no second human checkpoint).</done>
</task>

<task type="auto">
  <name>Task 2: Terminal-skin MUI theme + shared app shell + glob route auto-discovery</name>
  <files>.planning/design/mockup-src/src/main.tsx, .planning/design/mockup-src/src/App.tsx, .planning/design/mockup-src/src/theme.ts, .planning/design/mockup-src/src/shell/Shell.tsx, .planning/design/mockup-src/src/shell/Header.tsx, .planning/design/mockup-src/src/shell/StatusLine.tsx, .planning/design/mockup-src/src/shell/Keybar.tsx, .planning/design/mockup-src/src/routes/_shell/shell-demo.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 0 Risk 1 (terminal-skin theme constraints), § 1 (anti-slop stance), § 2 (four shell regions: header/body/status/keybar; number-key nav 1-5; reserved keys Esc/q/?//; ANSI-safe semantic color table)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Pattern 3" (HashRouter + base:'./'), § "Pitfall 3" (HashRouter not BrowserRouter), § "Pitfall 5" (theme.transitions → 0ms for deterministic capture)
    - ~/.claude/plugins/marketplaces/agent-toolkit/skills/mui/SKILL.md (v7 sx prop, slots/slotProps, ThemeProvider, package-exports imports)
  </read_first>
  <action>
    Using the `/mui` skill (MUI v7 `createTheme` + `ThemeProvider`, `sx` prop, slots/slotProps) and the `agent-ui-ux-designer` terminal-skin direction: write `theme.ts` as a `createTheme` that enforces the terminal skin — `typography.fontFamily` = JetBrains Mono (imported from `@fontsource/jetbrains-mono`, self-hosted), `shape.borderRadius: 0`, `shadows` all `'none'`, disabled ripple via default `componentsProps`, all `transitions.duration.*` set to `0` (Pitfall 5 determinism), a dark ANSI-like palette with the SEMANTIC roles from UX-DIRECTION §2 (healthy=green, warning=yellow, error/destructive=red, dim=gray, focus=reverse/bold not a new hue), and a fixed max-width monospace "terminal" container. Write `Shell.tsx` composing the four regions from UX-DIRECTION §2 — `Header` (app name `gitid` + current view name + global context chip), a body slot (children), `StatusLine` (one-line transient feedback incl. backup path slot), `Keybar` (always-visible context keybindings). Every state MUST pair color with a glyph AND a word (never color alone) and remain legible under monochrome. Write `App.tsx` with `HashRouter` (Pitfall 3) and `import.meta.glob('./routes/**/*.route.tsx', { eager: true })` route auto-discovery: each route module default-exports `{ path, element }`; App maps them into `<Routes>` inside `<Shell>` — so surface plans ADD `*.route.tsx` files and NEVER edit App.tsx. Write `main.tsx` wrapping App in `ThemeProvider` + `CssBaseline`. Write `routes/_shell/shell-demo.route.tsx` — a single demo route rendering the empty shell with placeholder body text, proving the shell renders (this is the ONLY route this plan ships; surfaces add the rest).
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm exec vite build && test -f dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `theme.ts` enforces the terminal skin: `grep -q 'borderRadius: 0' .planning/design/mockup-src/src/theme.ts` AND `grep -q "none" .planning/design/mockup-src/src/theme.ts` (shadows) AND references JetBrains Mono
    - `App.tsx` uses glob auto-discovery and HashRouter (not BrowserRouter): `grep -q 'import.meta.glob' .planning/design/mockup-src/src/App.tsx` AND `grep -q 'HashRouter' .planning/design/mockup-src/src/App.tsx` AND `! grep -q 'BrowserRouter' .planning/design/mockup-src/src/App.tsx`
    - All four shell region components exist (Header, StatusLine, Keybar, Shell) under `src/shell/`
    - `pnpm exec tsc --noEmit` reports no type errors AND `pnpm exec vite build` exits 0
    - The demo route renders inside the shell (build succeeds with the `_shell` route present)
  </acceptance_criteria>
  <done>A terminal-skinned MUI v7 theme and the shared four-region app shell render via HashRouter with glob route auto-discovery; the mockup reads as a terminal and later surfaces add route files without touching App.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Recipe-accurate copy fixtures + FIELDS.md / CRITIQUE.md templates + design-dir scaffold</name>
  <files>.planning/design/mockup-src/src/data/recipeFixtures.ts, .planning/design/FIELDS.template.md, .planning/design/CRITIQUE.template.md</files>
  <read_first>
    - recipes/ssh-config.recipe (alias-per-identity Host block, Hostname ssh.github.com, Port 443, User git, IdentityFile, IdentitiesOnly yes, IgnoreUnknown UseKeychain / Host * UseKeychain+AddKeysToAgent)
    - recipes/gitconfig.recipe (includeIf hasconfig:remote.*.url + gitdir:, insteadOf URL rewrite, per-identity fragment user.name/email/signingkey/commit.gpgsign, [core] ignorecase=false, [push] autoSetupRemote, [pull] rebase, [fetch] prune, [diff] colorMoved, [merge] conflictstyle)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review (DLV-02 methodology)" (the FIELDS.md field-parity matrix format)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3 (semantic parity MUST-match list), § 5 (mutation ceremony), § 6 (approval checklist)
  </read_first>
  <action>
    Write `recipeFixtures.ts` exporting typed constants with the REAL recipe copy every surface reuses (no lorem): the canonical alias `Host personal.github.com` block (`Hostname ssh.github.com`, `Port 443`, `User git`, `IdentityFile ~/.ssh/id_ed25519_personal`, `IdentitiesOnly yes`) using ed25519 per recipes/README "structure not key type"; the macOS globals block (`IgnoreUnknown UseKeychain`, `Host *` `UseKeychain yes` + `AddKeysToAgent yes`); the `includeIf hasconfig:remote.*.url:git@personal.github.com:*/**` default-match line and the `gitdir:~/personal/` alternative; the `insteadOf` URL-rewrite block; a per-identity git fragment (`user.name`, `user.email`, `gpg.format=ssh`, `user.signingkey` as a PATH, `commit.gpgsign=true`); the `~/.ssh/allowed_signers` line with the email BYTE-IDENTICAL to `user.email`; the global-git recipe defaults (`init.defaultBranch=main`, `core.ignorecase=false`, `push.autoSetupRemote`, `pull.rebase`, `fetch.prune`, `merge.conflictstyle`, `diff.colorMoved`); and the `# BEGIN/END gitid managed: <identity>` sentinels. Include a stable timestamped backup-path sample string for the mutation-ceremony `backup-notice` states. Write `FIELDS.template.md` — the per-surface semantic field-parity manifest template from RESEARCH § HTML↔TUI Parity Review (columns: #, Field, Label, Order, HTML present, TUI present, Notes) with a header block for surface name + per-screen sections. Write `CRITIQUE.template.md` — the agent-ui-ux-designer findings-log template: an HTML-aesthetic section, a semantic-parity findings table (each row keyed to a MUST-match dimension from UX-DIRECTION §3, status OPEN/RESOLVED), and a rule that 0 OPEN is required before approval. DLV-02: note in both templates that `agent-ui-ux-designer` fills the critique and the `/mui`-built mockup is the HTML side of every parity row.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && grep -q 'IdentitiesOnly' src/data/recipeFixtures.ts && grep -q 'allowed_signers' src/data/recipeFixtures.ts</automated>
  </verify>
  <acceptance_criteria>
    - `recipeFixtures.ts` type-checks (`pnpm exec tsc --noEmit` clean) and contains the recipe-critical strings: `grep -q 'Port 443' .../recipeFixtures.ts`, `grep -q 'IdentitiesOnly yes' .../recipeFixtures.ts`, `grep -q 'hasconfig:remote' .../recipeFixtures.ts`, `grep -q 'insteadOf' .../recipeFixtures.ts`, `grep -q 'allowed_signers' .../recipeFixtures.ts`, `grep -q 'gpg.format' .../recipeFixtures.ts` (paths relative to `.planning/design/mockup-src/src/data/`)
    - The default match strategy is `gitdir` per DLV/UX-DIRECTION but hasconfig is the recipe primary — both present: `grep -q 'gitdir' .../recipeFixtures.ts` AND `grep -q 'hasconfig' .../recipeFixtures.ts`
    - The managed-block sentinel is present: `grep -q 'BEGIN gitid managed' .../recipeFixtures.ts`
    - `FIELDS.template.md` has the parity-matrix columns: `grep -q 'HTML present' .planning/design/FIELDS.template.md` AND `grep -q 'TUI present' .planning/design/FIELDS.template.md`
    - `CRITIQUE.template.md` encodes the 0-OPEN rule and names agent-ui-ux-designer: `grep -q 'OPEN' .planning/design/CRITIQUE.template.md` AND `grep -q 'agent-ui-ux-designer' .planning/design/CRITIQUE.template.md`
    - No lorem/placeholder copy: `! grep -riq 'lorem' .planning/design/mockup-src/src/data/recipeFixtures.ts`
  </acceptance_criteria>
  <done>Every surface can pull byte-accurate recipe copy from one typed module, and the semantic-parity manifest + critique templates exist so each surface's FIELDS.md/CRITIQUE.md is a fill-in-the-blank of a locked format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev toolchain ← npm registry (first Node.js toolchain in an all-Go repo) | new supply-chain surface added to dev/design tooling only; never the shipped Go binary |
| headless capture ← local mockup dist/ | mockup is ALWAYS loaded via a local file:// path to the repo's own dist/, never remote (capture happens in 02-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | 10 new npm packages (react/mui/vite/emotion/router/ts) | mitigate | Exact version pins (no `@latest`, no cross-major caret), committed `pnpm-lock.yaml`, `pnpm install --frozen-lockfile` only in CI/make, slopcheck `[OK]` for all 10 (02-RESEARCH § Package Legitimacy Audit). AUTOMATED verification mirroring Phase 1 Plan 05 Task 1 — preserves the one-checkpoint milestone (no second human gate); the packages carry `[OK]` verdicts, so no per-package human checkpoint is inserted. |
| T-02-SC2 | Tampering | @mui/material silently resolving to v9 | mitigate | Pin `7.3.11` exactly; acceptance asserts lockfile has no `mui/material@9` |
| T-02-ID | Information Disclosure | Google-Fonts CDN font load at capture time | mitigate | Self-host the font via `@fontsource/jetbrains-mono`; acceptance greps out any `fonts.googleapis.com` reference |
</threat_model>

## Artifacts this phase produces

- `.planning/design/mockup-src/` — pinned pnpm + Vite + React 19 + MUI v7.3.11 workspace (package.json, committed pnpm-lock.yaml, vite.config.ts base:'./', tsconfig.json, index.html, .gitignore, README.md)
- `.planning/design/mockup-src/src/theme.ts` — terminal-skin MUI v7 theme (monospace, shadows none, borderRadius 0, transitions 0ms, ANSI-safe semantic palette)
- `.planning/design/mockup-src/src/App.tsx` + `src/main.tsx` — HashRouter + `import.meta.glob` route auto-discovery (surfaces add files, never edit App.tsx)
- `.planning/design/mockup-src/src/shell/{Shell,Header,StatusLine,Keybar}.tsx` — the shared four-region app shell
- `.planning/design/mockup-src/src/data/recipeFixtures.ts` — typed recipe-accurate canonical copy for all surfaces
- `.planning/design/mockup-src/src/routes/_shell/shell-demo.route.tsx` — the shell demo route (only route this plan ships)
- `.planning/design/FIELDS.template.md`, `.planning/design/CRITIQUE.template.md` — per-surface semantic-parity manifest + critique-log templates

<verification>
- `pnpm install --frozen-lockfile && pnpm exec vite build` produces a static `dist/index.html` loadable via file:// with no network
- MUI pinned at 7.3.11 (lockfile has no v9); theme enforces the terminal skin; App uses HashRouter + glob discovery
- Recipe fixtures carry byte-accurate canonical copy (Port 443, IdentitiesOnly yes, includeIf hasconfig/gitdir, insteadOf, allowed_signers, sentinels) — no lorem
- FIELDS/CRITIQUE templates encode the semantic-parity matrix + 0-OPEN rule
</verification>

<success_criteria>
- DLV-01: an HTML/MUI-v7 mockup foundation exists that encodes the shell layout, terminal skin, and recipe copy every surface's fields/labels/copy will be authored from — built with the `/mui` skill under `agent-ui-ux-designer` direction.
- DLV-02: `/mui` + `agent-ui-ux-designer` are both engaged and named in every task of this UI plan.
- Foundation is fan-out-safe: surfaces add route files, dummy files, and manifests without editing any shared file.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-01-SUMMARY.md` when done.
</output>
