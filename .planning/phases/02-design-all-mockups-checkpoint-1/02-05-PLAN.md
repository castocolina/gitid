---
phase: 02-design-all-mockups-checkpoint-1
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/git-screen/FIELDS.md
  - .planning/design/git-screen/manifest.json
  - .planning/design/git-screen/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/git-screen/git-form-empty.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/git-form-filled.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/match-strategy-select.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/review-readonly.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/confirm-write.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/result-success.route.tsx
  - internal/dummytui/surface_gitscreen.go
  - internal/dummytui/surface_gitscreen_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The git-configuration screen is mocked (MUI v7) and dummied (TUI) across all 7 named states from UX-DIRECTION §4.2, with recipe-accurate copy"
    - "The allowed_signers email is shown BYTE-IDENTICAL to user.email in the review-readonly state, in both media, so a mismatch is visible not buried (GITUI-04)"
    - "Match strategy shows gitdir / hasconfig / both with a live includeIf preview, default gitdir (GITUI-03)"
    - "Both media captured to .planning/design/git-screen/{html,tui}/ (7 PNGs each); dummy-nav e2e reaches every git-screen screen; CRITIQUE.md 0 OPEN"
  artifacts:
    - path: ".planning/design/git-screen/FIELDS.md"
      provides: "semantic field-parity manifest for the 7 git-screen states"
      contains: "allowed_signers"
    - path: ".planning/design/git-screen/manifest.json"
      provides: "per-screen {htmlRoute, keys, signature}"
      contains: "match-strategy-select"
    - path: "internal/dummytui/surface_gitscreen.go"
      provides: "TUI dummy git-screen surface (7 screens), registered, backend-free"
      contains: "Register"
  key_links:
    - from: "src/routes/git-screen/review-readonly.route.tsx"
      to: "recipeFixtures allowed_signers + user.email"
      via: "byte-identical email shown side-by-side"
      pattern: "allowed_signers"
    - from: "internal/dummytui/surface_gitscreen.go"
      to: "dummytui.Register"
      via: "init()-registration, no model.go edit"
      pattern: "Register"
---

<objective>
Fan-out surface: the **git-configuration screen** (owned later by Phase 4). Repeat the proven per-surface pipeline (02-04): `FIELDS.md` → `manifest.json` → MUI mockup → TUI dummy surface → capture → `agent-ui-ux-designer` semantic-parity critique → `CRITIQUE.md` 0 OPEN, for all 7 states from UX-DIRECTION §4.2.

States (verbatim §4.2): `git-form-empty` · `git-form-filled` (`user.name`/`user.email`, `gpg.format=ssh`, `user.signingkey` as a PATH not literal, `commit.gpgsign`) · `match-strategy-select` (gitdir / hasconfig / both, live `includeIf` preview, default gitdir) · `review-readonly` (fragment + `includeIf` + `allowed_signers` together) · `confirm-write` · `backup-notice` · `result-success`.

Highest-risk affordance (§4.2): the `allowed_signers` email MUST be BYTE-IDENTICAL to `user.email` (GITUI-04) — show them side-by-side in `review-readonly` so a mismatch is visible.

DLV-02: author the MUI mockup with the `/mui` skill (v7, terminal-skin theme + shell) and the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + semantic-parity critique. Name both in every task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/gitconfig.recipe
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git-screen FIELDS.md + manifest.json + MUI mockup (7 states)</name>
  <files>.planning/design/git-screen/FIELDS.md, .planning/design/git-screen/manifest.json, .planning/design/mockup-src/src/routes/git-screen/git-form-empty.route.tsx, .planning/design/mockup-src/src/routes/git-screen/git-form-filled.route.tsx, .planning/design/mockup-src/src/routes/git-screen/match-strategy-select.route.tsx, .planning/design/mockup-src/src/routes/git-screen/review-readonly.route.tsx, .planning/design/mockup-src/src/routes/git-screen/confirm-write.route.tsx, .planning/design/mockup-src/src/routes/git-screen/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/git-screen/result-success.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.2 (7 states + allowed_signers byte-identical affordance), § 3 (MUST-match), § 5 (four-beat ceremony)
    - .planning/design/create-flow/FIELDS.md + a create-flow route file (02-04 — the proven format to copy)
    - .planning/design/mockup-src/src/data/recipeFixtures.ts (git fragment, includeIf hasconfig/gitdir, allowed_signers, gpg.format=ssh), .planning/design/FIELDS.template.md
    - recipes/gitconfig.recipe (includeIf hasconfig:remote.*.url + gitdir:, per-identity fragment fields), REQUIREMENTS.md GITUI-01..05 (per-identity fields, match strategy default gitdir, allowed_signers byte-identical, review→confirm→write)
  </read_first>
  <action>
    Using the `/mui` skill + `agent-ui-ux-designer` direction and the proven 02-04 format: author `.planning/design/git-screen/FIELDS.md` (7-state field/order/label/copy table), `manifest.json` (7 `{screen, htmlRoute, keys, signature}` entries), and 7 `*.route.tsx` files under `src/routes/git-screen/` rendering inside `<Shell>` with REAL recipe copy from `recipeFixtures` (no lorem): the git fragment fields (`user.name`, `user.email`, `gpg.format=ssh`, `user.signingkey` as a PATH, `commit.gpgsign`); the match-strategy selector (gitdir / hasconfig / both, DEFAULT gitdir, live `includeIf` preview); the read-only review showing fragment + `includeIf` + `allowed_signers` TOGETHER with the `allowed_signers` email rendered SIDE-BY-SIDE with `user.email` to make byte-identity visible (GITUI-04); confirm-write naming the target file `~/.gitconfig.d/<identity>` + sentinels; backup-notice with timestamped path; result-success. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/git-screen/manifest.json'))))") -eq 7</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 7 screens: `test $(python3 -c "import json;print(len(json.load(open('.planning/design/git-screen/manifest.json'))))") -eq 7` AND `grep -q 'match-strategy-select' .planning/design/git-screen/manifest.json`
    - 7 route files exist: `test $(ls .planning/design/mockup-src/src/routes/git-screen/*.route.tsx | wc -l) -eq 7`
    - allowed_signers byte-identity is shown in review: `grep -rq 'allowed_signers' .planning/design/mockup-src/src/routes/git-screen/` AND `grep -rq 'gpg.format' .planning/design/mockup-src/src/routes/git-screen/`
    - Match default is gitdir and hasconfig present: `grep -q 'gitdir' .planning/design/git-screen/FIELDS.md` AND `grep -q 'hasconfig' .planning/design/git-screen/FIELDS.md`
    - No lorem: `! grep -riq 'lorem' .planning/design/mockup-src/src/routes/git-screen/`
    - `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The git-screen MUI mockup renders all 7 recipe-accurate states with the allowed_signers byte-identity affordance, specified by FIELDS.md and enumerated by manifest.json.</done>
</task>

<task type="auto">
  <name>Task 2: git-screen TUI dummy surface (7 screens, registered, backend-free)</name>
  <files>internal/dummytui/surface_gitscreen.go, internal/dummytui/surface_gitscreen_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — the proven surface-registration + Render pattern to mirror)
    - internal/dummytui/registry.go (SurfaceDef/ScreenDef/Register/RenderScreen)
    - .planning/design/git-screen/FIELDS.md + manifest.json (Task 1 — the labels/copy/signatures to mirror)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (shell regions, color+glyph+word), § 4.2
  </read_first>
  <action>
    Write `surface_gitscreen.go` in `package dummytui` mirroring the 02-04 pattern: register the git-screen surface (a modal flow launched after the SSH create flow, per UX-DIRECTION §2) with 7 `ScreenDef`s whose IDs match `manifest.json`, `Keys` forming the flow (form → match-strategy → review → confirm → backup → result), and per-screen `Render` reproducing the SAME labels/copy/defaults as the MUI mockup (git fragment fields, `gpg.format=ssh`, `user.signingkey` PATH, match strategy default gitdir + live includeIf preview, the review showing `allowed_signers` email side-by-side with `user.email`, sentinel-visible confirm, backup path). NO backend import. Write `surface_gitscreen_test.go` asserting 7 registered screens, each RenderScreen non-empty + contains its signature, connected Keys path.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run GitScreen && ! (go list -deps ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)')</automated>
  </verify>
  <acceptance_criteria>
    - `surface_gitscreen.go` registers 7 screens: `grep -q 'Register' internal/dummytui/surface_gitscreen.go`; test asserts count 7
    - `go test -race ./internal/dummytui/... -run GitScreen` passes
    - Copy parity anchors: `grep -q 'gpg.format' internal/dummytui/surface_gitscreen.go` AND `grep -q 'allowed_signers' internal/dummytui/surface_gitscreen.go`
    - No backend import: `! grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)' internal/dummytui/surface_gitscreen.go`
    - `make lint` passes
  </acceptance_criteria>
  <done>The git-screen surface exists in the TUI dummy with all 7 screens registered, mirroring the mockup's copy/defaults, proven backend-free.</done>
</task>

<task type="auto">
  <name>Task 3: Capture git-screen (both media) + agent-ui-ux-designer parity critique → CRITIQUE.md 0 OPEN</name>
  <files>.planning/design/git-screen/CRITIQUE.md, .planning/design/git-screen/FIELDS.md</files>
  <read_first>
    - .planning/design/create-flow/CRITIQUE.md (02-04 — the proven critique format + resolution discipline)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review", .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.2, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md (make targets + -run filters)
  </read_first>
  <action>
    Build dist (`pnpm build`) then capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/git-screen' ./internal/screenshot/...` (writes 7 HTML + 7 TUI PNGs under `.planning/design/git-screen/{html,tui}/`), and run the dummy-nav e2e scoped to this surface (`go test -tags e2e -race -run 'TestDummyNav/git-screen' ./e2e/...`). Then spawn `agent-ui-ux-designer` to produce `.planning/design/git-screen/CRITIQUE.md` (aesthetic pass on HTML + §3 semantic-parity matrix HTML↔TUI); fill FIELDS.md HTML-present/TUI-present columns; resolve every divergence to 0 OPEN, giving the `allowed_signers`-byte-identity and match-strategy-default rows explicit parity checks. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/git-screen' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNav/git-screen' ./e2e/... && test $(ls .planning/design/git-screen/html/*.png | wc -l) -eq 7 && test $(ls .planning/design/git-screen/tui/*.png | wc -l) -eq 7 && ! grep -q 'OPEN' .planning/design/git-screen/CRITIQUE.md</automated>
  </verify>
  <acceptance_criteria>
    - 7 HTML + 7 TUI PNGs: `test $(ls .planning/design/git-screen/html/*.png | wc -l) -eq 7` AND `test $(ls .planning/design/git-screen/tui/*.png | wc -l) -eq 7`
    - The surface-scoped dummy-nav e2e passes (every git-screen screen reachable, zero writes)
    - `CRITIQUE.md` has 0 OPEN and records both passes: `! grep -q 'OPEN' .planning/design/git-screen/CRITIQUE.md` AND `grep -q 'RESOLVED' .planning/design/git-screen/CRITIQUE.md`
    - FIELDS.md parity columns filled for all 7 screens; allowed_signers-byte-identity is an explicit parity row
    - `make lint` passes
  </acceptance_criteria>
  <done>git-screen is captured in both media (7+7), navigable on the real binary, and its parity critique is resolved to 0 OPEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-screen mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the git-screen e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB4 | Tampering / Elevation of Privilege | git-screen dummy reaching a backend write path | mitigate | `surface_gitscreen.go` imports no backend package; inherits the 02-02 import-graph gate + 02-03 zero-write e2e |
| T-02-SIGN | Tampering | allowed_signers email diverging from user.email in the frozen reference | mitigate | The parity matrix pins allowed_signers==user.email as a MUST-match row; review-readonly shows them side-by-side; CRITIQUE.md 0 OPEN before approval |
| T-02-CONT | Tampering | managed-block containment not shown in the git write preview | mitigate | confirm-write renders the `# BEGIN/END gitid managed:` sentinels + target file path (§5) |
</threat_model>

## Artifacts this phase produces

- `.planning/design/git-screen/{FIELDS.md, manifest.json, CRITIQUE.md, html/*.png (7), tui/*.png (7)}`
- `.planning/design/mockup-src/src/routes/git-screen/*.route.tsx` — 7 MUI v7 route files
- `internal/dummytui/surface_gitscreen.go` (+ `_test.go`) — TUI dummy git-screen surface, 7 screens, registered, backend-free

<verification>
- 7 HTML + 7 TUI PNGs; surface-scoped dummy-nav e2e passes; CRITIQUE.md 0 OPEN
- allowed_signers byte-identity + match-strategy default gitdir are explicit parity rows
- Dummy imports zero backend packages; no lorem copy
</verification>

<success_criteria>
- DLV-01/DLV-05 for git-screen: HTML mockup + navigable backend-free TUI dummy, both captured, parity resolved.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-05-SUMMARY.md` when done.
</output>
