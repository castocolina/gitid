---
phase: 02-design-all-mockups-checkpoint-1
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/git-screen/FIELDS.md
  - .planning/design/git-screen/manifest.json
  - .planning/design/git-screen/parity.json
  - .planning/design/git-screen/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/git-screen/git-form-empty.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/git-form-filled.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/match-strategy-select.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/review-readonly.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/confirm-write.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/git-screen/result-success.route.tsx
  - internal/dummytui/surface_gitscreen.go
  - internal/dummytui/surface_gitscreen_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The git-configuration screen is mocked (MUI v7) and dummied (TUI) across all 7 named states from UX-DIRECTION §4.2, with recipe-accurate copy; the TUI surface is a KEYLESS modal that declares a TARGET-OWNED launch binding (LaunchFrom identity-manager + LaunchKey) so it is launched FROM Identities and reachable on the real binary via the 02-02 launch mechanism"
    - "The allowed_signers email is shown BYTE-IDENTICAL to user.email in review-readonly, in both media, so a mismatch is visible (GITUI-04)"
    - "Match strategy shows gitdir / hasconfig / both with a live includeIf preview, default gitdir (GITUI-03)"
    - "Both media captured to .planning/design/git-screen/{html,tui}/ (7 PNGs each) with the breadcrumb gate; dummy-nav e2e reaches every git-screen screen THROUGH the launch key in keysFromHome; parity.json has no unresolved row"
  artifacts:
    - path: ".planning/design/git-screen/manifest.json"
      provides: "hardened per-screen {surface, screen, htmlRoute, keysFromHome, signature} (keysFromHome absolute from startup incl. the launch key)"
      contains: "match-strategy-select"
    - path: ".planning/design/git-screen/parity.json"
      provides: "machine-checkable §3 parity rows + allowed_signers-byte-identity row (gate: no row status != resolved)"
      contains: "status"
    - path: "internal/dummytui/surface_gitscreen.go"
      provides: "TUI dummy git-screen surface (7 screens), keyless modal with a LaunchFrom/LaunchKey binding, registered, backend-free"
      contains: "LaunchFrom"
  key_links:
    - from: "src/routes/git-screen/review-readonly.route.tsx"
      to: "recipeFixtures allowed_signers + user.email"
      via: "byte-identical email shown side-by-side"
      pattern: "allowed_signers"
    - from: "internal/dummytui/surface_gitscreen.go"
      to: "dummytui.Register + the 02-02 launch mechanism"
      via: "init()-registration (keyless modal flow) declaring LaunchFrom identity-manager + LaunchKey, so route() launches it from Identities; no model.go/data.go edit"
      pattern: "LaunchFrom"
---

<objective>
Fan-out surface: the **git-configuration screen** (owned later by Phase 4). Repeat the proven per-surface pipeline (02-04): `FIELDS.md` → `manifest.json` (hardened schema) → MUI mockup → TUI dummy → capture → `agent-ui-ux-designer` critique → `parity.json` 0-unresolved, for all 7 states from UX-DIRECTION §4.2.

States (verbatim §4.2): `git-form-empty` · `git-form-filled` (`user.name`/`user.email`, `gpg.format=ssh`, `user.signingkey` as a PATH, `commit.gpgsign`) · `match-strategy-select` (gitdir / hasconfig / both, live `includeIf` preview, default gitdir) · `review-readonly` (fragment + `includeIf` + `allowed_signers` together) · `confirm-write` · `backup-notice` · `result-success`.

Highest-risk affordance (§4.2): the `allowed_signers` email MUST be BYTE-IDENTICAL to `user.email` (GITUI-04) — show them side-by-side in `review-readonly`.

Review folded in (Codex 02-REVIEWS.md): HIGH-4 the parity gate is a structured `parity.json` (no row status != resolved); HIGH-3(c) hardened manifest schema (unique screen/htmlRoute/signature + absolute keysFromHome); HIGH-1 the ALLOWLIST no-backend proof; MEDIUM-10 fan-out isolation — this plan writes ONLY its own surface files (its design dir, its 7 route files, surface_gitscreen.go + _test.go); it does NOT edit App.tsx/model.go/data.go/package.json/pnpm-lock.yaml/Makefile or any other surface's files (git-screen is a keyless modal → plain `Register` + a TARGET-OWNED launch binding in its own file, so no data.go placeholder to replace and no shared-file edit). The shared `dist/` build output is gitignored and rebuilt per capture; if wave-4 plans run concurrently, serialize BOTH the `pnpm build` step AND the whole-module `make lint` (review M2 — both are whole-module operations with no per-surface isolation, so concurrent runs can false-fail; the authoritative full capture is the serial pass in 02-11). Review-spec iteration 2 folded in: C3 — git-screen declares `LaunchFrom: "identity-manager"` + `LaunchKey` (e.g. `g`) so the 02-02 `route()` launches it FROM Identities; its `keysFromHome` are REAL absolute keystroke sequences (re-home `1` → the launch key → intra-modal keys), so 02-03/02-11's dummy-nav e2e reaches every git-screen screen through the running binary (not via a direct RenderScreen call).

DLV-02: author the MUI mockup with `/mui` (v7, terminal-skin theme + shell + breadcrumb) and the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both in every task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/gitconfig.recipe
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git-screen FIELDS.md + manifest.json (hardened, absolute keysFromHome via the launch key) + parity.json seed + MUI mockup (7 states)</name>
  <files>.planning/design/git-screen/FIELDS.md, .planning/design/git-screen/manifest.json, .planning/design/git-screen/parity.json, .planning/design/mockup-src/src/routes/git-screen/git-form-empty.route.tsx, .planning/design/mockup-src/src/routes/git-screen/git-form-filled.route.tsx, .planning/design/mockup-src/src/routes/git-screen/match-strategy-select.route.tsx, .planning/design/mockup-src/src/routes/git-screen/review-readonly.route.tsx, .planning/design/mockup-src/src/routes/git-screen/confirm-write.route.tsx, .planning/design/mockup-src/src/routes/git-screen/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/git-screen/result-success.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.2 (7 states + allowed_signers byte-identical affordance), § 3 (MUST-match), § 5 (four-beat ceremony), § 2 (git-screen is a modal flow launched FROM Identities; the KEY-ALLOCATION TABLE — single authority: git-screen's LaunchKey is `g`; do NOT invent a key, the 02-02 registration guard rejects a colliding one — review iter-3)
    - .planning/design/create-flow/{FIELDS.md, manifest.json, parity.json} + a create-flow route (02-04 — the proven format to copy: hardened manifest with keysFromHome via the launch key + parity.json seed + route title `<surface>/<screen>`)
    - .planning/design/mockup-src/src/data/recipeFixtures.ts (git fragment, includeIf hasconfig/gitdir, allowed_signers, gpg.format=ssh), .planning/design/FIELDS.template.md, .planning/design/PARITY.template.json
    - recipes/gitconfig.recipe (includeIf hasconfig:remote.*.url + gitdir:, per-identity fragment fields), REQUIREMENTS.md GITUI-01..05
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` and the proven 02-04 format: author `.planning/design/git-screen/FIELDS.md` (7-state field/order/label/copy table), `manifest.json` (7 hardened `{surface:"git-screen", screen, htmlRoute, keysFromHome, signature}` entries — unique screen/htmlRoute/signature; route titles `git-screen/<screen>`; `keysFromHome` ABSOLUTE from startup THROUGH the 02-02 launch mechanism — first `1` (re-home to Identities), then the git-screen LAUNCH key Task 2 declares (`g`, allocated in the UX-DIRECTION §2 key-allocation table, review iter-3) that opens the modal, then the intra-modal transition keys), and copy `PARITY.template.json` to `.planning/design/git-screen/parity.json` seeding the §3 rows + an `allowed-signers-byte-identity` row + a `match-strategy-default-gitdir` row. Author 7 `*.route.tsx` under `src/routes/git-screen/` inside `<Shell>` with REAL recipe copy (no lorem): the git fragment fields (`user.name`, `user.email`, `gpg.format=ssh`, `user.signingkey` as a PATH, `commit.gpgsign`); the match-strategy selector (gitdir / hasconfig / both, DEFAULT gitdir, live `includeIf` preview); the read-only review showing fragment + `includeIf` + `allowed_signers` TOGETHER with the `allowed_signers` email SIDE-BY-SIDE with `user.email` (GITUI-04); confirm-write naming `~/.gitconfig.d/<identity>` + sentinels; backup-notice; result-success. Rebuild dist (`pnpm build`); confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/git-screen/manifest.json'))))") -eq 7 && python3 -c "import json;m=json.load(open('.planning/design/git-screen/manifest.json'));assert len(set(e['screen'] for e in m))==7 and len(set(e['signature'] for e in m))==7 and all(e.get('keysFromHome') for e in m) and all(e['keysFromHome'][0]=='1' for e in m)"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 7 screens in the hardened schema (unique screen+signature, non-empty keysFromHome — asserted by verify) AND `grep -q 'keysFromHome' .planning/design/git-screen/manifest.json` AND `grep -q 'match-strategy-select' .planning/design/git-screen/manifest.json`
    - keysFromHome are ABSOLUTE through the launch mechanism (review C3): every entry's keysFromHome starts with `1` (re-home to Identities) followed by the git-screen launch key, then intra-modal keys (asserted by the verify python's `keysFromHome[0]=='1'` check)
    - `parity.json` seeded with §3 rows + allowed-signers row: `python3 -c "import json;r=json.load(open('.planning/design/git-screen/parity.json'));assert len(r)>=8"`
    - 7 route files exist: `test $(ls .planning/design/mockup-src/src/routes/git-screen/*.route.tsx | wc -l) -eq 7`
    - allowed_signers byte-identity shown in review: `grep -rq 'allowed_signers' .../git-screen/` AND `grep -rq 'gpg.format' .../git-screen/`
    - Match default is gitdir and hasconfig present: `grep -q 'gitdir' .planning/design/git-screen/FIELDS.md` AND `grep -q 'hasconfig' .planning/design/git-screen/FIELDS.md`
    - No lorem: `! grep -riq 'lorem' .../git-screen/`; `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The git-screen MUI mockup renders all 7 recipe-accurate states with the allowed_signers byte-identity affordance, specified by FIELDS.md, enumerated by a hardened manifest.json whose keysFromHome reach each screen through the launch mechanism, with a seeded parity.json.</done>
</task>

<task type="auto">
  <name>Task 2: git-screen TUI dummy surface (7 screens, keyless modal, launch binding, registered, backend-free)</name>
  <files>internal/dummytui/surface_gitscreen.go, internal/dummytui/surface_gitscreen_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — the proven keyless-modal surface with a LaunchFrom/LaunchKey binding + Render pattern to mirror)
    - internal/dummytui/registry.go (SurfaceDef with LaunchFrom/LaunchKey/ScreenDef/Register/RegisterOrReplace/RenderScreen; the placeholder identity-manager surface — ID `identity-manager` — a launch binding targets)
    - .planning/design/git-screen/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures; keysFromHome encodes the launch key this task declares)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (shell regions, breadcrumb, color+glyph+word; git-screen launched FROM Identities; the KEY-ALLOCATION TABLE — git-screen LaunchKey = `g`, the single authority to declare against; the 02-02 guard rejects a colliding key), § 4.2
  </read_first>
  <action>
    Per DLV-02 (this is a UI task): this dummy screen reproduces the `/mui`-built mockup (the parity source) and `agent-ui-ux-designer` validates the terminal-skin cross-media parity (UX-DIRECTION §2). Write `surface_gitscreen.go` in `package dummytui` mirroring 02-04: register the git-screen surface via plain `Register` with an EMPTY `ActivationKey` (a keyless modal flow launched FROM Identities, per UX-DIRECTION §2 — NOT a number-key view, so no placeholder to replace and no data.go edit; 02-02 EXEMPTS the empty ActivationKey from the duplicate-key check — review H2 — so git-screen + create-flow both register keyless without collision) AND a TARGET-OWNED launch binding `LaunchFrom: "identity-manager"` + `LaunchKey` (the same key encoded in manifest.json's keysFromHome — `g`, allocated in the UX-DIRECTION §2 key-allocation table, review iter-3; the 02-02 registration guard rejects a colliding LaunchKey) — review C3: this is what makes 02-02's `route()` launch git-screen from Identities and what 02-03/02-11's e2e drive; the binding lives on THIS surface's own file, so no data.go/model.go/identity-manager-plan edit. Define 7 `ScreenDef`s whose IDs match `manifest.json`, `Keys` forming the flow (form → match-strategy → review → confirm → backup → result — the same transitions keysFromHome walk), and per-screen `Render` reproducing the SAME labels/copy/defaults as the MUI mockup (git fragment fields, `gpg.format=ssh`, `user.signingkey` PATH, match strategy default gitdir + live includeIf preview, the review showing `allowed_signers` side-by-side with `user.email`, sentinel-visible confirm, backup path). NO backend import. Write `surface_gitscreen_test.go` asserting 7 registered screens; keyless (empty ActivationKey) with LaunchFrom=="identity-manager" + non-empty LaunchKey; each RenderScreen non-empty + contains its signature + the `git-screen/<id>` breadcrumb; connected Keys path.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run GitScreen && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `surface_gitscreen.go` registers 7 screens: `grep -q 'Register' internal/dummytui/surface_gitscreen.go`; test asserts count 7
    - It is a keyless modal with a launch binding (review C3): `grep -q 'LaunchFrom' internal/dummytui/surface_gitscreen.go` AND `grep -q 'LaunchKey' internal/dummytui/surface_gitscreen.go`; the test asserts empty ActivationKey + LaunchFrom=="identity-manager" + non-empty LaunchKey
    - `go test -race ./internal/dummytui/... -run GitScreen` passes (each RenderScreen contains its signature + the `git-screen/<id>` breadcrumb)
    - Copy parity anchors: `grep -q 'gpg.format' internal/dummytui/surface_gitscreen.go` AND `grep -q 'allowed_signers' internal/dummytui/surface_gitscreen.go`
    - No-backend ALLOWLIST holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - This plan touched no shared file — git-screen is a keyless modal (plain Register + its own launch binding), so no data.go/model.go/App.tsx/package.json/lockfile/Makefile edit (review H1 — exit-code-honest, matching 02-11's idiom): `[ -z "$(git diff --name-only -- internal/dummytui/data.go internal/dummytui/model.go .planning/design/mockup-src/src/App.tsx .planning/design/mockup-src/package.json .planning/design/mockup-src/pnpm-lock.yaml Makefile)" ]` (FAILS if any shared file was modified); `make lint` passes
  </acceptance_criteria>
  <done>The git-screen surface exists in the TUI dummy with all 7 screens registered as a keyless modal flow that declares its LaunchFrom/LaunchKey launch binding (launched from Identities, reachable on the real binary via the 02-02 mechanism), mirroring the mockup's copy/defaults, proven backend-free by the allowlist, with no shared-file edits.</done>
</task>

<task type="auto">
  <name>Task 3: Capture git-screen (both media) + agent-ui-ux-designer critique → parity.json 0-unresolved</name>
  <files>.planning/design/git-screen/CRITIQUE.md, .planning/design/git-screen/FIELDS.md, .planning/design/git-screen/parity.json</files>
  <read_first>
    - .planning/design/create-flow/{CRITIQUE.md, parity.json} (02-04 — the proven critique + structured-parity resolution discipline)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review", .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.2, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md (make targets + -run filters + the launch-driven dummy-nav e2e)
  </read_first>
  <action>
    Build dist (`pnpm build`) then capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/git-screen' ./internal/screenshot/...` (7 HTML + 7 TUI PNGs under `.planning/design/git-screen/{html,tui}/`, each save gated on the `git-screen/<screen>` breadcrumb), and run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/git-screen' ./e2e/...`) which reaches each git-screen screen through the launch key in keysFromHome. Then spawn `agent-ui-ux-designer`: record the HTML-aesthetic pass in `CRITIQUE.md` (human narrative) and fill `.planning/design/git-screen/parity.json` — one row per §3 dimension + the `allowed-signers-byte-identity` and `match-strategy-default-gitdir` rows — resolving every divergence so NO row has `status != resolved`. Fill FIELDS.md HTML/TUI columns. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/git-screen' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/git-screen' ./e2e/... && test $(ls .planning/design/git-screen/html/*.png | wc -l) -eq 7 && test $(ls .planning/design/git-screen/tui/*.png | wc -l) -eq 7 && python3 -c "import json;r=json.load(open('.planning/design/git-screen/parity.json'));import sys;sys.exit(1 if (not r or [x for x in r if x.get('status')!='resolved']) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - 7 HTML + 7 TUI PNGs: `test $(ls .planning/design/git-screen/html/*.png | wc -l) -eq 7` AND `test $(ls .planning/design/git-screen/tui/*.png | wc -l) -eq 7`
    - The surface-scoped dummy-nav e2e passes (every git-screen screen reachable through the launch key, breadcrumb+signature asserted, zero writes)
    - `parity.json` has no unresolved row and includes the allowed-signers-byte-identity + match-strategy-default rows: `python3 -c "import json;r=json.load(open('.planning/design/git-screen/parity.json'));assert r and all(x['status']=='resolved' for x in r)"`
    - `CRITIQUE.md` records the aesthetic pass + links parity.json; FIELDS.md columns filled for all 7 screens
    - `make lint` passes
  </acceptance_criteria>
  <done>git-screen is captured in both media (7+7), navigable on the real binary through the launch mechanism, and its parity is a machine-checkable parity.json with no unresolved row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-screen mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the git-screen e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB4 | Tampering / Elevation of Privilege | git-screen dummy reaching a backend write path | mitigate | `surface_gitscreen.go` imports no backend package; inherits the 02-02 import-graph ALLOWLIST + 02-03 zero-write e2e |
| T-02-ML4 | Repudiation | git-screen screens appearing covered while unreachable on the real binary | mitigate | The surface declares a LaunchFrom/LaunchKey binding; manifest keysFromHome drive that launch key; the surface-scoped dummy-nav e2e asserts each git-screen breadcrumb through the running binary (review C3) |
| T-02-SIGN | Tampering | allowed_signers email diverging from user.email in the frozen reference | mitigate | parity.json pins allowed_signers==user.email as a machine-checkable row; review-readonly shows them side-by-side; gate requires no unresolved row |
| T-02-CONT | Tampering | managed-block containment not shown in the git write preview | mitigate | confirm-write renders the `# BEGIN/END gitid managed:` sentinels + target file path (§5) |
</threat_model>

## Artifacts this phase produces

- `.planning/design/git-screen/{FIELDS.md, manifest.json (hardened; keysFromHome via the launch key), parity.json, CRITIQUE.md, html/*.png (7), tui/*.png (7)}`
- `.planning/design/mockup-src/src/routes/git-screen/*.route.tsx` — 7 MUI v7 route files (titles `git-screen/<screen>`)
- `internal/dummytui/surface_gitscreen.go` (+ `_test.go`) — TUI dummy git-screen keyless modal surface, 7 screens, LaunchFrom/LaunchKey binding, registered, backend-free

<verification>
- 7 HTML + 7 TUI PNGs; surface-scoped dummy-nav e2e passes (reached through the launch key); parity.json no unresolved row
- allowed_signers byte-identity + match-strategy default gitdir are parity.json rows
- Dummy passes the no-backend allowlist; the surface declares its LaunchFrom/LaunchKey launch binding; no lorem; no shared-file edits
</verification>

<success_criteria>
- **Code review (required before this plan is done):** on completion, request a fresh-context code review via the `superpowers:requesting-code-review` skill, verifying the work against this plan `must_haves` and every task `<acceptance_criteria>`; resolve all CRITICAL/HIGH findings before the plan is marked complete. Supplements — not replaces — the phase-level `/gsd-code-review` and the phase-level external cross-vendor (Codex) review the execution loop runs.
- DLV-01/DLV-05 for git-screen: HTML mockup + navigable backend-free TUI dummy launched from Identities via the 02-02 mechanism, both captured, parity resolved in parity.json.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-05-SUMMARY.md` when done.
</output>
</content>
