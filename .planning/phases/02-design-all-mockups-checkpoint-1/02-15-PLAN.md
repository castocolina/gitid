---
phase: 02-design-all-mockups-checkpoint-1
plan: 15
type: execute
wave: 8
depends_on: [02-14]
files_modified:
  - internal/dummytui/identities.go
  - internal/dummytui/identities_test.go
  - internal/dummytui/theme.go
  - internal/dummytui/theme_test.go
  - internal/dummytui/frame.go
  - internal/dummytui/frame_test.go
  - internal/dummytui/app.go
  - internal/dummytui/app_test.go
  - internal/dummytui/globalgit.go
  - internal/dummytui/globalgit_test.go
  - internal/dummytui/globalssh.go
  - internal/dummytui/globalssh_test.go
  - internal/dummytui/batch3_test.go
  - e2e/dummy_demo_e2e_test.go
  - .planning/design/mockup-src/src/theme.ts
  - .planning/design/mockup-src/src/demo/DemoApp.tsx
  - .planning/design/mockup-src/src/demo/Frame.tsx
  - .planning/design/mockup-src/src/demo/screens/Identities.tsx
  - .planning/design/mockup-src/src/demo/screens/GlobalGit.tsx
  - .planning/design/mockup-src/src/demo/MutationCeremony.tsx
  - .planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md
  - .planning/design/create-flow/FIELDS.md
  - .planning/design/global-git/FIELDS.md
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "D1: In BOTH demos EVERY form field (wizard steps, Edit SSH, Configure Git, Clone) is ONE constant-height row in every state — focus is signalled by accent color on the value + bracket delimiters plus a redundant non-color cue (`▸` marker TUI / 2px outline WEB), NEVER by a reflowing box; `renderFocusedFieldBox` no longer exists in identities.go and no field grows or shrinks on focus"
    - "D2: Both radio groups (algorithm step-1, match-strategy git-step) render ALL options at constant height with the `(←/→ change)` hint on the group HEADER line, visible in BOTH focus states — no expand/collapse branch remains in `gitForm.view`; the web renders both as MUI `RadioGroup`s (the strategy Select and the algorithm Select are gone)"
    - "D3: Frozen glyphs are shared named constants — checkbox `☑`/`☐`, radio `●`/`○` — and the web renders them via theme-level `MuiCheckbox`/`MuiRadio` glyph overrides so no stock Material icons remain; disabled glyphs render through the hint role"
    - "D4: MAIN NAV renders `[1] Identities · [2] Global SSH · [3] Global Git · [4] Doctor` in both media; plain ←/→ switch top-level views 1–4 ONLY when the active screen leaves them unhandled (capturing panes and Global SSH's ←/→ sub-tabs unaffected — precedence clause 4); the current view under a capturing pane renders the NEW `ActiveNavDimmed`/`activeNavDimmed` state (accent foreground, NO background fill); a `←→ switch view` contextual footer advertises it on top-level non-capturing states"
    - "D5: The wizard stepper reverts to `Step n/4 · <label> ● ○ ○ ○` (long labels) — counter bold, `·` faint, active label bold+accent (`styleStepperActive`), dots `●` accent for ≤ step / `○` faint; the line is NEVER faint as a whole; a single always-visible faint line directly under the stepper carries the step-conditional Shift-chord hint"
    - "D6: The Git step renders `Back (Esc)  [ Skip Git ]  [ Continue ]` on ONE row with both frozen hint lines always visible BELOW the row"
    - "D7: ONE hoisted chord gate at the top of `handleWizardKey` handles Shift+←/→ at EVERY step including the previously-DEAD review step 3 (Shift+← 3→2 back to Git); the four scattered per-step chord cases are gone; forward (Shift+→) is validity-gated and, when blocked, emits the frozen status note naming the gate; `[ Continue ]` disabled suffix reads `— needs user.name + a valid email`; a raw-byte PTY test injects `\\x1b[1;2D`/`\\x1b[1;2C` at each step and asserts the step index moves"
    - "D8: Clicking anywhere on a TUI form-field row focuses it; clicking a radio option row selects that option (strategy click also focuses `gitFieldStrategy`); clicking a checkbox row toggles it; disabled algorithm rows are inert — hit zones derive from the rendered label strings, never magic row numbers"
    - "D9: Global Git `user.email (global fallback)` is a first-class EDITABLE field + `☐/☑` apply checkbox — unset/unchecked by default (recipes default preserved) — that joins the apply ceremony; its helper/advisory copy frames it as a fallback that identity includeIf fragments always override, and the divergence from recipes/ is documented in FIELDS.md + 02-STYLE-SPEC.md"
    - "Affordance audit: every pane in the audit table carries its contextual-footer affordance with the frozen copy verbatim (Edit SSH / Configure Git / Clone panes gain a footer they lacked); no non-obvious interaction is undocumented on screen (the transversal principle)"
    - "All exit gates green: `go test -race ./internal/dummytui/...`, `make test`, `make lint`, `make test-e2e` (100×30 PTY walk re-proving the tightest pane lands ≤ ~21 of 25 body rows after the reflow removal), `make gate-no-backend-files`, the extended copy-freeze grep, `pnpm typecheck && pnpm build`, and the new raw-byte Shift-chord PTY e2e"
  artifacts:
    - path: "internal/dummytui/identities.go"
      provides: "single-row `formFieldLine` (no `renderFocusedFieldBox`), always-expanded radios, `Step n/4` `renderStepper`, one-row git buttons, hoisted Shift chord gate + `stepBack`/`stepForward` helpers, click-to-focus field/radio/checkbox hit-testing"
      contains: "Step "
    - path: "internal/dummytui/theme.go"
      provides: "`FieldFocused` = accent foreground + bold, NO border (D1); new `ActiveNavDimmed` role (D4)"
      contains: "ActiveNavDimmed"
    - path: "internal/dummytui/frame.go"
      provides: "`[%d] %s` `headerTabText`, four-state nav (Active/ActiveNavDimmed/DisabledNav/plain), `←→ switch view` contextual footer"
      contains: "[%d] %s"
    - path: "internal/dummytui/app.go"
      provides: "top-level plain ←/→ view switch (clamped 1..4) that fires only when the active screen leaves the key unhandled"
      contains: "left"
    - path: "internal/dummytui/globalgit.go"
      provides: "editable `user.email (global fallback)` field + apply checkbox joining the apply ceremony, with the includeIf-precedence invariant preserved"
      contains: "global fallback"
    - path: ".planning/design/mockup-src/src/theme.ts"
      provides: "`MuiCheckbox`/`MuiRadio` terminal-glyph overrides (D3), `activeNavDimmed` role (D4), single-row field sx (D1)"
      contains: "activeNavDimmed"
    - path: ".planning/design/mockup-src/src/demo/screens/Identities.tsx"
      provides: "both Selects replaced by RadioGroups (D2), `Step n/4` StepDots revert + conditional chord sub-line (D5/D7), one-row buttons + reason text (D6/D7), single-row fieldSx (D1)"
      contains: "Step "
    - path: ".planning/design/mockup-src/src/demo/screens/GlobalGit.tsx"
      provides: "editable `user.email (global fallback)` TextField + terminal-glyph Checkbox wired into applyChosen + ceremony annotation (D9)"
      contains: "global fallback"
    - path: ".planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md"
      provides: "§3/§5/§7 rewritten in lockstep with D1/D5/audit, plus a `Conscious divergences from recipes/` note for D9"
      contains: "Conscious divergences from recipes/"
  key_links:
    - from: "internal/dummytui/identities.go formFieldLine"
      to: "internal/dummytui/theme.go FieldFocused (color-only, no border)"
      via: "focus is color + bracket + marker, never a reflowing box"
      pattern: "FieldFocused"
    - from: "internal/dummytui/app.go top-level ←/→"
      to: "02-STYLE-SPEC.md §2 arrow-key precedence rule"
      via: "fires only after the active screen returns handled=false (capturing panes + Global SSH sub-tabs unaffected)"
      pattern: "left|right"
    - from: "internal/dummytui/identities.go hoisted chord gate"
      to: "step-3 review ceremony (previously dead)"
      via: "Shift+← reaches stepBack from inside the ceremony; Shift+→ shares the SAME validity predicate as plain-right"
      pattern: "shift\\+left|shift\\+right"
    - from: "internal/dummytui/globalgit.go user.email (global fallback)"
      to: "includeIf-precedence invariant"
      via: "the fallback is used only where no identity matches; identity fragments always override — pinned by the ceremony copy + FIELDS.md/STYLE-SPEC divergence note"
      pattern: "global fallback"
---

<objective>
Operationalize the BINDING checkpoint-2 design contract
(`02-DESIGN-DECISIONS-CHECKPOINT-2.md`, D1–D9 + affordance audit — user-approved
at the 02-12 checkpoint) as ONE route-back polish wave before the DLV-08 approval
is re-presented. The contract is authoritative: this plan does not re-decide any
D-item or audit row — it implements them and moves the docs/tests in lockstep.

**This plan explicitly SUPERSEDES the parts of 02-14 that the contract overturns.**
02-14 shipped the 3-row `renderFocusedFieldBox` field contour and the bracketed
`[1] SSH · [2] Test · [3] Git · [4] Review` WIZARD stepper. The contract reverses
both: D1 kills the box (every field is one constant-height row, focus = color
only) and D5 reverts the wizard stepper to `Step n/4 · <label> ● ○ ○ ○`, moving the
bracket format onto the MAIN NAV per D4. Where 02-14's must_haves/tests froze those
superseded decisions, this plan updates them and says so.

Purpose: close the checkpoint-2 change requests the 02-12 human review raised, honor
the transversal principle (everything non-obvious is documented on screen in BOTH
demos), and re-prove the 100×30 row budget after the reflow removal — so 02-12 can
re-present two demos that match the approved contract.

Output: the TUI demo reflowed to the contract (single-row fields, always-expanded
radios, reverted stepper, one-row git buttons, one hoisted Shift chord gate,
click-to-focus, editable global-fallback user.email, affordance-audit footers); the
web demo mirrored 1:1 (RadioGroups, glyph checkbox/radio, nav mirror, StepDots
revert, editable user.email); 02-STYLE-SPEC.md §3/§5/§7 rewritten + a recipes/
divergence note; FIELDS.md rows updated; every gate re-run green.

DLV-02: the web half is refined with `/mui` under `agent-ui-ux-designer` direction;
the TUI mirrors it 1:1; a FRESH `agent-ui-ux-designer` critique + a fresh-context
code review are ORCHESTRATOR-run exit gates (the executor cannot spawn agents —
name them, do not attempt to run them yourself).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-DESIGN-DECISIONS-CHECKPOINT-2.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-14-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TUI demo — reflow to the checkpoint-2 contract (D1/D2/D4/D5/D6/D7/D8/D9 + audit footers) with all Go/e2e pins moved</name>
  <files>internal/dummytui/identities.go, internal/dummytui/identities_test.go, internal/dummytui/theme.go, internal/dummytui/theme_test.go, internal/dummytui/frame.go, internal/dummytui/frame_test.go, internal/dummytui/app.go, internal/dummytui/app_test.go, internal/dummytui/globalgit.go, internal/dummytui/globalgit_test.go, internal/dummytui/globalssh.go, internal/dummytui/globalssh_test.go, internal/dummytui/batch3_test.go, e2e/dummy_demo_e2e_test.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-DESIGN-DECISIONS-CHECKPOINT-2.md (THE CONTRACT — every D-item template + the affordance-audit table + row-budget math; implement from here, do NOT re-derive or soften any item)
    - internal/dummytui/identities.go (current post-02-14 state: `formFieldLine`+`renderFocusedFieldBox` at 288-334 → collapse to one row and DELETE the box; `sshForm.view` 349-388 + `gitForm.view` 512-559 field/radio rendering; the expand/collapse strategy branch at 536-546; `wizardSteps` 569 (long, keep) + `stepShortLabels` 574 + `styleStepperActive` 1845 + `renderStepper` 1851-1865 (revert); `handleWizardKey` 1297-1523 with the FOUR scattered Shift cases at 1324/1331, 1404/1411, 1460/1467 and the step-3 default at 1501 that swallows the chord — hoist ONE gate; the git-step button rows at 1970-1974; `wizardSkipButton`/`wizardContinueButton` 1817-1818; `handleClick` 1547-1609)
    - internal/dummytui/theme.go (current `FieldFocused` at 64-66 = RoundedBorder+BorderForeground → change to accent fg + bold, NO border; `ActiveNav` 75-78 + `DisabledNav` 73 → add the NEW `ActiveNavDimmed` = bold + ANSI-4 foreground, no bg)
    - internal/dummytui/frame.go (`headerTabText` 228-230 `" %d %s "` → `" [%d] %s "`; `renderHeader` 240-260 → add the 4th nav state; `tabLabels` 71 = Identities/Global SSH/Global Git/Doctor; `FooterAction` + `renderFooterLine` 286-292 for the contextual footers)
    - internal/dummytui/app.go (the `handleKey` globals-last block 285-295 with `case "1","2","3","4"` — add the top-level ←/→ switch here, AFTER the screen handler returns unhandled at 275-283, so capturing panes and Global SSH sub-tabs keep their arrows)
    - internal/dummytui/ceremony.go (D7 root cause: `handleKey` 130-165 `default:` swallows the chord — this is why step-3 review is DEAD; D9 diff annotation rides in the ceremonyConfig Preview/Heading; the ceremony footer audit rows `Tab/←→ Cancel / Confirm` + `Enter confirm` are set by host `screenView.actions`, not here)
    - internal/dummytui/globalgit.go (current user.email is awareness-only, never checkable at 133 + comment 8-9 → D9 makes it editable + applicable; `overlaidGitOptions`/`gitApplyChosen`/`baselineCeremony` + `handleKey`/`handleClick`/`view`)
    - internal/dummytui/globalssh.go (audit footers: `space toggle`, keep `←→ Options / Storage`, add `↑↓ layout`; the `FooterAction`/`capturesKeys` plumbing at 542-554)
    - internal/dummytui/identities_test.go (the pins that MOVE: `[1] SSH`/`[2] Test`/`[3] Git`/`[4] Review` at 119/131/237/347, the `renderStepper` test 812-823, the field-contour `TestWizardFieldContour*` box pins, the button pins 372/442/721/799) ; internal/dummytui/batch3_test.go (click pin 87, the arrow tests 337-374) ; e2e/dummy_demo_e2e_test.go (stepper mustSee 209/222/231 + the raw-byte `sendKey` harness + byte constants 129-134)
    - CLAUDE.md (TDD authoring order; English-only; never `--no-verify`; recipes/ North Star)
  </read_first>
  <behavior>
    - D1 field contour: `formFieldLine` returns exactly ONE row in every state. Focused → `▸ ` accent marker + bold label `padRight(label,16)` + `[`+`input.View()`+`]` wrapped in the new color-only `Theme.FieldFocused` (accent fg + bold). Blurred → `  ` gutter + bold label + `[`+`input.Value()`+`]` wrapped in `Theme.FieldBlurred` (Faint). Locked → dim value + faint `(locked)`, NO brackets. A test asserts NO field ever renders more than one line and that `renderFocusedFieldBox` is gone (its box top-border SGR never appears in a rendered form).
    - D2 radios: `gitForm.view` renders all three strategy options ALWAYS (no expand/collapse branch); the `(←/→ change)` hint sits on the `▸ Match strategy` HEADER line in BOTH focus states; the algorithm group likewise. A test asserts the strategy option count is constant across focused/blurred and that `(←/→ change)` is present when blurred.
    - D4 nav: `headerTabText(i)` renders `[1] Identities` … `[4] Doctor`; when `capturesKeys` and the tab is active it renders `Theme.ActiveNavDimmed` (accent fg, no bg); plain ←/→ at the top level move the tab index and clamp at 1 and 4, firing only when the active screen returned unhandled. Tests: header SGR for the active-dimmed state differs from the full `ActiveNav` background; a top-level ←/→ from Identities detail advances/rewinds the tab; a ←/→ inside a capturing wizard/select does NOT switch tabs.
    - D5 stepper: `renderStepper(step)` renders `Step n/4 · <long label> ● ○ ○ ○` — counter bold, `·` faint, active long label `styleStepperActive` (bold+accent), `●` accent for indices ≤ step / `○` faint; a test asserts the line is not Faint as a whole and the active label carries `styleStepperActive`, and that the bracket format `[1] SSH` no longer appears.
    - D6/D7 buttons + chord: the git step renders `Back (Esc)  [ Skip Git ]  [ Continue ]` on ONE row with both frozen hints below. ONE hoisted gate handles Shift+←/→ at every step: Shift+← always `stepBack` (step 0 → exit to detail), Shift+→ validity-gated `stepForward`. A table-driven test over steps 0–3 (including from inside the step-3 review ceremony) asserts Shift+← moves back and Shift+→ is blocked at each unpassed step. `[ Continue ]` disabled suffix is `— needs user.name + a valid email`.
    - D8 click-to-focus: clicking a field row focuses it; clicking a strategy option selects it and focuses `gitFieldStrategy`; clicking a checkbox row toggles; disabled algorithm rows are inert. Tests reuse the delete-scope click mechanism and assert focus/selection change from a click on the rendered row.
    - D9 user.email: `globalgit.go` renders `user.email (global fallback)` as an editable single-row field + `☐/☑` apply checkbox, unchecked/empty by default; toggling it joins `gitApplyChosen`; the apply ceremony heading/diff/result carry the frozen fallback copy. A test asserts the row is checkable, defaults off, and the ceremony copy names the includeIf-precedence invariant.
  </behavior>
  <action>
    Write failing tests first (CLAUDE.md TDD order), then implement in `internal/dummytui`
    strictly from `02-DESIGN-DECISIONS-CHECKPOINT-2.md`. Do NOT paraphrase frozen copy — the
    exact strings below are byte-exact and pinned by the copy-freeze grep (Task 3).

    (1) **D1 — single-row field, delete the box.** In `identities.go` collapse `formFieldLine`
    to one constant-height row per the D1 template (`▸ ` accent marker gutter focused / `  `
    otherwise; bold `padRight(label,16)`; `[value]` brackets present focused AND blurred;
    locked = dim value + faint `(locked)`, no brackets). DELETE `renderFocusedFieldBox`
    (identities.go:312-334) and its +2-row accounting. In `theme.go` change `FieldFocused` to
    `Foreground(ANSI 4) + Bold`, NO border; leave `FieldBlurred` (Faint) unchanged. This
    SUPERSEDES 02-14's field-contour box — update the `TestWizardFieldContour*` pins to assert a
    single-row color-only focus (no rounded-border SGR).

    (2) **D2 — always-expanded radios + header hint.** In `gitForm.view` remove the
    expand/collapse strategy branch (identities.go:536-546) — render all three options ALWAYS
    with `●`/`○`, and move `(←/→ change)` onto the `▸ Match strategy` header line, visible
    focused AND blurred (today it shows only when blurred — backwards). The selected `●` + label
    render through `FieldFocused` when the group is focused, plain-bold when blurred. Keep the
    always-drawn hint `Determines which repos this Git identity applies to.` The algorithm group
    already renders all options; keep its header `(←/→ change)`.

    (3) **D4 — main nav brackets + arrows + ActiveNavDimmed.** In `frame.go` change
    `headerTabText` to `" [%d] %s "` (renders `[1] Identities … [4] Doctor`; `headerTabAt`
    hit-testing derives from the same string, no separate change). Add role `ActiveNavDimmed`
    to `theme.go` (`Bold + Foreground(ANSI 4)`, NO background) and a fourth `renderHeader`
    branch: active tab + `capturesKeys` → `ActiveNavDimmed`; active tab otherwise → `ActiveNav`
    (accent background); inactive + capturesKeys → `DisabledNav`; inactive otherwise → plain.
    In `app.go` add plain ←/→ to the globals-last block (after the screen handler returns
    unhandled) that switches the tab 1..4 and clamps at the ends — capturing panes and Global
    SSH's ←/→ sub-tabs are unaffected because they return `handled=true` first (precedence
    clause 4). Add a `←→ switch view` contextual footer on top-level non-capturing states
    (frozen copy: `←→ switch view`). Global SSH keeps its existing `←→ Options / Storage`.

    (4) **D5 — stepper revert + chord hint line.** Revert `renderStepper` to
    `Step n/4 · <label> ● ○ ○ ○` using the LONG `wizardSteps` labels: `Step %d/4` bold, ` · `
    faint, active label `styleStepperActive` (bold+accent), dots `●` accent for indices ≤ step /
    `○` faint — the line is NEVER Faint as a whole. Delete `stepShortLabels` and the bracket
    format (it moves to the MAIN NAV per D4). Directly UNDER the stepper draw ONE always-visible
    faint line carrying the step-conditional chord hint (frozen copy, verbatim): step 0
    `Shift+→ next section · Shift+← exits the wizard`; steps 1–2
    `Shift+←/→ jump sections · forward needs a valid step`; step 3
    `Shift+← back to Git · Enter writes`.

    (5) **D6 — git buttons on one row, hints below.** Render
    `Back (Esc)  [ Skip Git ]  [ Continue ]` on ONE row; below it, ALWAYS, the two frozen hints:
    `Skip keeps this identity SSH-only and marks it incomplete.` and
    `Continue reviews the Git fragment, includeIf, and allowed_signers entries before writing.`
    (`Theme.Hint`). Button copy is unchanged.

    (6) **D7 — hoist ONE chord gate; delete the four per-step cases.** Add helpers
    `stepBack()` and `stepForward(s)` and a SINGLE Shift gate at the top of `handleWizardKey`
    ABOVE the step switch. `shift+left`: step 0 → `m.pane = paneDetail` (Esc parity, surfaced by
    the step-0 hint); otherwise `stepBack()` (uniform, INCLUDING 3→2 back to Git — the fix for
    the DEAD review step where `ceremony.handleKey`'s `default:` swallowed the chord).
    `shift+right`: validity-gated `stepForward(s)` — when blocked, emit the frozen status note
    naming the gate: step 0 `Can't continue yet — check the alias prefix, hostname, and port.`;
    step 2 `Can't continue yet — add user.name and a valid email.` (never a validity override —
    Shift overrides focus ownership only). Delete the four scattered per-step `shift+left`/
    `shift+right` cases (identities.go:1324/1331, 1404/1411, 1460/1467). Change the `[ Continue ]`
    disabled suffix from the generic `— disabled` to `— needs user.name + a valid email`. In
    `e2e/dummy_demo_e2e_test.go` add a raw-byte PTY test that injects `\x1b[1;2D` (Shift+Left)
    and `\x1b[1;2C` (Shift+Right) via the existing `sendKey` harness at each step and asserts the
    step index moves (proving a terminal decode regression cannot pass CI silently).

    (7) **D8 — click-to-focus.** Extend `identities.handleClick` so the ENTIRE rendered field
    row (label + `[value]`) is the hit target for wizard steps, Edit SSH, Configure Git, and
    Clone panes (zones derive from the rendered label strings — never magic row numbers); a
    click on a radio option row selects it (reuse the delete-scope mechanism at
    identities.go:1590-1598) and a strategy click also focuses `gitFieldStrategy`; disabled
    algorithm entries do not respond; checkbox rows toggle. In `globalgit.go`/`globalssh.go` the
    checkbox rows already toggle on click — extend to the D9 user.email row. NO on-screen click
    affordance is added (explicitly decided — the visible row + glyph IS the documentation).

    (8) **D9 — editable global-fallback user.email.** In `globalgit.go` promote the user.email
    row from awareness-only to a first-class editable single-row field (D1 template) + `☐/☑`
    apply checkbox that joins `gitApplyChosen`; preserve the recipes default (unchecked/empty by
    default — setting it is explicit opt-in). Frozen copy, verbatim: row label
    `user.email (global fallback)`. The helper line is, byte-exact on ONE line:
    `Fallback author for repos no identity matches. Identities always override this through their includeIf fragment — setting it never changes an identity's author.`
    The advisory line is, byte-exact on ONE line:
    `Recipes leave this unset by default. Set it only if you want a catch-all author for unmatched repos.`
    The apply ceremony: heading
    `Set global fallback user.email`; target `~/.gitconfig` with backup; diff preview annotated
    `(global fallback — identities override via includeIf)`; result
    `Global fallback user.email set — used only where no identity matches; identity fragments
    still win.` This is a DOCUMENTED divergence from recipes/ (recorded in FIELDS.md + STYLE-SPEC
    by Task 3); the demo stays 100% in-memory (DLV-05 — no real file write).

    (9) **Affordance-audit footers.** Add the contextual footers from the audit table with the
    frozen copy verbatim: Edit SSH pane `Tab/↑↓ fields` + `Enter rewrite Host block`; Configure
    Git pane `Tab/↑↓ fields` + `Enter write Git identity`; Clone pane `Tab switch · Enter clone`;
    all ceremonies `Tab/←→ Cancel / Confirm` + `Enter confirm`; Global SSH options `space toggle`
    (keep `←→ Options / Storage`) + storage `↑↓ layout`; Global Git `space toggle`. Footers are
    chrome — they cost ZERO body rows.

    (10) **Move every stale pin.** Revert the stepper pins from the `[1] SSH`/`[2] Test`/
    `[3] Git`/`[4] Review` format to `Step n/4` in identities_test.go (119/131/237/347), the
    `renderStepper` test (812-823), and e2e/dummy_demo_e2e_test.go (209/222/231); update the
    field-contour tests to the single-row color-only assertion; keep the button pins
    (372/442/721/799, batch3 87) and the arrow tests (batch3 337-374) valid under the hoisted
    gate. Name `/mui` + `agent-ui-ux-designer` as the direction for the mirrored web work
    (DLV-02). Touch ONLY the files in this task's `<files>`; add no new Go dependencies (lipgloss
    Foreground/Bold + bubbles textinput are already in the pinned charm.land v2 stack).
  </action>
  <verify>
    <automated>go test -race ./internal/dummytui/... && make lint</automated>
  </verify>
  <acceptance_criteria>
    - `renderFocusedFieldBox` is deleted from identities.go; `formFieldLine` renders exactly one line in focused/blurred/locked states (a test asserts no rounded-border SGR and single-line height); `theme.go` `FieldFocused` has NO border (`! grep -q 'RoundedBorder' internal/dummytui/theme.go` for the FieldFocused role)
    - `gitForm.view` renders all three strategy options at constant height with `(←/→ change)` on the header line in both focus states; no expand/collapse branch remains
    - `headerTabText` renders `[1] Identities … [4] Doctor`; `theme.go` defines `ActiveNavDimmed`; a header test proves the active-dimmed SGR differs from the `ActiveNav` background; a top-level ←/→ switches tabs only when the screen returns unhandled (a wizard/select ←/→ does not)
    - `renderStepper` renders `Step n/4 · <label> ● ○ ○ ○` (long labels), the active label is `styleStepperActive` (asserted NOT faint-whole-line), and `[1] SSH` no longer appears anywhere
    - the git step renders `Back (Esc)  [ Skip Git ]  [ Continue ]` on one row with both frozen hints below; `[ Continue ]` disabled suffix is `— needs user.name + a valid email`
    - a SINGLE hoisted Shift gate exists; the four per-step chord cases are gone; a table-driven test proves Shift+← goes back at steps 0–3 (incl. from the review ceremony 3→2) and Shift+→ is blocked at each unpassed step with the frozen status note; the raw-byte `\x1b[1;2D`/`\x1b[1;2C` PTY test moves the step index
    - clicking a field/radio/checkbox row focuses/selects/toggles it; disabled algorithm rows are inert
    - `globalgit.go` renders `user.email (global fallback)` editable + checkable (default off) with the frozen helper/advisory + ceremony copy verbatim; the includeIf-precedence invariant is stated in the result line
    - the audit-table contextual footers are present with frozen copy; `go test -race ./internal/dummytui/...` and `make lint` pass
  </acceptance_criteria>
  <done>The TUI demo matches the checkpoint-2 contract byte-for-byte on D1/D2/D4/D5/D6/D7/D8/D9 and the affordance audit, every superseded 02-14 pin is moved to the reverted copy, the hoisted chord gate makes Shift+←/→ work at every step (incl. the previously-dead review), and `go test -race`/`make lint` are green — with the full exit-gate battery deferred to Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Web demo — mirror the contract 1:1 (D1 fieldSx, D2 RadioGroups, D3 glyph checkbox/radio, D4 nav mirror, D5 StepDots revert, D6 row + reason, D7 notify, D9 editable user.email)</name>
  <files>.planning/design/mockup-src/src/theme.ts, .planning/design/mockup-src/src/demo/DemoApp.tsx, .planning/design/mockup-src/src/demo/Frame.tsx, .planning/design/mockup-src/src/demo/screens/Identities.tsx, .planning/design/mockup-src/src/demo/screens/GlobalGit.tsx, .planning/design/mockup-src/src/demo/MutationCeremony.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-DESIGN-DECISIONS-CHECKPOINT-2.md (THE CONTRACT — the web half of every D-item; the frozen copy is byte-exact and shared with Task 1)
    - Task 1's identities.go/globalgit.go changes (the 1:1 target the web mirrors — same reverted stepper, same radios, same one-row buttons, same editable user.email, same frozen strings)
    - .planning/design/mockup-src/src/theme.ts (the `roles` export 73-100 incl. `activeNav` 89-93, `focusedField`/`blurredField` 77-78 — add `activeNavDimmed`; add `MuiCheckbox`/`MuiRadio` glyph overrides near the existing `MuiInputLabel`/`MuiButton` overrides; `semanticColors.accent` is the shared accent)
    - .planning/design/mockup-src/src/demo/screens/Identities.tsx (`focusedFieldSx` 112; the match-strategy Select 335-352 and the algorithm Select 568-591 → BOTH become RadioGroups; `StepDots` 255-266 + `WIZARD_STEPS` 245 → revert; the `wizardArrowNav`/local guard 439-461; the git-step buttons + hints 720-740)
    - .planning/design/mockup-src/src/demo/DemoApp.tsx (the global onKey guard + the Shift+←/→ focus-override chord 103-114 — add the plain-←/→ top-level view switch mirroring app.go)
    - .planning/design/mockup-src/src/demo/Frame.tsx (nav tabs 100-129 through `roles.activeNav` — add the `[i+1] label` brackets + the `activeNavDimmed` state; footer 185-195)
    - .planning/design/mockup-src/src/demo/screens/GlobalGit.tsx (the user.email awareness row + `applyChosen` 54 + the Checkbox 130 + `notify` 192 → editable field + glyph checkbox + ceremony annotation)
    - .planning/design/mockup-src/src/demo/MutationCeremony.tsx (the D9 diff annotation `(global fallback — identities override via includeIf)` on the web ceremony preview)
  </read_first>
  <action>
    Implement from `02-DESIGN-DECISIONS-CHECKPOINT-2.md`, mirroring Task 1's TUI 1:1 (DLV-02:
    `/mui` under `agent-ui-ux-designer` — name both). Frozen copy is byte-exact; do NOT
    re-derive or paraphrase — quote the same strings Task 1 uses.

    (1) **D1 fieldSx.** MUI outlined TextField never reflows — adjust `fieldSx`/`focusedFieldSx`
    so the rest state uses `roles.blurredField` (outline `#2a2d33`, input opacity 0.85) and focus
    tints value + label with `roles.focusedField.color` + a 2px accent outline. No new web color
    role; no layout that changes height on focus.

    (2) **D2 RadioGroups.** Replace BOTH the match-strategy Select (Identities.tsx:335-352) AND
    the algorithm Select (568-591) with MUI `RadioGroup`s (the delete-scope/SSH-storage pattern
    is already established). Algorithm rows keep per-option availability sub-text + `disabled`;
    keep the always-rendered semantic `helperText` and APPEND the `(←/→ change)` nav hint to the
    group label. Strategy options render all three, always.

    (3) **D3 glyph checkbox/radio.** In `theme.ts` add `components.MuiCheckbox.defaultProps` /
    `MuiRadio.defaultProps` that render monospace glyph spans — checkbox `☐`/`☑`, radio `○`/`●`
    (the D3 frozen glyphs, shared with the TUI) — plus a disabled `styleOverrides` →
    `roles.hint.color`. This removes the stock Material icons everywhere at once.

    (4) **D4 nav mirror.** In `Frame.tsx` render the nav labels as `[{i+1}] {label}` and add the
    `activeNavDimmed` state (accent text/border, transparent bg, weight 700) for the current view
    while a modal/edit/ceremony captures keys. Add `activeNavDimmed` to `theme.ts` `roles`. In
    `DemoApp.tsx` add plain ←/→ that switch views 1–4 at the top level, mirroring the `'1'..'4'`
    block — fire only when no capturing pane/select/input owns the key (the wizard's local guard
    and Global SSH's sub-tabs are unaffected). Add the `←→ switch view` footer affordance on
    top-level non-capturing states.

    (5) **D5 StepDots revert + conditional sub-line.** Rebuild `StepDots` to render
    `Step n/4 · <label> ● ○ ○ ○` (counter bold, active label 700/accent, `●` accent ≤ step / `○`
    dim — never dim as a whole line), using `WIZARD_STEPS` (long labels). Directly under it,
    render the identical step-conditional chord sub-line (same frozen strings as Task 1:
    `Shift+→ next section · Shift+← exits the wizard` / `Shift+←/→ jump sections · forward needs
    a valid step` / `Shift+← back to Git · Enter writes`) through `roles.hint`.

    (6) **D6 row layout + D7 reason/notify.** Collapse the nested step-3 button Stacks into ONE
    row Stack (`Back (Esc)  [ Skip Git ]  [ Continue ]`) with a hint Stack below carrying the two
    frozen hints. Put the reason text `— needs user.name + a valid email` under the disabled
    Continue; call `notify(...)` on a blocked Shift+→ with the same step-conditional frozen status
    note as the TUI (`Can't continue yet — …`).

    (7) **D9 editable user.email.** In `GlobalGit.tsx` promote the user.email row to a `TextField`
    (D1 `fieldSx`) + a terminal-glyph `Checkbox` (D3) wired into `applyChosen`, unchecked/empty by
    default. Use the same frozen row label / helper / advisory copy as Task 1. In
    `MutationCeremony.tsx` annotate the D9 ceremony preview `(global fallback — identities
    override via includeIf)` and use the frozen heading/result strings.

    Do NOT edit the Go sources, FIELDS.md, or 02-STYLE-SPEC.md here (Task 3 owns the docs lockstep
    + the atomic copy-freeze gate). Verify with typecheck + build.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm typecheck && pnpm build</automated>
    <human-check>In the served web demo (`make demo-web`): fields are one constant height, focus is color/outline only; the match-strategy and algorithm groups are always-expanded radios with `(←/→ change)` on the header; the main nav reads `[1] Identities … [4] Doctor` and plain ←/→ switch views only when no field/pane is focused; the wizard stepper reads `Step n/4 · <label> ● ○ ○ ○` with the chord hint below; step-3 buttons are on one row with hints below; the disabled Continue shows `— needs user.name + a valid email`; checkboxes/radios render `☐/☑`/`○/●` glyphs; Global Git `user.email (global fallback)` is editable + checkable (default off). (Re-confirmed at the 02-12 re-presentation.)</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` clean + `pnpm build` OK (verify-routes) after the changes
    - both the match-strategy Select and the algorithm Select are replaced by RadioGroups; no `<Select`/`MenuItem` remains for either group
    - `theme.ts` renders `☐/☑` and `○/●` glyphs via `MuiCheckbox`/`MuiRadio` overrides (no stock Material check/radio icons); `activeNavDimmed` role exists
    - `Frame.tsx` nav reads `[{i+1}] {label}` and dims the active view to `activeNavDimmed` under a capturing pane; `DemoApp.tsx` adds a plain-←/→ top-level view switch that does not fire from inside a field/select/wizard
    - `StepDots` renders `Step n/4 · <label> ● ○ ○ ○` (reverted) with the conditional chord sub-line; the step-3 buttons are one row with the two frozen hints below and the `— needs user.name + a valid email` reason on the disabled Continue
    - `GlobalGit.tsx` user.email is an editable field + glyph checkbox (default off) with the frozen copy; the ceremony carries the `(global fallback — identities override via includeIf)` annotation
  </acceptance_criteria>
  <done>The web demo mirrors the checkpoint-2 contract 1:1 — single-row fields, always-expanded RadioGroups, terminal-glyph checkbox/radio, bracketed nav with the dimmed active state + plain-arrow view switch, the reverted `Step n/4` stepper with the chord sub-line, one-row buttons with reason text, and the editable global-fallback user.email — validated by typecheck+build and confirmed live.</done>
</task>

<task type="auto">
  <name>Task 3: Docs/spec lockstep (02-STYLE-SPEC §3/§5/§7 + recipes/ divergence note + FIELDS.md rows) and the FULL exit-gate battery</name>
  <files>.planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md, .planning/design/create-flow/FIELDS.md, .planning/design/global-git/FIELDS.md</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-DESIGN-DECISIONS-CHECKPOINT-2.md (the "Docs/tests that MUST move in lockstep" clause + the D9 divergence-documentation requirement + the row-budget check "tightest pane ~21 of 25 body rows")
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md (§3 parity-dimensions `field-contour`/`dim-states` rows, §5 frozen stepper short↔long map, §6 copy-freeze atomicity gate, §7 row-budget "+2 box" math — all SUPERSEDED by D1/D5 and must be rewritten)
    - .planning/design/create-flow/FIELDS.md (the git-form button-copy rows 147-160 + the emphasis-role parity rows 162-179 — update `field-contour`/`arrow-nav`/`dim-states` to the contract)
    - .planning/design/global-git/FIELDS.md (the user.email awareness rows 65/128 → D9 EDITABLE + divergence note)
    - CLAUDE.md (recipes/ North Star — gitid uses ed25519 + the recipes structure; D9 is a CONSCIOUS divergence that must be documented; English-only; never `--no-verify`)
  </read_first>
  <action>
    Rewrite the docs in lockstep with Tasks 1–2, then run the full exit-gate battery. Frozen
    copy is byte-exact — quote it so the copy-freeze grep pins it.

    (1) **02-STYLE-SPEC.md rewrite.** §5 (frozen stepper): replace the short↔long bracket map
    with the reverted `Step n/4 · <label> ● ○ ○ ○` format (long labels; active bold+accent, not
    faint) and state the bracket format moved to the MAIN NAV (D4). §3 (parity dimensions):
    rewrite `field-contour` to "every field is ONE constant-height row, focus = accent color +
    bracket + `▸`/2px-outline redundant cue, NO box, NO reflow" (kills the "+2 box" language) and
    rewrite `dim-states` for the new `ActiveNavDimmed`/`activeNavDimmed` state + the bracketed
    nav + plain-arrow view switch; add rows/notes for the always-expanded radios and the glyph
    checkbox/radio. §7 (row budget): replace the "+2 box" math with the reflow-removed budget and
    record the contract's acceptance number — the tightest pane (wizard step 2) lands at ~21 of
    25 body rows, the PTY e2e is the proof. Add a NEW `## Conscious divergences from recipes/`
    note stating the D9 rationale (an editable global-fallback user.email is an explicit opt-in
    that recipes leave unset by default) and the PRESERVED invariant (identity includeIf
    fragments always override the global fallback — setting it never changes an identity's
    author; gitid still uses ed25519 + the recipes structure, this diverges on ONE field only).

    (2) **FIELDS.md rows.** In create-flow/FIELDS.md update the emphasis-role parity rows to the
    contract (single-row field-contour, always-expanded radios, reverted stepper, bracketed nav +
    plain-arrow view switch, ActiveNavDimmed, click-to-focus, chord visibility with per-step
    hints); keep the frozen git-form button + hint rows. In global-git/FIELDS.md add/replace the
    user.email row to `Global Git · user.email (global fallback) · EDITABLE` with a note that it
    DIVERGES from recipes/ (recipes leave it unset; setting it is opt-in; identities always
    override via includeIf) and update the stale "user.email was left alone" ceremony rows to the
    new editable-fallback framing.

    (3) **FULL exit-gate battery — run in order and record raw output in the SUMMARY:**
    `go test -race ./internal/dummytui/...`; the no-backend ALLOWLIST
    `go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$'` is empty; the EXTENDED copy-freeze grep
    `! grep -rn 'Skip — SSH only\|Continue: review & write\|\[1\] SSH\|\[2\] Test\|\[3\] Git\|\[4\] Review\| — disabled' internal/dummytui .planning/design/mockup-src/src .planning/design/create-flow .planning/design/global-git`
    (zero matches — the superseded 02-14 wizard-stepper bracket strings and the generic
    `— disabled` Continue suffix are gone from BOTH demos + FIELDS.md); the PRESENT-copy assertions
    `grep -rq 'user.email (global fallback)'` and `grep -rq 'needs user.name + a valid email'`
    across both demos; `make test`; `make lint`; `make test-e2e` (the 100×30 raw-keystroke PTY
    walk INCLUDING the new raw-byte Shift-chord test — proof the reflow removal kept the tightest
    pane inside the 25-row body budget); `make gate-no-backend-files`; and
    `cd .planning/design/mockup-src && pnpm typecheck && pnpm build`.

    (4) **Orchestrator-run exit gates (name them, do NOT attempt to run them — the executor
    cannot spawn agents):** a FRESH `agent-ui-ux-designer` critique of the two LIVE demos on the
    checkpoint-2 dimensions, and a fresh-context code review (`superpowers:requesting-code-review`)
    against this plan's `must_haves` + every task `<acceptance_criteria>`. Record them as
    outstanding orchestrator gates in the SUMMARY; their CRITICAL/HIGH findings must be resolved
    before 02-12 is re-presented (DLV-02).
  </action>
  <verify>
    <automated>go test -race ./internal/dummytui/... && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ] && ! grep -rn 'Skip — SSH only\|Continue: review & write\|\[1\] SSH\|\[2\] Test\|\[3\] Git\|\[4\] Review\| — disabled' internal/dummytui .planning/design/mockup-src/src .planning/design/create-flow .planning/design/global-git && grep -rq 'user.email (global fallback)' internal/dummytui .planning/design/mockup-src/src && make test && make lint && make test-e2e && make gate-no-backend-files && cd .planning/design/mockup-src && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - 02-STYLE-SPEC.md §5 shows the reverted `Step n/4 · <label> ● ○ ○ ○` stepper (bracket format moved to MAIN NAV); §3 `field-contour`/`dim-states` rows match D1/D4 (single-row, ActiveNavDimmed, bracketed nav, plain-arrow switch); §7 replaces the "+2 box" math with the reflow-removed budget + the "~21 of 25 rows" acceptance number; a `## Conscious divergences from recipes/` note documents D9 + the preserved includeIf-precedence invariant
    - create-flow/FIELDS.md emphasis-role parity rows match the contract; global-git/FIELDS.md carries the `user.email (global fallback) · EDITABLE` divergence row and the updated ceremony rows
    - the extended copy-freeze grep finds ZERO occurrences of `Skip — SSH only`, `Continue: review & write`, `[1] SSH`/`[2] Test`/`[3] Git`/`[4] Review`, or the generic ` — disabled` suffix across internal/dummytui + mockup-src/src + create-flow + global-git; the new frozen strings (`user.email (global fallback)`, `needs user.name + a valid email`) are PRESENT in both demos
    - ALL machine gates green: `go test -race`, the no-backend allowlist (empty), `make test`, `make lint`, `make test-e2e` (100×30 PTY walk incl. the raw-byte Shift-chord test — the row-budget proof after the reflow removal), `make gate-no-backend-files`, `pnpm typecheck && pnpm build`
    - the fresh `agent-ui-ux-designer` critique + fresh-context code review are NAMED as orchestrator-run exit gates in the SUMMARY (the executor did not run them; their HIGH/CRITICAL findings block the 02-12 re-presentation)
  </acceptance_criteria>
  <done>02-STYLE-SPEC.md §3/§5/§7 + the recipes/ divergence note and both FIELDS.md files match the checkpoint-2 contract; the extended copy-freeze grep proves the superseded copy is gone and the new frozen copy is consistent across both demos + docs; every machine exit gate (incl. the 100×30 PTY walk re-proving the row budget and the raw-byte chord test) is green; and the two orchestrator-run agent gates are named for the 02-12 re-presentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live demos ← user's real files | both demos are 100% in-memory dummy state; D9's global-fallback user.email is a DUMMY ceremony — no read/write of ~/.gitconfig, ~/.ssh (DLV-05) |
| cmd/gitid-dummy + internal/dummytui ← backend packages | the import-graph allowlist still holds after the reflow/nav/chord/D9 changes |
| Phase-2 change-set ← git working tree | `gate-no-backend-files` keeps this plan inside design-only paths (.planning/, internal/dummytui/, e2e/, mockup-src/) |
| approval record ← consistent artifacts | the checkpoint-2 copy (reverted stepper, frozen chord/D9 strings) must be identical across both demos + FIELDS.md before 02-12 is re-signed — enforced by the extended copy-freeze grep |
| identity author ← global fallback | D9's global user.email must NEVER be presented as overriding an identity — the includeIf-precedence invariant is a trust boundary the copy pins |

## STRIDE Threat Register (ASVS L1 — block on HIGH)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15-D9WRITE | Tampering | D9 global-fallback user.email write semantics | mitigate | The demo stays 100% in-memory (DLV-05); `make test-e2e` re-runs the real binary under a sandboxed HOME and re-asserts ZERO files created at 100×30. The ceremony preview annotation `(global fallback — identities override via includeIf)` + the frozen result `…identity fragments still win.` pin the includeIf-precedence invariant so a fallback can never be presented as overriding an identity author; the divergence + invariant are documented in FIELDS.md + 02-STYLE-SPEC.md |
| T-02-15-CHORD | Elevation of Privilege / Tampering | the hoisted Shift chord bypassing the step-validity gate | mitigate | `stepForward(s)` under Shift+→ shares the SAME validity predicate as plain Right (`step0Valid`/`testStage2`/`git.valid()`); a table-driven test asserts Shift+→ is blocked at every unpassed step and the blocked-forward status note names the gate — Shift overrides focus ownership only, NEVER validity |
| T-02-15-NB | Tampering / Elevation of Privilege | a backend import slipping in via the nav/D9/click work | mitigate | the whole-dummy `go list -deps` ALLOWLIST is re-run as a Task 3 gate; the changes add only lipgloss/bubbles usage already imported and MUI role tokens reusing existing colors |
| T-02-15-COPY | Repudiation / Tampering | 02-12 re-signed against inconsistent reverted copy (one demo/FIELDS still on the superseded `[1] SSH` stepper or the generic `— disabled`) | mitigate | the extended copy-freeze grep forbids the superseded 02-14 wizard-stepper bracket strings + `— disabled` across both demos + FIELDS.md AND asserts the new frozen strings present; the docs lockstep is atomic in Task 3 |
| T-02-15-ROW | Denial of Service (frame clip) | the reflow removal + stepper/chord-hint/footer changes overflowing the 100×30 body budget | mitigate | the field reflow is a NET −2 rows per open form (D1) offset against D2/D5 additions; the 100×30 raw-keystroke PTY walk is the standing proof and the contract's acceptance number is ~21 of 25 body rows at the tightest pane |
| T-02-15-SC | Tampering | supply chain via new dependencies | accept (n/a) | NO new dependencies: lipgloss Foreground/Bold + bubbles textinput + MUI RadioGroup/Checkbox/Radio are already in the pinned stack; no package-manager install task exists in this plan |
</threat_model>

<verification>
- 02-STYLE-SPEC.md §3/§5/§7 rewritten to the contract (reverted stepper, single-row field-contour, reflow-removed row budget) + a `Conscious divergences from recipes/` note for D9
- `go test -race ./internal/dummytui/...` green (existing suite + the new single-row-field/always-radio/reverted-stepper/hoisted-chord/click-to-focus/D9 tests)
- Web: `pnpm typecheck` clean + `pnpm build` OK (verify-routes)
- the EXTENDED copy-freeze grep finds zero occurrences of the superseded `[1] SSH`/`[2] Test`/`[3] Git`/`[4] Review` wizard-stepper strings, `Skip — SSH only`, `Continue: review & write`, or ` — disabled` across internal/dummytui + mockup-src/src + create-flow + global-git; the new frozen strings are present in both demos
- the no-backend import ALLOWLIST is empty; `make test`, `make lint`, `make test-e2e` (100×30 PTY walk incl. the raw-byte `\x1b[1;2D`/`\x1b[1;2C` Shift-chord test), `make gate-no-backend-files` all green
- a fresh `agent-ui-ux-designer` critique of the two live demos on the checkpoint-2 dimensions + a fresh-context code review are ORCHESTRATOR-run exit gates named in the SUMMARY; their HIGH/CRITICAL findings resolved before 02-12 is re-presented (DLV-02)
</verification>

<success_criteria>
- **Code review (required before this plan is done):** on completion, the orchestrator requests a fresh-context code review via `superpowers:requesting-code-review` against this plan's `must_haves` and every task `<acceptance_criteria>`; resolve all CRITICAL/HIGH findings before the plan is marked complete. Supplements — not replaces — the phase-level `/gsd-code-review` and the external cross-vendor (Codex) review.
- Every checkpoint-2 contract item is operationalized: D1 single-row color-only fields (box deleted), D2 always-expanded radios + header hint, D3 terminal-glyph checkbox/radio on the web, D4 bracketed nav + plain-arrow view switch + ActiveNavDimmed, D5 reverted `Step n/4` stepper + step-conditional chord hint, D6 one-row git buttons + hints, D7 one hoisted chord gate (Shift works at EVERY step incl. the previously-dead review) + blocked-forward notes + raw-byte CSI e2e, D8 click-to-focus rows/radios/checkboxes, D9 editable global-fallback user.email — plus every affordance-audit footer.
- The contract SUPERSEDES 02-14 where they conflict (field box, bracketed wizard stepper) and the plan/docs say so; the copy-freeze grep proves the superseded copy is gone from both demos + FIELDS.md.
- DLV-02: the web half is refined with `/mui` under `agent-ui-ux-designer`; the TUI mirrors it 1:1; the fresh designer critique is the exit gate.
- DLV-05: no backend logic is added — the import allowlist, zero-writes PTY e2e, and gate-no-backend-files stay green; D9 is a DUMMY ceremony, a documented recipes/ divergence on ONE field with the includeIf-precedence invariant preserved.
- The row budget holds after the reflow removal: the tightest pane lands ~21 of 25 body rows, re-proven by the 100×30 PTY walk.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-15-SUMMARY.md` when done.
</output>
</content>
</invoke>
