---
phase: 02-design-all-mockups-checkpoint-1
plan: 07
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/global-ssh/FIELDS.md
  - .planning/design/global-ssh/manifest.json
  - .planning/design/global-ssh/parity.json
  - .planning/design/global-ssh/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/global-ssh/options-list.route.tsx
  - .planning/design/mockup-src/src/routes/global-ssh/option-detail.route.tsx
  - .planning/design/mockup-src/src/routes/global-ssh/fix-preview.route.tsx
  - .planning/design/mockup-src/src/routes/global-ssh/confirm-write.route.tsx
  - .planning/design/mockup-src/src/routes/global-ssh/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/global-ssh/result-applied.route.tsx
  - internal/dummytui/surface_globalssh.go
  - internal/dummytui/surface_globalssh_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The global-SSH-options screen is mocked and dummied across all 6 named states from UX-DIRECTION §4.4"
    - "The options list surfaces the dangerous-by-default options (StrictHostKeyChecking, ForwardAgent, HashKnownHosts, IdentitiesOnly, AddKeysToAgent, UseKeychain), each with current value + risk + recommended value, with EXPLANATORY copy (GSSH-01)"
    - "Recommendations are advisory, never blocking — yellow ! advisory, not a red compliance gate; the user can leave any option unchanged (§4.4)"
    - "Both media captured (6 PNGs each) with the breadcrumb gate; dummy-nav e2e reaches every screen; parity.json has no unresolved row; the surface claims number key 2 via RegisterOrReplace"
  artifacts:
    - path: ".planning/design/global-ssh/manifest.json"
      provides: "hardened per-screen {surface, screen, htmlRoute, keysFromHome, signature} for 6 states"
      contains: "options-list"
    - path: ".planning/design/global-ssh/parity.json"
      provides: "machine-checkable §3 rows + per-option-explanation + advisory-not-blocking rows (gate: no row status != resolved)"
      contains: "status"
    - path: "internal/dummytui/surface_globalssh.go"
      provides: "TUI dummy global-ssh surface (view 2, RegisterOrReplace key 2), backend-free"
      contains: "RegisterOrReplace"
  key_links:
    - from: "src/routes/global-ssh/options-list.route.tsx"
      to: "dangerous-by-default option set + explanations"
      via: "advisory (yellow !), never blocking"
      pattern: "StrictHostKeyChecking"
    - from: "internal/dummytui/surface_globalssh.go"
      to: "dummytui.RegisterOrReplace (number key 2)"
      via: "init()-registration replacing the placeholder, no data.go edit"
      pattern: "RegisterOrReplace"
---

<objective>
Fan-out surface: **Global SSH options** (view `2`, owned later by Phase 6). Repeat the proven per-surface pipeline (02-04) for all 6 states from UX-DIRECTION §4.4.

States (verbatim §4.4): `options-list` (StrictHostKeyChecking, ForwardAgent, HashKnownHosts, IdentitiesOnly, AddKeysToAgent, UseKeychain — each with current value + risk + recommended value) · `option-detail` (the explanation) · `fix-preview` · `confirm-write` · `backup-notice` · `result-applied`.

Highest-risk affordance (§4.4): recommendations are **advisory, never blocking** — a yellow `!` advisory, never a red compliance gate; the user may apply none.

Review folded in (Codex 02-REVIEWS.md): HIGH-2 number-key view (2) → `RegisterOrReplace` (no data.go edit); HIGH-4 structured `parity.json` gate; HIGH-3(c) hardened manifest schema; HIGH-1 the ALLOWLIST no-backend proof; LOW-11 tighten the weak `recommended` acceptance to assert the advisory (not-blocking) treatment precisely; MEDIUM-10 fan-out isolation — writes ONLY its own surface files, never editing shared files; serialize `pnpm build` if wave-4 runs concurrently.

DLV-02: author the MUI mockup with `/mui`, the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/ssh-config.recipe
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: global-ssh FIELDS.md + manifest.json (hardened) + parity.json seed + MUI mockup (6 states)</name>
  <files>.planning/design/global-ssh/FIELDS.md, .planning/design/global-ssh/manifest.json, .planning/design/global-ssh/parity.json, .planning/design/mockup-src/src/routes/global-ssh/options-list.route.tsx, .planning/design/mockup-src/src/routes/global-ssh/option-detail.route.tsx, .planning/design/mockup-src/src/routes/global-ssh/fix-preview.route.tsx, .planning/design/mockup-src/src/routes/global-ssh/confirm-write.route.tsx, .planning/design/mockup-src/src/routes/global-ssh/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/global-ssh/result-applied.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.4 (6 states + advisory-not-blocking affordance), § 2 (master-detail archetype, advisory yellow ! color role), § 5 (advisory ≠ blocking)
    - .planning/design/create-flow/{FIELDS.md, manifest.json, parity.json} + a create-flow route (02-04 — proven format)
    - REQUIREMENTS.md GSSH-01, .planning/design/mockup-src/src/data/recipeFixtures.ts, .planning/design/FIELDS.template.md, .planning/design/PARITY.template.json
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` and the 02-04 format: author `FIELDS.md` (6-state table), `manifest.json` (6 hardened `{surface:"global-ssh", screen, htmlRoute, keysFromHome, signature}` entries — unique screen/htmlRoute/signature, absolute keysFromHome; route titles `global-ssh/<screen>`), and copy `PARITY.template.json` to `parity.json` seeding the §3 rows + a `per-option-explanation-verbatim` row + an `advisory-not-blocking` row. Author 6 `*.route.tsx` under `src/routes/global-ssh/` inside `<Shell>` (master-detail): the `options-list` surfacing the six dangerous-by-default options (StrictHostKeyChecking, ForwardAgent, HashKnownHosts, IdentitiesOnly, AddKeysToAgent, UseKeychain), each with its current value + risk + recommended value; `option-detail` with the full EXPLANATION copy (contractual — verbatim); `fix-preview`; `confirm-write` (sentinel-visible, target file named); `backup-notice`; `result-applied`. Recommendations render as a yellow `!` advisory (glyph + word), NEVER a red block — the screen must not read as a compliance gate, and the user can leave any option unchanged. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/global-ssh/manifest.json'))))") -eq 6 && python3 -c "import json;m=json.load(open('.planning/design/global-ssh/manifest.json'));assert len(set(e['screen'] for e in m))==6 and len(set(e['signature'] for e in m))==6 and all(e.get('keysFromHome') for e in m)"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 6 hardened-schema screens (unique screen+signature, non-empty keysFromHome) AND `grep -q 'options-list' .planning/design/global-ssh/manifest.json`
    - `parity.json` seeded with §3 + per-option-explanation + advisory-not-blocking rows: `python3 -c "import json;r=json.load(open('.planning/design/global-ssh/parity.json'));assert len(r)>=9"`
    - 6 route files exist: `test $(ls .planning/design/mockup-src/src/routes/global-ssh/*.route.tsx | wc -l) -eq 6`
    - The dangerous option set is present WITH per-option risk + recommended value AND the advisory (not blocking) treatment (LOW-11 tightened): `grep -rq 'StrictHostKeyChecking' .../global-ssh/` AND `grep -rq 'ForwardAgent' .../global-ssh/` AND `grep -riq 'recommended' .../global-ssh/` AND `grep -riq 'advisory' .../global-ssh/` AND `! grep -riq 'blocked\|must fix\|required to proceed' .../global-ssh/`
    - Advisory-not-blocking explicit in FIELDS.md: `grep -qi 'advisory' .planning/design/global-ssh/FIELDS.md`
    - No lorem: `! grep -riq 'lorem' .../global-ssh/`; `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The global-ssh MUI mockup renders all 6 states with the explained dangerous-by-default option set, advisory (never blocking), with a seeded parity.json.</done>
</task>

<task type="auto">
  <name>Task 2: global-ssh TUI dummy surface (6 screens, RegisterOrReplace key 2, backend-free)</name>
  <files>internal/dummytui/surface_globalssh.go, internal/dummytui/surface_globalssh_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/registry.go (RegisterOrReplace), internal/dummytui/data.go (02-02 — the placeholder GlobalSSH view on key 2 to REPLACE)
    - .planning/design/global-ssh/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 2 = key 2; advisory color role; breadcrumb), § 4.4
  </read_first>
  <action>
    Write `surface_globalssh.go` in `package dummytui` mirroring 02-04: register global-ssh as the primary view on number key `2` via `RegisterOrReplace` (review HIGH-2 — replaces the 02-02 placeholder, no data.go edit, no duplicate-key conflict). Define 6 `ScreenDef`s matching `manifest.json`; render the options-list (six options, current/risk/recommended, advisory yellow `!` glyph+word), option-detail (explanation), fix-preview, sentinel-visible confirm-write, backup-notice, result-applied — hardcoded fixture values. NO backend import. Write `surface_globalssh_test.go` asserting 6 registered screens, global-ssh sole owner of key 2, each RenderScreen non-empty + contains its signature + the `global-ssh/<id>` breadcrumb, connected Keys path.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run GlobalSSH && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `surface_globalssh.go` claims key 2 via RegisterOrReplace: `grep -q 'RegisterOrReplace' internal/dummytui/surface_globalssh.go`; test asserts count 6 AND sole ownership of key 2
    - `go test -race ./internal/dummytui/... -run GlobalSSH` passes (breadcrumb per screen)
    - Copy anchors: `grep -q 'StrictHostKeyChecking' internal/dummytui/surface_globalssh.go`
    - No-backend ALLOWLIST holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - No data.go edit; `make lint` passes
  </acceptance_criteria>
  <done>The global-ssh surface exists in the dummy as view 2 (sole owner of key 2 via RegisterOrReplace) with all 6 screens, mirroring the mockup, proven backend-free, no data.go edit.</done>
</task>

<task type="auto">
  <name>Task 3: Capture global-ssh (both media) + agent-ui-ux-designer critique → parity.json 0-unresolved</name>
  <files>.planning/design/global-ssh/CRITIQUE.md, .planning/design/global-ssh/FIELDS.md, .planning/design/global-ssh/parity.json</files>
  <read_first>
    - .planning/design/create-flow/{CRITIQUE.md, parity.json} (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.4, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/global-ssh' ./internal/screenshot/...` (6 HTML + 6 TUI PNGs, breadcrumb-gated); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/global-ssh' ./e2e/...`). Spawn `agent-ui-ux-designer`: record the HTML-aesthetic pass in `CRITIQUE.md` and fill `.planning/design/global-ssh/parity.json` — §3 rows + `per-option-explanation-verbatim` + `advisory-not-blocking` — resolving every divergence so NO row has `status != resolved`. Fill FIELDS.md columns. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/global-ssh' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/global-ssh' ./e2e/... && test $(ls .planning/design/global-ssh/html/*.png | wc -l) -eq 6 && test $(ls .planning/design/global-ssh/tui/*.png | wc -l) -eq 6 && python3 -c "import json;r=json.load(open('.planning/design/global-ssh/parity.json'));import sys;sys.exit(1 if (not r or [x for x in r if x.get('status')!='resolved']) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - 6 HTML + 6 TUI PNGs: `test $(ls .planning/design/global-ssh/html/*.png | wc -l) -eq 6` AND `test $(ls .planning/design/global-ssh/tui/*.png | wc -l) -eq 6`
    - The surface-scoped dummy-nav e2e passes (breadcrumb+signature asserted, zero writes)
    - `parity.json` has no unresolved row and includes the per-option-explanation + advisory-not-blocking rows: `python3 -c "import json;r=json.load(open('.planning/design/global-ssh/parity.json'));assert r and all(x['status']=='resolved' for x in r)"`
    - FIELDS.md columns filled for all 6 screens; `make lint` passes
  </acceptance_criteria>
  <done>global-ssh captured in both media (6+6), navigable, parity resolved in a machine-checkable parity.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| global-ssh mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB6 | Tampering / Elevation of Privilege | global-ssh dummy reaching a backend write path | mitigate | `surface_globalssh.go` imports no backend package; inherits the 02-02 import-graph ALLOWLIST + 02-03 zero-write e2e |
| T-02-ADV | Repudiation | advisory recommendations rendered as a blocking compliance gate in the frozen reference | mitigate | advisory-not-blocking (yellow ! not red block; user may apply none) is a parity.json row + a tightened acceptance (LOW-11) rejecting blocking language |
| T-02-EXPL | Information Disclosure | per-option risk/recommended explanations omitted or inaccurate | mitigate | explanation copy is contractual/verbatim and a parity.json row (GSSH-01) |
| T-02-RG6 | Tampering | fan-out registration failing on a duplicate activation key | mitigate | `RegisterOrReplace` cleanly replaces the key-2 placeholder; the test asserts sole ownership of key 2 |
</threat_model>

## Artifacts this phase produces

- `.planning/design/global-ssh/{FIELDS.md, manifest.json (hardened), parity.json, CRITIQUE.md, html/*.png (6), tui/*.png (6)}`
- `.planning/design/mockup-src/src/routes/global-ssh/*.route.tsx` — 6 MUI v7 route files (titles `global-ssh/<screen>`)
- `internal/dummytui/surface_globalssh.go` (+ `_test.go`) — TUI dummy view-2 surface (RegisterOrReplace key 2), 6 screens, backend-free

<verification>
- 6 HTML + 6 TUI PNGs; surface-scoped dummy-nav e2e passes; parity.json no unresolved row
- Dangerous-by-default option set + verbatim explanations; advisory-not-blocking (tightened) — parity.json rows
- Dummy passes the no-backend allowlist; sole owner of key 2 via RegisterOrReplace; no data.go edit; no lorem
</verification>

<success_criteria>
- DLV-01/DLV-05 for global-ssh: HTML mockup + navigable backend-free TUI dummy, both captured, parity resolved in parity.json.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-07-SUMMARY.md` when done.
</output>
