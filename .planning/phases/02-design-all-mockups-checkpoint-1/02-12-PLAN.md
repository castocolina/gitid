---
phase: 02-design-all-mockups-checkpoint-1
plan: 12
type: execute
wave: 6
depends_on: [02-11]
files_modified:
  - .planning/design/APPROVAL.md
autonomous: false
requirements: [DLV-08, DLV-02]
user_setup: []
must_haves:
  truths:
    - "The user is presented the COMPLETE reference set (all 50 HTML + 50 TUI screenshots across 7 surfaces), each surface's FIELDS.md semantic manifest, and the UX-DIRECTION §6 A–F approval checklist"
    - "The user signs off on copy, field order, options, and defaults (the FREEZE), the safety affordances (four-beat ceremony, destructive-confirm, advisory-not-blocking, read-only Health), and the parity+accessibility (NO_COLOR, keyboard-only)"
    - "The user's approval is recorded as a **APPROVED:** <date> by <user> line in .planning/design/APPROVAL.md — the single hard checkpoint (DLV-08) of the whole milestone"
    - "This line is the gate: NO backend logic is written for ANY surface (Phases 3–9) before it exists"
  artifacts:
    - path: ".planning/design/APPROVAL.md"
      provides: "the recorded DLV-08 approval (the **APPROVED:** line) gating all Phase 3–9 backend work"
      contains: "APPROVED:"
  key_links:
    - from: ".planning/design/APPROVAL.md **APPROVED:** line"
      to: "Phases 3–9 backend work"
      via: "DLV-05/DLV-08 gate — no backend before this line exists"
      pattern: "APPROVED:"
---

<objective>
The phase's single human checkpoint (DLV-08): present the complete, verified design reference set to the user and record their approval. This is the ONE hard stop of the entire autonomous build loop — the approved HTML + TUI-dummy screenshots become the frozen reference every later phase (3–9) is visually regression-tested against, and NO backend logic is written for any surface until the `**APPROVED:**` line exists in `.planning/design/APPROVAL.md`.

This plan is `autonomous: false` — it contains exactly one `checkpoint:human-verify` task. All implementation, capture, e2e, and consistency work is already complete (02-01..02-11); this plan only presents and records the human decision.

Note on human_verify_mode: this project's config default is `end-of-phase`, but DLV-08 is the milestone's ONE architectural hard checkpoint (REQUIREMENTS Resolved Decision #1), so it is intentionally modeled as a blocking `checkpoint:human-verify` — not a soft end-of-phase verify — per the phase brief.

DLV-02: the presentation is of the `/mui`-built mockups and the dummy screenshots, with the `agent-ui-ux-designer` critiques (all 0 OPEN) available for the user's review. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-VALIDATION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-11-SUMMARY.md
@.planning/design/REFERENCE-INDEX.md
@.planning/design/APPROVAL.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: ★ DLV-08 — present the complete design reference set and record user approval</name>
  <files>.planning/design/APPROVAL.md</files>
  <read_first>
    - .planning/design/REFERENCE-INDEX.md (02-11 — the enumerated complete reference set: every surface's html/tui PNGs + FIELDS.md + CRITIQUE.md)
    - .planning/design/APPROVAL.md (02-11 — the §6 A–F approval-checklist scaffold to walk; the **APPROVED:** line is absent and this task adds it on approval)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 6 (the DLV-08 approval checklist A–F: shell&IA, per-surface completeness, copy/fields/options/defaults FREEZE, safety affordances, parity&accessibility, explicit acknowledgment)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-11-SUMMARY.md (evidence the comprehensive e2e + full 50+50 capture + 0-OPEN critiques + no-backend gate all passed)
  </read_first>
  <what-built>
    The complete Phase 2 design reference set is ready for the single human checkpoint:
    - HTML/MUI-v7 mockups (terminal-skinned) for all 7 surfaces, every named state — 50 screenshots under `.planning/design/<surface>/html/`.
    - A navigable, backend-free Go TUI dummy (`cmd/gitid-dummy`) for the same surfaces/states — 50 screenshots under `.planning/design/<surface>/tui/`.
    - Per-surface `FIELDS.md` semantic manifests + `agent-ui-ux-designer` `CRITIQUE.md` (all 0 OPEN), enumerated in `.planning/design/REFERENCE-INDEX.md`.
    - Proven: the dummy is navigable on the real binary (comprehensive PTY e2e, zero writes under sandbox HOME) and imports zero backend packages (`go list -deps`).
    All built with the `/mui` skill under `agent-ui-ux-designer` direction (DLV-01/02); no backend logic has been written for any surface (DLV-05).
  </what-built>
  <action>
    Present the complete reference set to the user for the single DLV-08 approval (this is a presentation + record task, not implementation — all artifacts already exist). Surface the `.planning/design/REFERENCE-INDEX.md` index and, per surface, the `html/*.png` (MUI mockup) alongside the `tui/*.png` (TUI dummy), each surface's `FIELDS.md`, and its resolved `CRITIQUE.md`. Walk the user through the UX-DIRECTION §6 A–F approval checklist recorded in `.planning/design/APPROVAL.md`. If the user requests changes, do NOT record approval — report which surface plan the loop must route back to. If the user approves, record their decision by writing a line `**APPROVED:** <YYYY-MM-DD> by <user>` into `.planning/design/APPROVAL.md` (today's date + the user's name), verbatim. Do NOT write any backend logic for any surface — this line is the gate that unblocks Phases 3–9. Name `/mui` + `agent-ui-ux-designer` as the tools that produced the presented artifacts.
  </action>
  <how-to-verify>
    1. Open `.planning/design/REFERENCE-INDEX.md` — the index of the complete reference set.
    2. Review each surface side-by-side: its `html/*.png` (MUI mockup) vs `tui/*.png` (TUI dummy), plus its `FIELDS.md` (fields/order/labels/copy) and `CRITIQUE.md` (resolved parity findings). Surfaces: create-flow (12), git-screen (7), identity-manager (8), global-ssh (6), global-git (6), health (5), fixer (6).
    3. Walk the UX-DIRECTION §6 approval checklist recorded in `.planning/design/APPROVAL.md`:
       - A. Shell & IA — one shell, 5 number-key views + palette, reserved keys consistent, terminal skin reads as one product.
       - B. Per-surface completeness — every §4 named state present in BOTH media incl. empty/first-run states (list-empty, nothing-to-fix).
       - C. Copy/fields/options/defaults FREEZE — field order + labels final; helper/explanation copy final; option sets final (algorithm catalog, match strategy default gitdir, delete choices, reuse-vs-generate); defaults recipe-accurate (Port 443, IdentitiesOnly yes, gpg.format=ssh, init.defaultBranch=main, core.ignorecase=false, blank-prefix WYSIWYG); recipe fidelity (alias Host block, insteadOf, includeIf hasconfig/gitdir, allowed_signers byte-identical to user.email).
       - D. Safety affordances — four-beat mutation ceremony (preview→confirm→backup path→result) on every mutating surface; destructive actions never default to yes; irreversible full-delete strongest confirm; Health visibly read-only; advisory options visibly non-blocking.
       - E. Parity & accessibility — semantic parity critique resolved (0 OPEN); legible under NO_COLOR; keyboard-only operable.
       - F. Explicit acknowledgment — the approved screenshots become the FROZEN reference set for Phases 3–9, and no backend logic is written for any surface before approval.
    4. If anything needs changing, describe it — the loop routes back to the relevant surface plan (do NOT approve). If satisfied, approve.
    5. On approval, add a line `**APPROVED:** <YYYY-MM-DD> by <your-name>` to `.planning/design/APPROVAL.md` (or reply "approved" and the executor records it verbatim with today's date and your name).
  </how-to-verify>
  <verify>
    <human-check>The user has reviewed the complete reference set + §6 A–F checklist and either approved (an `**APPROVED:** <date> by <user>` line is written to .planning/design/APPROVAL.md) or requested changes (no line written; loop routes back).</human-check>
    <automated>grep -q '\*\*APPROVED:\*\*' .planning/design/APPROVAL.md</automated>
  </verify>
  <acceptance_criteria>
    - The user has reviewed the complete reference set and the §6 A–F checklist.
    - `.planning/design/APPROVAL.md` contains a `**APPROVED:**` line with a date and the approver: `grep -q '\*\*APPROVED:\*\*' .planning/design/APPROVAL.md`
    - No backend logic exists for any surface (the whole point of the gate): the dummy no-backend gate still holds and no `internal/*` backend surface implementation has been added under this phase.
  </acceptance_criteria>
  <resume-signal>Reply "approved" (the executor records the **APPROVED:** line), or describe the changes needed (the loop routes back to the relevant surface plan — nothing is approved).</resume-signal>
  <done>The user has reviewed the complete reference set and the §6 checklist and recorded a **APPROVED:** line in .planning/design/APPROVAL.md — the single DLV-08 checkpoint that unblocks Phase 3–9 backend work; no backend logic was written before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approval record ← human decision | the **APPROVED:** line is the single authoritative human gate; only the user authorizes it |
| Phases 3–9 backend work ← APPROVAL.md | backend work is gated on the presence of the **APPROVED:** line (a plan-ordering guarantee) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-APPROVE | Repudiation / Elevation of Privilege | backend work starting before genuine human approval | mitigate | DLV-08 blocking `checkpoint:human-verify`; the `**APPROVED:**` line is required; Phase 3+ plans `depends_on` this phase's completion. This is the milestone's single hard stop — never auto-approvable (auto_advance does not apply to this checkpoint). |
| T-02-STALE | Tampering | approving a reference set that no longer matches the artifacts | mitigate | 02-11 verified counts (50+50), 0-OPEN critiques, and comprehensive e2e immediately before this checkpoint; REFERENCE-INDEX.md enumerates exactly what is approved |
</threat_model>

## Artifacts this phase produces

- `.planning/design/APPROVAL.md` — updated with the `**APPROVED:** <date> by <user>` line: the single DLV-08 checkpoint record that gates all Phase 3–9 backend work

<verification>
- `.planning/design/APPROVAL.md` contains a `**APPROVED:**` line (date + approver) after the user signs off
- The §6 A–F checklist was walked; the frozen reference set (50+50 screenshots + FIELDS + resolved CRITIQUE) is what is approved
- No backend logic was written for any surface before the line exists (DLV-05/DLV-08)
</verification>

<success_criteria>
- DLV-08: the user has approved the complete HTML + TUI-dummy design; the approval is recorded as the single hard checkpoint of the milestone.
- The approved screenshots are the frozen reference set for the Phase 3–9 visual-regression gates.
- DLV-02: the presented artifacts were built with `/mui` + `agent-ui-ux-designer`; both named.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-12-SUMMARY.md` when done.
</output>
