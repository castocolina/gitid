---
phase: 02-design-all-mockups-checkpoint-1
plan: 12
type: execute
wave: 9
depends_on: [02-11, 02-13, 02-14, 02-15]
files_modified:
  - .planning/design/APPROVAL.md
autonomous: false
requirements: [DLV-08, DLV-02]
user_setup: []
must_haves:
  truths:
    - "The user is presented the COMPLETE design reference: (a) the INTERACTIVE WEB DEMO served at http://localhost:8747 and (b) the LIVE gitid-dummy Go TUI demo (built from cmd/gitid-dummy, 02-13), plus .planning/design/REFERENCE-INDEX.md, the 02-REDESIGN-SPEC.md frame spec, each surface's FIELDS.md, and the UX-DIRECTION §6 A–F approval checklist"
    - "The user signs off on copy, field order, options, and defaults (the FREEZE), the safety affordances (compressed 2-state ceremony with backup promise/receipt, destructive typed-confirm, advisory-not-blocking, read-only diagnostics), and parity+accessibility (both demos read as one product; NO_COLOR-legible severity words; keyboard-only operable)"
    - "The user signs off on the checkpoint-2 route-back polish (02-15, operationalizing the BINDING 02-DESIGN-DECISIONS-CHECKPOINT-2.md contract): in BOTH demos every form field is ONE constant-height row (focus = accent color + bracket + `▸`/2px-outline redundant cue, NO reflowing box); both radio groups (algorithm, match strategy) are ALWAYS-EXPANDED with `(←/→ change)` on the header; the web renders terminal-glyph checkboxes/radios (`☐/☑`, `○/●`); the MAIN NAV reads `[1] Identities · [2] Global SSH · [3] Global Git · [4] Doctor` with plain ←/→ switching top-level views and the current view rendered `ActiveNavDimmed` under a capturing pane; the WIZARD stepper is the reverted `Step n/4 · <label> ● ○ ○ ○` (active label bold+accent, never faint) with an always-visible step-conditional Shift-chord hint below it; the Git step has `Back (Esc)  [ Skip Git ]  [ Continue ]` on one row with both frozen hints below; Shift+←/→ works at EVERY step (incl. the previously-dead review step, back to Git) with forward validity-gated; clicking a field/radio/checkbox row focuses/selects/toggles it; Global Git `user.email (global fallback)` is editable + checkable (default off) framed so identity includeIf fragments always override it; and every affordance-audit footer is present"
    - "The user's approval is recorded as a **APPROVED:** <date> by <user-supplied approver> line in .planning/design/APPROVAL.md — the approver string is SUPPLIED BY THE USER and never inferred (the single hard checkpoint, DLV-08)"
    - "This line is the gate: NO backend logic is written for ANY surface (Phases 3–9) before it exists"
  artifacts:
    - path: ".planning/design/APPROVAL.md"
      provides: "the recorded DLV-08 approval (the **APPROVED:** line with a user-supplied approver) gating all Phase 3–9 backend work"
      contains: "APPROVED:"
  key_links:
    - from: ".planning/design/APPROVAL.md **APPROVED:** line"
      to: "Phases 3–9 backend work"
      via: "DLV-05/DLV-08 gate — no backend before this line exists"
      pattern: "APPROVED:"
---

<objective>
The phase's single human checkpoint (DLV-08): present the complete, verified design reference to the user and record their approval. This is the ONE hard stop of the entire autonomous build loop — the approved design (the interactive web demo + the live Go TUI demo, with REFERENCE-INDEX.md and the redesign spec as its record) is the reference every later phase (3–9) builds against, and NO backend logic is written for any surface until the `**APPROVED:**` line exists in `.planning/design/APPROVAL.md`.

**Deliverable change (checkpoint-tail replan):** the original presentation — 50 HTML + 50 TUI static reference PNGs — was rejected at the checkpoint and removed (commit 7453561). What is presented now is LIVE: (a) the interactive web demo (mockup-src, index route, `http://localhost:8747`) and (b) the live, executable `gitid-dummy` Go TUI demo built by 02-13, both mirroring `02-REDESIGN-SPEC.md` and seeded from the same recipe-faithful fixture data. Screenshots are no longer the approval artifact; they can be re-captured from the live demos later as development checks.

This plan is `autonomous: false` — one `checkpoint:human-verify` task. All implementation, e2e, gates, and consistency work is already complete (02-01..02-11 + 02-13 + the 02-14 checkpoint-feedback polish wave + the 02-15 checkpoint-2 route-back wave); this plan only presents and records the human decision. Because it `depends_on: [02-11, 02-13, 02-14, 02-15]`, a MISSING `.planning/phases/02-design-all-mockups-checkpoint-1/02-15-SUMMARY.md` — alongside `02-14-SUMMARY.md` — is a BLOCKER (each is the evidence that its wave actually ran); do NOT present or record approval without BOTH; neither is skippable. 02-15 is the wave that operationalizes the user-approved 02-DESIGN-DECISIONS-CHECKPOINT-2.md contract (D1–D9 + affordance audit) that the 02-12 feedback round produced, and it SUPERSEDES the parts of 02-14 the contract overturns (the field-contour box, the bracketed wizard stepper).

Review folded in (Codex 02-REVIEWS.md): LOW-12 the approver string MUST be SUPPLIED BY THE USER — the executor never infers a name from git config/email/etc.; if the user approves without giving a name, ask for it before writing the line. The evidence presented includes the machine-checkable gates from 02-11/02-13 (no-backend import ALLOWLIST, zero-writes PTY e2e, gate-no-backend-files), not only narratives.

Note on human_verify_mode: this project's config default is `end-of-phase`, but DLV-08 is the milestone's ONE architectural hard checkpoint (REQUIREMENTS Resolved Decision #1), so it is intentionally modeled as a blocking `checkpoint:human-verify`.

DLV-02: the presented web demo was built with the `/mui` skill under `agent-ui-ux-designer` direction; the TUI demo (02-13) mirrors it 1:1. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-REDESIGN-SPEC.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-13-SUMMARY.md
@.planning/design/REFERENCE-INDEX.md
@.planning/design/APPROVAL.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: ★ DLV-08 — present the live design demos and record user approval (user-supplied approver)</name>
  <files>.planning/design/APPROVAL.md</files>
  <read_first>
    - .planning/design/REFERENCE-INDEX.md (the authoritative reference map: web-demo serve instructions, key map, workflows to exercise, per-surface FIELDS.md/CRITIQUE.md index, removed-vs-kept inventory)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-REDESIGN-SPEC.md (the approved-direction frame spec both demos implement)
    - .planning/design/APPROVAL.md (the §6 A–F approval-checklist scaffold; the **APPROVED:** line is absent and this task adds it on approval)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 6 (the DLV-08 approval checklist A–F)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-13-SUMMARY.md (evidence the live TUI demo passed its gates: no-backend ALLOWLIST, zero-writes PTY walk, make test/lint/test-e2e, gate-no-backend-files)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-STYLE-SPEC.md (the semantic style contract: role table per medium, the arrow-key precedence rule, the emphasis-role parity dimensions — rewritten by 02-15 to the checkpoint-2 contract: single-row field-contour, reverted `Step n/4` stepper, reflow-removed row budget, and the `Conscious divergences from recipes/` note)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-DESIGN-DECISIONS-CHECKPOINT-2.md (the BINDING user-approved contract D1–D9 + affordance audit that 02-15 operationalizes — the design language the two demos now share)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-14-SUMMARY.md (evidence the round-2 polish landed: style contract, arrow-key precedence, atomic copy freeze — the substrate 02-15 revised)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-15-SUMMARY.md (evidence the checkpoint-2 route-back landed: single-row fields, always-expanded radios, glyph checkboxes, bracketed nav + ActiveNavDimmed + plain-arrow view switch, reverted `Step n/4` stepper + per-step chord hints, one-row Git buttons, hoisted chord gate working at EVERY step, click-to-focus, editable global-fallback user.email, affordance-audit footers — all gates + the raw-byte Shift-chord PTY test green, and the fresh agent-ui-ux-designer critique + code review resolved)
  </read_first>
  <what-built>
    The complete Phase 2 design reference is ready for the single human checkpoint — now as two LIVE, interactive demos instead of the removed static PNG set:
    - The INTERACTIVE WEB DEMO: the mockup SPA's index route (`.planning/design/mockup-src/src/demo/`, MUI v7 terminal skin, built with `/mui` under `agent-ui-ux-designer` direction) — a keyboard-driven, stateful walkthrough of every surface with dummy data; serve per REFERENCE-INDEX.md and open http://localhost:8747/.
    - The LIVE Go TUI DEMO: `cmd/gitid-dummy` (02-13) — a fully interactive Bubble Tea v2 app mirroring the web demo 1:1 per 02-REDESIGN-SPEC.md (numbered header tabs + health chip, live master-detail Identities, 4-pane-state create wizard with the two-stage test, edit/clone/delete, Global SSH Options + STORE-01 Storage sub-tabs, Global Git with the main-vs-master highlight, Doctor absorbing the Fixer with f/F fix ceremonies and live healing), seeded from `internal/dummytui/data.go` (recipe-faithful per `recipes/`).
    - Now carrying the checkpoint-2 route-back polish (02-15): single-row color-only fields, always-expanded radios, terminal-glyph checkboxes/radios, `[n]`-bracketed main nav with plain-arrow view switching + the ActiveNavDimmed state, the reverted `Step n/4 · <label> ● ○ ○ ○` wizard stepper with per-step Shift-chord hints, one-row Git buttons, a hoisted Shift-chord gate that works at every step, click-to-focus, an editable global-fallback user.email, and the affordance-audit footers.
    - The record around them: `.planning/design/REFERENCE-INDEX.md`, `02-REDESIGN-SPEC.md`, `02-STYLE-SPEC.md`, `02-DESIGN-DECISIONS-CHECKPOINT-2.md`, and each surface's `FIELDS.md` (semantic field contracts) + `CRITIQUE.md`.
    - Proven before presentation: the TUI demo imports ZERO backend packages (go list -deps ALLOWLIST), writes ZERO files (PTY e2e under sandboxed HOME on the real binary, incl. the raw-byte Shift-chord test), and the branch-level `gate-no-backend-files` confirms Phase 2 changed only design-allowlisted paths. No backend logic has been written for any surface (DLV-05).
  </what-built>
  <action>
    Present the complete design reference to the user for the single DLV-08 approval (presentation + record, not implementation — all artifacts already exist). First, refresh any stale wording inside `.planning/design/APPROVAL.md` that still describes the removed static PNG set so its checklist references the CURRENT deliverables: the interactive web demo (http://localhost:8747), the live `gitid-dummy` TUI demo, REFERENCE-INDEX.md, 02-REDESIGN-SPEC.md, and the per-surface FIELDS.md/CRITIQUE.md — keep the §6 A–F structure and the no-backend/user-supplied-approver rules intact; do NOT add the APPROVED line during this refresh. Then: serve the web demo per REFERENCE-INDEX.md and give the user the URL; build and hand the user the live TUI demo (`go build ./cmd/gitid-dummy` then run it in a ≥100x30 terminal). Walk the user through the UX-DIRECTION §6 A–F approval checklist recorded in `.planning/design/APPROVAL.md`, exercising the same workflows in BOTH demos. If the user requests changes, do NOT record approval — report which plan the loop must route back to: 02-15 for any checkpoint-2 contract dimension (single-row field shape, always-expanded radios, glyph checkboxes, bracketed nav / plain-arrow view switch / ActiveNavDimmed, the reverted `Step n/4` stepper, per-step Shift-chord hints, one-row Git buttons, click-to-focus, the editable global-fallback user.email, or any affordance-audit footer), 02-14 for a round-2 style-contract/precedence issue not overturned by the contract, 02-13 for a pre-polish live-TUI-demo structural issue, and the relevant mockup-src surface for a web-demo-only issue. If the user approves, record their decision by writing a line `**APPROVED:** <YYYY-MM-DD> by <approver>` into `.planning/design/APPROVAL.md`, where `<approver>` is a string the USER SUPPLIES (review LOW-12) — do NOT infer it from git config, the commit author, the email, or any other source; if the user approves but does not give a name, ASK for the approver string before writing the line, and do not write the line until it is provided. Do NOT write any backend logic for any surface — this line is the gate that unblocks Phases 3–9. Name `/mui` + `agent-ui-ux-designer` as the tools that produced the presented web demo the TUI mirrors.
  </action>
  <how-to-verify>
    1. Open `.planning/design/REFERENCE-INDEX.md` — the map of the design reference (demo run instructions, key map, workflows, per-surface docs).
    2. Run BOTH demos side by side: the web demo at http://localhost:8747 (serve per REFERENCE-INDEX.md) and the live TUI demo (`go build ./cmd/gitid-dummy && ./bin/... or go run ./cmd/gitid-dummy` in a ≥100x30 terminal). Exercise in each: tab nav 1–4 + health chip; live master-detail on Identities; the full create wizard (SSH form → algorithm → two-stage test incl. the simulate-failure toggle → Git form + match strategy with dual preview → ceremony); edit / clone / delete (scope choice + typed destructive confirm); Global SSH Options apply + Storage & preview (STORE-01) migration; Global Git baseline (main-vs-master) incl. the editable global-fallback user.email; Doctor f / F fix-all with live healing; `?` help legend; `q` quit.
    3. Walk the UX-DIRECTION §6 approval checklist recorded in `.planning/design/APPROVAL.md`:
       - A. Shell & IA — one frame in both media: numbered header tabs `[1] Identities · [2] Global SSH · [3] Global Git · [4] Doctor` (active = accent background) + health chip, breadcrumb line, contextual footer + reserved keys (Enter/Esc/?/Ctrl+P/q), the two demos read as one product.
       - B. Per-surface completeness — every surface and flow state is reachable LIVE in both demos, including empty/first-run and failure states (test-fail path, all-green Doctor).
       - C. Copy/fields/options/defaults FREEZE — field order + labels final; helper/explanation copy final; option sets final (algorithm catalog with disabled -sk rationale, match strategy default gitdir, delete choices, storage layouts); defaults recipe-accurate (Port 443, IdentitiesOnly yes, gpg.format=ssh, init.defaultBranch=main, core.ignorecase=false, blank-prefix WYSIWYG); recipe fidelity (alias Host block, includeIf hasconfig/gitdir, allowed_signers byte-identical to user.email).
       - D. Safety affordances — the 2-state ceremony (preview + backup promise → receipt) on every mutating flow; destructive actions never default-focused, irreversible full-delete requires the typed identity name; Doctor diagnoses read-only until an explicit fix; advisory options visibly non-blocking.
       - E. Parity & accessibility — the TUI demo mirrors the web demo's behavior and copy; severity/state always glyph + WORD (NO_COLOR-legible); keyboard-only operable.
       - E2. Interaction contract (02-15 checkpoint-2) — in BOTH demos: plain ←/→ switch top-level views 1–4 when no field/pane owns the key, and the current view dims to ActiveNavDimmed while a pane captures keys; inside the wizard ←/→ move sections when focus is not in a text input/select (forward gated on step validity, back always allowed), a focused input's ←/→ still move the cursor (never stolen), and Shift+←/→ is a focus-override chord that reaches step-nav from any focus AND works at EVERY step (including the review step, back to Git) but NEVER bypasses the forward validity gate; the wizard stepper reads `Step n/4 · <label> ● ○ ○ ○` (active label bold+accent, never faint) with a step-conditional Shift-chord hint directly below it; the Git step shows `Back (Esc)  [ Skip Git ]  [ Continue ]` on one row with both frozen hints below and `— needs user.name + a valid email` on the disabled Continue.
       - E3. Field shape, radios, glyphs, click, fallback (02-15) — every form field is ONE constant-height row (focus = accent color + bracket + `▸`/2px-outline redundant cue, no reflow); both radio groups (algorithm, match strategy) are always-expanded with `(←/→ change)` on the header; checkboxes/radios render terminal glyphs (`☐/☑`, `○/●`) on both sides; clicking a field/radio/checkbox row focuses/selects/toggles it; Global Git `user.email (global fallback)` is editable + checkable (default off) with copy framing it so identity includeIf fragments always override it; and every affordance-audit footer is present (Edit SSH / Configure Git / Clone panes now have footers; ceremonies show `Tab/←→ Cancel / Confirm`, etc.).
       - F. Explicit acknowledgment — the approved demos ARE the design reference for Phases 3–9 (the real TUI grows out of the gitid-dummy frame), and no backend logic is written for any surface before approval.
    4. If anything needs changing, describe it — the loop routes back to the relevant plan (do NOT approve). If satisfied, approve AND provide the approver name to record.
    5. On approval, a line `**APPROVED:** <YYYY-MM-DD> by <your-supplied-name>` is added to `.planning/design/APPROVAL.md` (reply "approved" WITH the approver name; the executor will ask if it is missing — it never guesses the name).
  </how-to-verify>
  <verify>
    <human-check>The user has reviewed both live demos + the §6 A–F checklist and either approved WITH a supplied approver name (an `**APPROVED:** <date> by <user-supplied name>` line is written to .planning/design/APPROVAL.md) or requested changes (no line written; loop routes back).</human-check>
    <automated>grep -qE '\*\*APPROVED:\*\* [0-9]{4}-[0-9]{2}-[0-9]{2} by .+' .planning/design/APPROVAL.md</automated>
  </verify>
  <acceptance_criteria>
    - The user has interacted with BOTH live demos (web at http://localhost:8747 and the gitid-dummy TUI) and reviewed the §6 A–F checklist.
    - `.planning/design/APPROVAL.md` contains a `**APPROVED:**` line with a date AND a non-empty, user-supplied approver: `grep -qE '\*\*APPROVED:\*\* [0-9]{4}-[0-9]{2}-[0-9]{2} by .+' .planning/design/APPROVAL.md` (the approver was supplied by the user, not inferred)
    - No backend logic exists for any surface: the whole-dummy no-backend ALLOWLIST still holds and `make gate-no-backend-files` confirms no backend file was changed under this phase
  </acceptance_criteria>
  <resume-signal>Reply "approved" AND supply the approver name (the executor records `**APPROVED:** <date> by <name>`; if the name is missing it will ask — it never infers it), or describe the changes needed (the loop routes back to the relevant plan — nothing is approved).</resume-signal>
  <done>The user has reviewed both live demos + the §6 checklist and recorded a **APPROVED:** line (with a user-supplied approver) in .planning/design/APPROVAL.md — the single DLV-08 checkpoint that unblocks Phase 3–9 backend work; no backend logic was written before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approval record ← human decision | the **APPROVED:** line is the single authoritative human gate; only the user authorizes it, and the approver string is user-supplied |
| Phases 3–9 backend work ← APPROVAL.md | backend work is gated on the presence of the **APPROVED:** line (a plan-ordering guarantee) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-APPROVE | Repudiation / Elevation of Privilege | backend work starting before genuine human approval | mitigate | DLV-08 blocking `checkpoint:human-verify`; the `**APPROVED:**` line is required; Phase 3+ plans `depends_on` this phase. Never auto-approvable (auto_advance does not apply). |
| T-02-ATTRIB | Repudiation | the approval attributed to an inferred/wrong approver | mitigate | The approver string is USER-SUPPLIED (review LOW-12); the executor asks if missing and never infers it; the acceptance regex requires a non-empty `by <name>` |
| T-02-STALE | Tampering | approving a reference that no longer matches the artifacts | mitigate | 02-13 verified the live TUI demo (no-backend ALLOWLIST, zero-writes PTY e2e, make test/lint/test-e2e), 02-15 re-ran every gate after the checkpoint-2 route-back (incl. the raw-byte Shift-chord PTY test + the 100×30 row-budget walk), and `make gate-no-backend-files` immediately precedes this checkpoint; the user approves the LIVE demos themselves, not stale captures; APPROVAL.md's checklist is refreshed to the current deliverables before presenting |
</threat_model>

## Artifacts this phase produces

- `.planning/design/APPROVAL.md` — checklist refreshed to the live-demo deliverables and, on approval, updated with the `**APPROVED:** <date> by <user-supplied approver>` line: the single DLV-08 checkpoint record that gates all Phase 3–9 backend work

<verification>
- `.planning/design/APPROVAL.md` contains a `**APPROVED:**` line (date + user-supplied approver) after the user signs off
- The §6 A–F checklist was walked against BOTH live demos (web + gitid-dummy TUI); what is approved is the live design they present (carrying the 02-15 checkpoint-2 route-back polish)
- No backend logic was written for any surface before the line exists (DLV-05/DLV-08)
</verification>

<success_criteria>
- **Code review (required before this plan is done):** on completion, request a fresh-context code review via the `superpowers:requesting-code-review` skill, verifying the work against this plan `must_haves` and every task `<acceptance_criteria>`; resolve all CRITICAL/HIGH findings before the plan is marked complete. Supplements — not replaces — the phase-level `/gsd-code-review` and the phase-level external cross-vendor (Codex) review the execution loop runs.
- DLV-08: the user has approved the complete design via the interactive web demo + the live Go TUI demo; the approval is recorded as the single hard checkpoint of the milestone, attributed to a user-supplied approver.
- The approved live demos are the design reference for the Phase 3–9 waves (the real TUI grows out of the approved gitid-dummy frame; regression checks may re-capture screenshots from the live demos as needed).
- DLV-02: the presented web demo was built with `/mui` + `agent-ui-ux-designer`; both named; the TUI demo mirrors it 1:1.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-12-SUMMARY.md` when done.
</output>
</content>
