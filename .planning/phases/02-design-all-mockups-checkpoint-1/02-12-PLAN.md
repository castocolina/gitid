---
phase: 02-design-all-mockups-checkpoint-1
plan: 12
type: execute
wave: 6
depends_on: [02-11]
files_modified:
  - .planning/design/APPROVAL.md
autonomous: false
requirements: [DLV-08, DLV-02]
user_setup: []
must_haves:
  truths:
    - "The user is presented the COMPLETE reference set (all HTML + all TUI screenshots across 7 surfaces, counts computed from the manifests), each surface's FIELDS.md semantic manifest + parity.json, and the UX-DIRECTION §6 A–F approval checklist"
    - "The user signs off on copy, field order, options, and defaults (the FREEZE), the safety affordances (four-beat ceremony, destructive-confirm, advisory-not-blocking, read-only Health), and the parity+accessibility (NO_COLOR, keyboard-only)"
    - "The user's approval is recorded as a **APPROVED:** <date> by <user-supplied approver> line in .planning/design/APPROVAL.md — the approver string is SUPPLIED BY THE USER and never inferred (the single hard checkpoint, DLV-08)"
    - "This line is the gate: NO backend logic is written for ANY surface (Phases 3–9) before it exists"
  artifacts:
    - path: ".planning/design/APPROVAL.md"
      provides: "the recorded DLV-08 approval (the **APPROVED:** line with a user-supplied approver) gating all Phase 3–9 backend work"
      contains: "APPROVED:"
  key_links:
    - from: ".planning/design/APPROVAL.md **APPROVED:** line"
      to: "Phases 3–9 backend work"
      via: "DLV-05/DLV-08 gate — no backend before this line exists"
      pattern: "APPROVED:"
---

<objective>
The phase's single human checkpoint (DLV-08): present the complete, verified design reference set to the user and record their approval. This is the ONE hard stop of the entire autonomous build loop — the approved HTML + TUI-dummy screenshots become the frozen reference every later phase (3–9) is visually regression-tested against, and NO backend logic is written for any surface until the `**APPROVED:**` line exists in `.planning/design/APPROVAL.md`.

This plan is `autonomous: false` — one `checkpoint:human-verify` task. All implementation, capture, e2e, gates, and consistency work is already complete (02-01..02-11); this plan only presents and records the human decision.

Review folded in (Codex 02-REVIEWS.md): LOW-12 the approver string MUST be SUPPLIED BY THE USER — the executor never infers a name from git config/email/etc.; if the user approves without giving a name, ask for it before writing the line. The evidence presented includes the machine-checkable `parity.json` (no unresolved row) and the 02-11 no-backend-files gate, not only the CRITIQUE.md narratives.

Note on human_verify_mode: this project's config default is `end-of-phase`, but DLV-08 is the milestone's ONE architectural hard checkpoint (REQUIREMENTS Resolved Decision #1), so it is intentionally modeled as a blocking `checkpoint:human-verify`.

DLV-02: the presentation is of the `/mui`-built mockups + the dummy screenshots, with the `agent-ui-ux-designer` critiques (parity.json all resolved) available. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-VALIDATION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-11-SUMMARY.md
@.planning/design/REFERENCE-INDEX.md
@.planning/design/APPROVAL.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: ★ DLV-08 — present the complete design reference set and record user approval (user-supplied approver)</name>
  <files>.planning/design/APPROVAL.md</files>
  <read_first>
    - .planning/design/REFERENCE-INDEX.md (02-11 — the enumerated complete reference set: every surface's html/tui PNGs + FIELDS.md + parity.json + CRITIQUE.md; the COMPUTED counts)
    - .planning/design/APPROVAL.md (02-11 — the §6 A–F approval-checklist scaffold; the **APPROVED:** line is absent and this task adds it on approval)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 6 (the DLV-08 approval checklist A–F)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-11-SUMMARY.md (evidence the comprehensive e2e + full capture + parity.json 0-unresolved + no-backend ALLOWLIST + final-key-owners + no-backend-files gate all passed)
  </read_first>
  <what-built>
    The complete Phase 2 design reference set is ready for the single human checkpoint:
    - HTML/MUI-v7 mockups (terminal-skinned) for all 7 surfaces, every named state, under `.planning/design/<surface>/html/` (count computed from the manifests).
    - A navigable, backend-free Go TUI dummy (`cmd/gitid-dummy`) for the same surfaces/states, under `.planning/design/<surface>/tui/`.
    - Per-surface `FIELDS.md` (human), `parity.json` (machine-checkable, 0 rows unresolved), and `agent-ui-ux-designer` `CRITIQUE.md`, enumerated in `.planning/design/REFERENCE-INDEX.md`.
    - Proven: the dummy is navigable on the real binary (comprehensive PTY e2e, re-homing + breadcrumb screen-ID + signature, zero writes under sandbox HOME), imports zero backend packages (go list -deps ALLOWLIST), owns keys 1–5 correctly, and Phase 2 changed only the allowed directories (no-backend-files gate).
    All built with the `/mui` skill under `agent-ui-ux-designer` direction (DLV-01/02); no backend logic has been written for any surface (DLV-05).
  </what-built>
  <action>
    Present the complete reference set to the user for the single DLV-08 approval (presentation + record, not implementation — all artifacts already exist). Surface `.planning/design/REFERENCE-INDEX.md` and, per surface, the `html/*.png` (MUI mockup) alongside the `tui/*.png` (TUI dummy), each surface's `FIELDS.md`, its `parity.json` (all rows resolved), and its `CRITIQUE.md`. Walk the user through the UX-DIRECTION §6 A–F approval checklist recorded in `.planning/design/APPROVAL.md`. If the user requests changes, do NOT record approval — report which surface plan the loop must route back to. If the user approves, record their decision by writing a line `**APPROVED:** <YYYY-MM-DD> by <approver>` into `.planning/design/APPROVAL.md`, where `<approver>` is a string the USER SUPPLIES (review LOW-12) — do NOT infer it from git config, the commit author, the email, or any other source; if the user approves but does not give a name, ASK for the approver string before writing the line, and do not write the line until it is provided. Do NOT write any backend logic for any surface — this line is the gate that unblocks Phases 3–9. Name `/mui` + `agent-ui-ux-designer` as the tools that produced the presented artifacts.
  </action>
  <how-to-verify>
    1. Open `.planning/design/REFERENCE-INDEX.md` — the index of the complete reference set (counts computed from the manifests).
    2. Review each surface side-by-side: its `html/*.png` (MUI mockup) vs `tui/*.png` (TUI dummy), plus its `FIELDS.md`, its `parity.json` (every row `status: resolved`), and its `CRITIQUE.md`. Surfaces: create-flow, git-screen, identity-manager, global-ssh, global-git, health, fixer.
    3. Walk the UX-DIRECTION §6 approval checklist recorded in `.planning/design/APPROVAL.md`:
       - A. Shell & IA — one shell, 5 number-key views + palette, reserved keys consistent, the `<surface>/<screen>` breadcrumb, terminal skin reads as one product.
       - B. Per-surface completeness — every §4 named state present in BOTH media incl. empty/first-run states (list-empty, nothing-to-fix).
       - C. Copy/fields/options/defaults FREEZE — field order + labels final; helper/explanation copy final; option sets final (algorithm catalog, match strategy default gitdir, delete choices, reuse-vs-generate); defaults recipe-accurate (Port 443, IdentitiesOnly yes, gpg.format=ssh, init.defaultBranch=main, core.ignorecase=false, blank-prefix WYSIWYG); recipe fidelity (alias Host block, insteadOf, includeIf hasconfig/gitdir, allowed_signers byte-identical to user.email).
       - D. Safety affordances — four-beat mutation ceremony on every mutating surface; destructive actions never default to yes; irreversible full-delete strongest confirm; Health visibly read-only; advisory options visibly non-blocking.
       - E. Parity & accessibility — semantic parity resolved (every parity.json row resolved); legible under NO_COLOR; keyboard-only operable.
       - F. Explicit acknowledgment — the approved screenshots become the FROZEN reference set for Phases 3–9, and no backend logic is written for any surface before approval.
    4. If anything needs changing, describe it — the loop routes back to the relevant surface plan (do NOT approve). If satisfied, approve AND provide the approver name to record.
    5. On approval, a line `**APPROVED:** <YYYY-MM-DD> by <your-supplied-name>` is added to `.planning/design/APPROVAL.md` (reply "approved" WITH the approver name; the executor will ask if it is missing — it never guesses the name).
  </how-to-verify>
  <verify>
    <human-check>The user has reviewed the complete reference set + §6 A–F checklist and either approved WITH a supplied approver name (an `**APPROVED:** <date> by <user-supplied name>` line is written to .planning/design/APPROVAL.md) or requested changes (no line written; loop routes back).</human-check>
    <automated>grep -qE '\*\*APPROVED:\*\* [0-9]{4}-[0-9]{2}-[0-9]{2} by .+' .planning/design/APPROVAL.md</automated>
  </verify>
  <acceptance_criteria>
    - The user has reviewed the complete reference set and the §6 A–F checklist.
    - `.planning/design/APPROVAL.md` contains a `**APPROVED:**` line with a date AND a non-empty, user-supplied approver: `grep -qE '\*\*APPROVED:\*\* [0-9]{4}-[0-9]{2}-[0-9]{2} by .+' .planning/design/APPROVAL.md` (the approver was supplied by the user, not inferred)
    - No backend logic exists for any surface: the whole-dummy no-backend ALLOWLIST still holds and the 02-11 no-backend-files gate confirms no backend file was changed under this phase
  </acceptance_criteria>
  <resume-signal>Reply "approved" AND supply the approver name (the executor records `**APPROVED:** <date> by <name>`; if the name is missing it will ask — it never infers it), or describe the changes needed (the loop routes back to the relevant surface plan — nothing is approved).</resume-signal>
  <done>The user has reviewed the complete reference set + the §6 checklist and recorded a **APPROVED:** line (with a user-supplied approver) in .planning/design/APPROVAL.md — the single DLV-08 checkpoint that unblocks Phase 3–9 backend work; no backend logic was written before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approval record ← human decision | the **APPROVED:** line is the single authoritative human gate; only the user authorizes it, and the approver string is user-supplied |
| Phases 3–9 backend work ← APPROVAL.md | backend work is gated on the presence of the **APPROVED:** line (a plan-ordering guarantee) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-APPROVE | Repudiation / Elevation of Privilege | backend work starting before genuine human approval | mitigate | DLV-08 blocking `checkpoint:human-verify`; the `**APPROVED:**` line is required; Phase 3+ plans `depends_on` this phase. Never auto-approvable (auto_advance does not apply). |
| T-02-ATTRIB | Repudiation | the approval attributed to an inferred/wrong approver | mitigate | The approver string is USER-SUPPLIED (review LOW-12); the executor asks if missing and never infers it; the acceptance regex requires a non-empty `by <name>` |
| T-02-STALE | Tampering | approving a reference set that no longer matches the artifacts | mitigate | 02-11 verified computed counts, whole-set parity.json 0-unresolved, the comprehensive e2e, and the no-backend-files gate immediately before this checkpoint; REFERENCE-INDEX enumerates exactly what is approved |
</threat_model>

## Artifacts this phase produces

- `.planning/design/APPROVAL.md` — updated with the `**APPROVED:** <date> by <user-supplied approver>` line: the single DLV-08 checkpoint record that gates all Phase 3–9 backend work

<verification>
- `.planning/design/APPROVAL.md` contains a `**APPROVED:**` line (date + user-supplied approver) after the user signs off
- The §6 A–F checklist was walked; the frozen reference set (computed-count screenshots + FIELDS + resolved parity.json) is what is approved
- No backend logic was written for any surface before the line exists (DLV-05/DLV-08)
</verification>

<success_criteria>
- DLV-08: the user has approved the complete HTML + TUI-dummy design; the approval is recorded as the single hard checkpoint of the milestone, attributed to a user-supplied approver.
- The approved screenshots are the frozen reference set for the Phase 3–9 visual-regression gates.
- DLV-02: the presented artifacts were built with `/mui` + `agent-ui-ux-designer`; both named.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-12-SUMMARY.md` when done.
</output>
