---
phase: 02-design-all-mockups-checkpoint-1
plan: 13
type: execute
wave: 6
depends_on: [02-11]
files_modified:
  - cmd/gitid-dummy/main.go
  - internal/dummytui/doc.go
  - internal/dummytui/store.go
  - internal/dummytui/store_test.go
  - internal/dummytui/fixplans.go
  - internal/dummytui/fixplans_test.go
  - internal/dummytui/frame.go
  - internal/dummytui/frame_test.go
  - internal/dummytui/ceremony.go
  - internal/dummytui/ceremony_test.go
  - internal/dummytui/app.go
  - internal/dummytui/app_test.go
  - internal/dummytui/identities.go
  - internal/dummytui/identities_test.go
  - internal/dummytui/globalssh.go
  - internal/dummytui/globalssh_test.go
  - internal/dummytui/globalgit.go
  - internal/dummytui/globalgit_test.go
  - internal/dummytui/doctor.go
  - internal/dummytui/doctor_test.go
  - e2e/harness_test.go
  - e2e/dummy_demo_e2e_test.go
autonomous: true
requirements: [DLV-05, DLV-02]
user_setup: []
must_haves:
  truths:
    - "Building and running `cmd/gitid-dummy` in a 100x30 (or larger) terminal launches a LIVE, fully interactive Bubble Tea v2 demo — every action really mutates in-memory state seeded from internal/dummytui/data.go; NO backend package is imported and ZERO files are written under a sandboxed HOME"
    - "The app frame mirrors the interactive web demo (mockup-src/src/demo/) 1:1 per 02-REDESIGN-SPEC.md: one header row with brand + numbered nav tabs `1 Identities · 2 Global SSH · 3 Global Git · 4 Doctor` (active tab reverse-video) + a right health chip with per-severity counts (`N ids · ! w · ✗ e`, `✓ ok` when clean), a thin dim breadcrumb line, a transient status line, and a contextual-only footer plus the reserved keys Enter/Esc/?/Ctrl+P/q — no vim keys, no navigation in the footer"
    - "Identities is LIVE master-detail: arrow selection re-renders the right detail pane immediately (no Enter); sidebar rows carry tone glyph + S/G capability pips + N⚑ finding flags + an inline legend line; the right pane hosts the 4-pane-state create wizard (SSH form → two-stage test with a demo failure toggle → full Git form with Back/Skip/Continue and dual fragment/includeIf preview → 2-state ceremony), edit-SSH (SAME form, identity fields locked), clone, and delete with scope choice + typed destructive confirm — all driving the reducer so the sidebar and header chip update live"
    - "Doctor absorbs the Fixer (FIX-02): auto-scan on first entry, findings grouped SSH → per-identity (+ global) → Git with the locked severity contract (~ info cyan · ! warning yellow · ✗ error/critical red, word always shown), `f` fixes the selected finding and `F` walks every fixable finding through the SAME per-fix ceremony with a `k / n fixed` counter; each success removes the finding LIVE, decrements the header chip, and heals identity states"
    - "Global SSH has [Options] and [Storage & preview] (STORE-01) sub-tabs switched with ←/→; Global Git is master-detail over the 11-option baseline with the main-vs-master highlight; both apply through the shared 2-state ceremony and are visibly advisory, never blocking"
    - "Preview areas AND their labels render dimmer than editable fields (round-3 feedback: lipgloss Faint + dashed border so read-only previews never read as inputs)"
    - "Unit tests cover the reducer and the model's pure Update/View functions; a PTY e2e drives the REAL built gitid-dummy binary at 100x30 via raw keystrokes through launch + tab nav + a full wizard walk + a fix ceremony, asserting zero writes under sandbox HOME; make test / make lint / make test-e2e / make gate-no-backend-files are green"
  artifacts:
    - path: "cmd/gitid-dummy/main.go"
      provides: "the live demo entry point (tea.NewProgram over the internal/dummytui app model, alt-screen + mouse)"
      contains: "dummytui"
    - path: "internal/dummytui/store.go"
      provides: "DemoState + typed actions + pure Reduce() mirroring mockup-src/src/demo/store.ts, seeded from data.go"
      contains: "Reduce"
    - path: "internal/dummytui/app.go"
      provides: "root Bubble Tea v2 model: 4-tab routing, help overlay with the full 8-state legend, Ctrl+P palette, quit prompt, key-handler stack"
      contains: "tea.Model"
    - path: "internal/dummytui/identities.go"
      provides: "live master-detail Identities surface: sidebar pips/flags/legend, SSH-first detail, 4-pane-state create wizard, edit/clone/delete/fix panes"
      min_lines: 300
    - path: "internal/dummytui/doctor.go"
      provides: "Doctor surface absorbing the Fixer: auto-scan, grouped findings, f/F fix ceremonies with counter, live healing"
      contains: "Fix all"
    - path: "e2e/dummy_demo_e2e_test.go"
      provides: "PTY e2e on the real gitid-dummy binary: raw-keystroke walk (tabs, wizard, fix) + zero-writes sandbox assertion at 100x30"
      contains: "TestDummyDemo"
  key_links:
    - from: "cmd/gitid-dummy/main.go"
      to: "internal/dummytui app model"
      via: "tea.NewProgram(dummytui.NewApp(), ...)"
      pattern: "NewApp"
    - from: "internal/dummytui/store.go"
      to: "internal/dummytui/data.go"
      via: "seed: IdentityManagerRows + HealthFindings + GlobalSSHOptions + GlobalGitOptions + AlgorithmCatalog"
      pattern: "IdentityManagerRows"
    - from: "e2e/dummy_demo_e2e_test.go"
      to: "cmd/gitid-dummy real binary under sandboxed HOME"
      via: "pty.StartWithSize at 100x30, raw keystrokes, vt emulator snapshots"
      pattern: "gitid-dummy"
---

<objective>
Rebuild `cmd/gitid-dummy` as a LIVE, executable, interactive Go TUI demo — dummy data, in-memory state, NO backend — mirroring the interactive web demo (`.planning/design/mockup-src/src/demo/`) 1:1 per `02-REDESIGN-SPEC.md`. This replaces the removed static reference set (50 HTML + 50 TUI PNGs + static screen dummy, removed in commit 7453561 after the checkpoint rejected the static paradigm) as the TUI half of what the 02-12 human checkpoint presents, and is the starting point for the Phase 3–9 backend waves: the real product TUI will grow out of this frame.

The demo implements the approved-direction frame spec at 100x30 minimum: numbered header nav tabs (reverse-video active) + health chip with per-severity counts, breadcrumb line, live master-detail on Identities, the 4-pane-state create wizard with the two-stage ssh test, edit = same form with locked identity fields, clone, delete with scope choice + typed destructive confirm, Global SSH with Options + STORE-01 Storage sub-tabs, Global Git master-detail with the main-vs-master highlight, Doctor absorbing the Fixer (auto-scan, grouped findings, f fix / F fix-all with counter, live healing), `?` help overlay with the full legend, and a `q` quit prompt. All state lives in a reducer mirroring the web `store.ts` actions, seeded from `internal/dummytui/data.go` (the preserved Go mirror of recipeFixtures.ts — recipe-faithful per `recipes/`, the North Star).

**Framework decision (recorded):** Bubble Tea v2 is retained (charm.land/bubbletea/v2 + lipgloss/v2 + bubbles/v2, already pinned in go.mod per CLAUDE.md's stack). `github.com/grindlemire/go-tui` was evaluated at the user's suggestion and REJECTED as too immature: very new, minimal adoption/ecosystem, unproven maintenance — versus the pinned charm.land v2 stack whose pure Update/View (Elm architecture) is exactly what enables this project's proven PTY/golden test method (e2e/ui_pty_e2e_test.go, internal/screenshot).

Screenshots are NOT a deliverable of this plan — the live demos themselves are what 02-12 presents. PNG captures can be re-taken later from this live demo via `internal/screenshot` as development checks, as needed (REFERENCE-INDEX.md "Kept deliberately").

Purpose: DLV-05 (a navigable Go TUI dummy with full navigation and NO backend logic, proven backend-free before any presentation) + DLV-02 (the TUI half of the design reference the checkpoint approves) — now as a LIVE interactive demo instead of static captures.
Output: `cmd/gitid-dummy` live app, `internal/dummytui/{store,fixplans,frame,ceremony,app,identities,globalssh,globalgit,doctor}.go` + tests, `e2e/dummy_demo_e2e_test.go` PTY walk, all gates green.

DLV-02: the web demo this mirrors was built with `/mui` under `agent-ui-ux-designer` direction; this plan's job is FIDELITY to that approved-direction demo and to 02-REDESIGN-SPEC.md — divergences are review findings, not design freedom.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-REDESIGN-SPEC.md
@.planning/design/REFERENCE-INDEX.md
@recipes/README.md
@internal/dummytui/data.go
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foundation — reducer store, app frame, 2-state ceremony, overlays, cmd/gitid-dummy entry point</name>
  <files>cmd/gitid-dummy/main.go, internal/dummytui/doc.go, internal/dummytui/store.go, internal/dummytui/store_test.go, internal/dummytui/fixplans.go, internal/dummytui/fixplans_test.go, internal/dummytui/frame.go, internal/dummytui/frame_test.go, internal/dummytui/ceremony.go, internal/dummytui/ceremony_test.go, internal/dummytui/app.go, internal/dummytui/app_test.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-REDESIGN-SPEC.md (§1 app frame, §6 ceremony compression, §7 keyboard model — the contract this task implements)
    - .planning/design/mockup-src/src/demo/store.ts (the reducer to mirror: DemoState, all 12 action types, healthRollup/findingCounts/newBackupPath/findingsFor helpers, the findingIdentity attribution map)
    - .planning/design/mockup-src/src/demo/Frame.tsx (header zones, breadcrumb join with ›, RESERVED footer keys, health chip content)
    - .planning/design/mockup-src/src/demo/MutationCeremony.tsx (2-state ceremony semantics incl. PreviewLabel/PreviewBlock dimming — round-3 feedback)
    - .planning/design/mockup-src/src/demo/DemoApp.tsx (HELP_KEYS table, the full 8-state LEGEND, key-routing precedence: dialogs > local handler stack > tab keys > ?/q)
    - .planning/design/mockup-src/src/demo/fixplans.ts (PlanFor per finding id)
    - internal/dummytui/data.go (the seed fixtures — every string the demo renders comes from here, NEVER re-typed)
    - CLAUDE.md (charm.land v2 import paths; TDD authoring order; English-only)
  </read_first>
  <action>
    Write failing tests first (CLAUDE.md TDD authoring order), then implement. `internal/dummytui/store.go`: mirror `store.ts` in Go — a `DemoIdentity` struct (embed/copy IdentityManagerRow fields + GitName, GitEmail, MatchStrategy, Hostname string, Port int), `DemoFinding` (HealthFinding + Identity string), `SSHStorageLayout` ("sentinel"|"include"), `DemoState{Identities, Findings, Scanned, SSHApplied []string, GitBaselineApplied bool, SSHStorage, Backups []string}`. Seed function builds initial state from data.go: IdentityManagerRows (rows with a GitFragmentPath get GitName "`<name>` identity" / GitEmail "you@`<name>`.example" like the web seed) and HealthFindings with the findingIdentity attribution map (ssh-key-perms-archived→archived, ssh-identitiesonly-contradiction→clientB, git-includeif-missing-fragment→legacy, git-opensource-no-host-block→opensource; ssh-duplicate-host-star stays global). Define a typed Action set mirroring store.ts exactly — AddIdentity, ConfigureGit, CloneIdentity, DeleteIdentity(scope everything|git-only), NewKey, MarkScanned, FixFinding, ApplySSH(keys), ApplyGitBaseline, EditSSH, SetSSHStorage, Reset — and a pure `Reduce(DemoState, Action) DemoState` implementing the same transitions (delete-everything also drops the identity's findings; fix of git-includeif-missing-fragment heals "legacy" to complete; clone copies the source with a new key path/alias/fragment and the "Cloned from" note; edit-ssh updates sshHost/hostname/port). Port helpers: `FindingCounts` (warnings; errors = error+critical), `HealthRollup`, `FindingsFor(state, name)`, `NewBackupPath(file)` (timestamped `<file>.backup.<RFC3339-with-dashes>` shape). `fixplans.go`: `PlanFor(DemoFinding) FixPlan{File, Diff, Destructive *struct{ConfirmWord, Warning}, Result}` mirroring fixplans.ts per finding id — the contradiction fix reuses data.go's FixerFixPreviewLines verbatim and is destructive with confirm word "clientb.github.com". `frame.go`: pure render helpers over lipgloss/v2 — `RenderFrame(width, height, ...)` producing the §1 chrome: ONE header row `gitid` + flat square tabs `1 Identities · 2 Global SSH · 3 Global Git · 4 Doctor` (number part of the label; active = lipgloss Reverse(true)) + right-aligned health chip (`N ids · ✓ ok` when clean, else `N ids · ! <w> ✗ <e>`); a thin FAINT breadcrumb line joining `<Tab label> › <crumbs...>` with ›; the body; a status line (transient feedback region); a footer with CONTEXTUAL action hints first then the reserved `Enter activate · Esc back · ? help · Ctrl+P palette · q quit` — never navigation, never vim keys. Export shared `PreviewLabel(string)` and `PreviewBlock(text string, diff bool)` render helpers that are deliberately DIMMER than field labels/inputs (lipgloss Faint(true) + a dashed/normal-dim border; diff mode colors leading + green and - red) — round-3 feedback: previews must never read as editable. `ceremony.go`: a self-contained 2-state ceremony component model (§6) usable inside any pane — state A: heading, `Touches <targets>` line, backup PROMISE lines with "(written first — restore it to undo)", the dim preview block, optional Destructive{ConfirmWord, Warning} gating: a typed-confirm text input (bubbles/v2 textinput) must equal ConfirmWord before the confirm action enables, and the affirmative is NEVER default-focused when destructive (Cancel focused); Enter/y confirms when enabled, Esc cancels; state B: receipt `✓ <result>` + `Wrote → <target>` per target + `Backed up → <path>` per backup, Enter = Done. It reports done/cancel to the host pane via a callback or returned message — the host dispatches the reducer action on done. `app.go`: the root `tea.Model` (`NewApp()`) — holds DemoState, active tab (identities|global-ssh|global-git|doctor), per-tab child models, overlay state (help, palette, quit prompt), a transient status message, and width/height from tea.WindowSizeMsg (design target 100x30 minimum; render adaptively above it). Key routing precedence mirrors DemoApp.tsx: open overlay consumes keys first (Esc closes; palette filters + Enter opens first match); else the active screen's local handler (forms/ceremonies own their keys, returning handled/not); else globals: "1".."4" switch tabs, "?" opens help, "ctrl+p" opens palette, "q" opens the quit prompt (Enter or y quits via tea.Quit, Esc stays — a real quit, unlike the browser demo). Help overlay renders the full key table (HELP_KEYS, adapted: q really quits; Ctrl+P palette lists views/actions — no browser ref routes) AND the full 8-state legend table (tone glyph · state word · S pip · G pip · meaning) copied from DemoApp.tsx's LEGEND, sourcing glyphs from data.go's IdentityManagerGlyphByState. Palette: filterable list of the 4 views + help (bubbles textinput + list). `cmd/gitid-dummy/main.go`: tea.NewProgram(NewApp(), tea.WithAltScreen(), mouse cell motion enabled per §7 full mouse support), exit non-zero on error; a trivial main_test.go is NOT required — app behavior is tested in the package. Update `doc.go`: the package now HOLDS the live demo (no longer "upcoming"); keep the recipes/North-Star provenance paragraph. NO imports of any first-party package other than internal/dummytui itself from cmd/gitid-dummy (the DLV-05 allowlist, re-verified in Task 3). No new third-party dependencies: charm.land bubbletea/bubbles/lipgloss v2 are already pinned in go.mod; creack/pty + charmbracelet/x/vt are already e2e test deps — no package-legitimacy checkpoint needed.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy && go test -race ./internal/dummytui/... && make lint</automated>
  </verify>
  <acceptance_criteria>
    - Table-driven reducer tests cover EVERY action type (add/configure-git/clone/delete-both-scopes/new-key/mark-scanned/fix-finding incl. legacy healing/apply-ssh/apply-git-baseline/edit-ssh/set-ssh-storage/reset) and Reduce never mutates its input (assert on the prior state after the call)
    - FindingCounts/HealthRollup tests pin the seeded counts (1 warning; 3 error+critical → chip `8 ids · ! 1 ✗ 3`) and the all-clean `✓ ok` variant
    - Frame render test (ANSI-stripped) shows all four numbered tab labels, the breadcrumb joined with ›, the reserved footer keys, and the health chip counts; active-tab reverse-video asserted via the styled output containing the reverse SGR around the active label
    - Ceremony test: destructive confirm stays disabled until the typed word matches ConfirmWord exactly (Enter before that is a no-op), affirmative not default-focused when destructive, Esc in state A cancels without dispatch, receipt lists Wrote→/Backed up→ lines
    - Help overlay contains all 8 MGR-02 state words and the S/G pip legend; `q` then Enter produces tea.Quit; `q` then Esc stays
    - `make lint` passes (zero tolerance)
  </acceptance_criteria>
  <done>The live app skeleton runs: reducer + frame + ceremony + overlays proven by unit tests on pure Update/View, cmd/gitid-dummy builds, previews render visibly dimmer than fields, and nothing imports a backend package.</done>
</task>

<task type="auto">
  <name>Task 2: Identities surface — live master-detail, 4-pane-state create wizard, edit/clone/delete/fix panes</name>
  <files>internal/dummytui/identities.go, internal/dummytui/identities_test.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-REDESIGN-SPEC.md (§2 live master-detail + 8-state mapping, §3 wizard pane-states, §6 ceremony, §7 keys)
    - .planning/design/mockup-src/src/demo/screens/Identities.tsx (the exact behavior to mirror: pips(), PaneMode set, providerDefaults, auto-join/hostTouched logic, CreateWizard states + validation + finish(), edit lockIdentity, clone validation, delete scope dialog + destructive confirm, per-finding fix, crumbs + footer actions per pane)
    - internal/dummytui/data.go (AlgorithmCatalog incl. -sk MacOS "Needs libfido2..." entries; CreateFlowTestStage1/2Command flag order; GitScreenIncludeIfGitdirLine/HasconfigLine; GitScreenFragmentText shape; IdentityManagerDeleteChoice* copy)
  </read_first>
  <action>
    Write failing tests first, then implement `identities.go` as the Identities tab child model, mirroring Identities.tsx behavior-for-behavior. SIDEBAR (~38% width, always visible, dimmed to ~75% emphasis while a form/ceremony pane is open): top inline legend line `S ssh · G git   ✓ ok  ! attn  ✗ broken` (faint); one row per identity: tone glyph colored by state tone (data.go IdentityManagerGlyphByState; tone success/warning/error per the web toneColor mapping), bold name, `N⚑` finding-count flag (warning color) when FindingsFor > 0, S/G capability pips per spec §2 (S: ✗ if state key-missing, ✓ if SSHHost set, – otherwise; G: ✗ if fragment-path-missing, ✓ if GitFragmentPath set, – otherwise; pip colors ✓ green / – dim gray / ✗ red), and a one-line ellipsized faint note. Arrow ↑/↓ moves the selection and the right pane re-renders the detail IMMEDIATELY — no Enter, no view switch. PANE MODES mirroring the web PaneMode union: detail | create | edit-ssh | git | clone | delete(scope) | fix(findingID), plus the delete-scope chooser. DETAIL pane: header = name + state chip (glyph + full state WORD — the only place the word appears outside help); SSH section FIRST ("SSH — shown first, always"): Host alias / Hostname · Port · User git / IdentityFile / IdentitiesOnly yes, or the warning `! No gitid-managed Host block — relies on the global SSH config.`; Git section: fragment/author/signing+strategy lines, or `! Git not configured — no fabricated values shown.` + a `[Configure now]` action (key g); read-only Global-baseline strip (FAINT, GITUI-01 kept intact): `init.defaultBranch=main · core.ignorecase=false · autocrlf=input/lf · push.autoSetupRemote=true · pull.rebase=true · merge=diff3` + `Edit in Global Git (3)` hint reflecting state.GitBaselineApplied ("applied ✓" vs "not applied yet"); findings sub-panel `Findings (n) — same data the Doctor shows (4)`: per finding severity glyph + WORD (colored), title, `Fix…` when SuggestedFix exists (else "info only"), plus an `Open the Doctor (4)` hint — selecting Fix… swaps the pane to the compressed ceremony built from fixplans.PlanFor (FixFinding on done; the sidebar/chip heal live). Detail-mode keys (also footer hints, every action reachable as a focusable control too): n new · e edit SSH · g configure Git · c clone · d delete. Esc from ANY form pane returns to detail without dispatching (never destructive). CREATE WIZARD (4 pane-states, slim `Step n/4 · <label>` dots ● ○; crumb "New identity"): STATE 1 SSH details — one field per row (round-3): Provider (freeform input with suggestions github.com/gitlab.com/bitbucket.org; picking/typing github.com defaults hostname ssh.github.com + port 443, anything else defaults hostname=provider + port 22, and the defaults stop applying once the user edits hostname/port); Alias prefix (default "acme"-like sensible demo default is NOT required — start empty-or-"acme" matching the web default "acme"; blank prefix → SSH Host = the provider host verbatim, WYSIWYG, helper says so; duplicate resulting name shows `"<name>" already exists — pick another prefix.` and blocks Next); SSH Host (alias) auto-joined `<prefix>.<provider>`, editable — once manually edited the auto-join stops and the helper flips to "Manually edited — auto-join off"; Real hostname; Port (digits-only validation); Key algorithm select over data.go AlgorithmCatalog — ed25519 `★ recommended` default, the -sk entries DISABLED with the rationale rendered per option: `Disabled: needs libfido2 + a FIDO2 security key — none detected on this machine` (helper notes gitid probes the local toolchain, KEY-03/PLAT-01; demo simulates no FIDO2 key); below, `Live Host-block preview — written exactly like this on confirm` as a DIM PreviewBlock rebuilding on every keystroke (Host/Hostname/Port/User git/IdentityFile ~/.ssh/id_ed25519_<name>/IdentitiesOnly yes). Enter/Next advances only when valid. STATE 2 Test — info line "Key <path> generated (<algo>). Both stages run against /tmp/gitid-test-….config — your live ~/.ssh/config is untouched until the final confirm."; the DEMO CONTROL failure toggle (checkbox: "simulate a provider failure (key not registered) to preview the error path", faint sub-note "Review aid only… locks while a stage is running and once the test has passed", disabled outside idle/failed); Stage 1 `— key DIRECT against the provider (TEST-01)` shows the verbatim command with the CONSISTENT flag order `ssh -T -F <tmp> -p <port> -i <key> git@<hostname>` (same order as data.go CreateFlowTestStage1Command) as a dim block, Run → brief running state (tea.Tick) → success line `✓ Hi <name>! You've successfully authenticated…` or the failure path `✗ git@<hostname>: Permission denied (publickey).` + explanation + `Copy public key` (status-line feedback "(demo)") + Retry (clears the toggle); Stage 2 `— resolve BY ALIAS (TEST-02). No -i here on purpose: the config must supply the key; that is exactly what this stage proves.` with `ssh -G -F <tmp> <alias> | grep identityfile` → `✓ identityfile <key>`; Next enabled only after stage 2. STATE 3 Git identity — the FULL form immediately (round-3: assume the user wants Git): user.name; user.email with @-validation and helper "Kept byte-identical to ~/.ssh/allowed_signers (GITUI-04)"; the fixed signing line `Signing: gpg.format = ssh (fixed) · signingkey = <key>.pub — a PATH, never key material.`; Match strategy select with the exact copy `gitdir (default) — applies inside ~/<name>/` / `hasconfig — repos whose remote uses this alias` / `both — either condition (two includeIf blocks = OR)`; DUAL dim previews side by side: `~/.gitconfig.d/<name> (fragment file — preview)` (the [user]/[gpg]/[commit] fragment) AND `~/.gitconfig (includeIf block — preview)` (per selected strategy, aliasing data.go's includeIf lines with the new name); the read-only baseline strip again; buttons Back (Esc) / `Skip — SSH only (identity stays incomplete)` / `Continue: review & write (Enter)` (disabled until name non-empty + email has @). STATE 4 = the shared 2-state ceremony: heading `Create identity "<name>" — <algo>, test passed ✓`, targets [~/.ssh/config, ~/.gitconfig.d/<name>, ~/.gitconfig, ~/.ssh/allowed_signers] when Git configured else [~/.ssh/config], the combined review preview, backup promises; on Done dispatch AddIdentity (complete when Git configured, else incomplete with the SSH-only note), status message, select the new row, return to detail. EDIT-SSH pane: the SAME field set as wizard state 1 rendered by the SAME form code with lockIdentity=true (Provider + Alias prefix disabled with the helpers "Locked — the provider comes from the Host alias" / "Locked — the identity name never changes in place; use Clone to rename"), followed by the ceremony `Rewrite the managed Host block for "<name>"` → EditSSH on done. CLONE pane: explanation "The clone gets its own new key and Host alias; the Git author is copied (MGR-04).", name input defaulting `<name>-clone` with taken/empty validation and the `Creates <clone>.github.com + ~/.ssh/id_ed25519_<clone>` helper; confirm dispatches CloneIdentity (no ceremony, matching the web), selects the clone. DELETE: first the scope chooser (safer `Delete Git identity only (safer — SSH stays)` DEFAULT-FOCUSED, `Delete everything (SSH + Git + key) — irreversible` in error color), then the ceremony — everything-scope is destructive with ConfirmWord = the identity name and warning "This removes the key file too — it cannot be regenerated…", diff-style preview of removed block/includeIf/key (git-only variant keeps `Host … (unchanged)` context); on Done dispatch DeleteIdentity, git-only heals to state incomplete, everything re-selects a fallback row. Crumbs per pane mirror the web (e.g. `<name> › Edit SSH`, `New identity`). All copy in this task comes from data.go constants or Identities.tsx verbatim — never invented.
  </action>
  <verify>
    <automated>go test -race ./internal/dummytui/... && make lint</automated>
  </verify>
  <acceptance_criteria>
    - Pips test: the mapping over ALL 8 seeded fixture rows matches spec §2 (e.g. clientB = tone ✗ + S✗ G–; legacy = tone ✗ + S✓ G✗; opensource = tone ! + S– G✓)
    - Live-detail test: sending ↓ changes the rendered detail header from "personal" to "work" in the SAME View() output — no Enter involved
    - Wizard validation tests: duplicate prefix blocks Next with the exact error copy; blank prefix yields the provider host verbatim as SSH Host (WYSIWYG); manual SSH-Host edit switches the helper to "Manually edited — auto-join off" and later prefix edits no longer change it; port accepts digits only; the -sk algorithm entries are disabled with the libfido2 rationale in the render
    - Test-stage tests: stage-1 command string equals the `ssh -T -F <tmp> -p <port> -i <key> git@<hostname>` order; stage-2 command contains `-G` and the alias and does NOT contain `-i`, and the "No -i here on purpose" rationale renders; the simulate-fail toggle produces the Permission-denied path and Retry returns to idle; the toggle is disabled while running and after pass
    - Flow tests: Skip at state 3 creates an INCOMPLETE identity with the SSH-only note; the full path creates a COMPLETE identity, the sidebar gains the row, and the header chip's id count increments; delete-everything requires typing the identity name before confirm enables and removes the row + its findings; delete git-only sets state incomplete and keeps the row; the per-finding Fix ceremony on "legacy" heals it to complete and decrements the chip
    - Edit-SSH renders the SAME form with Provider/Alias prefix disabled + the two Locked helpers
    - `make lint` passes
  </acceptance_criteria>
  <done>Identities is a live master-detail surface with the complete create/edit/clone/delete/fix lifecycle mutating shared state — behavior-identical to the web demo, proven by unit tests on the pure model.</done>
</task>

<task type="auto">
  <name>Task 3: Global SSH (Options + Storage), Global Git, Doctor (absorbs Fixer) + PTY e2e on the real binary + all gates</name>
  <files>internal/dummytui/globalssh.go, internal/dummytui/globalssh_test.go, internal/dummytui/globalgit.go, internal/dummytui/globalgit_test.go, internal/dummytui/doctor.go, internal/dummytui/doctor_test.go, e2e/harness_test.go, e2e/dummy_demo_e2e_test.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-REDESIGN-SPEC.md (§4 Global SSH/Git + STORE-01 sub-tabs, §5 Doctor severity contract + fix-all counter)
    - .planning/design/mockup-src/src/demo/screens/GlobalSsh.tsx (sub-tab strip, applied-overlay over options, apply-ceremony diff incl. declined lines, storage radio + per-strategy resulting-config previews, migrate ceremony)
    - .planning/design/mockup-src/src/demo/screens/GlobalGit.tsx (11-row list, main-vs-master highlight, user.email awareness row, baseline ceremony)
    - .planning/design/mockup-src/src/demo/screens/Doctor.tsx (auto-scan effect, severity ordering + grouping, f/F, batch counter banner, live finishFix, all-green state)
    - e2e/ui_pty_e2e_test.go + e2e/harness_test.go (the raw-keystroke PTY harness patterns: startPTY/sendKey/waitFor/snapshot, SandboxHome, package-level buildOnce binary builder; the removed BuildDummyBinary helper is recoverable from commit 7453561^)
    - internal/dummytui/data.go (GlobalSSHOptions/GlobalSSHDetailExplanation/GlobalSSHAdvisoryNote/GlobalSSHHostStarBlockText sentinels; GlobalGitOptions/GlobalGitDetailExplanation/GlobalGitResultMessage; HealthFindings severities; FixerBatchFixNote)
  </read_first>
  <action>
    Write failing tests first, then implement the three remaining tabs and the e2e. `globalssh.go` (§4 + STORE-01): a sub-tab strip `[Options] [Storage & preview]` switched with ←/→ (and clickable); crumb = the active sub-tab. OPTIONS: master-detail over data.go GlobalSSHOptions with the applied-state overlay (keys in state.SSHApplied render as current=recommended, needsAction=false, one-liner prefixed "Applied by gitid — "); rows: apply checkbox (pending rows only; space toggles), !/✓ tone, bold key, risk chip, faint `now: <current> → <recommended>` line; ↑/↓ selects and the detail pane updates live (IdentitiesOnly shows the full GlobalSSHDetailExplanation, others their one-liner) + the advisory Alert (GlobalSSHAdvisoryNote — advisory, never a compliance gate); `a` (footer hint `apply N selected`) opens the ceremony `Write Host * managed block to ~/.ssh/config` — target respects state.SSHStorage (include layout targets ~/.ssh/config.d/gitid.config), diff preview = `+` line per chosen key, context lines for already-set options, and `<key> — left unchanged (declined; advisory)` for pending-but-unchecked ones; on Done dispatch ApplySSH; status message with the applied count. A warning banner above (browse mode only) surfaces SSH-section doctor findings with an `Open Doctor (4)` hint. STORAGE (STORE-01): radio choice `Sentinel blocks in ~/.ssh/config (default)` vs `gitid-owned ~/.ssh/config.d/gitid.config via one Include line` with a "— current" marker from state.SSHStorage; the faint note about Include paths (absolute or ~/.ssh-relative, Include NEAR THE TOP, migration backed-up + reversible, STORE-03); the right side renders the RESULTING CONFIG per selected strategy as dim preview blocks (sentinel: in-place personal Host block + the Host * managed block reflecting applied options via a managedHostStar helper over data.go sentinels; include: the `Include ~/.ssh/config.d/gitid.config` top-of-file preview PLUS the owned-file preview); when the selection differs from current, `Enter migrate layout…` opens the migration ceremony with the layout-specific +/- diff; on Done dispatch SetSSHStorage. `globalgit.go` (GGIT-01): master-detail over data.go GlobalGitOptions (all 11 rows); init.defaultBranch row carries the main-vs-master HIGHLIGHT (tinted row + a `main vs master` warning-colored chip); the `user.email (global)` row renders as awareness-only (never checked, never applied — gitid never writes a global [user] section); checkbox per needs-action row; detail shows GlobalGitDetailExplanation for init.defaultBranch else the one-liner + the advisory Alert; `a apply N selected` → ceremony `Write baseline managed block to ~/.gitconfig` previewing the sentinel-delimited managed block (GlobalGitSentinel* + the recommended key=value lines, compact) → ApplyGitBaseline on Done; result = data.go GlobalGitResultMessage; status notes user.email untouched; Git-section doctor-findings banner like Global SSH's. `doctor.go` (§5, absorbs the Fixer — FIX-02, no fifth tab): on first entry auto-run the scan — a brief scanning state (`running doctor scan…`, status "Scanning ~/.ssh/config, ~/.gitconfig, fragments, keys, allowed_signers…") driven by tea.Tick, then MarkScanned; instant on later visits. LEFT: findings grouped `SSH · <identity|global>` then `Git · <identity|global>`, severity-ordered critical>error>warning>info within the flat selection order; each row: severity glyph + WORD with the LOCKED contract (~ info cyan · ! warning yellow · ✗ error AND critical red — the word disambiguates, NEVER ✗ for a warning), bold title, faint family + `fixable`/`info only`. RIGHT detail: glyph+word, title, family chip, identity chip when attributed, the explanation, `Suggested fix: <…>` + `f · Fix this…` only when SuggestedFix exists (else "Informational only — nothing to fix."). `f` opens the compressed in-pane ceremony from fixplans.PlanFor; on Done dispatch FixFinding — the finding disappears LIVE, the header chip counts decrement, and healed identities (legacy) change state everywhere. `F` (`fix all (n)`, shown when >1 fixable) walks EVERY fixable finding through the SAME per-fix ceremony sequentially with a banner `Fix all — k / n fixed; each change still previews its own diff and backup before writing.` — never a silent batch; Esc cancels the remainder. All-green (scanned, zero findings): the two ✓ summaries from data.go FixerNothingToFix*/HealthAllGreen*. E2E `e2e/dummy_demo_e2e_test.go` (//go:build e2e): restore a `BuildDummyBinary(t)` helper in harness_test.go (same sync.Once + os.MkdirTemp pattern as BuildBinary, building ./cmd/gitid-dummy — the removed helper in 7453561^ is the reference); `TestDummyDemo_LiveWalk` starts the REAL binary under SandboxHome via pty.StartWithSize at 100x30 (NOT 80x24 — this demo's minimum geometry) with the vt emulator, then drives RAW keystrokes: assert launch (header tabs + `Identities` breadcrumb + a known sidebar row like "personal" + the legend line); ↓ then assert the detail switched to "work" (live master-detail proof); "2"/"3"/"4" asserting each breadcrumb + a screen signature string (e.g. "Storage & preview" tab label on 2, "main vs master" on 3, a finding title on 4); back to "1", "n" → `Step 1/4`, fill minimal fields (type a fresh prefix), Enter → `Step 2/4`, Enter (run stage 1) → wait for the `Hi ` success, Enter (stage 2) → `identityfile`, Enter → `Step 3/4`, Enter (Continue) → the ceremony heading, Enter (confirm) → the receipt `Wrote →`, Enter (Done) → the new row in the sidebar; "4", `f` on the selected fixable finding → ceremony → Enter → receipt → assert the finding title gone and the chip count decremented; `?` opens help (assert a legend row) and Esc closes; `q` then Esc stays, `q` then Enter exits cleanly (process wait, exit 0). After the walk, walk the sandbox HOME recursively and assert ZERO files/dirs were created (DLV-05 zero-writes). Keep the walk lean — it must fit the package's 180s -race budget alongside the existing PTY suite. GATES, in order: the whole-dummy no-backend ALLOWLIST `go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/...` filtered to `github.com/castocolina/gitid/` must contain NOTHING but internal/dummytui + cmd/gitid-dummy; `make test`; `make lint`; `make test-e2e`; `make gate-no-backend-files` (the branch allowlist already names internal/dummytui/, cmd/gitid-dummy/, e2e/ — this plan's files all fall inside it).
  </action>
  <verify>
    <automated>go test -race ./internal/dummytui/... && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ] && make test && make lint && make test-e2e && make gate-no-backend-files</automated>
  </verify>
  <acceptance_criteria>
    - Global SSH tests: ←/→ toggles the sub-tabs; applying a chosen subset marks those keys applied ("Applied by gitid — " overlay) while a declined pending key renders the "left unchanged (declined; advisory)" diff line; the storage previews switch between sentinel and Include layouts and `Migrate layout…` only appears when the choice differs from current; SetSSHStorage round-trips (reversible)
    - Global Git tests: 11 rows render; init.defaultBranch carries the main-vs-master highlight chip; the user.email row is never checkable/applied; the apply ceremony previews the `# BEGIN/END gitid managed: global-git` sentinels and Done yields GlobalGitResultMessage with user.email untouched
    - Doctor tests: first entry scans then shows findings grouped SSH-then-Git with per-identity subgroup labels; severity render pins the locked contract (info=`~ info`, warning=`! warning`, error=`✗ error`, critical=`✗ critical` — never ✗ for warning); `f` fix removes the finding and decrements FindingCounts; `F` walks all 4 fixable findings with the k/n banner text and each shows its own ceremony; fixing git-includeif-missing-fragment heals "legacy" to complete; all-green renders both ✓ summaries
    - `TestDummyDemo_LiveWalk` passes under `make test-e2e`: real binary, 100x30 PTY, raw keystrokes, tab nav + full wizard walk + fix ceremony + help + clean quit, and the sandbox-HOME zero-writes assertion holds
    - The no-backend ALLOWLIST gate holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - `make test`, `make lint`, `make test-e2e`, and `make gate-no-backend-files` are ALL green
  </acceptance_criteria>
  <done>All four tabs are live and behavior-identical to the web demo; the Doctor absorbs the Fixer with live healing; the real gitid-dummy binary is proven interactive end-to-end via raw-keystroke PTY at 100x30 with zero file writes; every gate (allowlist, test, lint, e2e, no-backend-files) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live demo ← user's real files | the demo is 100% in-memory dummy state; it must never read or write ~/.ssh or ~/.gitconfig |
| cmd/gitid-dummy ← backend packages | DLV-05: no backend logic before approval — enforced structurally by the import-graph allowlist |
| Phase-2 change-set ← git working tree | the branch-level gate-no-backend-files allowlist keeps this plan inside design-only directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13-NB | Tampering / Elevation of Privilege | a backend import slipping into the live demo | mitigate | Whole-dummy `go list -deps` ALLOWLIST (nothing first-party beyond internal/dummytui + cmd/gitid-dummy), re-run as a Task 3 gate |
| T-02-13-WRITE | Tampering | the demo touching real user files | mitigate | All "writes" are reducer transitions on in-memory state; the PTY e2e runs the real binary under a sandboxed HOME and asserts ZERO files created |
| T-02-13-DRIFT | Repudiation | the TUI demo silently diverging from the approved-direction web demo / redesign spec | mitigate | Every rendered string comes from internal/dummytui/data.go or is mirrored verbatim from the demo sources named in read_first; unit tests pin the contractual copy (flag order, no `-i` in stage 2, severity words, advisory notes, locked-field helpers) |
| T-02-13-BEGATE | Tampering | files outside the Phase-2 design allowlist changing before the 02-12 approval | mitigate | `make gate-no-backend-files` (merge-base allowlist) runs as a Task 3 gate; every file this plan touches is already inside the allowlist |
| T-02-13-SC | Tampering | supply chain via new dependencies | accept (n/a) | NO new dependencies: charm.land bubbletea/bubbles/lipgloss v2 and creack/pty + x/vt are already pinned in go.mod (Phase 1 / earlier Phase 2 plans); no package-manager install task exists in this plan |
</threat_model>

## Artifacts this plan produces

- `cmd/gitid-dummy/` — the LIVE interactive Go TUI demo binary (Bubble Tea v2, alt-screen, mouse), the TUI half of the 02-12 presentation and the seed frame for Phases 3–9
- `internal/dummytui/{store,fixplans,frame,ceremony,app,identities,globalssh,globalgit,doctor}.go` + `*_test.go` — reducer + pure Update/View models mirroring `mockup-src/src/demo/`, seeded from `data.go`
- `e2e/dummy_demo_e2e_test.go` + the restored `BuildDummyBinary` harness helper — raw-keystroke PTY proof on the real binary at 100x30 with the zero-writes sandbox assertion

<verification>
- `go build ./cmd/gitid-dummy` compiles; running it in a 100x30 terminal shows the §1 frame with all four tabs live
- `go test -race ./internal/dummytui/...` green (reducer + frame + ceremony + all four surfaces)
- The no-backend ALLOWLIST holds; `make test`, `make lint`, `make test-e2e` (incl. TestDummyDemo_LiveWalk), and `make gate-no-backend-files` all green
- Previews and their labels render dimmer than editable fields; screenshots deliberately NOT captured (available later via internal/screenshot as development checks)
</verification>

<success_criteria>
- **Code review (required before this plan is done):** on completion, request a fresh-context code review via the `superpowers:requesting-code-review` skill, verifying the work against this plan `must_haves` and every task `<acceptance_criteria>`; resolve all CRITICAL/HIGH findings before the plan is marked complete. Supplements — not replaces — the phase-level `/gsd-code-review` and the phase-level external cross-vendor (Codex) review the execution loop runs.
- DLV-05: a live, executable, fully navigable Go TUI demo exists with NO backend logic — proven structurally (import allowlist), behaviorally (PTY e2e on the real binary), and hermetically (zero writes under sandbox HOME) — BEFORE the 02-12 presentation.
- DLV-02: the TUI demo mirrors the `/mui`-built interactive web demo 1:1 per 02-REDESIGN-SPEC.md; the framework decision (Bubble Tea v2 retained; go-tui evaluated and rejected as too immature) is recorded in this plan.
- The demo seeds exclusively from `internal/dummytui/data.go` (recipe-faithful per `recipes/`, the North Star) so both media render the same canonical configuration.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-13-SUMMARY.md` when done.
</output>
