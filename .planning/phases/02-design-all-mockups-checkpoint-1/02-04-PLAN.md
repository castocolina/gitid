---
phase: 02-design-all-mockups-checkpoint-1
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - .planning/design/create-flow/FIELDS.md
  - .planning/design/create-flow/manifest.json
  - .planning/design/create-flow/parity.json
  - .planning/design/create-flow/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/create-flow/algo-catalog.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/ssh-form-empty.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/ssh-form-filled.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/ssh-form-blank-prefix.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/reuse-key-vs-generate.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/macos-globals-block.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/test-stage1-direct.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/test-stage2-by-alias.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/test-fail.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/confirm-write.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/result-success.route.tsx
  - internal/dummytui/surface_createflow.go
  - internal/dummytui/surface_createflow_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The create-identity flow (pilot surface) is mocked in MUI v7 across all 12 named states from UX-DIRECTION §4.1, with recipe-accurate copy (Port 443, IdentitiesOnly yes, ed25519 default, macOS globals under IgnoreUnknown, ssh -G proof, backup path)"
    - "The same 12 states exist in the TUI dummy (surface_createflow.go), registered as a KEYLESS modal flow that declares a TARGET-OWNED launch binding (LaunchFrom identity-manager + LaunchKey) so it is launched FROM Identities and reachable on the real binary via the 02-02 launch mechanism, navigable, backend-free"
    - "Both media are screenshot-captured to .planning/design/create-flow/{html,tui}/ (12 PNGs each), each save gated on the `<surface>/<screen>` breadcrumb; the dummy-nav e2e re-homes then reaches every create-flow screen THROUGH the launch key in keysFromHome, asserting the breadcrumb + signature"
    - "agent-ui-ux-designer's semantic-parity critique is recorded as a machine-checkable parity.json (one row per §3 dimension + the test→confirm→backup affordance) with NO row status != resolved; CRITIQUE.md remains the human narrative"
    - "The test→confirm→backup safety boundary is unmistakable in BOTH media (nothing mutates before confirm; backup path shown)"
  artifacts:
    - path: ".planning/design/create-flow/manifest.json"
      provides: "per-screen {surface, screen, htmlRoute, keysFromHome, signature} driving capture + e2e (unique screen/htmlRoute/signature; keysFromHome absolute from startup incl. the launch key)"
      contains: "keysFromHome"
    - path: ".planning/design/create-flow/parity.json"
      provides: "machine-checkable §3 parity rows (gate: no row status != resolved)"
      contains: "status"
    - path: "internal/dummytui/surface_createflow.go"
      provides: "TUI dummy create-flow surface (12 screens) registered keyless with a LaunchFrom/LaunchKey launch binding, backend-free"
      contains: "LaunchFrom"
    - path: ".planning/design/create-flow/CRITIQUE.md"
      provides: "agent-ui-ux-designer human narrative critique (companion to parity.json)"
      contains: "parity"
  key_links:
    - from: "src/routes/create-flow/*.route.tsx"
      to: "src/data/recipeFixtures.ts"
      via: "real recipe copy, no lorem; each route title = `create-flow/<screen>`"
      pattern: "recipeFixtures"
    - from: "internal/dummytui/surface_createflow.go"
      to: "dummytui.Register + RenderScreen + the 02-02 launch mechanism"
      via: "init()-registration (keyless modal flow) declaring LaunchFrom identity-manager + LaunchKey, so route() launches it from Identities; no model.go/data.go edit"
      pattern: "LaunchFrom"
---

<objective>
Take the **create-identity flow as the pilot surface** (UX-DIRECTION §1: shell-first → pilot → fan-out) — the most complex surface (multi-step form, live `Host`-block preview, two-stage test with real command output, confirm, backup) — through the full per-surface pipeline: `FIELDS.md` → `manifest.json` (hardened schema) → MUI mockup (12 states) → TUI dummy surface (12 screens) → capture → `agent-ui-ux-designer` parity critique → `parity.json` 0-unresolved. This de-risks the design language and the parity rubric on ONE surface before the six-surface fan-out, and defines the exact 3-task per-surface shape every fan-out plan (02-05..02-10) repeats.

Review folded in (Codex 02-REVIEWS.md): HIGH-4 the "0 OPEN" gate becomes a structured `parity.json` (rows keyed to §3, gate = no row status != resolved); HIGH-3(c) the manifest uses the hardened schema `{surface, screen, htmlRoute, keysFromHome, signature}` with unique screen/htmlRoute/signature + absolute keysFromHome; HIGH-1 the no-backend proof is the ALLOWLIST. Review-spec iteration 2 folded in: C3 — create-flow is a KEYLESS modal surface that declares a TARGET-OWNED launch binding (`LaunchFrom: "identity-manager"`, `LaunchKey`) in its own file, so the 02-02 `route()` launches it FROM Identities; its `keysFromHome` are REAL absolute keystroke sequences from startup — re-home to Identities, press the launch key, then the intra-modal transition keys — so 02-03/02-11's dummy-nav e2e reaches every create-flow screen through the running binary (not via a direct RenderScreen call).

Purpose: DLV-01 (HTML mockup encodes fields/order/labels/copy/flow) + DLV-05 (dummy mirrors it, backend-free) for the pilot; validate the whole pipeline end-to-end before fanning out.
Output: `.planning/design/create-flow/{FIELDS.md, manifest.json, parity.json, CRITIQUE.md, html/*.png, tui/*.png}`, the 12 MUI route files, `internal/dummytui/surface_createflow.go`.

States (lift VERBATIM from UX-DIRECTION §4.1, all 12): `algo-catalog` (ed25519 best/default + ed25519-sk/rsa-4096/ecdsa-p256/ecdsa-sk, security + macOS/Linux notes) · `ssh-form-empty` · `ssh-form-filled` (live `Host` block preview) · `ssh-form-blank-prefix` (WYSIWYG provider host verbatim) · `reuse-key-vs-generate` · `macos-globals-block` (`Host *` UseKeychain/AddKeysToAgent under `IgnoreUnknown`) · `test-stage1-direct` · `test-stage2-by-alias` (`ssh -G` proving IdentityFile) · `test-fail` · `confirm-write` · `backup-notice` · `result-success`.

DLV-02: author the MUI mockup with `/mui` (v7 sx/slots, 02-01 terminal-skin theme + shell) and the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic pass (HTML) AND the semantic-parity critique (HTML↔TUI). Name both in every task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-01-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-02-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
@recipes/ssh-config.recipe
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: create-flow FIELDS.md + manifest.json (hardened schema, absolute keysFromHome via the launch key) + parity.json seed + MUI mockup (12 states)</name>
  <files>.planning/design/create-flow/FIELDS.md, .planning/design/create-flow/manifest.json, .planning/design/create-flow/parity.json, .planning/design/mockup-src/src/routes/create-flow/algo-catalog.route.tsx, .planning/design/mockup-src/src/routes/create-flow/ssh-form-empty.route.tsx, .planning/design/mockup-src/src/routes/create-flow/ssh-form-filled.route.tsx, .planning/design/mockup-src/src/routes/create-flow/ssh-form-blank-prefix.route.tsx, .planning/design/mockup-src/src/routes/create-flow/reuse-key-vs-generate.route.tsx, .planning/design/mockup-src/src/routes/create-flow/macos-globals-block.route.tsx, .planning/design/mockup-src/src/routes/create-flow/test-stage1-direct.route.tsx, .planning/design/mockup-src/src/routes/create-flow/test-stage2-by-alias.route.tsx, .planning/design/mockup-src/src/routes/create-flow/test-fail.route.tsx, .planning/design/mockup-src/src/routes/create-flow/confirm-write.route.tsx, .planning/design/mockup-src/src/routes/create-flow/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/create-flow/result-success.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.1 (the 12 named states + the test→confirm→backup affordance), § 3 (the seven MUST-match dimensions), § 5 (four-beat ceremony), § 2 (create-flow is a modal flow launched FROM Identities; the KEY-ALLOCATION TABLE — single authority: create-flow's LaunchKey is `n`; do NOT invent a key, the 02-02 registration guard rejects a colliding one — review iter-3)
    - .planning/design/FIELDS.template.md + .planning/design/PARITY.template.json (the formats to fill), .planning/design/mockup-src/src/data/recipeFixtures.ts (real copy), src/theme.ts + src/shell/* (terminal skin + shell + breadcrumb)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-02-SUMMARY.md (the launch mechanism: create-flow is reached by re-home to Identities then the LaunchKey — the shape keysFromHome must encode) + .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md (the manifest KeysFromHome contract + the e2e that drives it)
    - recipes/ssh-config.recipe (Host block, Hostname ssh.github.com, Port 443, User git, IdentityFile, IdentitiesOnly yes, IgnoreUnknown UseKeychain / Host * globals)
    - REQUIREMENTS.md KEY-01, SSHUI-01..03, TEST-01/02; ~/.claude/plugins/marketplaces/agent-toolkit/skills/mui/SKILL.md
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` direction: author `.planning/design/create-flow/FIELDS.md` (per-screen field/order/label/copy table for all 12 states — the DLV-01 spec). Author `manifest.json` as an array of the HARDENED schema `{surface:"create-flow", screen, htmlRoute, keysFromHome, signature}` for the 12 states (review HIGH-3c): `screen` matches the state name; `htmlRoute` e.g. `/create-flow/ssh-form-filled`; `keysFromHome` = the ABSOLUTE keystroke sequence from app startup that reaches that screen THROUGH the 02-02 launch mechanism (review C3) — first the number key `1` (re-home to Identities), then the create-flow LAUNCH key (the `LaunchKey` Task 2 declares — `n`, allocated in the UX-DIRECTION §2 key-allocation table, review iter-3) that opens the modal at its entry screen, then the intra-modal transition keys (`ScreenDef.Keys`) that walk to the target screen; `signature` = a SCREEN-SPECIFIC unique marker (not a generic string reused across screens). Each route file's `title` MUST be `create-flow/<screen>` so the shell breadcrumb + capture screen-ID assertion resolve. Copy `.planning/design/PARITY.template.json` to `.planning/design/create-flow/parity.json` and seed it with the seven §3 rows + a `test-confirm-backup-boundary` row (statuses left `open` until Task 3 resolves them). Author the 12 `*.route.tsx` (auto-discovered by 02-01's glob) rendering inside `<Shell>` with REAL recipe copy (NO lorem): the algorithm catalog (ed25519 best/default + availability notes); the SSH form field order `Alias prefix → SSH Host → Real hostname → Port` (default 443) + live `Host` block preview; blank-prefix WYSIWYG; reuse-vs-generate; macOS globals; the two test stages showing the EXACT command + real output + `ssh -G` IdentityFile resolution; test-fail; confirm-write naming the target file + the `# BEGIN/END gitid managed:` sentinels; backup-notice with the timestamped path; result-success. The test→confirm→backup boundary must be visually unmistakable. Rebuild dist (`pnpm build` — runs verify-routes) and confirm each route renders.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/create-flow/manifest.json'))))") -eq 12 && python3 -c "import json;m=json.load(open('.planning/design/create-flow/manifest.json'));ids=[e['screen'] for e in m];sigs=[e['signature'] for e in m];assert len(set(ids))==12 and len(set(sigs))==12 and all(e.get('keysFromHome') for e in m) and all(e['keysFromHome'][0]=='1' for e in m)"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 12 screens in the hardened schema with unique screen + signature + non-empty keysFromHome (asserted by the verify python) AND `grep -q 'keysFromHome' .planning/design/create-flow/manifest.json` AND `grep -q 'algo-catalog' .planning/design/create-flow/manifest.json`
    - keysFromHome are ABSOLUTE through the launch mechanism (review C3): every entry's keysFromHome starts with `1` (re-home to Identities) followed by the create-flow launch key, then intra-modal keys (asserted by the verify python's `keysFromHome[0]=='1'` check)
    - `parity.json` exists with the §3 rows + the test-confirm-backup row: `python3 -c "import json;r=json.load(open('.planning/design/create-flow/parity.json'));assert len(r)>=8 and all('status' in x for x in r)"`
    - 12 route files exist: `test $(ls .planning/design/mockup-src/src/routes/create-flow/*.route.tsx | wc -l) -eq 12`
    - Recipe copy is real, not lorem: `grep -rq 'IdentitiesOnly yes' .../create-flow/` AND `grep -rq 'Port 443' .../create-flow/` AND `grep -rq 'ssh -G' .../create-flow/` AND `! grep -riq 'lorem' .../create-flow/`
    - Field order + defaults in FIELDS.md: `grep -q 'Alias prefix' .planning/design/create-flow/FIELDS.md` AND `grep -q '443' .planning/design/create-flow/FIELDS.md`
    - Mutation-ceremony states present: manifest/routes include `confirm-write`, `backup-notice`, `result-success`, `test-fail`
    - `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0 (routes compile + pass verify-routes)
  </acceptance_criteria>
  <done>The create-flow HTML/MUI mockup renders all 12 recipe-accurate states inside the terminal-skin shell, specified by FIELDS.md, enumerated by a hardened-schema manifest.json whose keysFromHome reach each screen through the launch mechanism, with a seeded parity.json — built with /mui under agent-ui-ux-designer direction.</done>
</task>

<task type="auto">
  <name>Task 2: create-flow TUI dummy surface (12 screens, keyless modal, launch binding, backend-free)</name>
  <files>internal/dummytui/surface_createflow.go, internal/dummytui/surface_createflow_test.go</files>
  <read_first>
    - internal/dummytui/registry.go + data.go (02-02: SurfaceDef with LaunchFrom/LaunchKey, ScreenDef, Register/RegisterOrReplace, RenderScreen; the placeholder identity-manager surface — ID `identity-manager` — that a launch binding targets)
    - .planning/design/create-flow/FIELDS.md + manifest.json (Task 1 — the field/copy/signature contract the TUI mirrors byte-for-byte on labels/copy; keysFromHome encodes the launch key this task declares)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (shell regions, breadcrumb, ANSI-safe color+glyph+word; create-flow launched FROM Identities; the KEY-ALLOCATION TABLE — create-flow LaunchKey = `n`, the single authority to declare against; the 02-02 guard rejects a colliding key), § 4.1 (the 12 states + safety boundary)
    - .planning/design/mockup-src/src/routes/create-flow/*.route.tsx (Task 1 — the /mui parity source)
  </read_first>
  <action>
    Write `surface_createflow.go` in `package dummytui`: register the create-flow surface via `Register(SurfaceDef{...})` from an `init()` as a KEYLESS modal flow launched FROM the Identities view — plain `Register` with an EMPTY `ActivationKey` (which 02-02 EXEMPTS from the duplicate-key check — review H2 — so the two keyless modal surfaces create-flow + git-screen both register without collision) AND a TARGET-OWNED launch binding `LaunchFrom: "identity-manager"` + `LaunchKey` (the same key encoded in manifest.json's keysFromHome — `n`, allocated in the UX-DIRECTION §2 key-allocation table, review iter-3; the 02-02 registration guard rejects a colliding LaunchKey) — review C3: this is what makes 02-02's `route()` launch create-flow from Identities and what 02-03/02-11's e2e drive; because the binding lives on THIS surface's own file, no data.go/model.go/identity-manager-plan edit is needed. Define its 12 `ScreenDef`s with IDs matching `manifest.json` screens, `Keys` transitions forming the flow order (catalog → ssh-form → test → confirm → backup → result, with branches to blank-prefix/reuse/macos-globals/test-fail — the same transitions manifest.json's keysFromHome walk), and a `Render func() string` per screen reproducing the SAME labels/copy/options/defaults/signatures as the MUI mockup (`Port 443`, `IdentitiesOnly yes`, the live `Host` block preview, the exact test command + output, `ssh -G` proof, the sentinel-visible confirm preview, the timestamped backup path) using the terminal-skin regions + ANSI-safe color+glyph+word. NO backend import (only bubbletea/lipgloss/bubbles + x/ansi). Write `surface_createflow_test.go` (plain) asserting: the surface registers 12 screens; it is keyless (empty ActivationKey) with LaunchFrom=="identity-manager" and a non-empty LaunchKey; each `RenderScreen("create-flow", id)` is non-empty, contains its manifest `signature`, and contains the `create-flow/<id>` breadcrumb; the flow's Keys map forms a connected path reaching every screen. The `/mui` mockup is the parity source; `agent-ui-ux-designer` validates the cross-media result in Task 3.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run CreateFlow && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `surface_createflow.go` registers the surface with 12 screens: `grep -q 'Register' internal/dummytui/surface_createflow.go`; the test asserts a count of 12
    - It is a keyless modal with a launch binding (review C3): `grep -q 'LaunchFrom' internal/dummytui/surface_createflow.go` AND `grep -q 'LaunchKey' internal/dummytui/surface_createflow.go`; the test asserts empty ActivationKey + LaunchFrom=="identity-manager" + non-empty LaunchKey
    - `go test -race ./internal/dummytui/... -run CreateFlow` passes (12 screens; each RenderScreen non-empty + contains its signature + the `create-flow/<id>` breadcrumb; connected Keys path)
    - Copy parity anchors present: `grep -q 'IdentitiesOnly yes' internal/dummytui/surface_createflow.go` AND `grep -q 'Port 443' internal/dummytui/surface_createflow.go` AND `grep -q 'ssh -G' internal/dummytui/surface_createflow.go`
    - No-backend ALLOWLIST holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - `go build ./cmd/gitid-dummy/...` exits 0; `make lint` passes
  </acceptance_criteria>
  <done>The create-flow surface exists in the TUI dummy with all 12 screens registered as a keyless modal flow that declares its LaunchFrom/LaunchKey launch binding (launched from Identities, reachable on the real binary via the 02-02 mechanism), each screen carrying its breadcrumb screen-ID, mirroring the /mui mockup's labels/copy/defaults, and proven backend-free by the allowlist.</done>
</task>

<task type="auto">
  <name>Task 3: Capture create-flow (both media) + agent-ui-ux-designer critique → parity.json 0-unresolved</name>
  <files>.planning/design/create-flow/CRITIQUE.md, .planning/design/create-flow/FIELDS.md, .planning/design/create-flow/parity.json</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review" (aesthetic HTML-only + structured field-parity matrix), § "Open Question 3" (TUI judged on structural parity + keyboard-nav usability)
    - .planning/design/PARITY.template.json + .planning/design/create-flow/parity.json (Task 1 seed — fill html_present/tui_present/status/resolution now that both media exist), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.1
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md (make targets + -run filters + the breadcrumb-asserting capture + the launch-driven dummy-nav e2e)
  </read_first>
  <action>
    Run the per-surface capture: `make screenshot-html-mockups` and `make screenshot-tui-mockups` (or the create-flow-filtered `go test -tags screenshot -run 'TestCaptureAllMockupScreens/create-flow'`) to write 12 HTML + 12 TUI PNGs under `.planning/design/create-flow/{html,tui}/` (each save gated on the `create-flow/<screen>` breadcrumb); and `make dummy-nav-e2e` to prove every create-flow screen is reachable on the real binary — through the launch key in keysFromHome (review C3) — with zero writes. Then spawn `agent-ui-ux-designer` for TWO passes (DLV-02): (1) an aesthetic/usability pass on the HTML screenshots ONLY (recorded in `CRITIQUE.md`, the human narrative); (2) fill `.planning/design/create-flow/parity.json` — one row per §3 MUST-match dimension (field set + order; labels + helper copy verbatim; option sets incl. the algorithm catalog; defaults Port 443 / IdentitiesOnly yes / ed25519; flow order + named-state list; safety-affordances presence — live preview/confirm/backup/error/destructive; keybindings surfaced) PLUS the `test-confirm-backup-boundary` row — setting `html_present`/`tui_present`/`status`/`resolution` for each. Resolve EVERY divergence (fix the divergent mockup or dummy, re-capture, or record an explicit documented "HTML-only/TUI-only by design" resolution) so NO parity.json row has `status != resolved`. Fill the HTML-present/TUI-present columns of `FIELDS.md` for humans. Name `/mui` (HTML side) + `agent-ui-ux-designer` (critic).
  </action>
  <verify>
    <automated>make screenshot-html-mockups && make screenshot-tui-mockups && make dummy-nav-e2e && test $(ls .planning/design/create-flow/html/*.png | wc -l) -eq 12 && test $(ls .planning/design/create-flow/tui/*.png | wc -l) -eq 12 && python3 -c "import json;r=json.load(open('.planning/design/create-flow/parity.json'));bad=[x for x in r if x.get('status')!='resolved'];import sys;sys.exit(1 if bad or not r else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - 12 HTML + 12 TUI PNGs: `test $(ls .planning/design/create-flow/html/*.png | wc -l) -eq 12` AND `test $(ls .planning/design/create-flow/tui/*.png | wc -l) -eq 12`
    - `make dummy-nav-e2e` reaches every create-flow screen with zero writes (exits 0) — through the launch key in keysFromHome, not a direct RenderScreen call
    - `parity.json` has one row per §3 dimension + the test-confirm-backup row, and NO row has `status != resolved`: `python3 -c "import json;r=json.load(open('.planning/design/create-flow/parity.json'));assert r and all(x['status']=='resolved' for x in r)"`
    - `CRITIQUE.md` records the HTML-aesthetic pass and links parity.json: `grep -qi 'parity' .planning/design/create-flow/CRITIQUE.md`
    - FIELDS.md HTML-present/TUI-present columns filled for all 12 screens; the mutation-ceremony + test-before-mutate affordances are explicit parity.json rows
    - `make lint` passes
  </acceptance_criteria>
  <done>The create-flow pilot is captured in both media (12+12 PNGs), proven navigable on the real binary through the launch mechanism, and its agent-ui-ux-designer parity is a machine-checkable parity.json with no unresolved row — validating the full per-surface pipeline (incl. the structured parity gate) before fan-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create-flow mockup/dummy ← design artifacts only | pilot is a design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the create-flow e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB3 | Tampering / Elevation of Privilege | create-flow dummy screens reaching a backend write path | mitigate | `surface_createflow.go` imports no backend package; inherits 02-02's `go list -deps` ALLOWLIST + 02-03's zero-write e2e |
| T-02-ML3 | Repudiation | create-flow screens appearing covered while unreachable on the real binary | mitigate | The surface declares a LaunchFrom/LaunchKey binding; manifest keysFromHome drive that launch key; the dummy-nav e2e asserts each create-flow breadcrumb through the running binary (review C3) |
| T-02-PAR | Repudiation | approved design silently diverging from the recipe end-state, OR a parity gate that passes while divergent | mitigate | The §3 parity.json rows pin recipe defaults (Port 443, IdentitiesOnly yes, ed25519, sentinels) as machine-checkable rows; the gate requires NO row status != resolved (review HIGH-4) |
| T-02-SAFE | Information Disclosure | test-before-mutate / backup affordance omitted from the frozen reference | mitigate | The four-beat ceremony (§5) + test-against-throwaway states are REQUIRED states (test-fail/confirm-write/backup-notice) captured in both media + a parity.json row |
</threat_model>

## Artifacts this phase produces

- `.planning/design/create-flow/FIELDS.md` — 12-state semantic field-parity manifest (human; HTML/TUI columns filled)
- `.planning/design/create-flow/manifest.json` — 12 hardened-schema `{surface, screen, htmlRoute, keysFromHome, signature}` entries (unique screen/htmlRoute/signature; keysFromHome absolute from startup incl. the launch key)
- `.planning/design/create-flow/parity.json` — machine-checkable §3 parity rows + test-confirm-backup row (gate: no row status != resolved)
- `.planning/design/create-flow/CRITIQUE.md` — agent-ui-ux-designer human narrative critique
- `.planning/design/mockup-src/src/routes/create-flow/*.route.tsx` — 12 MUI v7 route files (titles `create-flow/<screen>`)
- `.planning/design/create-flow/{html,tui}/*.png` — 12 + 12 captured screenshots
- `internal/dummytui/surface_createflow.go` (+ `_test.go`) — TUI dummy create-flow surface, 12 screens, keyless modal with a LaunchFrom/LaunchKey binding, registered, backend-free

<verification>
- 12 HTML + 12 TUI PNGs exist; `make dummy-nav-e2e` reaches every create-flow screen (through the launch key) with zero writes
- Recipe copy is real (Port 443, IdentitiesOnly yes, ssh -G, sentinels); no lorem
- parity.json has no unresolved row; FIELDS.md columns filled; both `/mui` + `agent-ui-ux-designer` engaged
- Dummy imports pass the no-backend allowlist; the surface declares its LaunchFrom/LaunchKey launch binding
</verification>

<success_criteria>
- DLV-01/DLV-05 for the pilot: create-flow exists as an HTML/MUI mockup AND a navigable backend-free TUI dummy launched from Identities via the 02-02 mechanism, both screenshot-captured, with parity resolved in a structured parity.json.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named across all three tasks.
- The per-surface pipeline (FIELDS→manifest→parity seed→mockup→dummy→capture→critique→parity.json) is proven, ready for the six-surface fan-out.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md` when done.
</output>
</content>
