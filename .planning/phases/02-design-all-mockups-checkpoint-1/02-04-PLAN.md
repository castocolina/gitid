---
phase: 02-design-all-mockups-checkpoint-1
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - .planning/design/create-flow/FIELDS.md
  - .planning/design/create-flow/manifest.json
  - .planning/design/create-flow/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/create-flow/algo-catalog.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/ssh-form-empty.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/ssh-form-filled.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/ssh-form-blank-prefix.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/reuse-key-vs-generate.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/macos-globals-block.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/test-stage1-direct.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/test-stage2-by-alias.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/test-fail.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/confirm-write.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/create-flow/result-success.route.tsx
  - internal/dummytui/surface_createflow.go
  - internal/dummytui/surface_createflow_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The create-identity flow (pilot surface) is mocked in MUI v7 across all 12 named states from UX-DIRECTION §4.1, with recipe-accurate copy (Port 443, IdentitiesOnly yes, ed25519 catalog default, macOS globals under IgnoreUnknown, ssh -G proof, backup path)"
    - "The same 12 states exist in the TUI dummy (surface_createflow.go) registered into the shell, navigable, backend-free"
    - "Both media are screenshot-captured to .planning/design/create-flow/{html,tui}/ (12 PNGs each) and the dummy-nav e2e reaches every create-flow screen"
    - "agent-ui-ux-designer's semantic-parity critique (fields/labels/copy/options/defaults/flow-order/safety-affordances) is run and CRITIQUE.md has 0 OPEN findings"
    - "The test→confirm→backup safety boundary is unmistakable in BOTH media (tested against throwaway temp files; nothing mutates before confirm; backup path shown)"
  artifacts:
    - path: ".planning/design/create-flow/FIELDS.md"
      provides: "semantic field-parity manifest for all 12 create-flow states"
      contains: "Alias prefix"
    - path: ".planning/design/create-flow/manifest.json"
      provides: "per-screen {htmlRoute, keys, signature} driving capture + e2e"
      contains: "algo-catalog"
    - path: "internal/dummytui/surface_createflow.go"
      provides: "TUI dummy create-flow surface (12 screens) registered, backend-free"
      contains: "Register"
    - path: ".planning/design/create-flow/CRITIQUE.md"
      provides: "agent-ui-ux-designer parity findings, 0 OPEN before approval"
      contains: "RESOLVED"
  key_links:
    - from: "src/routes/create-flow/*.route.tsx"
      to: "src/data/recipeFixtures.ts"
      via: "real recipe copy, no lorem"
      pattern: "recipeFixtures"
    - from: "internal/dummytui/surface_createflow.go"
      to: "dummytui.Register + RenderScreen"
      via: "init()-registration, no model.go edit"
      pattern: "Register"
---

<objective>
Take the **create-identity flow as the pilot surface** (UX-DIRECTION §1: shell-first → pilot → fan-out) — the most complex surface, exercising nearly every pattern (multi-step form, live `Host`-block preview, two-stage test with real command output, confirm, backup) — all the way through the per-surface pipeline: `FIELDS.md` spec → `manifest.json` → MUI mockup (all 12 states) → TUI dummy surface (12 screens) → capture → `agent-ui-ux-designer` semantic-parity critique → `CRITIQUE.md` 0 OPEN. This de-risks the design language and the parity rubric on ONE surface before the six-surface fan-out. This plan defines the exact 3-task per-surface shape every fan-out plan (02-05..02-10) repeats.

Purpose: DLV-01 (HTML mockup encodes fields/order/labels/copy/flow) + DLV-05 (dummy mirrors it, backend-free) for the pilot; validate the whole pipeline end-to-end before fanning out.
Output: `.planning/design/create-flow/{FIELDS.md, manifest.json, CRITIQUE.md, html/*.png, tui/*.png}`, the 12 MUI route files, and `internal/dummytui/surface_createflow.go`.

States (lift VERBATIM from UX-DIRECTION §4.1, all 12): `algo-catalog` (top-5: ed25519 best/default, ed25519-sk, rsa-4096, ecdsa-p256, ecdsa-sk, with security + macOS/Linux availability notes) · `ssh-form-empty` · `ssh-form-filled` (live `Host` block preview) · `ssh-form-blank-prefix` (WYSIWYG provider host verbatim) · `reuse-key-vs-generate` · `macos-globals-block` (`Host *` UseKeychain/AddKeysToAgent under `IgnoreUnknown`) · `test-stage1-direct` (exact command + real output) · `test-stage2-by-alias` (`ssh -G` proving which IdentityFile resolves) · `test-fail` · `confirm-write` (preview of the resulting block + target file) · `backup-notice` (timestamped path) · `result-success`.

DLV-02: this is a UI surface. Author the MUI mockup with the `/mui` skill (v7 sx/slots, the 02-01 terminal-skin theme + shell) and the dummy screens to match; spawn `agent-ui-ux-designer` for the aesthetic pass (HTML) AND the semantic-parity critique (HTML↔TUI). Name both in every task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-01-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-02-SUMMARY.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
@recipes/ssh-config.recipe
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: create-flow FIELDS.md spec + manifest.json + MUI mockup (all 12 states)</name>
  <files>.planning/design/create-flow/FIELDS.md, .planning/design/create-flow/manifest.json, .planning/design/mockup-src/src/routes/create-flow/algo-catalog.route.tsx, .planning/design/mockup-src/src/routes/create-flow/ssh-form-empty.route.tsx, .planning/design/mockup-src/src/routes/create-flow/ssh-form-filled.route.tsx, .planning/design/mockup-src/src/routes/create-flow/ssh-form-blank-prefix.route.tsx, .planning/design/mockup-src/src/routes/create-flow/reuse-key-vs-generate.route.tsx, .planning/design/mockup-src/src/routes/create-flow/macos-globals-block.route.tsx, .planning/design/mockup-src/src/routes/create-flow/test-stage1-direct.route.tsx, .planning/design/mockup-src/src/routes/create-flow/test-stage2-by-alias.route.tsx, .planning/design/mockup-src/src/routes/create-flow/test-fail.route.tsx, .planning/design/mockup-src/src/routes/create-flow/confirm-write.route.tsx, .planning/design/mockup-src/src/routes/create-flow/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/create-flow/result-success.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.1 (the 12 named states + highest-risk affordance: test→confirm→backup boundary), § 3 (semantic MUST-match list), § 5 (four-beat mutation ceremony: preview→confirm→backup path→result)
    - .planning/design/FIELDS.template.md (the parity-matrix format to fill), .planning/design/mockup-src/src/data/recipeFixtures.ts (real copy), .planning/design/mockup-src/src/theme.ts + src/shell/* (terminal skin + shell to render inside)
    - recipes/ssh-config.recipe (Host block shape, Hostname ssh.github.com, Port 443, User git, IdentityFile, IdentitiesOnly yes, IgnoreUnknown UseKeychain / Host * globals)
    - REQUIREMENTS.md KEY-01 (top-5 catalog, ed25519 default), SSHUI-01..03 (field order Alias prefix→SSH Host→Real hostname→Port 443; blank prefix WYSIWYG; live preview), TEST-01/02 (two-stage, command-visible, ssh -G proof); ~/.claude/plugins/marketplaces/agent-toolkit/skills/mui/SKILL.md
  </read_first>
  <action>
    Using the `/mui` skill (v7 components, `sx`, the 02-01 terminal-skin theme + Shell) and `agent-ui-ux-designer` direction: FIRST author `.planning/design/create-flow/FIELDS.md` by copying `FIELDS.template.md` and filling a per-screen field/order/label/copy table for all 12 states — this IS the mockup spec (DLV-01 "encodes layout, field order, labels, copy, flow"). Author `manifest.json` as an array of `{screen, htmlRoute, keys, signature}` for the 12 states (htmlRoute e.g. `/create-flow/ssh-form-filled`; keys = the dummy keystrokes to reach it; signature = a text marker present in both media, e.g. `IdentitiesOnly yes` for `ssh-form-filled`). Then author the 12 `*.route.tsx` files, each default-exporting `{ path, element }` (auto-discovered by 02-01's glob) and rendering inside `<Shell>` with REAL recipe copy from `recipeFixtures` (NO lorem): the algorithm catalog with ed25519 marked best/default + macOS/Linux availability notes; the SSH form with the exact field order `Alias prefix → SSH Host → Real hostname → Port` (default 443) and a live `Host` block preview; the blank-prefix WYSIWYG variant (SSH Host = provider host verbatim); reuse-vs-generate choice; the macOS globals block; the two test stages showing the EXACT command run and real output plus the `ssh -G` IdentityFile resolution; the test-fail error state; the confirm-write preview naming the target file + showing the `# BEGIN/END gitid managed:` sentinels; the backup-notice with the timestamped path; the result-success. The test→confirm→backup boundary must be visually unmistakable (nothing mutates before confirm; tests run against throwaway temp files). Rebuild the dist and confirm each route renders.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/create-flow/manifest.json'))))") -eq 12</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 12 screens matching UX-DIRECTION §4.1: `test $(python3 -c "import json;print(len(json.load(open('.planning/design/create-flow/manifest.json'))))") -eq 12` AND `grep -q 'algo-catalog' .planning/design/create-flow/manifest.json` AND `grep -q 'test-stage2-by-alias' .planning/design/create-flow/manifest.json`
    - 12 route files exist under `src/routes/create-flow/`: `test $(ls .planning/design/mockup-src/src/routes/create-flow/*.route.tsx | wc -l) -eq 12`
    - Recipe copy is real, not lorem: `grep -rq 'IdentitiesOnly yes' .planning/design/mockup-src/src/routes/create-flow/` AND `grep -rq 'Port 443' .planning/design/mockup-src/src/routes/create-flow/` AND `grep -rq 'ssh -G' .planning/design/mockup-src/src/routes/create-flow/` AND `! grep -riq 'lorem' .planning/design/mockup-src/src/routes/create-flow/`
    - Field order + defaults present in FIELDS.md: `grep -q 'Alias prefix' .planning/design/create-flow/FIELDS.md` AND `grep -q '443' .planning/design/create-flow/FIELDS.md`
    - Mutation-ceremony safety states present: manifest/routes include `confirm-write`, `backup-notice`, `result-success`, `test-fail`
    - `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0 (all routes compile/render)
  </acceptance_criteria>
  <done>The create-flow HTML/MUI mockup renders all 12 recipe-accurate states inside the terminal-skin shell, specified by FIELDS.md and enumerated by manifest.json — built with /mui under agent-ui-ux-designer direction.</done>
</task>

<task type="auto">
  <name>Task 2: create-flow TUI dummy surface (12 screens, registered, backend-free)</name>
  <files>internal/dummytui/surface_createflow.go, internal/dummytui/surface_createflow_test.go</files>
  <read_first>
    - internal/dummytui/registry.go + data.go (from 02-02: SurfaceDef/ScreenDef, Register, RenderScreen; the placeholder Identities surface that launches create-flow as a modal flow)
    - .planning/design/create-flow/FIELDS.md + manifest.json (Task 1 — the field/copy/signature contract the TUI must mirror byte-for-byte on labels/copy)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (terminal shell regions, ANSI-safe semantic color+glyph+word), § 4.1 (the 12 states + safety boundary)
    - .planning/design/mockup-src/src/routes/create-flow/*.route.tsx (Task 1 — the /mui parity source the dummy screens mirror)
  </read_first>
  <action>
    Write `surface_createflow.go` in `package dummytui`: register the create-flow surface via `Register(SurfaceDef{...})` from an `init()` (or the package's aggregation point) as a modal flow launched FROM the Identities view (not a top-level number key, per UX-DIRECTION §2). Define its 12 `ScreenDef`s with IDs matching `manifest.json` screens, `Keys` transitions forming the flow order (catalog → ssh-form → test → confirm → backup → result, with branches to blank-prefix/reuse/macos-globals/test-fail), and a `Render func() string` per screen reproducing the SAME labels/copy/options/defaults/signatures as the MUI mockup (fields, `Port 443`, `IdentitiesOnly yes`, the live `Host` block preview text, the exact test command + output, `ssh -G` proof, the sentinel-visible confirm preview, the timestamped backup path) using the terminal-skin shell regions and the ANSI-safe color+glyph+word convention. NO backend import (only bubbletea/lipgloss/bubbles + x/ansi). Write `surface_createflow_test.go` (plain test) asserting: the surface registers 12 screens; each `RenderScreen("create-flow", id)` is non-empty and contains its manifest `signature`; the flow's Keys map forms a connected path reaching every screen. Parity is a conscious act — the labels/copy/defaults MUST match Task 1 (the semantic-parity gate in Task 3 checks this). The `/mui` mockup is the parity source; `agent-ui-ux-designer` validates the cross-media result in Task 3.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run CreateFlow && ! (go list -deps ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)')</automated>
  </verify>
  <acceptance_criteria>
    - `surface_createflow.go` registers the surface with 12 screens: `grep -q 'Register' internal/dummytui/surface_createflow.go`; the test asserts a count of 12
    - `go test -race ./internal/dummytui/... -run CreateFlow` passes (12 screens, each RenderScreen non-empty + contains its signature, connected Keys path)
    - Copy parity anchors present in the dummy: `grep -q 'IdentitiesOnly yes' internal/dummytui/surface_createflow.go` AND `grep -q 'Port 443' internal/dummytui/surface_createflow.go` AND `grep -q 'ssh -G' internal/dummytui/surface_createflow.go`
    - No backend import: `! grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)' internal/dummytui/surface_createflow.go` AND `go list -deps ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)'` returns NON-zero
    - `go build ./cmd/gitid-dummy/...` exits 0; `make lint` passes
  </acceptance_criteria>
  <done>The create-flow surface exists in the TUI dummy with all 12 screens registered and navigable, mirroring the /mui mockup's labels/copy/defaults, and proven backend-free.</done>
</task>

<task type="auto">
  <name>Task 3: Capture create-flow (both media) + agent-ui-ux-designer semantic-parity critique → CRITIQUE.md 0 OPEN</name>
  <files>.planning/design/create-flow/CRITIQUE.md, .planning/design/create-flow/FIELDS.md</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review (DLV-02 methodology)" (aesthetic pass HTML-only + structured field-parity matrix HTML↔TUI), § "Open Question 3" (scope aesthetic critique to HTML; TUI judged on structural parity + keyboard-nav usability)
    - .planning/design/CRITIQUE.template.md (the findings-log format; 0 OPEN rule), .planning/design/create-flow/FIELDS.md (Task 1 — fill the HTML-present/TUI-present columns now that both media exist)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3 (semantic MUST-match dimensions), § 4.1 (highest-risk affordance)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md (the capture + dummy-nav make targets and their -run filters)
  </read_first>
  <action>
    Run the per-surface capture: `make screenshot-html-mockups` and `make screenshot-tui-mockups` (or the create-flow-filtered `go test -tags screenshot -run 'TestCaptureAllMockupScreens/create-flow'`) to write 12 HTML PNGs + 12 TUI PNGs under `.planning/design/create-flow/{html,tui}/`; and `make dummy-nav-e2e` to prove every create-flow screen is reachable on the real binary with zero writes. Then spawn `agent-ui-ux-designer` to produce `.planning/design/create-flow/CRITIQUE.md` from the template with TWO passes (DLV-02): (1) an aesthetic/usability pass on the HTML screenshots ONLY (F-pattern, left-side bias, Fitts's/Hick's, accessibility, distinctive-not-generic — but terminal-skin-appropriate), and (2) a structured HTML↔TUI semantic-parity matrix over every §3 MUST-match dimension (field set + order, labels + helper copy verbatim, option sets incl. the algorithm catalog, defaults Port 443 / IdentitiesOnly yes / ed25519, flow order + named-state list, the presence of the four-beat mutation ceremony + test-before-mutate affordance, keybindings surfaced). Fill the HTML-present/TUI-present columns of `FIELDS.md`. Resolve EVERY divergence (fix the divergent mockup or dummy, re-capture, or record an explicit documented "HTML-only/TUI-only by design" note) so `CRITIQUE.md` reaches 0 OPEN. Name both `/mui` (the HTML side) and `agent-ui-ux-designer` (the critic) explicitly.
  </action>
  <verify>
    <automated>make screenshot-html-mockups && make screenshot-tui-mockups && make dummy-nav-e2e && test $(ls .planning/design/create-flow/html/*.png | wc -l) -eq 12 && test $(ls .planning/design/create-flow/tui/*.png | wc -l) -eq 12 && ! grep -q 'OPEN' .planning/design/create-flow/CRITIQUE.md</automated>
  </verify>
  <acceptance_criteria>
    - 12 HTML PNGs + 12 TUI PNGs captured: `test $(ls .planning/design/create-flow/html/*.png | wc -l) -eq 12` AND `test $(ls .planning/design/create-flow/tui/*.png | wc -l) -eq 12`
    - `make dummy-nav-e2e` reaches every create-flow screen with zero writes (exits 0)
    - `CRITIQUE.md` exists, records both an HTML-aesthetic pass and the §3 semantic-parity matrix, and has 0 OPEN findings: `! grep -q 'OPEN' .planning/design/create-flow/CRITIQUE.md` AND `grep -q 'RESOLVED' .planning/design/create-flow/CRITIQUE.md`
    - FIELDS.md HTML-present/TUI-present columns are filled for all 12 screens
    - The mutation-ceremony + test-before-mutate affordances are explicitly checked in the parity matrix (a §3 row each)
    - `make lint` passes
  </acceptance_criteria>
  <done>The create-flow pilot is captured in both media (12+12 PNGs), proven navigable on the real binary, and its agent-ui-ux-designer semantic-parity critique is resolved to 0 OPEN — validating the full per-surface pipeline before fan-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create-flow mockup/dummy ← design artifacts only | pilot is a design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the create-flow e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB3 | Tampering / Elevation of Privilege | create-flow dummy screens reaching a backend write path | mitigate | `surface_createflow.go` imports no backend package; inherits 02-02's `go list -deps` gate + 02-03's zero-write e2e assertion |
| T-02-PAR | Repudiation | approved design silently diverging from the recipe end-state | mitigate | The §3 semantic-parity matrix pins recipe defaults (Port 443, IdentitiesOnly yes, ed25519, sentinels) as MUST-match rows; CRITIQUE.md must reach 0 OPEN before approval |
| T-02-SAFE | Information Disclosure | test-before-mutate / backup affordance omitted from the frozen reference | mitigate | The four-beat mutation ceremony (§5) + test-against-throwaway states are REQUIRED states (test-fail/confirm-write/backup-notice) captured in both media |
</threat_model>

## Artifacts this phase produces

- `.planning/design/create-flow/FIELDS.md` — 12-state semantic field-parity manifest (HTML/TUI-present columns filled)
- `.planning/design/create-flow/manifest.json` — 12 `{screen, htmlRoute, keys, signature}` entries (drives capture + e2e)
- `.planning/design/create-flow/CRITIQUE.md` — agent-ui-ux-designer aesthetic + parity findings, 0 OPEN
- `.planning/design/mockup-src/src/routes/create-flow/*.route.tsx` — 12 MUI v7 route files (auto-discovered)
- `.planning/design/create-flow/{html,tui}/*.png` — 12 + 12 captured screenshots
- `internal/dummytui/surface_createflow.go` (+ `_test.go`) — TUI dummy create-flow surface, 12 screens, registered, backend-free

<verification>
- 12 HTML + 12 TUI PNGs exist; `make dummy-nav-e2e` reaches every create-flow screen with zero writes
- Recipe copy is real (Port 443, IdentitiesOnly yes, ssh -G, sentinels); no lorem
- CRITIQUE.md 0 OPEN; FIELDS.md parity columns filled; both `/mui` + `agent-ui-ux-designer` engaged
- Dummy imports zero backend packages
</verification>

<success_criteria>
- DLV-01/DLV-05 for the pilot: create-flow exists as an HTML/MUI mockup AND a navigable backend-free TUI dummy, both screenshot-captured, with semantic parity resolved.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named across all three tasks.
- The per-surface pipeline (FIELDS→manifest→mockup→dummy→capture→critique) is proven, ready for the six-surface fan-out.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md` when done.
</output>
