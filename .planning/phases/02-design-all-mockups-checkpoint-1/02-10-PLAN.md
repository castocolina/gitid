---
phase: 02-design-all-mockups-checkpoint-1
plan: 10
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/fixer/FIELDS.md
  - .planning/design/fixer/manifest.json
  - .planning/design/fixer/parity.json
  - .planning/design/fixer/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/fixer/fixer-list.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/fix-preview.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/confirm-destructive.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/result-applied.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/nothing-to-fix.route.tsx
  - internal/dummytui/surface_fixer.go
  - internal/dummytui/surface_fixer_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The fixer is mocked and dummied across all 6 named states from UX-DIRECTION §4.7, incl. the healthy nothing-to-fix empty state"
    - "It presents SSH and Git problems in two sections, each with severity + plain explanation + suggested fix, applied only with confirm + backup (FIX-01/02)"
    - "Fix-in-place rewrites of existing directives show a before/after diff and name the backup path before applying; any batch-fix still previews every change (§4.7)"
    - "Both media captured (6 PNGs each) with the breadcrumb gate; dummy-nav e2e reaches every screen; parity.json has no unresolved row; the surface claims number key 5 via RegisterOrReplace"
  artifacts:
    - path: ".planning/design/fixer/manifest.json"
      provides: "hardened per-screen {surface, screen, htmlRoute, keysFromHome, signature} for 6 states"
      contains: "fix-preview"
    - path: ".planning/design/fixer/parity.json"
      provides: "machine-checkable §3 rows + fix-in-place-diff + nothing-to-fix rows (gate: no row status != resolved)"
      contains: "status"
    - path: "internal/dummytui/surface_fixer.go"
      provides: "TUI dummy fixer surface (view 5, RegisterOrReplace key 5), backend-free"
      contains: "RegisterOrReplace"
  key_links:
    - from: "src/routes/fixer/fix-preview.route.tsx"
      to: "before/after diff + backup path"
      via: "fix-in-place preview before confirm"
      pattern: "diff"
    - from: "internal/dummytui/surface_fixer.go"
      to: "dummytui.RegisterOrReplace (number key 5)"
      via: "init()-registration replacing the placeholder, no data.go edit"
      pattern: "RegisterOrReplace"
---

<objective>
Fan-out surface: the **Fixer** (view `5`, owned later by Phase 8). Repeat the proven per-surface pipeline (02-04) for all 6 states from UX-DIRECTION §4.7.

States (verbatim §4.7): `fixer-list` (two sections SSH / Git, each problem: severity + plain explanation + suggested fix) · `fix-preview` (diff of the exact change) · `confirm-destructive` (for anything that rewrites existing directives) · `backup-notice` · `result-applied` · `nothing-to-fix` (the healthy empty state).

Highest-risk affordance (§4.7): **fix-in-place rewrites of existing user directives** — each fix shows a before/after diff and names the backup path before applying; batch-fix (if offered) still previews every change.

Review folded in (Codex 02-REVIEWS.md): HIGH-2 number-key view (5) → `RegisterOrReplace` (no data.go edit); HIGH-4 structured `parity.json` gate; HIGH-3(c) hardened manifest schema; HIGH-1 the ALLOWLIST no-backend proof; MEDIUM-10 fan-out isolation — writes ONLY its own surface files; serialize BOTH `pnpm build` AND the whole-module `make lint` if wave-4 runs concurrently (review M2 — both are whole-module with no per-surface isolation).

DLV-02: author the MUI mockup with `/mui`, the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/ssh-config.recipe
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fixer FIELDS.md + manifest.json (hardened) + parity.json seed + MUI mockup (6 states)</name>
  <files>.planning/design/fixer/FIELDS.md, .planning/design/fixer/manifest.json, .planning/design/fixer/parity.json, .planning/design/mockup-src/src/routes/fixer/fixer-list.route.tsx, .planning/design/mockup-src/src/routes/fixer/fix-preview.route.tsx, .planning/design/mockup-src/src/routes/fixer/confirm-destructive.route.tsx, .planning/design/mockup-src/src/routes/fixer/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/fixer/result-applied.route.tsx, .planning/design/mockup-src/src/routes/fixer/nothing-to-fix.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.7 (6 states + fix-in-place affordance + nothing-to-fix empty state), § 2 (master-detail; SSH+Git sections), § 5 (four-beat ceremony: fix-in-place shows diff + backup path)
    - .planning/design/create-flow/{FIELDS.md, manifest.json, parity.json} + a create-flow route (02-04 — proven format)
    - REQUIREMENTS.md FIX-01/02, .planning/design/mockup-src/src/data/recipeFixtures.ts, .planning/design/PARITY.template.json
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` and the 02-04 format: author `FIELDS.md` (6-state table), `manifest.json` (6 hardened `{surface:"fixer", screen, htmlRoute, keysFromHome, signature}` entries — unique screen/htmlRoute/signature, absolute keysFromHome; route titles `fixer/<screen>`), and copy `PARITY.template.json` to `parity.json` seeding the §3 rows + a `fix-in-place-diff-and-backup` row + a `nothing-to-fix-empty-state` row. Author 6 `*.route.tsx` under `src/routes/fixer/` inside `<Shell>` (master-detail, SSH + Git sections): `fixer-list` (each problem shows severity + plain explanation + suggested fix, split SSH / Git); `fix-preview` showing the exact BEFORE/AFTER diff; `confirm-destructive` for rewrites of existing directives; `backup-notice` naming the timestamped backup path BEFORE applying; `result-applied`; `nothing-to-fix` (the healthy empty state). A batch-fix, if shown, must still preview every change. Real recipe copy, no lorem. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/fixer/manifest.json'))))") -eq 6 && python3 -c "import json;m=json.load(open('.planning/design/fixer/manifest.json'));assert len(set(e['screen'] for e in m))==6 and len(set(e['signature'] for e in m))==6 and all(e.get('keysFromHome') for e in m)"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 6 hardened-schema screens (unique screen+signature, non-empty keysFromHome) AND `grep -q 'nothing-to-fix' .planning/design/fixer/manifest.json`
    - `parity.json` seeded with §3 + fix-in-place + nothing-to-fix rows: `python3 -c "import json;r=json.load(open('.planning/design/fixer/parity.json'));assert len(r)>=9"`
    - 6 route files exist: `test $(ls .planning/design/mockup-src/src/routes/fixer/*.route.tsx | wc -l) -eq 6`
    - Two-section + severity + diff + backup present: `grep -rq 'severity' .../fixer/` AND `grep -riq 'diff' .../fixer/` AND `grep -rq 'backup' .../fixer/`
    - The healthy empty state exists: `grep -q 'nothing-to-fix' .planning/design/fixer/FIELDS.md`
    - No lorem: `! grep -riq 'lorem' .../fixer/`; `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The fixer MUI mockup renders all 6 states incl. the healthy empty state, with two-section problems, before/after diff previews, and backup-before-apply, with a seeded parity.json.</done>
</task>

<task type="auto">
  <name>Task 2: fixer TUI dummy surface (6 screens, RegisterOrReplace key 5, backend-free)</name>
  <files>internal/dummytui/surface_fixer.go, internal/dummytui/surface_fixer_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/registry.go (RegisterOrReplace), internal/dummytui/data.go (02-02 — the placeholder Fixer view on key 5 to REPLACE)
    - .planning/design/fixer/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 5 = key 5; breadcrumb), § 4.7
  </read_first>
  <action>
    Per DLV-02 (this is a UI task): this dummy screen reproduces the `/mui`-built mockup (the parity source) and `agent-ui-ux-designer` validates the terminal-skin cross-media parity (UX-DIRECTION §2). Write `surface_fixer.go` in `package dummytui` mirroring 02-04: register fixer as the primary view on number key `5` via `RegisterOrReplace` (review HIGH-2 — replaces the 02-02 placeholder, no data.go edit). Define 6 `ScreenDef`s matching `manifest.json`; render fixer-list (SSH+Git sections, severity + explanation + suggested fix, ✓/!/✗ glyph+word), fix-preview (before/after diff), confirm-destructive, backup-notice (timestamped path), result-applied, nothing-to-fix — hardcoded fixture values. NO backend import. Write `surface_fixer_test.go` asserting 6 registered screens, fixer sole owner of key 5, each RenderScreen non-empty + contains its signature + the `fixer/<id>` breadcrumb, connected Keys path.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run Fixer && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `surface_fixer.go` claims key 5 via RegisterOrReplace: `grep -q 'RegisterOrReplace' internal/dummytui/surface_fixer.go`; test asserts count 6 AND sole ownership of key 5
    - `go test -race ./internal/dummytui/... -run Fixer` passes (breadcrumb per screen)
    - Copy anchors: `grep -qi 'severity' internal/dummytui/surface_fixer.go`
    - No-backend ALLOWLIST holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - No shared-file edit — RegisterOrReplace claims key 5 without touching data.go (review H1 — exit-code-honest, matching 02-11's idiom): `[ -z "$(git diff --name-only -- internal/dummytui/data.go internal/dummytui/model.go .planning/design/mockup-src/src/App.tsx .planning/design/mockup-src/package.json .planning/design/mockup-src/pnpm-lock.yaml Makefile)" ]` (FAILS if any shared file was modified); `make lint` passes
  </acceptance_criteria>
  <done>The fixer surface exists in the dummy as view 5 (sole owner of key 5 via RegisterOrReplace) with all 6 screens incl. the healthy empty state, mirroring the mockup, proven backend-free, no data.go edit.</done>
</task>

<task type="auto">
  <name>Task 3: Capture fixer (both media) + agent-ui-ux-designer critique → parity.json 0-unresolved</name>
  <files>.planning/design/fixer/CRITIQUE.md, .planning/design/fixer/FIELDS.md, .planning/design/fixer/parity.json</files>
  <read_first>
    - .planning/design/create-flow/{CRITIQUE.md, parity.json} (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.7, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/fixer' ./internal/screenshot/...` (6 HTML + 6 TUI PNGs, breadcrumb-gated); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/fixer' ./e2e/...`). Spawn `agent-ui-ux-designer`: record the HTML-aesthetic pass in `CRITIQUE.md` and fill `.planning/design/fixer/parity.json` — §3 rows + `fix-in-place-diff-and-backup` + `nothing-to-fix-empty-state` — resolving every divergence so NO row has `status != resolved`. Fill FIELDS.md columns. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/fixer' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/fixer' ./e2e/... && test $(ls .planning/design/fixer/html/*.png | wc -l) -eq 6 && test $(ls .planning/design/fixer/tui/*.png | wc -l) -eq 6 && python3 -c "import json;r=json.load(open('.planning/design/fixer/parity.json'));import sys;sys.exit(1 if (not r or [x for x in r if x.get('status')!='resolved']) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - 6 HTML + 6 TUI PNGs: `test $(ls .planning/design/fixer/html/*.png | wc -l) -eq 6` AND `test $(ls .planning/design/fixer/tui/*.png | wc -l) -eq 6`
    - The surface-scoped dummy-nav e2e passes (breadcrumb+signature asserted, zero writes)
    - `parity.json` has no unresolved row and includes the fix-in-place + nothing-to-fix rows: `python3 -c "import json;r=json.load(open('.planning/design/fixer/parity.json'));assert r and all(x['status']=='resolved' for x in r)"`
    - FIELDS.md columns filled for all 6 screens; `make lint` passes
  </acceptance_criteria>
  <done>fixer captured in both media (6+6), navigable, parity resolved in a machine-checkable parity.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixer mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB9 | Tampering / Elevation of Privilege | fixer dummy reaching a backend write path | mitigate | `surface_fixer.go` imports no backend package; inherits the 02-02 import-graph ALLOWLIST + 02-03 zero-write e2e |
| T-02-FIX | Tampering | fix-in-place rewrite of existing directives without a visible diff/backup in the frozen reference | mitigate | fix-preview shows before/after diff; backup-notice names the path BEFORE applying; both are parity.json rows (§5); batch-fix still previews every change |
| T-02-SEV | Information Disclosure | severity/explanation/suggested-fix omitted | mitigate | severity + explanation + suggested fix per problem are captured states + parity.json rows (FIX-01) |
| T-02-RG9 | Tampering | fan-out registration failing on a duplicate activation key | mitigate | `RegisterOrReplace` cleanly replaces the key-5 placeholder; the test asserts sole ownership of key 5 |
</threat_model>

## Artifacts this phase produces

- `.planning/design/fixer/{FIELDS.md, manifest.json (hardened), parity.json, CRITIQUE.md, html/*.png (6), tui/*.png (6)}`
- `.planning/design/mockup-src/src/routes/fixer/*.route.tsx` — 6 MUI v7 route files (titles `fixer/<screen>`)
- `internal/dummytui/surface_fixer.go` (+ `_test.go`) — TUI dummy view-5 surface (RegisterOrReplace key 5), 6 screens, backend-free

<verification>
- 6 HTML + 6 TUI PNGs; surface-scoped dummy-nav e2e passes; parity.json no unresolved row
- Two-section problems, fix-in-place diff + backup-before-apply, nothing-to-fix empty state — parity.json rows
- Dummy passes the no-backend allowlist; sole owner of key 5 via RegisterOrReplace; no data.go edit; no lorem
</verification>

<success_criteria>
- **Code review (required before this plan is done):** on completion, request a fresh-context code review via the `superpowers:requesting-code-review` skill, verifying the work against this plan `must_haves` and every task `<acceptance_criteria>`; resolve all CRITICAL/HIGH findings before the plan is marked complete. Supplements — not replaces — the phase-level `/gsd-code-review` and the phase-level external cross-vendor (Codex) review the execution loop runs.
- DLV-01/DLV-05 for fixer: HTML mockup + navigable backend-free TUI dummy, both captured, parity resolved in parity.json.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-10-SUMMARY.md` when done.
</output>
