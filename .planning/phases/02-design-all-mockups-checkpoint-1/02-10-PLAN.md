---
phase: 02-design-all-mockups-checkpoint-1
plan: 10
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/fixer/FIELDS.md
  - .planning/design/fixer/manifest.json
  - .planning/design/fixer/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/fixer/fixer-list.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/fix-preview.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/confirm-destructive.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/result-applied.route.tsx
  - .planning/design/mockup-src/src/routes/fixer/nothing-to-fix.route.tsx
  - internal/dummytui/surface_fixer.go
  - internal/dummytui/surface_fixer_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The fixer is mocked and dummied across all 6 named states from UX-DIRECTION §4.7, incl. the healthy nothing-to-fix empty state"
    - "It presents SSH and Git problems in two sections, each with severity + plain explanation + suggested fix, applied only with confirm + backup (FIX-01/02)"
    - "Fix-in-place rewrites of existing directives show a before/after diff and name the backup path before applying; any batch-fix still previews every change (§4.7)"
    - "Both media captured (6 PNGs each); dummy-nav e2e reaches every screen; CRITIQUE.md 0 OPEN"
  artifacts:
    - path: ".planning/design/fixer/manifest.json"
      provides: "per-screen {htmlRoute, keys, signature} for 6 states"
      contains: "fix-preview"
    - path: "internal/dummytui/surface_fixer.go"
      provides: "TUI dummy fixer surface (view 5, number key 5), backend-free"
      contains: "Register"
  key_links:
    - from: "src/routes/fixer/fix-preview.route.tsx"
      to: "before/after diff + backup path"
      via: "fix-in-place preview before confirm"
      pattern: "diff"
    - from: "internal/dummytui/surface_fixer.go"
      to: "dummytui.Register (number key 5)"
      via: "init()-registration, no model.go edit"
      pattern: "Register"
---

<objective>
Fan-out surface: the **Fixer** (view `5`, owned later by Phase 8). Repeat the proven per-surface pipeline (02-04) for all 6 states from UX-DIRECTION §4.7.

States (verbatim §4.7): `fixer-list` (two sections SSH / Git, each problem: severity + plain explanation + suggested fix) · `fix-preview` (diff of the exact change) · `confirm-destructive` (for anything that rewrites existing directives) · `backup-notice` · `result-applied` · `nothing-to-fix` (the healthy empty state).

Highest-risk affordance (§4.7): **fix-in-place rewrites of existing user directives** — each fix shows a before/after diff and names the backup path before applying; batch-fix (if offered) still previews every change (no silent multi-file mutation).

DLV-02: author the MUI mockup with `/mui`, the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/ssh-config.recipe
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fixer FIELDS.md + manifest.json + MUI mockup (6 states)</name>
  <files>.planning/design/fixer/FIELDS.md, .planning/design/fixer/manifest.json, .planning/design/mockup-src/src/routes/fixer/fixer-list.route.tsx, .planning/design/mockup-src/src/routes/fixer/fix-preview.route.tsx, .planning/design/mockup-src/src/routes/fixer/confirm-destructive.route.tsx, .planning/design/mockup-src/src/routes/fixer/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/fixer/result-applied.route.tsx, .planning/design/mockup-src/src/routes/fixer/nothing-to-fix.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.7 (6 states + fix-in-place affordance + nothing-to-fix empty state), § 2 (master-detail; SSH+Git sections), § 5 (four-beat ceremony: fix-in-place shows diff + backup path)
    - .planning/design/create-flow/FIELDS.md + a create-flow route (02-04 — proven format)
    - REQUIREMENTS.md FIX-01/02 (severity + explanation + suggested fix; confirm + backup; two-section fixer UX), .planning/design/mockup-src/src/data/recipeFixtures.ts
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` direction and the 02-04 format: author `FIELDS.md` (6-state table), `manifest.json` (6 entries), and 6 `*.route.tsx` under `src/routes/fixer/` inside `<Shell>` (master-detail, SSH + Git sections): `fixer-list` (each problem shows severity + plain explanation + suggested fix, split SSH / Git); `fix-preview` showing the exact BEFORE/AFTER diff of the change; `confirm-destructive` for rewrites of existing directives; `backup-notice` naming the timestamped backup path BEFORE applying; `result-applied`; `nothing-to-fix` (the healthy empty state — designed, not an afterthought). A batch-fix, if shown, must still preview every change. Real recipe copy, no lorem. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/fixer/manifest.json'))))") -eq 6</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 6 screens: `test $(python3 -c "import json;print(len(json.load(open('.planning/design/fixer/manifest.json'))))") -eq 6` AND `grep -q 'nothing-to-fix' .planning/design/fixer/manifest.json`
    - 6 route files exist: `test $(ls .planning/design/mockup-src/src/routes/fixer/*.route.tsx | wc -l) -eq 6`
    - Two-section + severity + diff + backup present: `grep -rq 'severity' .planning/design/mockup-src/src/routes/fixer/` AND `grep -riq 'diff' .planning/design/mockup-src/src/routes/fixer/` AND `grep -rq 'backup' .planning/design/mockup-src/src/routes/fixer/`
    - The healthy empty state exists: `grep -q 'nothing-to-fix' .planning/design/fixer/FIELDS.md`
    - No lorem: `! grep -riq 'lorem' .planning/design/mockup-src/src/routes/fixer/`
    - `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The fixer MUI mockup renders all 6 states incl. the healthy empty state, with two-section problems, before/after diff previews, and backup-before-apply.</done>
</task>

<task type="auto">
  <name>Task 2: fixer TUI dummy surface (6 screens, number key 5, backend-free)</name>
  <files>internal/dummytui/surface_fixer.go, internal/dummytui/surface_fixer_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/registry.go, internal/dummytui/data.go (02-02 — the placeholder Fixer view on key 5 to replace)
    - .planning/design/fixer/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 5 = key 5), § 4.7
  </read_first>
  <action>
    Write `surface_fixer.go` in `package dummytui` mirroring 02-04: register fixer as the primary view on number key `5` (replacing the 02-02 placeholder). Define 6 `ScreenDef`s matching `manifest.json`; render fixer-list (SSH+Git sections, severity + explanation + suggested fix, ✓/!/✗ glyph+word), fix-preview (before/after diff), confirm-destructive, backup-notice (timestamped path), result-applied, nothing-to-fix — hardcoded fixture values. NO backend import. Write `surface_fixer_test.go` asserting 6 registered screens, each RenderScreen non-empty + contains its signature, connected Keys path.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run Fixer && ! (go list -deps ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)')</automated>
  </verify>
  <acceptance_criteria>
    - `surface_fixer.go` registers 6 screens on number key 5: `grep -q 'Register' internal/dummytui/surface_fixer.go`; test asserts count 6
    - `go test -race ./internal/dummytui/... -run Fixer` passes
    - Copy anchors: `grep -qi 'severity' internal/dummytui/surface_fixer.go`
    - No backend import: `! grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)' internal/dummytui/surface_fixer.go`
    - `make lint` passes
  </acceptance_criteria>
  <done>The fixer surface exists in the dummy as view 5 with all 6 screens incl. the healthy empty state, mirroring the mockup, proven backend-free.</done>
</task>

<task type="auto">
  <name>Task 3: Capture fixer (both media) + agent-ui-ux-designer parity critique → CRITIQUE.md 0 OPEN</name>
  <files>.planning/design/fixer/CRITIQUE.md, .planning/design/fixer/FIELDS.md</files>
  <read_first>
    - .planning/design/create-flow/CRITIQUE.md (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.7, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/fixer' ./internal/screenshot/...` (6 HTML + 6 TUI PNGs); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNav/fixer' ./e2e/...`). Spawn `agent-ui-ux-designer` for `.planning/design/fixer/CRITIQUE.md` (aesthetic pass HTML + §3 parity matrix), giving the two-section split, the fix-in-place before/after diff + backup-before-apply, and the nothing-to-fix empty state explicit parity rows. Fill FIELDS.md parity columns; resolve to 0 OPEN. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/fixer' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNav/fixer' ./e2e/... && test $(ls .planning/design/fixer/html/*.png | wc -l) -eq 6 && test $(ls .planning/design/fixer/tui/*.png | wc -l) -eq 6 && ! grep -q 'OPEN' .planning/design/fixer/CRITIQUE.md</automated>
  </verify>
  <acceptance_criteria>
    - 6 HTML + 6 TUI PNGs: `test $(ls .planning/design/fixer/html/*.png | wc -l) -eq 6` AND `test $(ls .planning/design/fixer/tui/*.png | wc -l) -eq 6`
    - The surface-scoped dummy-nav e2e passes
    - `CRITIQUE.md` 0 OPEN with both passes; two-section, fix-in-place diff + backup-before-apply, nothing-to-fix are explicit parity rows
    - FIELDS.md parity columns filled for all 6 screens
    - `make lint` passes
  </acceptance_criteria>
  <done>fixer captured in both media (6+6), navigable, parity resolved to 0 OPEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixer mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB9 | Tampering / Elevation of Privilege | fixer dummy reaching a backend write path | mitigate | `surface_fixer.go` imports no backend package; inherits the 02-02 import-graph gate + 02-03 zero-write e2e |
| T-02-FIX | Tampering | fix-in-place rewrite of existing directives without a visible diff/backup in the frozen reference | mitigate | fix-preview shows before/after diff; backup-notice names the path BEFORE applying; both are MUST-match parity rows (§5); batch-fix still previews every change |
| T-02-SEV | Information Disclosure | severity/explanation/suggested-fix omitted | mitigate | severity + explanation + suggested fix per problem are captured states and parity rows (FIX-01) |
</threat_model>

## Artifacts this phase produces

- `.planning/design/fixer/{FIELDS.md, manifest.json, CRITIQUE.md, html/*.png (6), tui/*.png (6)}`
- `.planning/design/mockup-src/src/routes/fixer/*.route.tsx` — 6 MUI v7 route files
- `internal/dummytui/surface_fixer.go` (+ `_test.go`) — TUI dummy view-5 surface, 6 screens, backend-free

<verification>
- 6 HTML + 6 TUI PNGs; surface-scoped dummy-nav e2e passes; CRITIQUE.md 0 OPEN
- Two-section problems, fix-in-place diff + backup-before-apply, nothing-to-fix empty state — parity rows
- Dummy imports zero backend packages; no lorem
</verification>

<success_criteria>
- DLV-01/DLV-05 for fixer: HTML mockup + navigable backend-free TUI dummy, both captured, parity resolved.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-10-SUMMARY.md` when done.
</output>
