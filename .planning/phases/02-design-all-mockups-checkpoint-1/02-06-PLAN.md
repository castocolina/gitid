---
phase: 02-design-all-mockups-checkpoint-1
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/identity-manager/FIELDS.md
  - .planning/design/identity-manager/manifest.json
  - .planning/design/identity-manager/parity.json
  - .planning/design/identity-manager/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/identity-manager/list-empty.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/list-populated.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/detail-ssh-first.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/action-menu.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/clone-name-prompt.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/delete-choice.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/confirm-destructive.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/backup-notice.route.tsx
  - internal/dummytui/surface_identitymanager.go
  - internal/dummytui/surface_identitymanager_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The identity manager (the app's home/view-1) is mocked and dummied across all 8 named states from UX-DIRECTION §4.3, incl. the first-run list-empty landing"
    - "Per-row health uses the 8-label taxonomy legibly under NO_COLOR (glyph+word, not color alone)"
    - "The delete-choice offers 'delete everything (SSH+Git+key)' vs 'delete Git identity only'; the safer option is default-focused and the irreversible path carries the strongest confirm (MGR-06, §5)"
    - "Clone/new-key/delete modals render via the dummy's placeOverlay (backend-free); detail shows SSH-first, never rendering git attributes for an SSH-only identity (MGR-03/07); the surface claims number key 1 via RegisterOrReplace"
    - "Both media captured (8 PNGs each) with the breadcrumb gate; dummy-nav e2e reaches every screen incl. modals; parity.json has no unresolved row"
  artifacts:
    - path: ".planning/design/identity-manager/manifest.json"
      provides: "hardened per-screen {surface, screen, htmlRoute, keysFromHome, signature} for 8 states"
      contains: "delete-choice"
    - path: ".planning/design/identity-manager/parity.json"
      provides: "machine-checkable §3 parity rows + delete-choice/NO_COLOR/SSH-first rows (gate: no row status != resolved)"
      contains: "status"
    - path: "internal/dummytui/surface_identitymanager.go"
      provides: "TUI dummy identity-manager surface (view 1, RegisterOrReplace key 1) + modals via placeOverlay, backend-free"
      contains: "RegisterOrReplace"
  key_links:
    - from: "internal/dummytui/surface_identitymanager.go"
      to: "internal/dummytui placeOverlay + RegisterOrReplace"
      via: "clone/delete modals composited over dimmed list; claims key 1 without editing data.go"
      pattern: "RegisterOrReplace"
    - from: "src/routes/identity-manager/list-populated.route.tsx"
      to: "8-label taxonomy (glyph+word)"
      via: "per-row health legible under NO_COLOR"
      pattern: "key-missing"
---

<objective>
Fan-out surface: the **Identity Manager** — the app's home view (number key `1`), owned later by Phase 5. Repeat the proven per-surface pipeline (02-04) for all 8 states from UX-DIRECTION §4.3, INCLUDING the modal overlays (clone/new-key/delete-choice) which exercise the dummy's `placeOverlay` helper.

States (verbatim §4.3): `list-empty` (first-run landing) · `list-populated` (per-row 8-label health taxonomy) · `detail-ssh-first` (SSH first, then Git; NEVER git attributes for an SSH-only identity) · `action-menu` · `clone-name-prompt` (DISTINCT new name) · `delete-choice` ("delete everything (SSH+Git+key)" vs "delete Git identity only") · `confirm-destructive` · `backup-notice`.

Highest-risk affordance (§4.3): the delete choice — safer option default-focused; irreversible "everything" path gets the strongest confirm (§5). Row health legible under NO_COLOR (glyph + word).

Review folded in (Codex 02-REVIEWS.md): HIGH-2 this is a number-key view (1) that REPLACES the 02-02 placeholder → register via `RegisterOrReplace` (no data.go edit, no duplicate-key conflict); HIGH-4 structured `parity.json` gate; HIGH-3(c) hardened manifest schema; HIGH-1 the ALLOWLIST no-backend proof; MEDIUM-10 fan-out isolation — writes ONLY its own surface files (design dir, its 8 route files, surface_identitymanager.go + _test.go), never editing App.tsx/model.go/data.go/package.json/Makefile or another surface's files. Shared gitignored `dist/` is rebuilt per capture; serialize `pnpm build` if wave-4 runs concurrently (authoritative capture is 02-11).

DLV-02: author the MUI mockup with `/mui` and the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: identity-manager FIELDS.md + manifest.json (hardened) + parity.json seed + MUI mockup (8 states)</name>
  <files>.planning/design/identity-manager/FIELDS.md, .planning/design/identity-manager/manifest.json, .planning/design/identity-manager/parity.json, .planning/design/mockup-src/src/routes/identity-manager/list-empty.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/list-populated.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/detail-ssh-first.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/action-menu.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/clone-name-prompt.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/delete-choice.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/confirm-destructive.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/backup-notice.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.3 (8 states + delete-choice + NO_COLOR row-health), § 2 (master-detail archetype), § 5 (destructive-confirm rules)
    - .planning/design/create-flow/{FIELDS.md, manifest.json, parity.json} + a create-flow route (02-04 — proven format)
    - .planning/phases/01-foundations-spikes-ci/01-04-PLAN.md (the 8-label identity state taxonomy — the exact labels)
    - REQUIREMENTS.md MGR-01..08, .planning/design/mockup-src/src/data/recipeFixtures.ts, .planning/design/PARITY.template.json
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` and the 02-04 format: author `FIELDS.md` (8-state table), `manifest.json` (8 hardened `{surface:"identity-manager", screen, htmlRoute, keysFromHome, signature}` entries — unique screen/htmlRoute/signature, absolute keysFromHome; route titles `identity-manager/<screen>`), and copy `PARITY.template.json` to `parity.json` seeding the §3 rows + `delete-choice-safe-default`, `no_color-row-health`, `ssh-first-detail` rows. Author 8 `*.route.tsx` under `src/routes/identity-manager/` inside `<Shell>` (master-detail: list left, detail right) with real fixture data: `list-empty` (explicit empty-state copy); `list-populated` rendering each row with its 8-label taxonomy state (complete / incomplete / git-only / key-unused / key-used-ssh-only / key-used-both / key-missing / fragment-path-missing) as glyph + WORD (NO_COLOR-legible); `detail-ssh-first` (SSH first, NO git attributes for SSH-only); `action-menu`; `clone-name-prompt` (DISTINCT name); `delete-choice` (two options, safer default-focused); `confirm-destructive` (strongest confirm for the irreversible path); `backup-notice`. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/identity-manager/manifest.json'))))") -eq 8 && python3 -c "import json;m=json.load(open('.planning/design/identity-manager/manifest.json'));assert len(set(e['screen'] for e in m))==8 and len(set(e['signature'] for e in m))==8 and all(e.get('keysFromHome') for e in m)"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 8 hardened-schema screens (unique screen+signature, non-empty keysFromHome) AND `grep -q 'delete-choice' .planning/design/identity-manager/manifest.json` AND `grep -q 'list-empty' .planning/design/identity-manager/manifest.json`
    - `parity.json` seeded with §3 + delete/no_color/ssh-first rows: `python3 -c "import json;r=json.load(open('.planning/design/identity-manager/parity.json'));assert len(r)>=10"`
    - 8 route files exist: `test $(ls .planning/design/mockup-src/src/routes/identity-manager/*.route.tsx | wc -l) -eq 8`
    - The 8-label taxonomy appears with words (NO_COLOR-legible): `grep -rq 'key-missing' .../identity-manager/` AND `grep -rq 'fragment-path-missing' .../identity-manager/`
    - Both delete choices present: `grep -rq 'delete everything' .../identity-manager/` AND `grep -rq 'Git identity only' .../identity-manager/`
    - No lorem: `! grep -riq 'lorem' .../identity-manager/`; `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The identity-manager MUI mockup renders all 8 states incl. the first-run empty landing, the NO_COLOR-legible 8-label taxonomy, and the two-way delete choice, with a seeded parity.json.</done>
</task>

<task type="auto">
  <name>Task 2: identity-manager TUI dummy surface (8 screens incl. modals via placeOverlay, RegisterOrReplace key 1, backend-free)</name>
  <files>internal/dummytui/surface_identitymanager.go, internal/dummytui/surface_identitymanager_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/overlay.go (02-02 — placeOverlay for the clone/delete modals), internal/dummytui/registry.go (Register vs RegisterOrReplace), internal/dummytui/data.go (the placeholder Identities view on key 1 to REPLACE)
    - .planning/design/identity-manager/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 1 = key 1; master-detail; modals over dimmed layout; breadcrumb), § 4.3
  </read_first>
  <action>
    Write `surface_identitymanager.go` in `package dummytui` mirroring 02-04: register the identity-manager as the primary view on number key `1` (the app home) via `RegisterOrReplace` (review HIGH-2 — REPLACES the 02-02 `data.go` placeholder that owns key 1, so there is NO duplicate-activation-key conflict and NO edit to data.go). Define 8 `ScreenDef`s matching `manifest.json`; render `list-empty`/`list-populated` (per-row 8-label taxonomy, glyph+word), `detail-ssh-first` (SSH-first, no git attrs for SSH-only), `action-menu`, and the modal states (`clone-name-prompt`, `delete-choice`, `confirm-destructive`) composited via `placeOverlay` over the dimmed list (delete-choice default-focused on the safer option), `backup-notice`. NO backend import — hardcoded fixture identities in this file. Write `surface_identitymanager_test.go` asserting: 8 registered screens; identity-manager is the SOLE owner of key 1 after RegisterOrReplace (no stale placeholder); each RenderScreen non-empty + contains its signature + the `identity-manager/<id>` breadcrumb; the modal screens exercise placeOverlay without panic; a connected Keys path reaches every screen incl. modals.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run IdentityManager && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `surface_identitymanager.go` claims key 1 via RegisterOrReplace: `grep -q 'RegisterOrReplace' internal/dummytui/surface_identitymanager.go`; test asserts count 8 AND identity-manager is the sole owner of key 1
    - Modals use placeOverlay: `grep -q 'placeOverlay' internal/dummytui/surface_identitymanager.go`
    - `go test -race ./internal/dummytui/... -run IdentityManager` passes (incl. modal no-panic + breadcrumb per screen)
    - Taxonomy words present: `grep -q 'key-missing' internal/dummytui/surface_identitymanager.go`
    - No-backend ALLOWLIST holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - No data.go edit (RegisterOrReplace, not a placeholder edit): `git diff --name-only internal/dummytui/data.go | grep -q data.go && false || true`; `make lint` passes
  </acceptance_criteria>
  <done>The identity-manager surface exists in the dummy as view 1 (sole owner of key 1 via RegisterOrReplace) with all 8 screens incl. placeOverlay-composited modals, mirroring the mockup, proven backend-free, with no data.go edit.</done>
</task>

<task type="auto">
  <name>Task 3: Capture identity-manager (both media) + agent-ui-ux-designer critique → parity.json 0-unresolved</name>
  <files>.planning/design/identity-manager/CRITIQUE.md, .planning/design/identity-manager/FIELDS.md, .planning/design/identity-manager/parity.json</files>
  <read_first>
    - .planning/design/create-flow/{CRITIQUE.md, parity.json} (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review", § "Open Question 3" (NO_COLOR + keyboard-nav usability on the TUI side)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.3, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/identity-manager' ./internal/screenshot/...` (8 HTML + 8 TUI PNGs, each save gated on the breadcrumb); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/identity-manager' ./e2e/...`). Spawn `agent-ui-ux-designer`: record the HTML-aesthetic pass in `CRITIQUE.md` and fill `.planning/design/identity-manager/parity.json` — §3 rows + `delete-choice-safe-default`, `no_color-row-health`, `ssh-first-detail` — resolving every divergence so NO row has `status != resolved`. Fill FIELDS.md columns. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/identity-manager' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/identity-manager' ./e2e/... && test $(ls .planning/design/identity-manager/html/*.png | wc -l) -eq 8 && test $(ls .planning/design/identity-manager/tui/*.png | wc -l) -eq 8 && python3 -c "import json;r=json.load(open('.planning/design/identity-manager/parity.json'));import sys;sys.exit(1 if (not r or [x for x in r if x.get('status')!='resolved']) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - 8 HTML + 8 TUI PNGs: `test $(ls .planning/design/identity-manager/html/*.png | wc -l) -eq 8` AND `test $(ls .planning/design/identity-manager/tui/*.png | wc -l) -eq 8`
    - The surface-scoped dummy-nav e2e passes (all 8 screens incl. modals reachable, breadcrumb+signature asserted, zero writes)
    - `parity.json` has no unresolved row and includes delete-choice/NO_COLOR/SSH-first rows: `python3 -c "import json;r=json.load(open('.planning/design/identity-manager/parity.json'));assert r and all(x['status']=='resolved' for x in r)"`
    - FIELDS.md columns filled for all 8 screens; `make lint` passes
  </acceptance_criteria>
  <done>identity-manager captured in both media (8+8), navigable incl. modals, parity resolved in a machine-checkable parity.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity-manager mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB5 | Tampering / Elevation of Privilege | identity-manager dummy (esp. delete modal) reaching a backend write path | mitigate | `surface_identitymanager.go` imports no backend package; inherits the 02-02 import-graph ALLOWLIST + 02-03 zero-write e2e |
| T-02-DEL | Tampering | irreversible full-delete defaulting to yes in the frozen reference | mitigate | delete-choice default-focuses the safer option; confirm-destructive requires the strongest confirm; both are parity.json rows (§5) requiring resolution before approval |
| T-02-COLOR | Information Disclosure | health state conveyed by color alone (illegible under NO_COLOR) | mitigate | 8-label taxonomy rendered as glyph + WORD; NO_COLOR legibility is a parity.json row |
| T-02-RG5 | Tampering | fan-out registration failing on a duplicate activation key | mitigate | `RegisterOrReplace` cleanly replaces the key-1 placeholder (review HIGH-2); the test asserts sole ownership of key 1 |
</threat_model>

## Artifacts this phase produces

- `.planning/design/identity-manager/{FIELDS.md, manifest.json (hardened), parity.json, CRITIQUE.md, html/*.png (8), tui/*.png (8)}`
- `.planning/design/mockup-src/src/routes/identity-manager/*.route.tsx` — 8 MUI v7 route files (titles `identity-manager/<screen>`)
- `internal/dummytui/surface_identitymanager.go` (+ `_test.go`) — TUI dummy view-1 surface (RegisterOrReplace key 1), 8 screens incl. placeOverlay modals, backend-free

<verification>
- 8 HTML + 8 TUI PNGs; surface-scoped dummy-nav e2e passes incl. modals; parity.json no unresolved row
- 8-label taxonomy legible under NO_COLOR; delete-choice safe-default; SSH-first detail — parity.json rows
- Dummy passes the no-backend allowlist; identity-manager sole owner of key 1 via RegisterOrReplace; no data.go edit; no lorem
</verification>

<success_criteria>
- DLV-01/DLV-05 for identity-manager: HTML mockup + navigable backend-free TUI dummy (incl. modals), both captured, parity resolved in parity.json.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-06-SUMMARY.md` when done.
</output>
