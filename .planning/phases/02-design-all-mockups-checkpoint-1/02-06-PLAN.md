---
phase: 02-design-all-mockups-checkpoint-1
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/identity-manager/FIELDS.md
  - .planning/design/identity-manager/manifest.json
  - .planning/design/identity-manager/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/identity-manager/list-empty.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/list-populated.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/detail-ssh-first.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/action-menu.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/clone-name-prompt.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/delete-choice.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/confirm-destructive.route.tsx
  - .planning/design/mockup-src/src/routes/identity-manager/backup-notice.route.tsx
  - internal/dummytui/surface_identitymanager.go
  - internal/dummytui/surface_identitymanager_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The identity manager (the app's home/view-1) is mocked and dummied across all 8 named states from UX-DIRECTION §4.3, incl. the first-run list-empty landing (designed, not an afterthought)"
    - "Per-row health uses the 8-label taxonomy (complete/incomplete/git-only/key-unused/key-used-ssh-only/key-used-both/key-missing/fragment-path-missing) legibly under NO_COLOR (glyph+word, not color alone)"
    - "The delete-choice offers 'delete everything (SSH+Git+key)' vs 'delete Git identity only'; the safer option is default-focused and the irreversible path carries the strongest confirm (MGR-06, §5)"
    - "Clone/new-key/delete modals render via the dummy's placeOverlay (backend-free); detail shows SSH-first, never rendering git attributes for an SSH-only identity (MGR-03/07)"
    - "Both media captured (8 PNGs each); dummy-nav e2e reaches every screen incl. modals; CRITIQUE.md 0 OPEN"
  artifacts:
    - path: ".planning/design/identity-manager/manifest.json"
      provides: "per-screen {htmlRoute, keys, signature} for 8 states"
      contains: "delete-choice"
    - path: "internal/dummytui/surface_identitymanager.go"
      provides: "TUI dummy identity-manager surface (view 1, number key 1) + modals via placeOverlay, backend-free"
      contains: "Register"
  key_links:
    - from: "internal/dummytui/surface_identitymanager.go"
      to: "internal/dummytui placeOverlay"
      via: "clone/new-key/delete-choice modals composited over dimmed list"
      pattern: "placeOverlay"
    - from: "src/routes/identity-manager/list-populated.route.tsx"
      to: "8-label taxonomy (glyph+word)"
      via: "per-row health legible under NO_COLOR"
      pattern: "key-missing"
---

<objective>
Fan-out surface: the **Identity Manager** — the app's home view (number key `1`), owned later by Phase 5. Repeat the proven per-surface pipeline (02-04) for all 8 states from UX-DIRECTION §4.3, INCLUDING the modal overlays (clone/new-key/delete-choice) which exercise the dummy's `placeOverlay` helper.

States (verbatim §4.3): `list-empty` (first-run landing — designed) · `list-populated` (per-row 8-label health taxonomy) · `detail-ssh-first` (SSH details first, then Git; NEVER git attributes for an SSH-only identity) · `action-menu` · `clone-name-prompt` (must be a DISTINCT new name) · `delete-choice` ("delete everything (SSH+Git+key)" vs "delete Git identity only") · `confirm-destructive` · `backup-notice`.

Highest-risk affordance (§4.3): the **delete choice** — two destructive options; the safer one default-focused; the irreversible "everything" path gets the strongest confirm (§5). Row health states legible under NO_COLOR (glyph + word).

DLV-02: author the MUI mockup with `/mui` and the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: identity-manager FIELDS.md + manifest.json + MUI mockup (8 states)</name>
  <files>.planning/design/identity-manager/FIELDS.md, .planning/design/identity-manager/manifest.json, .planning/design/mockup-src/src/routes/identity-manager/list-empty.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/list-populated.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/detail-ssh-first.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/action-menu.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/clone-name-prompt.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/delete-choice.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/confirm-destructive.route.tsx, .planning/design/mockup-src/src/routes/identity-manager/backup-notice.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.3 (8 states + delete-choice affordance + NO_COLOR row-health), § 2 (master-detail body archetype: list left ⅓, detail right ⅔), § 5 (destructive-confirm rules)
    - .planning/design/create-flow/FIELDS.md + a create-flow route (02-04 — proven format)
    - .planning/phases/01-foundations-spikes-ci/01-04-PLAN.md (the 8-label identity state taxonomy — the exact labels to display per row)
    - REQUIREMENTS.md MGR-01..08 (completeness, taxonomy, SSH-first detail, clone distinct name, delete two-way choice, per-identity health), .planning/design/mockup-src/src/data/recipeFixtures.ts
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` direction and the 02-04 format: author `FIELDS.md` (8-state table), `manifest.json` (8 entries), and 8 `*.route.tsx` under `src/routes/identity-manager/` inside `<Shell>` using the master-detail body archetype (list left, detail right) with real fixture data: the first-run `list-empty` landing (explicit empty-state copy, not blank); `list-populated` rendering each identity row with its 8-label taxonomy state (complete / incomplete / git-only / key-unused / key-used-ssh-only / key-used-both / key-missing / fragment-path-missing) as glyph + WORD (legible under NO_COLOR, never color alone); `detail-ssh-first` showing SSH details first then Git and rendering NO git attributes for an SSH-only identity; `action-menu`; `clone-name-prompt` requiring a DISTINCT new name; `delete-choice` with the two options ("delete everything (SSH+Git+key)" vs "delete Git identity only"), the safer one default-focused; `confirm-destructive` with the strongest confirm for the irreversible path; `backup-notice`. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/identity-manager/manifest.json'))))") -eq 8</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 8 screens: `test $(python3 -c "import json;print(len(json.load(open('.planning/design/identity-manager/manifest.json'))))") -eq 8` AND `grep -q 'delete-choice' .planning/design/identity-manager/manifest.json` AND `grep -q 'list-empty' .planning/design/identity-manager/manifest.json`
    - 8 route files exist: `test $(ls .planning/design/mockup-src/src/routes/identity-manager/*.route.tsx | wc -l) -eq 8`
    - The 8-label taxonomy appears with words (NO_COLOR-legible): `grep -rq 'key-missing' .planning/design/mockup-src/src/routes/identity-manager/` AND `grep -rq 'fragment-path-missing' .planning/design/mockup-src/src/routes/identity-manager/`
    - Both delete choices present: `grep -rq 'delete everything' .planning/design/mockup-src/src/routes/identity-manager/` AND `grep -rq 'Git identity only' .planning/design/mockup-src/src/routes/identity-manager/`
    - No lorem: `! grep -riq 'lorem' .planning/design/mockup-src/src/routes/identity-manager/`
    - `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The identity-manager MUI mockup renders all 8 states incl. the first-run empty landing, the NO_COLOR-legible 8-label taxonomy, and the two-way delete choice.</done>
</task>

<task type="auto">
  <name>Task 2: identity-manager TUI dummy surface (8 screens incl. modals via placeOverlay, backend-free)</name>
  <files>internal/dummytui/surface_identitymanager.go, internal/dummytui/surface_identitymanager_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/overlay.go (02-02 — placeOverlay for the clone/new-key/delete modals), internal/dummytui/registry.go
    - .planning/design/identity-manager/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 1 = number key 1; master-detail; modals over dimmed layout), § 4.3
  </read_first>
  <action>
    Write `surface_identitymanager.go` in `package dummytui` mirroring 02-04: register the identity-manager as the primary view on number key `1` (the app home). Define 8 `ScreenDef`s matching `manifest.json`; render `list-empty`/`list-populated` (per-row 8-label taxonomy, glyph+word), `detail-ssh-first` (SSH-first, no git attrs for SSH-only), `action-menu`, and the modal states (`clone-name-prompt`, `delete-choice`, `confirm-destructive`) composited via `placeOverlay` over the dimmed list (delete-choice default-focused on the safer option). `backup-notice`. NO backend import — hardcoded fixture identities in this file. Write `surface_identitymanager_test.go` asserting 8 registered screens, each RenderScreen non-empty + contains its signature, the modal screens exercise placeOverlay without panic, and a connected Keys path reaches every screen incl. modals.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run IdentityManager && ! (go list -deps ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)')</automated>
  </verify>
  <acceptance_criteria>
    - `surface_identitymanager.go` registers 8 screens on number key 1: `grep -q 'Register' internal/dummytui/surface_identitymanager.go`; test asserts count 8
    - Modals use placeOverlay: `grep -q 'placeOverlay' internal/dummytui/surface_identitymanager.go`
    - `go test -race ./internal/dummytui/... -run IdentityManager` passes (incl. modal no-panic)
    - Taxonomy words present: `grep -q 'key-missing' internal/dummytui/surface_identitymanager.go`
    - No backend import: `! grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)' internal/dummytui/surface_identitymanager.go`
    - `make lint` passes
  </acceptance_criteria>
  <done>The identity-manager surface exists in the dummy as view 1 with all 8 screens incl. placeOverlay-composited modals, mirroring the mockup, proven backend-free.</done>
</task>

<task type="auto">
  <name>Task 3: Capture identity-manager (both media) + agent-ui-ux-designer parity critique → CRITIQUE.md 0 OPEN</name>
  <files>.planning/design/identity-manager/CRITIQUE.md, .planning/design/identity-manager/FIELDS.md</files>
  <read_first>
    - .planning/design/create-flow/CRITIQUE.md (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review", § "Open Question 3" (NO_COLOR + keyboard-nav usability on the TUI side)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.3, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/identity-manager' ./internal/screenshot/...` (8 HTML + 8 TUI PNGs under `.planning/design/identity-manager/{html,tui}/`); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNav/identity-manager' ./e2e/...`). Spawn `agent-ui-ux-designer` for `.planning/design/identity-manager/CRITIQUE.md` (aesthetic pass HTML + §3 parity matrix), giving the delete-choice default-focus, the NO_COLOR row-health legibility, and the SSH-first-detail (no git attrs for SSH-only) explicit parity rows. Fill FIELDS.md parity columns; resolve to 0 OPEN. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/identity-manager' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNav/identity-manager' ./e2e/... && test $(ls .planning/design/identity-manager/html/*.png | wc -l) -eq 8 && test $(ls .planning/design/identity-manager/tui/*.png | wc -l) -eq 8 && ! grep -q 'OPEN' .planning/design/identity-manager/CRITIQUE.md</automated>
  </verify>
  <acceptance_criteria>
    - 8 HTML + 8 TUI PNGs: `test $(ls .planning/design/identity-manager/html/*.png | wc -l) -eq 8` AND `test $(ls .planning/design/identity-manager/tui/*.png | wc -l) -eq 8`
    - The surface-scoped dummy-nav e2e passes (all 8 screens incl. modals reachable, zero writes)
    - `CRITIQUE.md` 0 OPEN with both passes; delete-choice, NO_COLOR row-health, and SSH-first-detail are explicit parity rows
    - FIELDS.md parity columns filled for all 8 screens
    - `make lint` passes
  </acceptance_criteria>
  <done>identity-manager captured in both media (8+8), navigable incl. modals, parity resolved to 0 OPEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity-manager mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB5 | Tampering / Elevation of Privilege | identity-manager dummy (esp. delete modal) reaching a backend write path | mitigate | `surface_identitymanager.go` imports no backend package; inherits the 02-02 import-graph gate + 02-03 zero-write e2e |
| T-02-DEL | Tampering | irreversible full-delete defaulting to yes in the frozen reference | mitigate | delete-choice default-focuses the safer option; confirm-destructive requires the strongest confirm; both are MUST-match parity rows (§5) before approval |
| T-02-COLOR | Information Disclosure | health state conveyed by color alone (illegible under NO_COLOR) | mitigate | 8-label taxonomy rendered as glyph + WORD; NO_COLOR legibility is a parity row |
</threat_model>

## Artifacts this phase produces

- `.planning/design/identity-manager/{FIELDS.md, manifest.json, CRITIQUE.md, html/*.png (8), tui/*.png (8)}`
- `.planning/design/mockup-src/src/routes/identity-manager/*.route.tsx` — 8 MUI v7 route files
- `internal/dummytui/surface_identitymanager.go` (+ `_test.go`) — TUI dummy view-1 surface, 8 screens incl. placeOverlay modals, backend-free

<verification>
- 8 HTML + 8 TUI PNGs; surface-scoped dummy-nav e2e passes incl. modals; CRITIQUE.md 0 OPEN
- 8-label taxonomy legible under NO_COLOR; delete-choice safe-default; SSH-first detail — all parity rows
- Dummy imports zero backend packages; no lorem
</verification>

<success_criteria>
- DLV-01/DLV-05 for identity-manager: HTML mockup + navigable backend-free TUI dummy (incl. modals), both captured, parity resolved.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-06-SUMMARY.md` when done.
</output>
