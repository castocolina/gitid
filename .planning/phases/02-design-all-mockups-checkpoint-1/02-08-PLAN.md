---
phase: 02-design-all-mockups-checkpoint-1
plan: 08
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/global-git/FIELDS.md
  - .planning/design/global-git/manifest.json
  - .planning/design/global-git/parity.json
  - .planning/design/global-git/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/global-git/options-list.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/option-detail.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/fix-preview.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/confirm-write.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/result-applied.route.tsx
  - internal/dummytui/surface_globalgit.go
  - internal/dummytui/surface_globalgit_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The global-git-options screen is mocked and dummied across all 6 named states from UX-DIRECTION §4.5"
    - "The options list manages init.defaultBranch (highlighting main vs master), core.ignorecase=false, autocrlf/eol, global user.email, and recipe defaults (push.autoSetupRemote, pull.rebase, fetch.prune, aliases, color, merge.conflictstyle, diff.colorMoved), each explained (GGIT-01)"
    - "The write preview shows the managed-block sentinels so the user sees content outside managed blocks is preserved verbatim (GGIT-01, §4.5)"
    - "Both media captured (6 PNGs each) with the breadcrumb gate; dummy-nav e2e reaches every screen; parity.json has no unresolved row; the surface claims number key 3 via RegisterOrReplace"
  artifacts:
    - path: ".planning/design/global-git/manifest.json"
      provides: "hardened per-screen {surface, screen, htmlRoute, keysFromHome, signature} for 6 states"
      contains: "options-list"
    - path: ".planning/design/global-git/parity.json"
      provides: "machine-checkable §3 rows + main-vs-master + managed-block-containment rows (gate: no row status != resolved)"
      contains: "status"
    - path: "internal/dummytui/surface_globalgit.go"
      provides: "TUI dummy global-git surface (view 3, RegisterOrReplace key 3), backend-free"
      contains: "RegisterOrReplace"
  key_links:
    - from: "src/routes/global-git/options-list.route.tsx"
      to: "recipeFixtures git defaults (defaultBranch main, ignorecase false, ...)"
      via: "each option explained; main-vs-master highlighted"
      pattern: "defaultBranch"
    - from: "internal/dummytui/surface_globalgit.go"
      to: "dummytui.RegisterOrReplace (number key 3)"
      via: "init()-registration replacing the placeholder, no data.go edit"
      pattern: "RegisterOrReplace"
---

<objective>
Fan-out surface: **Global Git options** (view `3`, owned later by Phase 7). Repeat the proven per-surface pipeline (02-04) for all 6 states from UX-DIRECTION §4.5.

States (verbatim §4.5): `options-list` (`init.defaultBranch` highlighting main vs master, `core.ignorecase=false`, autocrlf/eol, global `user.email`, recipe defaults: `push.autoSetupRemote`, `pull.rebase`, `fetch.prune`, aliases, color, `merge.conflictstyle`, `diff.colorMoved`) · `option-detail` · `fix-preview` · `confirm-write` · `backup-notice` · `result-applied`.

Highest-risk affordance (§4.5): writes must **preserve content outside managed blocks verbatim** (GGIT-01) — the preview must show the managed-block sentinels.

Review folded in (Codex 02-REVIEWS.md): HIGH-2 number-key view (3) → `RegisterOrReplace` (no data.go edit); HIGH-4 structured `parity.json` gate; HIGH-3(c) hardened manifest schema; HIGH-1 the ALLOWLIST no-backend proof; MEDIUM-10 fan-out isolation — writes ONLY its own surface files; serialize `pnpm build` if wave-4 runs concurrently.

DLV-02: author the MUI mockup with `/mui`, the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/gitconfig.recipe
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: global-git FIELDS.md + manifest.json (hardened) + parity.json seed + MUI mockup (6 states)</name>
  <files>.planning/design/global-git/FIELDS.md, .planning/design/global-git/manifest.json, .planning/design/global-git/parity.json, .planning/design/mockup-src/src/routes/global-git/options-list.route.tsx, .planning/design/mockup-src/src/routes/global-git/option-detail.route.tsx, .planning/design/mockup-src/src/routes/global-git/fix-preview.route.tsx, .planning/design/mockup-src/src/routes/global-git/confirm-write.route.tsx, .planning/design/mockup-src/src/routes/global-git/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/global-git/result-applied.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.5 (6 states + managed-block-containment affordance), § 2 (master-detail archetype), § 5 (managed-block containment shown)
    - .planning/design/create-flow/{FIELDS.md, manifest.json, parity.json} + a create-flow route (02-04 — proven format)
    - recipes/gitconfig.recipe ([core] ignorecase=false, [push] autoSetupRemote, [pull] rebase, [fetch] prune, [diff] colorMoved, [merge] conflictstyle, aliases, color), REQUIREMENTS.md GGIT-01, .planning/design/mockup-src/src/data/recipeFixtures.ts, .planning/design/PARITY.template.json
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` and the 02-04 format: author `FIELDS.md` (6-state table), `manifest.json` (6 hardened `{surface:"global-git", screen, htmlRoute, keysFromHome, signature}` entries — unique screen/htmlRoute/signature, absolute keysFromHome; route titles `global-git/<screen>`), and copy `PARITY.template.json` to `parity.json` seeding the §3 rows + a `main-vs-master-highlight` row + a `managed-block-containment` row. Author 6 `*.route.tsx` under `src/routes/global-git/` inside `<Shell>` (master-detail): the `options-list` managing `init.defaultBranch` with main-vs-master HIGHLIGHTED, `core.ignorecase=false`, autocrlf/eol, global `user.email`, and the recipe defaults (`push.autoSetupRemote`, `pull.rebase`, `fetch.prune`, aliases, color, `merge.conflictstyle`, `diff.colorMoved`), EACH with explanation copy; `option-detail`; `fix-preview`; `confirm-write` rendering the `# BEGIN/END gitid managed:` sentinels so content outside managed blocks is visibly preserved verbatim (GGIT-01); `backup-notice`; `result-applied`. Real recipe copy, no lorem. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/global-git/manifest.json'))))") -eq 6 && python3 -c "import json;m=json.load(open('.planning/design/global-git/manifest.json'));assert len(set(e['screen'] for e in m))==6 and len(set(e['signature'] for e in m))==6 and all(e.get('keysFromHome') for e in m)"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 6 hardened-schema screens (unique screen+signature, non-empty keysFromHome) AND `grep -q 'options-list' .planning/design/global-git/manifest.json`
    - `parity.json` seeded with §3 + main-vs-master + managed-block rows: `python3 -c "import json;r=json.load(open('.planning/design/global-git/parity.json'));assert len(r)>=9"`
    - 6 route files exist: `test $(ls .planning/design/mockup-src/src/routes/global-git/*.route.tsx | wc -l) -eq 6`
    - defaultBranch main-vs-master + recipe defaults present: `grep -rq 'defaultBranch' .../global-git/` AND `grep -rq 'ignorecase' .../global-git/` AND `grep -rq 'autoSetupRemote' .../global-git/`
    - Managed-block containment shown in confirm: `grep -rq 'BEGIN gitid managed' .../global-git/`
    - No lorem: `! grep -riq 'lorem' .../global-git/`; `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The global-git MUI mockup renders all 6 states with the explained baseline + recipe-default option set and visible managed-block containment, with a seeded parity.json.</done>
</task>

<task type="auto">
  <name>Task 2: global-git TUI dummy surface (6 screens, RegisterOrReplace key 3, backend-free)</name>
  <files>internal/dummytui/surface_globalgit.go, internal/dummytui/surface_globalgit_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/registry.go (RegisterOrReplace), internal/dummytui/data.go (02-02 — the placeholder GlobalGit view on key 3 to REPLACE)
    - .planning/design/global-git/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 3 = key 3; breadcrumb), § 4.5
  </read_first>
  <action>
    Write `surface_globalgit.go` in `package dummytui` mirroring 02-04: register global-git as the primary view on number key `3` via `RegisterOrReplace` (review HIGH-2 — replaces the 02-02 placeholder, no data.go edit, no duplicate-key conflict). Define 6 `ScreenDef`s matching `manifest.json`; render options-list (defaultBranch main-vs-master, ignorecase false, autocrlf/eol, user.email, recipe defaults, each explained), option-detail, fix-preview, sentinel-visible confirm-write, backup-notice, result-applied — hardcoded fixture values. NO backend import. Write `surface_globalgit_test.go` asserting 6 registered screens, global-git sole owner of key 3, each RenderScreen non-empty + contains its signature + the `global-git/<id>` breadcrumb, connected Keys path.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run GlobalGit && [ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `surface_globalgit.go` claims key 3 via RegisterOrReplace: `grep -q 'RegisterOrReplace' internal/dummytui/surface_globalgit.go`; test asserts count 6 AND sole ownership of key 3
    - `go test -race ./internal/dummytui/... -run GlobalGit` passes (breadcrumb per screen)
    - Copy anchors: `grep -q 'defaultBranch' internal/dummytui/surface_globalgit.go`
    - No-backend ALLOWLIST holds: `[ -z "$(go list -deps ./cmd/gitid-dummy/... ./internal/dummytui/... | grep '^github.com/castocolina/gitid/' | grep -vE '(internal/dummytui|cmd/gitid-dummy)$')" ]`
    - No data.go edit; `make lint` passes
  </acceptance_criteria>
  <done>The global-git surface exists in the dummy as view 3 (sole owner of key 3 via RegisterOrReplace) with all 6 screens, mirroring the mockup, proven backend-free, no data.go edit.</done>
</task>

<task type="auto">
  <name>Task 3: Capture global-git (both media) + agent-ui-ux-designer critique → parity.json 0-unresolved</name>
  <files>.planning/design/global-git/CRITIQUE.md, .planning/design/global-git/FIELDS.md, .planning/design/global-git/parity.json</files>
  <read_first>
    - .planning/design/create-flow/{CRITIQUE.md, parity.json} (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.5, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/global-git' ./internal/screenshot/...` (6 HTML + 6 TUI PNGs, breadcrumb-gated); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/global-git' ./e2e/...`). Spawn `agent-ui-ux-designer`: record the HTML-aesthetic pass in `CRITIQUE.md` and fill `.planning/design/global-git/parity.json` — §3 rows + `main-vs-master-highlight` + `managed-block-containment` — resolving every divergence so NO row has `status != resolved`. Fill FIELDS.md columns. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/global-git' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNavReachesAllScreens/global-git' ./e2e/... && test $(ls .planning/design/global-git/html/*.png | wc -l) -eq 6 && test $(ls .planning/design/global-git/tui/*.png | wc -l) -eq 6 && python3 -c "import json;r=json.load(open('.planning/design/global-git/parity.json'));import sys;sys.exit(1 if (not r or [x for x in r if x.get('status')!='resolved']) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - 6 HTML + 6 TUI PNGs: `test $(ls .planning/design/global-git/html/*.png | wc -l) -eq 6` AND `test $(ls .planning/design/global-git/tui/*.png | wc -l) -eq 6`
    - The surface-scoped dummy-nav e2e passes (breadcrumb+signature asserted, zero writes)
    - `parity.json` has no unresolved row and includes main-vs-master + managed-block rows: `python3 -c "import json;r=json.load(open('.planning/design/global-git/parity.json'));assert r and all(x['status']=='resolved' for x in r)"`
    - FIELDS.md columns filled for all 6 screens; `make lint` passes
  </acceptance_criteria>
  <done>global-git captured in both media (6+6), navigable, parity resolved in a machine-checkable parity.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| global-git mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB7 | Tampering / Elevation of Privilege | global-git dummy reaching a backend write path | mitigate | `surface_globalgit.go` imports no backend package; inherits the 02-02 import-graph ALLOWLIST + 02-03 zero-write e2e |
| T-02-VERB | Tampering | write preview not showing managed-block containment | mitigate | confirm-write renders the sentinels; managed-block containment is a parity.json row (GGIT-01, §5) requiring resolution before approval |
| T-02-EXPL2 | Information Disclosure | per-option explanations omitted | mitigate | explanation copy is contractual and a parity.json row (GGIT-01) |
| T-02-RG7 | Tampering | fan-out registration failing on a duplicate activation key | mitigate | `RegisterOrReplace` cleanly replaces the key-3 placeholder; the test asserts sole ownership of key 3 |
</threat_model>

## Artifacts this phase produces

- `.planning/design/global-git/{FIELDS.md, manifest.json (hardened), parity.json, CRITIQUE.md, html/*.png (6), tui/*.png (6)}`
- `.planning/design/mockup-src/src/routes/global-git/*.route.tsx` — 6 MUI v7 route files (titles `global-git/<screen>`)
- `internal/dummytui/surface_globalgit.go` (+ `_test.go`) — TUI dummy view-3 surface (RegisterOrReplace key 3), 6 screens, backend-free

<verification>
- 6 HTML + 6 TUI PNGs; surface-scoped dummy-nav e2e passes; parity.json no unresolved row
- main-vs-master highlight, per-option explanations, managed-block containment — parity.json rows
- Dummy passes the no-backend allowlist; sole owner of key 3 via RegisterOrReplace; no data.go edit; no lorem
</verification>

<success_criteria>
- DLV-01/DLV-05 for global-git: HTML mockup + navigable backend-free TUI dummy, both captured, parity resolved in parity.json.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-08-SUMMARY.md` when done.
</output>
