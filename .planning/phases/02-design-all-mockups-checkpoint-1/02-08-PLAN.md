---
phase: 02-design-all-mockups-checkpoint-1
plan: 08
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - .planning/design/global-git/FIELDS.md
  - .planning/design/global-git/manifest.json
  - .planning/design/global-git/CRITIQUE.md
  - .planning/design/mockup-src/src/routes/global-git/options-list.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/option-detail.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/fix-preview.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/confirm-write.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/backup-notice.route.tsx
  - .planning/design/mockup-src/src/routes/global-git/result-applied.route.tsx
  - internal/dummytui/surface_globalgit.go
  - internal/dummytui/surface_globalgit_test.go
autonomous: true
requirements: [DLV-01, DLV-02, DLV-05]
user_setup: []
must_haves:
  truths:
    - "The global-git-options screen is mocked and dummied across all 6 named states from UX-DIRECTION §4.5"
    - "The options list manages init.defaultBranch (highlighting main vs master), core.ignorecase=false, autocrlf/eol, global user.email, and recipe defaults (push.autoSetupRemote, pull.rebase, fetch.prune, aliases, color, merge.conflictstyle, diff.colorMoved), each explained (GGIT-01)"
    - "The write preview shows the managed-block sentinels so the user sees content outside managed blocks is preserved verbatim (GGIT-01, §4.5)"
    - "Both media captured (6 PNGs each); dummy-nav e2e reaches every screen; CRITIQUE.md 0 OPEN"
  artifacts:
    - path: ".planning/design/global-git/manifest.json"
      provides: "per-screen {htmlRoute, keys, signature} for 6 states"
      contains: "options-list"
    - path: "internal/dummytui/surface_globalgit.go"
      provides: "TUI dummy global-git surface (view 3, number key 3), backend-free"
      contains: "Register"
  key_links:
    - from: "src/routes/global-git/options-list.route.tsx"
      to: "recipeFixtures git defaults (defaultBranch main, ignorecase false, ...)"
      via: "each option explained; main-vs-master highlighted"
      pattern: "defaultBranch"
    - from: "internal/dummytui/surface_globalgit.go"
      to: "dummytui.Register (number key 3)"
      via: "init()-registration, no model.go edit"
      pattern: "Register"
---

<objective>
Fan-out surface: **Global Git options** (view `3`, owned later by Phase 7). Repeat the proven per-surface pipeline (02-04) for all 6 states from UX-DIRECTION §4.5.

States (verbatim §4.5): `options-list` (`init.defaultBranch` highlighting main vs master, `core.ignorecase=false`, autocrlf/eol, global `user.email`, recipe defaults: `push.autoSetupRemote`, `pull.rebase`, `fetch.prune`, aliases, color, `merge.conflictstyle`, `diff.colorMoved`) · `option-detail` · `fix-preview` · `confirm-write` · `backup-notice` · `result-applied`.

Highest-risk affordance (§4.5): writes must **preserve content outside managed blocks verbatim** (GGIT-01) — the preview must show the managed-block sentinels so the user sees their hand-written git config is untouched.

DLV-02: author the MUI mockup with `/mui`, the dummy to match; spawn `agent-ui-ux-designer` for the aesthetic + parity critique. Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-04-SUMMARY.md
@recipes/gitconfig.recipe
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: global-git FIELDS.md + manifest.json + MUI mockup (6 states)</name>
  <files>.planning/design/global-git/FIELDS.md, .planning/design/global-git/manifest.json, .planning/design/mockup-src/src/routes/global-git/options-list.route.tsx, .planning/design/mockup-src/src/routes/global-git/option-detail.route.tsx, .planning/design/mockup-src/src/routes/global-git/fix-preview.route.tsx, .planning/design/mockup-src/src/routes/global-git/confirm-write.route.tsx, .planning/design/mockup-src/src/routes/global-git/backup-notice.route.tsx, .planning/design/mockup-src/src/routes/global-git/result-applied.route.tsx</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 4.5 (6 states + managed-block-containment affordance), § 2 (master-detail archetype), § 5 (managed-block containment shown)
    - .planning/design/create-flow/FIELDS.md + a create-flow route (02-04 — proven format)
    - recipes/gitconfig.recipe ([core] ignorecase=false, [push] autoSetupRemote, [pull] rebase, [fetch] prune, [diff] colorMoved, [merge] conflictstyle, aliases, color), REQUIREMENTS.md GGIT-01, .planning/design/mockup-src/src/data/recipeFixtures.ts
  </read_first>
  <action>
    Using `/mui` + `agent-ui-ux-designer` direction and the 02-04 format: author `FIELDS.md` (6-state table), `manifest.json` (6 entries), and 6 `*.route.tsx` under `src/routes/global-git/` inside `<Shell>` (master-detail): the `options-list` managing `init.defaultBranch` with main-vs-master HIGHLIGHTED, `core.ignorecase=false`, autocrlf/eol, global `user.email`, and the recipe defaults (`push.autoSetupRemote`, `pull.rebase`, `fetch.prune`, aliases, color, `merge.conflictstyle`, `diff.colorMoved`), EACH with explanation copy; `option-detail`; `fix-preview`; `confirm-write` rendering the `# BEGIN/END gitid managed:` sentinels so content outside managed blocks is visibly preserved verbatim (GGIT-01); `backup-notice`; `result-applied`. Real recipe copy, no lorem. Rebuild dist; confirm routes render.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm exec tsc --noEmit && pnpm build && cd - && test $(python3 -c "import json;print(len(json.load(open('.planning/design/global-git/manifest.json'))))") -eq 6</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` lists exactly 6 screens: `test $(python3 -c "import json;print(len(json.load(open('.planning/design/global-git/manifest.json'))))") -eq 6` AND `grep -q 'options-list' .planning/design/global-git/manifest.json`
    - 6 route files exist: `test $(ls .planning/design/mockup-src/src/routes/global-git/*.route.tsx | wc -l) -eq 6`
    - defaultBranch main-vs-master + recipe defaults present: `grep -rq 'defaultBranch' .planning/design/mockup-src/src/routes/global-git/` AND `grep -rq 'ignorecase' .planning/design/mockup-src/src/routes/global-git/` AND `grep -rq 'autoSetupRemote' .planning/design/mockup-src/src/routes/global-git/`
    - Managed-block containment shown in confirm: `grep -rq 'BEGIN gitid managed' .planning/design/mockup-src/src/routes/global-git/`
    - No lorem: `! grep -riq 'lorem' .planning/design/mockup-src/src/routes/global-git/`
    - `pnpm exec tsc --noEmit` clean AND `pnpm build` exits 0
  </acceptance_criteria>
  <done>The global-git MUI mockup renders all 6 states with the explained baseline + recipe-default option set and visible managed-block containment.</done>
</task>

<task type="auto">
  <name>Task 2: global-git TUI dummy surface (6 screens, number key 3, backend-free)</name>
  <files>internal/dummytui/surface_globalgit.go, internal/dummytui/surface_globalgit_test.go</files>
  <read_first>
    - internal/dummytui/surface_createflow.go (02-04 — proven surface pattern), internal/dummytui/registry.go, internal/dummytui/data.go (02-02 — the placeholder GlobalGit view on key 3 to replace)
    - .planning/design/global-git/FIELDS.md + manifest.json (Task 1 — labels/copy/signatures)
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 2 (view 3 = key 3), § 4.5
  </read_first>
  <action>
    Write `surface_globalgit.go` in `package dummytui` mirroring 02-04: register global-git as the primary view on number key `3` (replacing the 02-02 placeholder). Define 6 `ScreenDef`s matching `manifest.json`; render options-list (defaultBranch main-vs-master, ignorecase false, autocrlf/eol, user.email, recipe defaults, each explained), option-detail, fix-preview, sentinel-visible confirm-write, backup-notice, result-applied — hardcoded fixture values. NO backend import. Write `surface_globalgit_test.go` asserting 6 registered screens, each RenderScreen non-empty + contains its signature, connected Keys path.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -race ./internal/dummytui/... -run GlobalGit && ! (go list -deps ./internal/dummytui/... | grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)')</automated>
  </verify>
  <acceptance_criteria>
    - `surface_globalgit.go` registers 6 screens on number key 3: `grep -q 'Register' internal/dummytui/surface_globalgit.go`; test asserts count 6
    - `go test -race ./internal/dummytui/... -run GlobalGit` passes
    - Copy anchors: `grep -q 'defaultBranch' internal/dummytui/surface_globalgit.go`
    - No backend import: `! grep -qE 'castocolina/gitid/internal/(identity|keygen|sshconfig|gitconfig|filewriter|tester|doctor|adopter|uploader)' internal/dummytui/surface_globalgit.go`
    - `make lint` passes
  </acceptance_criteria>
  <done>The global-git surface exists in the dummy as view 3 with all 6 screens, mirroring the mockup, proven backend-free.</done>
</task>

<task type="auto">
  <name>Task 3: Capture global-git (both media) + agent-ui-ux-designer parity critique → CRITIQUE.md 0 OPEN</name>
  <files>.planning/design/global-git/CRITIQUE.md, .planning/design/global-git/FIELDS.md</files>
  <read_first>
    - .planning/design/create-flow/CRITIQUE.md (02-04 — proven format), .planning/design/CRITIQUE.template.md
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "HTML↔TUI Parity Review"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-UX-DIRECTION.md § 3, § 4.5, .planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md
  </read_first>
  <action>
    Build dist and capture this surface only: `go test -tags screenshot -run 'TestCaptureAllMockupScreens/global-git' ./internal/screenshot/...` (6 HTML + 6 TUI PNGs); run the surface-scoped dummy-nav e2e (`go test -tags e2e -race -run 'TestDummyNav/global-git' ./e2e/...`). Spawn `agent-ui-ux-designer` for `.planning/design/global-git/CRITIQUE.md` (aesthetic pass HTML + §3 parity matrix), giving the main-vs-master highlight, the per-option explanations, and the managed-block containment explicit parity rows. Fill FIELDS.md parity columns; resolve to 0 OPEN. Name `/mui` + `agent-ui-ux-designer`.
  </action>
  <verify>
    <automated>cd .planning/design/mockup-src && pnpm build && cd - && go test -tags screenshot -run 'TestCaptureAllMockupScreens/global-git' ./internal/screenshot/... && go test -tags e2e -race -run 'TestDummyNav/global-git' ./e2e/... && test $(ls .planning/design/global-git/html/*.png | wc -l) -eq 6 && test $(ls .planning/design/global-git/tui/*.png | wc -l) -eq 6 && ! grep -q 'OPEN' .planning/design/global-git/CRITIQUE.md</automated>
  </verify>
  <acceptance_criteria>
    - 6 HTML + 6 TUI PNGs: `test $(ls .planning/design/global-git/html/*.png | wc -l) -eq 6` AND `test $(ls .planning/design/global-git/tui/*.png | wc -l) -eq 6`
    - The surface-scoped dummy-nav e2e passes
    - `CRITIQUE.md` 0 OPEN with both passes; main-vs-master, per-option explanations, managed-block containment are explicit parity rows
    - FIELDS.md parity columns filled for all 6 screens
    - `make lint` passes
  </acceptance_criteria>
  <done>global-git captured in both media (6+6), navigable, parity resolved to 0 OPEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| global-git mockup/dummy ← design artifacts only | design artifact; no real dotfile access; capture loads local file:// only |
| dummy binary ← sandboxed HOME | the e2e walk runs the real binary under an isolated HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-NB7 | Tampering / Elevation of Privilege | global-git dummy reaching a backend write path | mitigate | `surface_globalgit.go` imports no backend package; inherits the 02-02 import-graph gate + 02-03 zero-write e2e |
| T-02-VERB | Tampering | write preview not showing managed-block containment (user can't see hand-written config is preserved) | mitigate | confirm-write renders the sentinels; managed-block containment is a MUST-match parity row (GGIT-01, §5) before approval |
| T-02-EXPL2 | Information Disclosure | per-option explanations omitted | mitigate | explanation copy is contractual and a parity row (GGIT-01 "each option explained") |
</threat_model>

## Artifacts this phase produces

- `.planning/design/global-git/{FIELDS.md, manifest.json, CRITIQUE.md, html/*.png (6), tui/*.png (6)}`
- `.planning/design/mockup-src/src/routes/global-git/*.route.tsx` — 6 MUI v7 route files
- `internal/dummytui/surface_globalgit.go` (+ `_test.go`) — TUI dummy view-3 surface, 6 screens, backend-free

<verification>
- 6 HTML + 6 TUI PNGs; surface-scoped dummy-nav e2e passes; CRITIQUE.md 0 OPEN
- main-vs-master highlight, per-option explanations, managed-block containment — parity rows
- Dummy imports zero backend packages; no lorem
</verification>

<success_criteria>
- DLV-01/DLV-05 for global-git: HTML mockup + navigable backend-free TUI dummy, both captured, parity resolved.
- DLV-02: `/mui` + `agent-ui-ux-designer` engaged and named in all three tasks.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-08-SUMMARY.md` when done.
</output>
