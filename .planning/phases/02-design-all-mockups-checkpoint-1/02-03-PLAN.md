---
phase: 02-design-all-mockups-checkpoint-1
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - internal/screenshot/design_capture_test.go
  - internal/screenshot/manifest.go
  - e2e/harness_test.go
  - e2e/dummy_nav_e2e_test.go
  - Makefile
autonomous: true
requirements: [DLV-01, DLV-05, DLV-02]
user_setup: []
must_haves:
  truths:
    - "make screenshot-html-mockups builds the mockup dist/ and captures one PNG per manifest screen via Phase 1's go-rod file:// pipeline into .planning/design/<surface>/html/"
    - "make screenshot-tui-mockups captures one PNG per manifest screen via Phase 1's freeze pipeline over dummytui.RenderScreen() View() strings into .planning/design/<surface>/tui/"
    - "make dummy-nav-e2e drives the REAL built cmd/gitid-dummy binary via raw PTY keystrokes, asserts every manifest screen is reachable AND zero files are written under a sandboxed HOME"
    - "All three drivers are manifest-driven: they glob .planning/design/*/manifest.json, so surfaces ADD a manifest and coverage grows with NO edit to the driver/e2e/Makefile (fan-out-safe); capture + e2e both emit per-surface subtests"
    - "The capture + e2e paths are distinct TUI-render paths (View()-dump→freeze for screenshots; real-PTY for nav proof) — never conflated"
  artifacts:
    - path: "internal/screenshot/design_capture_test.go"
      provides: "manifest-driven TestCaptureAllMockupScreens (per-surface subtests) extending Phase 1 captureHTML/captureTUI"
      contains: "//go:build screenshot"
    - path: "e2e/dummy_nav_e2e_test.go"
      provides: "manifest-driven TestDummyNav PTY walker (per-surface subtests) over the real gitid-dummy binary + zero-write assertion"
      contains: "//go:build e2e"
    - path: "e2e/harness_test.go"
      provides: "BuildDummyBinary (mirrors BuildBinary for ./cmd/gitid-dummy)"
      contains: "BuildDummyBinary"
    - path: "Makefile"
      provides: "screenshot-html-mockups, screenshot-tui-mockups, dummy-nav-e2e targets"
      contains: "dummy-nav-e2e"
  key_links:
    - from: "internal/screenshot/design_capture_test.go"
      to: "captureHTML/captureTUI (Phase 1 internal/screenshot/html.go, tui.go)"
      via: "extend not rebuild — one call per (surface,screen) manifest tuple"
      pattern: "captureHTML|captureTUI"
    - from: "e2e/dummy_nav_e2e_test.go"
      to: "cmd/gitid-dummy real binary under sandboxed HOME"
      via: "creack/pty + x/vt harness reused verbatim from ui_pty_e2e_test.go"
      pattern: "BuildDummyBinary"
    - from: "all three drivers"
      to: ".planning/design/*/manifest.json"
      via: "glob discovery (surfaces add manifests, drivers unchanged)"
      pattern: "manifest.json"
---

<objective>
Wire the full capture + navigation-proof tooling that the pilot and fan-out surfaces plug into, EXTENDING Phase 1's `internal/screenshot` (never rebuilding it): (1) a `screenshot`-tagged capture driver that globs `.planning/design/*/manifest.json` and calls Phase 1's `captureHTML` (go-rod, file://dist#route) and `captureTUI` (freeze over `dummytui.RenderScreen` View() strings) once per screen, writing PNGs under `.planning/design/<surface>/{html,tui}/`; (2) an `e2e`-tagged PTY walker (`TestDummyNav`) that drives the REAL built `cmd/gitid-dummy` binary via raw keystrokes — reusing `ui_pty_e2e_test.go`'s harness verbatim + a new `BuildDummyBinary` — asserting every manifest screen is reachable AND zero files are written under a sandboxed HOME; (3) three Makefile targets. All three are manifest-driven AND emit per-surface subtests so the six-surface fan-out adds manifests (and runs `-run .../<surface>`) without touching any shared driver.

Purpose: DLV-01 needs screenshots of every screen; DLV-05 needs the dummy proven navigable on the real binary before presentation. Making the drivers manifest-driven with per-surface subtests is what lets Wave 4 fan out six surfaces in parallel with zero shared-file conflicts.
Output: `internal/screenshot/{design_capture_test.go, manifest.go}`, `e2e/{harness_test.go +BuildDummyBinary, dummy_nav_e2e_test.go}`, three Makefile targets.

PREFLIGHT (hard cross-phase dependency): this plan CONSUMES Phase 1 Plan 05's `internal/screenshot/html.go` (`captureHTML`) and `tui.go` (`captureTUI`) behind `//go:build screenshot`. Task 1 fails fast with an actionable message if those functions are absent — Phase 1 Plan 05 MUST have landed. Do NOT duplicate Phase 1 scope.

DLV-02: capture/e2e are test tooling for UI artifacts. The capture pipeline produces the screenshots the `/mui` mockup and dummy are judged on, and `agent-ui-ux-designer` consumes those PNGs for the parity critique (later plans). Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md
@.planning/phases/01-foundations-spikes-ci/01-05-PLAN.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-02-SUMMARY.md
@e2e/ui_pty_e2e_test.go
@e2e/harness_test.go
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifest schema + manifest-driven screenshot capture driver (extends Phase 1)</name>
  <files>internal/screenshot/manifest.go, internal/screenshot/design_capture_test.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md § "internal/screenshot/design_capture_test.go" (extend Phase 1 captureHTML/captureTUI; the Deviation/dependency note — cite the PLAN, fail fast if functions absent) and § "Extension pattern"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Pattern 1" (extend, don't rebuild), § "Pitfall 7" (never re-instantiate the go-rod launcher — call Phase 1's function), § "Pitfall 8" (View()-dump→freeze vs PTY are different paths)
    - .planning/phases/01-foundations-spikes-ci/01-05-PLAN.md § must_haves.artifacts (captureHTML in html.go, captureTUI in tui.go, both //go:build screenshot; artifact layout .planning/design/<surface>/{tui,html}/)
    - internal/dummytui/registry.go (from 02-02: RenderScreen(surface,screen) — the deterministic View() source for TUI capture)
  </read_first>
  <action>
    Write `manifest.go` (NO build tag — plain package code so surfaces and the e2e both consume it) defining the per-surface manifest type: a `ScreenManifestEntry{Surface, Screen, HTMLRoute, Keys []string, Signature string}` and `LoadManifests(designDir string) ([]ScreenManifestEntry, error)` that globs `.planning/design/*/manifest.json` and unmarshals each surface's array. `HTMLRoute` is the HashRouter fragment (e.g. `/create-flow/ssh-form-filled`); `Keys` is the keystroke sequence from the dummy's entry to that screen; `Signature` is a text marker present in BOTH media used by the e2e walker. Write `design_capture_test.go` behind `//go:build screenshot` with `TestCaptureAllMockupScreens`: FIRST fail fast if Phase 1's `captureHTML`/`captureTUI` symbols are unavailable (a compile-time reference guarded by an actionable `t.Fatal` message naming "Phase 1 Plan 05 internal/screenshot must land first"); then `LoadManifests`, group by surface, and for each surface run a subtest `t.Run(surface, ...)` (so `-run TestCaptureAllMockupScreens/create-flow` works per RESEARCH sampling) with nested `t.Run("html")` / `t.Run("tui")` so the two Makefile targets can filter on `.../html` / `.../tui`; the html subtest builds `file://<repoRoot>/.planning/design/mockup-src/dist/index.html#<HTMLRoute>` and calls Phase 1 `captureHTML(url, .planning/design/<surface>/html/<screen>.png)`; the tui subtest calls `dummytui.RenderScreen(surface, screen)` for the View() string and passes it to Phase 1 `captureTUI(view, .planning/design/<surface>/tui/<screen>.png)`. Never re-instantiate a go-rod launcher or freeze wrapper here (Pitfall 7) — import and call the Phase 1 functions only.
  </action>
  <verify>
    <automated>go vet -tags screenshot ./internal/screenshot/... && go test -tags screenshot -run 'TestCaptureAllMockupScreens' ./internal/screenshot/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/screenshot/design_capture_test.go` is build-tag isolated and extends Phase 1: `grep -q '//go:build screenshot' internal/screenshot/design_capture_test.go` AND `grep -qE 'captureHTML|captureTUI' internal/screenshot/design_capture_test.go`
    - It calls the dummy's deterministic render for TUI: `grep -q 'dummytui.RenderScreen' internal/screenshot/design_capture_test.go`
    - It is manifest-driven: `grep -q 'LoadManifests' internal/screenshot/design_capture_test.go` AND `grep -q 'manifest.json' internal/screenshot/manifest.go`
    - Per-surface subtests exist with nested html/tui subtests so `-run .../<surface>/html` and `.../<surface>/tui` work
    - With no manifests yet present, `go test -tags screenshot -run TestCaptureAllMockupScreens ./internal/screenshot/...` passes as a no-op (zero screens) OR fails fast with the Phase-1-missing message if internal/screenshot/{html,tui}.go are absent — never a silent blank-PNG pass
    - `go build ./cmd/gitid` still excludes go-rod/freeze (build-tag isolation preserved): `go list -deps ./cmd/gitid | grep -qE 'go-rod/rod|charmbracelet/freeze'` returns NON-zero
    - `make lint` passes
  </acceptance_criteria>
  <done>A build-tag-isolated, manifest-driven capture driver captures one HTML PNG and one TUI PNG per registered screen by calling Phase 1's pipelines — surfaces grow coverage by adding manifests, per-surface/html/tui subtests are runnable, and the driver fails fast (never blank-passes) when Phase 1 tooling is missing.</done>
</task>

<task type="auto">
  <name>Task 2: BuildDummyBinary + manifest-driven dummy-nav PTY e2e (per-surface subtests, every screen reachable, zero writes)</name>
  <files>e2e/harness_test.go, e2e/dummy_nav_e2e_test.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md § "e2e/dummy_nav_e2e_test.go" (reuse ptySession/startPTY/sendKey/waitFor/snapshot/close verbatim; add BuildDummyBinary mirroring BuildBinary lines 44-73; saveFrame → .planning/design/dummy-nav-frames/) and § "Build-tag convention"
    - e2e/ui_pty_e2e_test.go (harness: ptySession struct, startPTY, sendKey, waitFor, snapshot, close, saveFrame — reuse; single-owner emu goroutine — do NOT re-derive)
    - e2e/harness_test.go (BuildBinary sync.Once/temp-dir/HOME-restore structure; SandboxHome)
    - internal/screenshot/manifest.go (Task 1: LoadManifests — the e2e reuses the same manifest source of truth for keystrokes + signatures)
  </read_first>
  <action>
    In `e2e/harness_test.go` add `BuildDummyBinary(t) string` mirroring `BuildBinary` exactly (a SEPARATE package-level `sync.Once` + binary-path pair so it does not collide with the real-binary cache) but running `go build -o <bin> ./cmd/gitid-dummy`; keep the temp-dir + `HOME=realHome`-restore structure. Write `e2e/dummy_nav_e2e_test.go` behind `//go:build e2e` with `TestDummyNavReachesAllScreens`: build the dummy via `BuildDummyBinary`, create a sandboxed HOME via `SandboxHome(t)`, launch under `startPTY` with `HOME=<sandbox>`; then load the manifests (reuse `screenshot.LoadManifests` or an equivalent read of `.planning/design/*/manifest.json`) and GROUP entries by surface into `t.Run(surface, ...)` subtests (so a single surface runs via `-run TestDummyNavReachesAllScreens/<surface>` from the fan-out plans, and the full walk via `-run TestDummyNav`); within each surface subtest, for EACH manifest entry drive the entry's `Keys` sequence with `sendKey` and assert `waitFor` finds the entry's `Signature` in the decoded frame (`saveFrame` on each, writing to `.planning/design/dummy-nav-frames/`). After walking every screen, assert ZERO files were created under the sandboxed HOME (walk the sandbox dir; fail if any non-seed file exists) — the belt-and-suspenders runtime DLV-05 proof complementing the import-graph check. Use arg-slice `exec.Command` (`//nolint:gosec`). Keep this path SEPARATE from screenshot capture (Pitfall 8): the e2e decodes real-PTY frames via x/vt for reachability assertions; it does NOT produce the approval PNGs.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -tags e2e -race -timeout 60s -run TestDummyNav ./e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - `BuildDummyBinary` exists and targets the dummy: `grep -q 'func BuildDummyBinary' e2e/harness_test.go` AND `grep -q 'cmd/gitid-dummy' e2e/harness_test.go`
    - `dummy_nav_e2e_test.go` is e2e-tagged and manifest-driven: `grep -q '//go:build e2e' e2e/dummy_nav_e2e_test.go` AND `grep -qE 'manifest.json|LoadManifests' e2e/dummy_nav_e2e_test.go`
    - It groups by surface into subtests so `-run TestDummyNavReachesAllScreens/<surface>` works (a `t.Run(` over surfaces)
    - It drives the REAL binary and asserts zero writes under sandbox HOME: `grep -q 'SandboxHome' e2e/dummy_nav_e2e_test.go` AND the test walks the sandbox and fails on any created file
    - `go test -tags e2e -race -timeout 60s -run TestDummyNav ./e2e/...` passes (with the shell views from 02-02 reachable; grows to full coverage as surfaces add manifests)
    - Frames are written under `.planning/design/` not the Phase-5.7 dir: `grep -q '.planning/design' e2e/dummy_nav_e2e_test.go`
    - `make lint` passes
  </acceptance_criteria>
  <done>The real cmd/gitid-dummy binary is driven by raw PTY keystrokes over every manifest screen (grouped into per-surface subtests) and proven to write zero files under a sandboxed HOME — reusing the existing hardened harness, extended by BuildDummyBinary, and manifest-driven so fan-out surfaces need no e2e edits.</done>
</task>

<task type="auto">
  <name>Task 3: Makefile targets — screenshot-html-mockups, screenshot-tui-mockups, dummy-nav-e2e</name>
  <files>Makefile</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md § "Makefile targets ..." (the exact new-target shapes + the -mockups suffix to avoid colliding with Phase 1's single-fixture screenshot-tui/screenshot-html; add to the .PHONY line)
    - Makefile (existing test-e2e target build-then-test convention; the .PHONY line; note Phase 1 Plan 05 adds screenshot-tui/screenshot-html — do NOT redefine those)
    - .planning/phases/01-foundations-spikes-ci/01-05-PLAN.md § key_links (go test -tags screenshot -run TestCapture... invocation shape)
  </read_first>
  <action>
    Add three `.PHONY` Makefile targets, named distinctly from Phase 1's single-fixture `screenshot-tui`/`screenshot-html` (do not redefine those). `screenshot-html-mockups`: `cd .planning/design/mockup-src && pnpm install --frozen-lockfile && pnpm build` (build the dist/ first), then `go test -tags screenshot -run 'TestCaptureAllMockupScreens/.*/html' ./internal/screenshot/...` (frozen-lockfile ONLY — never a bare pnpm install). `screenshot-tui-mockups`: `go build -o bin/gitid-dummy ./cmd/gitid-dummy` (ensure the dummy compiles) then `go test -tags screenshot -run 'TestCaptureAllMockupScreens/.*/tui' ./internal/screenshot/...`. `dummy-nav-e2e`: `go build -o bin/gitid-dummy ./cmd/gitid-dummy` then `go test -tags e2e -race -timeout 60s -run TestDummyNav ./e2e/...`. Add all three to the existing `.PHONY:` line. Add `##`-style help comments matching the repo's Makefile convention.
  </action>
  <verify>
    <automated>make dummy-nav-e2e</automated>
  </verify>
  <acceptance_criteria>
    - The three targets exist and are PHONY: `grep -q 'screenshot-html-mockups:' Makefile` AND `grep -q 'screenshot-tui-mockups:' Makefile` AND `grep -q 'dummy-nav-e2e:' Makefile` AND they appear on a `.PHONY` line
    - `screenshot-html-mockups` uses frozen-lockfile only: `grep -q 'frozen-lockfile' Makefile` AND `! grep -qE 'pnpm install[^-]' Makefile` (no bare pnpm install)
    - The targets do not redefine Phase 1's `screenshot-tui:`/`screenshot-html:` (distinct `-mockups` names)
    - `make dummy-nav-e2e` exits 0
    - `make lint` passes
  </acceptance_criteria>
  <done>Three make targets drive the mockup HTML capture, the dummy TUI capture, and the dummy-nav PTY proof — each frozen-lockfile-safe and named to coexist with Phase 1's single-fixture targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| headless Chromium ← local mockup dist/ | go-rod loads ONLY a local file:// path to the repo's own build; no remote content |
| dummy binary ← sandboxed HOME | e2e runs the real binary under an isolated HOME and asserts zero writes |
| capture/e2e tooling ← go/pnpm toolchain | exec via arg-slice; pnpm only via --frozen-lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-CAP | Information Disclosure / Tampering | go-rod loading remote/untrusted content | mitigate | Capture targets are ALWAYS `file://<repoRoot>/.planning/design/mockup-src/dist/...`; reuse Phase 1's pinned-Chromium launcher (never re-instantiate it — Pitfall 7) |
| T-02-NB2 | Tampering / Elevation of Privilege | dummy touching real ~/.ssh/~/.gitconfig at runtime | mitigate | e2e runs under `SandboxHome(t)` and asserts zero created files — runtime complement to 02-02's import-graph proof |
| T-02-SC3 | Tampering | bare `pnpm install` pulling drifted deps in make/CI | mitigate | `screenshot-html-mockups` uses `pnpm install --frozen-lockfile` only; acceptance greps out any bare install |
| T-02-EX2 | Tampering | exec.Command to go build/go test/pnpm | mitigate | Arg-slice form only, `//nolint:gosec` where the binary path is harness-controlled |
</threat_model>

## Artifacts this phase produces

- `internal/screenshot/manifest.go` — `ScreenManifestEntry` + `LoadManifests` (globs `.planning/design/*/manifest.json`); shared by capture + e2e
- `internal/screenshot/design_capture_test.go` — `//go:build screenshot` `TestCaptureAllMockupScreens` (per-surface + html/tui subtests, extends Phase 1 captureHTML/captureTUI)
- `e2e/harness_test.go` — adds `BuildDummyBinary` (mirrors BuildBinary for ./cmd/gitid-dummy)
- `e2e/dummy_nav_e2e_test.go` — `//go:build e2e` `TestDummyNavReachesAllScreens` PTY walker (per-surface subtests) + zero-write assertion; frames under `.planning/design/dummy-nav-frames/`
- `Makefile` — `screenshot-html-mockups`, `screenshot-tui-mockups`, `dummy-nav-e2e` targets

<verification>
- `make dummy-nav-e2e` drives the real dummy over every manifest screen and asserts zero writes under sandbox HOME
- `make screenshot-html-mockups` / `screenshot-tui-mockups` capture per-screen PNGs via Phase 1's pipelines into `.planning/design/<surface>/{html,tui}/`
- All three drivers are manifest-driven with per-surface subtests; adding a surface manifest grows coverage with no driver edits
- go-rod/freeze stay out of `go build ./cmd/gitid`; capture and PTY paths remain distinct
</verification>

<success_criteria>
- DLV-01: every screen becomes screenshot-capturable via `make screenshot-*-mockups` (extending, not rebuilding, Phase 1's tooling).
- DLV-05: the dummy is proven navigable on the REAL binary with zero writes, BEFORE any presentation — the "e2e before presenting" ordering the user asked for.
- DLV-02: the capture pipeline feeds the `/mui` + `agent-ui-ux-designer` parity critique; both named.
- Fan-out-safe: manifest-driven drivers with per-surface subtests mean Wave 4 surfaces add manifests, not driver edits.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md` when done.
</output>
