---
phase: 02-design-all-mockups-checkpoint-1
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - internal/screenshot/manifest.go
  - internal/screenshot/manifest_test.go
  - internal/screenshot/design_adapter.go
  - internal/screenshot/design_capture_test.go
  - e2e/harness_test.go
  - e2e/dummy_nav_e2e_test.go
  - Makefile
autonomous: true
requirements: [DLV-01, DLV-05, DLV-02]
user_setup: []
must_haves:
  truths:
    - "make screenshot-html-mockups builds the mockup dist/ and captures one PNG per manifest screen via Phase 1's file:// pipeline into .planning/design/<surface>/html/; make screenshot-tui-mockups captures one PNG per manifest screen via Phase 1's freeze pipeline over dummytui.RenderScreen() into .planning/design/<surface>/tui/"
    - "The manifest schema is HARDENED: every entry requires surface, a globally-unique screen, a globally-unique htmlRoute, a globally-unique screen-specific signature, and keysFromHome (absolute from startup) — LoadManifests FAILS on any missing field or duplicate; every manifest screen is cross-validated to exist in BOTH the MUI routes (via the rendered breadcrumb) AND dummytui.RenderScreen"
    - "make dummy-nav-e2e drives the REAL built cmd/gitid-dummy binary via raw PTY keystrokes, RE-HOMES before each manifest entry then sends keysFromHome, asserts the active SCREEN (the `<surface>/<screen>` breadcrumb) AND the screen-specific signature per frame, AND asserts zero files are written under a sandboxed HOME"
    - "A thin Phase-1 ADAPTER (internal/screenshot/design_adapter.go) wraps Phase 1's actual html.go/tui.go capture functions behind stable CaptureHTMLScreen/CaptureTUIScreen names, so 02-03 does not guess Phase-1 signatures and fails fast if they are absent"
    - "All drivers are manifest-driven with per-surface subtests, so surfaces ADD a manifest and coverage grows with NO edit to the driver/e2e/Makefile (fan-out-safe)"
  artifacts:
    - path: "internal/screenshot/manifest.go"
      provides: "ScreenManifestEntry{Surface,Screen,HTMLRoute,KeysFromHome,Signature} + LoadManifests with schema+uniqueness validation + ScreenID helper"
      contains: "KeysFromHome"
    - path: "internal/screenshot/design_adapter.go"
      provides: "//go:build screenshot stable CaptureHTMLScreen/CaptureTUIScreen wrapping Phase 1 html.go/tui.go"
      contains: "CaptureHTMLScreen"
    - path: "internal/screenshot/design_capture_test.go"
      provides: "manifest-driven TestCaptureAllMockupScreens (per-surface + html/tui subtests) asserting the breadcrumb screen-ID before each capture"
      contains: "//go:build screenshot"
    - path: "e2e/dummy_nav_e2e_test.go"
      provides: "manifest-driven TestDummyNav PTY walker (per-surface, re-home + screen-ID + signature + zero-write)"
      contains: "//go:build e2e"
    - path: "Makefile"
      provides: "screenshot-html-mockups, screenshot-tui-mockups, dummy-nav-e2e targets"
      contains: "dummy-nav-e2e"
  key_links:
    - from: "internal/screenshot/design_capture_test.go"
      to: "design_adapter.go CaptureHTMLScreen/CaptureTUIScreen → Phase 1 html.go/tui.go"
      via: "stable adapter (no guessing Phase-1 signatures); one call per (surface,screen) manifest tuple"
      pattern: "CaptureHTMLScreen|CaptureTUIScreen"
    - from: "e2e/dummy_nav_e2e_test.go"
      to: "cmd/gitid-dummy real binary under sandboxed HOME"
      via: "re-home + keysFromHome; assert `<surface>/<screen>` breadcrumb + signature"
      pattern: "BuildDummyBinary"
    - from: "all drivers"
      to: ".planning/design/*/manifest.json"
      via: "glob discovery + schema/uniqueness validation (surfaces add manifests, drivers unchanged)"
      pattern: "manifest.json"
---

<objective>
Wire the capture + navigation-proof tooling the pilot and fan-out surfaces plug into, EXTENDING Phase 1's `internal/screenshot` (never rebuilding it): (1) a hardened manifest schema + a `screenshot`-tagged capture driver that globs `.planning/design/*/manifest.json` and calls Phase 1's capture via a thin ADAPTER once per screen, asserting the rendered `<surface>/<screen>` breadcrumb before saving (never a blank PNG); (2) an `e2e`-tagged PTY walker that drives the REAL `cmd/gitid-dummy` binary, RE-HOMES before each entry, sends absolute `keysFromHome`, asserts the active SCREEN (breadcrumb) AND a screen-specific signature, and asserts zero writes under a sandboxed HOME; (3) three Makefile targets. All manifest-driven with per-surface subtests so the fan-out adds manifests without touching any shared driver.

Review folded in (Codex 02-REVIEWS.md): HIGH-3(a) re-home before each entry / absolute keysFromHome; HIGH-3(b) assert the active SCREEN (breadcrumb), not only a signature; HIGH-3(c) hardened manifest schema (surface, unique screen/htmlRoute/signature, explicit keysFromHome); HIGH-3(d) cross-validate every manifest screen exists in BOTH MUI routes and dummytui.RenderScreen; MEDIUM-7 add a thin Phase-1 adapter (CaptureHTMLScreen/CaptureTUIScreen) so 02-03 does not guess Phase-1 signatures and still fails fast; MEDIUM-9 route validation is enforced (the capture breadcrumb assertion proves each htmlRoute resolves to the correct route module).

Purpose: DLV-01 needs screenshots of every screen; DLV-05 needs the dummy proven navigable on the real binary before presentation. Manifest-driven per-surface subtests let Wave 4 fan out six surfaces in parallel with zero shared-file conflicts.
Output: `internal/screenshot/{manifest.go, manifest_test.go, design_adapter.go, design_capture_test.go}`, `e2e/{harness_test.go +BuildDummyBinary, dummy_nav_e2e_test.go}`, three Makefile targets.

PREFLIGHT (hard cross-phase dependency): this plan CONSUMES Phase 1 Plan 05's `internal/screenshot/html.go` + `tui.go` capture functions behind `//go:build screenshot`. The ADAPTER (Task 1) is where the Phase-1 mapping lives: read Phase 1's ACTUAL exported symbols and wrap them — do NOT guess names. If those functions are absent, the adapter fails to compile with an actionable message naming "Phase 1 Plan 05 internal/screenshot must land first". Do NOT duplicate Phase 1 scope.

DLV-02: the capture pipeline produces the screenshots the `/mui` mockup + dummy are judged on, and `agent-ui-ux-designer` consumes those PNGs for the parity critique (later plans). Name both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md
@.planning/phases/01-foundations-spikes-ci/01-05-PLAN.md
@.planning/phases/02-design-all-mockups-checkpoint-1/02-02-SUMMARY.md
@e2e/ui_pty_e2e_test.go
@e2e/harness_test.go
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hardened manifest schema + Phase-1 adapter + manifest-driven capture driver + cross-validation</name>
  <files>internal/screenshot/manifest.go, internal/screenshot/manifest_test.go, internal/screenshot/design_adapter.go, internal/screenshot/design_capture_test.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md § "internal/screenshot/design_capture_test.go" (extend Phase 1; the Deviation/dependency note — cite the PLAN, fail fast) and § "Extension pattern"
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-RESEARCH.md § "Pattern 1" (extend, don't rebuild), § "Pitfall 7" (never re-instantiate the go-rod launcher — call Phase 1's function), § "Pitfall 8" (View()-dump→freeze vs PTY are different paths)
    - .planning/phases/01-foundations-spikes-ci/01-05-PLAN.md § must_haves.artifacts (Phase 1 html.go/tui.go capture funcs, both //go:build screenshot; layout .planning/design/<surface>/{tui,html}/) — read the ACTUAL exported signatures once Phase 1 has landed; the adapter wraps them, do not guess
    - internal/dummytui/registry.go (from 02-02: RenderScreen(surface,screen) full-shell string incl. the `<surface>/<screen>` breadcrumb — the deterministic TUI source + the cross-validation target)
  </read_first>
  <action>
    Write `manifest.go` (NO build tag) defining `ScreenManifestEntry{Surface, Screen, HTMLRoute string; KeysFromHome []string; Signature string}` and a `ScreenID(e)` helper returning `e.Surface+"/"+e.Screen`. `HTMLRoute` is the HashRouter fragment (e.g. `/create-flow/ssh-form-filled`); `KeysFromHome` is the ABSOLUTE keystroke sequence from app startup/home (review HIGH-3a); `Signature` is a screen-specific text marker. `LoadManifests(designDir string) ([]ScreenManifestEntry, error)` globs `.planning/design/*/manifest.json`, unmarshals each surface array, and VALIDATES (review HIGH-3c): every entry has non-empty Surface/Screen/HTMLRoute/Signature and non-empty KeysFromHome; the `<surface>/<screen>` ScreenID is globally unique; HTMLRoute is globally unique; Signature is globally unique — returns an error naming the first violation. Write `manifest_test.go` (plain) with `TestManifestSchema` asserting LoadManifests rejects a duplicate screen/htmlRoute/signature and a missing field, AND `TestManifestCrossValidation` (review HIGH-3d, TUI side) asserting for every real manifest entry that `dummytui.RenderScreen(e.Surface, e.Screen)` succeeds and its output contains the `<surface>/<screen>` breadcrumb (the HTML side is cross-validated at capture time below + by 02-01's verify-routes.mjs). Write `design_adapter.go` behind `//go:build screenshot` (review MEDIUM-7): stable wrappers `CaptureHTMLScreen(url, outPath string) error` and `CaptureTUIScreen(view, outPath string) error` that call Phase 1's ACTUAL html.go/tui.go capture functions (read their real exported names/signatures from Phase 1 Plan 05 and adapt here — a compile-time reference so a missing/renamed Phase-1 symbol fails the build with a comment naming "Phase 1 Plan 05"). Write `design_capture_test.go` behind `//go:build screenshot` with `TestCaptureAllMockupScreens`: `LoadManifests`, group by surface into `t.Run(surface, ...)` with nested `t.Run("html")`/`t.Run("tui")` so the Makefile targets filter on `.../html`/`.../tui`; the html subtest builds `file://<repoRoot>/.planning/design/mockup-src/dist/index.html#<HTMLRoute>`, and BEFORE saving asserts the rendered page contains the `<surface>/<screen>` breadcrumb screen-ID (review HIGH-3b/d + MEDIUM-9 — a missing/wrong route fails hard, never a blank PNG) then calls `CaptureHTMLScreen(url, .planning/design/<surface>/html/<screen>.png)`; the tui subtest calls `dummytui.RenderScreen(surface, screen)`, asserts the string contains the breadcrumb, then `CaptureTUIScreen(view, .planning/design/<surface>/tui/<screen>.png)`. Never re-instantiate a go-rod launcher/freeze wrapper here (Pitfall 7) — go only through the adapter.
  </action>
  <verify>
    <automated>go test ./internal/screenshot/... -run 'TestManifest' -count=1 && go vet -tags screenshot ./internal/screenshot/... && go test -tags screenshot -run 'TestCaptureAllMockupScreens' ./internal/screenshot/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The manifest schema is hardened: `grep -q 'KeysFromHome' internal/screenshot/manifest.go` AND `grep -q 'func LoadManifests' internal/screenshot/manifest.go` AND LoadManifests rejects duplicate screen/htmlRoute/signature + missing fields (proven by `TestManifestSchema`)
    - Cross-validation exists (TUI side): `TestManifestCrossValidation` asserts `dummytui.RenderScreen` succeeds + contains the breadcrumb for every manifest entry: `grep -q 'RenderScreen' internal/screenshot/manifest_test.go`
    - The Phase-1 adapter exists and is the only capture entry point: `grep -q 'CaptureHTMLScreen' internal/screenshot/design_adapter.go` AND `grep -q 'CaptureTUIScreen' internal/screenshot/design_adapter.go` AND `grep -qE 'CaptureHTMLScreen|CaptureTUIScreen' internal/screenshot/design_capture_test.go`
    - The capture driver asserts the breadcrumb screen-ID before each save (never blank-passes): `grep -qi 'breadcrumb\|ScreenID\|/<screen>' internal/screenshot/design_capture_test.go` (the html + tui subtests assert the `<surface>/<screen>` marker)
    - It is build-tag isolated + manifest-driven + per-surface/html/tui subtests: `grep -q '//go:build screenshot' internal/screenshot/design_capture_test.go` AND `grep -q 'LoadManifests' internal/screenshot/design_capture_test.go`
    - With no manifests yet, `go test -tags screenshot -run TestCaptureAllMockupScreens ./internal/screenshot/...` passes as a no-op OR fails fast with the Phase-1-missing adapter message — never a silent blank-PNG pass
    - `go build ./cmd/gitid` still excludes go-rod/freeze: `go list -deps ./cmd/gitid | grep -qE 'go-rod/rod|charmbracelet/freeze'` returns NON-zero
    - `make lint` passes
  </acceptance_criteria>
  <done>A hardened, cross-validated, manifest-driven capture driver captures one HTML PNG + one TUI PNG per registered screen through a stable Phase-1 adapter, asserting the `<surface>/<screen>` breadcrumb before each save — surfaces grow coverage by adding manifests, and the driver fails fast (never blank-passes) when Phase 1 tooling or a route is missing.</done>
</task>

<task type="auto">
  <name>Task 2: BuildDummyBinary + manifest-driven dummy-nav PTY e2e (re-home, screen-ID + signature, zero writes)</name>
  <files>e2e/harness_test.go, e2e/dummy_nav_e2e_test.go</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md § "e2e/dummy_nav_e2e_test.go" (reuse ptySession/startPTY/sendKey/waitFor/snapshot/close verbatim; add BuildDummyBinary mirroring BuildBinary; saveFrame → .planning/design/dummy-nav-frames/) and § "Build-tag convention"
    - e2e/ui_pty_e2e_test.go (harness: ptySession, startPTY, sendKey, waitFor, snapshot, close, saveFrame — reuse; single-owner emu goroutine — do NOT re-derive)
    - e2e/harness_test.go (BuildBinary sync.Once/temp-dir/HOME-restore; SandboxHome)
    - internal/screenshot/manifest.go (Task 1: LoadManifests + ScreenID — the e2e reuses the same manifest source for keysFromHome + signature + the breadcrumb screen-ID)
  </read_first>
  <action>
    In `e2e/harness_test.go` add `BuildDummyBinary(t) string` mirroring `BuildBinary` exactly (a SEPARATE package-level `sync.Once` + binary-path pair) but running `go build -o <bin> ./cmd/gitid-dummy`; keep the temp-dir + `HOME=realHome`-restore. Write `e2e/dummy_nav_e2e_test.go` behind `//go:build e2e` with `TestDummyNavReachesAllScreens`: build the dummy, create a sandboxed HOME via `SandboxHome(t)`, launch under `startPTY` with `HOME=<sandbox>`; load the manifests (reuse `screenshot.LoadManifests`) and GROUP by surface into `t.Run(surface, ...)` subtests. Within each surface subtest, for EACH manifest entry: FIRST RE-HOME (review HIGH-3a) — send `Esc` (bounded loop) until the header breadcrumb shows the Identities home, asserting the home breadcrumb is reached, so every entry is order-INDEPENDENT; THEN drive the entry's absolute `KeysFromHome` with `sendKey`; THEN assert `waitFor` finds BOTH the active-SCREEN marker `<surface>/<screen>` (the breadcrumb — review HIGH-3b, proves WHICH screen) AND the entry's screen-specific `Signature` in the decoded frame (`saveFrame` each, writing to `.planning/design/dummy-nav-frames/`). After walking every screen, assert ZERO files were created under the sandboxed HOME (walk the sandbox dir; fail on any non-seed file) — the runtime DLV-05 proof complementing the import-graph allowlist. Arg-slice `exec.Command` (`//nolint:gosec`). Keep this path SEPARATE from screenshot capture (Pitfall 8): the e2e decodes real-PTY frames via x/vt for reachability; it does NOT produce the approval PNGs.
  </action>
  <verify>
    <automated>go build ./cmd/gitid-dummy/... && go test -tags e2e -race -timeout 60s -run TestDummyNav ./e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - `BuildDummyBinary` exists and targets the dummy: `grep -q 'func BuildDummyBinary' e2e/harness_test.go` AND `grep -q 'cmd/gitid-dummy' e2e/harness_test.go`
    - `dummy_nav_e2e_test.go` is e2e-tagged + manifest-driven: `grep -q '//go:build e2e' e2e/dummy_nav_e2e_test.go` AND `grep -qE 'manifest.json|LoadManifests' e2e/dummy_nav_e2e_test.go`
    - It RE-HOMES before each entry and asserts the SCREEN not just a signature: `grep -qi 'home' e2e/dummy_nav_e2e_test.go` AND the test asserts the `<surface>/<screen>` breadcrumb per frame in ADDITION to the signature
    - It groups by surface into subtests so `-run TestDummyNavReachesAllScreens/<surface>` works (a `t.Run(` over surfaces)
    - It drives the REAL binary + asserts zero writes under sandbox HOME: `grep -q 'SandboxHome' e2e/dummy_nav_e2e_test.go` AND the test walks the sandbox and fails on any created file
    - `go test -tags e2e -race -timeout 60s -run TestDummyNav ./e2e/...` passes (shell views from 02-02 reachable; grows as surfaces add manifests)
    - Frames under `.planning/design/`: `grep -q '.planning/design' e2e/dummy_nav_e2e_test.go`
    - `make lint` passes
  </acceptance_criteria>
  <done>The real cmd/gitid-dummy binary is driven by raw PTY keystrokes that re-home before each order-independent entry, assert the active SCREEN (`<surface>/<screen>` breadcrumb) plus a screen-specific signature, and prove zero writes under a sandboxed HOME — reusing the hardened harness, extended by BuildDummyBinary, manifest-driven so fan-out surfaces need no e2e edits.</done>
</task>

<task type="auto">
  <name>Task 3: Makefile targets — screenshot-html-mockups, screenshot-tui-mockups, dummy-nav-e2e</name>
  <files>Makefile</files>
  <read_first>
    - .planning/phases/02-design-all-mockups-checkpoint-1/02-PATTERNS.md § "Makefile targets ..." (the -mockups suffix to avoid colliding with Phase 1's single-fixture screenshot-tui/screenshot-html; add to .PHONY)
    - Makefile (existing test-e2e build-then-test convention; the .PHONY line; Phase 1 Plan 05 adds screenshot-tui/screenshot-html — do NOT redefine those)
    - .planning/phases/01-foundations-spikes-ci/01-05-PLAN.md § key_links (go test -tags screenshot -run TestCapture... invocation shape)
  </read_first>
  <action>
    Add three `.PHONY` targets, named distinctly from Phase 1's single-fixture `screenshot-tui`/`screenshot-html`. `screenshot-html-mockups`: `cd .planning/design/mockup-src && pnpm install --frozen-lockfile && pnpm build` (pnpm build runs 02-01's verify-routes.mjs then vite build — the route-uniqueness gate), then `go test -tags screenshot -run 'TestCaptureAllMockupScreens/.*/html' ./internal/screenshot/...` (frozen-lockfile ONLY — never a bare pnpm install). `screenshot-tui-mockups`: `go build -o bin/gitid-dummy ./cmd/gitid-dummy` then `go test -tags screenshot -run 'TestCaptureAllMockupScreens/.*/tui' ./internal/screenshot/...`. `dummy-nav-e2e`: `go build -o bin/gitid-dummy ./cmd/gitid-dummy` then `go test -tags e2e -race -timeout 60s -run TestDummyNav ./e2e/...`. Add all three to the existing `.PHONY:` line + `##`-style help comments.
  </action>
  <verify>
    <automated>make dummy-nav-e2e</automated>
  </verify>
  <acceptance_criteria>
    - The three targets exist + are PHONY: `grep -q 'screenshot-html-mockups:' Makefile` AND `grep -q 'screenshot-tui-mockups:' Makefile` AND `grep -q 'dummy-nav-e2e:' Makefile`
    - `screenshot-html-mockups` uses frozen-lockfile only: `grep -q 'frozen-lockfile' Makefile` AND `! grep -qE 'pnpm install[^-]' Makefile`
    - The targets do not redefine Phase 1's `screenshot-tui:`/`screenshot-html:` (distinct `-mockups` names)
    - `make dummy-nav-e2e` exits 0
    - `make lint` passes
  </acceptance_criteria>
  <done>Three make targets drive the mockup HTML capture (via a route-gated pnpm build), the dummy TUI capture, and the dummy-nav PTY proof — each frozen-lockfile-safe and named to coexist with Phase 1's single-fixture targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| headless Chromium ← local mockup dist/ | go-rod loads ONLY a local file:// path to the repo's own build; no remote content |
| dummy binary ← sandboxed HOME | e2e runs the real binary under an isolated HOME and asserts zero writes |
| capture/e2e tooling ← go/pnpm toolchain | exec via arg-slice; pnpm only via --frozen-lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-CAP | Information Disclosure / Tampering | go-rod loading remote/untrusted content | mitigate | Capture targets are ALWAYS `file://<repoRoot>/.planning/design/mockup-src/dist/...`; reuse Phase 1's pinned-Chromium launcher via the adapter (never re-instantiate — Pitfall 7) |
| T-02-NB2 | Tampering / Elevation of Privilege | dummy touching real ~/.ssh/~/.gitconfig at runtime | mitigate | e2e runs under `SandboxHome(t)` and asserts zero created files — runtime complement to 02-02's import-graph allowlist |
| T-02-FP | Repudiation | a capture/e2e passing on the WRONG screen (false positive) | mitigate | manifest schema enforces unique screen/htmlRoute/signature; capture asserts the `<surface>/<screen>` breadcrumb before saving; e2e re-homes + asserts the breadcrumb + signature per frame (review HIGH-3) |
| T-02-SC3 | Tampering | bare `pnpm install` pulling drifted deps in make/CI | mitigate | `screenshot-html-mockups` uses `pnpm install --frozen-lockfile` only; acceptance greps out any bare install |
</threat_model>

## Artifacts this phase produces

- `internal/screenshot/manifest.go` (+ `manifest_test.go`) — hardened `ScreenManifestEntry` + `LoadManifests` (schema+uniqueness validation) + `ScreenID`; cross-validation of every screen against `dummytui.RenderScreen`
- `internal/screenshot/design_adapter.go` — `//go:build screenshot` stable `CaptureHTMLScreen`/`CaptureTUIScreen` wrapping Phase 1 html.go/tui.go (no guessing; fail-fast)
- `internal/screenshot/design_capture_test.go` — `//go:build screenshot` `TestCaptureAllMockupScreens` (per-surface + html/tui subtests; asserts the breadcrumb before each save)
- `e2e/harness_test.go` — adds `BuildDummyBinary`
- `e2e/dummy_nav_e2e_test.go` — `//go:build e2e` `TestDummyNavReachesAllScreens` (re-home + screen-ID + signature + zero-write); frames under `.planning/design/dummy-nav-frames/`
- `Makefile` — `screenshot-html-mockups`, `screenshot-tui-mockups`, `dummy-nav-e2e`

<verification>
- `make dummy-nav-e2e` re-homes before each screen, asserts the breadcrumb + signature, and asserts zero writes under sandbox HOME
- `make screenshot-html-mockups`/`screenshot-tui-mockups` capture per-screen PNGs via Phase 1's adapter into `.planning/design/<surface>/{html,tui}/`; each save is gated on the breadcrumb screen-ID
- LoadManifests rejects duplicate/missing fields; every screen cross-validated against dummytui.RenderScreen and the MUI route
- go-rod/freeze stay out of `go build ./cmd/gitid`; capture and PTY paths remain distinct
</verification>

<success_criteria>
- DLV-01: every screen becomes screenshot-capturable via `make screenshot-*-mockups` (extending, not rebuilding, Phase 1's tooling through a stable adapter).
- DLV-05: the dummy is proven navigable on the REAL binary — order-independent, screen-ID-asserted, zero writes — BEFORE any presentation.
- DLV-02: the capture pipeline feeds the `/mui` + `agent-ui-ux-designer` parity critique; both named.
</success_criteria>

<output>
Create `.planning/phases/02-design-all-mockups-checkpoint-1/02-03-SUMMARY.md` when done.
</output>
