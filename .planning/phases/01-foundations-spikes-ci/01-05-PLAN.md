---
phase: 01-foundations-spikes-ci
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - go.mod
  - go.sum
  - internal/screenshot/tui.go
  - internal/screenshot/html.go
  - internal/screenshot/doc.go
  - internal/screenshot/determinism.go
  - internal/screenshot/determinism_test.go
  - internal/screenshot/tui_capture_test.go
  - internal/screenshot/html_capture_test.go
  - .planning/design/fonts/README.md
  - .planning/design/_spike/fixture.html
  - .planning/design/_spike/GOLDENS.md
autonomous: true
requirements: [TOOL-05, DLV-03, TOOL-02]
must_haves:
  truths:
    - "Supply-chain legitimacy for freeze@v0.2.2 and go-rod@v0.116.2 is verified AUTOMATICALLY (pinned versions + Go checksum-DB verification + a documented pinned-version provenance review), with NO second human checkpoint in this one-checkpoint milestone"
    - "`make screenshot-tui` renders a deterministic View()-dump golden to a PNG using a vendored monospace font, a fixed geometry, a fixed theme, stripped timestamp metadata, and a recorded golden hash that re-runs reproduce"
    - "`make screenshot-html` renders a fixture HTML page to a PNG via headless Chromium at a fixed viewport + device-scale-factor + color scheme, a PINNED Chromium revision + cache path, with defined offline/failure behavior, and a recorded golden hash"
    - "Each make target has a concrete runnable entry point: a driving `_test.go` under the `screenshot` build tag (TestCaptureTUI / TestCaptureHTML) invoked via `go test -tags screenshot -run TestCapture...` that writes the golden PNG"
    - "Both capture paths write versioned PNGs under .planning/design/<surface>/{tui,html}/"
    - "The screenshot dependencies never enter the shipped gitid binary's dependency graph (build-tag isolated)"
    - "`make setup-env` installs the pinned freeze version and provisions the pinned headless browser revision"
  artifacts:
    - path: "Makefile"
      provides: "screenshot-tui + screenshot-html targets (each invokes a driving screenshot-tagged test); setup-env installs freeze + pinned browser"
      contains: "screenshot-tui"
    - path: "internal/screenshot/tui.go"
      provides: "View()-dump → freeze → PNG capture (build-tag: screenshot)"
      contains: "//go:build screenshot"
    - path: "internal/screenshot/tui_capture_test.go"
      provides: "TestCaptureTUI — runnable capture entry point make screenshot-tui invokes (build-tag: screenshot)"
      contains: "func TestCaptureTUI"
    - path: "internal/screenshot/html.go"
      provides: "fixture HTML → go-rod headless Chromium (pinned revision) → PNG (build-tag: screenshot)"
      contains: "//go:build screenshot"
    - path: "internal/screenshot/html_capture_test.go"
      provides: "TestCaptureHTML — runnable capture entry point make screenshot-html invokes (build-tag: screenshot)"
      contains: "func TestCaptureHTML"
    - path: "internal/screenshot/determinism.go"
      provides: "PNG timestamp-metadata stripping + SHA-256 golden hashing (buildable in the shipped graph? NO — build-tag: screenshot)"
      contains: "sha256"
  key_links:
    - from: "Makefile screenshot-tui"
      to: "freeze --font.file <vendored ttf> --theme <pinned>"
      via: "pinned font + theme for CI-deterministic rendering"
      pattern: "font.file"
    - from: "Makefile screenshot-tui / screenshot-html"
      to: "go test -tags screenshot -run TestCaptureTUI|TestCaptureHTML ./internal/screenshot/..."
      via: "driving _test.go under the screenshot build tag is the runnable capture entry point that writes the golden PNG (the make targets have something to invoke)"
      pattern: "go test -tags screenshot"
    - from: "go build ./cmd/gitid"
      to: "no go-rod/freeze in dependency graph"
      via: "//go:build screenshot tag isolation"
      pattern: "go:build screenshot"
    - from: "internal/screenshot/html.go"
      to: "go-rod launcher pinned Chromium revision + fixed cache path"
      via: "reproducible browser provisioning + offline/failure behavior"
      pattern: "Revision"
---

<objective>
Build the repeatable screenshot tooling (TOOL-05, DLV-03), hardened per Codex review: a `make screenshot-tui` target that renders a deterministic Bubble Tea `View()`-dump golden to a PNG via `charmbracelet/freeze` (pinned, vendored monospace font, fixed geometry + theme, stripped timestamp metadata, recorded golden hash), and a `make screenshot-html` target that renders a fixture HTML page to a PNG via a headless-Chromium `go-rod` driver (fixed viewport + device scale factor + color scheme, PINNED Chromium revision + cache path, defined offline/failure behavior, recorded golden hash). Each make target has a CONCRETE runnable entry point — a driving `_test.go` under the `screenshot` build tag (`TestCaptureTUI` / `TestCaptureHTML`) that the target invokes via `go test -tags screenshot -run TestCapture...` and that actually writes the PNG. Both write versioned PNGs under `.planning/design/<surface>/{tui,html}/`. The two new dependencies are dev/build-tools only — isolated behind a `//go:build screenshot` tag so `go build ./cmd/gitid` never pulls a headless browser into the shipped binary. `make setup-env` installs `freeze@v0.2.2` and provisions the pinned browser (extends TOOL-02).

Codex resolutions folded in:
- The former blocking-HUMAN supply-chain legitimacy checkpoint is REPLACED by an AUTOMATED verification task (Task 1). This removes the second human stop in the one-checkpoint milestone (Codex MEDIUM) and, with no blocking gate, this plan is now `autonomous: true` and no longer stalls Wave 1 (Codex MEDIUM wave hazard). Go modules are outside the npm/pip/cargo human-gate scope; legitimacy is proven via pinned versions + Go checksum-DB verification + the RESEARCH § Package Legitimacy Audit slopcheck (independently corroborated by Codex).
- Screenshot determinism is PROVEN (golden hash + metadata stripping + fixed render parameters), not merely asserted as "non-empty PNG" (Codex MEDIUM). The determinism helpers AND the end-to-end capture path (driving test → PNG) are both exercised.
- Chromium reproducibility is PINNED (revision + cache path) with tested offline/failure behavior (Codex MEDIUM).

Purpose: Phase 2 (design) and every later UI wave call these targets to capture reference/regression screenshots. Phase 1 builds the tooling and proves it against a trivial fixture (mockups do not exist until Phase 2). NOTE: not the Playwright MCP (agent-only; cannot run in make/CI).
Output: `Makefile` targets, `internal/screenshot/{tui,html,doc,determinism}.go`, the driving `{tui,html}_capture_test.go` entry points, a vendored font, a fixture HTML page, and recorded golden hashes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-spikes-ci/01-CONTEXT.md
@.planning/phases/01-foundations-spikes-ci/01-RESEARCH.md
@.planning/phases/01-foundations-spikes-ci/01-PATTERNS.md
@Makefile
@internal/platform/platform.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated supply-chain verification for freeze + go-rod (pinned versions + checksum-DB + provenance review)</name>
  <files>go.mod, go.sum, .planning/design/_spike/GOLDENS.md</files>
  <read_first>
    - .planning/phases/01-foundations-spikes-ci/01-RESEARCH.md § "Package Legitimacy Audit" (slopcheck [OK] + isolated-scratch-module compile for both packages), § Standard Stack (freeze@v0.2.2, go-rod@v0.116.2), § Security Domain (pin explicit Chromium revision)
    - .planning/phases/01-foundations-spikes-ci/01-REVIEWS.md § Codex (independent re-verification of freeze v0.2.2 + go-rod v0.116.2 against live sources; MEDIUM "second human checkpoint" + wave-hazard findings)
    - Makefile setup-env (lines 61-73 — note it ALREADY installs goimports@latest + gosec@latest; those pre-existing dev-tool installs are UNRELATED to this plan and must NOT be treated as this plan's `@latest` regressions)
  </read_first>
  <action>
    This REPLACES the former blocking-human legitimacy gate with automated verification (Codex MEDIUM: restore the one-checkpoint milestone; remove the Wave-1 stall). Pin EXACT versions in go.mod: `github.com/go-rod/rod v0.116.2` (and freeze installed as a dev tool at `@v0.2.2`, NOT a runtime require — its Makefile install line is added in Task 2). Run `go get github.com/go-rod/rod@v0.116.2` then `go mod verify` and confirm go.sum entries are consistent with the Go checksum database (GONOSUMCHECK/GONOSUMDB unset — the default sum.golang.org verification must pass; never set GOFLAGS=-insecure or GONOSUMCHECK). Record a short pinned-version provenance note in `.planning/design/_spike/GOLDENS.md` citing: RESEARCH § Package Legitimacy Audit slopcheck `[OK]` + isolated-compile for both packages, Codex's independent confirmation that both exist at the pinned versions, the EXACT pins `freeze@v0.2.2` and `go-rod v0.116.2`, and the exact pinned Chromium revision string to be used by go-rod (Task 3). Do NOT introduce any NEW `@latest` for this plan's dependencies. This task is automated (autonomous) and gated by `go mod verify` success, not a human sign-off. SCOPE NOTE (Codex HIGH): the freeze/go-rod pins are asserted per THIS plan's new installs — do NOT grep the whole Makefile for `@latest`, because setup-env already carries the unrelated pre-existing `goimports@latest`/`gosec@latest` dev-tool installs.
  </action>
  <verify>
    <automated>go mod verify && grep -q "go-rod/rod v0.116.2" go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `go mod verify` reports "all modules verified"
    - `grep -q "github.com/go-rod/rod v0.116.2" go.mod` succeeds (exact pin, not a range/latest)
    - `.planning/design/_spike/GOLDENS.md` records the provenance note + the exact pins `freeze@v0.2.2` and `go-rod v0.116.2` + the exact pinned Chromium revision string
    - This plan's go.mod change introduces no `@latest` and no insecure/checksum-bypass flag, scoped to go.mod (NOT a whole-Makefile grep, since setup-env's pre-existing `goimports@latest`/`gosec@latest` are unrelated to this plan): `! grep -q "@latest" go.mod` AND `! grep -qE "GONOSUMCHECK|insecure" go.mod`
    - `make lint` passes
  </acceptance_criteria>
  <done>Both dev/build dependencies are pinned (freeze@v0.2.2, go-rod v0.116.2) and verified against the Go checksum DB with a documented provenance review, replacing the human legitimacy gate — no second human checkpoint, no Wave-1 stall; the @latest check is scoped to this plan's go.mod, not the pre-existing setup-env dev-tool installs.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic TUI capture — View()-dump → freeze → PNG + vendored font + golden hash + driving TestCaptureTUI + `make screenshot-tui` (TOOL-05, DLV-03, TOOL-02)</name>
  <files>internal/screenshot/tui.go, internal/screenshot/doc.go, internal/screenshot/determinism.go, internal/screenshot/determinism_test.go, internal/screenshot/tui_capture_test.go, Makefile, .planning/design/fonts/README.md, .planning/design/_spike/GOLDENS.md</files>
  <read_first>
    - internal/platform/platform.go (arg-slice exec.Command + `#nosec G204` annotation style to mirror when shelling to freeze from Go)
    - Makefile (setup-env target lines 61-73 — where to add the freeze install; target-composition convention; note the pre-existing goimports@latest/gosec@latest are unrelated to this plan)
    - .planning/phases/01-foundations-spikes-ci/01-RESEARCH.md § "Code Examples" (verified `freeze --execute` invocation, real 640x332 PNG), § "Pitfall 6" (`--font.file` determinism), § "Pitfall 8" (`go install freeze` is allowed), § "Open Question 1" (confirm the exact freeze flag for a static ANSI file with `freeze --help`)
    - .planning/phases/01-foundations-spikes-ci/01-PATTERNS.md § "internal/screenshot/tui.go + html.go" (No Analog — use the exec-wrapper shape)
  </read_first>
  <action>
    First run `freeze --help` in full and confirm the exact flag to render a pre-captured ANSI/`.txt` golden to PNG (Open Question 1) — prefer a direct file argument over `--execute "cat golden"`. Add `go install github.com/charmbracelet/freeze@v0.2.2` to the `setup-env` target (Pitfall 8) — pin to the EXACT version `@v0.2.2`, never `@latest`. Vendor a specific monospace TTF under `.planning/design/fonts/` and document its provenance/license in `.planning/design/fonts/README.md`. Create `internal/screenshot/determinism.go` (behind `//go:build screenshot`) with helpers to STRIP non-deterministic PNG metadata (tIME chunk and any tEXt/creation-time chunks) and to compute a SHA-256 over the normalized PNG bytes; add `determinism_test.go` asserting stripping is idempotent and hashing is stable for a fixed input. Create `internal/screenshot/tui.go` behind `//go:build screenshot` with a function that takes a captured `View()` golden string, writes it to a temp `.txt`, and invokes `freeze` (arg-slice `exec.Command`, `#nosec G204` annotation, ALWAYS passing `--font.file <vendored ttf>`, a FIXED `--theme`, and a FIXED geometry 100x30 per D-04), then runs the metadata-strip + hash. Give the target a RUNNABLE ENTRY POINT (Codex HIGH #3): create `internal/screenshot/tui_capture_test.go` behind `//go:build screenshot` with `TestCaptureTUI` that calls the tui.go capture function against a trivial fixture `View()` golden, writes the PNG under `.planning/design/_spike/tui/`, computes its SHA-256, and asserts it reproduces the golden recorded in `.planning/design/_spike/GOLDENS.md` (re-running is deterministic). Record that golden SHA-256 in GOLDENS.md. Add a `screenshot-tui` Makefile target that invokes this driving test: `go test -tags screenshot -run TestCaptureTUI ./internal/screenshot/...` (this is what actually renders the PNG — the make target now has a concrete runnable entry point, not just a build-tag-gated library function with nothing to call it). Add `internal/screenshot/doc.go` documenting the build-tag isolation rule.
  </action>
  <verify>
    <automated>make screenshot-tui && test -s .planning/design/_spike/tui/*.png && go test -tags screenshot ./internal/screenshot/... -run 'TestCaptureTUI|TestDeterminism' -race</automated>
  </verify>
  <acceptance_criteria>
    - `make screenshot-tui` exits 0 and produces a non-empty PNG under `.planning/design/_spike/tui/` (`test -s` succeeds) AND re-running reproduces the SAME SHA-256 recorded in GOLDENS.md
    - `file .planning/design/_spike/tui/*.png` reports "PNG image data"
    - The runnable capture entry point exists and exercises the ACTUAL capture path end-to-end (not just the determinism helpers): `grep -q "//go:build screenshot" internal/screenshot/tui_capture_test.go` AND `grep -q "func TestCaptureTUI" internal/screenshot/tui_capture_test.go`, and `make screenshot-tui` invokes it (`grep -q "TestCaptureTUI" Makefile`)
    - `grep -q "//go:build screenshot" internal/screenshot/tui.go` succeeds
    - `go build ./cmd/gitid` still succeeds AND does not add freeze to the shipped graph: `go list -deps ./cmd/gitid | grep -q charmbracelet/freeze` returns NON-zero (freeze absent)
    - `grep -q "font.file" Makefile` (deterministic font passed) AND the freeze invocation pins a fixed `--theme` and 100x30 geometry
    - The freeze dev-tool install is pinned to the exact version (not @latest): `grep -q "freeze@v0.2.2" Makefile` AND `! grep -q "freeze@latest" Makefile`
    - determinism_test asserts metadata-strip idempotence + stable hash; the recorded golden hash exists in GOLDENS.md
    - `make lint` passes
  </acceptance_criteria>
  <done>`make screenshot-tui` deterministically renders a View()-dump golden to a versioned PNG through a real runnable entry point (TestCaptureTUI under the screenshot build tag): vendored font, fixed theme/geometry, stripped timestamp metadata, reproduced golden hash; freeze is pinned @v0.2.2 and absent from the shipped binary's dependency graph.</done>
</task>

<task type="auto">
  <name>Task 3: Deterministic HTML capture — fixture → go-rod (pinned Chromium) → PNG + golden hash + driving TestCaptureHTML + `make screenshot-html` (TOOL-05, DLV-03, TOOL-02)</name>
  <files>internal/screenshot/html.go, internal/screenshot/html_capture_test.go, Makefile, go.mod, go.sum, .planning/design/_spike/fixture.html, .planning/design/_spike/GOLDENS.md</files>
  <read_first>
    - internal/tester/tester.go (ResolvedVia — exec/driver-wrapper-returning-a-result shape to imitate structurally)
    - internal/screenshot/determinism.go (Task 2 — reuse the metadata-strip + SHA-256 helpers for the HTML golden)
    - .planning/phases/01-foundations-spikes-ci/01-RESEARCH.md § Standard Stack (go-rod v0.116.2, launcher revision pinning), § Anti-Patterns (keep go-rod out of the shipped binary via build tag), § Security Domain (pin an explicit Chromium revision for reproducibility), § "Open Question 3" (build-tag file in main module vs tools/go.mod — start with build tag)
    - .planning/design/_spike/GOLDENS.md (the pinned Chromium revision string recorded in Task 1)
    - .planning/phases/01-foundations-spikes-ci/01-PATTERNS.md § "No Analog Found"
  </read_first>
  <action>
    Place the go-rod import ONLY in `internal/screenshot/html.go` behind `//go:build screenshot` so it never enters `go build ./cmd/gitid`. Create a trivial fixture HTML page `.planning/design/_spike/fixture.html`. Implement `html.go` with a function that: configures the go-rod launcher to a PINNED Chromium revision (the exact string recorded in GOLDENS.md, NOT "always latest") AND a FIXED cache/user-data path; defines offline/failure behavior — if the pinned revision is not cached and cannot be provisioned, fail fast with a clear, actionable error naming the revision + cache path (never silently fall back to a different browser); launches headless with a FIXED viewport (e.g. 1280x800), device scale factor 1, and an emulated fixed color scheme (prefers-color-scheme pinned); navigates to a `file://` URL of the fixture under a go-rod context TIMEOUT (Codex: timeouts on browser tooling); waits for load; captures a full-page PNG; then runs the Task-2 metadata-strip + SHA-256. Give the target a RUNNABLE ENTRY POINT (Codex HIGH #3): create `internal/screenshot/html_capture_test.go` behind `//go:build screenshot` with `TestCaptureHTML` that calls the html.go capture function against the fixture, writes the PNG under `.planning/design/_spike/html/`, computes its SHA-256, and asserts it reproduces the golden recorded in GOLDENS.md; record that golden in GOLDENS.md. Add a `screenshot-html` Makefile target that invokes this driving test: `go test -tags screenshot -run TestCaptureHTML ./internal/screenshot/...` (the concrete runnable entry point — the make target now has something to invoke that actually produces the PNG). In `setup-env`, provision the PINNED Chromium revision (go-rod launcher download of the pinned revision into the fixed cache path). Run `go mod tidy` and confirm the shipped binary graph is unaffected. Add a unit/behavior check (in html_capture_test.go or a sibling screenshot-tagged test) that an EMPTY cache path with provisioning disabled yields the clear offline-failure error (proves the failure path).
  </action>
  <verify>
    <automated>make screenshot-html && test -s .planning/design/_spike/html/*.png && go test -tags screenshot ./internal/screenshot/... -run TestCaptureHTML</automated>
  </verify>
  <acceptance_criteria>
    - `make screenshot-html` exits 0 and produces a non-empty PNG under `.planning/design/_spike/html/` (`test -s` succeeds) AND re-running reproduces the SAME recorded SHA-256
    - `file .planning/design/_spike/html/*.png` reports "PNG image data"
    - The runnable capture entry point exists and exercises the ACTUAL capture path end-to-end (not just helpers): `grep -q "//go:build screenshot" internal/screenshot/html_capture_test.go` AND `grep -q "func TestCaptureHTML" internal/screenshot/html_capture_test.go`, and `make screenshot-html` invokes it (`grep -q "TestCaptureHTML" Makefile`)
    - `grep -q "//go:build screenshot" internal/screenshot/html.go` succeeds
    - `go list -deps ./cmd/gitid | grep -q go-rod/rod` returns NON-zero (go-rod absent from the shipped binary graph)
    - html.go pins an explicit Chromium revision + fixed cache path (not "latest"): `grep -qE "Revision|revision" internal/screenshot/html.go`, and sets a fixed viewport + device scale factor + color scheme + a context timeout
    - html.go has a tested offline/failure path returning a clear actionable error when the pinned revision cannot be provisioned
    - `make lint` passes
  </acceptance_criteria>
  <done>`make screenshot-html` renders a fixture page to a versioned PNG through a real runnable entry point (TestCaptureHTML under the screenshot build tag) via headless Chromium at a fixed viewport/scale/color-scheme with a PINNED revision + cache path, reproduces a recorded golden hash, has a tested offline-failure path, and keeps go-rod isolated from the shipped binary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev toolchain ← third-party installers (freeze, go-rod, Chromium) | new supply-chain surface added to the dev/CI environment (never the shipped binary) |
| headless Chromium ← local fixture HTML | fixture is a gitid-authored local `file://` page; no remote/untrusted content loaded in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | freeze@v0.2.2 + go-rod@v0.116.2 installs | mitigate | Task 1 AUTOMATED verification (Go modules are outside the npm/pip/cargo human-gate scope): pin exact versions (`@v0.2.2`, `@v0.116.2`), `go mod verify` against the Go checksum DB, RESEARCH slopcheck `[OK]` + isolated compile, documented pinned-version provenance review (Codex independently confirmed both). No NEW `@latest` for this plan's deps (the `@latest` check is scoped to go.mod / the freeze install line, not the pre-existing setup-env goimports/gosec installs); T-01-SC kept in the register |
| T-01-SC2 | Tampering | go-rod Chromium provisioning | mitigate | Pin an EXPLICIT Chromium revision + fixed cache path; defined fail-fast offline/failure behavior (never silently swap browsers); download path is Google's official snapshot CDN |
| T-01-13 | Elevation of Privilege | screenshot deps leaking into shipped binary | mitigate | `//go:build screenshot` isolation; acceptance asserts `go list -deps ./cmd/gitid` contains neither freeze nor go-rod |
| T-01-14 | Tampering | freeze `exec.Command` invocation from Go | mitigate | Arg-slice form only, `#nosec G204 -- fixed args` annotation; golden path is gitid-controlled temp file |
</threat_model>

## Artifacts this plan produces

- `Makefile` — `screenshot-tui`, `screenshot-html` targets (each invokes its driving `go test -tags screenshot -run TestCapture...` entry point); `setup-env` extended with `go install .../freeze@v0.2.2` + pinned-revision browser provisioning
- `internal/screenshot/tui.go` — `//go:build screenshot` View()-dump → freeze → PNG capture (fixed font/theme/geometry)
- `internal/screenshot/tui_capture_test.go` — `//go:build screenshot` `TestCaptureTUI` runnable capture entry point (writes the golden PNG; the concrete thing `make screenshot-tui` invokes)
- `internal/screenshot/html.go` — `//go:build screenshot` fixture → go-rod (pinned Chromium) → PNG capture (fixed viewport/scale/color-scheme, context timeout, offline-failure path)
- `internal/screenshot/html_capture_test.go` — `//go:build screenshot` `TestCaptureHTML` runnable capture entry point (writes the golden PNG; the concrete thing `make screenshot-html` invokes)
- `internal/screenshot/determinism.go` + `_test.go` — `//go:build screenshot` PNG metadata-strip + SHA-256 golden hashing
- `internal/screenshot/doc.go` — build-tag isolation rule doc
- `.planning/design/fonts/` — vendored monospace TTF + README (provenance/license)
- `.planning/design/_spike/fixture.html` — trivial HTML capture fixture
- `.planning/design/_spike/GOLDENS.md` — pinned-version provenance note (freeze@v0.2.2 + go-rod v0.116.2) + pinned Chromium revision + recorded golden hashes
- `.planning/design/<surface>/{tui,html}/*.png` — versioned reference PNG artifact layout (D-04)

<verification>
- `make screenshot-tui` and `make screenshot-html` each produce a non-empty PNG whose SHA-256 reproduces the recorded golden, via a real runnable entry point (TestCaptureTUI / TestCaptureHTML) — the actual capture path is exercised end-to-end, not just the determinism helpers
- `go build ./cmd/gitid` unaffected; `go list -deps` confirms neither dev dep enters the shipped graph
- `go mod verify` passes; this plan's go.mod introduces no `@latest`/`GONOSUMCHECK`; freeze pinned @v0.2.2 in the Makefile install
- `make lint` clean
</verification>

<success_criteria>
- TOOL-05 / DLV-03: repeatable, DETERMINISTIC TUI + HTML capture as make targets with concrete runnable entry points (driving screenshot-tagged tests), versioned PNG artifacts with recorded golden hashes.
- TOOL-02: `make setup-env` installs freeze (pinned @v0.2.2) + provisions the pinned browser revision.
- Screenshot deps isolated from the shipped binary (build tag); supply-chain verified automatically (no second human checkpoint; @latest check scoped to this plan's deps, not the pre-existing setup-env installs).
</success_criteria>

<output>
Create `.planning/phases/01-foundations-spikes-ci/01-05-SUMMARY.md` when done.
</output>
