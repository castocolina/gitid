---
phase: 01-foundations-spikes-ci
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/identity/state.go
  - internal/identity/state_test.go
autonomous: true
requirements: [MGR-02, DLV-07]
must_haves:
  truths:
    - "Each identity is classified into one of the 8 locked states from parsed managed blocks, with no sidecar DB"
    - "The classifier is a pure function over Reconstruct() output plus key-existence/usage facts"
    - "key-unused is computed by cross-referencing key files against every Host block's IdentityFile"
    - "An SSH-only identity classifies as incomplete/git-only correctly, never rendering nonexistent git state"
    - "All 8 states are proven by table-driven tests over fixture configs"
  artifacts:
    - path: "internal/identity/state.go"
      provides: "State enum (8 values) + ClassifyState pure function + crossReferenceUnusedKeys helper"
      contains: "func ClassifyState"
  key_links:
    - from: "internal/identity/state.go ClassifyState"
      to: "identity.Account (Reconstruct output) + key-existence facts"
      via: "pure function, no filesystem write, no sidecar DB"
      pattern: "func ClassifyState"
---

<objective>
Add the MGR-02 identity state-taxonomy core to `internal/identity`: a pure `ClassifyState` function that maps a reconstructed `Account` (plus key-existence and key-usage facts) to exactly one of the 8 locked states — complete / incomplete / git-only / key-unused / key-used-ssh-only / key-used-both / key-missing / fragment-path-missing. Computed entirely from parsed managed blocks with no sidecar DB (DLV-07 UI-free TDD core). Include a small `crossReferenceUnusedKeys` helper (the canonical "key-unused" computation) so later consumers share one implementation.

Purpose: The identity manager (Phase 5, MGR-01/03/07) renders per-row state and the debug/list command (01-06) surfaces it in Phase 1. This plan builds the classifier and proves all 8 states table-driven; it does NOT modify the doctor (that dedup is a Phase 8 concern — avoids a Wave-1 file conflict with 01-03's orphans.go change).
Output: `internal/identity/state.go` + `state_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-spikes-ci/01-CONTEXT.md
@.planning/phases/01-foundations-spikes-ci/01-RESEARCH.md
@.planning/phases/01-foundations-spikes-ci/01-PATTERNS.md
@internal/identity/loader.go
@internal/identity/identity.go
@internal/identity/modes.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State enum + key cross-reference/existence helpers (MGR-02 inputs, interface-first)</name>
  <files>internal/identity/state.go, internal/identity/state_test.go</files>
  <read_first>
    - internal/identity/identity.go (Account struct lines 26-51 — Name, Alias, KeyPath, FragmentPath, Incomplete fields the classifier reads)
    - internal/identity/loader.go (Reconstruct lines 23-91 — the input shape; how Incomplete/missing is computed today)
    - internal/doctor/checks/orphans.go (Class 3 unused-key cross-reference lines 116-134 — the logic to mirror as a pure helper here; do NOT modify orphans.go in this plan)
    - .planning/phases/01-foundations-spikes-ci/01-RESEARCH.md § "Open Question 2" (shared helper lives in internal/identity), § phase_requirements MGR-02 row
    - .planning/phases/01-foundations-spikes-ci/01-PATTERNS.md § "internal/identity/state.go"
  </read_first>
  <behavior>
    - A State type (named constants) enumerates exactly the 8 locked states
    - crossReferenceUnusedKeys(keyPaths, referencedIdentityFiles) returns the key paths present on disk but referenced by no Host block
    - keyExists/keyUsedInSSH/keyUsedInGit facts are derived as plain booleans from injected inputs (no direct filesystem calls inside the pure classifier)
  </behavior>
  <action>
    Write RED tests first (state_test.go with compile-clean `_`-param stubs). Define `type State string` (or int with a String()) with the 8 named constants matching the MGR-02 wording exactly. Implement `crossReferenceUnusedKeys(keyPaths []string, referencedIdentityFiles []string) []string` as a pure function mirroring the orphans.go Class 3 set-difference logic (key on disk, referenced by no Host block). Keep key-existence a caller-provided fact: the pure classifier in Task 2 takes booleans, and a thin `StatOf(paths, stat func(string)(os.FileInfo,error))` style helper (or the caller) resolves existence — do not stat inside the pure path. Use the `identity: <action>: %w` error prefix where errors are possible.
  </action>
  <verify>
    <automated>go test ./internal/identity/... -run 'TestCrossReferenceUnusedKeys|TestStateConstants' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/identity/... -run 'TestCrossReferenceUnusedKeys|TestStateConstants' -race` exits 0
    - state.go declares exactly 8 State constants (test asserts the set)
    - `grep -q "func crossReferenceUnusedKeys" internal/identity/state.go` succeeds
    - state.go does NOT modify or import doctor (`! grep -q "internal/doctor" internal/identity/state.go`)
    - `make lint` passes
  </acceptance_criteria>
  <done>The 8-state enum and the pure key cross-reference/existence helpers exist and are green, with no filesystem writes and no doctor coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClassifyState 8-state mapping, table-driven over fixtures (MGR-02, DLV-07)</name>
  <files>internal/identity/state.go, internal/identity/state_test.go</files>
  <read_first>
    - internal/identity/loader.go (how Account.Incomplete encodes missing "ssh-host-block"/"gitconfig"/fragment pieces — precursor signal for incomplete/git-only/fragment-path-missing)
    - internal/identity/modes_test.go / internal/identity/loader_test.go (table-driven test convention to mirror; Go stdlib testing, no third-party framework)
    - recipes/ssh-config.recipe + recipes/gitconfig.recipe (canonical complete-identity shape the "complete" state represents)
  </read_first>
  <behavior>
    - complete: Host block + gitconfig fragment both present and key resolves
    - incomplete: SSH present, no git config
    - git-only: git identity relying on global SSH, no own Host block
    - key-unused: key file exists, no identity references it
    - key-used-ssh-only: key referenced by a Host block but not wired for signing
    - key-used-both: key wired for auth AND signing
    - key-missing: identity references a key file that is absent
    - fragment-path-missing: an includeIf points at a non-existent fragment
    - Each of the 8 states is exercised by at least one table row over a fixture Account/keys input
  </behavior>
  <action>
    Write the RED table test first enumerating one row per state (compile-clean). Implement `ClassifyState(acct Account, keyExists, keyUsedInSSH, keyUsedInGit bool) State` as a pure decision function consuming the Reconstruct-shaped Account plus the injected booleans (from Task 1 helpers). Derive incomplete/git-only/fragment-path-missing from `acct.Incomplete`/`acct.Alias`/`acct.FragmentPath` (never render git attributes for an SSH-only identity). Map key states from the keyExists/keyUsedInSSH/keyUsedInGit facts and the cross-reference helper. Return exactly one State per input. Keep it a pure function — no filesystem, no sidecar DB (DLV-07). Add a doc comment tying each branch to the MGR-02 wording.
  </action>
  <verify>
    <automated>go test ./internal/identity/... -run 'TestClassifyState' -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/identity/... -run 'TestClassifyState' -race` exits 0
    - The table test has ≥1 row asserting each of the 8 states (test names or table cases cover all 8)
    - `grep -q "func ClassifyState" internal/identity/state.go` succeeds
    - ClassifyState performs no filesystem access (facts are arguments): `! grep -q "os.Stat\\|os.ReadFile\\|os.Open" internal/identity/state.go`
    - `make lint` passes
  </acceptance_criteria>
  <done>ClassifyState maps every input to exactly one of the 8 states, proven by a table-driven test covering all 8, as a pure UI-free-core function with no sidecar DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed managed blocks → state classification | untrusted config content is parsed upstream (sshconfig/gitconfig); this plan only reads the already-parsed Account shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | ClassifyState inputs | accept | Read-only pure function; no writes, no exec, no sidecar DB — a malformed Account yields a state label, never a mutation. Low risk for a local single-user tool |
| T-01-12 | Information Disclosure | state fixtures | mitigate | Fixtures use synthetic identity names/paths under `t.TempDir()`; no real key material is embedded in test data |
</threat_model>

## Artifacts this plan produces

- `internal/identity/state.go` — `type State`, the 8 State constants, `ClassifyState(acct Account, keyExists, keyUsedInSSH, keyUsedInGit bool) State`, `crossReferenceUnusedKeys(keyPaths, referencedIdentityFiles []string) []string`

<verification>
- `go test ./internal/identity/... -race` green, all 8 states covered
- `make lint` clean
- No filesystem writes / no sidecar DB / no doctor import (DLV-07 + Wave-1 conflict avoidance)
</verification>

<success_criteria>
- MGR-02: 8-state taxonomy computed by the UI-free TDD core from parsed managed blocks, no sidecar DB, all states table-proven.
- DLV-07: pure UI-free core; classifier is a pure function.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-spikes-ci/01-04-SUMMARY.md` when done.
</output>
