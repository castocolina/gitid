---
phase: 01-foundations-spikes-ci
plan: 06
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-04]
files_modified:
  - cmd/gitid/debug.go
  - cmd/gitid/debug_test.go
  - cmd/gitid/main.go
  - e2e/debug_e2e_test.go
autonomous: true
requirements: [KEY-01, PLAT-01, MGR-02, DLV-07]
must_haves:
  truths:
    - "A debug/list command prints the 5-algorithm catalog with resolved local availability (generatable = implemented AND available)"
    - "Catalog availability resolves from the RAW `ssh -Q key` protocol tokens (caps.KeyTypes), NOT the already-mapped catalog names (caps.Algorithms), so entries whose token the local toolchain supports show Available=true"
    - "The command prints the local capability probe results (structured ssh version, SSL flavor, agent/FIDO/keychain statuses)"
    - "The command prints each identity's IdentityHealth (both axes + problems) by consuming identity.BuildInventory — it does NOT re-derive identity/key state in cmd/"
    - "The command NEVER leaks secrets: no PEM PRIVATE KEY markers, no passphrase fields/values, no raw private-key contents, no full-environment dump"
    - "The command's real dependency wiring (platform.BuildProbeDeps + identity.BuildInventoryDeps) is exercised by a PTY/e2e test, not only fakes"
  artifacts:
    - path: "cmd/gitid/debug.go"
      provides: "Cobra debug command wiring platform probe + keygen catalog + identity.BuildInventory, thin glue"
      contains: "newDebugCmd"
    - path: "e2e/debug_e2e_test.go"
      provides: "PTY/e2e test driving the real built binary's debug command (injected-seam closure)"
      contains: "//go:build e2e"
  key_links:
    - from: "cmd/gitid/debug.go"
      to: "platform.Probe(platform.BuildProbeDeps()) + keygen.ResolveAvailability + identity.BuildInventory"
      via: "thin glue: build the EXPORTED real probe deps (platform.BuildProbeDeps), gather probe results, resolve catalog availability from caps.KeyTypes (raw ssh -Q key tokens) + caps.FIDO.Usable(), build the identity inventory, print"
      pattern: "BuildInventory"
    - from: "cmd/gitid/debug.go keygen.ResolveAvailability"
      to: "caps.KeyTypes (raw ssh -Q key protocol tokens)"
      via: "second argument is caps.KeyTypes, NOT caps.Algorithms (mapped names) — the catalog QueryToken matches raw tokens"
      pattern: "ResolveAvailability\\([^,]*, ?caps\\.KeyTypes"
    - from: "e2e/debug_e2e_test.go"
      to: "real built gitid binary"
      via: "runs the debug subcommand end-to-end (real wiring, not stubs)"
      pattern: "debug"
---

<objective>
Add the D-08 debug/list command to `cmd/gitid` — the Phase-1 surface that proves KEY-01 (catalog + per-OS availability), PLAT-01 (local capability probe), and MGR-02 (per-identity state) via a real, test-exercised command. It is thin glue: it gathers capabilities via `platform.Probe(ctx, platform.BuildProbeDeps())` (the EXPORTED probe-deps constructor from 01-01 — capital B — the real probe wiring) plus `platform.ProbeKeyTypes`/`ProbeSSHVersion`, resolves catalog availability with `keygen.ResolveAvailability(cat, caps.KeyTypes, caps.FIDO.Usable())` — binding the second argument to `caps.KeyTypes` (the RAW `ssh -Q key` protocol tokens the catalog's QueryToken matches against), NOT `caps.Algorithms` (already-mapped catalog names) — builds the identity inventory with `identity.BuildInventory(identity.BuildInventoryDeps())` (Codex HIGH #4 — the command consumes the real aggregation layer instead of re-deriving state in cmd/), and prints. It MUST NEVER leak secrets. An e2e test drives the REAL built binary (closing the recurring injected-seam wiring blindspot — the real `platform.BuildProbeDeps()` + `identity.BuildInventoryDeps()` closures are exercised, not only fakes).

Purpose: This is Phase 1 Success Criterion 2's "surfaced by a debug/list command and proven by tests" and D-11's Phase-1 state-taxonomy readout. No product UI — a plain CLI readout only.
Output: `cmd/gitid/debug.go` (+ unit test), a registration in `main.go`, and `e2e/debug_e2e_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-spikes-ci/01-CONTEXT.md
@.planning/phases/01-foundations-spikes-ci/01-RESEARCH.md
@.planning/phases/01-foundations-spikes-ci/01-PATTERNS.md
@cmd/gitid/doctor.go
@e2e/harness_test.go
@.planning/phases/01-foundations-spikes-ci/01-01-SUMMARY.md
@.planning/phases/01-foundations-spikes-ci/01-02-SUMMARY.md
@.planning/phases/01-foundations-spikes-ci/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `gitid debug caps` command wiring catalog + probe + identity inventory (KEY-01, PLAT-01, MGR-02)</name>
  <files>cmd/gitid/debug.go, cmd/gitid/debug_test.go, cmd/gitid/main.go</files>
  <read_first>
    - cmd/gitid/doctor.go (newDoctorCmd construction + import set lines 1-40 — the thin-glue Cobra pattern to mirror)
    - cmd/gitid/main.go (how subcommands are registered on the root command)
    - internal/keygen/catalog.go (Catalog + ResolveAvailability + Generatable — from 01-02; ResolveAvailability's second arg is the RAW supportedTokens slice its QueryToken matches against)
    - internal/platform/capabilities.go + keytypes.go + version.go (Probe → Capabilities from 01-01: AgentStatus/FIDOStatus/KeychainStatus, FIDO.Usable(); the EXPORTED `BuildProbeDeps()` constructor supplies Probe's Deps; KeyTypes = the RAW `ssh -Q key` protocol tokens like `sk-ssh-ed25519@openssh.com` — feed THESE to ResolveAvailability — vs Algorithms = already-mapped catalog names, which must NOT be passed to ResolveAvailability; SupportedAlgorithms, ProbeSSHVersion → SSHVersion struct)
    - internal/identity/inventory.go (BuildInventory + BuildInventoryDeps — from 01-04; the aggregation layer to consume) + internal/identity/state.go (IdentityHealth)
    - .planning/phases/01-foundations-spikes-ci/01-PATTERNS.md § "cmd/gitid/debug.go" (Security note: never print Material/PrivPEM)
    - .planning/phases/01-foundations-spikes-ci/01-RESEARCH.md § Security Domain (private-key-material-in-logs mitigation)
  </read_first>
  <action>
    Create `debug.go` with `newDebugCmd()` returning a thin `*cobra.Command` (Use "debug", with a "caps" subcommand or a single command that prints all three sections). Its RunE: (1) gathers local capabilities via `platform.Probe(ctx, platform.BuildProbeDeps())` (using the EXPORTED `BuildProbeDeps` constructor from 01-01 — capital B — the real probe wiring) plus `platform.ProbeKeyTypes`/`ProbeSSHVersion`, and prints the structured SSHVersion fields + agent/FIDO/keychain STATUS strings (not bools); (2) calls `keygen.Catalog()` then `keygen.ResolveAvailability(cat, caps.KeyTypes, caps.FIDO.Usable())` and prints each algorithm with its Implemented/Available/Generatable/default flags + security + per-OS note — bind the second argument to `caps.KeyTypes` (the RAW `ssh -Q key` protocol tokens like `sk-ssh-ed25519@openssh.com` that the catalog's `QueryToken` matches against), NOT `caps.Algorithms` (already-mapped catalog names); passing `caps.Algorithms` would silently resolve EVERY entry Available=false; (3) builds the identity inventory via `identity.BuildInventory(identity.BuildInventoryDeps())` and prints each entry's IdentityHealth (IdentityState axis, KeyState axis, Problems) plus the UnusedKeys list — DO NOT re-derive state in cmd/ (consume the builder). All logic stays in the internal packages — the command only gathers + prints. Register the command in main.go alongside the existing subcommands. The command MUST NEVER reference `keygen.Material`/`PrivPEM`, print passphrase fields, dump raw private-key file contents, or dump the full environment (`os.Environ()`) — print catalog metadata + public/probe/state info only. Write a unit test (debug_test.go) asserting the command runs and its output contains a catalog section, a capabilities section, and an identity-state section, and asserts ABSENCE of: any "PRIVATE KEY" PEM marker, the substrings "passphrase"/"Passphrase"/"PrivPEM", and any full-environment dump.
  </action>
  <verify>
    <automated>go test ./cmd/gitid/... -run 'TestDebug' -race && go build ./cmd/gitid</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/gitid/... -run 'TestDebug' -race` exits 0 and `go build ./cmd/gitid` succeeds
    - `grep -q "func newDebugCmd" cmd/gitid/debug.go` succeeds
    - The command is registered in main.go (`grep -q "newDebugCmd" cmd/gitid/main.go`)
    - The command wires the EXPORTED real probe-deps constructor from 01-01 (capital B — cross-package): `grep -q "platform.BuildProbeDeps" cmd/gitid/debug.go` succeeds
    - The command feeds the RAW `ssh -Q key` tokens to the resolver: `grep -qE "ResolveAvailability\\([^,]*, ?caps\\.KeyTypes" cmd/gitid/debug.go` succeeds (and it does NOT pass the mapped `caps.Algorithms` to ResolveAvailability — that would resolve every entry Available=false)
    - The command consumes the inventory builder (`grep -q "BuildInventory" cmd/gitid/debug.go`) and does NOT re-implement classification in cmd/ (`! grep -q "ClassifyState\\|func .*Classify" cmd/gitid/debug.go` — it may call, not define)
    - `! grep -q "PrivPEM\\|Material{\\|os.Environ" cmd/gitid/debug.go` (no private-key material / env dump)
    - debug_test.go asserts output contains catalog + capabilities + per-identity IdentityHealth, and asserts absence of "PRIVATE KEY", "passphrase"/"Passphrase"/"PrivPEM", and a full-environment dump
    - `make lint` passes
  </acceptance_criteria>
  <done>`gitid debug` prints the algorithm catalog with resolved availability (from the raw caps.KeyTypes tokens), the structured capability probe (via the EXPORTED platform.BuildProbeDeps wiring), and per-identity IdentityHealth by consuming BuildInventory, wired as thin glue with no secret leakage.</done>
</task>

<task type="auto">
  <name>Task 2: PTY/e2e test driving the REAL binary's debug command (injected-seam closure, DLV-07)</name>
  <files>e2e/debug_e2e_test.go</files>
  <read_first>
    - e2e/harness_test.go (buildOnce/packageBin, SandboxHome, hermetic HOME + fake-ssh-on-PATH pattern; //go:build e2e tag)
    - e2e/ui_pty_e2e_test.go (existing PTY/e2e driving of the real binary — the closest analog for raw invocation)
    - cmd/gitid/doctor_realwiring_test.go (the existing "real wiring, not stubs" pattern this project uses to close the injected-seam blindspot)
    - MEMORY note: "Doctor injected-seam wiring blindspot" — the real build*Deps() (now including identity.BuildInventoryDeps + platform.BuildProbeDeps — EXPORTED, capital B) must be exercised, not only fakes
  </read_first>
  <action>
    Add `e2e/debug_e2e_test.go` (`//go:build e2e`) using the harness (SandboxHome + built packageBin) to run the REAL `gitid debug` subcommand end-to-end and assert on its real stdout: a catalog section listing the 5 algorithms with resolved availability (proving the raw-token wiring — e.g. the ed25519 entry, whose `ssh-ed25519` token the sandbox's `ssh -Q key`/injected fake-ssh lists, shows Available=true), a capabilities section reflecting the sandbox's real `ssh -Q key`/`ssh -V` (or the harness's injected fake-ssh on PATH), and per-identity IdentityHealth for a seeded fixture identity. Assert the output NEVER contains a PEM "PRIVATE KEY" marker, the substrings "passphrase"/"Passphrase"/"PrivPEM", or a full-environment dump. This exercises the real dependency wiring (platform.BuildProbeDeps + identity.BuildInventoryDeps), closing the recurring injected-seam blindspot for this new command surface.
  </action>
  <verify>
    <automated>make test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `make test-e2e` exits 0 (builds the real binary and runs the e2e suite including the debug test)
    - `grep -q "//go:build e2e" e2e/debug_e2e_test.go` succeeds
    - The e2e test runs the real built binary's `debug` subcommand (not an in-process stub) and asserts the three output sections, including at least one catalog entry resolving Available=true from the sandbox's `ssh -Q key` tokens (proves caps.KeyTypes feeds ResolveAvailability end-to-end)
    - The e2e test asserts the output does NOT contain "PRIVATE KEY", "passphrase"/"Passphrase"/"PrivPEM", or a full-environment dump
    - `make lint` passes
  </acceptance_criteria>
  <done>An e2e test drives the real built binary's debug command end-to-end, proving the real wiring (platform.BuildProbeDeps probe + identity.BuildInventoryDeps inventory) and the raw-token availability resolution, and confirming no secret leakage in the output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug command → stdout/logs | catalog + probe + state readout printed to the terminal; must not leak secrets |
| debug command → internal packages | thin glue calling read-only probe/classify/inventory functions; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Information Disclosure | debug command output | mitigate | The command references only catalog metadata + public/probe/state info; it never touches `keygen.Material`/`PrivPEM`, never prints passphrase fields, never dumps raw private-key contents or the environment; both a unit test and the e2e test assert absence of "PRIVATE KEY", "passphrase"/"PrivPEM", and a full-env dump |
| T-01-16 | Spoofing | injected-seam blindspot (fake vs real deps) | mitigate | The e2e test exercises the REAL build*Deps() wiring (platform.BuildProbeDeps probe + identity.BuildInventoryDeps inventory) on the built binary (mirrors doctor_realwiring_test), so a broken real seam cannot pass on green fakes |
| T-01-17 | Tampering | probe exec calls reached via the command | mitigate | Inherited from 01-01: all probes are arg-slice `exec.CommandContext` with no shell and bounded timeouts; the command adds no new exec surface |
</threat_model>

## Artifacts this plan produces

- `cmd/gitid/debug.go` — `newDebugCmd() *cobra.Command` (catalog + structured capabilities + per-identity IdentityHealth via BuildInventory; probe wired via the EXPORTED platform.BuildProbeDeps; ResolveAvailability fed from caps.KeyTypes)
- `cmd/gitid/main.go` — debug command registered on the root command
- `e2e/debug_e2e_test.go` — `//go:build e2e` real-binary PTY/e2e test (injected-seam closure)

<verification>
- `go test ./cmd/gitid/... -race` + `make test-e2e` green
- No secret leakage in command output (asserted twice: PEM marker, passphrase/PrivPEM, env dump)
- `make lint` clean
</verification>

<success_criteria>
- KEY-01 / PLAT-01 / MGR-02: catalog + availability (resolved from the raw caps.KeyTypes tokens) + structured capability probe + per-identity IdentityHealth all surfaced by a debug/list command that consumes the real inventory builder, proven by tests.
- DLV-07: the command is thin glue over the UI-free core; real wiring (platform.BuildProbeDeps + identity.BuildInventoryDeps) is e2e-exercised.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-spikes-ci/01-06-SUMMARY.md` when done.
</output>
