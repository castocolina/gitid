---
phase: 01-foundations-spikes-ci
plan: 06
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-04]
files_modified:
  - cmd/gitid/debug.go
  - cmd/gitid/debug_test.go
  - cmd/gitid/main.go
  - e2e/debug_e2e_test.go
autonomous: true
requirements: [KEY-01, PLAT-01, MGR-02, DLV-07]
must_haves:
  truths:
    - "A debug/list command prints the 5-algorithm catalog with resolved local availability"
    - "The command prints the local capability probe results (ssh version, SSL flavor, libfido2/agent/keychain)"
    - "The command prints each identity's computed state from the 8-state taxonomy"
    - "The command NEVER prints private key material (no PrivPEM / Material)"
    - "The command's real dependency wiring (build*Deps) is exercised by a PTY/e2e test, not only fakes"
  artifacts:
    - path: "cmd/gitid/debug.go"
      provides: "Cobra debug command wiring platform probe + keygen catalog + identity state, thin glue"
      contains: "newDebugCmd"
    - path: "e2e/debug_e2e_test.go"
      provides: "PTY/e2e test driving the real built binary's debug command (injected-seam closure)"
      contains: "//go:build e2e"
  key_links:
    - from: "cmd/gitid/debug.go"
      to: "platform.Probe + keygen.ResolveAvailability + identity.ClassifyState"
      via: "thin glue: gather probe results, resolve catalog availability, classify state, print"
      pattern: "ResolveAvailability"
    - from: "e2e/debug_e2e_test.go"
      to: "real built gitid binary"
      via: "runs the debug subcommand end-to-end (real wiring, not stubs)"
      pattern: "debug"
---

<objective>
Add the D-08 debug/list command to `cmd/gitid` — the Phase-1 surface that proves KEY-01 (catalog + per-OS availability), PLAT-01 (local capability probe), and MGR-02 (per-identity state) via a real, test-exercised command. It is thin glue: it calls `platform.Probe`/`ProbeKeyTypes`/`ProbeSSHVersion`, resolves catalog availability with `keygen.ResolveAvailability`, classifies each reconstructed identity with `identity.ClassifyState`, and prints. It MUST NEVER print private key material. An e2e test drives the REAL built binary (closing the recurring injected-seam wiring blindspot — the real `build*Deps()` closure is exercised, not only fakes).

Purpose: This is Phase 1 Success Criterion 2's "surfaced by a debug/list command and proven by tests" and D-11's Phase-1 state-taxonomy readout. No product UI — a plain CLI readout only.
Output: `cmd/gitid/debug.go` (+ unit test), a registration in `main.go`, and `e2e/debug_e2e_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-spikes-ci/01-CONTEXT.md
@.planning/phases/01-foundations-spikes-ci/01-RESEARCH.md
@.planning/phases/01-foundations-spikes-ci/01-PATTERNS.md
@cmd/gitid/doctor.go
@e2e/harness_test.go
@.planning/phases/01-foundations-spikes-ci/01-01-SUMMARY.md
@.planning/phases/01-foundations-spikes-ci/01-02-SUMMARY.md
@.planning/phases/01-foundations-spikes-ci/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `gitid debug caps` command wiring catalog + probe + state (KEY-01, PLAT-01, MGR-02)</name>
  <files>cmd/gitid/debug.go, cmd/gitid/debug_test.go, cmd/gitid/main.go</files>
  <read_first>
    - cmd/gitid/doctor.go (newDoctorCmd construction + import set lines 1-40 — the thin-glue Cobra pattern to mirror)
    - cmd/gitid/main.go (how subcommands are registered on the root command)
    - internal/keygen/catalog.go (Catalog + ResolveAvailability signatures — from 01-02)
    - internal/platform/capabilities.go + keytypes.go + version.go (Probe, SupportedAlgorithms, ProbeSSHVersion — from 01-01)
    - internal/identity/state.go (ClassifyState — from 01-04) + internal/identity/loader.go (Reconstruct)
    - .planning/phases/01-foundations-spikes-ci/01-PATTERNS.md § "cmd/gitid/debug.go" (Security note: never print Material/PrivPEM)
    - .planning/phases/01-foundations-spikes-ci/01-RESEARCH.md § Security Domain (private-key-material-in-logs mitigation)
  </read_first>
  <action>
    Create `debug.go` with `newDebugCmd()` returning a thin `*cobra.Command` (Use "debug", with a "caps" subcommand or a single command that prints all three sections). Its RunE: (1) probes local capabilities via `platform.ProbeKeyTypes`/`ProbeSSHVersion`/`Probe`, (2) calls `keygen.Catalog()` then `keygen.ResolveAvailability(cat, platform.SupportedAlgorithmTokens, caps.HasLibfido2)` and prints each algorithm with its Implemented/Available/default flags + security + per-OS note, (3) reconstructs identities and prints each one's `identity.ClassifyState(...)` result. All logic stays in the internal packages — the command only gathers + prints. Register the command in main.go alongside the existing subcommands. The command MUST NEVER reference `keygen.Material`/`PrivPEM` — print catalog metadata + public/probe info only. Write a unit test (debug_test.go) asserting the command runs and its output contains a catalog section, a capabilities section, and a state section, and does NOT contain any "PRIVATE KEY" / PEM marker.
  </action>
  <verify>
    <automated>go test ./cmd/gitid/... -run 'TestDebug' -race && go build ./cmd/gitid</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/gitid/... -run 'TestDebug' -race` exits 0 and `go build ./cmd/gitid` succeeds
    - `grep -q "func newDebugCmd" cmd/gitid/debug.go` succeeds
    - The command is registered in main.go (`grep -q "newDebugCmd" cmd/gitid/main.go`)
    - `! grep -q "PrivPEM\\|Material{" cmd/gitid/debug.go` (no private-key material referenced)
    - debug_test.go asserts output contains catalog + capabilities + per-identity state, and asserts absence of "PRIVATE KEY"
    - `make lint` passes
  </acceptance_criteria>
  <done>`gitid debug` prints the algorithm catalog with resolved availability, the local capability probe, and per-identity state, wired as thin glue with no private-key material printed.</done>
</task>

<task type="auto">
  <name>Task 2: PTY/e2e test driving the REAL binary's debug command (injected-seam closure, DLV-07)</name>
  <files>e2e/debug_e2e_test.go</files>
  <read_first>
    - e2e/harness_test.go (buildOnce/packageBin, SandboxHome, hermetic HOME + fake-ssh-on-PATH pattern; //go:build e2e tag)
    - e2e/ui_pty_e2e_test.go (existing PTY/e2e driving of the real binary — the closest analog for raw invocation)
    - cmd/gitid/doctor_realwiring_test.go (the existing "real wiring, not stubs" pattern this project uses to close the injected-seam blindspot)
    - MEMORY note: "Doctor injected-seam wiring blindspot" — the real build*Deps() must be exercised, not only fakes
  </read_first>
  <action>
    Add `e2e/debug_e2e_test.go` (`//go:build e2e`) using the harness (SandboxHome + built packageBin) to run the REAL `gitid debug` subcommand end-to-end and assert on its real stdout: a catalog section listing the 5 algorithms, a capabilities section reflecting the sandbox's real `ssh -Q key`/`ssh -V` (or the harness's injected fake-ssh on PATH), and per-identity state for a seeded fixture identity. Assert the output NEVER contains a PEM "PRIVATE KEY" marker. This exercises the real dependency wiring (not injected fakes), closing the recurring injected-seam blindspot for this new command surface.
  </action>
  <verify>
    <automated>make test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `make test-e2e` exits 0 (builds the real binary and runs the e2e suite including the debug test)
    - `grep -q "//go:build e2e" e2e/debug_e2e_test.go` succeeds
    - The e2e test runs the real built binary's `debug` subcommand (not an in-process stub) and asserts the three output sections
    - The e2e test asserts the output does NOT contain "PRIVATE KEY"
    - `make lint` passes
  </acceptance_criteria>
  <done>An e2e test drives the real built binary's debug command end-to-end, proving the real wiring and confirming no private-key material is ever printed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug command → stdout/logs | catalog + probe + state readout printed to the terminal; must not leak secrets |
| debug command → internal packages | thin glue calling read-only probe/classify functions; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Information Disclosure | debug command output | mitigate | The command references only catalog metadata + public/probe info; it never touches `keygen.Material`/`PrivPEM`; both a unit test and the e2e test assert the absence of a "PRIVATE KEY" marker in output |
| T-01-16 | Spoofing | injected-seam blindspot (fake vs real deps) | mitigate | The e2e test exercises the REAL build*Deps() wiring on the built binary (mirrors doctor_realwiring_test), so a broken real seam cannot pass on green fakes |
| T-01-17 | Tampering | probe exec calls reached via the command | mitigate | Inherited from 01-01: all probes are arg-slice exec.Command with no shell; the command adds no new exec surface |
</threat_model>

## Artifacts this plan produces

- `cmd/gitid/debug.go` — `newDebugCmd() *cobra.Command` (catalog + capabilities + per-identity state readout)
- `cmd/gitid/main.go` — debug command registered on the root command
- `e2e/debug_e2e_test.go` — `//go:build e2e` real-binary PTY/e2e test (injected-seam closure)

<verification>
- `go test ./cmd/gitid/... -race` + `make test-e2e` green
- No private-key material in command output (asserted twice)
- `make lint` clean
</verification>

<success_criteria>
- KEY-01 / PLAT-01 / MGR-02: catalog + availability + capability probe + per-identity state all surfaced by a debug/list command and proven by tests.
- DLV-07: the command is thin glue over the UI-free core; real wiring is e2e-exercised.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-spikes-ci/01-06-SUMMARY.md` when done.
</output>
