---
phase: 03.1-baseline-global-git-config-global-gitignore
verified: 2026-06-11T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 03.1: Baseline Global Git Config + Global Gitignore Verification Report

**Phase Goal:** gitid manages a coherent global baseline — a shared git-config layer
(core/push/pull/fetch/color toggles + curated aliases, core.ignorecase=false) and a
curated global gitignore wired via core.excludesfile — all in idempotent managed blocks
written with the same backup → preview → confirm safety as identity artifacts, plus
optional HTTPS→SSH insteadOf rewrites; the baseline is viewable and re-derivable from
disk (no sidecar DB).

**Verified:** 2026-06-11T00:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC-1: baseline block writes core.ignorecase=false, push.autoSetupRemote=true, pull.rebase=true, fetch.prune=true, color.ui=auto + alias set; idempotent second run; foreign content preserved | VERIFIED | `RenderBaselineBlock` hardcodes all Tier-1 keys; `WriteBaselineFile` skips write on byte-equal content; `TestWriteBaselineFile_Idempotent` and `TestWriteBaselineFile_PreservesForeign` pass; `TestBaselineSetup_Idempotency` passes end-to-end with zero diff on second run |
| 2 | SC-2: core.excludesfile points to ~/.gitignore_global with .DS_Store, Thumbs.db, *.log, *.bak, *.tmp, *.swp; re-running idempotent | VERIFIED | `DefaultGitignorePatterns()` contains all 6 SC-2-locked patterns in fixed order; `WriteGlobalGitignore` skips write on byte-equal; `TestWriteGlobalGitignore_Idempotent` passes; `core.excludesfile = ~/.gitignore_global` hardcoded in renderer (line 424) |
| 3 | SC-3: full baseline previewed before write + single confirm; timestamped backup of every mutated file before any change | VERIFIED | `printBaselinePreview` called before confirm gate; `confirm(reader, out, "Write baseline now?")` gates all writes; `filewriter.Write` creates `.bak.<timestamp>` backup before any mutation (filewriter.go:36-37); `--dry-run` short-circuits before confirm |
| 4 | SC-4: HTTPS→SSH insteadOf rewrites offered, shown + editable/deselectable before write (URLRW-01) | VERIFIED | Three per-host `Keep rewrite for <host>? [Y/n]:` prompts in `runBaselineSetup` (lines 97-106); blast-radius warning emitted in preview; empty kept-slice causes `WriteBaselineFile` to call `RemoveBlock("url-rewrites")` (D-07); `TestBaselineSetup_DryRun` asserts blast-radius warning in output |
| 5 | SC-5: gitid reads back + displays managed baseline from disk; no sidecar DB | VERIFIED | `ReadBaselineState` uses `filewriter.ListBlocks` across three files; no `git config --global` or `--includes` calls; installed/not-installed/incomplete states handled; `TestReadBaselineState` and `TestBaselineShow_*` all pass |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/filewriter/block.go` | `PrependBlockIfNotFound` pure transform | VERIFIED | Function exists, documented, delegates to `ReplaceBlock` on exists branch, prepends on first write; WR-07 fix: no redundant empty-input branch |
| `internal/filewriter/block_prepend_test.go` | RED→GREEN tests for top-placement + in-place update | VERIFIED | `TestPrependBlockIfNotFound` and `TestPrependBlockIfNotFound_UpdateInPlace` both pass; uses `BeginPrefix`/`EndPrefix` constants |
| `internal/gitconfig/baseline.go` | All renderers, writers, read-side helpers | VERIFIED | `RenderBaselineBlock(cfg) (string,error)`, `RenderURLRewritesBlock(rewrites) (string,error)`, `RenderGitignoreBlock(patterns) string`, `WriteBaselineFile`, `WriteBaselineInclude`, `WriteGlobalGitignore`, `ScanConflicts`, `ReadBaselineState`, `RemoveURLRewritesBlock`, `BaselineKeySet` all present and substantive |
| `internal/gitconfig/baseline_test.go` | Render-stability, idempotency, foreign-preservation, conflict, read-back tests | VERIFIED | 20+ named tests pass covering all VALIDATION.md test map names |
| `cmd/gitid/baseline.go` | `gitid baseline setup/show` commands + orchestration | VERIFIED | `newBaselineSetupCmd`, `newBaselineShowCmd`, `runBaselineSetup`, `runBaselineShow` all present and wired |
| `cmd/gitid/baseline_test.go` | Setup --dry-run + show command tests | VERIFIED | `TestBaselineSetup_DryRun`, `TestBaselineShow_Installed`, `TestBaselineShow_NotInstalled`, `TestBaselineShow_Incomplete`, `TestBaselineSetup_Idempotency` all pass |
| `cmd/gitid/main.go` | baseline subcommand group registered on root | VERIFIED | `root.AddCommand(baseline)` present; `baseline.AddCommand(newBaselineSetupCmd())` and `baseline.AddCommand(newBaselineShowCmd())` wired |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `internal/filewriter/block.go PrependBlockIfNotFound` | `ReplaceBlock` | delegates on exists branch | WIRED | `return ReplaceBlock(existing, name, blockBody)` at line 150 |
| `internal/gitconfig/baseline.go WriteBaselineInclude` | `filewriter.PrependBlockIfNotFound` | prepend [include] at top | WIRED | `filewriter.PrependBlockIfNotFound(existing, "baseline-include", includeBody)` at line 621 |
| `internal/gitconfig/baseline.go WriteBaselineFile` | `filewriter.ReplaceBlock` + `filewriter.Write` | render block body → compose → write | WIRED | `filewriter.ReplaceBlock(existing, "baseline", baselineBlock)` at line 551; `filewriter.Write(baselineFilePath, composed, gitconfigMode)` at line 567 |
| `internal/gitconfig/baseline.go ScanConflicts` | `filewriter.ListBlocks` + `filewriter.RemoveBlock` | strip managed blocks before git parse | WIRED | `filewriter.ListBlocks(content)` + loop of `filewriter.RemoveBlock(userPortion, b.Name)` at lines 83-85 |
| `cmd/gitid/baseline.go runBaselineSetup` | `gitconfig.WriteGlobalGitignore`, `gitconfig.WriteBaselineFile`, `gitconfig.WriteBaselineInclude` | orchestrate three writers after confirm | WIRED | All three calls present at lines 129, 134, 145 |
| `cmd/gitid/baseline.go runBaselineShow` | `gitconfig.ReadBaselineState` | read-back from disk then print | WIRED | `gitconfig.ReadBaselineState(absGitconfig, absBaseline, absGitignore)` at line 204 |
| `cmd/gitid/main.go newRootCmd` | `newBaselineSetupCmd` / `newBaselineShowCmd` | `root.AddCommand(baseline)` | WIRED | Lines 48-54 in main.go |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `printBaselineState` | `state gitconfig.BaselineState` | `ReadBaselineState` reads disk files via `filewriter.ListBlocks` then `parseGitconfigBlockBody` | Yes — disk content, not hardcoded | FLOWING |
| `printBaselinePreview` | `baselineBlock` rendered output | `RenderBaselineBlock(cfg)` with default config from `DefaultBaselineConfig()` | Yes — real renders from deterministic config | FLOWING |
| `ScanConflicts` | `userKeys map[string]string` | `git config --file <tempfile> --list` on user-owned portion after stripping managed blocks | Yes — executes git on real file content | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| PrependBlockIfNotFound places block at top, delegates to ReplaceBlock on exists | `go test ./internal/filewriter/... -run 'TestPrependBlockIfNotFound'` | PASS (3 subtests) | PASS |
| Renderers are byte-stable; WR-03 error return (not panic) on invalid input | `go test ./internal/gitconfig/... -run 'TestRenderBaselineBlock|TestRenderURLRewritesBlock|TestRenderGitignoreBlock'` | PASS (15 subtests) | PASS |
| Writers idempotent; foreign content preserved; include wired at TOP | `go test ./internal/gitconfig/... -run 'TestWriteBaselineFile|TestWriteGlobalGitignore|TestWriteBaselineInclude'` | PASS (8 subtests) | PASS |
| ScanConflicts strips blocks first; WR-02 value comparison; WR-05 malformed graceful | `go test ./internal/gitconfig/... -run 'TestScanConflicts|TestScanConflicts_WR02|TestScanConflicts_WR05'` | PASS (8 subtests) | PASS |
| ReadBaselineState: installed/not-installed/incomplete states | `go test ./internal/gitconfig/... -run 'TestReadBaselineState'` | PASS (4 subtests) | PASS |
| CLI dry-run, show, end-to-end idempotency | `go test ./cmd/gitid/... -run 'TestBaselineSetup_DryRun|TestBaselineShow|TestBaselineSetup_Idempotency'` | PASS (7 subtests) | PASS |
| Full test suite (race detector) | `make test` | All 12 packages pass | PASS |
| Lint (golangci-lint v2 + gosec) | `make lint` | 0 issues | PASS |

---

### Probe Execution

No probe scripts declared for this phase. Step 7c: SKIPPED (no probe-*.sh files declared or found).

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| GLOBAL-01 | Plans 01, 02, 03, 04 | Global git config toggles in idempotent sentinel-delimited block, foreign content preserved | SATISFIED | `RenderBaselineBlock` writes Tier-1 keys unconditionally; `WriteBaselineFile` uses `ReplaceBlock`; idempotency tests pass; REQUIREMENTS.md updated to Complete |
| URLRW-01 | Plans 02, 03, 04 | HTTPS→SSH insteadOf rewrites offered, editable/deselectable before write | SATISFIED | Three per-host Y/n prompts with blast-radius warning; `WriteBaselineFile` removes block when slice is empty; `RemoveURLRewritesBlock` independent helper exists; REQUIREMENTS.md updated to Complete |
| GITIGNORE-01 | Plans 02, 04 | gitid-managed `~/.gitignore_global` with curated excludes, wired via core.excludesfile, safe-write chokepoint | SATISFIED | `DefaultGitignorePatterns()` contains all 6 SC-2-locked patterns; `WriteGlobalGitignore` routes through `filewriter.Write`; `core.excludesfile = ~/.gitignore_global` hardcoded in renderer; REQUIREMENTS.md updated to Complete |

---

### Code Review Findings — Closure Verification (03.1-REVIEW.md)

The review identified 1 Critical + 7 Warnings. All are fixed in the `fix(03.1):` commits:

| Finding | Fix Present | Evidence |
|---------|-------------|----------|
| CR-01: multi-surface partial write, no rollback | FIXED | `runBaselineSetup` has full rollback with `restoreBackup`; include written last; detailed error messages with backup paths |
| WR-01: `WriteBaselineInclude` ignored `baselineFilePath` parameter | FIXED | `includeBody := "[include]\n\tpath = " + baselineFilePath` — parameter is honoured, no `_ = baselineFilePath` |
| WR-02: `ScanConflicts` reported conflict even on equal values | FIXED | `userVal != baseVal` comparison at line 129; `TestScanConflicts_WR02` proves identical values are not conflicts |
| WR-03: renderers panicked on invalid input | FIXED | `RenderBaselineBlock` and `RenderURLRewritesBlock` return `(string, error)` and propagate `validateValue` errors |
| WR-04: parser split on ` = ` corrupting values with embedded `=` | FIXED | `parseGitconfigBlockBody` uses `strings.Index(trimmed, "=")` (first `=` only); tab-prefix guard for section headers |
| WR-05: malformed `~/.gitconfig` caused hard failure in `ScanConflicts` | FIXED | Non-zero git exit returns `nil, nil` with `//nolint:nilerr` and explanatory comment (line 111) |
| WR-06: `promptYN` silently accepted on read error (default-Y safe?) | FIXED | `if err != nil && err != io.EOF { return false }` — fail-safe direction on non-EOF errors |
| WR-07: redundant empty-input branch in `PrependBlockIfNotFound` | FIXED | Single `append([]byte(block), existing...)` path; comment explains WR-07 rationale |

---

### Anti-Patterns Found

None. No TBD/FIXME/XXX markers found in any phase file. No stub returns, placeholder text, or dead empty implementations. All `return []URLRewrite{...}` and `return []string{...}` values in `DefaultURLRewrites` and `DefaultGitignorePatterns` are real seed data, not stubs.

---

### Human Verification Required

None. All success criteria are verifiable programmatically. The `make test` + `make lint` suite is green, and all observable behaviors have test coverage including the end-to-end idempotency check.

---

### Gaps Summary

No gaps. All 5 success criteria are VERIFIED against the actual codebase with test evidence. All 3 requirement IDs (GLOBAL-01, URLRW-01, GITIGNORE-01) are implemented, traced, and marked Complete in REQUIREMENTS.md. All 8 code review findings from 03.1-REVIEW.md are confirmed fixed in the current code.

---

_Verified: 2026-06-11T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
