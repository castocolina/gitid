---
phase: 03.1
slug: baseline-global-git-config-global-gitignore
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-11
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from 03.1-RESEARCH.md §"Validation Architecture". Task IDs are TBD
> until the planner assigns plan/wave numbers — map by requirement for now.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `go test` (stdlib), no external test library |
| **Config file** | none — convention: `*_test.go` per package |
| **Quick run command** | `go test ./internal/gitconfig/... ./internal/filewriter/... ./cmd/gitid/...` |
| **Full suite command** | `make test` (`go test -race -coverprofile=coverage.out ./...`) |
| **Estimated runtime** | ~15 seconds (quick), ~40 seconds (full, -race) |

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/filewriter/... ./internal/gitconfig/...`
- **After every plan wave:** Run `make test` (full suite with `-race`)
- **Before `/gsd-verify-work`:** `make test` green + `make lint` green
- **Max feedback latency:** ~40 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | 1 | GLOBAL-01 | — | `PrependBlockIfNotFound` places block at TOP on first write (floor) | unit | `go test ./internal/filewriter/... -run TestPrependBlockIfNotFound` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | GLOBAL-01 | — | `PrependBlockIfNotFound` updates in-place on 2nd write (floor position preserved) | unit | `go test ./internal/filewriter/... -run TestPrependBlockIfNotFound_UpdateInPlace` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | GLOBAL-01 | — | `RenderBaselineBlock` is byte-stable (SC-1 idempotency) | unit | `go test ./internal/gitconfig/... -run TestRenderBaselineBlock` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | GLOBAL-01 | — | Baseline write → re-run produces zero diff | unit | `go test ./internal/gitconfig/... -run TestWriteBaselineFile_Idempotent` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | GLOBAL-01 | — | Foreign content outside managed block preserved verbatim | unit | `go test ./internal/gitconfig/... -run TestWriteBaselineFile_PreservesForeign` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | GLOBAL-01 | T-V5 | Conflict scan finds user-key/baseline-key overlap (managed blocks stripped first) | unit | `go test ./internal/gitconfig/... -run TestScanConflicts` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | URLRW-01 | T-V5 | `RenderURLRewritesBlock` is byte-stable; injection-validated values | unit | `go test ./internal/gitconfig/... -run TestRenderURLRewritesBlock` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | URLRW-01 | — | `url-rewrites` block removable independently (user deselects all) | unit | `go test ./internal/gitconfig/... -run TestRemoveURLRewritesBlock` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | GITIGNORE-01 | — | `WriteGlobalGitignore` writes all SC-2 patterns + idempotent | unit | `go test ./internal/gitconfig/... -run TestWriteGlobalGitignore_Idempotent` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | GITIGNORE-01 | — | Foreign content in `~/.gitignore_global` preserved verbatim | unit | `go test ./internal/gitconfig/... -run TestWriteGlobalGitignore_PreservesForeign` | ❌ W0 | ⬜ pending |
| TBD | TBD | 3 | GLOBAL-01 | — | `gitid baseline show` reads back state from disk (no sidecar DB) | unit/integration | `go test ./cmd/gitid/... -run TestBaselineShow` | ❌ W0 | ⬜ pending |
| TBD | TBD | 3 | SAFE-01/02/03 | — | Backup created before every write; writes atomic | unit | `go test ./internal/filewriter/... -run TestWrite_BackupCreated` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `internal/filewriter/block_prepend_test.go` (or extend `block_test.go`) — `PrependBlockIfNotFound` top-placement + in-place update
- [ ] `internal/gitconfig/baseline_test.go` — GLOBAL-01/URLRW-01/GITIGNORE-01 render + write + idempotency + conflict-scan + read-back
- [ ] `cmd/gitid/baseline_test.go` — `gitid baseline setup` (dry-run) + `gitid baseline show`

*No framework install needed — `go test` stdlib already in use.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Floor semantics proof against real git resolution | GLOBAL-01 (SC-1) | Needs a real `git config --includes` resolve over a constructed `~/.gitconfig`; better as an integration test but listed here as the observable check | Write baseline to a temp `~/.gitconfig` with user `core.ignorecase=true` already set, prepend the managed `[include]`, then `git config --file <tmp> --includes --get core.ignorecase` → must return `true` (user wins) |
| Idempotency no-diff across the 3 files | GLOBAL-01/GITIGNORE-01/URLRW-01 (SC-1/SC-2) | End-to-end file diff over `~/.gitconfig`, `~/.gitconfig.d/00-baseline`, `~/.gitignore_global` | Run setup twice; diff each file before/after the 2nd run → zero diff; only one backup (from the 1st run) created |
| insteadOf blast-radius preview wording | URLRW-01 (SC-4) | Subjective UX | Confirm the preview shows each editable/deselectable mapping and a heads-up that a global rewrite affects all HTTPS clones for that host |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 40s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
