---
phase: "03.1"
plan: "03"
subsystem: gitconfig
tags: [read-side, conflict-scan, block-stripped, sidecar-free, tdd]
dependency_graph:
  requires: ["03.1-02"]
  provides: ["03.1-04"]
  affects: ["internal/gitconfig"]
tech_stack:
  added: []
  patterns:
    - "ScanConflicts: ListBlocks+RemoveBlock strip managed blocks before git config --file --list (RESEARCH C2/Pitfall C algorithm)"
    - "ReadBaselineState: filewriter.ListBlocks across three files, no sidecar DB (IDENT-07 model)"
    - "BaselineKeySet: authoritative Tier-1 key→value map, shared between renderer and scan"
    - "RemoveURLRewritesBlock: filewriter.RemoveBlock(url-rewrites) + filewriter.Write"
key_files:
  created: []
  modified:
    - internal/gitconfig/baseline.go
    - internal/gitconfig/baseline_test.go
decisions:
  - "ScanConflicts writes user portion to os.CreateTemp (arg-slice git exec, no --includes per C6)"
  - "BaselineKeySet contains only Tier-1 unconditional keys — conservative scan never false-positives on absent Tier-2 keys"
  - "ReadBaselineState uses readFileSilent helper (missing = empty, not error) mirroring WriteIncludeIf !os.IsNotExist guard"
  - "parseGitconfigBlockBody uses section-context line scanner (SplitN ' = ' 2) — no git exec needed for read-back (SC-5)"
  - "defer os.Remove placed after tmp.Close() — T-03.1-06 temp-file cleanup"
metrics:
  duration: "4 minutes"
  completed: "2026-06-11"
  tasks: 2
  files_modified: 2
---

# Phase 03.1 Plan 03: Conflict Scan + Read-Back Summary

**One-liner:** Block-stripped ScanConflicts (C2 algorithm via ListBlocks+RemoveBlock+temp file) + sidecar-free ReadBaselineState (IDENT-07 ListBlocks across three files) + independent RemoveURLRewritesBlock.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| RED | Failing tests for ScanConflicts, RemoveURLRewritesBlock, ReadBaselineState + lint-clean stubs | 8087d01 | baseline.go, baseline_test.go |
| GREEN T1 | ScanConflicts + RemoveURLRewritesBlock + BaselineKeySet + all types | 1c3c13b | baseline.go |
| GREEN T2 | ReadBaselineState + parser helpers (indexBlocks, parseGitconfigBlockBody, parseURLRewritesBlockBody, parseGitignoreBlockBody) | 1c3c13b | baseline.go |

## What Was Built

### New symbols in `internal/gitconfig/baseline.go`

**Types:**
- `Conflict` struct: `Key, UserValue, BaselineValue, Winner string`
- `BaselineState` struct: `Installed bool`, `Incomplete bool`, `Missing []string`, `BaselineKeys map[string]string`, `URLRewrites []URLRewrite`, `GitignorePatterns []string`

**Functions:**
- `BaselineKeySet() map[string]string` — authoritative Tier-1 key→value map (core.ignorecase, core.excludesfile, push.autosetupremote, pull.rebase, fetch.prune, color.ui)
- `ScanConflicts(gitconfigPath string, baselineKeys map[string]string) ([]Conflict, error)` — strips managed blocks first (C2 algorithm), temp file + git exec (no --includes)
- `RemoveURLRewritesBlock(baselineFilePath string) (backupPath string, err error)` — independently removes url-rewrites block (D-07)
- `ReadBaselineState(gitconfigPath, baselineFilePath, gitignorePath string) (BaselineState, error)` — sidecar-free read-back across three files (SC-5, IDENT-07)
- Private helpers: `readFileSilent`, `indexBlocks`, `parseGitconfigBlockBody`, `parseURLRewritesBlockBody`, `parseGitignoreBlockBody`

### TDD Gate Compliance

- RED commit `8087d01`: `test(03.1-03)` — failing tests + `_`-param stubs that pass lint (CLAUDE.md TDD-RED-stub convention)
- GREEN commit `1c3c13b`: `feat(03.1-03)` — full implementations, all 10 new tests pass
- No REFACTOR pass needed (code was clean after GREEN)

## Algorithm Correctness Proofs

**ScanConflicts (RESEARCH C2 / Pitfall C):**
- `TestScanConflicts/managed-block_keys_not_reported` proves that a `baseline-include` managed block containing `core.ignorecase=false` does NOT appear as a conflict — only the user's own `core.ignorecase=true` (outside any block) is reported
- `TestScanConflicts/excludesfile_conflict_reported` proves Pitfall E detection: user's `core.excludesfile=~/.gitignore` is flagged against baseline `~/.gitignore_global`

**No --includes (C6):**
- `grep -c '"--includes"' internal/gitconfig/baseline.go` = 0 (verified)

**Block stripping:**
- `grep -A25 'func ScanConflicts' ... | grep -Ec 'ListBlocks|RemoveBlock'` = 2 (verified)

**Temp file cleanup (T-03.1-06):**
- `grep -A30 'func ScanConflicts' ... | grep -c 'os.Remove'` = 2 (defer + error-path cleanup)

**ReadBaselineState sidecar-free (SC-5):**
- `grep -A30 'func ReadBaselineState' ... | grep -Ec '\-\-global|\-\-includes'` = 0 (verified)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. All implementations are complete and tested.

## Threat Flags

No new network endpoints or auth paths introduced. Read-only paths (`ReadBaselineState`) have no exec. `ScanConflicts` exec is scoped to a gitid-controlled temp file.

All T-03.1-05/06/07 mitigations applied:
- `//nolint:gosec // arg-slice form, no shell; trusted temp path (G204)` on `exec.Command`
- `defer os.Remove(tmp.Name())` immediately after `tmp.Close()`
- `//nolint:gosec // ... trusted gitid-managed path (G304)` on all `os.ReadFile` calls

## Self-Check: PASSED

- `internal/gitconfig/baseline.go` contains `func ScanConflicts`, `func RemoveURLRewritesBlock`, `func ReadBaselineState`, `func BaselineKeySet`, type `Conflict`, type `BaselineState`
- Commits 8087d01 (RED) and 1c3c13b (GREEN) verified in git log
- `make test` and `make lint` both green (0 issues)
