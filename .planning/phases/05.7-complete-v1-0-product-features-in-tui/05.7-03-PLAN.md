---
phase: 05.7-complete-v1-0-product-features-in-tui
plan: 03
type: tdd
wave: 1
depends_on: ["05.7-01"]
files_modified:
  - internal/repoclone/repoclone.go
  - internal/repoclone/repoclone_test.go
  - internal/repoclone/repoclone_stub_test.go
autonomous: true
requirements: [REPO-01]
must_haves:
  truths:
    - "repoclone.ProviderFromURL parses https://, git@host:, and ssh:// URLs to a provider hostname"
    - "repoclone.RewriteToAlias rewrites an HTTPS/host URL to git@<alias>:<org>/<repo> using a provider→alias lookup"
    - "repoclone.Clone refuses to clone when the destination already exists (typed ErrDestExists), before any exec"
    - "repoclone.Clone validates the destination is under the resolved ~/git base (deps.UserHomeDir()+\"/git\"), rejecting absolute/escape paths before exec"
    - "Clone and Pull shell out via explicit exec arg slices (never sh -c) and return their full output lines"
  artifacts:
    - path: "internal/repoclone/repoclone.go"
      provides: "ProviderFromURL(), RewriteToAlias(), DestPath(), Clone(), Pull(), Deps struct, ErrDestExists, ErrUnknownProvider, ErrDestOutsideBase"
      exports: ["ProviderFromURL", "RewriteToAlias", "DestPath", "Clone", "Pull", "Deps", "ErrDestExists", "ErrDestOutsideBase"]
      min_lines: 90
    - path: "internal/repoclone/repoclone_test.go"
      provides: "GREEN unit tests: URL parse table, rewrite, dest-exists, dest-escape guard"
      contains: "func TestProviderFromURL"
  key_links:
    - from: "internal/repoclone/repoclone.go"
      to: "git binary (os/exec)"
      via: "exec.Command arg slice (no shell)"
      pattern: "exec\\.Command\\(\"git\""
    - from: "internal/repoclone/Clone"
      to: "Deps.Stat + Deps.UserHomeDir"
      via: "dest-exists + dest-under-base guard before exec"
      pattern: "deps\\.(Stat|UserHomeDir)"
---

<objective>
Build the UI-free `internal/repoclone` package (REPO-01 core) test-first:
detect the provider from a clone URL, rewrite the URL to the matching gitid
SSH alias (`git@<alias>:<org>/<repo>`, recipe form), compute the per-client
destination under `~/git/<client>/<repo>` (D-09), and run `git clone` + verify
`git -C <dest> pull` via explicit `os/exec` arg slices (never a shell). All exec
is injected through `Deps` so the pure logic (parse/rewrite/dest/guard) is fully
unit-testable without touching git; the real clone/pull path is exercised against
a local bare remote in the Plan 01 e2e harness (D-13).

Purpose: Isolates the three external operations (provider detect, clone, pull)
behind a Deps seam so the TUI add-repo modal (Plan 07) and the `gitid add repo`
CLI command (Plan 06) both compose it without embedding os/exec in the UI.
Output: GREEN `internal/repoclone` package replacing the Plan 01 RED stub.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-RESEARCH.md
@CLAUDE.md
@recipes/ssh-config.recipe
@internal/identity/identity.go
@tui/deps.go
</context>

<artifacts_this_phase_produces>
New symbols (exclude from drift checks): `repoclone.ProviderFromURL`,
`repoclone.RewriteToAlias`, `repoclone.DestPath`, `repoclone.Clone`,
`repoclone.Pull`, `repoclone.ErrDestExists`, `repoclone.ErrUnknownProvider`,
`repoclone.ErrDestOutsideBase`, `repoclone.liveClone`, `repoclone.livePull`
(the Deps struct + ErrDestExists/ErrUnknownProvider sentinels already exist as
Plan 01 skeletons; this plan fills the bodies and adds ErrDestOutsideBase).

LOCKED allowed-base seam (single form — REVIEWS.md #10, no executor choice):
- The allowed base is derived INTERNALLY by Clone via `deps.UserHomeDir()` joined
  with `"git"` (i.e. `<home>/git`). Clone does NOT take a base-dir parameter and
  Deps has NO `AllowedBaseDir` field. The caller passes the already-computed
  `destPath` (from `DestPath(baseDir, client, url)`); Clone re-derives the base
  and rejects any destPath that does not lie under `<home>/git` with the typed
  `ErrDestOutsideBase`. This keeps the guard wireable from a single source of truth.
- `func Clone(cloneURL, destPath string, deps Deps) ([]string, error)`.
- `func Pull(destPath string, deps Deps) ([]string, error)`.
- `func DestPath(baseDir, client, rawURL string) (string, error)` — baseDir is the
  expanded `<home>/git`; pure, no exec, no filesystem.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD ProviderFromURL + RewriteToAlias + DestPath (pure logic)</name>
  <read_first>
    - internal/repoclone/repoclone.go (Plan 01 RED stub — Deps + ErrDestExists/ErrUnknownProvider present)
    - recipes/ssh-config.recipe (alias form `personal.github.com`; recipes/gitconfig.recipe rewrite `git@<provider>:` insteadOf)
    - internal/sshconfig (ParseManagedHosts / alias lookup — how provider→alias is indexed; grep for the exported reader)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md (internal/repoclone/repoclone.go section — ProviderFromURL signature)
  </read_first>
  <behavior>
    - Test: ProviderFromURL("https://github.com/org/repo.git") == "github.com"
    - Test: ProviderFromURL("git@github.com:org/repo.git") == "github.com"
    - Test: ProviderFromURL("ssh://git@gitlab.example.com:443/org/repo") == "gitlab.example.com"
    - Test: ProviderFromURL("not-a-url") returns ("", ErrUnknownProvider)
    - Test: RewriteToAlias("https://github.com/org/repo.git", alias="personal.github.com") == "git@personal.github.com:org/repo.git"
    - Test: RewriteToAlias preserves the org/repo path and .git suffix; no double scheme
    - Test: DestPath(baseDir=<home>/git, client="personal", url) == <home>/git/personal/repo
  </behavior>
  <action>
    Implement ProviderFromURL covering https://, git@host:, and ssh:// forms (use net/url
    for scheme URLs; a manual split for the scp-like `git@host:path` form). Return
    ErrUnknownProvider when no host can be extracted. Implement
    RewriteToAlias(rawURL, alias string) (string, error): extract org/repo path (strip scheme
    + host, strip leading slash), produce `git@<alias>:<org>/<repo>` keeping the original
    `.git` suffix if present — never hand-roll with naive string replace that could leave a
    stray scheme. Implement DestPath(baseDir, client, rawURL) (string, error): repo name =
    last path segment minus `.git`; join baseDir/client/repo. Keep all three PURE (no exec,
    no filesystem) so they are table-test friendly.
  </action>
  <verify>
    <automated>go test ./internal/repoclone/... -run 'TestProviderFromURL|TestRewriteToAlias|TestDestPath' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All three table tests pass GREEN
    - Unknown/garbage URL returns ErrUnknownProvider (errors.Is asserted)
    - RewriteToAlias output matches `git@<alias>:<org>/<repo>` exactly (recipe form) for https and git@ inputs
  </acceptance_criteria>
  <done>Provider detection, alias rewrite, and destination computation are pure, table-tested, and recipe-aligned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD Clone/Pull seam — dest-exists guard, dest-under-base guard, arg-slice exec</name>
  <read_first>
    - internal/repoclone/repoclone.go (pure logic now GREEN from Task 1)
    - tui/deps.go (lines ~412-438 — `tuiRunSSHAdd` arg-slice exec pattern with //nolint:gosec G204)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-RESEARCH.md (§ Pitfall 5: dest exists; § Security Domain — clone into attacker dest)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md (liveClone/livePull arg-slice pattern + dest-exists guard)
  </read_first>
  <behavior>
    - Test: Clone(url, dest, deps) where deps.UserHomeDir returns a home and dest is under <home>/git, and Deps.Stat returns a FileInfo (dest exists) → returns ErrDestExists, clone func NOT invoked
    - Test: Clone where Stat returns os.ErrNotExist → calls the injected clone func with (url, dest) and returns its lines
    - Test: Clone rejects a dest NOT under <home>/git (e.g. "/etc/x" or "<home>/git/../escape" that cleans outside base) with ErrDestOutsideBase, BEFORE Stat and exec (counter-fake: Stat not called)
    - Test: Pull(dest, deps) calls the injected pull func with dest and returns lines
    - Test: liveClone/livePull build exec.Command("git","clone",url,dest) / ("git","-C",dest,"pull") — assert via a recorded command-arg fake (no real git in unit test)
  </behavior>
  <action>
    Implement Clone(cloneURL, destPath string, deps Deps) ([]string, error): FIRST derive the
    allowed base internally — home, err := deps.UserHomeDir(); base := filepath.Join(home, "git").
    Reject when filepath.Clean(destPath) is not within base (use filepath.Rel + reject results
    starting with ".." or an absolute escape) → return ErrDestOutsideBase BEFORE any Stat or exec.
    Then `deps.Stat(destPath)`: if err == nil → ErrDestExists; if !os.IsNotExist(err) → wrap;
    else call deps.Clone(cloneURL, destPath). Implement Pull(destPath, deps) → deps.Pull(destPath).
    Add package-level `liveClone(cloneURL, destPath string) ([]string, error)` and
    `livePull(destPath string) ([]string, error)` using exec.Command arg slices with
    `//nolint:gosec // arg-slice; no shell; URL/dest validated (G204)` and CombinedOutput split
    into lines. These live funcs are wired into Deps by the CLI/TUI in Plan 06 — keep them
    here so the nil-guard wiring has real targets. NEVER use exec.Command("sh","-c", ...).
  </action>
  <verify>
    <automated>go test ./internal/repoclone/... -count=1 && grep -c 'sh", "-c"' internal/repoclone/repoclone.go</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/repoclone/...` fully GREEN
    - `grep -c 'sh", "-c"' internal/repoclone/repoclone.go` returns 0 (no shell expansion)
    - dest-exists returns ErrDestExists with NO clone invocation (counter-fake asserts)
    - dest-outside-base (absolute or `..`-escape) rejected with ErrDestOutsideBase before Stat/exec
    - `make lint` (incl. gosec G204) passes without --no-verify
  </acceptance_criteria>
  <done>Clone/Pull seam enforces dest-exists + dest-under-base guards (base derived internally via deps.UserHomeDir) before exec; live exec uses arg slices only; whole package GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied clone URL → exec git | The URL is fully attacker-controllable input crossing into a subprocess |
| computed destPath → git clone target | A crafted URL/client could try to write outside ~/git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.7-03-01 | Tampering | clone URL → git subprocess | mitigate | exec.Command("git","clone",url,dest) explicit arg slice; never sh -c (gosec G204); Task 2 grep gate asserts no shell |
| T-05.7-03-02 | Elevation of Privilege | destPath escaping ~/git | mitigate | dest-under-base guard (base = deps.UserHomeDir()+"/git") rejects absolute paths and ".." with ErrDestOutsideBase before any Stat/exec (Task 2 test) |
| T-05.7-03-03 | Tampering | pull arg injection | mitigate | exec.Command("git","-C",dest,"pull") arg slice; dest is gitid-derived, not raw URL |
| T-05.7-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs; git is a pre-declared runtime dep (RESEARCH Environment Availability) |
</threat_model>

<verification>
- `go test ./internal/repoclone/... -count=1` GREEN
- No `sh -c` anywhere (grep gate = 0)
- dest-exists + dest-under-base guards proven by tests
- `make lint` (gosec) green without --no-verify
</verification>

<success_criteria>
- ProviderFromURL/RewriteToAlias/DestPath pure + table-tested + recipe-aligned
- Clone refuses existing dest (ErrDestExists) and out-of-base dest (ErrDestOutsideBase) before exec; base derived internally via deps.UserHomeDir()+"/git"
- liveClone/livePull exist for live wiring; arg-slice only
</success_criteria>

<output>
Create `.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-03-SUMMARY.md` when done
</output>
