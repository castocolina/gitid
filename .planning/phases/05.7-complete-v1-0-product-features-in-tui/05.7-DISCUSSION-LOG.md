# Phase 5.7 — Discussion Log

**Date:** 2026-06-21
**Mode:** discuss (standard)
*Human-reference record only — downstream agents read CONTEXT.md, not this file.*

## Pre-discussion: scope reconciliation

Before discussing gray areas, a check of `REQUIREMENTS.md` against the live code
found that 3 of the 6 items in the original ROADMAP 5.7 entry were **already built**
(`GLOBAL-01`, `URLRW-01`, `CLI-02`, plus alt-SSH 443 editing). Surfaced to the user;
scope re-aimed at the genuinely-open items.

**Decision — scope (multi-select):** all four open items in scope —
match-strategy completion, `ADOPT-01`, `REPO-01`, `AUTOUP-01`.

**Decision — match default:** *Keep `gitdir` default, add `hasconfig`/`both`
options.* (Resolves the open `gitid` vs `recipe` match divergence.)

## Gray areas discussed

User selected all four areas to discuss.

| Area | Options presented | Selection |
|---|---|---|
| Fragment adoption (ADOPT-01) | Migrate-default / Reference-default / Ask-per-fragment | **Migrate into `~/.gitconfig.d/`, offer reference** → D-04/D-05 |
| add repo no-match (REPO-01) | Inline create / Abort with guidance / Clone with chosen identity | **Offer inline create, then clone** → D-08 |
| Surface split | add-repo CLI-only split / Both TUI+CLI / TUI-first | **Everything in both TUI and CLI** → D-10 |
| gh/glab upload (AUTOUP-01) | Auth+signing when authed / Auth only / Detect+prompt never auto | **Detect + prompt, never auto** → D-11/D-12 |

## Notes & derived decisions

- Migrate = copy-then-repoint, never destructive; offer to remove the original after
  success but never auto-delete (CLAUDE.md safe-write) → D-05.
- `hasconfig` value derives from alias as `remote.*.url:git@<alias>:*/**`, editable in
  the live preview → D-03.
- `add repo` base dir assumed `~/git/<client>/<repo>` (PRD open-decision #2) → D-09.
- Verification must drive the real `gitid` CLI + live TUI, not seams → D-13.

## Deferred (not this phase)

Switching the default match to `hasconfig`; configurable clone base dir; gitid-driven
gh/glab login; full mouse parity/theming; v1 non-goals (Windows, GPG, web UI, auto
rotation, secret-vault).

## Carried forward (locked, not re-asked)

Two-pane + modal shell, prove-before-write on structural edits, backup→preview→confirm
on every write, copy = public key only, live `includeIf` preview (Phase 5.6 D-01..D-16);
create-flow + provider/match reconstruction (Phase 5.5).
