---
phase: 05.7-complete-v1-0-product-features-in-tui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tui/wiring_test.go
  - e2e/harness_test.go
  - e2e/adopt_e2e_test.go
  - e2e/addrepo_e2e_test.go
  - e2e/upload_e2e_test.go
  - internal/adopter/adopter.go
  - internal/adopter/adopter_stub_test.go
  - internal/repoclone/repoclone.go
  - internal/repoclone/repoclone_stub_test.go
  - internal/uploader/uploader.go
  - internal/uploader/uploader_stub_test.go
autonomous: true
requirements: [ADOPT-01, REPO-01, AUTOUP-01]
must_haves:
  truths:
    - "make test-e2e compiles the new e2e files (adopt/addrepo/upload) and they FAIL (RED) because the features are unimplemented"
    - "TestBuildTUIDepsNilGuard_Phase57 exists and FAILS (RED) on nil function-field assertions of zero-value adopter/repoclone/uploader Deps — and the whole tui package still compiles (no buildTUIDeps arity change in this plan)"
    - "FakeGHDir, FakeGLabDir, FakeGitDir, and setupLocalBareRepo helpers exist in e2e/harness_test.go and compile"
    - "The three production package files (internal/adopter/adopter.go, internal/repoclone/repoclone.go, internal/uploader/uploader.go) exist with their Deps/enum/sentinel types and RED stub bodies; go build ./... exits 0"
    - "RED unit stubs exist for the three new core packages and run under `go test` without --no-verify"
  artifacts:
    - path: "e2e/harness_test.go"
      provides: "FakeGHDir / FakeGLabDir / FakeGitDir / setupLocalBareRepo PATH-stub helpers"
      contains: "func FakeGHDir"
    - path: "e2e/adopt_e2e_test.go"
      provides: "RED e2e for ADOPT-01 migrate + reference"
      contains: "//go:build e2e"
    - path: "e2e/addrepo_e2e_test.go"
      provides: "RED e2e for REPO-01 local-bare clone + alias rewrite"
      contains: "setupLocalBareRepo"
    - path: "e2e/upload_e2e_test.go"
      provides: "RED e2e for AUTOUP-01 gh-ok / auth-fail / not-present"
      contains: "FakeGHDir"
    - path: "internal/adopter/adopter.go"
      provides: "adopter package skeleton — Deps struct (incl. WriteIncludeIf 3-arg seam, ListCandidates), AdoptMethod enum, AdoptResult, ErrAmbiguousIdentity; RED stub bodies"
      contains: "type Deps struct"
    - path: "internal/repoclone/repoclone.go"
      provides: "repoclone package skeleton — Deps struct, ErrDestExists/ErrUnknownProvider sentinels; RED stub bodies"
      contains: "type Deps struct"
    - path: "internal/uploader/uploader.go"
      provides: "uploader package skeleton — Deps struct, Tool/AuthStatus enums, KeyType constants; RED stub bodies"
      contains: "type Deps struct"
    - path: "tui/wiring_test.go"
      provides: "extended D-16 nil-guard for adopter/repoclone/uploader Deps (zero-value form — RED)"
      contains: "TestBuildTUIDepsNilGuard_Phase57"
  key_links:
    - from: "e2e/upload_e2e_test.go"
      to: "FakeGHDir"
      via: "PATH prepend"
      pattern: "FakeGHDir"
    - from: "tui/wiring_test.go"
      to: "adopter/repoclone/uploader Deps types"
      via: "zero-value struct nil-field assertions"
      pattern: "adopter\\.Deps\\{\\}"
---

<objective>
Lay the Wave 0 test scaffolding that closes the recurring D-13/D-16 injected-seam
blindspot BEFORE any feature code exists. Extend the e2e harness with PATH-stub
fakes (`gh`/`glab`/`git`) and a local-bare-remote helper, add the three new
RED e2e test files (adopt, addrepo, upload), create the three new core package
FILES (`internal/adopter/adopter.go`, `internal/repoclone/repoclone.go`,
`internal/uploader/uploader.go`) with their Deps/enum/sentinel types and RED stub
bodies (so Wave 1 Plans 02-04 have a package to fill and the whole module
compiles), and add a RED extension of the `TestBuildTUIDepsNilGuard` wiring guard
for the three new `Deps` structs.

Purpose: Every feature wave (02-08) layers GREEN implementations on top of these
RED tests. The nil-guard and the real-binary e2e tests are the anti-blindspot
mechanism — they drive the REAL `gitid` binary in a sandbox HOME (D-13), not
injected model internals. The three production `.go` files created here are the
compile-time foundation Wave 1 depends on.
Output: 11 files — 3 production package skeletons, 3 RED unit stub tests, 3 RED
e2e files, 1 harness extension, 1 RED nil-guard extension — all compiling but
FAILING on behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-RESEARCH.md
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates these new symbols (exclude from source-grounding drift checks):
- `e2e.FakeGHDir(t, mode)`, `e2e.FakeGLabDir(t, mode)`, `e2e.FakeGitDir(t, mode)`, `e2e.setupLocalBareRepo(t)` — harness helpers
- `TestAdopt_Migrate`, `TestAdopt_Reference` (e2e/adopt_e2e_test.go)
- `TestAddRepo_LocalClone`, `TestAddRepo_AliasRewrite` (e2e/addrepo_e2e_test.go)
- `TestUpload_GHOk`, `TestUpload_GHAuthFail`, `TestUpload_GHNotPresent` (e2e/upload_e2e_test.go)
- `TestBuildTUIDepsNilGuard_Phase57` (tui/wiring_test.go)
- the three production package skeletons: `internal/adopter/adopter.go`,
  `internal/repoclone/repoclone.go`, `internal/uploader/uploader.go` with their
  `Deps` structs, enums (`AdoptMethod`, `Tool`, `AuthStatus`), and sentinels
  (`ErrDestExists`, `ErrUnknownProvider`, `ErrAmbiguousIdentity`) — RED stub bodies
- RED unit stub test funcs in `internal/adopter`, `internal/repoclone`, `internal/uploader`
- new package directories: `internal/adopter/`, `internal/repoclone/`, `internal/uploader/`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend e2e harness with gh/glab/git fakes + local bare remote</name>
  <read_first>
    - e2e/harness_test.go (the file being modified — `SandboxHome`, `BuildBinary`, `FakeSSHDir` pattern lines 1-139)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md (`e2e/harness_test.go (extend)` section — exact FakeGHDir literal)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-RESEARCH.md (§ Pattern 3: PATH-stubbed Binary e2e Test)
  </read_first>
  <action>
    Append to e2e/harness_test.go (package e2e, build tag already present) three new
    helpers mirroring FakeSSHDir exactly: `FakeGHDir(t *testing.T, mode string) string`,
    `FakeGLabDir(t *testing.T, mode string) string`, `FakeGitDir(t *testing.T, mode string) string`.
    Each writes a STATIC shell-script literal (never built from variables — G306/G204-clean)
    to t.TempDir() at 0o700, sets the matching env var via t.Setenv
    (`GITID_FAKE_GH_MODE`/`GITID_FAKE_GLAB_MODE`/`GITID_FAKE_GIT_MODE`), and returns the dir.
    Modes: gh/glab — "ok" (auth status exit 0 + ssh-key add prints "Added SSH key." exit 0),
    "auth-fail" (auth status exit 1). The fake gh script must branch on $1 ("auth" vs "ssh-key")
    so `Detect` (auth status) and `UploadKey` (ssh-key add) both resolve. git fake: "clone-ok"
    (mkdir dest + exit 0), "clone-fail" (exit 128). Also add
    `setupLocalBareRepo(t *testing.T) (repoURL, repoName string)` that runs
    `exec.Command("git","init","--bare",t.TempDir())` and returns `"file://"+bare`, filepath.Base(bare).
    Use the REAL system git for setupLocalBareRepo (clone target), not the fake.
  </action>
  <verify>
    <automated>go vet ./e2e/... && grep -v '^#' e2e/harness_test.go | grep -c "func FakeGHDir\|func FakeGLabDir\|func FakeGitDir\|func setupLocalBareRepo"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' e2e/harness_test.go | grep -c "func FakeGHDir\|func FakeGLabDir\|func FakeGitDir\|func setupLocalBareRepo"` returns 4
    - `go vet ./e2e/...` exits 0 (the e2e package compiles under the e2e build tag)
    - Each fake script is a single const literal with no string concatenation from test inputs
  </acceptance_criteria>
  <done>Four harness helpers exist, compile under `-tags e2e`, and use static script literals only.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED e2e tests (adopt, addrepo, upload) driving the real binary</name>
  <read_first>
    - e2e/create_e2e_test.go (analog — SandboxHome + BuildBinary + cmd.Env HOME pattern)
    - e2e/match_e2e_test.go (analog — seed file, run binary, assert ~/.gitconfig content; lines 44-80)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md (`e2e/adopt_e2e_test.go`, `e2e/addrepo_e2e_test.go`, `e2e/upload_e2e_test.go` sections)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-RESEARCH.md (§ Layer 2: CLI e2e Tests table)
  </read_first>
  <action>
    Create three files, each with `//go:build e2e` and `package e2e`:
    (a) e2e/adopt_e2e_test.go — `TestAdopt_Migrate`: seed ~/.gitconfig_work, run
    `gitid adopt <home>/.gitconfig_work --method migrate --yes`, assert original
    preserved + copy at ~/.gitconfig.d/work + `[includeIf` present in ~/.gitconfig.
    `TestAdopt_Reference`: same seed, `--method reference`, assert original unchanged
    and includeIf points at the original path.
    (b) e2e/addrepo_e2e_test.go — `TestAddRepo_LocalClone`: use setupLocalBareRepo,
    run `gitid add repo file://<bare> --client personal --yes`, assert clone at
    ~/git/personal/<name>. `TestAddRepo_AliasRewrite`: seed identity personal.github.com,
    run `gitid add repo https://github.com/org/repo --yes`, assert rewritten
    `git@personal.github.com:org/repo` appears in stdout.
    (c) e2e/upload_e2e_test.go — `TestUpload_GHOk`: FakeGHDir(t,"ok") on PATH, seed an
    identity, run `gitid identity copy <name> --upload-keys --yes`, assert "--type authentication"
    OR "Added SSH key" in combined output. `TestUpload_GHAuthFail`: FakeGHDir(t,"auth-fail"),
    assert fallback manual-instructions text present. `TestUpload_GHNotPresent`: no gh on PATH,
    assert NO assisted-upload section in output.
    These are RED — they MUST fail now (commands/flags do not yet exist). Use
    cmd.Env = append(os.Environ(), "HOME="+home, "PATH="+fakeDir+":"+os.Getenv("PATH")).
    Do NOT use t.Skip — the tests fail honestly so the feature waves turn them green.
  </action>
  <verify>
    <automated>go vet -tags e2e ./e2e/... && go test -tags e2e -run 'TestAdopt|TestAddRepo|TestUpload' ./e2e/... 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All three files compile under `-tags e2e` (go vet exits 0)
    - `go test -tags e2e -run 'TestAdopt|TestAddRepo|TestUpload' ./e2e/...` reports FAIL (RED) — not pass, not skip
    - No `t.Skip` calls in the three new files (grep returns 0)
  </acceptance_criteria>
  <done>Three RED e2e files exist, compile, and fail on behavior because the features are unbuilt.</done>
</task>

<task type="auto">
  <name>Task 3: Create the three production package skeletons + RED unit stubs + RED nil-guard (zero-value form)</name>
  <read_first>
    - tui/wiring_test.go (the file being modified — TestBuildTUIDepsNilGuard lines 119-205; note buildTUIDeps returns 5 values today)
    - tui/deps.go (current buildTUIDeps signature line 31 — returns 4 Deps + error; DO NOT change its arity in this plan)
    - internal/identity/identity.go (analog — Deps struct shape + sentinel errors + fmt.Errorf wrapping)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md (internal/adopter, internal/repoclone, internal/uploader sections — EXACT Deps/enum/sentinel signatures; note Deps.WriteIncludeIf is the 3-arg seam func(id, fragPath string, matches []gitconfig.Match) (string, error) with gitconfigPath captured by the live closure)
    - $HOME/.claude/gsd-core/references/tdd.md (RED-stub gate rules)
    - reference: project memory "TDD RED stub under strict lint" — failing stubs must pass the lint-gated pre-commit hook (zero+sentinel return, `_`-named params), never --no-verify
  </read_first>
  <action>
    (a) Create the three production package files so the directories exist, the Deps
    TYPES are importable by Wave 1 + the nil-guard, and the whole module compiles:
    - internal/adopter/adopter.go — package doc comment; the `Deps` struct EXACTLY per
      PATTERNS.md (ReadFile, WriteFile, CopyFile, BackupAndRemove, the 3-arg
      `WriteIncludeIf func(id, fragPath string, matches []gitconfig.Match) (backupPath string, err error)`,
      ReadFragment, and `ListCandidates func(homeDir string) ([]string, error)`);
      the `AdoptMethod` enum (AdoptMigrate, AdoptReferenceInPlace), `AdoptResult` struct
      (BackupPaths, MigratedPath, IncludeIfBody), and the `ErrAmbiguousIdentity` sentinel.
      Add RED stub bodies for `Adopt(...)`, `ListCandidates(homeDir string) ([]string, error)`,
      and `MatchIdentityName(...)` returning zero + `errors.New("adopter: not implemented")`,
      `_`-named params so the lint hook passes.
    - internal/repoclone/repoclone.go — package doc; the `Deps` struct EXACTLY per
      PATTERNS.md (Stat, Clone, Pull, UserHomeDir); the `ErrDestExists` and
      `ErrUnknownProvider` sentinels; RED stub bodies for `ProviderFromURL`,
      `RewriteToAlias`, `DestPath`, `Clone`, `Pull` returning zero + sentinel.
    - internal/uploader/uploader.go — package doc; the `Deps` struct (LookPath, RunCmd);
      `Tool` enum (ToolGH, ToolGLab); `AuthStatus` enum (AuthAuthenticated,
      AuthNotLoggedIn, AuthToolNotFound); `KeyType` string constants
      (KeyAuthentication="authentication", KeySigning="signing"); RED stub bodies for
      `Detect`, `UploadKey`, `CommandPreview` returning zero + sentinel.
    (b) Append `TestBuildTUIDepsNilGuard_Phase57` to tui/wiring_test.go. LOCKED FORM
    (no executor choice): the test does NOT call buildTUIDeps() — that function still
    returns 5 values in this plan, so calling it with an 8-value destructure would break
    compilation of the whole tui test package and block the lint hook. Instead the test
    constructs ZERO-VALUE Deps structs of the three new types and asserts each function
    field is non-nil:
        var adoptDeps adopter.Deps
        var repoCloneDeps repoclone.Deps
        var uploadDeps uploader.Deps
        // assert adoptDeps.WriteFile != nil, .CopyFile, .WriteIncludeIf, .BackupAndRemove,
        //        .ReadFragment, .ListCandidates; repoCloneDeps.Clone/.Pull/.Stat;
        //        uploadDeps.LookPath/.RunCmd — every assertion FAILS (fields are nil)
    Add a comment `// RED (Plan 01): zero-value form. Plan 06 (05.7-06) rewires this to
    drive the real 8-value buildTUIDeps() return.` so the rewire target is explicit. This
    compiles (types exist from step a), runs, and FAILS on every nil assertion — a genuine
    non-vacuous RED wiring guard, with `go build ./...` exiting 0.
    (c) Add RED unit stubs internal/adopter/adopter_stub_test.go,
    internal/repoclone/repoclone_stub_test.go, internal/uploader/uploader_stub_test.go — one
    failing test each asserting the not-yet-real behavior (e.g.
    `repoclone.ProviderFromURL("https://github.com/o/r")` == "github.com" — currently RED).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/adopter/... ./internal/repoclone/... ./internal/uploader/... 2>&1 | tail -6; go test ./tui/... -run TestBuildTUIDepsNilGuard_Phase57 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 (whole module compiles — buildTUIDeps arity UNCHANGED; RED stubs return zero+sentinel)
    - `make lint` passes WITHOUT --no-verify (RED stubs use `_`-named params; no unused-param/empty-body lint failures; no 8-value buildTUIDeps call anywhere)
    - The three new package unit stubs run and FAIL (RED) on behavior
    - `TestBuildTUIDepsNilGuard_Phase57` compiles, runs, and FAILS (RED) on the zero-value nil-field assertions (non-vacuous: it inspects real adopter/repoclone/uploader Deps types)
  </acceptance_criteria>
  <done>Three production package skeletons exist with Deps/enum/sentinel types + RED stubs; nil-guard extension is compiling-RED (zero-value form, Plan 06 rewire noted) and unit stubs are RED; module builds and lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → PATH-stubbed fake binaries | Fake gh/glab/git scripts execute from a temp dir prepended to PATH; they are static literals |
| test → real git (setupLocalBareRepo) | Real `git init --bare` against t.TempDir(); no network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.7-01-01 | Tampering | fake script construction (e2e harness) | mitigate | Scripts are compile-time const literals; never concatenated from test inputs (G306/G204-clean) — confirmed by Task 1 acceptance |
| T-05.7-01-02 | Tampering | RED package stubs (os/exec absent) | accept | Stubs return sentinel errors only; no exec, no file writes until feature waves |
| T-05.7-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; `teatest/v2` (only candidate, [ASSUMED]) is NOT added here — deferred to the TUI wave that needs it, behind its own legitimacy checkpoint |
</threat_model>

<verification>
- `go build ./...` green; `make lint` green without --no-verify
- `go test -tags e2e -run 'TestAdopt|TestAddRepo|TestUpload' ./e2e/...` = RED (compiles, fails on behavior)
- `go test ./tui/... -run TestBuildTUIDepsNilGuard_Phase57` = RED (zero-value form)
- New unit stubs in adopter/repoclone/uploader = RED
</verification>

<success_criteria>
- e2e harness has FakeGHDir/FakeGLabDir/FakeGitDir/setupLocalBareRepo
- Three RED e2e files (adopt/addrepo/upload) compile and fail on behavior
- Three production package skeletons exist (adopter/repoclone/uploader) with Deps/enum/sentinel types
- Nil-guard extension is RED (zero-value form) and references all new Deps fields; Plan 06 rewire noted
- Whole module builds; pre-commit hooks pass without --no-verify
</success_criteria>

<output>
Create `.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-01-SUMMARY.md` when done
</output>
