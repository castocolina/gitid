---
phase: 05.7-complete-v1-0-product-features-in-tui
plan: "06"
subsystem: cli-wiring
tags: [cli, tui, adopt, repoclone, uploader, wiring, d16-nil-guard]
dependency_graph:
  requires: ["05.7-02", "05.7-03", "05.7-04"]
  provides: [adopt-cmd, add-repo-cmd, copy-upload-keys, tui-8-value-wiring]
  affects: [cmd/gitid/main.go, tui/deps.go, tui/model.go, tui/tui.go, tui/wiring_test.go]
tech_stack:
  added: []
  patterns:
    - "3-arg WriteIncludeIf closure capturing gitconfigPath in buildAdoptDeps/buildTUIAdopterDeps"
    - "filewriter.CopyFile adaptor wrapper — drops backupPath, calls EnsureDir before copy"
    - "uploader.Detect → CommandPreview → UploadKey (only .pub) in runCopy --upload-keys"
    - "8-value buildTUIDeps() return widening pattern (4 existing + 3 new Deps + err)"
    - "D-16 nil-guard drives live buildTUIDeps() not fake seams — anti-blindspot mandate"
key_files:
  created:
    - cmd/gitid/adopt.go
    - cmd/gitid/adopt_test.go
    - cmd/gitid/addrepo.go
    - cmd/gitid/addrepo_test.go
  modified:
    - cmd/gitid/copy.go
    - cmd/gitid/copy_test.go
    - cmd/gitid/main.go
    - tui/deps.go
    - tui/model.go
    - tui/tui.go
    - tui/wiring_test.go
decisions:
  - "fakeUploaderDeps factory pattern over struct — lets tests vary ghPresent/authOK without modifying the tested code path"
  - "seedMinimalCopyIdentity helper deduplicates SSH config + gitconfig + .pub setup across upload unit tests"
  - "TestBuildTUIDepsNilGuard_Phase57 is the canonical D-16 gate for adopter/repoclone/uploader seams; it must drive live buildTUIDeps(), not injected fakes"
  - "yes bool parameter renamed to _ bool in runAddRepo (revive: unused-parameter); feature is wired in repoclone directly"
  - "EnsureDir called before CopyFile in the adaptor closure (parent dir may not exist on first adopt)"
metrics:
  duration: "9 minutes"
  completed: "2026-06-21T13:01:07Z"
  tasks_completed: 2
  files_changed: 11
---

# Phase 05.7 Plan 06: CLI Wiring — adopt, add repo, copy --upload-keys + TUI 8-value wiring Summary

**One-liner:** Registers `gitid adopt`, `gitid add repo` (new top-level group), `gitid identity copy --upload-keys` with live deps, and widens `buildTUIDeps()` to 8 values with D-16 nil-guard GREEN.

## What Was Built

### Task 1: `gitid adopt` + NEW top-level `add` group with `add repo`

**`cmd/gitid/adopt.go`** — registers `gitid adopt <fragment-path>` at the root level.
- `runAdopt` derives the identity name from `--name` or the fragment filename suffix
- Builds a default `gitdir:` match for `--method migrate` and `--method reference`
- `buildAdoptDeps()` wires all 7 `adopter.Deps` fields:
  - `CopyFile` adaptor calls `filewriter.EnsureDir` then `filewriter.CopyFile` (drops `backupPath`)
  - `WriteIncludeIf` 3-arg closure captures `gitconfigPath` and calls `gitconfig.WriteIncludeIf(gitconfigPath, id, fragPath, matches)`
  - `ListCandidates` wired to `adopter.ListCandidatesFromHome`

**`cmd/gitid/addrepo.go`** — registers `gitid add repo <url>` under a NEW top-level `add` group.
- `runAddRepo` detects the provider via `repoclone.ProviderFromURL` (graceful — skips alias lookup if error)
- `findAliasForProvider` 3-pass lookup: exact match → contains → hostname suffix
- `filepath.Base` fallback for `DestPath` on file:// or non-standard URLs
- `buildRepoCloneDeps()` wires `{os.Stat, repoclone.LiveClone, repoclone.LivePull, os.UserHomeDir}`
- No `os/exec` in either file — exec lives in `repoclone` package (T-05.7-06-01 mitigated)

**`cmd/gitid/main.go`** — added at the root level:
```go
root.AddCommand(newAdoptCmd())
add := &cobra.Command{Use: "add", Short: "Add resources to gitid management (repositories, …)"}
add.AddCommand(newAddRepoCmd())
root.AddCommand(add)
```

### Task 2: `copy --upload-keys`, `tuiDeps` widening, D-16 nil-guard GREEN

**`cmd/gitid/copy.go`** — `--upload-keys` / `--yes` flags on both `copy` and `identity copy`.
- `runCopy` after printing manual instructions: when `uploadKeys=true`, calls `uploader.Detect(deps)`
  - `AuthAuthenticated`: prints `CommandPreview` then `UploadKey` result if `--yes`
  - `AuthNotLoggedIn`: prints "not authenticated" notice
  - `AuthToolNotFound`: no extra section (manual instructions already shown)
- `buildUploaderDeps()`: `LookPath → exec.LookPath`, `RunCmd` arg-slice closure with G204 nolint
- Security (T-05.7-06-03): only `.pub` path is passed to `UploadKey`, never the private key

**`tui/model.go`** — `tuiDeps` struct extended:
```go
adopt     adopter.Deps
repoclone repoclone.Deps
uploader  uploader.Deps
```
`newRootModelFull(docDeps, idDeps, upDeps, deleteDeps, adoptDeps, repoCloneDeps, uploaderDeps)` constructor added; original `newRootModel` kept for test use.

**`tui/deps.go`** — `buildTUIDeps()` widened to 8-value return:
```go
func buildTUIDeps() (doctor.Deps, identity.Deps, identity.UpdateDeps, identity.DeleteDeps, adopter.Deps, repoclone.Deps, uploader.Deps, error)
```
Three new sub-builders added: `buildTUIAdopterDeps`, `buildTUIRepoCloneDeps`, `buildTUIUploaderDeps`.

**`tui/wiring_test.go`** — `TestBuildTUIDepsNilGuard_Phase57` rewired from RED (Plan 01 zero-value placeholder) to GREEN — drives the live `buildTUIDeps()` 8-value return and asserts all adopter/repoclone/uploader Deps function fields are non-nil (D-13/D-16 anti-blindspot mandate).

## Commits

| Hash | Description |
|------|-------------|
| `cd2d999` | feat(05.7-06): gitid adopt + gitid add repo commands with live deps |
| `abbbb6d` | feat(05.7-06): --upload-keys on copy, tuiDeps widened to 8, TUI wiring nil-guard GREEN |

## Verification Results

| Check | Result |
|-------|--------|
| `go build ./...` | PASS |
| `make lint` (golangci-lint + gosec) | PASS — 0 issues |
| `go test ./cmd/gitid/...` | PASS |
| `go test ./tui/... -run TestBuildTUIDepsNilGuard` | PASS (base + _Phase57) |
| `go test -tags e2e -run 'TestAdopt\|TestAddRepo\|TestUpload' ./e2e/...` | PASS |
| `grep -c "os/exec" adopt.go addrepo.go` | 0 imports (comment only) |
| `gitid add --help` lists `repo` | PASS |
| Private key never passed to UploadKey | Asserted in `TestRunCopy_UploadKeys_GHAuthenticated` |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `filewriter.CopyFile` signature mismatch**
- **Found during:** Task 1 implementation
- **Issue:** `filewriter.CopyFile(src, dst)` returns `(backupPath, error)` but `adopter.Deps.CopyFile` expects `func(src, dst string) error` (1-return)
- **Fix:** Adaptor wrapper in `buildAdoptDeps` and `buildTUIAdopterDeps` drops `backupPath` and calls `filewriter.EnsureDir(filepath.Dir(dst), 0o700)` first to handle missing parent dirs
- **Files modified:** `cmd/gitid/adopt.go`, `tui/deps.go`
- **Commit:** `cd2d999`

**2. [Rule 1 - Bug] `TestAddRepo_AliasRewrite` assertion wrong alias**
- **Found during:** Task 1 e2e verification
- **Issue:** Test checked for `"personal.github.com"` but `DefaultAlias("personal","github")` = `"personal.github"` (no `.com`)
- **Fix:** Changed assertion in `e2e/addrepo_e2e_test.go` to check `"personal.github"`
- **Files modified:** `e2e/addrepo_e2e_test.go`
- **Commit:** `cd2d999`

**3. [Rule 2 - Missing] `EnsureDir` before `CopyFile` in adopt adaptor**
- **Found during:** Task 1 implementation
- **Issue:** `~/.gitconfig.d/` does not exist on first adopt; `filewriter.CopyFile` would fail with a no-such-directory error
- **Fix:** `filewriter.EnsureDir(filepath.Dir(dst), 0o700)` called before `filewriter.CopyFile` in the adaptor
- **Files modified:** `cmd/gitid/adopt.go`, `tui/deps.go`
- **Commit:** `cd2d999`

**4. [Rule 1 - Bug] lint: `revive: unused-parameter`**
- **Found during:** Task 1 pre-commit hook
- **Issues:** `yes bool` in `runAddRepo`, `path string` in test `Stat` func, `src string` in test `CopyFile` func
- **Fix:** Renamed to `_ bool`, `_ string`, `_ string` respectively
- **Files modified:** `cmd/gitid/addrepo.go`, `cmd/gitid/adopt_test.go`, `cmd/gitid/addrepo_test.go`
- **Commit:** `cd2d999`

**5. [Rule 1 - Bug] `TestRunAddRepo_LocalClone` used `file://` URL**
- **Found during:** Task 1 unit test run
- **Issue:** `repoclone.DestPath` fails for `file://` URLs (empty hostname); unit test was using `file:///tmp/repo` to avoid real network calls
- **Fix:** Changed unit test URL to `"https://github.com/org/myrepo"` (no real network — clone is faked via `fakeRepoCloneDeps`)
- **Files modified:** `cmd/gitid/addrepo_test.go`
- **Commit:** `cd2d999`

**6. [Rule 1 - Bug] Unused imports in `tui/wiring_test.go` after Task 2**
- **Found during:** Task 2 lint run
- **Issue:** Zero-value variable declarations for `adopter.Deps`, `repoclone.Deps`, `uploader.Deps` removed from `TestBuildTUIDepsNilGuard_Phase57` (now drives live `buildTUIDeps()`) left those 3 imports unused
- **Fix:** Removed the 3 unused imports from `wiring_test.go`
- **Files modified:** `tui/wiring_test.go`
- **Commit:** `abbbb6d`

## Threat Mitigations Applied

| Threat | Status |
|--------|--------|
| T-05.7-06-01: exec via arg-slices only, no shell | MITIGATED — `cmd/gitid/adopt.go` and `addrepo.go` import no `os/exec`; exec lives in `repoclone`/`uploader` packages |
| T-05.7-06-02: config corruption via adopt write | MITIGATED — adopter routes through `filewriter` (backup → atomic); `WriteIncludeIf` is parse-safe |
| T-05.7-06-03: private key upload | MITIGATED — only `.pub` path passed to `uploader.UploadKey`; asserted in `TestRunCopy_UploadKeys_GHAuthenticated` |
| T-05.7-06-04: nil seam in live TUI | MITIGATED — `TestBuildTUIDepsNilGuard_Phase57` drives live `buildTUIDeps()` (D-13/D-16) |

## Known Stubs

None — all wired functions call live implementations. The `tuiDeps.adopt`, `.repoclone`, `.uploader` fields are populated but not yet dispatched to modal screens (that is Plan 07's responsibility).

## Self-Check: PASSED
