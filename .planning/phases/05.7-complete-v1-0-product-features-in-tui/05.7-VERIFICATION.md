---
phase: 05.7-complete-v1-0-product-features-in-tui
verified: 2026-06-21T00:00:00Z
status: human_needed
score: 5/5
overrides_applied: 0
human_verification:
  - test: "Run the live gitid binary in a real SSH terminal and confirm the match-strategy expanded selector shows radio buttons (gitdir / hasconfig / both) with the live includeIf preview updating as you arrow through them."
    expected: "Expanded match-strategy field shows three radio options and the includeIf preview line updates per selection. Collapsed view shows 'gitdir (folder)' summary and compact preview."
    why_human: "The PTY e2e captures the collapsed view correctly; the expanded radio/preview rendering was not fully captured in PTY frames due to Tab-count timing (D1-COPY-04 in UI-CRITIQUE.md). Unit tests (TestWizardMatchSelectorNavigation) cover the logic but not the terminal-rendered output."
  - test: "With a real managed identity on disk, press 'c' on it, confirm the gh/glab upload-assist section appears when gh is authenticated or manual instructions appear when gh is absent."
    expected: "When gh is present and authenticated: per-key upload prompt section visible below the public key. When gh absent: 'Upload instructions' section only."
    why_human: "The PTY e2e TestUIPTY_CopyUploadAssist exercised the upload-assist path but reported the copy modal blocked by a PubPath expansion issue in the sandbox HOME (no real identity on disk). The CLI path (gitid identity copy --upload-keys) is fully e2e-tested. The TUI copy modal render needs a real identity."
---

# Phase 5.7: Complete v1.0 Product Features in TUI — Verification Report

**Phase Goal:** Close four genuinely-unbuilt v1.0 product gaps, reachable from BOTH the integrated TUI and the CLI (D-10 parity): match-strategy selector (gitdir default + hasconfig/both, live includeIf preview); ADOPT-01 adopt/migrate plain fragments; REPO-01 add repo clone+verify-pull; AUTOUP-01 assisted gh/glab public-key upload.
**Verified:** 2026-06-21
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | TUI create wizard exposes gitdir (default), hasconfig, and both as selectable match strategies with a live includeIf preview; hasconfig auto-derives as `git@<alias>:*/**`; both writes two OR-applied includeIf blocks; `--match gitdir\|hasconfig\|both` mirrors on the CLI | VERIFIED | `tui/wizard.go` `liveIncludeIfPreview()` at line 361; `matchStrategy` enum strategyGitdir/Hasconfig/Both; `cmd/gitid/match.go` `matchFromFlag()` + `--match` flag in `cmd/gitid/add.go` line 56; e2e `TestUIPTY_MatchStrategySelector` PASS |
| 2 | `gitid adopt <path>` and TUI unmanaged-pane Adopt affordance migrate or reference a plain-style fragment; migrate copies into `~/.gitconfig.d/<name>` and repoints the managed includeIf; NEVER deletes the original | VERIFIED | `internal/adopter/adopter.go` `Adopt()` — zero calls to `BackupAndRemove` inside `Adopt()`; `cmd/gitid/adopt.go` `runAdopt()` with "Original fragment preserved" output; `tui/adopt.go` `adoptModel`; e2e `TestAdopt_Migrate` and `TestAdopt_Reference` PASS |
| 3 | `gitid add repo <url>` and TUI Add Repo modal detect provider, rewrite URL to alias, clone into `~/git/<client>/<repo>`, verify with `git pull`; both clone and pull output shown; no-match launches inline create | VERIFIED | `internal/repoclone/repoclone.go` `Clone()`+`Pull()` with `ErrOptionLikeURL` guard; `cmd/gitid/addrepo.go` `runAddRepo()` always prints clone+pull lines; `tui/addrepo.go` multi-step state machine; e2e `TestAddRepo_LocalClone` and `TestAddRepo_AliasRewrite` PASS |
| 4 | When gh/glab is present and authenticated, copy modal + wizard step 3 + `gitid identity copy --upload-keys` offer per-key upload with explicit confirmation; shown command equals run command; absent/unauthenticated falls back to manual instructions and never gates create/copy | VERIFIED | `internal/uploader/uploader.go` `CommandPreview()` and `buildArgs()` share the same arg slice (shown == run); `cmd/gitid/copy.go` `runCopy()` with `--upload-keys`; `tui/copy.go` upload-assist section; e2e `TestUpload_GHOk`, `TestUpload_GHAuthFail`, `TestUpload_GHNotPresent` PASS; private key NEVER passed to `UploadKey` (only `pubPath`) |
| 5 | Every feature has unit-TDD coverage AND at least one e2e test that drives the real gitid binary in a sandbox HOME — closing the recurring injected-seam blindspot (D-13/D-16) | VERIFIED | `TestBuildTUIDepsNilGuard_Phase57` PASS (drives live `buildTUIDeps()` 8-value return); PTY e2e `TestUIPTY_*` (7 tests) PASS against real binary over pseudo-terminal; `TestAdopt_Migrate`, `TestAddRepo_LocalClone`, `TestUpload_GHOk` and siblings PASS; `make test` all 17 packages green; `make test-e2e` green |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/adopter/adopter.go` | Adopter package with Deps/enum/sentinel types, never-delete Adopt | VERIFIED | 223 lines; `Adopt()` has zero `BackupAndRemove` calls; `ListCandidates` uses `filepath.Glob`; 86.9% test coverage |
| `internal/repoclone/repoclone.go` | repoclone with ErrOptionLikeURL guard, arg-slice exec | VERIFIED | 243 lines; `ErrOptionLikeURL` guard at line 185; `liveClone` uses `exec.Command("git","clone","--",...)` |
| `internal/uploader/uploader.go` | uploader with Detect/UploadKey/CommandPreview, shown==run | VERIFIED | 230 lines; `buildArgs` shared between `UploadKey` and `CommandPreview`; LookPath+RunCmd Deps |
| `tui/adopt.go` | Adopt modal TUI surface | VERIFIED | 4-step state machine; no os/exec in file; dispatches through `deps.adopt` |
| `tui/addrepo.go` | Add Repo modal TUI surface | VERIFIED | 9-step state machine; dispatches through `deps.repoclone` |
| `tui/copy.go` | Copy modal with upload-assist section | VERIFIED | Upload-assist section present; only `pubPath` passed to uploader |
| `tui/deps.go` | `buildTUIDeps` returns 8 values including adopt/repoclone/uploader Deps | VERIFIED | `buildTUIDeps()` at line 40 returns 8 values; `buildTUIAdopterDeps`, `buildTUIRepoCloneDeps`, `buildTUIUploaderDeps` all wired |
| `tui/model.go` — `tuiDeps` struct | carries adopt/repoclone/uploader fields | VERIFIED | Lines 66-68: `adopt adopter.Deps`, `repoclone repoclone.Deps`, `uploader uploader.Deps` |
| `cmd/gitid/adopt.go` | CLI `gitid adopt` command | VERIFIED | `newAdoptCmd()` registered in `newRootCmd()` at line 127 |
| `cmd/gitid/addrepo.go` | CLI `gitid add repo` command | VERIFIED | `newAddRepoCmd()` registered under `add` group in `newRootCmd()` lines 130-136 |
| `cmd/gitid/add.go` | `--match gitdir\|hasconfig\|both` flag | VERIFIED | `--match` flag at line 56; `matchFromFlag()` + `buildMatches()` in `cmd/gitid/match.go` |
| `e2e/adopt_e2e_test.go` | e2e for ADOPT-01 | VERIFIED | `TestAdopt_Migrate` and `TestAdopt_Reference` PASS |
| `e2e/addrepo_e2e_test.go` | e2e for REPO-01 | VERIFIED | `TestAddRepo_LocalClone` and `TestAddRepo_AliasRewrite` PASS |
| `e2e/upload_e2e_test.go` | e2e for AUTOUP-01 | VERIFIED | `TestUpload_GHOk`, `TestUpload_GHAuthFail`, `TestUpload_GHNotPresent` PASS |
| `e2e/ui_pty_e2e_test.go` | PTY-driven e2e for all four surfaces (D-13) | VERIFIED | 7 PTY tests (`TestUIPTY_*`) PASS; real binary over pseudo-terminal; raw keystroke input |
| `tui/wiring_test.go` `TestBuildTUIDepsNilGuard_Phase57` | D-16 nil-guard GREEN driving live `buildTUIDeps` | VERIFIED | Test drives real 8-value `buildTUIDeps()` return; asserts all 13 new Deps function fields non-nil; PASS |
| `.planning/phases/05.7-.../05.7-UI-CRITIQUE.md` | UI/UX design critique from real PTY frames | VERIFIED | 200+ lines; `agent-ui-ux-designer` role; 11 PTY frames reviewed; no CRITICAL/HIGH findings |
| `.planning/phases/05.7-.../05.7-CODE-REVIEW.md` | Code review over full Phase 5.7 diff | VERIFIED | `requesting-code-review` role; 25 commits, 66 files reviewed; no CRITICAL/HIGH/BLOCKING findings |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `tui/deps.go` `buildTUIAdopterDeps` | `adopter.Deps` | `filewriter.Write`, `filewriter.CopyFile`, `gitconfig.WriteIncludeIf` | VERIFIED | All 7 fields populated; closure captures `gitconfigPath` |
| `tui/deps.go` `buildTUIRepoCloneDeps` | `repoclone.Deps` | `os.Stat`, `repoclone.LiveClone`, `repoclone.LivePull`, `os.UserHomeDir` | VERIFIED | All 4 fields populated |
| `tui/deps.go` `buildTUIUploaderDeps` | `uploader.Deps` | `exec.LookPath`, `exec.Command` arg-slice RunCmd | VERIFIED | Both fields populated; arg-slice form confirmed |
| `tui/model.go` `tuiDeps` | `tui/adopt.go`, `tui/addrepo.go`, `tui/copy.go` | `deps.adopt`, `deps.repoclone`, `deps.uploader` field access | VERIFIED | Fields at lines 66-68 of model.go; used in modal dispatch |
| `cmd/gitid/main.go` `newRootCmd` | `newAdoptCmd()`, `newAddRepoCmd()` | `root.AddCommand`, `add.AddCommand` | VERIFIED | Lines 127 and 130-136 of main.go |
| `internal/adopter/adopter.go` `Adopt()` | never removes source | absence of `BackupAndRemove` call | VERIFIED | `grep deps.BackupAndRemove adopter.go` returns 0 hits inside Adopt body |
| `internal/uploader/uploader.go` `UploadKey()` | only `.pub` path | `pubPath` argument convention | VERIFIED | `cmd/gitid/copy.go` line 115: "SECURITY: only pubPath (.pub) is ever passed to uploader" |
| `internal/repoclone/repoclone.go` `Clone()` | rejects leading-'-' URL | `ErrOptionLikeURL` guard | VERIFIED | Line 185: `if strings.HasPrefix(cloneURL, "-")` before any exec |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `tui/adopt.go` | `fragment` paths list | `deps.adopt.ListCandidates(home)` → `adopter.ListCandidatesFromHome` → `filepath.Glob("~/.gitconfig_*")` | Yes — OS glob | FLOWING |
| `tui/addrepo.go` | clone output lines | `deps.repoclone.Clone()` → `repoclone.LiveClone()` → `exec.Command("git","clone",...)` | Yes — real git exec | FLOWING |
| `tui/copy.go` | upload-assist section | `uploader.Detect(deps)` → `deps.LookPath("gh")` → `exec.LookPath` | Yes — real PATH probe | FLOWING |
| `tui/wizard.go` | `liveIncludeIfPreview` | `matchSel` + `matchGitdirVal.Value()` + `matchHasconfigVal.Value()` → `gitconfig.RenderIncludeIf()` | Yes — live from text fields | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `make test` (all 17 packages) | `make test` | 17/17 packages PASS, no cached | PASS |
| `make lint` | `make lint` | `0 issues` | PASS |
| `make test-e2e` (15 e2e tests) | `make test-e2e` | All PASS in 31.8s | PASS |
| `TestBuildTUIDepsNilGuard_Phase57` isolated | `go test ./tui/... -run TestBuildTUIDepsNilGuard_Phase57 -v` | PASS | PASS |
| No `sh -c` in new files | `grep -rn "sh -c" internal/ tui/ cmd/` | 0 matches | PASS |
| `adopter.Adopt` never calls `BackupAndRemove` | `grep deps.BackupAndRemove internal/adopter/adopter.go` | 0 hits in function body | PASS |
| `repoclone.Clone` rejects option-like URLs | `ErrOptionLikeURL` guard at line 185 of repoclone.go | `strings.HasPrefix(cloneURL, "-")` → return error | PASS |
| PTY e2e all surfaces | `go test -tags e2e -run TestUIPTY ./e2e/...` | 7/7 PASS | PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| ADOPT-01 | 05.7-02, 05.7-06, 05.7-07 | Fragment adoption (migrate/reference, never-delete) | SATISFIED | `internal/adopter`, `cmd/gitid/adopt.go`, `tui/adopt.go`, e2e TestAdopt_* PASS |
| REPO-01 | 05.7-03, 05.7-06, 05.7-07 | `gitid add repo <url>` clone+verify-pull | SATISFIED | `internal/repoclone`, `cmd/gitid/addrepo.go`, `tui/addrepo.go`, e2e TestAddRepo_* PASS |
| AUTOUP-01 | 05.7-04, 05.7-06, 05.7-07 | Assisted gh/glab key upload (detect+prompt only) | SATISFIED | `internal/uploader`, `cmd/gitid/copy.go --upload-keys`, `tui/copy.go`, e2e TestUpload_* PASS |
| GIT-02 (D-06 match-strategy) | 05.7-05, 05.7-06 | gitdir/hasconfig/both combinable, --match CLI flag | SATISFIED | `cmd/gitid/match.go` `buildMatches()`/`matchFromFlag()`, `tui/wizard.go` `matchStrategy` enum + `liveIncludeIfPreview()` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `internal/uploader/uploader.go` | 77-80 | Open Question A2 comment: `glab --usage-type "auth"` vs `"auth_and_signing"` | INFO | Non-blocking; conservative "auth" value used with rationale; no `TBD`/`FIXME` marker |

No `TBD`, `FIXME`, or `XXX` markers in any Phase 5.7 production files.

### Human Verification Required

#### 1. Match-Strategy Expanded Selector — Live Radio Labels + includeIf Preview

**Test:** Run `./bin/gitid` (no args, requires TTY), press `a` to open the Create Identity wizard, Tab to the Match Strategy field, then arrow Down/Up through gitdir/hasconfig/both options.
**Expected:** The expanded selector shows three radio buttons with labels "folder (gitdir)", "repo URL (hasconfig)", "both". The `includeIf preview:` block below updates live with the correct `[includeIf "gitdir:~/git/<name>/"]` or `[includeIf "hasconfig:remote.*.url:git@<alias>:*/**"]` preview as you cycle through options.
**Why human:** The PTY e2e (`TestUIPTY_MatchStrategySelector`) exercised the collapsed view and strategy cycling correctly, but the test did not capture the expanded radio panel in a PTY frame due to Tab-count timing (UI-CRITIQUE D1-COPY-04). Unit tests (`TestWizardMatchSelectorNavigation` in `tui/wizard_test.go`) cover the logic; this verifies the rendered terminal output.

#### 2. TUI Copy Modal with Upload-Assist — Real Identity on Disk

**Test:** With a real managed identity on disk, open `./bin/gitid`, select the identity in the sidebar, press `c` to open the copy modal. Test two cases: (a) `gh` present and authenticated on PATH, (b) `gh` absent.
**Expected:** (a) Upload-assist section shows "Detected: gh (authenticated)" with per-key confirmation prompts. (b) Manual upload instructions shown only; no assisted-upload section.
**Why human:** `TestUIPTY_CopyUploadAssist` ran against the sandbox HOME where no real identity was created before the copy modal opened (the sandbox `PubPath` expansion issue noted in UI-CRITIQUE.md). The CLI path (`gitid identity copy --upload-keys`) is fully e2e-verified. The TUI modal render requires a real identity on disk to fully exercise the pubPath read and upload-assist render path.

---

## Summary

All 5 roadmap success criteria are VERIFIED with direct codebase evidence. All three build gates (`make test`, `make test-e2e`, `make lint`) are green. Security invariants confirmed:

- No `sh -c` anywhere (grep across internal/ + tui/ + cmd/ returns 0)
- `adopter.Adopt()` never calls `BackupAndRemove` inside its body (never auto-deletes)
- Only `.pub` path is ever passed to `uploader.UploadKey` (private key never uploaded)
- `repoclone.Clone` rejects option-like URLs (leading `-`) via `ErrOptionLikeURL` before any exec
- `ListCandidates` uses `filepath.Glob` exclusively (does not import doctor)

The D-16/D-13 blindspot is closed: `TestBuildTUIDepsNilGuard_Phase57` drives the real 8-value `buildTUIDeps()` return, and 7 PTY e2e tests drive the real binary over a pseudo-terminal with raw keystroke input.

Two items require human verification: the expanded match-strategy radio panel in the TUI wizard (PTY frames captured only the collapsed view), and the copy modal upload-assist section with a real identity on disk. Both are cosmetic/render verifications; the underlying logic is unit-tested and CLI e2e-verified. Phase completion is gated on the human checks per plan (Plan 08, Task 4).

---

_Verified: 2026-06-21_
_Verifier: Claude (gsd-verifier)_
