---
phase: 05.7-complete-v1-0-product-features-in-tui
plan: 06
type: execute
wave: 2
depends_on: ["05.7-02", "05.7-03", "05.7-04"]
files_modified:
  - cmd/gitid/adopt.go
  - cmd/gitid/adopt_test.go
  - cmd/gitid/addrepo.go
  - cmd/gitid/addrepo_test.go
  - cmd/gitid/copy.go
  - cmd/gitid/copy_test.go
  - cmd/gitid/main.go
  - tui/deps.go
  - tui/model.go
  - tui/wiring_test.go
autonomous: true
requirements: [ADOPT-01, REPO-01, AUTOUP-01]
must_haves:
  truths:
    - "A NEW top-level `add` cobra.Command is created in main.go (no `add` group exists today — newAddCmd is registered under `identity`); `add repo` registers under it"
    - "gitid adopt <path> --method migrate|reference --name <name> --yes runs the adopter flow non-interactively"
    - "gitid add repo <url> --client <name> --yes detects the provider, rewrites to the alias, clones into ~/git/<client>/<repo>, and runs verify-pull, showing both outputs"
    - "gitid identity copy <name> --upload-keys --yes detects gh/glab and uploads (or falls back to manual instructions when absent/unauthenticated)"
    - "tui/model.go tuiDeps gains adopt adopter.Deps, repoclone repoclone.Deps, uploader uploader.Deps fields; buildTUIDeps() returns 8 values with every new function field non-nil"
    - "The adopt/addrepo/upload e2e tests pass at the CLI level (real binary, sandbox HOME); TestBuildTUIDepsNilGuard_Phase57 is rewired from Plan 01's zero-value form to drive the live buildTUIDeps() return and is GREEN"
  artifacts:
    - path: "cmd/gitid/adopt.go"
      provides: "gitid adopt command + buildAdoptDeps live wiring"
      contains: "func newAdoptCmd"
    - path: "cmd/gitid/addrepo.go"
      provides: "gitid add repo command + buildRepoCloneDeps live wiring"
      contains: "func newAddRepoCmd"
    - path: "cmd/gitid/main.go"
      provides: "NEW top-level add cobra.Command group + adopt registration"
      contains: "newAddRepoCmd"
    - path: "tui/model.go"
      provides: "tuiDeps struct extended with adopt/repoclone/uploader Deps fields"
      contains: "adopter.Deps"
    - path: "tui/deps.go"
      provides: "buildTUIAdopterDeps/buildTUIRepoCloneDeps/buildTUIUploaderDeps + extended 8-value buildTUIDeps"
      contains: "buildTUIAdopterDeps"
  key_links:
    - from: "cmd/gitid/main.go"
      to: "new top-level add group then newAddRepoCmd; root newAdoptCmd"
      via: "AddCommand registration"
      pattern: "newAdoptCmd|newAddRepoCmd"
    - from: "tui/deps.go buildTUIDeps"
      to: "tui/model.go tuiDeps adopt/repoclone/uploader fields"
      via: "extended 8-value return populating the new struct fields"
      pattern: "buildTUIAdopterDeps|buildTUIRepoCloneDeps|buildTUIUploaderDeps"
---

<objective>
Wire the three new core packages into the REAL program entry on BOTH surfaces
(D-10 parity, D-13 real-entry): add the gitid adopt, gitid add repo, and
gitid identity copy --upload-keys Cobra commands with live Deps wiring, and extend
tui/deps.go buildTUIDeps() to return live adopter.Deps, repoclone.Deps, and
uploader.Deps PLUS add the three matching fields to the tuiDeps struct in
tui/model.go, so the TUI modals (Plan 07) have non-nil seams. This plan OWNS the
shared cmd/gitid/main.go, tui/deps.go, tui/model.go, and tui/wiring_test.go files.
It turns the Plan 01 adopt/addrepo/upload e2e tests GREEN at the CLI level and
rewires TestBuildTUIDepsNilGuard_Phase57 from its Plan-01 zero-value RED form to
the live-wiring GREEN form.

Live-code facts this plan honors:
- There is NO top-level add cobra command today. cmd/gitid/main.go registers
  newAddCmd() under the identity group (line 84). This plan CREATES a NEW
  top-level add group and registers add repo under it (REVIEWS.md #7).
- tuiDeps lives in tui/model.go (line 47), NOT tui/deps.go. The three new Deps
  fields are added to that struct (REVIEWS.md #8); tui/model.go is in
  files_modified.
- buildTUIDeps() returns 5 values today; this plan widens it to 8 and updates
  every caller's destructure.
- The adopter Deps.WriteIncludeIf seam is 3-arg (id, fragPath, matches); the live
  closure captures gitconfigPath and calls the underlying 4-arg
  gitconfig.WriteIncludeIf.

Purpose: This is the integration seam that closes the recurring injected-seam
blindspot — the e2e tests drive the actual gitid binary, and the nil-guard asserts
the live wiring (not a fake) is complete before any TUI modal runs.
Output: Three CLI commands (adopt at root; add repo under a NEW top-level add
group; copy --upload-keys) + live deps wiring in both tui/deps.go and tui/model.go;
CLI e2e + nil-guard GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-UI-SPEC.md
@CLAUDE.md
@recipes/gitconfig.recipe
@recipes/ssh-config.recipe
@cmd/gitid/add.go
@cmd/gitid/copy.go
@cmd/gitid/main.go
@tui/deps.go
@tui/model.go
</context>

<artifacts_this_phase_produces>
New symbols (exclude from drift checks): main.newAdoptCmd, main.runAdopt,
main.buildAdoptDeps, main.newAddRepoCmd, main.runAddRepo, main.buildRepoCloneDeps,
main.buildUploaderDeps, the NEW top-level add cobra group in newRootCmd, the
--upload-keys/--yes flags on copy, the --method/--name/--yes flags on adopt, the
--client/--dest/--yes flags on add repo, tui.buildTUIAdopterDeps,
tui.buildTUIRepoCloneDeps, tui.buildTUIUploaderDeps, the extended buildTUIDeps()
8-value return, and the three new tuiDeps fields (adopt, repoclone, uploader) in
tui/model.go.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gitid adopt + NEW top-level add group with add repo, live deps + main.go registration</name>
  <read_first>
    - cmd/gitid/add.go (newAddCmd thin-command + buildDeps live-wiring pattern lines 36-65, 560-600)
    - cmd/gitid/main.go (newRootCmd command tree lines 71-127 — identity, baseline, host groups register here; CONFIRM no top-level add group exists — newAddCmd is under identity at line 84)
    - internal/adopter/adopter.go + internal/repoclone/repoclone.go (the Deps structs + Adopt/Clone/Pull signatures from Plans 02/03; note the 3-arg Deps.WriteIncludeIf and the internal-base Clone guard)
    - internal/sshconfig (managed-host reader for provider to alias lookup used by add repo)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md (cmd/gitid/adopt.go + cmd/gitid/addrepo.go sections — exact cobra construction + buildAdoptDeps wiring; the WriteIncludeIf closure captures gitconfigPath)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-UI-SPEC.md (New Surface 2 CLI parity; New Surface 3 CLI parity + clone/pull output always printed)
  </read_first>
  <behavior>
    - Test (adopt_test.go): gitid adopt frag --method migrate --name work --yes calls adopter.Adopt(migrate) and prints the result steps
    - Test (adopt_test.go): --method reference selects AdoptReferenceInPlace
    - Test (addrepo_test.go): gitid add repo url --client personal --yes resolves alias, prints rewritten URL + clone + pull output
    - Test (addrepo_test.go): no matching identity → non-interactive errors clearly (CLI cannot inline-create without a TTY; --yes path documents the no-match exit; TUI handles inline create in Plan 07)
    - Test (main): gitid add --help shows repo as a subcommand (the NEW top-level add group exists)
    - e2e (from Plan 01): TestAdopt_Migrate, TestAdopt_Reference, TestAddRepo_LocalClone, TestAddRepo_AliasRewrite pass
  </behavior>
  <action>
    Create cmd/gitid/adopt.go with newAdoptCmd (Use "adopt <fragment-path>", ExactArgs(1), flags
    --method default migrate, --name, --yes) delegating to runAdopt(out, path, method, name, yes,
    buildAdoptDeps). runAdopt resolves the identity name (flag, then adopter.MatchIdentityName
    fallback), confirms unless --yes, calls adopter.Adopt, and prints each backup/copy/includeIf
    step; for migrate it offers remove-original ONLY on explicit --yes plus confirm, never auto.
    buildAdoptDeps wires adopter.Deps from filewriter + gitconfig per PATTERNS.md; the
    WriteIncludeIf field is the 3-arg seam closure that captures gitconfigPath and calls the
    underlying gitconfig.WriteIncludeIf(gitconfigPath, id, fragPath, matches); CopyFile wires to
    filewriter.CopyFile; ListCandidates wires to adopter.ListCandidates.
    Create cmd/gitid/addrepo.go with newAddRepoCmd (Use "repo <url>", ExactArgs(1), flags
    --client, --dest, --yes) delegating to runAddRepo. runAddRepo calls repoclone.ProviderFromURL,
    looks up a managed identity/alias for that provider via the sshconfig reader,
    repoclone.RewriteToAlias, repoclone.DestPath (~/git/<client>/<repo>, D-09), repoclone.Clone,
    then repoclone.Pull; it ALWAYS prints both the rewritten command and the clone+pull output
    (PRD §4.7). buildRepoCloneDeps wires Stat to os.Stat, Clone to repoclone.liveClone, Pull to
    repoclone.livePull, UserHomeDir to os.UserHomeDir.
    In main.go newRootCmd register adopt at the root (root.AddCommand(newAdoptCmd())) and CREATE A
    NEW top-level add group — one does NOT exist; newAddCmd is under identity. Construct a cobra
    command with Use "add" and a short description, add newAddRepoCmd() to it, then add that group
    to root. Do NOT touch the existing identity add registration. All exec stays inside repoclone
    (arg slices); the command layer never calls os/exec directly.
  </action>
  <verify>
    <automated>go test ./cmd/gitid/... -run 'TestAdopt|TestAddRepo' -count=1 && go test -tags e2e -run 'TestAdopt|TestAddRepo' ./e2e/... 2>&1 | tail -8; grep -c "os/exec" cmd/gitid/adopt.go cmd/gitid/addrepo.go</automated>
  </verify>
  <acceptance_criteria>
    - go test -tags e2e -run 'TestAdopt|TestAddRepo' ./e2e/... PASSES (real binary, sandbox HOME)
    - gitid adopt --help and gitid add repo --help show the flags (cobra registration works); gitid add --help lists repo
    - add repo prints BOTH the clone output and the pull output (asserted in e2e/unit)
    - grep -c "os/exec" cmd/gitid/adopt.go cmd/gitid/addrepo.go returns 0 for both (exec lives in repoclone)
    - make lint passes without --no-verify
  </acceptance_criteria>
  <done>adopt registered at root + a NEW top-level add group hosts add repo, both with live deps; their e2e tests GREEN; migrate never auto-deletes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: copy --upload-keys (gh/glab assist) + tuiDeps fields + buildTUIDeps 8-value + nil-guard GREEN</name>
  <read_first>
    - cmd/gitid/copy.go (runCopy lines 60-128 — clipboard + upload.Instructions; the --upload-keys insertion point; newCopyCmd line 21 + newIdentityCopyCmd line 34)
    - internal/uploader/uploader.go (Detect/UploadKey/CommandPreview from Plan 04 — the live RunCmd closure is defined HERE and in tui/deps.go, not in the package)
    - tui/model.go (tuiDeps struct line 47 — this is where the three new Deps fields go; modalKind consts; rootModel)
    - tui/deps.go (buildTUIDeps line 31 returning 4 Deps+err; buildTUIDeleteDeps lines 66-111 wiring analog; the 8-value extension target)
    - tui/wiring_test.go (TestBuildTUIDepsNilGuard lines 132-205 destructures 5 values; the _Phase57 zero-value RED form from Plan 01 to rewire)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-PATTERNS.md (tui/deps.go extend; tui/copy.go upload-assist; tui/wiring_test.go _Phase57 live form)
    - .planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-UI-SPEC.md (4a/4c — detect+prompt, per-key confirm, manual fallback, non-blocking; --upload-keys/--yes flags)
  </read_first>
  <behavior>
    - Test (copy_test.go): gitid identity copy name --upload-keys --yes with gh authenticated invokes UploadKey for auth + signing keys (recorded fake)
    - Test (copy_test.go): gh not present → no upload section, manual instructions still printed (non-blocking)
    - Test (copy_test.go): gh present but AuthNotLoggedIn → prints the not-authenticated notice + manual fallback, no upload attempt
    - Test (wiring): TestBuildTUIDepsNilGuard_Phase57 GREEN — all adopter/repoclone/uploader Deps fields non-nil from the LIVE buildTUIDeps() return
    - e2e (from Plan 01): TestUpload_GHOk, TestUpload_GHAuthFail, TestUpload_GHNotPresent pass
  </behavior>
  <action>
    Add --upload-keys and --yes flags to the copy commands (newCopyCmd + newIdentityCopyCmd). In
    runCopy, after printing manual instructions (unchanged default), when --upload-keys is set:
    call uploader.Detect(deps); if AuthAuthenticated, for each of the auth and signing keys prompt
    per-key (skip the prompt under --yes) and call uploader.UploadKey, printing CommandPreview then
    the result; if AuthNotLoggedIn print the not-authenticated notice; if AuthToolNotFound print
    nothing extra (manual instructions already shown). Upload NEVER gates the copy (D-11). Add
    buildUploaderDeps with LookPath wired to exec.LookPath and RunCmd wired to an arg-slice closure
    wrapping exec.Command and classifying *exec.ExitError, with a gosec G204 nolint comment.
    Extend tui/model.go: add the three fields to the tuiDeps struct — adopt adopter.Deps,
    repoclone repoclone.Deps, uploader uploader.Deps — importing the three internal packages.
    Extend tui/deps.go: add the three build sub-functions (buildTUIAdopterDeps,
    buildTUIRepoCloneDeps, buildTUIUploaderDeps) per PATTERNS.md, widen buildTUIDeps() to the
    8-value return (4 existing + adopter + repoclone + uploader + err), populate the new tuiDeps
    fields wherever the rootModel/tuiDeps is constructed, and update every caller of buildTUIDeps
    (tui.Run / rootModel construction / the existing TestBuildTUIDepsNilGuard 5-value destructure)
    to the new arity. Finally, rewire TestBuildTUIDepsNilGuard_Phase57 from its Plan-01 zero-value
    form to drive the real 8-value buildTUIDeps() return tuple (remove the zero-value placeholder
    and the "rewire in Plan 06" comment) — it must now be GREEN with every new function field
    non-nil.
  </action>
  <verify>
    <automated>go build ./... && go test ./cmd/gitid/... -run 'TestCopy|TestUpload' -count=1 && go test ./tui/... -run TestBuildTUIDepsNilGuard -count=1 && go test -tags e2e -run TestUpload ./e2e/... 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - go test ./tui/... -run TestBuildTUIDepsNilGuard (both base + _Phase57) PASSES — _Phase57 now drives the LIVE buildTUIDeps()
    - go test -tags e2e -run TestUpload ./e2e/... PASSES (gh-ok / auth-fail / not-present)
    - copy --upload-keys is non-blocking: with no gh, copy still succeeds and prints manual instructions
    - private key path is NEVER passed to UploadKey (only *.pub) — asserted in copy_test
    - tuiDeps in tui/model.go has the three new fields; make lint (gosec G204) passes without --no-verify; whole module go build ./... green
  </acceptance_criteria>
  <done>copy --upload-keys assists via gh/glab non-blockingly; tuiDeps in tui/model.go gains adopt/repoclone/uploader; buildTUIDeps returns 8 values with live non-nil seams; nil-guard rewired GREEN; upload e2e GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (clone URL, fragment path, identity name) to core packages | User input enters adopter/repoclone/uploader through the command layer |
| copy --upload-keys to gh/glab subprocess | Public key path + title cross into a subprocess |
| add repo to git clone/pull subprocess | Clone URL crosses into git via repoclone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.7-06-01 | Tampering | clone URL / pubPath into subprocess | mitigate | All exec lives in repoclone/uploader wiring (arg slices, no shell); cmd/gitid/adopt.go + addrepo.go never call os/exec (Task 1 grep gate) |
| T-05.7-06-02 | Tampering | config corruption via adopt write | mitigate | adopter routes through filewriter (backup → atomic → idempotent ReplaceBlock); parse-safe WriteIncludeIf |
| T-05.7-06-03 | Information Disclosure | private key uploaded | mitigate | copy passes only the .pub path to uploader.UploadKey; asserted in copy_test |
| T-05.7-06-04 | Denial of Service / blindspot | nil seam reaching live TUI | mitigate | TestBuildTUIDepsNilGuard_Phase57 drives real buildTUIDeps() and asserts every new field non-nil (D-13/D-16) |
| T-05.7-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs; gh/glab detected at runtime, not added to go.mod |
</threat_model>

<verification>
- go build ./... green; make lint (gosec) green without --no-verify
- go test -tags e2e -run 'TestAdopt|TestAddRepo|TestUpload' ./e2e/... GREEN
- go test ./tui/... -run TestBuildTUIDepsNilGuard GREEN (base + _Phase57 live form)
- add repo prints clone + pull output; copy --upload-keys non-blocking
- gitid add --help lists repo (new top-level add group)
</verification>

<success_criteria>
- gitid adopt (root) + gitid add repo (under a NEW top-level add group) + gitid identity copy --upload-keys registered with live deps
- tuiDeps in tui/model.go has adopt/repoclone/uploader fields; buildTUIDeps returns 8 values, all new Deps fields non-nil (nil-guard GREEN)
- Adopt/addrepo/upload e2e GREEN at CLI level (real binary)
- No os/exec outside repoclone/uploader wiring; no auto-delete; private key never uploaded
</success_criteria>

<output>
Create `.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-06-SUMMARY.md` when done
</output>
