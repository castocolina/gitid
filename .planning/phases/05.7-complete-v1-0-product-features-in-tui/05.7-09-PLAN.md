---
phase: 05.7-complete-v1-0-product-features-in-tui
plan: 09
type: execute
wave: 5
depends_on: []
files_modified:
  - tui/wizard.go
  - tui/wizard_test.go
autonomous: true
gap_closure: true
requirements: [TUI-04, SSH-01, SSH-02]
must_haves:
  truths:
    - "Before the write gate, the wizard shows a PREVIEW of the exact ~/.ssh/config Host <alias> block (alias, HostName, Port, IdentityFile, IdentitiesOnly yes) that WILL be written."
    - "The SSH test screen shows the exact ssh command AND the tested key path on the pre-write screen and on SUCCESS — not only on failure."
  artifacts:
    - path: "tui/wizard.go"
      provides: "renderSSHBlockPreview + always-visible command/path on the prove screen"
    - path: "tui/wizard_test.go"
      provides: "tests driving the real model Update/handleKey path for preview + command/path visibility"
  key_links:
    - from: "tui/wizard.go renderTestDetail"
      to: "tester.Result.Command + staged.TempPrivatePath"
      via: "render on PASS and pre-write, not only failure"
      pattern: "res\\.Command"
    - from: "tui/wizard.go renderSSHBlockPreview"
      to: "sshconfig.RenderHostBlock output shape"
      via: "preview the exact Host <alias> block before the gate"
      pattern: "Host "
---

<objective>
LEG 1 — SSH config legibility. Make the SSH side of the create wizard legible
BEFORE any write: surface the exact `~/.ssh/config` `Host <alias>` block that WILL
be written (G-2 preview half), and keep the SSH test command + key path visible on
the pre-write screen and on SUCCESS, not only on failure (G-3).

This plan is the first of two wizard plans (this one is preview/legibility; Plan 10
is the no-write feedback + discoverable skip-&-write-offline path). They are split
because both edit `tui/wizard.go` and must not run in parallel; this plan lands the
read-only, non-state-changing legibility surface first so Plan 10 builds on a stable
view layer.

Closes (partial): G-3 (SSH test screen command+path), G-2 (SSH-block preview half).

Purpose: The user reported "Sigo sin ver el bloque en ~/.ssh/config que agrega el
alias" and "pierdo de vista el comando que usas". Without seeing the block that will
be written and the command/path being tested, the SSH leg is opaque.
Output: A pre-gate SSH-block preview and consistent command+path display on the test
screen. No write-path behavior changes (those land in Plan 10).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# North Star — the SSH alias + Host block shape MUST match the recipe
@recipes/ssh-config.recipe
@recipes/README.md

# UAT gaps (authoritative)
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-UAT.md

# Files to edit + their current shape
@tui/wizard.go

# Renderers whose output the preview must mirror exactly (DO NOT re-render the Host
# block by hand — call the package, or mirror its exact field order/labels)
@internal/sshconfig/renderer.go

# tester.Result already carries Command — reuse it; do not invent a new command string
@internal/tester/tester.go

# Existing wizard tests show the real-model-path test style to follow
@tui/wizard_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSH test screen shows command + key path on pre-write and on SUCCESS (G-3)</name>
  <files>tui/wizard.go, tui/wizard_test.go</files>
  <behavior>
    - Pre-write/upload screen (wizardStepUpload): viewProve/viewUpload renders the exact
      pre-write ssh command and the tested key path (staged.TempPrivatePath) BEFORE the
      test runs, so the user sees what will be executed.
    - On Phase 1 SUCCESS (wizardStepProve1Done and later) the ssh command + tested key
      path remain visible (currently only a "✓ authenticated" checkmark is shown at
      tui/wizard.go:1223).
    - On Phase 2 SUCCESS (wizardStepProve2Done) the resolved command (phase2Result.Command)
      stays visible alongside the checkmark.
    - The command string MUST come from tester.Result.Command (phase1Result.Command /
      phase2Result.Command), never a hand-built string. The pre-write key path is
      staged.TempPrivatePath; renderUploadHeader already prints both staged paths, so the
      command is the missing piece — render it on PASS too.
    - Failure behavior is unchanged (renderTestDetail still fires on failure).
  </behavior>
  <action>
    Extend the prove-screen rendering in tui/wizard.go so the SSH command and tested key
    path are visible on the pre-write screen and on SUCCESS, not only on failure
    (UAT G-3, evidence tui/wizard.go:1191-1198, 1223-1240).

    Concretely:
    - Add a small helper (e.g. renderTestCommandLine(sb, res tester.Result)) that prints
      `$ <res.Command>` via StyleFaint when res.Command != "" — reuse the existing
      renderTestDetail "$ " prefix style for consistency. Do NOT print res.Output on PASS
      (output is failure-diagnostic; keep it inside renderTestDetail on failure only).
    - In viewProve (tui/wizard.go:1212), call the command-line helper for phase1Result on
      the wizardStepProve1Done/Prove2Running/Prove2Done/Written branch (the success branch
      at line 1223) so the phase-1 command stays visible after PASS.
    - Do the same for phase2Result on the phase-2 success branch (line 1237).
    - On the pre-test entry (viewUpload, tui/wizard.go:1201): the test has not run yet so
      phase1Result.Command is empty. Render the command that WILL run by formatting the
      exact preWriteArgs-equivalent string. Reuse tester.PreWrite's command shape: rather
      than duplicating arg construction, compute it from the same inputs the wizard passes
      to runPreWriteCmd (keyPath=staged.TempPrivatePath, hostname=provider, port). If
      mirroring the arg slice in tui risks drift, instead expose the pre-write command via
      a pure tester helper (see note) — prefer the helper to avoid divergence.
    - The tested key path is already shown by renderUploadHeader ("private (tested now):").
      Ensure it remains shown on every prove sub-step (it already is — verify in the test).

    Note on command-string source (avoid drift): tester builds the command via
    preWriteArgs + exec.Command.String() (internal/tester/tester.go:79-88). If you need the
    pre-test command string before running the test, add a tiny EXPORTED pure helper in the
    tester package, e.g. `func PreWriteCommand(keyPath, hostname string, port int) string`
    that returns `exec.Command("ssh", preWriteArgs(...)).String()` (read-only, no exec).
    This keeps the displayed pre-test command byte-identical to what PreWrite will run. If
    you add it, it is a pure read-only string builder — no new write surface, core stays
    UI-free. Add a unit test for it in internal/tester.

    ANTI-BLINDSPOT (project rule): drive the wizard through the REAL model path in tests —
    construct the wizard via makeTestWizardModel, advance with handleKey/update and faithful
    tester.Result values (with non-empty Command), then assert on view(width) output. Do NOT
    assert on a stubbed render helper in isolation.

    English-only for all code, comments, and test names.
  </action>
  <verify>
    <automated>go test ./tui/ ./internal/tester/ -run 'Wizard|TestDetail|PreWriteCommand|Prove' -count=1</automated>
  </verify>
  <done>
    Driving the wizard through update/handleKey to wizardStepProve1Done with a
    tester.Result whose Command is non-empty, view(width) contains the `$ ssh ...` command
    AND the tested key path; same holds at wizardStepProve2Done. On the upload/pre-test
    screen the command that WILL run is shown. Failure path output is unchanged. All tui +
    tester tests pass under the no-verify-free pre-commit hooks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-gate SSH Host-block preview (G-2 preview half)</name>
  <files>tui/wizard.go, tui/wizard_test.go</files>
  <behavior>
    - Before the write gate (on the upload/pre-test screen, and visible through the prove
      steps until written), the wizard shows the EXACT `Host <alias>` block that WILL be
      written to ~/.ssh/config: the Host line for the alias, HostName, Port, User git,
      IdentityFile (FinalPrivatePath), and IdentitiesOnly yes.
    - The alias shown is the per-recipe `<name>.<provider>` form (e.g. personal.github.com)
      via identity.DefaultAlias when the SSH Alias field is blank — NOT a bare host. The
      preview must reflect the user's typed alias when set.
    - The preview is produced by sshconfig.RenderHostBlock (the same renderer PersistAll
      uses at internal/identity/identity.go:246), so the preview is byte-faithful to what
      gets written.
    - RECIPE DIVERGENCE TO SURFACE (do not silently fix): the recipe canonical block uses
      alt-SSH `HostName ssh.<provider>` + `Port 443`, but the wizard currently defaults
      HostName=provider and Port=22 (tui/wizard.go:746, form default port "22"). The
      preview must show the ACTUAL values that will be written. If they differ from the
      recipe alt-SSH shape, render a one-line advisory note under the preview pointing to
      the recipe (e.g. "Recipe uses ssh.<provider>:443 (alt-SSH to bypass firewalls); this
      identity will use <hostname>:<port>."). Do NOT change the written values in this plan.
  </behavior>
  <action>
    Add a Host-block preview to the upload/pre-test wizard screen so the user sees the exact
    alias block that WILL be written before the gate (UAT G-2 "explicit SSH-block preview
    before the gate", evidence tui/wizard.go:887-934, 1288-1301; identity.go:239-289).

    Concretely:
    - Add a method (e.g. renderSSHBlockPreview(sb)) on createWizardModel that:
      1. Derives the alias the SAME way buildCreateInput does (tui/wizard.go:702-705):
         alias = inputs[5].Value(); if empty, identity.DefaultAlias(name, provider) where
         provider defaults to github.com. Confirm against recipes/ssh-config.recipe:
         `Host personal.github.com` — alias is `<name>.<provider>`, never bare host.
      2. Derives hostname + port exactly as buildCreateInput does (hostname = provider,
         port from inputs[4], default 22) so the preview equals the eventual write input.
      3. Uses the FINAL key path (staged.FinalPrivatePath) for IdentityFile — that is what
         PersistAll writes (identity.go:246 uses final.PrivatePath).
      4. Calls sshconfig.RenderHostBlock(alias, hostname, port, finalKeyPath, provider) and
         renders it under a "Will write to ~/.ssh/config:" header. Use StyleFaint/StyleBody
         consistent with the existing includeIf preview block (renderMatchSelector).
      5. Surfaces the recipe-divergence advisory line described in <behavior> when hostname
         != "ssh."+provider OR port != 443. Pull the recipe target shape from
         recipes/ssh-config.recipe (Hostname ssh.<provider>, Port 443). Read-only note only.
    - Call renderSSHBlockPreview from viewUpload (tui/wizard.go:1201) and viewProve
      (tui/wizard.go:1212) so it is visible before AND during the test, until written.
    - Guard against empty name (no preview until the form is filled), mirroring the existing
      includeIf-preview guard at tui/wizard.go:1126 (`if name != ""`).
    - Do NOT hand-build the Host block string in tui — always go through
      sshconfig.RenderHostBlock so the preview cannot drift from the writer (the same
      no-hand-build discipline used for includeIf via RenderIncludeIf, T-05.7-05-01).

    ANTI-BLINDSPOT: test through the real model path — fill inputs via the wizard, advance
    with update(keygenResultMsg{...}) so staged is populated, then assert view(width)
    contains the exact `Host personal.github.com`, `HostName`, `Port`, `IdentityFile`,
    `IdentitiesOnly yes` lines, and the divergence advisory when port=22/hostname=provider.

    English-only for all code, comments, and test names.
  </action>
  <verify>
    <automated>go test ./tui/ -run 'Wizard.*Preview|SSHBlockPreview|WizardSSH' -count=1</automated>
  </verify>
  <done>
    With a filled form (name "personal", provider "github.com", port 22, blank alias) and a
    populated staged key, view(width) contains the exact RenderHostBlock output for
    `Host personal.github.com` including `IdentitiesOnly yes` and the recipe-divergence
    advisory line (because 22 != 443). The preview is visible on both the upload screen and
    the prove screen. No write behavior changed. ./tui tests pass; `make lint` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → wizard form | provider/alias/port are user-typed; flow into RenderHostBlock for display only |
| wizard → ~/.ssh/config | NO write occurs in this plan; preview/display only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.7-09-01 | Information disclosure | SSH-block preview | accept | Preview shows only public-side config (alias/host/port/IdentityFile path/IdentitiesOnly); the private key is never rendered. Pub key already shown by renderUploadHeader. |
| T-05.7-09-02 | Tampering | preview/command drift from actual write | mitigate | Preview uses sshconfig.RenderHostBlock (same renderer as PersistAll); displayed command uses tester.Result.Command / tester.PreWriteCommand (same arg builder as PreWrite) — no hand-built strings. |
| T-05.7-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new third-party packages; only stdlib + existing internal packages. No install tasks. |
</threat_model>

<verification>
- `go build ./...` compiles.
- `go test ./tui/ ./internal/tester/ -count=1` passes.
- Manual-equivalent assertion in tests: the rendered view contains the exact Host block and
  the ssh command+path on PASS.
- `make fmt && make lint` clean over the whole module (pre-commit parity; never --no-verify).
</verification>

<success_criteria>
- The pre-gate SSH-block preview matches sshconfig.RenderHostBlock byte-for-byte.
- The alias uses the recipe `<name>.<provider>` convention (confirmed against
  recipes/ssh-config.recipe), never a bare host.
- The SSH test command + tested key path are visible on the pre-write screen and on SUCCESS.
- Recipe divergence (HostName/Port vs alt-SSH ssh.<provider>:443) is surfaced, not silently
  changed.
- Tests drive the real model Update/handleKey path (anti-blindspot).
</success_criteria>

<output>
Create `.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-09-SUMMARY.md` when done
</output>
