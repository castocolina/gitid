---
status: testing
phase: 05.7-complete-v1-0-product-features-in-tui
source: [05.7-01-SUMMARY.md, 05.7-02-SUMMARY.md, 05.7-03-SUMMARY.md, 05.7-04-SUMMARY.md, 05.7-05-SUMMARY.md, 05.7-06-SUMMARY.md, 05.7-07-SUMMARY.md, 05.7-08-SUMMARY.md]
started: 2026-06-21
updated: 2026-06-21
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Cold Start Smoke Test
expected: |
  Build fresh and launch. `make build` succeeds, then `./bin/gitid` (no args) enters
  the alt-screen TUI on the doctor dashboard with no crash/panic, and `./bin/gitid --help`
  lists the new commands: `adopt`, `add` (with `repo` subcommand), and `identity copy`.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: |
  `make build` succeeds; `./bin/gitid` launches the TUI dashboard without panic;
  `./bin/gitid --help` shows `adopt`, `add repo`, and `identity copy` (with --upload-keys).
result: skipped
reason: User went straight to the TUI create wizard instead of the CLI smoke test.

### 2. CLI — gitid adopt (fragment migration)
expected: |
  With a plain `~/.gitconfig_<name>` fragment present, `gitid adopt` detects it as an
  adoption candidate and offers migrate (copy into `~/.gitconfig.d/` + repoint includeIf)
  or reference-in-place. It writes a timestamped backup first and NEVER deletes the
  original fragment (you confirm the source file still exists afterward).
result: [pending]

### 3. CLI — gitid add repo <url>
expected: |
  `gitid add repo <git-url>` resolves the matching identity, rewrites the URL to that
  identity's SSH alias, and clones into `~/git/<provider>/<repo>`. It refuses a
  destination outside `~/git` and refuses to overwrite an existing destination.
result: [pending]

### 4. CLI — gitid identity copy --upload-keys
expected: |
  `gitid identity copy <name> --upload-keys` copies the PUBLIC key to the clipboard and,
  if `gh`/`glab` is installed and authenticated, offers to upload the public key (the
  shown command equals the command run). If the tool is absent it silently skips upload
  (no error). The PRIVATE key is never uploaded or shown.
result: [pending]

### 5. CLI — match-strategy --match flag
expected: |
  During identity create, `--match gitdir|hasconfig|both` controls the generated
  `includeIf` shape; default is `gitdir`. The resulting `~/.gitconfig` managed block
  matches the `recipes/` canonical shape (`gitdir:` and/or `hasconfig:remote.*.url:`).
result: skipped
reason: User tested the TUI create wizard directly (not the CLI flag). See gaps G-5.7-02/03.

### 6. TUI — Adopt modal from the Unmanaged sidebar
expected: |
  In the TUI, a plain `~/.gitconfig_<name>` fragment appears as a row in the Unmanaged
  section. Focusing that row shows an `[A]` Adopt affordance; pressing `A` opens the
  Adopt modal and runs the same migrate/reference flow. Pressing `A` on a managed
  identity row instead shows a guard toast ("select a fragment").
result: [pending]

### 7. TUI — Add Repo modal (ctrl+r)
expected: |
  From the Identities view, `ctrl+r` (or palette → Add Repo) opens the Add Repo modal:
  enter a URL, it detects the provider/identity, previews the alias rewrite, streams the
  clone, and shows a result. Esc closes the modal back to the main view.
result: [pending]

### 8. TUI — Copy / upload-assist modal (c)
expected: |
  Pressing `c` on an identity opens the copy modal; it copies the public key and, when
  `gh`/`glab` is authenticated, presents per-key upload prompts; when not logged in it
  shows a notice; when the tool is absent the upload section is omitted (no error).
result: [pending]

### 9. TUI — Wizard match-strategy selector + live preview
expected: |
  In the create wizard, Tab to the Match field; it offers gitdir / hasconfig / both, and
  the live `includeIf preview` block updates to reflect the chosen strategy, matching the
  `recipes/` shapes.
result: issue
reported: "La pantalla a veces se muestra incompleta cuando hago Tab para pasar las opciones (solo alias / solo dir / ambos); además no veo un campo para editar el folder."
severity: medium

### 10. TUI — Create wizard writes the ~/.ssh/config alias block
expected: |
  After completing the create wizard, ~/.ssh/config contains the per-identity managed
  `Host <alias>` block (e.g. `Host personal.github.com` → HostName github.com,
  IdentityFile, IdentitiesOnly yes), so the alias actually resolves for git push/clone.
result: issue
reported: "Sigo sin ver el bloque en ~/.ssh/config que agrega el alias; sin eso no funciona. No sé si usas github.com o personal.github.com. Si no se escribe el alias, debió quedar como github.com."
severity: major

### 11. TUI — SSH test screen shows the command and path
expected: |
  The pre-write SSH test screen shows the exact command run (e.g. `ssh -T -p 443 ...`)
  and the key path, and that information stays visible (or is recoverable) as the wizard
  advances past the test.
result: issue
reported: "Cuando pasa a la pantalla de test pierdo de vista el comando que usas; en una pantalla muestras el comando pero no la ruta, en la siguiente sí."
severity: major

### 12. Doctor — detect redundant Host * / duplicate global SSH directives
expected: |
  `gitid doctor` flags redundancy when ~/.ssh/config has multiple `Host *` stanzas or
  duplicate global directives (UseKeychain / AddKeysToAgent / IgnoreUnknown) across the
  user's pre-existing config and gitid's managed `_global` block, and suggests consolidation.
result: issue
reported: "Doctor was not able to see redundancy: my original 'IgnoreUnknown UseKeychain' is at root level, and the gitid-managed code adds another 'Host *' with UseKeychain/AddKeysToAgent. Why didn't doctor catch the duplicate Host */global directives?"
severity: major

## Summary

total: 12
passed: 0
issues: 4
pending: 0
skipped: 3

## Gaps

- truth: "TUI create wizard: focusing the match-strategy selector renders a complete, viewport-aware panel; the editable gitdir/hasconfig sub-fields (incl. the folder/gitdir path) are visible and reachable."
  status: failed
  reason: "User reported the screen renders incomplete when tabbing through gitdir/hasconfig/both, and saw no field to edit the folder. Root cause: the expanded match selector (~15-18 lines, tui/wizard.go:1082-1139) overflows the modal; modal height is computed POST-render (model.go:1002) and placeOverlay silently clamps overflowing rows (overlay.go:62-64) with no viewport/scroll handling. The editable gitdir sub-field lives inside this expanded panel, so the overflow also hides it."
  severity: medium
  test: 9
  artifacts: ["tui/wizard.go:1014", "tui/wizard.go:1082-1139", "tui/model.go:1002", "tui/overlay.go:62-64"]
  missing: ["viewport-aware modal rendering / scroll for the form modal when content exceeds terminal height", "USER REQUIREMENT: the wizard MUST expose clearly editable fields for BOTH the target-host alias AND the folder (gitdir) — always visible/reachable, never buried inside an overflowing expanded panel"]

- truth: "TUI create wizard reliably writes the ~/.ssh/config Host <alias> block, and when it does NOT write (auth gate not passed / cancelled) it tells the user clearly that nothing was written, why, and how to write offline."
  status: failed
  reason: "User completed the wizard but found no Host block in ~/.ssh/config. Working-as-designed gate: PersistAll/WriteSSH runs ONLY after both SSH-test phases PASS + Enter confirm, OR explicit skip+confirm (tui/wizard.go:890,934,1298; internal/identity/identity.go:267). A brand-new key not yet on the provider fails the ssh -T test, so the happy path never writes; Esc/q writes nothing (tui/wizard.go:872,880,893,927). The wizard does not clearly surface 'no config written + why + upload-first or skip-&-write-offline', leaving a dangling alias the user can't use."
  severity: major
  test: 10
  artifacts: ["tui/wizard.go:887-934", "tui/wizard.go:1288-1301", "internal/identity/identity.go:239-289"]
  missing: ["clear no-write feedback + discoverable skip-&-write-offline path + an explicit SSH-block preview before the gate so the user sees the alias/Host that WILL be written"]

- truth: "The SSH test screen shows the exact command and key path, and keeps it visible/recoverable as the wizard advances."
  status: failed
  reason: "renderTestDetail (tui/wizard.go:1191-1198) renders the command only on FAILURE (lines 1226,1240); on PASS only a checkmark is shown (1223,1237), and the post-write screen shows none. The user loses sight of the command/path once the test passes."
  severity: major
  test: 11
  artifacts: ["tui/wizard.go:1191-1198", "tui/wizard.go:1223-1240"]
  missing: ["consistent command+path display on the test screen (success and pre-write), not only on failure"]

- truth: "gitid doctor detects SSH-config redundancy: multiple Host * stanzas and duplicate global directives (UseKeychain/AddKeysToAgent/IgnoreUnknown) across the user's pre-existing config and gitid's managed _global block, and suggests consolidation."
  status: failed
  reason: "Doctor has no SSH-global redundancy check. overlap.go only compares includeIf match conditions between identities and explicitly skips _global (internal/doctor/checks/overlap.go:29). coherence.go only checks per-managed-identity artifacts (key/fragment existence, IdentitiesOnly, gpg.format, allowed_signers) — internal/doctor/checks/coherence.go. Nothing inspects duplicate Host * stanzas or redundant global directives, so the user's root-level 'IgnoreUnknown UseKeychain' + a 'Host *' UseKeychain + gitid's managed _global 'Host *' (UseKeychain/AddKeysToAgent/IgnoreUnknown) go unflagged. Note: the generated block is VALID (IgnoreUnknown may appear under Host * and precedes UseKeychain), and gitid DOES run ssh -G/ssh -T in the resolved/connectivity tests (internal/tester/tester.go:114,119) — but config coherence is parser-based only, never validated via ssh -G in the doctor."
  severity: warning
  test: 12
  artifacts: ["internal/doctor/checks/overlap.go:24-33", "internal/doctor/checks/coherence.go:35-43", "internal/tester/tester.go:111-123"]
  missing: ["a doctor check for duplicate Host * / redundant global directives (de-dupe across managed _global and pre-existing unmanaged config), optionally cross-checked via ssh -G effective config", "USER REQUIREMENT: this finding is ADVISORY (SeverityWarning) only — it must NEVER block/stop doctor or any write flow; report + suggest consolidation, no hard-fail"]
