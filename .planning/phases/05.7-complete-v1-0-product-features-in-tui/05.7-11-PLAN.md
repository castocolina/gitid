---
phase: 05.7-complete-v1-0-product-features-in-tui
plan: 11
type: execute
wave: 5
depends_on: []
files_modified:
  - internal/doctor/checks/redundancy.go
  - internal/doctor/checks/redundancy_test.go
  - internal/doctor/doctor.go
  - internal/doctor/doctor_test.go
  - cmd/gitid/doctor.go
  - tui/deps.go
  - tui/health.go
autonomous: true
gap_closure: true
requirements: [DOC-08, SSH-03]
must_haves:
  truths:
    - "gitid doctor flags SSH-config redundancy: multiple `Host *` stanzas and duplicate global directives (UseKeychain / AddKeysToAgent / IgnoreUnknown) across the user's pre-existing config AND gitid's managed `_global` block."
    - "The redundancy finding is ADVISORY (SeverityWarning) ONLY — it NEVER blocks or stops the doctor or any write flow, and has no destructive auto-fix."
    - "The check is wired into the real doctor families (Run + Families + cmd + tui) and runs on the live config, not only as an isolated function."
  artifacts:
    - path: "internal/doctor/checks/redundancy.go"
      provides: "CheckRedundancy(deps) []doctor.Finding following the CheckX(deps) pattern; SeverityWarning, Fix nil"
      exports: ["CheckRedundancy"]
    - path: "internal/doctor/doctor.go"
      provides: "FamilyRedundancy + CheckRedundancy field + Run/Families wiring (advisory, listed last)"
      contains: "FamilyRedundancy"
    - path: "cmd/gitid/doctor.go"
      provides: "CheckRedundancy: checks.CheckRedundancy wiring in buildDoctorDeps"
    - path: "tui/deps.go"
      provides: "CheckRedundancy wiring in the TUI doctor deps"
  key_links:
    - from: "internal/doctor/doctor.go Run"
      to: "deps.CheckRedundancy"
      via: "appended to the CheckFn dispatch list (nil-guarded)"
      pattern: "CheckRedundancy"
    - from: "cmd/gitid/doctor.go buildDoctorDeps"
      to: "checks.CheckRedundancy"
      via: "field assignment"
      pattern: "CheckRedundancy:\\s*checks.CheckRedundancy"
    - from: "tui/health.go familyCheckFn"
      to: "d.CheckRedundancy"
      via: "FamilyRedundancy case"
      pattern: "FamilyRedundancy"
---

<objective>
LEG 1 — doctor advisory. Add a new doctor check that detects SSH-config redundancy:
multiple `Host *` stanzas and duplicate global directives (UseKeychain /
AddKeysToAgent / IgnoreUnknown) across the user's pre-existing config AND gitid's
managed `_global` block, and suggests consolidation. The user's root-level
`IgnoreUnknown UseKeychain` plus a hand-written `Host *` UseKeychain plus gitid's
managed `_global` `Host *` (UseKeychain/AddKeysToAgent/IgnoreUnknown) currently go
unflagged because overlap.go skips `_global` and coherence.go is per-identity only.

HARD REQUIREMENT (UAT G-4): the finding is ADVISORY (doctor.SeverityWarning) ONLY —
it MUST NEVER block/stop the doctor or any write flow. Report + suggest
consolidation; no auto-fix (Fix nil) unless trivially safe — for this plan, Fix is
nil (report-only), consistent with CheckOverlap.

Closes: G-4. Doctor-only; no overlap with the wizard plans (09/10), so it runs in
parallel with Plan 09.

Purpose: User asked "Why didn't doctor catch the duplicate Host */global directives?"
Output: A new advisory FamilyRedundancy check, wired into cmd + tui doctor, with the
non-blocking severity guaranteed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# North Star — the managed _global block shape gitid writes (Host * + the 3 directives)
@recipes/ssh-config.recipe
@recipes/README.md

# UAT gaps (authoritative) — G-4 missing list + "ADVISORY only" hard requirement
@.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-UAT.md

# Existing advisory check to mirror EXACTLY (CheckX(deps) pattern, SeverityWarning, Fix nil)
@internal/doctor/checks/overlap.go

# Per-identity check (NOT the place for global redundancy) — shows the family pattern
@internal/doctor/checks/coherence.go

# Doctor core: Family constants, Deps struct, CheckFn fields, Run dispatch, Families order
@internal/doctor/doctor.go

# Wiring points
@cmd/gitid/doctor.go
@tui/deps.go
@tui/health.go

# SSH config parsing helpers available (full-config parse, raw block bodies)
@internal/sshconfig/reader.go

# The managed _global block content gitid emits (the 3 directives + Host *)
@internal/sshconfig/renderer.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CheckRedundancy detector (advisory) + FamilyRedundancy in doctor core</name>
  <files>internal/doctor/checks/redundancy.go, internal/doctor/checks/redundancy_test.go, internal/doctor/doctor.go, internal/doctor/doctor_test.go</files>
  <behavior>
    - New FamilyRedundancy = "Redundancy" family constant in doctor.go, added LAST in
      Families() (after FamilyOverlap), so it renders as an advisory section.
    - New CheckRedundancy CheckFn field in doctor.Deps, dispatched (nil-guarded) at the END
      of Run's CheckFn slice.
    - New CheckRedundancy(deps) in internal/doctor/checks/redundancy.go following the
      CheckOverlap shape exactly: reads the FULL ~/.ssh/config via deps.ReadFile(
      deps.SSHConfigPath); detects, across the ENTIRE config (pre-existing unmanaged content
      AND gitid's managed _global block):
        1. Multiple `Host *` stanzas (count > 1) → one finding.
        2. Duplicate global directives among UseKeychain / AddKeysToAgent / IgnoreUnknown —
           where the same directive appears more than once at global scope (root-level OR
           under any `Host *`). For each duplicated directive → a finding (or one combined
           finding listing them).
      Each finding: Family=FamilyRedundancy, Severity=doctor.SeverityWarning, Fix=nil,
      with a SuggestedFix string proposing consolidation into a single `Host *` block.
    - SEVERITY GUARANTEE: every finding is SeverityWarning. A unit test asserts that for a
      config triggering redundancy, doctor.ExitCode over only those findings is 1 (warning),
      never 2/3, and that the check NEVER returns a Fix (no auto-mutation).
    - Read-only: redundancy.go imports neither internal/filewriter nor os.Chmod (D-01).
    - Empty/missing config → no findings, no error (best-effort, like other checks).
  </behavior>
  <action>
    Create internal/doctor/checks/redundancy.go implementing CheckRedundancy(deps doctor.Deps)
    []doctor.Finding, mirroring internal/doctor/checks/overlap.go's structure (UAT G-4,
    evidence overlap.go:24-33, coherence.go:35-43).

    Detection approach (parser-based is sufficient per UAT; ssh -G cross-check is OPTIONAL
    and NOT required — do not gate on it):
    - Read the whole config: content, err := deps.ReadFile(deps.SSHConfigPath); on err or
      empty content return nil (no findings).
    - Count `Host *` stanzas: parse with the kevinburke ssh_config parser already used in
      internal/sshconfig (you may add a small EXPORTED helper in internal/sshconfig, e.g.
      ParseGlobalDirectives(content []byte) that returns the count of `Host *` stanzas and a
      map[directive][]occurrence for the three global directives) OR scan line-prefixed
      within sshconfig and expose the result. Prefer a sshconfig helper so the parsing logic
      lives next to the other parsers (reader.go) and stays unit-testable; redundancy.go then
      consumes the helper's struct. Detect the three directives case-insensitively
      (UseKeychain, AddKeysToAgent, IgnoreUnknown), counting occurrences at global scope:
      root-level lines AND lines under any `Host *` stanza (NOT under a specific Host alias).
      Note IgnoreUnknown legitimately PRECEDES UseKeychain in the managed block — that is NOT
      a duplicate; only flag the SAME directive appearing 2+ times.
    - Build findings: for >1 `Host *` → a "multiple Host * stanzas" warning; for each
      directive with count>1 → a "duplicate global directive <name>" warning. Title +
      Explanation name the redundancy and that it spans pre-existing config and gitid's
      managed _global block; SuggestedFix proposes consolidating into a single `Host *`.
      Fix: nil (report-only — advisory, no destructive auto-fix; matches CheckOverlap).

    Then in internal/doctor/doctor.go:
    - Add FamilyRedundancy Family = "Redundancy" to the Family constants.
    - Add `CheckRedundancy CheckFn` to the Deps struct (document: advisory, SeverityWarning).
    - Append deps.CheckRedundancy to the CheckFn slice in Run (LAST, nil-guarded — Run
      already skips nil fns).
    - Append FamilyRedundancy to Families() LAST (after FamilyOverlap) so it renders as an
      advisory section.

    Tests:
    - redundancy_test.go: table cases — (a) clean config → no findings; (b) two `Host *`
      stanzas → one Host* finding; (c) IgnoreUnknown at root + UseKeychain under Host * +
      managed _global Host * with UseKeychain/AddKeysToAgent/IgnoreUnknown (the user's exact
      scenario) → duplicate-directive findings, ALL SeverityWarning; (d) the legitimate
      managed block alone (IgnoreUnknown then UseKeychain once each, single Host *) → NO
      false positive. Assert Fix == nil on every finding. Assert doctor.ExitCode(findings)
      == 1 for the redundant case.
    - doctor_test.go: assert Families() includes FamilyRedundancy LAST and that Run dispatches
      a non-nil CheckRedundancy (and that a nil CheckRedundancy is safely skipped).

    Use the user's exact reported config as a fixture (root `IgnoreUnknown UseKeychain` +
    a `Host *` with UseKeychain + the managed `_global` `Host *` block) so the test proves
    the real scenario, not a synthetic one.

    English-only for all code, comments, and test names.
  </action>
  <verify>
    <automated>go test ./internal/doctor/... ./internal/sshconfig/... -run 'Redundancy|Families|Run|GlobalDirectives' -count=1</automated>
  </verify>
  <done>
    CheckRedundancy returns SeverityWarning, Fix==nil findings for: multiple `Host *`
    stanzas and duplicate UseKeychain/AddKeysToAgent/IgnoreUnknown at global scope across
    the user's exact config; returns no findings for the legitimate single managed block.
    doctor.ExitCode over a redundant-config finding set == 1. FamilyRedundancy is in
    Families() last; Run dispatches it nil-guarded. Tests pass; `make lint` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CheckRedundancy into cmd + tui doctor (non-blocking, runs live)</name>
  <files>cmd/gitid/doctor.go, tui/deps.go, tui/health.go</files>
  <behavior>
    - cmd/gitid/doctor.go buildDoctorDeps wires CheckRedundancy: checks.CheckRedundancy
      (alongside CheckOverlap at cmd/gitid/doctor.go:481), so `gitid doctor` runs it on the
      live ~/.ssh/config.
    - tui/deps.go wires CheckRedundancy: checks.CheckRedundancy (alongside CheckOverlap at
      tui/deps.go:491) so the TUI health view runs it.
    - tui/health.go maps FamilyRedundancy → d.CheckRedundancy in the per-family dispatch
      (alongside the FamilyOverlap/FamilyCoherence cases near tui/health.go:143).
    - The doctor exit-code path and any write/baseline flows are UNCHANGED: because every
      redundancy finding is SeverityWarning, the existing tiered ExitCode keeps a
      warning-only run at exit 1; redundancy NEVER raises exit to 2/3 and NEVER gates the
      Apply-fixes prompt or any write. Confirm there is no code path where a warning finding
      blocks the doctor (it cannot — Fix is nil so it is not collected by collectFixable).
  </behavior>
  <action>
    Wire the new check into both doctor entrypoints so it runs on the real config — the
    anti-blindspot rule: a check that exists but is not wired is invisible (MEMORY:
    doctor wiring blindspot; UAT G-4 evidence overlap.go wiring at cmd/gitid/doctor.go:481,
    tui/deps.go:491, tui/health.go:143).

    Concretely:
    - cmd/gitid/doctor.go: in buildDoctorDeps, add `CheckRedundancy: checks.CheckRedundancy,`
      next to the existing `CheckOverlap: checks.CheckOverlap,` (line ~481).
    - tui/deps.go: add the same assignment next to CheckOverlap (line ~491).
    - tui/health.go: add a `case doctor.FamilyRedundancy: fn = d.CheckRedundancy` next to the
      existing family cases (~line 143-144).
    - Do NOT change ExitCode, collectFixable, or applyFixes. Redundancy findings have Fix==nil
      so collectFixable already excludes them — verify (don't re-implement).

    Tests (drive the REAL doctor, not isolated seams — MEMORY anti-blindspot):
    - In the doctor cmd test (or a focused test), build deps with a real ReadFile over a
      temp ~/.ssh/config containing the redundant scenario, wire CheckRedundancy:
      checks.CheckRedundancy, run doctor.Run(deps), and assert a FamilyRedundancy warning
      finding is present AND doctor.ExitCode of the full run is not escalated beyond what the
      other findings would produce (i.e. redundancy alone yields exit 1).
    - For the TUI, add/extend a health test asserting FamilyRedundancy dispatches
      d.CheckRedundancy and produces the warning finding through the real family-run path.
    - Assert that with ONLY redundancy findings, the doctor does not enter any Apply/write
      gate (no fixable findings collected).

    English-only for all code, comments, and test names.
  </action>
  <verify>
    <automated>go test ./cmd/... ./tui/ -run 'Doctor|Redundancy|Health|Families|ExitCode' -count=1</automated>
  </verify>
  <done>
    `gitid doctor` and the TUI health view both run CheckRedundancy on the live config (wired
    in buildDoctorDeps + tui/deps.go + tui/health.go). A redundant config produces a
    FamilyRedundancy warning; the run's exit code stays 1 (advisory), no write/Apply gate is
    entered, and the test drives the real doctor.Run / family-run path (not an isolated
    function). All cmd + tui tests pass; `make lint` clean; pre-commit hooks pass without
    --no-verify.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.ssh/config (untrusted, hand-written + managed) → doctor | doctor reads the whole file (managed + unmanaged) for parser-based detection |
| doctor core → fix/write | redundancy has NO fix; doctor core never writes (D-01, write-free core enforced by depguard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.7-11-01 | Denial of service | advisory finding blocking doctor/write | mitigate | HARD REQUIREMENT: SeverityWarning only + Fix nil → ExitCode stays ≤1, never collected by collectFixable, never gates Apply/write. Unit test asserts exit code and no-fix. |
| T-05.7-11-02 | Tampering | destructive auto-consolidation | mitigate | Fix is nil — no auto-fix; the check only reports + suggests. No write surface added (redundancy.go imports no filewriter; depguard enforces write-free core). |
| T-05.7-11-03 | Spoofing/false-positive | flagging the legitimate managed block | mitigate | Detection flags only the SAME directive appearing 2+ times / >1 `Host *`; the legitimate single managed block (IgnoreUnknown then UseKeychain once) is covered by a negative test. |
| T-05.7-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new third-party packages; uses existing kevinburke/ssh_config + stdlib only. No install tasks. |
</threat_model>

<verification>
- `go build ./...` compiles.
- `go test ./internal/doctor/... ./internal/sshconfig/... ./cmd/... ./tui/ -count=1` passes.
- Severity guarantee proven: redundancy-only finding set → doctor.ExitCode == 1; Fix==nil on
  every redundancy finding; no Apply/write gate entered.
- Check is wired into Run + Families + cmd + tui and runs on the live config (anti-blindspot).
- `make fmt && make lint` clean over the whole module (pre-commit parity; never --no-verify).
</verification>

<success_criteria>
- Redundancy detected: multiple `Host *` stanzas + duplicate UseKeychain/AddKeysToAgent/
  IgnoreUnknown across pre-existing config and the managed `_global` block.
- Finding is ADVISORY (SeverityWarning) ONLY — never blocks/stops doctor or any write flow;
  no destructive auto-fix.
- No false positive on the legitimate single managed block.
- Wired into the real doctor families (cmd + tui), runs on the live config.
- Tests drive the real doctor.Run / family path (anti-blindspot).
</success_criteria>

<output>
Create `.planning/phases/05.7-complete-v1-0-product-features-in-tui/05.7-11-SUMMARY.md` when done
</output>
