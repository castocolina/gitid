---
phase: 09.6
status: findings
reviewed: 2026-09-19
head: 9bc3ee0
---

# Phase 9.6 code review

Scope: `git diff 9c73239..HEAD` (Wave 6 closeout + PTY/lint gap closure). Orchestrator review after `gsd-code-reviewer` spawn failed in this runtime.

## Critical

None found in the 09.6-06 delta.

## Warning

1. `cmd/gitid/wiring.go` `CommitGlobalSSHOverride` / `CommitGlobalGitOverride` ignore the `overrides` argument (renamed `_` to satisfy revive). Preview plans mention override counts, but the commit path calls the pre-override apply lifecycle. `TestGlobalSSH_RealPTYEnumRowEdit` still wrote a backup, so some value reaches disk — confirm it is the cycled value, not the recommended default. If it is the default, UXG-05 is not closed.

2. Eight `cmd/gitid` unit failures (`refusing path outside managed home`, custom-directive empty-diff) are present on Wave 5 HEAD `9c73239` as well. Not a 09.6-06 regression; still open on this branch.

## Info

1. Full `make test-e2e` remains red on CreateFlow upload, dummy-demo, install-script, and release tests. Cited Options PTY cases for this phase pass after `7d097a8`.
2. Nested Claude worktrees under `.claude/worktrees/` poison `golangci-lint` if left on disk. Remove them before `make lint`.

## Gap closure `7d097a8`

- Typed keys now persist via `OptionEditor.SetTextInput`.
- Fallback editor resets `fieldFocus` to name on `e`.
- PTY tests use `e` (PD3) and assert ceremony headings, not truncated footers.
- `make lint` is 0 issues on the main checkout.
