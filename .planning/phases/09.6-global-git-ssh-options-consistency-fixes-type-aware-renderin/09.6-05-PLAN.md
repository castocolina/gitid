---
phase: 09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin
plan: 05
type: execute
wave: 5
depends_on: ["09.6-04"]
files_modified:
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_regions_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - cmd/gitid-frame-promote/main.go
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [UXG-01, UXG-02, UXG-03, UXG-04, UXG-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every render state this phase introduced — the inline value cell, the open enum editor, the open text editor, the differs row without its warning glyph, and the renamed sub-tab — is registered in the visual-regression inventory on BOTH Options screens where applicable (D-07), and the gate can actually fail on each of them."
    - "Neither internal/globalgit/classify.go nor internal/globalssh/classify.go changed across the whole phase — the four-state classifier scope fence held (D-08)."
    - "The promoted PTY evidence frames show the shipped label and the shipped glyph behavior, so recorded evidence never contradicts the running UI."
    - "UXG-01 through UXG-05 are closed in REQUIREMENTS.md against quoted, named test evidence rather than a claim."
    - "The full gate battery is green at phase close: race tests, lint, make test, make test-e2e and the visual-regression gate."
  artifacts:
    - internal/screenshot/createflow.go
    - cmd/gitid/gate_visual_regression_test.go
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "internal/screenshot/createflow.go's ScreenSpec inventory and its visual-divergence allowlist are the registration point; the gate is invoked by make gate-visual-regression."
    - "cmd/gitid-frame-promote/main.go was parameterised by phase in plan 09.5-05 and is reused here rather than re-parameterised."
    - "REQUIREMENTS.md's traceability table at the end of the file carries one row per requirement ID naming its home phase and status."
---

<objective>
Close the phase: register every new render state in the visual-regression inventory with a
hand-verified fail path, re-promote the PTY evidence the rename invalidated, and close
UXG-01 through UXG-05 against quoted test evidence behind a full gate battery.

Purpose: four plans changed how two screens render and what one of them shows. Without this
plan the recorded visual evidence still shows the old label and the old glyph behavior, and
five requirements sit open with working code behind them.

Output: a phase whose evidence matches its code and whose requirements are closed with
citations.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-CONTEXT.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-01-SUMMARY.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-02-SUMMARY.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-03-SUMMARY.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-04-SUMMARY.md
@CLAUDE.md
</context>

<authority>
This plan inherits every decision the four preceding plans lock in their `<authority>`
blocks. It introduces no new product behavior.
</authority>

<tasks>

<task type="auto">
  <name>Task 1: Register every new render state in the visual-regression inventory and hand-verify the gate can fail on each</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_regions_test.go, cmd/gitid/gate_visual_regression_test.go</files>
  <read_first>
    - internal/screenshot/createflow.go lines 2940-2990 (the ScreenSpec entries plan 09.5-05 added for the properties browsers — the shape and the Interaction-description convention to follow)
    - internal/screenshot/createflow.go lines 2780-2820 (the visual-divergence allowlist and how a classified real-versus-dummy divergence is justified in prose)
    - internal/screenshot/createflow_regions.go lines 300-330 and 1395-1420 (a region constant and its extractor)
    - cmd/gitid/gate_visual_regression_test.go lines 1380-1400 and 2570-2600 (how a screen is enumerated in the gate and how a deterministic probe key is seeded into the fixture home)
    - the four SUMMARY files from this phase, for the exact list of render states each plan introduced
  </read_first>
  <action>
Register each render state this phase introduced as its own inventory entry, with an
Interaction description that names the exact keystroke sequence a reader could reproduce by
hand — the convention plan 09.5-05 established. The states are: an Options row showing its
inline resolved value, an open enum editor in the detail pane, an open free-text editor in
the detail pane, a differs row rendering without a warning glyph, and the renamed general-keys
sub-tab in browse mode. Register the states on BOTH Options screens where both screens have
them, since CONTEXT.md's D-07 requires the two screens to behave identically and a
single-screen registration would let one of them drift.

Where the real backend and the dummy fixture legitimately diverge on a new state, add an
allowlist entry with a written rationale naming what diverges and why, rather than loosening
a region's matching rule. Reuse the existing region extractors where the new content lands
inside an already-extracted region; add a new region constant plus extractor only where the
content genuinely sits outside them, and pin the new extractor's behavior in
`internal/screenshot/createflow_regions_test.go` the way the existing ones are pinned.

Then hand-verify the fail path for each new entry, one at a time: perturb the render the
entry covers, observe `make gate-visual-regression` report a genuine failure naming that
entry, revert, observe it pass. A registration whose fail path was never observed is a
vacuous gate. Record in the SUMMARY which entry you perturbed, what the failure output said,
and that you reverted.
  </action>
  <verify>
    <automated>make build &amp;&amp; make gate-visual-regression &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/screenshot/... -run 'TestCreateflowRegions|TestExtract'</automated>
    <fails_when>any stage exits non-zero, or the gate output reports fewer registered screens than the inventory declares</fails_when>
  </verify>
  <reversibility rating="reversible">Inventory entries and allowlist rows; no product behavior changes.</reversibility>
  <done>Every new render state is registered on both screens where applicable, each new entry's fail path was observed and reverted, and `make gate-visual-regression` passes with the fuller inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Re-promote this phase's PTY evidence with provenance</name>
  <files>cmd/gitid-frame-promote/main.go</files>
  <read_first>
    - cmd/gitid-frame-promote/main.go lines 80-145 (the per-phase promotion set and the provenance description text plan 09.5-05 parameterised)
    - the four SUMMARY files, for the list of real-PTY case names this phase added or changed
  </read_first>
  <action>
Add this phase's promotion set to the frame promoter, reusing the phase parameterisation plan
09.5-05 already built rather than re-parameterising it. The set must cover the frames the
rename invalidated (every Global Git sub-tab frame carrying the old label) plus the net-new
frames for the editors and the corrected differs-row glyph on both screens.

Write the provenance description so it states which phase produced each frame and which
real-PTY case captured it, in the same style the existing entries use. A promoted frame with
no traceable capture case is evidence nobody can re-derive.

Do not promote a frame this phase did not actually capture. If a state was registered in Task
1 but has no corresponding real-PTY case, say so in the SUMMARY as an open item rather than
promoting a frame captured some other way.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid-frame-promote/... &amp;&amp; make build &amp;&amp; make test-e2e</automated>
    <fails_when>any stage exits non-zero, or `make test-e2e` output contains a `--- FAIL` line for any Global Git or Global SSH real-PTY case</fails_when>
  </verify>
  <reversibility rating="reversible">A promotion-set entry and its description string.</reversibility>
  <done>This phase's frames are promoted with per-frame provenance naming their capture case, and `make test-e2e` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Close UXG-01 through UXG-05 against quoted evidence and run the full gate battery</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md section R (the UXP family) — the wording and checkbox convention a closed requirement follows
    - .planning/REQUIREMENTS.md's traceability table near the end of the file — the row shape (ID, home phase, status)
    - .planning/ROADMAP.md's Phase 9.5 section lines 556-577 — the Success Criteria "MET: <named test>" citation convention this phase must match
    - the four SUMMARY files, for the exact test function names to cite
  </read_first>
  <action>
Close each requirement clause by clause, not in bulk.

For each of UXG-01 through UXG-05, walk its wording one clause at a time and name the
specific test function or gate that proves that clause. A clause with no named proof is not
closed — record it as an open item in the SUMMARY and leave the requirement unchecked rather
than marking it done on the strength of the code existing. Mark the checkbox only when every
clause has a citation, and update the requirement's row in the traceability table.

Mirror the same citations into `.planning/ROADMAP.md`'s Phase 9.6 Success Criteria, using the
"MET: <named test>" convention Phase 9.5's section already uses, and tick the phase's plan
checkboxes and the progress-table row.

Explicitly record in the SUMMARY that the three items RESEARCH.md escalated were resolved as
plan decisions rather than guessed — the free-text classification of the default-branch row,
the revert-on-dismiss mechanism, and the deliberate decision to leave the Git/SSH
selectability asymmetry alone — and cite where each was decided.

Then run the full gate battery in one pass and record the actual output figures (test counts,
timings, issue counts), not a claim that it passed.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./... &amp;&amp; make lint &amp;&amp; make test &amp;&amp; make test-e2e &amp;&amp; make gate-visual-regression</automated>
    <fails_when>any stage exits non-zero, or `make lint` reports a nonzero issue count, or the race-test output reports any `FAIL` package line</fails_when>
    <human-check>Read the closed UXG-01..UXG-05 entries and confirm each cited test name actually exists in the tree (`rg -n '<name>' --glob '*_test.go'` returns a match) — a citation to a test that does not exist is worse than an open requirement.</human-check>
  </verify>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <done>UXG-01 through UXG-05 are closed with per-clause citations in REQUIREMENTS.md and mirrored into ROADMAP.md's Success Criteria; the full gate battery ran in one pass with its real figures recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped render -> promoted evidence frames | recorded evidence that disagrees with the running UI misleads every later reviewer |
| requirement text -> cited test | a citation to a nonexistent or vacuous test converts an open gap into a false closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.6-20 | Repudiation | a visual-regression entry whose fail path was never observed | medium | mitigate | Task 1 requires perturbing the render behind each new entry, observing a genuine named failure, and reverting — with the failure output quoted in the SUMMARY. A registration that cannot fail proves nothing. |
| T-09.6-21 | Repudiation | promoted frames still showing the pre-rename label | medium | mitigate | Task 2 re-promotes every Global Git frame the rename invalidated, with per-frame provenance naming the capture case. Task 2 also forbids promoting a frame no real-PTY case captured. |
| T-09.6-22 | Repudiation | requirement closure without real proof | high | mitigate | Task 3 closes clause by clause with a named test per clause, leaves a clause without proof visibly open, and its human check re-greps every cited name to confirm it exists in the tree. |
| T-09.6-SC | Tampering | dependency supply chain | low | accept | No packages installed, no module requirement added; the closing battery includes `make lint` over the whole module. |
</threat_model>

<verification>
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` green, with the test count recorded.
- `make lint` reports 0 issues.
- `make test`, `make test-e2e` and `make gate-visual-regression` all pass, with timings recorded.
- `go list -deps ./internal/tuikit` names no first-party backend package.
- `git diff go.mod go.sum` is empty across the whole phase.
- `git diff <phase-base-sha> -- internal/globalgit/classify.go internal/globalssh/classify.go` is empty — the D-08 scope fence held across all five plans.
- `git diff <phase-base-sha> -- internal/tuikit/views.go | rg 'func .*Selectable'` returns nothing — the PD4 non-goal held.
- Every requirement ID cited as closed has at least one existing test function backing it.
</verification>

<success_criteria>
1. Every render state this phase introduced is registered in the visual-regression inventory on both screens where applicable, with an observed fail path. (UXG-01, UXG-02, UXG-04)
2. This phase's PTY evidence is re-promoted with per-frame provenance and no stale label survives. (UXG-04)
3. UXG-01 through UXG-05 are closed clause by clause against named, existing tests. (UXG-01..UXG-05)
4. The full gate battery ran in one pass with its real output figures recorded. (UXG-01..UXG-05)
5. The three items RESEARCH.md escalated are recorded as resolved decisions with their decision sites cited. (UXG-01)
</success_criteria>

<output>
Create `.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-05-SUMMARY.md` when done
</output>
