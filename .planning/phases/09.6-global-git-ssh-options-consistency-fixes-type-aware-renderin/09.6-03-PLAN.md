---
phase: 09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin
plan: 03
type: execute
wave: 3
cross_ai: true
depends_on: ["09.6-02"]
files_modified:
  - internal/tuikit/globalgit.go
  - internal/tuikit/globalgit_test.go
  - internal/tuikit/optioneditor.go
  - internal/tuikit/optioneditor_test.go
  - internal/dummytui/fixturebackend.go
  - cmd/gitid/wiring_test.go
  - e2e/global_git_pty_e2e_test.go
autonomous: true
requirements: [UXG-01, UXG-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The two Global Git enum rows (merge.conflictstyle, diff.colorMoved) show their resolved value on the row and open the SAME arrow-cycle editor, with the SAME edit / commit / dismiss keys, that plan 09.6-02 proved on Global SSH (D-02/D-04/D-07, UXG-01)."
    - "A bundle row (core.autocrlf / core.eol, alias, color) renders exactly as it does today — checkbox only, no inline value, no editor (D-01, UXG-01)."
    - "The Options checkbox column is unchanged on every row, so every existing init.defaultBranch selectability and toggle assertion stays green without modification (PD1/PD8)."
    - "A staged Global Git override reaches disk only through the existing apply ceremony, and a hard-gated value on old git is still written as the declared fallback (UXG-05)."
    - "Both screens drive one shared editor value type — there is no second, divergent editor implementation on the Git side (PD17)."
  artifacts:
    - internal/tuikit/globalgit.go
    - internal/tuikit/optioneditor.go
    - internal/dummytui/fixturebackend.go
    - e2e/global_git_pty_e2e_test.go
  key_links:
    - "globalgit.go handleKey's editor guard must precede BOTH sub-tab-switch sites: the zero-options branch and the generic `case \"left\", \"right\"` (internal/tuikit/globalgit.go ~1094-1116). Guarding only one leaves a live collision on the zero-row path."
    - "baselineCeremonyFor (internal/tuikit/globalgit.go:774-778) and the ceremonyConfirmed dispatch (globalgit.go:945-951) are the two connection points PD15 names on the Git side, dispatching on the same has-staged-override predicate plan 09.6-02 built."
    - "A Global Git override still passes through globalgit.WriteRequestedValueFor's hard gate inside cmd/gitid/wiring.go's shared gitExplicitOverlay, so a cycled zdiff3 on git older than 2.35 is written as the declared fallback, never as an unsupported value."
---

<objective>
Expand plan 09.6-02's proven vertical slice sideways onto Global Git: render the resolved
value on enum and text rows, and give Global Git the same enum editor and override write path
Global SSH already has — by reusing the shared editor, never by writing a second one.

Purpose: Global Git carries ten of the phase's seventeen rows and the only hard
version-gated row. It is the larger half of UXG-01 and the half the user's bug report
actually named.

Output: `merge.conflictstyle` and `diff.colorMoved` editable in place through the existing
ceremony, with the checkbox column provably unchanged and the hard version gate provably
still in force over an overridden value.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-CONTEXT.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-RESEARCH.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-PATTERNS.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-01-PLAN.md
@.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-02-SUMMARY.md
@CLAUDE.md
</context>

<authority>
This plan inherits every decision `09.6-01-PLAN.md`'s `<authority>` block locks (D-01..D-12,
PD1..PD25) and every mechanism plan 09.6-02 proved. Read both before starting; neither is
restated here.

**PD26 (one editor implementation, reused — not ported).** The Git screen consumes the SHARED
editor value type and the SHARED eligibility predicates in `internal/tuikit/optioneditor.go`.
Copying the SSH screen's editor block into `globalgit.go` and adapting it is explicitly
forbidden: two adapted copies is how the two screens end up disagreeing about what Esc does,
which is the class of defect this phase exists to remove. Anything the Git screen needs that
the shared type does not yet offer is ADDED to the shared type, with its unit test, in this
plan.
</authority>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the resolved value on Global Git's enum and text rows, checkbox column provably unchanged</name>
  <files>internal/tuikit/globalgit.go, internal/tuikit/globalgit_test.go, internal/dummytui/fixturebackend.go</files>
  <precondition>`git` and `go` resolve on PATH (the Global Git probe shells out to `git config`); run `make setup-env` first if `go version` fails.</precondition>
  <read_first>
    - internal/tuikit/globalgit.go lines 1943-1996 (the master-list row loop: marker, checkbox box, the tone glyph plan 09.6-01 Task 3 narrowed, the key, the init.defaultBranch chip, and the two truncation calls bounded by the list width)
    - internal/tuikit/globalgit.go lines 669-691 (globalGitRowLine2's `now: X -> Y` sentence — the second line is NOT replaced by this task)
    - internal/tuikit/globalgit.go lines 601-640 (overlaidGitOptions — note the fallback-row branch at 605-613, which plan 09.6-04 owns; this task must not change it)
    - internal/tuikit/globalssh.go as plan 09.6-02 left it — the inline-value cell shape and the staged-pending marker to mirror
    - internal/tuikit/views.go: GlobalGitOptionView with the value-kind fields plan 09.6-01 added, and its Selectable predicate (do NOT edit it, PD4)
    - internal/dummytui/fixturebackend.go lines 864-888 (the Git option projection — PD24)
  </read_first>
  <behavior>
    - Test 1 (RED first): a Global Git enum row's first rendered line contains its resolved current value; a toggle row's first line does not.
    - Test 2: a bundle row renders byte-identically to its pre-plan output — no inline value, no editor affordance — asserted against a recorded expected line, not merely "does not contain".
    - Test 3: every row still renders exactly two lines, and no composed line exceeds the list width it was rendered at, including the widest declared enum value against the longest key with its chip.
    - Test 4 (PD1/PD8 regression guard, asserted directly rather than assumed): the checkbox column is unchanged for every row — an `init.defaultBranch` row that was selectable before is still selectable, still renders its checkbox and still toggles on space.
    - Test 5: the inline value cell coexists with the existing `init.defaultBranch` chip rather than replacing it, and the value is elided before the chip is pushed off the line.
    - Test 6: the dummy fixture's Git enum rows carry a current value inside their declared value set, so the approved surface renders the same states the real binary does.
  </behavior>
  <action>
Render the resolved current value as a right-hand cell on the row's FIRST line for enum and
text rows only, mirroring the cell shape plan 09.6-02 established on the SSH side rather than
inventing a second layout. Toggle and bundle rows keep today's exact first line.

The cell must coexist with the existing `init.defaultBranch` chip rather than replace it, and
the whole composed line must still pass through the existing width-bounded truncation. Elide
the value rather than letting it push the chip or the tone column off the line.

Do not touch `globalGitRowLine2` — the second line's `now: X -> Y` sentence and its Phase 9.4
differs wording stay exactly as they are. Do not touch `overlaidGitOptions`' fallback-row
branch; plan 09.6-04 owns it and changing it here would collide. Do not touch
`internal/globalgit/classify.go` (D-08) or `GlobalGitOptionView.Selectable` (PD4).

Before writing any new assertion, run the existing Global Git suite unmodified and record the
result in the SUMMARY. Per PD1 and PD8 the checkbox column is unchanged, so every existing
`init.defaultBranch` selectability and toggle assertion must already be green; if one is not,
fix the render, not the assertion.

Seed the dummy's Git enum rows in `internal/dummytui/fixturebackend.go` with a current value
drawn from their declared set (PD24), for the same reason plan 09.6-02 seeded the SSH side:
plan 09.6-06's real-versus-dummy comparison needs both surfaces able to reach the state.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... ./internal/dummytui/... -run 'TestGlobalGit|TestOptionRow|TestFixtureBackend' -v</automated>
    <fails_when>the command exits non-zero, or the `-v` output contains a `--- FAIL` line naming any pre-existing `init.defaultBranch` selectability or toggle test (which would mean PD1's additive design was not honored)</fails_when>
  </verify>
  <reversibility rating="reversible">One render branch guarded on the value kind; toggle and bundle rows take the pre-existing path untouched.</reversibility>
  <done>Enum and text rows on Global Git show their resolved value on the row; toggle and bundle rows render byte-identically to before; every row is still two lines within the list width; the whole pre-existing Global Git suite is green with no assertion rewritten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Global Git's enum editor and its override write path through the existing ceremony</name>
  <files>internal/tuikit/globalgit.go, internal/tuikit/globalgit_test.go, internal/tuikit/optioneditor.go, internal/tuikit/optioneditor_test.go, cmd/gitid/wiring_test.go</files>
  <read_first>
    - internal/tuikit/globalgit.go lines 958-981 (the existing fieldEditing early-intercept guard — the position the shared editor guard joins; PD17 folds this block itself into the shared editor in plan 09.6-04, not here) and lines 1094-1116 (the zero-options branch AND the generic `case "left", "right"` — the editor guard must precede BOTH)
    - internal/tuikit/globalgit.go lines 1164-1177 (the space toggle and its Selectable guard) and lines 1203-1236 (the `a` apply-ceremony dispatch a staged override must reach, including the fallback-row branch this task does not touch)
    - internal/tuikit/globalgit.go lines 659-667 (gitApplyChosen — the Git twin of the apply-chosen helper PD16's staged-override arm extends) and lines 1218-1226 (the pending tally the ceremony's N-of-M denominator reads)
    - internal/tuikit/globalgit.go lines 774-812 (baselineCeremonyFor: the `m.backend.GlobalGitApplyPlan(keys)` call PD15 makes conditional, and the cross-warning note it composes) and lines 940-951 (the ceremonyConfirmed dispatch)
    - internal/tuikit/optioneditor.go and internal/tuikit/globalssh.go as plan 09.6-02 left them — the shared editor value type, its eligibility predicates, and the dispatch-helper shape to reuse (PD26)
    - cmd/gitid/wiring.go and cmd/gitid/lifecycle.go as plan 09.6-01 left them — the Git option-value seam and the shared gitExplicitOverlay builder with its WriteRequestedValueFor hard-gate treatment
  </read_first>
  <behavior>
    - Test 1 (RED first): with the `merge.conflictstyle` row selected, the edit key opens the SHARED arrow-cycle editor positioned on the row's current value, and left/right cycle within its three declared values without changing the sub-tab.
    - Test 2 (RED first): the same holds when the Options list is EMPTY and the zero-options branch would otherwise handle left/right — the guard precedes both sub-tab-switch sites.
    - Test 3: Enter stages the cycled value and marks the row chosen; Esc restores the pre-edit value and leaves no staged override, including on a second edit session after a first Enter.
    - Test 4 (PD16 eligibility, one case per state): the edit key opens the editor on an enum row that is needs-action, already-set or differs; it does nothing and is NOT reported handled on a toggle row, a bundle row, a not-applicable row, a row carrying a probe error, and a row with no writable member.
    - Test 5 (PD16 apply eligibility): a staged override on an already-set Git enum row appears in the apply-chosen key set and is counted by the N-of-M denominator.
    - Test 6 (PD15 connection point): with no staged override, the ceremony calls the pre-existing `GlobalGitApplyPlan` and confirming calls the pre-existing `CommitGlobalGit`, byte-for-byte as today, asserted with a recording stub; with one staged, both route to the Git option-value seam. The two are never both called for one ceremony.
    - Test 7: the ceremony preview shows the STAGED value for an overridden key, not the policy recommendation.
    - Test 8 (the hard gate, end to end from this screen): a staged `zdiff3` on a machine reporting a git version below the row's declared minimum lands the row's declared fallback in the bytes the commit writes, and the resulting advisory names the substitution.
    - Test 9: an unknown key and an out-of-set value handed to the seam from this screen are each refused by name before any candidate bytes are composed.
    - Test 10 (PD26): the Git screen contains no second editor state block — the model's editor field is the shared type, asserted structurally by the shared type's own tests exercising both screens' entry points.
  </behavior>
  <action>
Give Global Git the editor and override path plan 09.6-02 proved on Global SSH by REUSING the
shared implementation, not by porting it (PD26).

Model and input. Add the shared editor value plus a staged-override map to the Global Git
model, and add the early key-intercept guard in `handleKey` placed BEFORE both the
zero-options branch's sub-tab switch and the generic sub-tab-switch case — this file has TWO
such sites and guarding only one leaves a live collision on the zero-row path. Structure the
guard exactly like the existing fieldEditing block a few hundred lines above it, and delegate
its cycle, snapshot and eligibility decisions to the shared editor rather than reimplementing
them. Add an edit-key case in the generic switch that opens the editor only for an
edit-eligible enum row on the Options sub-tab; free-text rows are wired in plan 09.6-04, and
this task must leave the fallback-author row's current Enter gesture alone so the two plans do
not collide.

Render the open editor's value list in the DETAIL pane (PD25), in the same slot the
fallback-author fields already occupy, never inline in the scrolling master list.

Apply eligibility. Extend the Git apply-chosen helper with PD16's staged-override arm and
extend the pending tally the ceremony's N-of-M denominator reads, exactly as plan 09.6-02 did
on the SSH side.

Connection point. In `baselineCeremonyFor`, replace the direct plan call with the same
dispatch-helper shape plan 09.6-02 introduced: the pre-existing planner when the chosen set
carries no staged override, the Git option-value seam when it does. Compose the preview lines
from the staged value where one exists, and leave the existing cross-warning note logic
untouched. In the `ceremonyConfirmed` branch, dispatch the existing commit or the seam's
commit under the SAME predicate, keeping the existing commit token wrapping untouched.

Write path. Nothing new is written here: plan 09.6-01 already built the shared
`gitExplicitOverlay` on `WriteRequestedValueFor`, so this task's job is to prove from THIS
screen's dispatch path that the hard gate still downgrades a staged `zdiff3` on old git. That
is the single highest-risk detail in the task — a cycled value that bypassed the gate would
make old git error at merge time on an unknown value, which is exactly what the gate exists to
prevent.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... ./cmd/gitid/... -run 'TestGlobalGit|TestOptionEditor|TestGitOptionValue|TestRunGlobalGitApply|TestWriteRequestedValueFor|TestBuildTUIDeps|TestNoBackendAllowlist' -v</automated>
    <fails_when>the command exits non-zero, or the `-v` output does not contain a PASS line for the hard-gate override test driven from this screen, or does not contain a PASS line for the zero-options-branch guard test, or contains `--- FAIL: TestNoBackendAllowlist`</fails_when>
  </verify>
  <reversibility rating="reversible">One additive model field, one guarded branch ahead of two existing switch sites, and one dispatch helper at each of two named call sites. The no-override path is unchanged.</reversibility>
  <done>`merge.conflictstyle` and `diff.colorMoved` are editable in place with the same keys Global SSH uses; a staged value reaches disk only through the existing ceremony; a hard-gated staged value is still downgraded to the declared fallback on old git; both screens drive one shared editor.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the Global Git enum journey in a real terminal</name>
  <files>e2e/global_git_pty_e2e_test.go</files>
  <read_first>
    - e2e/global_git_pty_e2e_test.go lines 480-560 (the sub-tab navigation harness and its frame-polling helpers) and lines 690-790 (the custom-key write case — the on-disk assertion pattern to mirror)
    - the SSH real-PTY enum case plan 09.6-02 added — the assertion sequence this case mirrors on the Git side
  </read_first>
  <action>
Add a real-PTY case driving the COMPILED binary against a seeded fixture home: land on Global
Git's Options sub-tab, navigate to the `merge.conflictstyle` row, press the edit key, cycle
with right, and assert BOTH that the displayed value changed AND that the breadcrumb still
names the Options sub-tab — the second assertion is what proves the guard beat the
sub-tab-switch binding.

Press the dismiss key and assert the row returned to its pre-edit value and the on-disk
managed block is byte-identical.

Re-open, cycle to a non-recommended value, commit with Enter, run the apply ceremony to
completion, and assert the on-disk managed block carries that exact value plus that a
timestamped backup exists beside the target.

Add a second case on a bundle row asserting the edit key does nothing there and the row's
rendered line is unchanged — the negative half of type-aware rendering, and the case most
likely to regress silently.

Reuse the file's existing session and seeding helpers rather than adding a second harness.
  </action>
  <verify>
    <automated>make build &amp;&amp; go test -tags e2e -count=1 ./e2e/... -run 'TestGlobalGit_RealPTYEnumRowEdit|TestGlobalGit_RealPTYBundleRowHasNoEditor'</automated>
    <fails_when>either stage exits non-zero, or the e2e output contains a `--- FAIL` line for either case, or the run reports no tests matched the pattern</fails_when>
    <human-check>Open `gitid`, go to Global Git -> Options, press the edit key on merge.conflictstyle and confirm the editor looks and behaves identically to Global SSH's, and that pressing it on the alias row does nothing at all.</human-check>
  </verify>
  <reversibility rating="reversible">Two new e2e test functions; no production code changes in this task.</reversibility>
  <done>Real-PTY runs prove the Git enum cycle, dismiss and commit contract against the bytes on disk, and prove a bundle row has no editor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes -> tuikit model | arbitrary terminal input reaches the Global Git editor state |
| machine config (`git config --list`) -> Options row render | unvalidated machine state now rendered on the master row's first line |
| staged model value -> the Git option-value seam -> `runGlobalGitApply` -> `~/.gitconfig` managed block | the only write boundary this plan exercises |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.6-14 | Tampering | an override routing around the `merge.conflictstyle` hard gate | high | mitigate | The override is folded into the overlay THROUGH `WriteRequestedValueFor`, never around it (plan 09.6-01). Task 2 behavior Test 8 re-proves it from THIS screen's dispatch path with a below-minimum reported git version, because a gate that holds in a unit test but is bypassed by the screen is still a broken gate. |
| T-09.6-15 | Tampering | the Git ceremony's conditional dispatch | high | mitigate | Task 2 behavior Test 6 asserts with a recording stub that the no-override path still calls the pre-existing planner and commit, and that the two paths are never both taken for one ceremony. |
| T-09.6-16 | Denial of Service | the editor guard versus the zero-options branch | medium | mitigate | `internal/tuikit/globalgit.go` has TWO sub-tab-switch sites; the guard precedes both and Esc always closes the editor. Asserted by Task 2 behavior Test 2, which drives the zero-row path specifically. |
| T-09.6-17 | Tampering | the arrow-cycle value set on the Git side | medium | mitigate | The cycle indexes only the declared value set from the curated policy table, whose contents are pinned per-row by plan 09.6-01's literal expected table; the overlay builder refuses an out-of-set value by name (Task 2 behavior Test 9). |
| T-09.6-18 | Repudiation | a second, divergent editor implementation on the Git side | medium | mitigate | PD26 forbids porting; Task 2 behavior Test 10 asserts the Git model carries the shared editor type, and the shared type's unit tests exercise both screens' entry points. Two adapted copies is how the two screens end up disagreeing about Esc. |
| T-09.6-SC | Tampering | dependency supply chain | low | accept | No packages installed, no module requirement added; covered by the unchanged `go.mod`/`go.sum` check in verification. |
</threat_model>

<verification>
- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` is green with no new `t.Skip`.
- `make lint` reports 0 issues.
- `make test` and `make test-e2e` pass.
- `go list -deps ./internal/tuikit` names no first-party backend package.
- `git diff` shows neither `Selectable()` body changed (PD4).
- `git diff internal/globalgit/classify.go internal/globalssh/classify.go` is empty (D-08).
- `git diff internal/tuikit/globalgit.go` shows no change inside `overlaidGitOptions`' fallback-row branch — that branch is plan 09.6-04's scope.
- `git diff go.mod go.sum` is empty.
</verification>

<success_criteria>
1. Enum and text rows on Global Git show their resolved value; toggle and bundle rows render byte-identically to before. (UXG-01, PD1)
2. Global Git's enum rows open the SHARED arrow-cycle editor with the same edit / commit / dismiss keys Global SSH uses, guarded ahead of BOTH sub-tab-switch sites. (UXG-01, D-07)
3. Edit eligibility and apply eligibility behave as PD16 states, with one test per row state. (UXG-01, UXG-05)
4. A staged Global Git override reaches disk only through the existing ceremony and still honors the hard version gate, proven from this screen's dispatch path. (UXG-05)
5. Every pre-existing `init.defaultBranch` selectability and toggle assertion is green with no assertion rewritten. (PD1, PD8)
</success_criteria>

## Review Dispositions Ledger

### Round 1 — 4a799a8

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 09.6-02: "route the override through `WriteValueFor`" is not implementable | HIGH | Resolved upstream in plan 09.6-01 Task 2 (`WriteRequestedValueFor`); this plan's Task 2 behavior Test 8 re-proves the gate from the Git screen's own dispatch path. |
| 09.6-02: `GlobalGitApplyPlan` builds recommendations independently; preview can diverge from commit | HIGH | Resolved upstream by the shared `gitExplicitOverlay` (plan 09.6-01 Task 2); this plan's Task 2 behavior Tests 6 and 7 assert the ceremony previews the staged value and routes both preview and commit through the same seam. |
| 09.6-02: local `ValueKind` types are not directly assignable to the tuikit type | MEDIUM | Resolved upstream in plan 09.6-01 Task 1 (explicit conversion functions with table tests); this plan consumes the already-populated DTO fields. |
| 09.6-02: the declared fixture file is `internal/dummytui/data.go`, Git side | MEDIUM | Task 1 targets `internal/dummytui/fixturebackend.go` lines 864-888 per PD24. |
| Cross-plan: the editor architecture is built incrementally with no shared state machine | HIGH | PD26 forbids porting; this plan consumes and, where needed, extends the shared `internal/tuikit/optioneditor.go` that plan 09.6-02 created, with Task 2 behavior Test 10 pinning it. |
| Cross-plan: no plan sets `cross_ai: true` | HIGH | Present in this plan's frontmatter. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| "Pins every policy row with completeness and per-row classification tests" as a task of this plan | Moved EARLIER, not deferred: the twelve-row classification table and its per-row completeness tests are plan 09.6-01 Task 1, so the vocabulary exists before either screen consumes it. |

<output>
Create `.planning/phases/09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin/09.6-03-SUMMARY.md` when done
</output>
