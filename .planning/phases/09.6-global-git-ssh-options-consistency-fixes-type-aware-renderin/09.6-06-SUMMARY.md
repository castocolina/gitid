---
phase: 09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin
plan: 06
status: complete
---

# Phase 9.6 Plan 06: Visual Evidence and Closeout

## Task 2: Evidence promotion

The Phase 9.6 promotion set is registered in `cmd/gitid-frame-promote/main.go` and contains seven frames captured by named real-PTY cases. The set covers the renamed `Other keys` browse states, the differs row, and the browse boundary states. The editor and fallback journeys remain real-PTY evidence but are not promoted because their tests do not capture frames.

The stale-label scan was observed RED before promotion. It reported stale `Set keys` lines in the Phase 9.5 promoted Global Git frames. The affected frames were regenerated from the current PTY suite and promoted through `go run ./cmd/gitid-frame-promote -phase 09.5`; Phase 9.6 frames were then promoted with `-phase 09.6`. The scan is green after promotion:

- `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./cmd/gitid-frame-promote/... -run 'TestPromot|TestPromotedFramesHaveNoRetiredLabel|TestPromotionCasesExist' -v` — PASS, 9 tests.
- `TestPromotedFramesHaveNoRetiredLabel` checks every promoted `*.txt` frame under `.planning/phases/*/ui-frames/` and reports file and line for retired literals.
- `TestPromotionCasesExist` resolves every Phase 9.6 promotion entry against a declared `e2e` test function.

The focused Global Git promotion capture run passed 8 tests, including the two custom-key cases, browse/differs cases, Other keys filter/browse/probe-failure cases, and sub-tab click. A broader `TestGlobal(Git|SSH)_RealPTY` run exposed four unrelated existing failures in fallback pair and SSH enum dismissal paths; those cases were not used as promotion sources.

## Route and evidence ledger

All Phase 9.6 visual states use Route A where the current PTY capture exists. The editor and fallback states use named real-PTY evidence but are not promoted because no capture helper is called:

| UI-SPEC state | Route | Evidence |
|---|---|---|
| Inline resolved value | A | `TestGlobalGit_RealPTYBrowse`, `TestGlobalGit_RealPTYDiffersRow` |
| Open enum editor | B / PTY | `TestGlobalSSH_RealPTYEnumRowEdit`, `TestGlobalGit_RealPTYEnumRowEdit` |
| Open free-text editor | B / PTY | `TestGlobalGit_RealPTYFallbackPairEdit` |
| Free-text validation error | B / PTY | `TestGitFallbackEmailInlineValidation` |
| Differs row without warning glyph | A | `TestGlobalGit_RealPTYDiffersRow` |
| Fallback authoritative pair | B / PTY | `TestGlobalGitFallbackRowShowsBackendSeededPair`, `TestGlobalGit_RealPTYFallbackPairEdit` |
| External-origin fallback | B / unit + PTY | `TestGitFallbackAuthorState_DisplayContract`, `TestGlobalGit_RealPTYSetKeysBrowse` |
| Staged override pending | B / PTY | `TestGlobalSSH_RealPTYEnumRowEdit`, `TestGlobalGit_RealPTYEnumRowEdit` |
| Ceremony preview staged value | B / PTY | `TestGlobalSSH_RealPTYEnumRowEdit`, `TestGlobalGit_RealPTYEnumRowEdit` |
| Renamed Other keys tab | A | `TestGlobalGit_RealPTYSetKeysBrowse`, `TestGlobalGit_RealPTYSetKeysFilter`, `TestGlobalGit_RealPTYSubTabStripClick` |

The Route-B frame-line residual is visible in the PTY assertions: `TestGlobalSSH_RealPTYEnumRowEdit` asserts the Options editor breadcrumb and staged value; `TestGlobalGit_RealPTYEnumRowEdit` asserts the selected enum value and ceremony preview; `TestGlobalGit_RealPTYFallbackPairEdit` asserts the fallback editor and receipt. These tests are named and source-resolved; they are not promoted frames.

## UXG closeout

UXG-01 through UXG-05 are checked in `REQUIREMENTS.md`, with named tests in each clause and matching traceability rows. The Phase 9.6 roadmap row is complete at six of six plans:

`| 9.6. Global Git/SSH Options Consistency Fixes (INSERTED) | 6/6 | Complete | 2026-09-18 |`

The UI-SPEC rule walk is recorded here:

- S-1: `TestGlobalSSHFixtureCoversFourOptionStates`, `TestGlobalGit_RealPTYBrowse`.
- L-1..L-5: `TestGlobalSSHOptionRowLineCount`, `TestGlobalGitOptionRowLineCount`, `TestGlobalGit_RealPTYBrowse`, `TestGlobalGit_RealPTYEnumRowEdit`.
- I-1..I-7: `TestOptionEditorCommitAndDismiss`, `TestGlobalSSH_RealPTYEnumRowEdit`, `TestGlobalGit_RealPTYFallbackPairEdit`, `TestGlobalGit_RealPTYEnumRowEdit`.
- I-8: `TestOptionEditorExoticCurrentValue`.
- I-9..I-10: `TestGitFallbackEmailInlineValidation`, `TestValidateDefaultBranch`.
- K-1..K-2: `TestOptionRowNeedsActionHasWarningGlyphDiffersDoesNot`, `TestGlobalGitDiffersRowRendersNoWarningGlyph`.
- C-1..C-6: `TestFrozenPropertiesCopy`, `TestRetiredCopyAbsent`, `TestRetiredCopyWalkScope`, `make gate-copy-freeze`, and `TestPromotedFramesHaveNoRetiredLabel`.
- Approved Base States 1, 5, and 10 have promoted Route-A frames. States 2, 3, 4, 6, 7, 8, and 9 are explicitly deferred to named real-PTY evidence because their current tests do not capture promotable frames.

Research escalations were resolved as plan decisions: free-text default-branch classification in PD8, revert-on-dismiss in PD3, and the deliberate Git/SSH selectability asymmetry in PD4 of `09.6-01-PLAN.md`. Wizard-wide helper-row unification remains Phase 9.7 D-07.

## Verification

Focused promotion verification: PASS (9 tests).
Focused Global Git PTY capture verification: PASS (8 tests).

Orchestrator-verified after merge `0d9148e` and gap-closure `7d097a8`:

- `make lint` — 0 issues
- `make gate-visual-regression` — PASS, 68 frames
- Cited Options PTY evidence — PASS: `TestGlobalGit_RealPTYFallbackPairSet`, `TestGlobalGit_RealPTYFallbackPairClear`, `TestGlobalGit_RealPTYFallbackPairEdit`, `TestGlobalSSH_RealPTYEnumRowEdit`
- `go test -count=1 ./cmd/gitid/` still has 8 failures already present on Wave 5 HEAD `9c73239` (`refusing path outside managed home` / custom-directive). Not introduced by 09.6-06.
- Full `make test-e2e` still has unrelated CreateFlow upload, dummy-demo, install-script, and release tests red; Options evidence named by this plan is green.
