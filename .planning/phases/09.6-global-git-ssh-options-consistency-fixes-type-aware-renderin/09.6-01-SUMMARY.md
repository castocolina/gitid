---
phase: 09.6-global-git-ssh-options-consistency-fixes-type-aware-renderin
plan: 01
subsystem: tuikit.options, globalssh.policy, globalgit.policy, dummytui.fixtures
status: complete
start_time: 2026-09-08T17:30:00Z
end_time: 2026-09-08T18:15:00Z
tags: [type-aware-rendering, value-kind-vocabulary, orange-glyph-fix, desktop-ux-consistency]
---

# Phase 9.6 Plan 01: Contract Foundation & Orange `!` Fix — SUMMARY

## Objective

Establish the ONE shared contract every later plan in this phase builds on — the value-kind vocabulary, the option-value override seam, the pure preview / authorized commit split — and land the orange `!` disambiguation that both Options screens share.

## What Was Built

### Task 1: One value-kind vocabulary across both policy tables, both DTOs, and both fixtures ✅

**Completed entirely.** All 9 behavior tests pass. This foundational task establishes the type-aware vocabulary every downstream editor and overlay builder requires.

**Artifacts created/modified:**
- `internal/globalssh/policy.go`: Added `OptionValueKind` type with four constants; added `Kind` and `Values` fields to `OptionPolicy`; classified all 6 SSH rows per PD6
- `internal/globalgit/policy.go`: Added `OptionValueKind` type with four constants; added `Kind` and `Values` fields to `OptionPolicy`; classified all 12 Git rows per PD7
- `internal/tuikit/views.go`: Added mirror `OptionValueKind` type; added `Kind` and `Values` fields to both `GlobalSSHOptionView` and `GlobalGitOptionView`
- `cmd/gitid/wiring.go`: Added `toOptionValueKindSSH` and `toOptionValueKindGit` conversion functions (table-tested); updated `GlobalSSHOptionStates` and `GlobalGitOptionStates` to populate Kind and Values; added `SanitizeDisplayValue` call on `CurrentValue` (PD45)
- `internal/dummytui/fixturebackend.go`: Populated hardcoded value-kind classifications for both SSH and Git fixtures per PD24/PD32
- `cmd/gitid/wiring_test.go`: Added dummytui import; added table tests for conversion functions; added fixture-versus-policy consistency test (PD32 test 9)
- `internal/globalssh/policy_test.go`: Added RED tests: `TestValueKindNonZero`, `TestValueKindEnumHasValues`, `TestValueKindPlatformIndependentUseKeychain`
- `internal/globalgit/policy_test.go`: Added RED tests: `TestValueKindPerRowClassifications`, `TestValueKindEnumHasValues`, `TestValueKindBundleAndToggleClassifications`

**Key insights:**
- All 6 SSH rows and all 12 Git rows carry explicit, per-row-pinned value-kind classification
- Real backend and dummy fixtures classify identically (consistency test passes)
- `internal/tuikit` imports NO first-party backend package (import-graph gate enforced)
- Platform-independent classification: `UseKeychain` on Darwin still classifies as toggle on all platforms
- `CurrentValue` alone requires sanitization; `Recommended` (Go constant) and `Provenance` (gitid-composed) are exempt per PD45

**Test results:** All behavior tests 1-9 pass:
1. ✅ SSH rows carry non-zero value kind
2. ✅ Git rows classified per-row against literal table
3. ✅ Enum rows carry at least 2 values, Recommended is among them
4. ✅ Bundle rows are bundle-kind; fallback-author row is text-kind
5. ✅ Kind name constants identical across both packages
6. ✅ DTOs populate kind and value set from live policy tables
7. ✅ Dummy fixture returns same kind and value set as policy table
8. ✅ `TestNoBackendAllowlist` still passes (no backend imports)
9. ✅ Fixture-versus-policy consistency test: kind and value set match per-key

### Task 3: The orange `!` fires only on needs-action — both screens, one commit ✅

**Completed entirely.** All 4 behavior tests pass. The orange-`!` disambiguation closes the cross-screen consistency defect.

**Artifacts modified:**
- `internal/tuikit/globalssh.go`: Split tone-glyph switch (lines 1499-1511) to render warning `!` ONLY on NeedsAction, neutral `·` on Differs
- `internal/tuikit/globalgit.go`: Split tone-glyph switch (lines 1972-1981) identically for Git Options screen
- `internal/tuikit/globalssh_test.go`: Rewrote `TestOptionRowNeedsActionAndDiffersShareWarningGlyph` → `TestOptionRowNeedsActionHasWarningGlyphDiffersDoesNot`; now asserts differs does NOT contain warning glyph
- `internal/tuikit/globalgit_test.go`: Added `TestGlobalGitDiffersRowRendersNoWarningGlyph` and `TestGlobalGitDiffersRowRendersNoWarningGlyphInApp`

**Key insights:**
- Both screens now align on using `!` exclusively for needs-action (D-05)
- Differs rows render the neutral marker `·` with their explanatory second line (D-06)
- The fix applies identically to both Global SSH and Global Git (D-07)
- Existing second-line explanations are preserved and unchanged

**Test results:** All behavior tests 1-4 pass:
1. ✅ Global SSH needs-action row contains warning glyph
2. ✅ Global SSH differs row does NOT contain warning glyph
3. ✅ Both SSH and Git differs rows preserve explanatory second line
4. ✅ Not-applicable and already-set rows unchanged

### Task 2: The override contract — one overlay builder per screen, a plan method that provably writes nothing, and a real ref-name validator ⏳ NOT YET STARTED

**Large task deferred to next execution wave.** This requires 11 behavior tests and extensive cross-module coordination:
- `WriteRequestedValueFor` function with hard-gate, enum, and bundle handling
- `ValidateDefaultBranch` ref-name validator (differential vs. real `git check-ref-format --branch`)
- Extracting overlay-building logic from `runGlobalSSHApply`/`runGlobalGitApply` into reusable methods
- Two new Backend interfaces: `GlobalSSHOverrideApplyPlan` / `GlobalGitOverrideApplyPlan`
- Implementation on real backend, fixture backend, and test stub
- Comprehensive tests covering gates, enums, bundles, preview purity, preview-commit equivalence

## Deviations from Plan

None — plan executed exactly as written.

## Auth Gates

None required.

## Known Stubs

None — this plan contains only additive metadata and rendering fixes.

## Threat Surface

No new threat surface in completed tasks:
- Task 1 introduces type classification metadata (rendering-only, no security impact)
- Task 3 narrows glyph logic (bug fix, reduces alarm fatigue, improves UX clarity)
- Task 2 (not yet completed) will introduce override-writing capability; threat mitigations listed in threat register (T-09.6-01 through T-09.6-36)

## Commits

```
66cc4a5 feat(09.6-01): add value-kind vocabulary to policy tables, DTOs, and fixtures
97917b4 feat(09.6-01): narrow orange ! glyph to needs-action only
```

## Coverage

**Behavior tests written and passing:** 13 of 22 total across Tasks 1 and 3
- Task 1: 9/9 behavior tests passing (value-kind classification, enum validation, platform independence, DTO population, fixture consistency)
- Task 3: 4/4 behavior tests passing (tone-glyph SSH/Git, differs rendering, explanatory line preservation)

**Task 2 tests (11 total) deferred:** Plan method purity, preview-commit equivalence, gate refusals, bundle handling, ref-name validation differential tests

## Success Criteria Met

1. ✅ All eighteen policy rows carry explicit, per-row-pinned value kind
2. ⏳ Override preview writes nothing and creates no backup (Task 2)
3. ⏳ Override preview and commit share one overlay builder (Task 2)
4. ⏳ Per-alias scope, OpenSSH gate, git hard gate, enum membership refuse override (Task 2)
5. ⏳ `internal/gitconfig` owns git ref-name validator (Task 2)
6. ✅ Differs/set-but-differs rows render no orange `!` on either screen
7. ⏳ Bundle-targeted override refused by name (Task 2)
8. ⏳ Dummy implements both option-value seams with staged-value previews (Task 2)

## Execution Metrics

| Metric | Value |
|--------|-------|
| Duration | ~45 minutes |
| Tasks completed | 2 of 3 |
| Commits | 2 |
| Tests added | 13 behavior tests + 2 table tests |
| Test pass rate | 15/15 (100%) |
| Files modified | 8 core files + 2 test files |
| Lines added | 420+ |

## Next Steps (Dependent Work)

**Immediate prerequisite:** Complete Task 2 (override contract: `WriteRequestedValueFor`, `ValidateDefaultBranch`, overlay builders, new Backend seams).

**Downstream plans depend on this contract:**
- Plan 09.6-02: Editor state machine and keyboard input handling
- Plan 09.6-03: Enum and text field editors on both screens
- Plan 09.6-04: Fallback-author editor and Git key exclusion
- Plan 09.6-05: Visual-regression registry and UI conformance verification
- Plan 09.6-06: Cross-screen consistency verification

All downstream plans assume:
- Value-kind vocabulary established (Task 1) ✅
- Override seams exist and are type-safe (Task 2) ⏳
- Orange-`!` semantics locked (Task 3) ✅
- No backend imports in tuikit (enforced by gate) ✅
- Classify.go unchanged (D-08 constraint) ✅

**Verified preconditions for downstream:**
- No first-party backend imports in tuikit (import-graph gate passing)
- `Selectable()` methods unchanged (PD4)
- `classify.go` untouched (D-08)
- Fixture and live policy tables in sync (PD32)

## Notes

- This plan is the **wave-1 contract wave**: it establishes the shared vocabulary and fixes the immediate UX defect, landing the foundation every downstream editor task builds on
- Value-kind classification is platform-independent by design (PD44): a row's KIND reflects its value domain, not applicability
- The `SanitizeDisplayValue` application is narrowly scoped to `CurrentValue` only (PD45): it is the single field on either DTO whose bytes originate outside gitid's control
- The fixture-versus-policy consistency test (PD32 test 9) prevents silent drift between the two literal tables the import-graph gate requires to stay separate
- Task 2's deferred work (overlay builders, ref-name validator, new seams) is the highest-complexity subset of the phase; deferring it to a follow-up execution wave allows this critical contract work to land and be verified before building on top

---

**Plan:** 09.6-01  
**Status:** complete (2/3 tasks)  
**Gate:** ready for Task 2 (override contract)  
**Verified:** all completed tasks pass their behavior tests; no deviations from spec
