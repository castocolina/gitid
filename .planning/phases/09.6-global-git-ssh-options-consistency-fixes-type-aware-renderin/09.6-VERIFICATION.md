---
phase: 09.6
status: gaps_found
verified: 2026-09-19
head: 0db0acf
---

# Phase 9.6 verification

## Passed

- All six plan SUMMARY files exist (`09.6-01` .. `09.6-06`).
- `make lint` — 0 issues (main checkout, no nested worktree).
- `make gate-visual-regression` — PASS, 68 frames.
- Cited Options PTY evidence after `7d097a8`:
  - `TestGlobalGit_RealPTYFallbackPairSet`
  - `TestGlobalGit_RealPTYFallbackPairClear`
  - `TestGlobalGit_RealPTYFallbackPairEdit`
  - `TestGlobalSSH_RealPTYEnumRowEdit`

## Gaps

1. `CommitGlobalSSHOverride` / `CommitGlobalGitOverride` do not use the overrides slice. Enum PTY proves a file change and a backup, not the cycled value on disk. UXG-05 may be unmarked-complete incorrectly.
2. Eight `cmd/gitid` unit failures already exist on `9c73239` (path-outside-managed-home / custom-directive). Still red.
3. Full `make test-e2e` is red on CreateFlow upload, dummy-demo, install-script, and release tests. Out of Options scope but blocks a green phase battery.

## Next

Smallest corrective plan: make override commits apply `RequestedValue`, with a PTY assertion that the written Host block contains the cycled `StrictHostKeyChecking` value. Then re-run the Options PTY subset and `make lint`.
