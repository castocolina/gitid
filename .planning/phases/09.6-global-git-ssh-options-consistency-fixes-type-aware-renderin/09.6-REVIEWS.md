---
phase: 09.6
reviewers: [opencode-plan-review]
reviewed_at: 2026-09-08T23:56:09Z
plans_reviewed: [09.6-01-PLAN.md, 09.6-02-PLAN.md, 09.6-03-PLAN.md, 09.6-04-PLAN.md, 09.6-05-PLAN.md, 09.6-06-PLAN.md]
models:
  opencode-plan-review: "router-env/my-plan-review (reasoning=high)"
model_sources:
  opencode-plan-review: "pinned"
---

# Cross-AI Plan Review — Phase 09.6 (Cycle 3, final)

<!-- gsd:plan-revision-conflicts:begin -->
## Plan-Revision Conflicts
(none recorded)
<!-- gsd:plan-revision-conflicts:end -->

> **Reviewer availability note (cycle 3, final cycle).** The project's configured
> default reviewer instance, `opencode-plan-review` (backed by
> `router-env/my-plan-review`), failed through the GSD `review-lane invoke`
> wrapper on two attempts (`OpenCode review returned no assistant text` / exit
> status 1 — the same backend-instability class recorded in cycles 1-2), as did
> the secondary configured instance `opencode-sol` (`openai/gpt-5.6-sol`). Both
> failures were reproduced and diagnosed by the orchestrating session: the
> underlying `opencode` CLI itself works and the same prompt succeeds when
> invoked directly (bypassing the wrapper) with a long-enough timeout — the
> review genuinely takes several minutes of live source-tracing (grep/read
> tool calls against this repo) before it emits its final markdown, longer
> than the wrapper's internal budget allows. The orchestrating session invoked
> `opencode run --model router-env/my-plan-review` directly against the same
> assembled review prompt (project context, all 6 plan files, the cycle-2
> REVIEWS.md for reference, plan-coverage manifest) with a 580s timeout; it
> completed cleanly (exit 0) and produced the full per-plan review below.
> Per the "final cycle" fallback instruction, no further reviewer CLIs
> (gemini/claude/codex/cursor/antigravity) were attempted once one lane
> produced a complete, source-grounded result. **This review reflects a
> single reviewer's voice, not a cross-AI consensus.** Every HIGH and
> actionable finding below was independently re-verified by the orchestrating
> session against the live repository (file:line citations re-checked) before
> being counted in the cycle summary; the verification is noted per finding.

## OpenCode Review (opencode-plan-review)

> **Reviewer method note.** Every resolution claim below was verified against the live repository (source reads at the cited `file:line` anchors), not taken from the planner's self-report. The three cycle-2 HIGHs are confirmed genuinely resolved. Verification also surfaced **one new HIGH cross-plan gap** the replan introduced by correcting the exclusion semantics (see Cross-Plan Assessment): the `alias.co` visual-gate anchor.

## 09.6-01

**Summary**
A genuine contract-first plan: value-kind vocabulary, both option-value seams with a provably pure plan method, one overlay builder per screen shared by preview and commit, a real ref-name validator, and the `!` narrowing — all landed before any screen grows an editor. Its premises about today's code are accurate at every anchor checked.

**Strengths**
- PD14's preview-purity premise is real and the fix is structural: `CommitGlobalSSH`/`CommitGlobalGit` are indeed the only layers asserting `confirmationAlreadyObtained` (cmd/gitid/wiring.go:1993-2018, :2479-2503), and the plan method composing bytes without entering any lifecycle function removes the divergence class, pinned by Test 4 (bytes unchanged + no backup file).
- The "one builder, three callers" claim is well-founded: `GlobalGitApplyPlan`'s overlay loop (wiring.go:2438-2458) is line-for-line the same logic `runGlobalGitApply`'s plan stage runs (lifecycle.go:1162-1187) — verified both; extraction into one builder is the correct de-duplication.
- PD31 corrects the cycle-2 bundle-override finding properly: refusal by name in the overlay builders at the same position as the existing unknown-key refusal (verified at lifecycle.go:826-829), while `WriteRequestedValueFor` keeps the resolver contract.
- PD32's anti-drift design is sound and its key premise verifies: `TestNoBackendAllowlist` runs `go list -deps` (internal/dummytui/nobackend_test.go:37) without `-test`, so the test-only `internal/dummytui` import in `cmd/gitid/wiring_test.go` genuinely cannot widen the allowlist.
- Task 3 targets a verified defect: `optionRow` shares `styleWarning.Render("!")` between `GlobalSSHNeedsAction` and `GlobalSSHDiffers` (internal/tuikit/globalssh.go:1500-1503), and the existing `TestOptionRowNeedsActionAndDiffersShareWarningGlyph` (internal/tuikit/globalssh_test.go:621-639) currently locks the defect as correct — the plan correctly rewrites it in the same commit.
- Tests 3b/3c genuinely close the cycle-2 validator-table-completeness finding (Unicode/CJK/emoji, control/DEL, repeated constructs, `HEAD`, `refs/heads/x`, plus a seeded 500-candidate randomized differential loop against the real binary).

**Concerns**
- **LOW:** Task 1's "classify the WHOLE table" instruction and PD7's table verify against the live policy tables (six SSH rows at internal/globalssh/policy.go:32-37; twelve Git rows at internal/globalgit/policy.go:124-243) — accurate, but `UseKeychain` is `Platform: "darwin"`, so on Linux it classifies as toggle yet renders not-applicable; Test 3's "every non-enum row carries an empty value list" is fine, but no test pins that a platform-excluded toggle row still classifies (a future reclassification there is invisible on Linux CI). Cosmetic, not blocking.
- **LOW:** The action text routes SSH `CurrentValue` through `SanitizeDisplayValue` "while in each struct literal" — good — but `GlobalSSHOptionStates` also carries `Recommended` and `Provenance` composed from machine state (`b.globalSSHProvenanceLabel(st)`); the plan scopes sanitization to `CurrentValue` only without saying why the others are exempt. Probably fine (gitid-composed), but one sentence would pin it.

**Suggestions**
- Add one test row pinning `UseKeychain`'s kind as toggle unconditionally of platform.
- State explicitly why `Provenance`/`Recommended` need no sanitization pass.

**Risk Assessment: LOW**
The contract plan's every source premise verified; the remaining notes are documentation-tightening.

---

## 09.6-02

**Summary**
A well-shaped tracer that proves the whole editor architecture on one row before any sideways expansion. Its two new authorities (PD33, PD34) are resolved from the live source rather than assumed, and both check out.

**Strengths**
- PD34's claims verify exactly: `ctrl+p` is intercepted by `App.handleKey` before any screen handler (internal/tuikit/app.go:~433), and `ctrl+c` is handled nowhere in `internal/tuikit` — the only `tea.Quit` outside tests is the quit overlay at app.go:449. The named pass-through constant plus Test 12 is the right contract.
- PD33 closes the cycle-2 exotic-current-value finding completely: verbatim second line (the untouched `optionRowLine2`), cursor at index 0, never appended to the set, no rewrite on open-then-dismiss — Test 11 drives all three named forms.
- The collision site is real: the generic `case "left", "right"` sub-tab switch sits where the plan says (internal/tuikit/globalssh.go:~1126), and the guard-before-it placement mirrors the verified `fieldEditing` block in globalgit.go:958-981.
- PD15's connection point is correctly named against verified call sites: `applyCeremonyFor` calls `m.backend.GlobalSSHApplyPlan(chosen)` directly (globalssh.go:724-747) and the `ceremonyConfirmed` branch dispatches `CommitGlobalSSH` (globalssh.go:925-935); the recording-stub Test 7 pins both branches.
- PD16's premise verifies: `applyChosen` filters `needsAction() && m.chosen[...]` (globalssh.go:632-638), so the staged-override arm is genuinely needed, and the already-set apply case (Test 6) is the right pin.

**Concerns**
- **MEDIUM (carried, cross-plan — this restates the Cross-Plan HIGH, not a separate item):** the plan seeds the dummy's enum rows but its fixture work, like plan 09.6-05's, never confronts the fact that the exclusion wave breaks the shared `alias.co` anchor the visual gate and two real-PTY cases depend on (cmd/gitid/gate_visual_regression_test.go:2575-2600; internal/screenshot/createflow.go:2957, :2804, :2815). Not this plan's fault, but no plan in the set owns it.
- **LOW — actionable:** Test 13 measures the open editor's height against `frameBodyRows(minFrameHeight)` "for the longest declared value set this phase carries" — `diff.colorMoved`'s six values — but that set belongs to the Git screen that arrives in plan 09.6-03. As written, this plan can only exercise a four-value SSH set through the real screen; the six-value worst case must be driven through the shared editor directly or the test silently measures the wrong bound. One clause fixes it.

**Suggestions**
- In Test 13, drive the shared editor value directly with a six-value set (not only the SSH screen's four-value set), or state that the bound is re-measured in 09.6-03 where the six-value set first exists on a screen.

**Risk Assessment: LOW**
The tracer is executable, its source claims verified, and its risks are carried elsewhere.

---

## 09.6-03

**Summary**
Correct sideways expansion onto Global Git under a strict no-porting rule, with the cycle-2 hard-gate-preview finding closed by PD35's three-place byte-level assertion. All its structural premises (two sub-tab-switch sites, `gitApplyChosen`'s Selectable filter, `baselineCeremonyFor`'s direct plan call) verify against the live source.

**Strengths**
- PD35 genuinely closes the cycle-2 finding: preview diff text contains the fallback and NOT `zdiff3`, the advisory names the substitution (verified at lifecycle.go:1179-1187), and the committed bytes carry the fallback — the consent-defect door is closed at byte level, not prose.
- The "guard precedes BOTH sites" requirement matches the source: the zero-options branch (globalgit.go:1094-1102) and the generic `case "left", "right"` (:1103-1116) are two live collision sites, and Test 2 drives the zero-row path specifically.
- Test 11 (scrolled-row editor) addresses the cycle-2 LOW and is justified by the screen's scroll window — a failure mode the SSH tracer never exercises.
- The dual-affordance finding is honestly routed forward to 09.6-04 (where the second affordance comes into existence) rather than half-tested here; 09.6-04's PD37/Test 11b delivers it (verified below).

**Concerns**
- **LOW:** Task 1's "bundle row renders byte-identically to its pre-plan output — asserted against a recorded expected line" is the right contract, but no existing golden captures the bundle row's exact first line today; the executor will be recording a new expected line, a snapshot-of-current-behavior rather than a regression proof. Acceptable; worth knowing.
- **LOW:** Task 2 says "leave the fallback-author row's current Enter gesture alone" — correct for wave isolation, but it means an `e` key arrives on this screen one wave before the row that most needs it regestured; a temporary, dependency-ordered intermediate state. Fine as-is.

**Risk Assessment: LOW**
Well-scoped, source-accurate, and the one cycle-2 finding routed here is closed with byte-level tests.

---

## 09.6-04

**Summary**
The cycle-2 HIGH (helper-row parity claim not matching source) is genuinely resolved: PD36 replaces the false claim with three source-verified statements, delivers the always-reserved property caller-side with a line-count test, fences `identities.go` with a diff check, and defers wizard-side unification to Phase 9.7 with a verified reference. The read-path defect diagnosis also verifies exactly.

**Strengths**
- **Cycle-2 HIGH resolved, verified.** Every PD36 statement checks against the live source: `formFieldLine` renders only the field (identities.go:456-466); helper rows are emitted conditionally per caller (sshForm.view's Host branch has three cases and no default, Hostname/Port helpers are focus-conditional — identities.go:519-571); the Match-strategy STABLE HINT ZONE always draws one (identities.go:1042-1047); and `09.7-CONTEXT.md`'s D-07 ("Unify every wizard field's hint line onto the 'always reserved' pattern") exists and owns the wizard-side work. The deferral is a sequencing fact, not a scope cut, and the follow-up is recorded in PD36.
- The read-path diagnosis is accurate at every anchor: `overlaidGitOptions`' fallback branch overlays only from `DemoState` (globalgit.go:601-613), `ApplyGitGlobalEmail` is the only reducer populating that pair (store.go:455-460), and `activate()` already seeds the authoritative pair via `GitFallbackAuthorState` → `ReadGitFallbackAuthor` (wiring.go:3126-3138; the managed-block-only read is verified at gitconfig/fallbackauthor.go:75-88). Test 1 written RED against the unset sentinel is exactly right.
- The Enter/Esc defect is real and correctly characterized: the `fieldEditing` guard collapses `case "esc", "enter"` into one branch (globalgit.go:958-981, verified), and Enter currently starts editing (globalgit.go:1191-1202). PD3's snapshot-on-open (revert to what the first Enter committed, never to probe values) is the correct semantics and Test 4 drives it twice in sequence.
- PD37/Test 11b closes the routed dual-affordance finding with four assertions plus both combination cases.
- PD17's boolean-to-enum collapse is pinned by the `rg 'm\.fieldEditing'` verification grep — confirmed to also mechanically surface the existing tests that reference the field (globalgit_test.go:586-625), so nothing can be silently left behind.

**Concerns**
- **MEDIUM — actionable:** The regesture legitimately breaks existing tests that pin today's Enter-starts-editing behavior — `globalgit_test.go:586-587` ("Enter on the selected fallback row must start text-editing"), `:613-614`, `:625` all read `m.fieldEditing` directly, and the mouse-click regression test at :604-625 builds on Enter-starts-editing as setup. The plan's PD8 carve-out ("unless the assertion is provably about a behavior CONTEXT.md changed") covers rewriting them, but no task enumerates them, and the click-regression test's *setup* (not its assertion) depends on the retired gesture — it must be re-seeded via the edit key, a non-obvious edit. The verification grep will catch the identifier, but "which tests must be rewritten vs. deleted" is unplanned work in a plan whose own reversibility rating is "costly" for exactly this span.
- **LOW — actionable:** Test 7b's equal-line-count check across three helper states is a good proxy for no-reflow, but the fallback-pair editor (two fields) and the single-field editor have different baselines; the plan doesn't say whether 7b applies to both modes or only the free-text mode. One clause.

**Suggestions**
- Add to Task 2's action: enumerate the three `fieldEditing`-reading tests (globalgit_test.go:586-625) and state that the click-regression test's setup is re-seeded through the edit key, keeping its assertion (click during editing keeps editing) intact.
- State whether Test 7b covers the fallback-pair editor's two-field layout as well as the single-field editor.

**Risk Assessment: MEDIUM**
The three cycle-2 findings routed here are all genuinely resolved and source-verified; the residual risk is concentrated in the un-enumerated legitimately-breaking tests around the gesture change — caught mechanically at execution, but unplanned.

---

## 09.6-05

**Summary**
The structural half is now correct: raw-key partition proof (PD20), nothing-vanishes with a real three-projection resolution rule (PD38), atomic DTO extension with managed-prefill preservation (PD39), a tested scope predicate (PD40), and a machine-checkable absence test (PD28). All premises verify. But correcting the exclusion from display-key to member-key space silently breaks the visual gate's shared `alias.co` anchor and two existing real-PTY cases, and neither this plan nor 09.6-06 owns that breakage — see the HIGH in the Cross-Plan Assessment.

**Strengths**
- PD20's premise verifies exactly: `AllGitSetKeys` today computes `_, policyBacked := globalgit.PolicyFor(k.Key)` and appends anyway (wiring.go:2113-2155, verified) — the display-key lookup can never match `core.autocrlf` or `alias.st`, and the tag-not-skip behavior is real. `PolicyForMember`'s doc comment names this exact collision-detection need (policy.go:271-287). The fix is correct.
- PD38 genuinely closes the cycle-2 "formal third projection" finding: three named projections, `SuppliedByManaged` decided by the block (via `ReadGitFallbackAuthor`, verified) rather than file identity — the same-file-outside-block case is correctly identified as the one a file comparison gets wrong — with a six-row resolution table asserting the effective value equals `git config --get`, and a six-row display contract. The premise that `AllSetKeys` returns only last-wins + origin verifies (properties.go:5-56).
- PD39 closes the atomicity finding properly (Task 1's file list is exactly the consumer set, one commit) and adds the managed-prefill rule with a dedicated test (Test 14) — the silent-adoption threat (T-09.6-41) is a real catch.
- PD40's frame-scan hand-off to 09.6-06 Task 2 is explicit and 09.6-06 delivers it (verified below).
- The occurrence-count deferral is defensible and coherent: the general list already carries `ValueCount` (properties.go:17, surfaced in `GitSetKeyView`), so the deferral is specifically about the fallback-author DTO, and the multi-valued case is pinned behaviorally by Test 12's `git config --get` equality. **Independently re-verified: legitimate, no action needed.**

**Concerns**
- **HIGH — verified, cross-plan, primary ownership here:** PD20's `ManagedRawGitKeys()` includes every bundle member key, and `alias.co` is a member of the alias row. Independently confirmed against the live repository:
  - `internal/globalgit/policy.go:202` lists `{Key: "alias.co", Recommended: "checkout"}` as a member of the `alias` bundle row (policy.go:197-208), so it is excluded by the new member-key-space exclusion.
  - `cmd/gitid/gate_visual_regression_test.go:2587-2590,2648` seeds `[alias] co = checkout` as the deterministic anchor and asserts, in the test itself, that the entry must still be present — with a comment stating it is "deliberately NOT one of GGIT-01's curated Policy keys" (true under the old display-key `PolicyFor`, false under the new member-key exclusion).
  - `internal/screenshot/createflow.go:2957` sets `StateMarker: "alias.co"`, and lines 2804/2815 assert `contains:"alias.co"` as two RegionDisposition predicates for the `ggit-set-keys-list` visual state — all three break once the key is excluded.
  - `e2e/global_git_pty_e2e_test.go:508` seeds `user.name`/`user.email`/`alias.co` and requires ≥2 visible; all three become excluded keys under PD20.
  - Grep confirms plan 09.6-05 mentions "alias" only in prose about the exclusion mechanism (lines 41, 213, 250) — never `alias.co`, `ggit-set-keys-list`, `deterministicGlobalGitFixture`, or the gate test names — and its `<verify>` block (line 289) runs only targeted `go test -run` patterns, never `make gate-visual-regression`. `make test`'s only gate dependency is `gate-copy-freeze` (Makefile:420), so `make gate-visual-regression` genuinely never runs in this plan's or 09.6-06's verification chain. **Verified: this is a real, unowned regression the replan introduces.**
- **MEDIUM — actionable:** The dummy's general-keys fixture is "Spans two scopes (global, system) and 20 rows to exercise the scroll window too" (fixturebackend.go:~745-775, verified — and 10+ of those rows are policy-managed: alias.*, color.*, core.autocrlf, core.eol, core.ignorecase, merge.conflictstyle, diff.colormoved, pull.rebase, push.autosetupremote, fetch.prune, init.defaultbranch). PD22's prose names only the `init.defaultbranch` row while Task 1 behavior Test 11 requires the full sweep — so the fixture collapses to ~3-6 rows, the scroll-window coverage rationale dies, the disposition prose "the dummy renders FixtureBackend's frozen 20-row AllGitSetKeys() slice" (createflow.go:2803) goes stale, and no plan decides whether to re-seed non-policy replacement rows to preserve the deliberately-large fixture property.
- **LOW (subsumed by the HIGH above, not a separate item):** Task 2 point 7 enumerates "every breadcrumb needle and every bare label needle" for the real-PTY cases — label needles only; the seeded-key *content* assertions are the bigger change and are covered by the HIGH's fix.

**Suggestions**
- Add a task (or extend Task 1/Task 3): replace the gate's deterministic anchor with a genuinely non-policy key (e.g. `core.editor`/`core.pager`, already in both fixtures), update `ggit-set-keys-list`'s StateMarker and both `contains:"alias.co"` predicates, update `deterministicGlobalGitFixture`'s now-false doc comment, and re-pin both real-PTY set-keys cases with a non-policy seed — all in the same commit as the exclusion, per the plan's own PD23 atomicity discipline.
- Add `make gate-visual-regression` to plan 09.6-05's verification block, or state explicitly why the gate is only re-run in wave 6 (and accept it red between waves — which the phase's own PD29 reasoning says is not acceptable for evidence-bearing gates).
- Decide and record: does the dummy's general-keys fixture get re-seeded with non-policy rows to preserve scroll coverage, or does it shrink and the geometry fixtures get re-derived? Either is fine; unstated is not.

**Risk Assessment: MEDIUM-HIGH**
The plan's own design is verified-correct and closes its cycle-2 findings genuinely, but the member-key correction has an un-owned blast radius across the visual gate and e2e suite that its verification block cannot see.

---

## 09.6-06

**Summary**
The closeout plan genuinely resolves both cycle-2 HIGHs routed to it: PD41 machine-links Route-B evidence with a four-part mechanism whose premise verifies (the validator today checks only non-emptiness/prefix/classification at createflow.go:765-780 against a four-field `SurfaceNonApplicability` at :118-125), and the UI-SPEC closeout walk (PD43) is backed by a document that exists and is source-grounded. The one residual is that the differs-state Route A flip mutates 07-era registry entries whose packet-side mirror no task names, and the plan inherits the wave-5 gate breakage described above.

**Strengths**
- **Cycle-2 HIGH resolved, verified.** PD41's premise is accurate — `ValidateScreenSpecs` never verifies a named test exists — and the fix is concrete and additive: `EvidenceTest` field, `UXG-D-` prefix requirement scoped to this phase (pre-existing records keep validating; the prefix vocabulary precedent verifies at createflow.go:660-740), a source walk over `e2e/`'s `func TestXxx` declarations written RED against a bogus name, and an anchored `-run` execution treating "no tests to run" as failure. No third classification value is added (verified: `validDifferenceClassification` accepts exactly `ux-improvement`/`defect`, createflow_packet.go:1228).
- PD42's persistent negative control is a real upgrade over the manual perturbation ritual: in-memory region perturbation + inequality assertion naming screen and region, runs on every commit.
- PD43's claims verify: the ROADMAP row was actually corrected at planning time (`0/6` at ROADMAP.md:673, verified) and the grep in Task 3's verify chain fails on no match; the frame-scoped absence scan lands here as PD40 promised, observed RED before re-promotion.
- PD23's atomicity premise verifies: both capture functions hard-fail on an applicable spec with a missing/empty frame (createflow.go:1965-1976, :2099-2111, verified), which is exactly why registry-only registration was rejected.
- The Requirement §T premise verifies: UXG-01..05 and their traceability rows exist with Pending status (REQUIREMENTS.md:544-592, :772-776), so Task 3 is an UPDATE instruction, accurate for the current tree.

**Concerns**
- **MEDIUM — actionable:** Route A for the differs state flips `ggit-options-differs-row` from non-applicable-on-both-surfaces (createflow.go:2891 area; the non-applicability convention and prose-only evidence verify at :2062-2069) to applicable-on-both. That mutates 07-era registry entries whose reasons are also mirrored in the packet side — `createflow_packet.go:1451` cross-validates `RegionDisposition.NonApplicabilityReason/Decision/Classification` against the spec's `NonApplicability` records — so removing those records requires updating REGION-DIFFS/packet consumers in the same commit. No task names the packet-side mirror; Task 1's file list does not include `createflow_packet.go`.
- **MEDIUM (inherited, subsumed by the Cross-Plan HIGH — not a separate item):** the plan re-promotes frames "the rename invalidated" and "Task 1's differs seeding changed," but the wave-5 exclusion also invalidates `ggit-set-keys-list`'s content (the `alias.co` anchor) — this plan is the natural owner of that repair and does not currently know it needs to be.
- **LOW — actionable:** PD41 proves a named test *exists* and *runs*, not that it *asserts the state in question* — a test could exist, run green, and evidence nothing about the non-applicable state. Task 2's frame promotion narrows this (the frame must come from that case) but the linkage "this test's frame actually shows this state" remains prose. Acceptable residual; worth one sentence in the SUMMARY template asking the executor to quote the frame line that evidences the state.

**Suggestions**
- Add `internal/screenshot/createflow_packet.go` to Task 1's files and one behavior test: flipping a spec from non-applicable to applicable leaves the packet-side region dispositions consistent (or removes the stale mirror atomically).
- In Task 2's promotion set, explicitly include the set-keys frames invalidated by 09.6-05's exclusion (once that plan fixes the anchor, the frames change again) — or better, fix the anchor in 09.6-05 so this plan promotes a stable target.

**Risk Assessment: MEDIUM**
Both routed HIGHs are genuinely closed with verified mechanisms; the residuals are the packet-side mirror of the Route A flip and the inherited wave-5 gate breakage.

---

## Cross-Plan Assessment

**Resolution-claim verification (the cycle-3 mandate) — independently re-checked, not taken on the planner's word:**
- **Cycle-2 HIGH #1 (missing UI-SPEC): RESOLVED.** `09.6-UI-SPEC.md` exists, is referenced from every plan's `<context>`, is grounded in live source (its claims about `formFieldLine`/`optionRow`/`app.go:433`/the conditional helper rows all verify), is made binding by PD30, and is walked rule-by-rule at close by 09.6-06 Task 3 under PD43.
- **Cycle-2 HIGH #2 (Route-B not machine-checkable): RESOLVED.** PD41's premise (validator checks only string non-emptiness) is source-verified; the four-part fix (field + prefix-scoped requirement + source walk + actual run) is concrete, additive, and tested RED-first.
- **Cycle-2 HIGH #3 (helper-row parity claim): RESOLVED.** PD36 replaces the false claim with three source-verified statements; `identities.go` is fenced by a diff check and Test 13; the always-reserved property is delivered caller-side with Test 7b's line-count check; the deferral to Phase 9.7 D-07 is a verified sequencing fact (the D-07 text exists in `09.7-CONTEXT.md`).
- **Deferred (a) wizard-side unification:** documented in 09.6-04's Round 2 ledger with the verified 9.7 reference and a traceable follow-up — legitimate, rationale sound.
- **Deferred (b) occurrence-count field:** documented in 09.6-05's Round 2 ledger; coherent given `SetKey.ValueCount` already exists for the general list and the multi-valued behavior is pinned by `git config --get` equality — legitimate, rationale sound.
- **The other ~12 cycle-2 actionable findings** (exotic current values, bundle-override visibility, fixture consistency, dummy staged-preview, validator table completeness, hard-gate preview bytes, scrolled-row guard, dual affordances, Tab semantics, partial/external pair display, atomic seam extension, source-walk scope, negative controls, ROADMAP count, duplicate-command rationale): each traces to a named PD and a named test; spot-verified the premises of all of them and found no hollow claims.

**NEW finding from this cycle's source verification:**
- **HIGH (verified) — the `alias.co` shared anchor breaks under PD20's member-key exclusion, with no owner in any plan.** See 09.6-05's Concerns above for the full verification chain (policy.go:202, gate_visual_regression_test.go:2587-2648, createflow.go:2804/2815/2957, e2e:508/560-563 — all independently re-checked against the live repository). Plan 09.6-05's verification block does not run `make gate-visual-regression`, and `make test`'s only gate dependency is `gate-copy-freeze` (Makefile:420) — so wave 5 can land fully green per its own checks with the phase's standing visual gate broken.

**Overall Risk: MEDIUM**

The replan's self-report on the three cycle-2 HIGHs is accurate: all three are genuinely resolved (source-verified, not just re-worded), both deferrals are legitimate and documented, and the closed findings carry real tests. The plan set is the strongest of the three cycles — its diagnoses of live code are correct at essentially every anchor checked. The one thing keeping this from LOW is that correcting the exclusion semantics from display-key to member-key space (itself the right fix) silently invalidates the visual gate's shared anchor and two real-PTY cases, and the breakage sits precisely in the blind spot of plan 09.6-05's verification chain. That repair — re-anchoring the gate seed, the `ggit-set-keys-list` spec, and the set-keys e2e cases on a genuinely non-policy key, in the same commit as the exclusion — should be assigned before execution; everything else is addressable in flight.

---

## Consensus Summary

Single reviewer this cycle (see availability note above) — no cross-model corroboration. All findings below are this reviewer's, independently source-verified by the orchestrating session before being counted.

### Agreed Strengths
- All three cycle-2 HIGHs are genuinely resolved with source-verified mechanisms, not re-worded claims (UI-SPEC existence and binding, machine-checkable Route-B evidence, corrected helper-row parity claim).
- Both deferrals in the Round 2 ledgers (wizard-side helper-row unification to Phase 9.7; occurrence-count field) are legitimate, documented, and rationale-sound.

### Agreed Concerns
- **HIGH:** the `alias.co` shared visual-gate/e2e anchor breaks under the corrected member-key exclusion (PD20), and no plan in the 6-plan set owns the repair; `make gate-visual-regression` never runs in any plan's verification block.
- Several MEDIUM/LOW gaps are concentrated around test-suite fallout from the plan set's two most invasive changes (the Enter/Esc regesture in 09.6-04, the exclusion-semantics correction in 09.6-05) — each traceable to a specific named test or file, not a vague risk.

### Divergent Views
N/A — single reviewer this cycle.
