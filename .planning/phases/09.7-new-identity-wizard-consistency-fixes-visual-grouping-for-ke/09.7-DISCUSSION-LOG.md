# Phase 9.7: New-Identity Wizard Consistency Fixes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-07
**Phase:** 9.7-new-identity-wizard-consistency-fixes-visual-grouping-for-ke
**Areas discussed:** Visual grouping technique, Test-connection color/persistence/log, Text-input fixed width, Hint visibility policy, Row-budget checkpoint

---

## Visual grouping technique (Key section, step 0; Git-identity fields, step 2)

| Option | Description | Selected |
|--------|-------------|----------|
| Bold header + blank line | Zero row cost, matches existing "Match strategy"/"Key" header pattern | |
| Bordered box (PreviewBlock) | Max visual clarity, costs ≥2 border rows, risks the documented 30-row budget | |
| Side rule / indentation | No precedent in codebase | |
| Hybrid (1 + 2) | Bold header by default; bordered box only where per-step budget allows | ✓ |

**User's choice:** Hybrid — bold header/blank-line as default, bordered box where budget allows.
**Notes:** User explicitly widened the audit scope: "eso deberia ver en todo el wizard si hay algo mas que sea aplicable este patron... solo llegue al step 1 y 2 del wizard pero debes revisar todos los cards del wizard." This drove a full re-audit of all 4 wizard steps + the Register-key modal (see CONTEXT.md D-02), not just the two steps the user had personally seen.

**Follow-up confirmation — grouping scope:**

| Option | Description | Selected |
|--------|-------------|----------|
| Confirmed scope: Key (step 0), user.name/email+ForceSSH (step 2), Demo-failure-control vs warning/upload (step 1); steps 3 and Register-key modal unchanged | Covers the 3 real inconsistency points found in the code audit | ✓ |
| Add something else | User wanted to name another cluster/screen | |

**User's choice:** Confirmed scope as proposed.

---

## Test-connection: color, persistence, typed log

| Option | Description | Selected |
|--------|-------------|----------|
| Reinforce glyph+color only | Zero new logic, but doesn't fix the "everything reads blended together" problem | |
| Glyph+color + blank-line separation | Attacks the real cause, reuses existing styles | ✓ |
| Explicit label prefix ("WARNING:", "ERROR:") | More accessible but redundant with existing glyphs | |

**User's choice:** Glyph+color + blank-line separation.

**Follow-up — warning persistence:**

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed in the proof/log viewport | proofText() is already the immutable session transcript — reuse it | ✓ |
| Persistent banner in wizard footer | Visible on every step, but a new UI element with no precedent | |
| Repeat the warning in Review (step 3) | Matches reviewCeremony()'s existing keyUnused/resultHint pattern, but doesn't cover the step-2 gap | |

**User's choice:** Fixed in the proof/log viewport.

**Follow-up — typed log:**

| Option | Description | Selected |
|--------|-------------|----------|
| Typed enum per entry | proofText() builds a list of typed entries (info/success/warning/error) the viewport colors per-entry | ✓ |
| Plain text + prefix-heuristic coloring | Faster to implement but fragile against copy changes and viewport truncation | |

**User's choice:** Typed enum per entry.

---

## Text-input fixed width

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed width on all fields, value at Claude's discretion | Consistent with the archived POC precedent (P0-1); planner sizes each field to its longest realistic value | ✓ |
| Fixed width only on the worst offenders (SSH Host, email) | Fewer changes, but leaves inconsistency between fields | |
| Defer out of 9.7 entirely | Confirms the user's original "just curiosity" framing | |

**User's choice:** Fixed width on all fields, exact value left to Claude's discretion.

---

## Hint visibility policy

| Option | Description | Selected |
|--------|-------------|----------|
| Always-reserved hint row everywhere | Eliminates the layout jump entirely, matches Match strategy's existing pattern; costs a fixed row per field | ✓ |
| Grow the wizard's row budget first | Avoids compromising layout, but a larger, undiscussed scope change | |
| Keep the mix, document when each pattern applies | Fewer code changes, doesn't fix the reported jump | |

**User's choice:** Always-reserved hint row everywhere.

---

## Row-budget checkpoint (cross-cutting)

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit planner gate (mirrors 09.4 D-06) | Planner measures actual row cost per step with all decisions applied together before implementation, decides what to trim if over budget | ✓ |
| Trust it fits, react if a PTY test fails | Less upfront planning work, but the codebase already documents this exact failure mode (CR-08 comment) | |

**User's choice:** Explicit planner gate.

---

## Claude's Discretion

- Exact numeric fixed width per text input.
- Exact wording/placement of blank-line separators — follow existing `helperLine`/`formFieldLine` conventions.
- Which specific faint/explanatory lines to compact or drop per step if the row-budget measurement comes up short (must be recorded as a trade-off, not silently dropped).

## Deferred Ideas

None — discussion stayed within phase scope. Phase 9.6 (sibling inserted phase) separately covers the Global Git/SSH Options screens.
