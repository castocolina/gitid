---
phase: 09.7-new-identity-wizard-consistency-fixes-visual-grouping-for-ke
plan: 01
type: execute
wave: 1
cross_ai: true
depends_on: []
files_modified:
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - e2e/create_flow_pty_e2e_test.go
autonomous: true
requirements: [UXW-01, UXW-02, UXW-03, UXW-04, UXW-05]
estimate:
  tokens: 80000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "Step 0 Key cluster, step 1 demo-failure cluster, and step 2 author cluster each have a bold header plus a blank-line separator (D-01, D-02)."
    - "Differently typed Test-connection blocks are separated by a blank line (D-03)."
    - "keyUnused warning is appended to the typed proof transcript and remains after leaving step 1 (D-04, D-05)."
    - "proofText is a slice of typed entries colored at render time, not a regex over one string (D-05)."
    - "Every wizard sshForm and gitForm text input calls SetWidth (D-06)."
    - "Every wizard field reserves its helper/hint row while blurred (D-07)."
    - "A row-budget test fails if Continue/Skip is missing from the 100x30 wizard pane after all of the above (D-08)."
    - "Step 3 ceremony and Register-key modal are unchanged (git diff empty on those render paths except shared newTextInput width)."
    - "Real-PTY coverage exists for grouping headers, persistent warning in the proof viewport, and no layout jump on Hostname focus."
    - "cross_ai: true; English; no classify.go edits; no real ~/.ssh writes."
---

<objective>
Make the New-Identity wizard visually consistent on steps 0–2: grouped
clusters, typed persistent proof, fixed-width inputs, always-reserved hints,
without blowing the 30-row pane.
</objective>

<context>
@.planning/phases/09.7-new-identity-wizard-consistency-fixes-visual-grouping-for-ke/09.7-CONTEXT.md
@.planning/phases/09.7-new-identity-wizard-consistency-fixes-visual-grouping-for-ke/09.7-UI-SPEC.md
@AGENTS.md
@.planning/ONESHOT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Grouping headers, reserved hints, row-budget gate</name>
  <files>internal/tuikit/identities.go, internal/tuikit/identities_test.go</files>
  <behavior>
    - RED: tests fail while Key/author/demo-failure clusters have no bold header.
    - After: headers present; Hostname/Port helper rows occupy a row while blurred; wizard step 0 and step 2 View() at 100x30 still contain Continue or Skip.
  </behavior>
  <action>
    Implement D-01/D-02/D-07. Measure D-08; compact faint lines if Continue/Skip would fall off. Do not box unless the budget allows. Do not touch ceremony/register-key independently.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit -run 'TestWizard|TestSSHForm|TestGitForm'</automated>
  </verify>
  <done>Headers and reserved hints exist; row-budget test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Typed proof transcript and persistent keyUnused warning</name>
  <files>internal/tuikit/identities.go, internal/tuikit/identities_test.go</files>
  <behavior>
    - RED: proofText() is still a single string; warning gone after leaving step 1.
    - After: typed entries; warning remains in proof after advancing; colors use existing theme roles.
  </behavior>
  <action>
    D-03/D-04/D-05. Structural typed list, not regex coloring.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit -run 'TestWizard|TestProof|TestFrozenReachable'</automated>
  </verify>
  <done>Warning persists in proof; entries are typed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fixed input widths and real-PTY proof</name>
  <files>internal/tuikit/identities.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - RED: newTextInput does not SetWidth; PTY does not see Key header / persistent warning.
    - After: every wizard input has a fixed width; PTY cases pass; make lint 0; classify.go untouched.
  </behavior>
  <action>
    D-06. Add PTY coverage. Run make lint. Do not expand into CreateFlow upload failures.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit ./e2e -tags e2e -run 'TestCreateFlow|TestWizard' ; make lint</automated>
  </verify>
  <done>Widths set; PTY green for this phase's new assertions; lint 0.</done>
</task>
</tasks>
