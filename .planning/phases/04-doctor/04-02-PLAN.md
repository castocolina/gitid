---
phase: 04-doctor
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - internal/doctor/checks/deps.go        # OVERWRITES the Plan 01 stub (same path, same CheckDeps signature)
  - internal/doctor/checks/deps_test.go
  - internal/doctor/checks/baseline.go    # OVERWRITES the Plan 01 stub (same path, same CheckBaseline signature)
  - internal/doctor/checks/baseline_test.go
  - internal/platform/platform.go
  - internal/platform/platform_test.go
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "`gitid doctor` reports each missing required dependency (ssh, ssh-keygen, ssh-add, git) with a per-OS install hint (brew/apt/dnf/pacman) (DOC-01)"
    - "A missing optional clipboard tool is reported as an info finding, not an error (DOC-01, D-05)"
    - "Baseline family reports excludesfile wiring, baseline [include] resolution, ignorecase drift, and curated-excludes presence (D-16)"
    - "core.ignorecase=true is reported as a warning expecting false (D-16/D-17 locked-value carve-out)"
    - "deps.go and baseline.go are OVERWRITTEN in place (the Plan 01 stub files), not added — no CheckDeps/CheckBaseline redeclaration; `go build ./... && go test ./internal/doctor/...` passes after the overwrite"
  artifacts:
    - path: "internal/doctor/checks/deps.go"
      provides: "CheckDeps() — composes deps.Detect + platform.InstallHint (DOC-01); OVERWRITES the Plan 01 stub"
      contains: "func CheckDeps"
    - path: "internal/doctor/checks/baseline.go"
      provides: "CheckBaseline() — folds D-16 four checks via injected ReadBaselineState; OVERWRITES the Plan 01 stub"
      contains: "func CheckBaseline"
    - path: "internal/platform/platform.go"
      provides: "InstallHint extended to git + clipboard tools (DOC-01 per-OS)"
      contains: "func InstallHint"
  key_links:
    - from: "internal/doctor/checks/deps.go"
      to: "doctor.Deps.DetectTools / InstallHint / CurrentOS"
      via: "injected functions composing internal/deps + internal/platform"
      pattern: "deps\\.(DetectTools|InstallHint|CurrentOS)"
    - from: "internal/doctor/checks/baseline.go"
      to: "gitconfig.ReadBaselineState (via injected dep)"
      via: "BaselineState.BaselineKeys[core.ignorecase] drift check + curated excludes"
      pattern: "ignorecase"
---

<objective>
Two report families that compose already-proven primitives: Dependencies (DOC-01) via deps.Detect +
an extended platform.InstallHint, and Baseline (D-16) via the existing gitconfig.ReadBaselineState.
This plan OVERWRITES the two per-family stub files Plan 01 created (`internal/doctor/checks/deps.go`
and `checks/baseline.go`) in place — same path, same `CheckDeps`/`CheckBaseline` signature — so there
is no function redeclaration and no shared-file edit. After this plan, `gitid doctor` shows real
findings in the `=== Dependencies ===` and `=== Baseline ===` sections that were empty stubs after Plan 01.

Purpose: Knock out the two lowest-risk families (pure composition of existing readers) in parallel with
the coherence/orphans and signing/agent slices. No edits to doctor.go or cmd/doctor.go, and no edits to
any other family's file — only the two stub files this plan owns are overwritten, plus extending
platform.InstallHint — so this runs concurrently with Plans 03/04.

Output: OVERWRITTEN internal/doctor/checks/{deps.go, baseline.go} (real implementations replacing the
stubs) + new {deps_test.go, baseline_test.go}, extended internal/platform/platform.go + platform_test.go.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-doctor/04-CONTEXT.md
@.planning/phases/04-doctor/04-RESEARCH.md
@.planning/phases/04-doctor/04-PATTERNS.md
@.planning/phases/04-doctor/04-UI-SPEC.md
</context>

## Artifacts this plan produces

- `checks.CheckDeps(doctor.Deps) []Finding` — OVERWRITES the Plan 01 stub file `internal/doctor/checks/deps.go` in place (same exported signature; the file's nil-returning stub body is replaced with the real implementation).
- `checks.CheckBaseline(doctor.Deps) []Finding` — OVERWRITES the Plan 01 stub file `internal/doctor/checks/baseline.go` in place.
- Extended `platform.InstallHint` (or new per-tool hint surface) covering `git` and the clipboard tool, returning brew/apt/dnf/pacman lines per DOC-01; single-platform hint when CurrentOS is known, all four when unknown.

> Wave-2 redeclaration rule (BLOCKER 1): both target files already exist as Plan 01 stubs. This plan does
> NOT create a new file alongside them — it replaces the stub body. Read the frozen `doctor.Deps` field set
> from `04-01-SUMMARY.md` before implementing so the wiring matches Plan 01's locked contract (WARNING 4).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend platform.InstallHint to git + clipboard, then OVERWRITE the CheckDeps stub (RED→GREEN)</name>
  <files>internal/platform/platform.go, internal/platform/platform_test.go, internal/doctor/checks/deps.go, internal/doctor/checks/deps_test.go</files>
  <read_first>
    - .planning/phases/04-doctor/04-01-SUMMARY.md (the FROZEN doctor.Deps field set + per-family file map — wire CheckDeps against these exact field names; deps.go is the stub this plan overwrites)
    - internal/platform/platform.go (current InstallHint switch lines 109-123 — OpenSSH-only; CurrentOS lines ~35; extend the same switch-on-os structure)
    - internal/deps/deps.go (Detect() lines 78-86, Report.MissingRequired() lines 22-34 — what CheckDeps composes)
    - internal/doctor/doctor.go (Deps fields DetectTools, InstallHint, CurrentOS, and Finding/Severity/Family from Plan 01 — consumed here)
    - .planning/phases/04-doctor/04-UI-SPEC.md §"Install Hint Format" (per-OS line alignment, single-platform-when-known), §"Copywriting Contract" DOC-01 required-vs-optional copy (exact — variation not permitted), §"Glyph Contract" (! glyph + [info] for optional)
    - .planning/phases/04-doctor/04-CONTEXT.md D-01 (DOC-01 family), D-05 (required→error, optional→info), §Claude's Discretion (per-OS hint text for git/clipboard is planner discretion — keep brew/apt/dnf/pacman)
    - .planning/phases/04-doctor/04-RESEARCH.md §"Pattern: deps.Detect + platform.InstallHint for DOC-01"
  </read_first>
  <behavior>
    - Test (platform): InstallHint for tool "git" on os "darwin" contains "brew" and "git"; on "linux" contains apt/dnf/pacman lines; on unknown os returns all four package-manager lines.
    - Test (platform): InstallHint for the clipboard tool returns the platform-appropriate package (e.g. xclip on linux) per the discretionary copy chosen.
    - Test (TestCheckDeps / required missing): fake DetectTools returns Report{SSH:false, others true} → one Dependencies finding, Severity==error, glyph ✗, SuggestedFix contains the ssh install hint.
    - Test (TestCheckDepsOptional): clipboard tool absent (Report.Clipboard==false) → one Dependencies finding, Severity==info (! glyph), copy "Public-key copy to clipboard will not work".
    - Test (TestCheckDepsAllPresent): all tools present → no Dependencies findings.
  </behavior>
  <action>
    RED: write platform_test.go cases for the extended InstallHint signature (decide the signature — either add a `tool` parameter `InstallHint(tool, os string) string` and migrate the single existing OpenSSH caller, or add `InstallHintFor(tool, os string)`; the Plan 01 Deps.InstallHint field already assumes `func(tool, os string) string`, so adopt `InstallHint(tool, os string)` and update the existing OpenSSH call site). Write deps_test.go with fake doctor.Deps. (deps.go already holds the Plan 01 `_`-param stub; the RED test fails against it.) Commit `test(04-02): ...`.
    GREEN: extend platform.InstallHint to switch on tool name (ssh/openssh, git, clipboard) and os, returning the per-OS hint block; show only CurrentOS's line when os is known, all four when unknown (matches UI-SPEC Install Hint Format). Update the existing OpenSSH caller to the new signature. OVERWRITE internal/doctor/checks/deps.go — replace the stub body of CheckDeps(deps) with: call deps.DetectTools(); for each missing REQUIRED tool (ssh, ssh-keygen, ssh-add, git) emit an error Finding (Family=FamilyDeps, ✗, Title "<tool> missing", Explanation + fix per UI-SPEC, SuggestedFix=deps.InstallHint(tool, deps.CurrentOS())); for a missing OPTIONAL clipboard tool emit an info Finding (! glyph copy). No Fix descriptor (dep installs are report-only, D-03). Keep the exported signature byte-identical to the Plan 01 stub. Run green, commit `feat(04-02): ...`.
  </action>
  <verify>
    <automated>go test ./internal/platform/... ./internal/doctor/checks/... -run 'TestInstallHint|TestCheckDeps' && go build ./... && go test ./internal/doctor/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go test ./internal/doctor/...` passes after the overwrite — CheckDeps is defined exactly once (the stub file replaced, not duplicated): `grep -rn 'func CheckDeps' internal/doctor/checks/` returns exactly one match.
    - `gitid doctor` (or the unit test) reports a missing `git` as an error with a brew/apt/dnf/pacman hint (DOC-01).
    - A missing clipboard tool is an info finding with `!` glyph, never an error.
    - InstallHint on a known platform shows only that platform's line; on unknown, all four.
    - `make lint` passes; `go build ./...` succeeds (existing OpenSSH caller migrated to new signature).
    - RED+GREEN commits present (`git log --grep='^test(04-02)'`, `--grep='^feat(04-02)'`).
  </acceptance_criteria>
  <done>Dependencies family is real (deps.go stub overwritten in place): required→error+hint, optional→info; platform.InstallHint covers git + clipboard. No Fix descriptors (report-only).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: OVERWRITE the CheckBaseline stub — fold D-16 four checks via ReadBaselineState (RED→GREEN)</name>
  <files>internal/doctor/checks/baseline.go, internal/doctor/checks/baseline_test.go</files>
  <read_first>
    - .planning/phases/04-doctor/04-01-SUMMARY.md (the FROZEN doctor.Deps field set — wire CheckBaseline against the locked baseline-related fields; baseline.go is the stub this plan overwrites)
    - internal/gitconfig/baseline.go (ReadBaselineState lines 171+, BaselineState struct lines 26-44, BaselineKeySet lines 52-61, DefaultGitignorePatterns lines 370+ — the authoritative curated-excludes list and ignorecase=false invariant)
    - internal/doctor/doctor.go (Deps baseline-related fields + Finding/Severity from Plan 01; the injected ReadBaselineState fn / baseline file path fields)
    - .planning/phases/04-doctor/04-CONTEXT.md D-16 (four checks: excludesfile wiring, baseline [include] resolves, ignorecase drift, curated excludes present), D-17 (ignorecase=false is a locked-value carve-out), D-15/D-19 (no full content compare)
    - .planning/phases/04-doctor/04-UI-SPEC.md §"Copywriting Contract" D-16 entries (exact copy for excludesfile/include/ignorecase/curated-excludes; severity: excludesfile & include → error, ignorecase & curated → warning; which carry [fix])
    - .planning/phases/04-doctor/04-RESEARCH.md §"Baseline checks (D-16)" (compose ReadBaselineState; state.BaselineKeys[core.ignorecase]=="false"; curated subset in state.GitignorePatterns)
    - .planning/phases/03.1-baseline-global-git-config-global-gitignore/03.1-CONTEXT.md (the baseline model being health-checked: 00-baseline include floor placement, the three managed blocks)
  </read_first>
  <behavior>
    - Test (TestBaselineExcludesfile): state with core.excludesfile unset OR the ~/.gitignore_global file missing → error finding "core.excludesfile: not set or file missing", fix "run 'gitid baseline setup'".
    - Test (TestBaselineIncludeMissing): state.Installed false / include block absent → error finding "baseline [include] block missing", [fix] marker (auto-fixable re-add path exists), fix "run 'gitid baseline setup'".
    - Test (TestBaselineIgnoreCaseDrift): state.BaselineKeys["core.ignorecase"]=="true" → warning "core.ignorecase: true (expected false)", fix mentions `git config --global core.ignorecase false`.
    - Test (TestBaselineCuratedExcludes): a curated pattern from DefaultGitignorePatterns absent from state.GitignorePatterns → warning "curated entries missing", [fix].
    - Test (TestBaselineAllPass): fully-configured state (installed, excludesfile wired + file present, ignorecase=false, all curated entries present) → no Baseline findings (renderer shows ✓ lines).
  </behavior>
  <action>
    RED: write baseline_test.go with fake doctor.Deps providing a synthetic BaselineState (or a fake ReadBaselineState fn returning crafted gitconfig.BaselineState) plus the curated-pattern list. (baseline.go already holds the Plan 01 `_`-param stub; the RED test fails against it.) Commit `test(04-02): ...`.
    GREEN: OVERWRITE internal/doctor/checks/baseline.go — replace the stub body of CheckBaseline(deps) with the real implementation composing the injected ReadBaselineState; keep the exported signature byte-identical to the Plan 01 stub. Emit the four D-16 findings per UI-SPEC copy/severity: excludesfile not set or file missing → error; baseline [include] missing → error with Fix descriptor (re-add is the auto-fixable class, D-02 missing-wiring re-add — set Fix non-nil so [fix] renders; the actual fixer wiring is Plan 05, here just mark fixable); ignorecase drift (BaselineKeys["core.ignorecase"]!="false") → warning; curated excludes (DefaultGitignorePatterns subset not all present in state.GitignorePatterns) → warning with Fix. Locked-value comparison is byte-exact ("false"/"true") per D-17 — no general content compare (D-19). Run green, commit `feat(04-02): ...`.
  </action>
  <verify>
    <automated>go test ./internal/doctor/checks/... -run TestBaseline && go build ./... && go test ./internal/doctor/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go test ./internal/doctor/...` passes after the overwrite — CheckBaseline defined exactly once: `grep -rn 'func CheckBaseline' internal/doctor/checks/` returns exactly one match.
    - core.ignorecase=true yields a warning expecting false (D-16/D-17).
    - Missing excludesfile wiring and missing baseline include each yield an error finding pointing to `gitid baseline setup`.
    - A missing curated pattern yields a warning with `[fix]`.
    - A fully-configured baseline yields zero findings.
    - No `internal/filewriter` import added to internal/doctor; `make lint` passes.
  </acceptance_criteria>
  <done>Baseline family folds all four D-16 checks (baseline.go stub overwritten in place) with correct severity + exact copy + fixable markers, fully fake-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → baseline check | reads ~/.gitconfig, ~/.gitconfig.d/00-baseline, ~/.gitignore_global via injected ReadBaselineState (gitid-managed paths) |
| subprocess → deps check | deps.Detect uses exec.LookPath only (no shell, no user args) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | os.ReadFile on gitconfig/baseline/gitignore (via ReadBaselineState) | mitigate | injected reader uses gosec-G304-annotated trusted gitid-managed paths; no key material read; report prints config values (ignorecase, excludesfile) not secrets |
| T-04-06 | Tampering | exec for tool detection | mitigate | deps.Detect uses exec.LookPath (no shell); CheckDeps adds no new exec; install hints are printed strings, never executed (report-only, D-03) |
| T-04-07 | Tampering | doctor core gaining write capability | mitigate | checks/deps.go + checks/baseline.go import no filewriter; Baseline [fix] markers carry an injected Fix descriptor executed only by the Plan 05 cmd fixer |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `go test ./internal/platform/... ./internal/doctor/checks/... -run 'TestInstallHint|TestCheckDeps|TestBaseline'` green.
- `go build ./... && go test ./internal/doctor/...` pass after both stub overwrites — CheckDeps and CheckBaseline each defined exactly once in package checks (no redeclaration).
- `make lint` + `go build ./...` pass (existing OpenSSH InstallHint caller migrated).
- `grep -rn 'internal/filewriter' internal/doctor/` returns nothing.
- `gitid doctor` now shows real findings under `=== Dependencies ===` and `=== Baseline ===`.
- RED→GREEN commits present for both TDD tasks.
</verification>

<success_criteria>
- Missing required dep → error + per-OS hint; missing optional clipboard → info (DOC-01).
- Baseline family covers excludesfile, include, ignorecase drift, curated excludes (D-16) with locked-value carve-out (D-17).
- platform.InstallHint extended to git + clipboard, single-platform-when-known.
- Both families compose existing primitives with zero new writes (D-01); deps.go + baseline.go stubs overwritten in place with no redeclaration.
</success_criteria>

<output>
Create `.planning/phases/04-doctor/04-02-SUMMARY.md` when done (RED/GREEN commits, the InstallHint signature decision, which Baseline findings carry Fix descriptors for Plan 05, confirmation that deps.go/baseline.go were overwritten in place and `go build ./...` is green).
</output>
</output>
