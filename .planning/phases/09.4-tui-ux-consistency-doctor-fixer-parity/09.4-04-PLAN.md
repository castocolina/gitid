---
phase: 09.4-tui-ux-consistency-doctor-fixer-parity
plan: 04
type: execute
wave: 4
depends_on: ["09.4-03"]
files_modified:
  - internal/tuikit/globalssh.go
  - internal/tuikit/globalssh_test.go
  - internal/tuikit/mouse_test.go
  - internal/screenshot/createflow.go
  - .planning/design/global-ssh/visual-divergence-allowlist.txt
  - e2e/global_ssh_pty_e2e_test.go
  - e2e/global_ssh_storage_pty_e2e_test.go
  - .planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/ui-frames/global-ssh-subtab-strip-bordered.txt
  - .planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/ui-frames/global-ssh-subtab-strip-bordered-nocolor.txt
autonomous: true
cross_ai: true
requirements: [UXP-04]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Global SSH's Options / Storage & preview strip renders inside a visible border, in the accent color, so it reads as a nested local control rather than another instance of the main tab bar (UXP-04, D-05)."
    - "The bordered strip is legible with color disabled — the border SHAPE, not the accent hue, carries the 'this is a different hierarchy level' signal (UI-SPEC Inherited Non-Negotiables)."
    - "Clicking either sub-tab label still switches sub-tabs after the strip grows taller and gains a left border edge — the hit-test moved with the render (D8 checkpoint-2 contract)."
    - "ALL FIVE strip-bearing render states still fit the fixed 100x30 capture geometry after the strip's added rows — normal Options, zero-options Options, options-error Options, normal Storage, the apply ceremony and the storage ceremony; the frame is never widened or heightened to make room (UI-SPEC Inherited Non-Negotiables)."
    - "A real xterm SGR mouse click on either sub-tab label switches sub-tabs in the COMPILED binary through a PTY — the identified regression risk is mouse coordinate drift, and keyboard navigation does not exercise it (ONESHOT.md Rule 8)."
    - "Exactly one place in the source states how many rows the strip occupies; the option row-budget, the storage row-budget, and the click hit-test all derive from it (UXP-04)."
    - "Local left/right on this screen still means 'switch sub-tab' and never 'switch main tab' — the existing D4 keybinding contract is unchanged; only its discoverability improves (D-05)."
  artifacts:
    - "internal/tuikit/globalssh.go: a single sub-tab-strip row-count source that the three consumers derive from"
    - "internal/tuikit/globalssh_test.go: TestSubTabStrip* covering render, row accounting, and the click hit-test"
  key_links:
    - "subTabStrip()'s rendered row count <-> gssOptionsTopLines() <-> renderStorage's own independent row-budget subtraction <-> handleClick's strip row assumption — FOUR sites today, three of which hold the number as a literal"
    - "the strip's left border edge <-> handleClick's x-coordinate math, which is currently derived from the raw label lengths with a one-column leading offset"
    - "the added rows <-> the Storage sub-tab's preview-block max-line budgets, which are the tightest consumer of the 25 available body rows"
  backstops:
    - statement: "The bordered strip's extra rows do not collide with either sub-tab's existing content at the fixed 100x30 geometry; if the full box overflows, the one-row title-in-border variant is used instead."
      verification: backstop
---

<objective>
Close UXP-04: give Global SSH's local sub-tab strip a visible bordered
treatment distinct from the main tab bar's flat inline-background format, so
a user can see that left/right here switches sub-tabs rather than main tabs.

Purpose: the sub-tab strip currently renders as one unstyled text line whose
active label is marked the same way the main tab bar marks its active tab.
Nothing on screen distinguishes local navigation from global navigation, so
arriving at the second sub-tab reads as a jump the user did not ask for. D-05
resolves this visually rather than with footer copy.
Output: a bordered, accent-colored strip; one source of truth for how many
rows it costs; and the click affordance proven still working — in-package and
through a real mouse click in the compiled binary.
</objective>

<review_response>
## Cycle 1 (Codex `gpt-5.6-sol`, REQUEST CHANGES)

Cross-AI review (`09.4-REVIEWS.md`) raised
four findings against this plan. Their disposition, recorded here so the executor
and the next reviewer can see it without reading REVIEWS.md:

| Finding | Severity | Disposition |
|---------|----------|-------------|
| Task 1 explicitly commits a failing test (`TestSubTabStripRendersBordered` RED), violating the one-buildable-logical-change rule; whole-module pre-commit hooks make a red commit especially bad | HIGH | **INCORPORATED.** Task 1 no longer authors or commits the bordered test at all — it commits only the THREE tests that pass against today's strip (row accounting, click hit-test, geometry). `TestSubTabStripRendersBordered` moved into Task 2, where it is authored, observed RED locally with its output recorded, and then committed GREEN together with the border implementation. No red state is ever committed. CLAUDE.md: "TDD still drives authoring order... it does not require shipping the RED and GREEN states as separate commits." |
| Geometry coverage omits three strip-bearing states — `subTabStrip()` also prefixes the apply/storage ceremonies, the options-error state, and the zero-options state | MEDIUM | **INCORPORATED.** The geometry test is now a table over ALL FIVE strip-bearing call-site classes at 100×30, enumerated explicitly in Task 1's `<behavior>` and re-asserted in Task 2. |
| The PTY step proves keyboard navigation, not click routing — but the identified regression risk is mouse coordinate drift, and the repo already has a raw SGR mouse helper | MEDIUM | **INCORPORATED.** Task 3 now requires a REAL raw SGR press/release on BOTH sub-tab labels via the existing `clickLabelRow` technique (`e2e/create_flow_pty_e2e_test.go:123`), asserting the active label and the Storage content change. Keyboard left/right stays as a separate, additional contract. |
| The allowlist is treated like stored golden frames, but it records semantic real/dummy divergences — a border added by shared `tuikit` code appears on both sides and may need no allowlist change | LOW | **INCORPORATED.** Task 3 now requires running the gate FIRST and editing the allowlist only if the divergence set or its predicates actually changed; the expected outcome is an EMPTY diff on the allowlist file, and a non-empty one must be justified. |
| Add the two planned `ui-frames` artifacts to `files_modified` | (suggestion) | **INCORPORATED.** Both frame paths are now in frontmatter `files_modified`. |

## Cycle 2 (opencode-plan-review `local-llm-env/my-plan-review`, source-grounded)

Cycle 2 verified both of this plan's cycle-1 fixes it spot-checked — the
four-site row-accounting claim (confirmed at all four cited locations in
`globalssh.go`) and the committed-RED removal (confirmed clean:
`TestSubTabStripRendersBordered` sits in Task 2 and Task 1's three tests are
green at their commit). It raised one further finding:

| Finding | Severity | Disposition |
|---------|----------|-------------|
| The Storage-pane overflow backstop is plausible but UNMEASURED before Wave 4 — Task 2's five-state measurement is the right control, but Task 1's geometry test should record the headroom MARGIN, not just pass/fail, so Task 2's box-variant choice is data-driven rather than guessed | MEDIUM | **INCORPORATED.** The gap is real and structural: Task 1 runs in this plan's first task and Task 2's variant choice is the plan's one genuinely open design question, yet nothing carried a number between them — Task 1 asserted only "it fits today", which Task 2 already assumed. Task 1's geometry test now `t.Logf`s available/used/difference for each of the five strip-bearing states, its `-v` output is a quoted acceptance criterion, and the tightest state's margin is recorded as a single labelled number addressed to Task 2. Task 2 now READS that number first and lets it decide the variant (2+ rows -> full box affordable; 1 row -> the one-row fallback is already forced; 0 -> STOP and escalate, the backstop's premise has failed), re-measuring afterwards only to confirm the prediction. Its acceptance criteria require the choice to cite the number rather than re-derive it. |
</review_response>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-CONTEXT.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
| Symbol | File | Kind |
|--------|------|------|
| `gssSubTabStripRows()` (or an equivalent single row-count source) | `internal/tuikit/globalssh.go` | NEW — the one place the strip's height is stated |
| `TestSubTabStripRowAccountingIsSingleSourced` | `internal/tuikit/globalssh_test.go` | NEW, Task 1 (Wave 0 gap — no `TestSubTabStrip*` exists today), committed GREEN |
| `TestSubTabStripClickSwitchesSubTabs` | `internal/tuikit/globalssh_test.go` | NEW, Task 1, committed GREEN |
| `TestSubTabStripFitsFixedGeometryInEveryStripState` | `internal/tuikit/globalssh_test.go` | NEW, Task 1, committed GREEN — table over all FIVE strip-bearing states |
| `TestSubTabStripRendersBordered` | `internal/tuikit/globalssh_test.go` | NEW, **Task 2** — authored there, observed RED as scratch work, committed GREEN with the implementation |
| Real SGR mouse sub-tab click coverage | `e2e/global_ssh_pty_e2e_test.go` / `e2e/global_ssh_storage_pty_e2e_test.go` | NEW / EXTENDED, Task 3 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — the missing sub-tab-strip test scaffold, committed GREEN against today's unbordered strip</name>
  <files>internal/tuikit/globalssh_test.go</files>
  <read_first>
    - `internal/tuikit/globalssh.go` lines 45-56 — the two sub-tab label constants and the per-row line constant
    - `internal/tuikit/globalssh.go`'s `subTabStrip`, `gssOptionsTopLines`, and `handleClick`'s strip branch (its `y` assumption and its x-coordinate math derived from the raw label lengths)
    - `internal/tuikit/globalssh.go`'s `renderOptions` and `renderStorage` — note that each subtracts the strip's rows from the body budget INDEPENDENTLY, and that one of them holds the number as a bare literal with an explanatory comment rather than calling the shared helper
    - `internal/tuikit/frame.go` lines 25-45 — the fixed frame geometry constants and `frameBodyRows`
    - `internal/tuikit/globalssh_test.go` — the existing helpers for driving the screen and stripping styling from a rendered frame
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-VALIDATION.md` — the Wave 0 Requirements list, which names this gap
  </read_first>
  <behavior>
    - `TestSubTabStripRowAccountingIsSingleSourced`: the number of lines the strip actually renders equals the number the Options body-budget helper reserves for it, and equals the number the Storage renderer reserves for it. Passes today (all three agree at one row) and is precisely the guard that catches a consumer left behind when Task 2 grows the strip.
    - `TestSubTabStripClickSwitchesSubTabs`: a click on the Storage label's cells switches to the Storage sub-tab and a click on the Options label's cells switches back — driven through the real click entry point at the real rendered coordinates, not by calling the sub-tab setter directly. Passes today.
    - `TestSubTabStripFitsFixedGeometryInEveryStripState`: at the fixed 100x30 geometry, NO strip-bearing render state's body exceeds the available body rows. A table test over ALL FIVE call-site classes that prefix `subTabStrip()`, because testing only the two browse states can miss clipping in the other three: (1) Options in its normal browse state, in its findings-banner-present variant (its tallest); (2) Options in its ZERO-OPTIONS state; (3) Options in its OPTIONS-ERROR state; (4) Storage in its normal browse state, in its sentinel-preview variant (its tallest); (5) the apply ceremony and the storage ceremony, both of which also prefix the strip. Passes today.
    - The SAME test also MEASURES and REPORTS each state's headroom — `available - used`, in rows — via `t.Logf`, so the numbers are in the test output on every run and not merely implied by a pass. This is the point of running it in Wave 1 rather than Wave 4: a cross-AI review MEDIUM observed that Task 2's box-variant choice (two-row full box vs one-row title-in-border) was otherwise being made against an UNMEASURED margin. A pass/fail-only assertion answers "does it fit today", which Task 2 already knows; the margin answers "how many rows can Task 2 spend", which is the question that actually decides the variant. If the tightest state's headroom is under 2 rows, the two-row full box is arithmetically excluded before a line of border code is written.
  </behavior>
  <action>
Write these THREE tests against the CURRENT unbordered strip and commit them
GREEN. **Do not author `TestSubTabStripRendersBordered` in this task and do not
commit any red test here** — a cross-AI review HIGH flagged the previous draft
for planning exactly that. CLAUDE.md is explicit: every commit must compile and
pass the pre-commit hooks, the hooks run `make fmt` + `make lint` over the whole
module, and TDD drives authoring order without requiring the RED state to ship
as its own commit. The bordered test belongs to Task 2, where its RED state is
observed and recorded as scratch work and only the GREEN state is committed
alongside the implementation that makes it pass.

This is the Wave 0 gap the phase's validation strategy identified: no
`TestSubTabStrip*` function exists anywhere today, which means the strip's row
accounting, its click hit-test, and its geometry fit have never had a positive
test — and the click math is exactly the silent-break class this project has
hit before (a desynced click path after a render-layout change). All three
tests pass against today's code, so this task is a pure, green, additive
commit; their value is that they are already in place and already meaningful
when Task 2 changes the strip's height underneath them.

Each must be written to be meaningful BOTH before and after Task 2, not merely
to pass today:
  - The row-accounting test must compute the actual rendered line count from
    the strip's own output and compare it against what the two consumers
    reserve, rather than asserting a hardcoded number. Written that way it
    keeps working across the growth and it is the guard that catches a
    consumer left behind. A hardcoded `1` would go green today and become a
    false failure (or worse, be "fixed" to `2`) tomorrow.
  - The geometry test must measure each state's rendered body line count
    against `frameBodyRows` at the fixed geometry, so it fails loudly if
    Task 2's added rows push ANY of the five states over. Enumerate the five
    states by locating every call site of `subTabStrip()` in `globalssh.go`
    and covering each one's class — do not work from the list in `<behavior>`
    alone; it is what planning found, not a guarantee of completeness. The
    Storage browse state and the storage ceremony are the tightest consumers
    of the body budget and are what will actually settle the overflow
    question, but the zero-options and options-error states must be present
    too, since a clipping bug there would be invisible to a two-state test.
  - **The geometry test must also REPORT the margin, not just assert the
    bound.** For each of the five states, `t.Logf` the available body rows,
    the rows actually used, and the difference. Task 2 chooses between the
    two-row full box and the one-row title-in-border fallback, and that
    choice is currently a guess: the plan's own prose says the Storage
    pane's headroom is "not obvious", and a cross-AI review MEDIUM flagged
    that the choice was being made against an unmeasured margin two waves
    before Task 2 runs. Emitting the five numbers here makes it a lookup.
    Copy the tightest state's headroom into this task's SUMMARY as a
    single, prominently-labelled number — that is the budget Task 2 spends.
  - The click test must dispatch through the same entry point a real mouse
    event takes, at coordinates read from the rendered frame, so it exercises
    the coordinate math rather than bypassing it.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run 'TestSubTabStrip'</automated>
  </verify>
  <acceptance_criteria>
    - The three named test functions exist in `internal/tuikit/globalssh_test.go` and ALL THREE are GREEN at this commit. No red test is committed by this task; `TestSubTabStripRendersBordered` does not exist yet (it is Task 2's).
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` and `make lint` are green at this commit — the pre-commit hooks run the whole module, so a red commit here would fail them.
    - The row-accounting test derives its expected value from the strip's own rendered output, not from a literal — confirmed by reading the test and stated in the SUMMARY.
    - The geometry test is a table covering all FIVE strip-bearing states (normal Options incl. the findings-banner variant, zero-options Options, options-error Options, normal Storage incl. the sentinel-preview variant, and both ceremony states), asserting against `frameBodyRows` at the fixed 100×30 geometry. The SUMMARY records the measured line count and the available budget for EACH of the five, and states how the five call sites were enumerated from `globalssh.go`.
    - **The test EMITS the headroom, it does not merely assert the bound.** `go test -run TestSubTabStripFitsFixedGeometryInEveryStripState -v ./internal/tuikit/...` prints, for each of the five states, the available rows, the used rows, and their difference — quote that `-v` output in the SUMMARY. A test that only passes/fails does not satisfy this criterion.
    - **The tightest state's headroom is called out as a single labelled number in the SUMMARY, addressed to Task 2**, together with the resulting constraint: whether the two-row full box is affordable at that margin or whether the one-row title-in-border fallback is already forced. Task 2's variant choice must cite this number rather than re-deriving or assuming it (cross-AI review MEDIUM: the choice was previously unmeasured until the wave that had to make it).
    - The click test dispatches through the real click entry point using coordinates taken from the rendered frame.
    - No other test changed behavior.
  </acceptance_criteria>
  <done>The sub-tab strip has positive, green, committed test coverage for its row accounting, its click hit-test, and its geometry fit across all five strip-bearing states — the scaffold Task 2 will change the strip underneath.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Border the sub-tab strip and collapse its row accounting to one source</name>
  <files>internal/tuikit/globalssh.go, internal/tuikit/globalssh_test.go, internal/tuikit/mouse_test.go</files>
  <read_first>
    - `internal/tuikit/globalssh.go`'s `subTabStrip` and all FIVE of its call sites (the two ceremony/error paths, the zero-options path, `renderOptions`, and `renderStorage`) — every one of them prepends the strip, but only two of them account for its rows
    - `internal/tuikit/globalssh.go`'s `gssOptionsTopLines` and `handleClick`'s strip branch
    - `internal/tuikit/globalssh.go`'s `renderStorage` — its body-budget line holds the strip's height as a bare literal with an explanatory comment, a THIRD lockstep site that this phase's research did not enumerate
    - `internal/tuikit/frame.go` around lines 495-610 and 878-906 — the existing dashed border rune set, the dim border color it normally pairs with, and the helper that folds a title into a one-row border top edge
    - `internal/tuikit/theme.go` — the accent color role UI-SPEC selects for this border
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md` — the Spacing/Layout row for the sub-tab strip (which names the exact border rune set, the color choice and its rationale, and the one-row fallback), plus the Color section's "Accent reserved for" list
    - `internal/tuikit/mouse_test.go` — any existing test that clicks Global SSH body coordinates and would shift with the strip
  </read_first>
  <behavior>
    - Test 1 (`TestSubTabStripRendersBordered`, authored HERE): the rendered strip occupies more than one line and its first and last lines carry border characters from the codebase's existing dashed border rune set, in the accent color role rather than the dim gray the preview blocks use. This is the task's RED-first test — write it, RUN it against the still-unbordered strip, record the failure output, then implement. The commit contains it GREEN together with the implementation.
    - Test 2: the active sub-tab label is still visually marked inside the box, and the inactive one is not.
    - Test 3: Task 1's row-accounting test stays green — the strip's real rendered height, the Options body budget, and the Storage body budget all agree at the NEW height.
    - Test 4: Task 1's click test stays green — clicking either label still switches sub-tabs at the new coordinates, including the new left border column and the border rows being INERT rather than mis-hitting a label.
    - Test 5: Task 1's geometry test stays green across ALL FIVE strip-bearing states at the fixed 100x30 frame — not just the two browse states.
    - Test 6: with color disabled, the bordered strip is still visibly a box and the active label is still distinguishable.
  </behavior>
  <action>
**Authoring order and commit shape.** Write `TestSubTabStripRendersBordered`
first, run it against the current unbordered strip, and RECORD its failure
output — that observed RED is the evidence this task owes. Then implement the
border and commit the green test together with the production change as ONE
commit. Do not commit the red state: CLAUDE.md requires every commit to compile
and pass the whole-module pre-commit hooks, and explicitly says TDD governs
authoring order rather than commit history. (A cross-AI review HIGH flagged the
previous draft, which planned a committed-RED test in Task 1.)

Border the strip per D-05, reusing the ONE existing border rune set in this
codebase rather than introducing a new border style — UI-SPEC budgets this
phase at zero new border styles beyond one reused rune set, and the codebase's
existing dashed set is that one. Color the border with the accent role, NOT
the dim gray the preview blocks pair it with: the accent echo says "this
strip is also navigation", while the border SHAPE (against the main tab bar's
flat inline-background format) says "a nested, local control at a different
hierarchy level". Both signals are needed, and the shape one is what survives
with color disabled.

**Collapse the row accounting to one source before changing the height.**
Today the strip's height is stated in FOUR places: the renderer itself, the
Options top-lines helper, the Storage renderer's budget subtraction (as a
bare literal), and the click handler's row assumption. Three of those hold it
as a literal that is silently wrong the moment the render changes. Introduce
one function or constant that states the height, have all four derive from
it, and make Task 1's row-accounting test the guard. Do this refactor as the
FIRST move of this task, while the strip is still one row and the behavior is
provably unchanged, then change the height. Changing the height first and
fixing consumers afterwards is how the desynced-click-math defect class
already documented in this file's own history recurs.

**Update the click hit-test for both dimensions.** The strip currently
occupies a single known row and its label x-ranges are computed from the raw
label lengths with a one-column leading offset. After bordering: the label
row is no longer the first strip row, so the row test becomes a comparison
against the derived height rather than a fixed row; and the x-ranges shift by
the border's left edge plus its padding. Derive both from the same values the
renderer uses. Treat the border rows and the left border column as inert — a
click there must not activate a label.

**Settle the overflow question with a measurement, not an assumption.**
UI-SPEC's UI Considerations carries this as an explicit backstop and names
the fallback: if the full box's added rows do not fit, use the one-row
title-in-border variant via the existing helper the preview blocks already
use for exactly this, keeping the addition to one row instead of two.

**Start from Task 1's recorded headroom, do not re-derive it.** Task 1's
geometry test already measured and logged the available/used/difference for
all five strip-bearing states, and its SUMMARY carries the tightest state's
margin as a single labelled number. Read that number FIRST and let it decide:
if the tightest headroom is 2 or more rows the full box is affordable; if it
is 1, the one-row title-in-border variant is already forced and building the
full box first is wasted work; if it is 0, STOP and escalate — neither variant
fits and the backstop's premise has failed. Cite the number in the SUMMARY as
the reason for the variant chosen.

Then re-measure after implementing, to confirm the prediction held, across the
same ALL FIVE strip-bearing states at the fixed geometry:
normal Options (findings-banner variant), zero-options Options, options-error
Options, normal Storage (sentinel-preview variant), and both ceremony states.
The Options browse state has generous headroom, but the Storage sub-tab's
stacked preview blocks and the storage ceremony consume nearly the whole body
budget already, so those decide; the zero-options and options-error states are
measured because a clipping bug there would otherwise be invisible. Record all
five measured numbers and the variant chosen in the SUMMARY; do not resolve an
overflow by changing the frame dimensions, which this project forbids.

Do not change any keybinding. The D4 contract that local left/right switches
sub-tabs on this screen (and that this screen is therefore excluded from the
top-level arrow-switch footer hint) is unchanged and must stay unchanged —
D-05 improves its discoverability visually and explicitly rejects a
copy/footer-text change as the fix.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run 'TestSubTabStrip|TestGlobalSSH|TestMouse'</automated>
  </verify>
  <acceptance_criteria>
    - All six behaviors have a passing test at this commit. `TestSubTabStripRendersBordered` was observed RED against the unbordered strip BEFORE the implementation and its failure output is quoted in the SUMMARY; the commit that lands contains it GREEN together with the production change. `git log` shows no commit in this plan whose test suite fails.
    - `rg -c 'sub-tab strip' internal/tuikit/globalssh.go` shows the height stated once, and `sed -n '/globalSSHModel) renderStorage/,/^}/p' internal/tuikit/globalssh.go | grep -c 'frameBodyRows(height) - 1'` returns 0 — the bare literal is gone.
    - The Options top-lines helper, the Storage body budget, and the click handler all call the single height source; confirmed by reading all three and stated in the SUMMARY.
    - The measured body line count for ALL FIVE strip-bearing states at the fixed geometry is recorded in the SUMMARY alongside the available budget for each, and the chosen box variant (two-row full box or one-row title-in-border) is justified by those numbers.
    - **The variant choice cites Task 1's PRE-implementation headroom number**, quoted from Plan 04 Task 1's SUMMARY, and the SUMMARY states whether the post-implementation re-measurement confirmed that prediction or contradicted it. A variant chosen without reference to that number does not satisfy this criterion (cross-AI review MEDIUM).
    - `NO_COLOR=1` capture of the Options sub-tab shows a visible box and a distinguishable active label; the captured frame text is in the SUMMARY.
    - No keybinding changed: `git diff internal/tuikit/globalssh.go` shows no edit inside the left/right key cases.
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` and `make lint` are green.
  </acceptance_criteria>
  <done>The sub-tab strip is a bordered, accent-colored box that fits the fixed geometry on both sub-tabs, its height is stated once and derived everywhere, and its click affordance is proven still working at the new coordinates.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive the Global SSH visual-regression frames and prove the strip end-to-end through a real PTY</name>
  <files>internal/screenshot/createflow.go, .planning/design/global-ssh/visual-divergence-allowlist.txt, e2e/global_ssh_pty_e2e_test.go, e2e/global_ssh_storage_pty_e2e_test.go</files>
  <read_first>
    - `internal/screenshot/createflow.go` — the Global SSH screen specs, their region dispositions and their `Interaction` strings; locate them by searching for the Global SSH screen IDs rather than assuming a line range
    - `.planning/design/global-ssh/visual-divergence-allowlist.txt` — this surface's own divergence allowlist, same per-surface convention as the Health/Fixer one Plan 02 re-derived
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-02-SUMMARY.md` — Plan 02 Task 2's re-derive-do-not-hand-patch procedure and its fail-path verification, which this task repeats for a different surface
    - `e2e/global_ssh_pty_e2e_test.go` (8 test functions, the Options sub-tab) and `e2e/global_ssh_storage_pty_e2e_test.go` (6 test functions, the Storage sub-tab) — the existing real-PTY coverage for this screen, including any assertion that names the strip's rendered shape or clicks its coordinates. The Storage file is the one that exercises the tight body budget. Note Plan 03 Task 4 also extended `global_ssh_pty_e2e_test.go`; read its SUMMARY first so the two additions compose rather than collide.
    - `e2e/create_flow_pty_e2e_test.go` lines 123-146 — `clickLabelRow(t, s, label)`, the existing raw xterm SGR mouse press/release helper (`\x1b[<0;COL;ROWM` / `...m`). It is package-level in `package e2e`, directly callable here, and it derives the column/row from the CURRENT decoded frame rather than hardcoding coordinates — which is the whole point, since the strip's coordinates are exactly what this change moves.
    - `Makefile` — the `gate-visual-regression` target's `-run` pattern, to confirm the Global SSH gate tests are actually selected by it
  </read_first>
  <precondition>`make build` succeeds and produces a `gitid` binary — the PTY suite drives the compiled binary, so a package-level green does not imply this suite can run.</precondition>
  <action>
**Run the visual gate FIRST; do not assume an allowlist edit is owed.** The
allowlist is not a store of golden frames — it records SEMANTIC real-versus-
dummy divergences and the predicates that classify them. This strip is drawn
by shared `tuikit` code, so the border appears on BOTH the real and the dummy
side and the divergence SET may not move at all. The expected outcome is that
`make gate-visual-regression` stays green with
`.planning/design/global-ssh/visual-divergence-allowlist.txt` UNTOUCHED.

Only if the gate demonstrates that the divergence set or its predicates
actually changed do you re-derive the affected entries FROM THE REGISTRY —
never hand-patch the text file, the same rule and procedure Plan 02 Task 2
established for the Doctor surface. Preserve each entry's classification and
rationale text; only the frames they classify have changed. Record in the
SUMMARY which of the two outcomes occurred, with the gate output that
justifies it.

Note that Plan 03 already moved these same frames (the option-row checkbox
column and glyph changed), so any re-derivation must be done against the
CURRENT tree, not against a remembered pre-phase baseline. Confirm the gate is
green before starting and record the starting state.

Then extend the real-PTY coverage. The strip is now a multi-row bordered
control whose click coordinates are computed from its own render — a class
of coupling this project has broken before, and unit tests drive the render
function rather than a real terminal. This screen already has two PTY files
(one per sub-tab); put each new case in whichever one owns the sub-tab it
asserts, and re-point any existing assertion in either file that names the
strip's old single-line shape.

**The mouse case is mandatory and is the point of this step.** The regression
risk this task exists to catch is MOUSE COORDINATE DRIFT — the strip grew a
border row on top and a border column on the left, so every label's x and y
moved. Keyboard left/right does not exercise that math at all, so a
keyboard-only PTY case would leave the actual risk unproven (a cross-AI review
MEDIUM against the previous draft, which planned exactly that). Send a REAL
xterm SGR press/release on BOTH sub-tab labels using the `clickLabelRow`
technique — coordinates derived from the decoded frame, never hardcoded — and
assert that the active-label marking moved AND that the Storage sub-tab's
content actually appeared. Also assert that a click on a BORDER row is inert:
it must not activate a label.

Cover, in the compiled binary at the fixed geometry:
  - reaching the Global SSH screen and asserting the bordered strip is present
    in the raw rendered frame (border runes on the strip's first and last
    lines),
  - a real SGR mouse click on the Storage label switching to Storage, and a
    real SGR mouse click on the Options label switching back, with the active-
    label marking following each time,
  - a real SGR mouse click on one of the strip's border rows leaving the active
    sub-tab unchanged,
  - the local left/right KEY still switching sub-tabs in both directions —
    kept as a SEPARATE contract from the mouse case, not a substitute for it,
  - and the Storage sub-tab's content not being clipped — the tight budget case
    Task 2 measured.

Finally, prove the visual gate can still fail for this surface: break one
allowlist entry, observe RED, revert to GREEN, and record both. Do this whether
or not the allowlist was re-derived — a gate that passes vacuously is worse
than no gate, and this is the moment to re-verify it. The break-and-revert is
scratch work; commit only the restored GREEN state.
  </action>
  <verify>
    <automated>make gate-visual-regression &amp;&amp; make build &amp;&amp; make test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `make gate-visual-regression` passes, and its `-v` output shows the Global SSH gate tests actually running (quote the run lines).
    - The fail path was hand-verified: a deliberately broken allowlist entry produced RED, quoted in the SUMMARY, and was reverted to GREEN. Only the restored GREEN state is committed.
    - The allowlist outcome is recorded: either `git diff --stat .planning/design/global-ssh/visual-divergence-allowlist.txt` is EMPTY (the expected outcome — the border renders on both the real and dummy side through shared code), or the gate output showing the divergence set genuinely changed is quoted and every re-derived entry kept its original classification and rationale text.
    - `make test-e2e` passes with the new or extended PTY cases, and `rg -c 't\.Skip' e2e/global_ssh_pty_e2e_test.go e2e/global_ssh_storage_pty_e2e_test.go` returns 0 for both files.
    - **A real xterm SGR mouse press/release switches sub-tabs in the compiled binary**: `rg -c '\\x1b\[<0;' e2e/global_ssh_pty_e2e_test.go e2e/global_ssh_storage_pty_e2e_test.go` returns non-zero for the file carrying the mouse case, OR that file calls the shared `clickLabelRow` helper. The SUMMARY quotes the click assertion and the frame excerpt showing the active-label marking moved. A keyboard-only sub-tab switch does NOT satisfy this criterion.
    - A click on a strip BORDER row is asserted inert — the active sub-tab is unchanged.
    - The keyboard left/right sub-tab switch is covered as a SEPARATE assertion from the mouse case, in both directions.
    - The PTY cases assert the bordered strip's presence (border runes on its first and last lines) and the Storage sub-tab's content being unclipped.
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` and `make lint` are green.
  </acceptance_criteria>
  <done>The Global SSH surface's visual-regression evidence matches the shipped bordered strip, the gate has been proven able to fail, and the strip's behavior is proven through the compiled binary in a real terminal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mouse click coordinates -> the strip's hit-test -> sub-tab state, or -> an option row's toggle | a row-offset error here routes a click intended for one target to another |
| rendered frame -> the visual-regression gate | the automated surface that detects layout drift `go build` cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.4-11 | Tampering | the option-row click path after the strip's height changes | medium | mitigate | The strip's height is collapsed to ONE source before the height changes, and all four consumers derive from it (Task 2). If the option body's top-line offset were left behind, a click aimed at one option row would land on a different row and toggle the wrong option into the apply set. Task 1's row-accounting test and Task 3's PTY case both guard it. The apply ceremony's preview plus typed confirm remains a second backstop, which is why this is medium and not high. |
| T-09.4-12 | Denial of Service | the fixed-geometry body budget on the Storage sub-tab | medium | mitigate | The Storage sub-tab's stacked preview blocks already consume nearly the whole 25-row body budget, so added strip rows can clip the resulting-config preview the user is being asked to approve before a migration. Task 1's geometry test and Task 2's measurement settle this before the change ships, and UI-SPEC's one-row fallback is the named remedy. Widening the frame is forbidden. |
| T-09.4-13 | Repudiation | the visual-regression allowlist after re-derivation | medium | mitigate | Re-derived entries could silently broaden what the gate tolerates, producing an unearned green. Task 3 requires each entry keep its original classification and rationale, and requires a break-and-revert proving the gate still fails. |
| T-09.4-SC | Tampering | package-manager installs | low | accept | No `go get`, `npm`, `pip`, or `cargo` install occurs. RESEARCH.md's Package Legitimacy Audit records zero external packages for this phase; `go.mod` is not modified. |
</threat_model>

<verification>
The full phase closeout battery, run from the repository root and recorded
with real output (this project does not accept a PASS claim without it):

1. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...`
2. `make lint`
3. `make test-e2e`
4. `make gate-visual-regression`
5. `make test` (which reaches `gate-copy-freeze`)
6. `go list -deps ./internal/tuikit | grep -c 'castocolina/gitid/internal/doctor'` returns 0

Plus this plan's own manual confirmation, which no automated check covers:

7. Launch the built binary at a 100x30 terminal, open Global SSH, and confirm
   by eye that the sub-tab strip reads as a nested control distinct from the
   main tab bar; repeat with `NO_COLOR=1`. Capture both frames to the two paths
   declared in this plan's `files_modified`:
   `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/ui-frames/global-ssh-subtab-strip-bordered.txt`
   and `…/global-ssh-subtab-strip-bordered-nocolor.txt`, following the
   `<surface>-<state>.txt` frame-capture convention this project uses in other
   phases' `ui-frames/` directories. Create the directory if it does not exist.
</verification>

<success_criteria>
- Global SSH's sub-tab strip renders as a bordered, accent-colored box that is
  visibly a different hierarchy level from the main tab bar, and stays legible
  without color.
- The strip's height is stated exactly once in the source; the two body
  budgets and the click hit-test all derive from it.
- ALL FIVE strip-bearing states fit the fixed 100x30 geometry, with the chosen
  box variant justified by five recorded measurements — and the choice was made
  against Task 1's pre-implementation headroom number rather than guessed, with
  the post-implementation re-measurement recorded as confirming or contradicting
  that prediction.
- Clicking either label still switches sub-tabs, proven through the real click
  entry point in unit tests AND through a real xterm SGR mouse press/release
  against the compiled binary in a real PTY; a border-row click is inert.
- Keyboard left/right sub-tab switching is covered as a separate contract.
- No keybinding changed.
- No commit in this plan is a red-test commit: the bordered test's RED state is
  observed and recorded inside Task 2 and only its GREEN state is committed,
  together with the implementation.
- The Global SSH visual-regression evidence matches the shipped shape, the
  allowlist outcome is recorded (expected: unchanged), and the gate has been
  proven able to fail.
</success_criteria>

<output>
Create `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-04-SUMMARY.md` when done.
Record: the RED-then-GREEN transition for the bordered-strip test, stating
explicitly that the RED state was observed but not committed; Task 1's quoted
`-v` headroom transcript for all five states plus the tightest state's margin as
a single labelled number; the post-implementation measured body line count and
available budget for ALL FIVE strip-bearing states with the chosen box variant
justified by citation of that number; the four row-accounting consumers confirmed
collapsed to one source; the real SGR mouse-click PTY assertion quoted with the
frame excerpt showing the active-label marking moved; the allowlist outcome
(unchanged, or re-derived with the gate output justifying it); the visual gate's
fail-path evidence; and the two captured frames (with and without color) at the
declared `ui-frames/` paths.
</output>
