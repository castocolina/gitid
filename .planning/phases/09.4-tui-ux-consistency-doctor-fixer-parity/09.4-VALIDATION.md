---
phase: 09.4
slug: tui-ux-consistency-doctor-fixer-parity
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-02
---

# Phase 09.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go stdlib `testing` (`go test`), plus this project's PTY harness for raw-keystroke e2e (`e2e/*_test.go`, package `e2e`) |
| **Config file** | none — `go test` flags are passed directly; `Makefile` targets (`test`, `test-e2e`, `lint`) wrap them, per `CLAUDE.md`'s Technology Stack table |
| **Quick run command** | `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... ./cmd/gitid/... -run 'TestTabs\|TestNewScreens\|TestDoctor\|TestFixer\|TestHealth\|TestDemoBanner'` |
| **Full suite command** | `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` then `make lint` then `make test-e2e` then `make gate-visual-regression` |
| **Estimated runtime** | ~950s for `make test-e2e` (full `-race`, per 09.3-02-SUMMARY.md's measured baseline); unit suite is seconds |

---

## Sampling Rate

- **After every task commit:** Run `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... ./cmd/gitid/...` (fast subset)
- **After every plan wave:** Run the full suite command above, including `make test-e2e` and `make gate-visual-regression`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~950 seconds (bounded by `make test-e2e`, the slowest component; not run after every task, only per wave)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD-01 | 01 | 1 | UXP-01 | — | Global SSH Options resets focus to first row on every activation | unit | `go test ./internal/tuikit/... -run TestGlobalSSH -race` | ✅ existing `globalssh_test.go` (extend) | ⬜ pending |
| TBD-02 | 01 | 1 | UXP-02 | — | Non-selectable row renders `·` placeholder in the checkbox column | unit + visual | `go test ./internal/tuikit/... -run TestOptionRow -race` + `make gate-visual-regression` | ✅ extend existing option-row tests | ⬜ pending |
| TBD-03 | 01 | 1 | UXP-03 | — | Bracket toggle `[ ]`/`[✓]` replaces `☐`/`☑` at the two option-row sites | unit | `go test ./internal/tuikit/... -run 'TestGlobalSSH\|TestGlobalGit' -race` | ✅ extend existing | ⬜ pending |
| TBD-04 | 01 | 1 | UXP-04 | — | Sub-tab strip renders bordered, distinct from main tab bar | unit + visual | `go test ./internal/tuikit/... -run TestSubTabStrip -race` + `make gate-visual-regression` | ❌ Wave 0 — no `TestSubTabStrip*` found | ⬜ pending |
| TBD-05 | 02 | 2 | UXP-05 | T-Tampering-1 | `Selectable()` remains the single gate for checkbox render + toggle key + click hit-test after the Doctor merge | unit | `go test ./internal/tuikit/... -run 'TestTabs\|TestNewScreens\|TestDoctor' -race` | ⚠️ existing files need rewrite (`doctor_test.go`, `app_test.go`) | ⬜ pending |
| TBD-06 | 02 | 2 | UXP-05 | T-Tampering-2 | `f`/`F` handlers keep gating on `sel.Fixable` (CR-01 regression guard) after the merge | unit + real-PTY e2e | `go test ./e2e/... -run TestHealthFixer -race` (renamed) | ⚠️ `e2e/health_fixer_pty_e2e_test.go` (8 functions) needs a rewrite pass, not a rename | ⬜ pending |
| TBD-07 | 02 | 2 | UXP-05 | — | Merged Doctor tab count (6→5) does not break `App.screens [6]screenModel` fixed-size array or `checkFixBatchHalt`'s `fixerModel` type assertion | unit | `go test ./internal/tuikit/... -run TestDoctor -race` | ❌ needs new/extended coverage — no existing test asserts `checkFixBatchHalt` post-merge | ⬜ pending |
| TBD-08 | 02 | 2 | UXP-05 | — | Visual-regression registry (`healthFixerSpecs()`, Region constants, allowlist) stays consistent with the merged single-screen shape | visual | `make gate-visual-regression` | ⚠️ existing registry encodes the two-screen shape; needs update | ⬜ pending |

*Task IDs are placeholders (`TBD-NN`) — the planner assigns real task IDs; this table is re-synced once PLAN.md exists.*

---

## Wave 0 Requirements

- [ ] `internal/tuikit/subtabstrip_test.go` (or equivalent) — no existing `TestSubTabStrip*` function found this session; UXP-04 needs a new unit test asserting the bordered strip's row output, given the click-hit-test math is exactly the kind of silent-break class that needs a positive test.
- [ ] `e2e/health_fixer_pty_e2e_test.go` — needs a REWRITE pass (8 functions, not a rename): each test's purpose must be preserved and re-targeted at the merged screen's single navigation path.
- [ ] No test currently asserts `findingsBanner`'s cross-reference text post-merge (the `globalssh.go:747` hardcoded `"Open Health (4)"` string) — if the planner scopes that fix in, add a targeted assertion; if scoped out, no Wave 0 gap.

---

## Manual-Only Verifications

*None. All phase behaviors have automated verification (unit, real-PTY e2e, or visual-regression).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 950s (bounded by `make test-e2e`)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
