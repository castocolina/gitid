---
phase: 09.4-tui-ux-consistency-doctor-fixer-parity
plan: 01
subsystem: tui-doctor
tags: [tui, doctor, fixer, health, visual-regression, e2e, uxp-05]
key-files:
  - internal/tuikit/doctor_screen.go
  - internal/tuikit/frame.go
  - internal/tuikit/app.go
  - e2e/health_fixer_pty_e2e_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
metrics:
  tasks: 4
  commits: 5
---

# Phase 09.4 Plan 01 — Summary

Wave 1 reverses Phase 8's FIX-02 split: one Doctor tab lists every finding
and carries the inline fix ceremony, so two screens can no longer report
contradicting counts for the same scan (UXP-05, D-06).

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 | _(decision, no code)_ | Checkpoint resolved **merge**, decided 2026-09-03 via the orchestrator session's AskUserQuestion. Banner and split were rejected; Plans 01 and 02 proceed. |
| 2 | `a4c43ed` | `feat(09.4-01): merge Health and Fixer into one Doctor tab`. Collapse Health + Fixer into `TabDoctor` (5-value `TabID`; key 4 opens Doctor; key 6 is gone). `fixer_screen.go` → `doctor_screen.go`; `health_screen.go` deleted. List is unfiltered `orderedFindings`; `f`/`F` still gate on `Fixable`. `setTab` always clears `identityName`; `setTabFiltered` re-seeds it for the `h` deep-link. `checkFixBatchHalt` asserts `doctorModel` at both sites. Tracer `TestDoctorMergedListShowsEveryFinding` proves (a) both findings listed + status count, (b) `f` on fixable opens the ceremony, (c) `f` on non-fixable stays in browse. |
| 3 | `e86b67d` | `test(09.4-01): guard Doctor batch halt, Fixable gate, and count parity`. Three compiler-blind guards in `doctor_screen_test.go`: `TestDoctorBatchHaltSurvivesModelRename` (T-09.4-01, D-16 halt **text** after mid-batch `PersistError`), `TestDoctorFixGateRespectsFixableBit` (T-09.4-02, `f`/`F` stay gated while info-only rows remain listed), `TestDoctorCountIsTheOnlyFindingCount` (UXP-05, status count equals `len(orderedFindings)` and exceeds the fixable subset). |
| 4 | `76e2309` | `test(09.4-01): retarget Doctor nav, labels, and extractor anchors`. Point every runtime-only surface at Doctor (key 4) and Git Ignore (key 5). Re-anchor header-status extractors on `Doctor` so a missing label fails loudly instead of skipping the region (T-09.4-07). Capture driver boots every Health/Fixer frame on `'4'`; ceremony breadcrumb predicate needle becomes `contains:"Doctor › Fix"`. |
| 4 | `20b790d` | `test(09.4-01): panic on missing Doctor only for real headers`. Synthetic screenshot-package frames have no nav chrome; a blanket panic on a missing Doctor anchor crashed unit tests (`make test` panicked on header line `"shared header"`). Fail loud only when the header already carries `[1] Identities`. |
| 4 | `0d89598` | `test(09.4-01): share Health/Fixer body dispositions after merge`. Distinct fix discovered by running the gate: the merged Doctor tab now renders both `health-body` and `fixer-body` on `health-findings` and `fixer-list`, so each spec classifies the sibling region's existing disposition. No renames, no allowlist edit. |

## Task 1 — checkpoint

Resolved **merge** on 2026-09-03 via the orchestrator session's AskUserQuestion.
Proceed with reversing Phase 8's FIX-02 split despite the measured blast radius
being larger than CONTEXT.md estimated when D-06 locked.

## Task 2 — merge (`a4c43ed`)

18 files, +450 / −517. New `doctorModel` in `internal/tuikit/doctor_screen.go`
(ported from `fixer_screen.go`, absorbing Health's unfiltered list, parse-error
frame, and info-only detail branch). `TabID` is five values:
`Identities`, `SSH`, `Git`, `Doctor`, `Ignore`. `keyResult.healthIdentity`
renamed to `doctorIdentity`. CLI surface (`gitid health`, hidden `gitid doctor`,
`gitid.health/v1` JSON envelope) is untouched.

Tracer conjunction in `TestDoctorMergedListShowsEveryFinding`:

```
(a) merged list must show both findings and status count
(b) f on the fixable row must open the fix ceremony
(c) f on the non-fixable row must leave browse mode — no ceremony, no pendingFixID
```

`internal/tuikit/doctor.go` has zero changed lines across the whole plan.

## Task 3 — guards (`e86b67d`)

102 lines added to `internal/tuikit/doctor_screen_test.go`. Each guard's doc
comment names T-09.4-01, T-09.4-02, or UXP-05. Batch-halt asserts the D-16
segments `"Fix 2 of 3 failed and was rolled back"`, `"the first 1 fixes already
applied stand"`, `"Nothing else in this batch was attempted"` — never merely
"no panic".

## Task 4 — wave-close green (`76e2309`, `20b790d`, `0d89598`)

### e2e files touched (`git show 76e2309 --stat e2e/`)

| File | Why |
|------|-----|
| `e2e/health_fixer_pty_e2e_test.go` | `openHealth`/`openFixer` both send key `4`; status wait is `"every fix is previewed"`; inline detail asserts `"f · Fix this…"` instead of `"Switch to Fixer to apply this."` |
| `e2e/ui_pty_e2e_test.go` | Nav-tab slice is `Identities, SSH, Git, Doctor, Ignore`; comment rewritten to record the Phase 09.4 reversal |
| `e2e/dummy_demo_e2e_test.go` | Launch `[4] Doctor` / `[5] Ignore`; key-6 press removed; tab-4 and mouse-click body waits use `"every fix is previewed"`; ceremony walk uses key `4` |
| `e2e/global_git_pty_e2e_test.go` | Right-arrow-leaves-Global-Git assertion now names `"Doctor"` (this file is otherwise Plan 03's — that is the only edit) |
| `e2e/git_configuration_pty_e2e_test.go` | Header-status extractor re-anchored on `"Doctor"`; miss path is `t.Fatalf` |
| `e2e/identity_manager_pty_e2e_test.go` | Same loud re-anchor; `extractIdentManagerRegion` threads `errorRecorder` |
| `e2e/create_flow_pty_e2e_test.go` | Third caller of the identity-manager extractor; inherits the loud miss |
| `e2e/gitignore_pty_e2e_test.go` | Extra file found by grepping `e2e/` for the removed key: Git Ignore opens on key `5` (was `6`) |

`rg -c 'TabHealth|TabFixer' e2e/` = 0.

Surviving bare `Health`/`Fixer` hits in `e2e/`:

- `e2e/health_fix_cli_e2e_test.go` — CLI verb `health` / helper `runHealthCLI` / `TestHealthFixCLIParity`. Deliberately not renamed (planner_assumptions).
- `e2e/health_fixer_pty_e2e_test.go` — function names `TestHealthFixer_*`, helpers `openHealth`/`openFixer`, and assertion *messages* still say "Health"/"Fixer". Scope fence: do not rename; Plan 02 rewrites the file.
- `e2e/ui_pty_e2e_test.go:436-437` — historical citation: "Phase 09.4 reversed 08-01-PLAN.md Task 1's TabID split: Health and Fixer collapse back into one Doctor tab".

`rg -c 't\.Skip' e2e/health_fixer_pty_e2e_test.go` = 2, both pre-existing:

```
t.Skipf("chflags uchg is macOS/BSD-only; skipping D-16 real-PTY halt coverage on %s (proven at unit level by TestBatchWalkHalt)", runtime.GOOS)
t.Skipf("chflags not found in PATH; skipping D-16 real-PTY halt coverage (proven at unit level by TestBatchWalkHalt): %v", err)
```

### Four loud-failure extractor branches

`e2e/git_configuration_pty_e2e_test.go`:

```
t.Fatalf("header-status: missing anchor %q in header line %q", gitScreenHeaderStatusAnchor, "")
t.Fatalf("header-status: missing anchor %q in header line %q", gitScreenHeaderStatusAnchor, header)
```

`e2e/identity_manager_pty_e2e_test.go`:

```
t.Fatalf("header-status: missing anchor %q in header line %q", identManagerHeaderStatusAnchor, "")
t.Fatalf("header-status: missing anchor %q in header line %q", identManagerHeaderStatusAnchor, header)
```

`internal/screenshot/createflow_regions.go` (`extractHeader` / `extractHeaderStatus`), after `20b790d` so synthetic frames without nav chrome do not panic:

```
if strings.Contains(plain, "[1] Identities") {
    panic(fmt.Sprintf("header: missing anchor %q in header line %q", headerNavAnchor, plain))
}
if strings.Contains(plain, "[1] Identities") {
    panic(fmt.Sprintf("header-status: missing anchor %q in header line %q", headerNavAnchor, plain))
}
```

Loud-path proof (scratch, restored before commit `76e2309`): temporary
`headerNavAnchor = "DoesNotExist "` / `gitScreenHeaderStatusAnchor = "DoesNotExist"`:

```
[FAIL] TestGitScreenHeaderStatusAnchorMissIsLoud
   git_configuration_pty_e2e_test.go:1084: header-status: missing anchor "DoesNotExist" in header li...
[FAIL] TestExtractHeaderAnchorMissIsLoud
   panic: header: missing anchor "DoesNotExist " in header line " gitid   [1] Identities · [2] SSH ·...
```

`rg -c 'LastIndex\(.*"Fixer"\)' e2e/ internal/screenshot/` = 0.

### Allowlist

`git diff --numstat a4c43ed^..HEAD -- .planning/design/health-fixer/visual-divergence-allowlist.txt` is empty (`0 0`).
`wc -l` remains **100**. The Go-side ceremony breadcrumb predicate in
`healthFixerSpecs` already carries `contains:"Doctor › Fix"` (`76e2309`); the
allowlist file's last-line needle still reads `contains:"Fixer › Fix"` and was
**not** edited — the gate went green without that exception firing.
`git diff --stat` on git-screen / identity-manager / create-flow allowlists is
empty (region shape preserved: one trailing nav segment plus the chip).

Scope fence counts (`CaptureHealthFixerScreens|healthFixerSpecs|RegionHealthBody|RegionFixerBody|RegionFixerCeremony`):
`createflow.go` 22, `createflow_regions.go` 14, `createflow_test.go` 5,
`createflow_packet_test.go` 4, `gate_visual_regression_test.go` 8. Symbols
were not renamed.

### Disposition-only follow-up (`0d89598`)

Pre-fix `make gate-visual-regression` (tee `1788409022`):

```
--- FAIL: TestGateVisualRegression (16.20s)
    screenshot: BuildRegionDiffs: frame "health-findings" region "fixer-body" differs without a screen-specific declared disposition
--- FAIL: TestNegativeControl_HealthFixerUnclassifiedDifference (8.19s)
--- FAIL: TestNegativeControl_HealthFixerPerturbedComparableRegion (8.19s)
```

Fix: add the sibling region's existing disposition to both list specs — no
new disposition, no allowlist line:

```
health-findings: RegionDispositions += fixerBodyDisposition
fixer-list:      RegionDispositions += healthBodyDisposition
```

Post-fix `make gate-visual-regression` (this session, `GATE_EXIT:0`):

```
=== RUN   TestGateVisualRegression
    gate_visual_regression_test.go:468: gate-visual-regression: OK — 57 RequiredScreenSpecs frames checked as a classified real/dummy symmetric union
--- PASS: TestGateVisualRegression (16.32s)
```

Focused Health/Fixer gate tests after the disposition commit:
`go test -tags screenshot -run 'TestHealthFixer|TestNegativeControl_HealthFixer|TestGateVisualRegression$' ./cmd/gitid/` — **9 passed**.

Doctor-named tests the Makefile filter actually executes:

```
=== RUN   TestHealthFixerHTMLNonApplicabilityPerSpec
=== RUN   TestHealthFixerAllowlistMatchesRegistry
=== RUN   TestHealthFixerAllowlistFormat
=== RUN   TestHealthFixerMakefileFilterSelectsControls
=== RUN   TestNegativeControl_HealthFixerMissingState
=== RUN   TestNegativeControl_HealthFixerUnclassifiedDifference
=== RUN   TestNegativeControl_HealthFixerPerturbedComparableRegion
=== RUN   TestNegativeControl_HealthFixerCrossSurfaceAllowlistLeakage
```

## Doctor-derived PTY evidence

`go test -tags e2e -count=1 -timeout 180s -v ./e2e/ -run 'TestHealthFixer_'`
this session — **8 passed**:

```
--- PASS: TestHealthFixer_RealPTYHealthFindingsInlineDetailAndIdentity (7.17s)
--- PASS: TestHealthFixer_RealPTYHealthAllGreen (5.92s)
--- PASS: TestHealthFixer_RealPTYPerIdentityHealth (5.77s)
--- PASS: TestHealthFixer_RealPTYParseError (5.20s)
--- PASS: TestHealthFixer_RealPTYFixerCeremonyWritesAndBacksUp (7.51s)
--- PASS: TestHealthFixer_RealPTYFixerBatchWalk (6.90s)
--- PASS: TestHealthFixer_RealPTYFixerBatchWalkHalt (6.90s)
--- PASS: TestHealthFixer_RealPTYFixerNothingToFix (5.97s)
```

One isolated Persist-timing flake (`flagship rewrite changed bytes beyond the
one directive` — file still `IdentitiesOnly no` immediately after the second
Enter) reproduced once and passed on retry plus the 8/8 run above. Not a
merge regression; both batch-walk tests and the ceremony retry were green
against the same binary.

Full `make test-e2e` was already confirmed 8/8 in `e2e/health_fixer_pty_e2e_test.go`
(~940s) by the prior dispatch; not re-run here.

## Verification (wave-close battery)

- `make test` — exit 0 (this session, after `0d89598`).
- `make lint` — 0 issues (`go vet` tagged screenshot/smoke/e2e/realaccount +
  `golangci-lint` tagged screenshot + untagged).
- `make test-e2e` — prior dispatch PASS ~940s; Doctor-derived 8/8 reconfirmed
  this session (quoted above).
- `make gate-visual-regression` — exit 0; `TestGateVisualRegression` PASS
  16.32s, 57 RequiredScreenSpecs frames classified.
- `go list -deps ./internal/tuikit | grep -c 'castocolina/gitid/internal/doctor'` — 0.
- `git diff --stat a4c43ed^..HEAD -- internal/tuikit/doctor.go` — empty.

## Deviations

1. **Disposition-only visual-gate fix (`0d89598`)** — not in Task 4's original
   action. The merge made `health-body` and `fixer-body` extractors hit the
   same Doctor list chrome, so the sibling region appeared unclassified.
   Classified with the sibling's *existing* disposition. Scope fence held:
   no renames, no allowlist edit.
2. **Panic gated on real headers (`20b790d`)** — Task 4's blanket `panic()`
   on a missing Doctor anchor crashed `make test` against synthetic
   screenshot frames (`header line "shared header"`). Loud path kept for
   real nav chrome (`[1] Identities` present).
3. **Allowlist needle not edited** — section E permitted a `1 1` ceremony-
   breadcrumb needle correction; observed outcome is `0 0`. Gate green
   without touching the file.
4. **`e2e/gitignore_pty_e2e_test.go`** — eighth e2e file, required by the
   plan's "grep the whole `e2e/` tree" instruction (key `6` → `5`).

## Plan 02 Task 1 hand-off

These assertions wait on the **ported Fixer status wording**
(`"every fix is previewed"`) and must move when UI-SPEC's merged copy lands:

- `e2e/dummy_demo_e2e_test.go:235` — `mustSee(..., "every fix is previewed", "tab 4: doctor status line after auto-scan")`
- `e2e/dummy_demo_e2e_test.go:372` — `mustSee(..., "every fix is previewed", "mouse: Doctor body after clicking the header tab")`
- `e2e/health_fixer_pty_e2e_test.go:112` — `openHealth`: `mustSee(..., "every fix is previewed", "key 4 opens Doctor after its real scan")`

## Self-Check

PASSED — all 4 tasks done, 5 atomic commits, no `--no-verify`,
`.planning/config.json` left untouched. Wave-close battery green:
`make test`, `make lint`, Doctor PTY 8/8, `make gate-visual-regression`.

## Note on Execution Path

Cross-AI executor (`opencode` / `local-llm-env/my-coding`) per Rule 12.
This last continuation committed the pending disposition fix (`0d89598`)
and wrote this SUMMARY.
