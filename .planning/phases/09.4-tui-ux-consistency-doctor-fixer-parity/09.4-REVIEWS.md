---
phase: 9.4
reviewers: [opencode-plan-review, codex]
reviewed_at: 2026-09-02T13:24:26Z
plans_reviewed: [09.4-01-PLAN.md, 09.4-02-PLAN.md, 09.4-03-PLAN.md, 09.4-04-PLAN.md]
models:
  opencode-plan-review: "local-llm-env/my-plan-review (reasoning=high)"
  codex: "gpt-5.6-sol (reasoning=high)"
model_sources:
  opencode-plan-review: "pinned"
  codex: "banner"
---

# Cross-AI Plan Review — Phase 9.4

## OpenCode Review (opencode-plan-review) — Convergence Cycle 2 (SUCCEEDED)

**LANE SUCCEEDED on this cycle's second attempt.** This is cycle 2 of the plan-review-convergence
loop: cycle 1's findings (8 HIGH + 6 actionable, documented in the Codex section below) were
addressed in a replanning pass committed at `4caa42d`, with each plan carrying a
`<review_response>` table. This run's job was to verify whether those fixes hold up and surface
any new findings — not merely re-litigate cycle 1.

Model: `local-llm-env/my-plan-review (reasoning=high)` via the `opencode` adapter (`options.baseURL
= http://llm.local:20128/v1`), invoked as the `opencode-plan-review` instance, with full repository
access (source-grounded).

**Retry history for this cycle:** attempt 1 failed with `[spawn error: ETIMEDOUT]` (same class of
transient failure recorded for the two cycle-1 attempts below) — no assistant text, stop
reason=tool-calls, 290 output tokens. After a ~20s wait, attempt 2 was retried on the same lane per
this project's established guidance (retry same lane on transient timeout rather than immediately
substituting a reviewer) and succeeded, returning a full source-grounded review.

### Overall Assessment

**Risk: MEDIUM-HIGH.** Plan-quality risk is assessed LOW — the reviewer traced every cycle-1
`<review_response>` disposition against the actual repo and found no fabricated fixes; the one
exception is a cycle-1 fix that rests on a factually wrong premise (see HIGH #2 below), which errs
toward *more* existing coverage than the plan's own prose claims, not less. Execution risk is HIGH,
concentrated in Plans 01–02's UXP-05 blast radius, driven by a newly discovered class of silent
test-degradation outside the plans' declared `files_modified`.

### Cycle-1 Fix Verification (spot-checked, all held up)

- Plan 01 Task 4 (wave closes red) — verified real: has its own `<files>`, a
  `make build && make test-e2e && make gate-visual-regression` verify, and a scope fence excluding
  Plan 02's work.
- Plan 01's reversion target correction (`globalGitModel`, not `fixerModel`) — verified: a real,
  compiling `screenModel` (`internal/tuikit/globalgit.go:117`), so the assertion at
  `internal/tuikit/app.go:315` compiles and yields the intended `ok == false` runtime path.
- `createflow_packet_test.go` caller coverage — verified: two calls at `:121` and `:125`, both
  behind `//go:build screenshot`; Plan 02's `rg -c` criterion correctly returns 2.
- `health_fixer_pty_e2e_test.go` skip-count correction — verified exact: `rg -c 't\.Skip'` returns
  2 (lines 318, 321), matching Plan 02 Task 3's criterion.
- Plan 03's raw-literal glyph warning (`batch3_test.go:222,231,237`) — verified real and
  non-obvious (a constant-only grep would miss these sites).
- Plan 03's zero-row Global Git finding — verified a real latent bug: `globalgit.go:851` indexes
  `options[selIdx]` unconditionally, unguarded for the empty case that `globalgit.go:541` only
  guards on the key path.
- Plan 04's four-site row-accounting claim — verified at all four cited locations
  (`globalssh.go:626,633,964,656`).
- Plan 04's committed-RED fix — verified clean: `TestSubTabStripRendersBordered` moved out of
  Task 1's deliverables into Task 2; Task 1's own tests are GREEN at that commit.

### New/Revised Concerns

**HIGH — NEW: four e2e files parse or assert the removed literal tab labels, none in any plan's
`files_modified`.** Plan 02 Task 1's grep (`rg -n 'Health|Fixer' --glob '*.go' internal cmd`)
excludes `e2e/`; Task 3 covers only `health_fixer_pty_e2e_test.go`. Four other files depend on the
labels:
  - `e2e/ui_pty_e2e_test.go:438` and `e2e/dummy_demo_e2e_test.go:237,239` — loud failures
    (`t.Fatalf`/wrong key mapping) once the labels change; these are safe but currently uncovered.
  - `e2e/git_configuration_pty_e2e_test.go:600-604` and
    `e2e/identity_manager_pty_e2e_test.go:1462-1466` — **silent**: both extract the header status
    text via `strings.LastIndex(header, "Fixer")` and `return ""` on no-match, so post-merge both
    extractors silently return `""` and their downstream PTY region comparisons at `:701`/`:1570`
    pass vacuously instead of failing — an unearned green in exactly the class this phase exists to
    eliminate.
  - Fix: add all four files to Plan 02's `files_modified`, widen Task 1's grep to
    `internal cmd e2e`, and replace the two silent `return ""` miss-paths with `t.Fatalf` or an
    anchor on a label the merge does not remove (e.g. `"Ignore"`).

**HIGH (premise correction) — Plan 02's own `<review_response>` for the cycle-1 "screenshot tests
never run" finding is itself incomplete.** It correctly rebuts the "never compiled" claim via
`lint-tagged` (`Makefile:263-264`), but then asserts the test *bodies* are "genuinely never RUN."
`Makefile:314`'s `test:` target already runs
`go test -tags screenshot -skip '...' ./internal/screenshot/...`, and neither
`createflow_test.go` nor `createflow_packet_test.go`'s capture-driver callers match the `-skip`
regex — so `make test` already executes them. Consequence: Plan 02's added command is redundant
with `make test`, not a gap-closer, and the rename's actual blast radius (the `captureCombined`
helper used broadly in `createflow_test.go`) is caught by `make test`, not by the `make lint` verify
Plan 02 Task 2 currently runs. Fix: correct the `<review_response>`/`<verification>` prose and add
`make test` to Task 2's verify.

**MEDIUM — Plan 02 Task 1's `findingsBanner` acceptance criterion under-counts remaining "Open
Health" sites.** The `sed`-scoped grep only covers one function in `globalssh.go`; a second literal
site at `internal/tuikit/identities.go:4608` ("h · Open Health for ") is in a different file and
function and is named in prose but not covered by any acceptance criterion.

**MEDIUM — Plan 04's Storage-pane overflow backstop is plausible but unmeasured before Wave 4.**
`renderStorage:964`'s row budget leaves headroom that is "not obvious," and Task 2's five-state
measurement is the right control, but Task 1's geometry test should record the headroom margin
(not just pass/fail) so the box-variant choice in Task 2 is data-driven.

**MEDIUM — Plan 01 Task 4's "allowlist should not change" prediction is likely wrong for the
merge itself** (as opposed to Plans 03/04, where it correctly holds): `visual-divergence-
allowlist.txt:100` pins a `"Fixer › Fix"` breadcrumb needle that the merged screen's breadcrumb
will not contain, and `:84` pins a `fixer-list` ScreenID Plan 01 does not rename. Task 4's
"STOP if the gate demands a change" could trigger and block the very green-close guarantee it
exists to provide. Suggest permitting the minimum breadcrumb-needle correction rather than a hard
STOP.

**LOW — Plan 01's palette-entry count criterion may over-count** if a Help entry also carries a
`tab:` field in the same slice (untested at review time; worth checking at edit time).

**LOW — Plan 02's scoping fix (`internal cmd e2e Makefile`, excluding `.planning/`) is correctly
designed but Task 1's own primary criterion still uses the narrower `internal cmd`**, missing the
`e2e` scope its own Task 3 already owns.

### Suggestions

1. Add the four e2e files above to Plan 02's `files_modified`; widen every legacy-name grep to
   include `e2e`.
2. Harden the two silent header extractors to fail loudly (`t.Fatalf` or a non-removed anchor
   label) rather than degrading to a vacuous pass.
3. Correct Plan 02's screenshot-test premise and add `make test` (not just `make lint`) to its
   Task 2 verify.
4. Add an acceptance criterion covering `identities.go:4608`'s "Open Health" literal.
5. Relax Plan 01 Task 4's allowlist STOP to permit the minimum breadcrumb-needle correction.
6. Have Plan 04 Task 1 record headroom margin, not just pass/fail, across all five strip states.

### Risk Assessment

**MEDIUM-HIGH.** Plan-quality risk is LOW (cycle-1 remediation is genuine, well-evidenced, and the
one wrong premise errs toward under-claiming existing coverage). Execution risk is HIGH,
concentrated in the silent-degradation class newly found in Plans 01–02's blast radius (the two
e2e header extractors, and the allowlist needle at `:100`) — both of which currently would pass or
vacuously pass rather than failing loudly.

---

## OpenCode Review (opencode-plan-review) — Cycles 1–2 (historical, failed)

**LANE FAILED both prior cycles — no review content was produced in either attempt.** This
section is preserved verbatim from the previous two commits (214f131, e516f79) as a faithful
record; it is not re-attempted or re-judged in this cycle.

`opencode-plan-review` is this project's configured `review.default_reviewers` entry (see
`.planning/config.json`), resolving through the `opencode` adapter to the
`local-llm-env/my-plan-review` model (`options.baseURL = http://llm.local:20128/v1` in
`~/.config/opencode/opencode.jsonc`). Per this project's own established guidance (retry the
same lane on ETIMEDOUT/503 rather than instantly swapping reviewers or accepting an empty
result as a pass), cycle 2 was an explicit retry of the same lane, not a substitution.

### Attempt 1 (recorded in commit 214f131, ~2026-09-02T01:26:21Z)

Failed after ~11 minutes with `[spawn error: ETIMEDOUT]` and no assistant text. At the time,
`curl -v http://llm.local:20128/v1/models` from that execution context reported `Resolving
timed out` (HTTP code `000`) — consistent with a DNS/mDNS resolution failure for `llm.local`
from that sandbox.

### Attempt 2 (recorded in commit e516f79, ~2026-09-02T01:30:xx–01:41:36Z, ~11 minutes)

Before starting this retry, connectivity to `llm.local` was independently re-checked and DID
resolve this time: an unauthenticated probe against `/v1/models` returned HTTP 401 (not a
resolution failure), with ~5s latency attributed to normal mDNS resolution for this hostname.
On that basis this retry was judged worth attempting rather than a certain repeat of a
host-down condition.

The retry nonetheless failed with the same symptom as attempt 1:

```
opencode review failed or returned empty output. stderr:

[spawn error: ETIMEDOUT]

Raw response body:
OpenCode review returned no assistant text (#1936: agent ended its turn with no final message).
Diagnostic: stop reason=tool-calls, output tokens=100
```

The diagnostic (`stop reason=tool-calls`, non-zero output tokens, `spawn error: ETIMEDOUT`) is
slightly different in character from attempt 1's plain DNS-resolution failure: the model did
emit some output (100 tokens, tool-calls) before the invocation ultimately timed out without
ever producing a final assistant message.

**No source-grounded review of the four 09.4 PLAN.md files was obtained in either attempt.**
Zero findings exist from this lane across both cycles.

### Attempt 3 (opencode-sol fallback, not committed to this file)

Between cycle 2 and this cycle, an attempt was made to fall back to the configured
`opencode-sol` instance. That attempt returned only a truncated 3-line stub ("I'll verify the
four plans...") with no findings, and its underlying subagent appears to have been
lost/killed before it could report properly. That attempt's output was never persisted to
this file and is recorded here only as context for why a fourth, independently-hosted lane
(Codex) was tried next.

---

## Codex Review (codex) — Cycle 3

**LANE SUCCEEDED.** Model: `gpt-5.6-sol (reasoning=high)` via the Codex CLI (`codex exec
--ephemeral -c model_reasoning_effort=high`), run with full repository access (source-grounded,
not text-only). Runtime: ~14 minutes.

### Overall Assessment

**Verdict: REQUEST CHANGES. Overall risk: HIGH.**

The architecture is sound: the plans correctly identify the full tab-routing blast radius,
preserve Health's unfiltered findings while retaining Fixer's guarded write ceremony, and cover
the most dangerous silent failure in `checkFixBatchHalt`. However, execution should not begin
yet. There are four blocking process/test defects:

- All four plans omit the mandatory `cross_ai: true` frontmatter field, despite the repository
  explicitly forbidding execution without it (`.planning/ONESHOT.md:23`, `:97`).
- Plan 01 knowingly closes Wave 1 with `make test-e2e` failing, contrary to the every-wave
  green-gate rule (`09.4-01-PLAN.md:456`, `.planning/ONESHOT.md:61`).
- Plan 02 does not actually compile the screenshot-tagged `internal/screenshot` tests and has
  an impossible no-`t.Skip` criterion.
- Plan 04 explicitly calls for committing a failing test, violating the buildable-commit rule
  (`docs/agent-instructions/engineering.md:24`).

The UI-SPEC state is also inconsistent: `.planning/STATE.md` says it is approved, while the
specification itself still has every checker box empty and says `Approval: pending`
(`STATE.md:8`, `09.4-UI-SPEC.md:370`).

### Recipe Alignment

No recipe divergence was found. These plans alter presentation, navigation, and suggested-fix
copy; they do not change generated SSH/Git configuration shapes. In particular:

- The recipe requires alias-specific host blocks, port 443, explicit identity files, and
  `IdentitiesOnly yes` (`recipes/README.md:17`, `recipes/ssh-config.recipe:30`).
- The existing real-PTY fix proves the ceremony changes only `IdentitiesOnly no` to `yes`,
  requires typed confirmation, and creates a backup (`e2e/health_fixer_pty_e2e_test.go:181`).
- Plan 03 explicitly leaves the SSH/Git classifiers unchanged (`09.4-03-PLAN.md:245`).
- The `hasconfig:` identity selection and `insteadOf` structure remain untouched
  (`recipes/gitconfig.recipe:35`, `:83`).

### Plan 01 — Doctor merge core

**Summary:** Correct structural model for reversing FIX-02. Accurately combines Health's
all-findings scope with Fixer's ceremony state and accounts for the fixed-size screen array,
navigation keys, deep-link routing, and silent concrete-type assertions. Main defect is wave
atomicity: it intentionally leaves required downstream gates red.

**Strengths:**
- Tab arithmetic is correct: the live shell has six members (`internal/tuikit/frame.go:58`),
  and the binding spec correctly places `TabDoctor` at key 4, moving Git Ignore to key 5
  (`09.4-UI-SPEC.md:56`).
- Catches all three routing mechanisms — keyboard/mouse identity deep-links plus the header
  chip — as genuinely independent sites (`internal/tuikit/app.go:453,515,554`).
- Selects the correct data source: Health's `findings` returns all ordered findings
  (`internal/tuikit/health_screen.go:102`); Fixer currently causes the contradictory count by
  narrowing to `fixableFindings` (`internal/tuikit/fixer_screen.go:110`).
- Write gate preserved correctly: `f` checks `sel.Fixable`, `F` builds its queue from
  `fixableFindings` (`internal/tuikit/fixer_screen.go:220`).
- Correctly elevates the silent type-assertion hazard in `checkFixBatchHalt`, which depends on
  two `fixerModel` assertions that can fail without a compile error (`internal/tuikit/app.go:314`).

**Concerns:**
- **HIGH** — Wave 1 is deliberately left red: the plan expects both E2E and visual gates to
  fail after completion (`09.4-01-PLAN.md:456`), contrary to the repository's every-wave green
  gate requirement (`.planning/ONESHOT.md:29,61`).
- **MEDIUM** — The named tracer `TestDoctorMergedListShowsEveryFinding` checks only list
  membership/count (`09.4-01-PLAN.md:220`); Health already displays all findings and the
  all-findings count (`internal/tuikit/health_screen.go:138`) — the actual merge property only
  emerges combined with the separate `f` assertion.
- **MEDIUM** — The proposed source-reversion proof does not simulate the silent failure: after
  `fixerModel` is deleted, reverting an assertion to `fixerModel` produces a compile error, not
  `ok == false` at runtime (`09.4-01-PLAN.md:391`), so it does not prove the guard catches the
  mechanism at `internal/tuikit/app.go:315`.

**Suggestions:**
- Move the E2E navigation rewrite and minimum screenshot-registry correction into Plan 01, or
  merge Plans 01 and 02 into one wave.
- Make the tracer one test that proves all three properties on the same tab: all findings
  displayed, a fixable selection opens the ceremony, a non-fixable selection does not.
- Demonstrate the type-assertion guard by temporarily asserting an existing wrong concrete
  model (e.g. `globalGitModel`) so the code compiles and fails through the intended silent
  `ok == false` path.

**Risk:** HIGH until wave atomicity is fixed; MEDIUM afterward.

### Plan 02 — Copy, visual registry, and PTY parity

**Summary:** Correctly recognizes that copy, screenshot registries, and raw PTY keystrokes can
drift while ordinary compilation remains green. The eight-behavior PTY rewrite is especially
valuable. The screenshot-test command and skip criteria are currently unsound.

**Strengths:**
- Stale-name sweep covers real producers, not merely renderers (`internal/doctor/checks/coherence.go:432`,
  `internal/dummytui/data.go:383`).
- Registry consolidation is necessary — the current capture driver hardcodes Health at key 4
  and Fixer at key 5 (`internal/screenshot/createflow.go:2063,2202`).
- Preserves all eight PTY behaviors including actual writes, backups, typed confirmation, and
  OS-level failure injection (`e2e/health_fixer_pty_e2e_test.go:188,339`).

**Concerns:**
- **HIGH** — Screenshot-tagged internal tests are not run: both `createflow_test.go` and
  `createflow_packet_test.go` are guarded by `//go:build screenshot`, but Plan 02's command
  omits `-tags screenshot` (`09.4-02-PLAN.md:289`) and `make gate-visual-regression` runs only
  `./cmd/gitid/...` (`Makefile:774`) — those internal tests are never compiled.
- **HIGH** — A renamed public capture function has an omitted caller: `createflow_packet_test.go`
  calls `CaptureHealthFixerScreens` twice (`:121`) but that file is absent from Plan 02's
  `files_modified` list (`09.4-02-PLAN.md:23`).
- **HIGH** — The no-skip criterion is impossible: the batch-halt helper legitimately contains
  two portability skips for non-Darwin systems / missing `chflags`
  (`e2e/health_fixer_pty_e2e_test.go:315`), but Plan 02 requires `rg -c 't.Skip' ...` to return
  zero (`09.4-02-PLAN.md:365`).
- **LOW** — One acceptance grep can never pass: it searches the entire repo for old identifiers
  (`09.4-02-PLAN.md:294`), and the plan and historical planning artifacts themselves contain
  those identifiers.

**Suggestions:**
- Add `internal/screenshot/createflow_packet_test.go` to the file list and update its calls.
- Verify with: `go test -tags screenshot -count=1 -race ./internal/screenshot/... ./cmd/gitid/...`
- Replace "zero skips" with "no new skips; exactly the two documented portability skips
  remain," and ensure the unit batch-halt test runs on every OS.
- Scope the legacy-name grep to current production/test surfaces, excluding `.planning`
  history, or maintain an explicit allowed-results file.

**Risk:** HIGH — the missing build tag allows renamed screenshot APIs and tests to remain
broken while every listed verification command appears green.

### Plan 03 — Focus, checkbox system, and rationale copy

**Summary:** Precise about focus derivation, shared column width, styling, and
render/hit-test coupling. Keeps selectability unchanged, protecting the write boundary. Main
gaps are the zero-row Global Git path and absent real-PTY acceptance for the new toggle and
placeholder behavior.

**Strengths:**
- Focus root cause correctly identified: Global SSH hardcodes `IdentitiesOnly` at construction
  (`internal/tuikit/globalssh.go:120`), Global Git resets on activation
  (`internal/tuikit/globalgit.go:117`).
- Correctly couples both rendered glyphs to both hit-test needles — current SSH/Git click
  paths search for the old glyphs directly (`globalssh.go:693`, `globalgit.go:835`).
- Preserves `Selectable()` as the sole authorization predicate, already documented/enforced in
  Global Git (`globalgit.go:835`).
- Scoped glyph exception is consistent with the binding UI spec and avoids changing
  identity-manager controls (`09.4-UI-SPEC.md:107`).

**Concerns:**
- **MEDIUM** — Zero-row Global Git behavior lacks an implementation instruction: relies on
  `gitDetailIndex` returning zero for an empty slice (`09.4-03-PLAN.md:124`), but `view` then
  indexes `options[selIdx]` unconditionally (`globalgit.go:970`); Global SSH already has the
  needed empty-state precedent (`globalssh.go:904`).
- **HIGH** — UXP-01/02/03 lack compiled-binary PTY proof: the policy requires the real TUI to
  be exercised through PTY keyboard/mouse interaction (`docs/agent-instructions/product-ui.md:15`),
  but current Global SSH/Git PTY browse tests assert names and navigation, not `[ ]`, `[✓]`,
  the dot placeholder, or reset-on-reentry (`e2e/global_ssh_pty_e2e_test.go:84`,
  `e2e/global_git_pty_e2e_test.go:94`).
- **LOW** — Visual-regression ownership is unresolved: the verification section expects
  allowlist re-derivation and possible E2E assertion changes (`09.4-03-PLAN.md:347`), but
  neither relevant allowlists nor PTY files appear in `files_modified`.

**Suggestions:**
- Add an explicit zero-options branch to Global Git's view, modeled on Global SSH, and test
  the rendered empty state rather than activation alone.
- Add real-PTY assertions for both screens covering first-row focus after re-entry, dot
  placeholders, off/on bracket toggles, and actual mouse toggling.
- Audit visual-regression output before assuming an allowlist edit — the gate classifies
  real-versus-dummy differences, not common frame changes
  (`cmd/gitid/gate_visual_regression_test.go:5`).

**Risk:** MEDIUM-HIGH — implementation is straightforward, but without PTY assertions the
phase can pass while the actual terminal affordances remain wrong.

### Plan 04 — Bordered sub-tab strip

**Summary:** Properly treats rendering height, body budgets, x/y click coordinates, and fixed
geometry as one coupled change, and reuses the existing dashed-border primitive. The
committed-RED task must be removed, and geometry/mouse coverage needs expansion.

**Strengths:**
- Identifies the true coupling: the strip currently renders one row
  (`internal/tuikit/globalssh.go:617`), Options reserves one row (`:629`), Storage
  independently subtracts one (`:960`), mouse routing assumes `y == 0` with raw label offsets
  (`:640`).
- Reusing `previewDashedBorder` avoids introducing a new visual primitive
  (`internal/tuikit/frame.go:500`).
- Fixed-geometry measurement and `NO_COLOR` checks directly address the spec's backstop
  (`09.4-UI-SPEC.md:168`).

**Concerns:**
- **HIGH** — Task 1 explicitly commits a failing test: the plan says to commit with
  `TestSubTabStripRendersBordered` red (`09.4-04-PLAN.md:103`), violating the one-buildable-
  logical-change rule (`docs/agent-instructions/engineering.md:24`); whole-module hooks make a
  red commit particularly problematic.
- **MEDIUM** — Geometry coverage omits three strip-bearing states: `subTabStrip()` also
  prefixes apply/storage ceremonies, the options-error state, and the zero-options state
  (`globalssh.go:865,872,913`); testing only normal Options and Storage browse states can miss
  clipping elsewhere.
- **MEDIUM** — The PTY step proves keyboard navigation, not click routing: it switches
  sub-tabs with left/right keys (`09.4-04-PLAN.md:252`), although the identified regression
  risk is mouse coordinate drift; the repo already has a raw SGR-mouse helper suitable for this
  (`e2e/create_flow_pty_e2e_test.go:123`).
- **LOW** — The allowlist is treated like stored golden frames, but it actually records
  semantic real/dummy divergences (`.planning/design/global-ssh/visual-divergence-allowlist.txt:60`);
  a border added by shared `tuikit` code appears on both sides and may require no allowlist
  change.

**Suggestions:**
- Observe the RED test locally, record its output, implement the border, then commit the
  green test and production change together.
- Exercise all five strip call-site classes at 100×30: normal Options, zero/error Options,
  normal Storage, apply ceremony, storage ceremony.
- Add a real SGR mouse click on both sub-tab labels and assert the active label and Storage
  content change; keep keyboard left/right coverage as a separate contract.
- Run the visual gate first; modify the allowlist only if the real/dummy difference set or its
  predicates actually change.
- Add the two planned `ui-frames` artifacts to `files_modified`.

**Risk:** MEDIUM-HIGH — the rendering approach is appropriate, but the planned red commit and
incomplete mouse/geometry evidence would leave important regressions undetected.

### Required Changes Before Execution (per Codex)

1. Add `cross_ai: true` to all four plans.
2. Reconcile and record the UI-SPEC approval state (`.planning/STATE.md` says approved;
   `09.4-UI-SPEC.md` still shows every checker box empty and `Approval: pending`).
3. Make Wave 1 fully green, preferably by moving the E2E/registry compatibility work from
   Plan 02 into Plan 01.
4. Fix Plan 02's screenshot-tagged test command, omitted packet-test caller, and platform-skip
   criterion.
5. Add real-PTY evidence for Plan 03's focus/toggle behavior.
6. Replace Plan 04's committed-RED task with observed RED followed by a green atomic commit,
   and add real PTY mouse coverage.

---

## Consensus Summary (Cycle 1 — Codex only)

Only one reviewer completed a source-grounded review in cycle 1 (Codex); `opencode-plan-review`
failed across all recorded attempts that cycle. No cross-AI consensus could be synthesized across
multiple completed reviewers — the findings below are Codex's alone, carried forward as cycle 1's
actionable output (all subsequently addressed in the `4caa42d` replanning pass; see cycle 2 above).

### Agreed Strengths
Not applicable (single completed reviewer) — see Codex's per-plan Strengths above.

### Agreed Concerns
Not applicable (single completed reviewer) — see Codex's per-plan Concerns above. The four
process-level HIGH findings (missing `cross_ai: true` frontmatter, Plan 01's red Wave 1 close,
Plan 02's uncompiled screenshot-tagged tests + omitted caller + impossible no-skip criterion,
Plan 04's committed-RED task) are the highest-priority items; they are procedural/test-process
defects, not architecture defects — Codex explicitly assessed the merge architecture itself as
sound.

### Divergent Views
Not applicable — only one reviewer produced findings this cycle.

## Recommended Next Steps (Cycle 1, historical)

1. Route Codex's six "Required changes before execution" items back into the four PLAN.md
   files via `/gsd-plan-phase 9.4 --reviews` before this phase executes.
2. Codex's review is source-grounded (full repo access) and independent of the `llm.local`
   endpoint that caused three prior failures across this phase's review history — no known
   infrastructure risk remains for accepting these findings.
3. `opencode-plan-review`/`opencode-sol` may still be worth investigating separately for
   `llm.local` stability, but that is no longer a blocker for phase 9.4 planning — Codex has
   supplied a complete, actionable review for this cycle.

---

## Consensus Summary (Cycle 2 — opencode-plan-review, this run)

Only one reviewer lane ran this cycle (`opencode-plan-review`, per `review.default_reviewers` —
no `--all` or explicit-flag override was used). It succeeded on its second attempt within this
cycle after one transient `ETIMEDOUT`. No cross-AI consensus can be synthesized against a second
completed reviewer this cycle; the findings above under "Convergence Cycle 2 (SUCCEEDED)" are this
run's full output.

### Agreed Strengths
Not applicable (single completed reviewer this cycle) — see the Cycle 2 section's "Cycle-1 Fix
Verification" above; all eight spot-checked cycle-1 fixes held up against the repo.

### Agreed Concerns
Not applicable (single completed reviewer this cycle). Highest priority: the NEW HIGH (four e2e
files outside `files_modified`, two of them degrading silently to a vacuous pass) and the premise
correction on Plan 02's screenshot-test claim (HIGH). Both require a further `<review_response>`
or plan edit before this phase should be considered converged.

### Divergent Views
Not applicable — only one reviewer produced findings this cycle.

## Recommended Next Steps (Cycle 2)

1. Route the new HIGH (four e2e files, two silent) and the premise-correction HIGH back into
   Plan 02 via `/gsd-plan-phase 9.4 --reviews` — these are the two unresolved HIGHs blocking
   convergence.
2. Fold the five actionable MEDIUM/LOW findings (Open Health site at `identities.go:4608`,
   Storage headroom margin, Plan 01 Task 4's allowlist STOP rigidity, palette-entry count risk,
   and Plan 02's remaining `internal cmd`-only grep) into the plans as edits or explicit
   deferrals.
3. `opencode-plan-review` is no longer a blocked lane for this phase — it produced a complete,
   source-grounded review on retry. The one-retry-on-ETIMEDOUT guidance should remain the default
   before escalating to a fallback lane.
