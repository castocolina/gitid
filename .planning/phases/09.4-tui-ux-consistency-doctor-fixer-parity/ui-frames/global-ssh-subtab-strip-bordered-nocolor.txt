Global SSH Options sub-tab with bordered sub-tab strip (NO_COLOR=1)

The bordered strip occupies 3 rows:
- Row 0: ╭╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╮ (top border with dashes, no color codes)
- Row 1: ┊ Options Storage & preview ┊ (labels with active one highlighted by text attributes, not color)
- Row 2: ╰╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╯ (bottom border with dashes, no color codes)

Below the strip are the Options sub-tab contents:
- Findings banner (if present)
- Option rows (2 lines each: title + detail)
- Detail pane on the right showing the selected option's explanation

The bordered strip remains visually distinct from the flat main tab bar even without color:
- The border SHAPE (╭╌╮ / ┊ / ╰╌╯) clearly marks this as a nested control
- The active label is still distinguished by text attributes (bold/reverse), not just color
- The dashed runes (╌) provide visual separation

This proves the strip design meets the NO_COLOR legibility requirement: the border shape and text attributes carry all semantic meaning; color is additive only.

Frame dimensions: 100 cols × 30 rows (fixed capture geometry)
Color: Disabled (NO_COLOR=1)
