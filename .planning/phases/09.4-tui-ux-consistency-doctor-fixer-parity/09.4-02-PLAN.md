---
phase: 09.4-tui-ux-consistency-doctor-fixer-parity
plan: 02
type: execute
wave: 2
depends_on: ["09.4-01"]
files_modified:
  - internal/tuikit/doctor_screen.go
  - internal/tuikit/design.go
  - internal/tuikit/globalssh.go
  - internal/tuikit/identities.go
  - internal/tuikit/frame.go
  - internal/tuikit/doctor.go
  - internal/tuikit/doc.go
  - internal/dummytui/data.go
  - internal/dummytui/doc.go
  - internal/doctor/checks/coherence.go
  - internal/doctor/checks/coherence_test.go
  - internal/tuikit/backend_stub_test.go
  - cmd/gitid/wiring.go
  - cmd/gitid/health.go
  - Makefile
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - .planning/design/health-fixer/visual-divergence-allowlist.txt
  - e2e/doctor_pty_e2e_test.go
  - e2e/health_fixer_pty_e2e_test.go
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [UXP-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No user-visible string anywhere in the product names a 'Health' tab or a 'Fixer' tab — not the help key map, not the command palette, not the cross-reference banners, not the identity manager, not the doctor engine's own SuggestedFix text, not the CLI's printed findings (UXP-05)."
    - "The merged Doctor status line states the finding count and the write discipline in one sentence, replacing both old screens' now-false status lines (UI-SPEC Copywriting Contract, D-06)."
    - "make gate-visual-regression passes against the merged single-screen shape, with the divergence allowlist and the ScreenSpec registry still in one-way correspondence (RESEARCH Pitfall 4)."
    - "All eight real-PTY behaviors that covered the split Health and Fixer screens still run, re-targeted at the merged tab's single navigation path, including the injected-write-failure batch-halt proof (RESEARCH Pitfall 7, DLV-06, D-16)."
    - "REQUIREMENTS.md and ROADMAP.md state the tab arithmetic the code actually ships (6 tabs to 5), not the stale 5-to-4 drafted before Phase 9.2 added a sixth tab (UI-SPEC Known Divergence #1)."
    - "make gate-copy-freeze passes with its frozen entry matching the shipped string, not a stale one (Makefile and design.go move in lockstep)."
  artifacts:
    - e2e/doctor_pty_e2e_test.go
    - "internal/screenshot/createflow.go: doctorSpecs() replacing healthFixerSpecs()"
    - "internal/screenshot/createflow.go: CaptureDoctorScreens replacing CaptureHealthFixerScreens"
    - .planning/design/health-fixer/visual-divergence-allowlist.txt
  key_links:
    - "internal/tuikit/design.go's GitIgnoreWiringNoBaseline <-> Makefile:465's gate-copy-freeze frozen entry — grep -F containment, so editing either alone fails the gate"
    - "the ScreenSpec registry <-> the 100-line divergence allowlist file <-> TestHealthFixerAllowlistMatchesRegistry — go build and go test stay green while these drift; only make gate-visual-regression catches it"
    - "the Makefile gate-visual-regression target's -run regex <-> the renamed gate test function names — a renamed test silently stops running rather than failing"
    - "internal/doctor/checks/coherence.go's SuggestedFix handoff clause <-> the merged screen's suggested-fix render <-> the gitid health CLI's printed output — one string, three consumers"
    - "the real-PTY suite's literal keystrokes <-> the merged tab's key number — these are raw bytes, not symbols, so no rename tool can rewrite them"
  backstops:
    - statement: "The merged Doctor screen's list plus detail pane still fit the fixed 100x30 capture geometry with both a fixable and a non-fixable finding present."
      verification: backstop
---

<objective>
Finish UXP-05: sweep every user-visible string that still names a tab the
product no longer has, consolidate the visual-regression registry onto the
merged single-screen shape, and rewrite the eight real-PTY behaviors that
encoded the two-tab navigation as literal keystrokes.

Purpose: Plan 01 made the merge compile and pass unit tests. Three surfaces
stay green through `go build` and `go test` while silently drifting — user
copy, the visual-regression registry, and the PTY suite's raw keystrokes. A
phase whose entire subject is naming and count inconsistency cannot ship
leaving stale names behind.
Output: a product with one findings tab and no reference anywhere to the two
it replaced; `make gate-visual-regression`, `make gate-copy-freeze`, and
`make test-e2e` all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-CONTEXT.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol | File | Kind |
|--------|------|------|
| `doctorSpecs()` | `internal/screenshot/createflow.go` | RENAMED from `healthFixerSpecs()` |
| `CaptureDoctorScreens` | `internal/screenshot/createflow.go` | RENAMED from `CaptureHealthFixerScreens` |
| `RegionDoctorBody`, `RegionDoctorCeremony` | `internal/screenshot/createflow_regions.go` | consolidated from `RegionHealthBody` / `RegionFixerBody` / `RegionFixerCeremony` |
| `TestDoctorAllowlistMatchesRegistry` | `cmd/gitid/gate_visual_regression_test.go` | RENAMED from `TestHealthFixerAllowlistMatchesRegistry` |
| `doctorScreenIDs` | `cmd/gitid/gate_visual_regression_test.go` | RENAMED from `healthFixerScreenIDs` |
| `fixerSuggestedFixHandoff`, `fixerSuggestedFixText` | `internal/tuikit/doctor_screen.go` | DELETED (the hand-off they stripped no longer exists) |
| `HealthReadOnlyNote` | `internal/dummytui/data.go` | DELETED (already dead — declared, never referenced) |
| `TestDoctor_RealPTY*` (8 functions) | `e2e/doctor_pty_e2e_test.go` | RENAMED + re-targeted from `TestHealthFixer_RealPTY*` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Stale tab-name sweep across every user-visible surface, plus the merged status copy</name>
  <files>internal/tuikit/doctor_screen.go, internal/tuikit/design.go, internal/tuikit/globalssh.go, internal/tuikit/identities.go, internal/tuikit/frame.go, internal/tuikit/doctor.go, internal/tuikit/doc.go, internal/dummytui/data.go, internal/dummytui/doc.go, internal/doctor/checks/coherence.go, internal/doctor/checks/coherence_test.go, internal/tuikit/backend_stub_test.go, cmd/gitid/wiring.go, cmd/gitid/health.go, Makefile, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md` — the Copywriting Contract table in full: the four DRAFT strings, and every row marked CITED/unchanged (those must NOT be touched)
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md` — Pitfall 5 and Assumption A2
    - `Makefile` lines 376-500 — the `gate-copy-freeze` target and its frozen string list, so the lockstep edit is made with the mechanism understood (`grep -rqF` containment across five package paths)
    - `internal/tuikit/design.go` around line 411 — `GitIgnoreWiringNoBaseline`
    - `internal/dummytui/data.go` lines 375-500 — the SuggestedFix fixtures and `HealthReadOnlyNote`
    - `internal/doctor/checks/coherence.go` around line 432 — the real engine's SuggestedFix format string
    - `.planning/design/mockup-src/src/demo/screens/Doctor.tsx` around line 164 — the original mockup's own status string, which UI-SPEC's merged status line adapts
  </read_first>
  <action>
Sweep every string that names a tab the product no longer has. Grep first,
edit second: run `rg -n 'Health|Fixer' --glob '*.go' internal cmd` plus
`rg -n 'Health|Fixer' Makefile README.md` and work the result list, rather
than editing only the sites named below — the list below is what planning
found, not a guarantee of completeness.

**A. Merged status lines (UI-SPEC Copywriting Contract, the two DRAFT
Doctor rows).** In `doctor_screen.go`, replace BOTH inherited status
strings — Health's, which told the user to switch to a tab that no longer
exists, and Fixer's, which counted only fixable findings and therefore
undercounts the merged list. Use UI-SPEC's default and per-identity DRAFT
strings verbatim. Both keep `pluralS` for the singular/plural form.

**B. Suggested-fix hand-off clause.** The real engine
(`internal/doctor/checks/coherence.go`) and the dummy fixtures
(`internal/dummytui/data.go`) both append a trailing clause pointing at the
Fixer screen; `doctor_screen.go` inherited machinery from `fixer_screen.go`
whose only job was to strip that clause before rendering. With one screen
there is nowhere to hand off TO. Remove the clause at both producers and
delete the now-purposeless strip constant and helper from `doctor_screen.go`,
rendering `sel.SuggestedFix` directly. Three test files pin the clause
(`internal/doctor/checks/coherence_test.go`, `internal/tuikit/backend_stub_test.go`,
and whatever survives of the screen tests Plan 01 merged) — update all of
them in the same commit; grep for the clause rather than trusting this list.
This clause also reaches the `gitid health` CLI's printed output and its
`e2e/health_fix_cli_e2e_test.go` coverage, so leaving it would have the CLI
point at a screen that does not exist.

**C. The frozen-copy lockstep pair.** `design.go`'s
`GitIgnoreWiringNoBaseline` tells the user to open the Fixer to set up a Git
baseline. It must name Doctor instead — and it is one of the strings
`make gate-copy-freeze` pins, so the `Makefile` entry must change in the SAME
commit. The gate uses `grep -rqF` containment, so a mismatched pair fails
loudly (good) rather than silently; verify by running the gate, not by
reading the diff.

**D. The remaining user-visible sites.** All of these render to a user:
  - `internal/tuikit/globalssh.go`'s `findingsBanner` trailing link text,
    which literally reads "Open Health (4)". Planner scope decision (RESEARCH
    Assumption A2, recommendation (b)): FIX IT in this phase. Leaving a
    screen that names a tab the product no longer has would reintroduce a
    smaller instance of the exact inconsistency class UXP-05 exists to
    eliminate. Point it at Doctor. Note that the digit stays `4` only because
    the merged tab happens to land in that slot — confirm that against the
    enum Plan 01 shipped rather than assuming it.
  - `internal/tuikit/identities.go`'s findings section header (which claims
    the rows are the same data another named tab shows) and its `h`-key
    footer label.
  - `internal/dummytui/data.go`'s read-only note constant: it is already
    DEAD (declared, never referenced anywhere in the module — verify with a
    fresh grep) and describes a read-only screen that no longer exists.
    Delete it rather than rewording it.
  - `cmd/gitid/wiring.go`'s post-fix withdrawal message, which names both
    removed tabs in one sentence.

**E. Doc comments that assert the old shape.** These do not render, but they
are the next reader's map and they currently document the split as current
design: `frame.go`'s `TabID` comment (if Plan 01 left any residue),
`doctor.go`'s file header (which describes itself as shared by two named
screens — rewrite to describe the single consumer; the FILE's code is still
untouched, only its comment changes), `internal/tuikit/doc.go`,
`internal/dummytui/doc.go`, `cmd/gitid/wiring.go`'s `DemoBanner` enumeration,
and `cmd/gitid/health.go`'s three references to the removed model type.

**F. Requirement and roadmap text.** `REQUIREMENTS.md`'s UXP-05 prose and
`ROADMAP.md`'s Phase 9.4 goal line both state the tab arithmetic as 5-to-4.
That was correct when drafted but Phase 9.2 has since added a sixth tab, so
the shipped math is 6-to-5 (UI-SPEC Known Divergence #1, binding). Correct
both, and add a one-line note recording WHY the number changed, matching this
project's existing scoped-divergence convention. Use scoped `Edit` calls on
both files — never rewrite either file wholesale (this project has a recorded
incident of a planning agent replacing a full ROADMAP with a stub).

Do NOT touch: the CLI verb names or the `gitid.health/v1` JSON envelope
(deliberate, see Plan 01's planner assumptions); any Copywriting Contract row
UI-SPEC marks CITED/unchanged; `internal/tuikit/doctor.go`'s code.
  </action>
  <verify>
    <automated>make gate-copy-freeze &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./... &amp;&amp; make lint</automated>
  </verify>
  <acceptance_criteria>
    - `make gate-copy-freeze` passes; the `Makefile` frozen entry and `design.go`'s constant are byte-identical to each other.
    - `rg -n 'Health|Fixer' --glob '*.go' internal cmd | grep -v '_test.go'` returns only: historical citations that are explicitly dated to Phase 8 or earlier, the CLI's own `health` verb and `gitid.health/v1` envelope, and `internal/sshconfig/rewrite.go`'s D-09 carve-out comment. Every remaining hit is listed in the SUMMARY with a one-line justification.
    - `rg -c 'available on the Fixer screen' .` returns 0.
    - `rg -c 'HealthReadOnlyNote' .` returns 0.
    - `sed -n '/^func findingsBanner/,/^}/p' internal/tuikit/globalssh.go | grep -v '^\s*//' | grep -c 'Doctor'` returns 1.
    - `grep -c 'tabs to 5' .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns a non-zero count for both files.
    - `wc -l .planning/ROADMAP.md` is within 5 lines of its pre-task value — the scoped-`Edit` rule held.
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` and `make lint` are green.
  </acceptance_criteria>
  <done>No user-visible string, and no doc comment describing current design, names a tab the product no longer has; the frozen-copy gate passes with the shipped string.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate the visual-regression ScreenSpec registry and its divergence allowlist onto one screen</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_test.go, cmd/gitid/gate_visual_regression_test.go, .planning/design/health-fixer/visual-divergence-allowlist.txt, Makefile</files>
  <read_first>
    - `internal/screenshot/createflow.go` lines 2050-2240 — `CaptureHealthFixerScreens`, its doc comment (including the second-row line-wrap rationale), and `healthFixerSpecs()` with all seven region dispositions
    - `internal/screenshot/createflow_regions.go` lines 220-250 and 350-365 and 1240-1280 and 1455-1475 — the three region constants, their capture-dispatch case arms, `extractFixerBody`, and the region inventory list
    - `.planning/design/health-fixer/visual-divergence-allowlist.txt` — all 100 lines, especially the STRICT SCHEMA header block
    - `cmd/gitid/gate_visual_regression_test.go` — `TestHealthFixerAllowlistMatchesRegistry` and the `healthFixerScreenIDs` set it filters on
    - `Makefile` line 774-776 — the `gate-visual-regression` target's `-run` regex
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md` — Pitfall 4 in full, and the Anti-Patterns entry forbidding a hand-patched allowlist
  </read_first>
  <action>
This registry is the third place that hard-codes "Health and Fixer are two
screens", and it is the one `go build` and `go test` cannot see. Consolidate
it as a unit.

**A. The capture driver.** Rename `CaptureHealthFixerScreens` to
`CaptureDoctorScreens` and rewrite its navigation: it currently boots the
tab at one key number for the read-only frame and a DIFFERENT key number for
the fix frame. There is now one key and one screen, so the three captured
frames become: the merged list after its real scan; the same list with a
fixable finding selected; and that finding's fix ceremony at its pre-write
state, reached by pressing the fix key IN PLACE. Preserve the existing
second-row selection choice and carry its rationale comment forward — it
exists because the dummy fixture's first row wraps a timestamped backup path
across two lines, which the timestamp normalizer cannot handle. Preserve the
existing positive assertion that the ceremony frame differs from the list
frame (a fixture bug that leaves the ceremony unopened would otherwise pass
the non-emptiness check).

**B. Screen IDs and specs.** Rename `healthFixerSpecs()` to `doctorSpecs()`.
The three `ScreenID` values name the removed screens; rename them to the
merged shape and update every `Interaction` string — each currently
instructs a reader to "boot the <name> tab (view N)" with view numbers that
are wrong twice over (the Phase 9.2 renumbering, then the merge). Collapse
the two body dispositions into one for the merged body and keep the ceremony
disposition. Critically, the breadcrumb disposition's needle is a literal
containing the removed tab's name — the merged screen's breadcrumb will not
contain it, so that needle must move or the disposition silently stops
matching what it claims to match.

**C. Region constants.** Consolidate the three `Region*` constants into the
merged set (one body region, one ceremony region), updating: their
declarations, their doc comments, the capture-dispatch `case` arms, the
extractor function(s) they dispatch to, and the region inventory list near
the end of the file. A region constant that survives with no spec
referencing it is dead weight the inventory test may or may not catch —
remove it rather than leaving it.

**D. The allowlist file.** RE-DERIVE it from the new registry; do NOT
diff-patch the text file by hand (RESEARCH names this as an anti-pattern:
the file and the registry are kept in one-way correspondence by a dedicated
test, and hand-editing one produces an entry that references a screen ID or
region the registry no longer has). Preserve the header block's STRICT
SCHEMA documentation and update its prose to describe one screen. Update the
gate test's own name and its screen-ID filter set to match.

**E. The Makefile's `-run` regex.** The `gate-visual-regression` target
selects tests by a name pattern that includes the removed screens' name.
A renamed test does not fail under a stale pattern — it silently stops
running, which is strictly worse. Update the pattern in the same commit and
prove the renamed tests actually execute by reading the `-v` output.

**F. Prove the gate can still fail.** After the gate passes, deliberately
break one allowlist entry, observe a RED failure, then revert. This project
has done exactly this hand-verification for this gate before (Phase 3 Wave 5)
because a visual gate that passes vacuously is worse than no gate. Record
both the RED output and the restored GREEN in the SUMMARY.
  </action>
  <verify>
    <automated>make gate-visual-regression &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/screenshot/... ./cmd/gitid/...</automated>
  </verify>
  <acceptance_criteria>
    - `make gate-visual-regression` passes and its `-v` output shows the renamed gate tests actually running (quote the run lines in the SUMMARY).
    - The fail-path was hand-verified: a deliberately broken allowlist entry produced a RED failure, quoted in the SUMMARY, and was reverted to GREEN.
    - `rg -c 'healthFixerSpecs|CaptureHealthFixerScreens|RegionHealthBody|RegionFixerBody|RegionFixerCeremony|healthFixerScreenIDs' .` returns 0.
    - `grep -c 'HealthFixer' Makefile` returns 0.
    - The allowlist file's entry count equals the number of `ux-improvement` dispositions the new registry declares for the merged screen IDs, and the correspondence test passes.
    - Every `Interaction` string in the new specs names a view number that matches the shipped 5-tab enum.
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` stays green.
  </acceptance_criteria>
  <done>The visual-regression registry, its region set, its allowlist file, its correspondence test, and the Makefile pattern that runs it all describe one Doctor screen; the gate has been proven able to fail.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the eight real-PTY behaviors onto the merged tab's single navigation path</name>
  <files>e2e/doctor_pty_e2e_test.go, e2e/health_fixer_pty_e2e_test.go</files>
  <read_first>
    - `e2e/health_fixer_pty_e2e_test.go` (all 379 lines) — all eight test functions, the shared session helpers at the top of the file, and the two navigation helpers around lines 105-140 that send the literal tab keys
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md` — Pitfall 7 in full, and Pitfall 2's "Warning signs" paragraph (the batch-halt test is Pitfall 2's acceptance test)
    - `.planning/phases/08-health-fixer/08-08-SUMMARY.md` — how the injected real write-failure was produced for the batch-halt proof, so the mechanism is reused rather than re-invented
    - `internal/tuikit/doctor_screen.go` — the merged key model the rewritten keystrokes must drive
  </read_first>
  <precondition>`make build` succeeds and produces a `gitid` binary — the PTY suite drives the COMPILED binary, not the package, so a package-level green from Plan 01 does not imply this suite can run.</precondition>
  <action>
This file is the project's DLV-06 closure for both merged-away screens and it
is the single most load-bearing test artifact the merge touches. It is a
REWRITE, not a rename: real-PTY tests send raw keystroke bytes against the
compiled binary, so they encode "press this key for Health, then press that
other key for Fixer" as literal navigation that no rename tool can rewrite
and that the compiler cannot flag.

Rename the file to `e2e/doctor_pty_e2e_test.go` and rewrite it so all EIGHT
covered behaviors survive, each re-targeted at the merged tab's single
navigation path (press the Doctor key once; the fix keys act in place, with
no second tab to switch to). The eight behaviors, none of which may be
dropped:

  1. findings list with inline detail and per-identity scoping
  2. the all-green / nothing-found state
  3. the per-identity deep-link path
  4. the configuration parse-error state
  5. a single fix that writes and creates its backup
  6. the Fix-all batch walk
  7. the Fix-all batch walk HALTING on a real, injected write failure
  8. the nothing-to-fix state

Two of these change meaning under the merge and must be strengthened, not
just re-pointed:
  - Behavior 8 previously meant "the Fixer tab shows nothing because it
    filtered to fixable findings only". On the merged screen the list shows
    every finding, so this test must now assert that non-fixable findings ARE
    listed while no fix affordance is offered — the count-parity outcome
    UXP-05 exists to produce. Assert on the rendered frame's text.
  - Behavior 7 is T-09.4-01's end-to-end acceptance test. It must assert
    POSITIVELY on the rollback message's text in the rendered frame. RESEARCH
    Pitfall 2's stated warning sign is precisely a version of this test that
    passes for the wrong reason because a type assertion early-returned; a
    "no panic / no crash" assertion does not discriminate. Reuse the existing
    OS-level write-failure injection rather than inventing a new one.

Keep every existing shared helper and fixture-home setup; only the
navigation and the assertions that named the removed screens change. Do not
add `t.Skip` anywhere.

Run the full e2e suite, not just this file — it is a single `e2e` package
and the run is long (roughly 950 seconds under `-race` per the project's
measured baseline), so budget for it and do not shortcut to a `-run` filter
for the final acceptance run.
  </action>
  <verify>
    <automated>make build &amp;&amp; make test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/health_fixer_pty_e2e_test.go` no longer exists; `e2e/doctor_pty_e2e_test.go` does.
    - `rg -c '^func Test' e2e/doctor_pty_e2e_test.go` returns 8, and each function's name and doc comment identify which of the eight listed behaviors it covers.
    - `make test-e2e` passes with no `t.Skip` added (`rg -c 't.Skip' e2e/doctor_pty_e2e_test.go` returns 0).
    - The batch-halt test contains a positive `strings.Contains`-style assertion on the rollback message text taken from the rendered PTY frame, and the SUMMARY quotes that assertion.
    - The nothing-to-fix test asserts a non-fixable finding IS present in the rendered list while no fix affordance is offered.
    - `rg -c 'TabHealth|TabFixer|Health tab|Fixer tab' e2e/` returns 0.
  </acceptance_criteria>
  <done>All eight real-PTY behaviors run against the merged tab through the compiled binary, with the injected-write-failure rollback proven by a positive text assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI keypress -> fix ceremony -> `Backend.Persist` | the real-write boundary; this plan's job is to PROVE it is unchanged, not to change it |
| rendered frame text -> the visual-regression gate and the copy-freeze gate | the automated surfaces that catch drift `go build` cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.4-03 | Tampering | the ported fix ceremony, end-to-end | high | mitigate | Task 3's behaviors 5, 6 and 7 drive the COMPILED binary through a real PTY and assert the write, the backup, and the rollback actually happen against real files — the only proof that the preview + typed-confirm + backup discipline (FIX-01, D-06's explicit "what this does NOT remove") survived the port byte-for-byte rather than merely looking similar. |
| T-09.4-01 | Tampering | `checkFixBatchHalt` end-to-end | high | mitigate | Task 3's behavior 7 injects a genuine OS-level write failure mid-batch and asserts positively on the rollback message text in the rendered frame. Paired with Plan 01 Task 3's unit guard, this closes the silent-no-op failure mode from both directions. |
| T-09.4-05 | Repudiation | the visual-regression gate | medium | mitigate | A gate whose `-run` pattern no longer matches its renamed tests reports success while executing nothing, producing an unearned green record for the phase. Task 2 updates the Makefile pattern in the same commit and requires reading the `-v` run lines, plus a deliberate break-and-revert to prove the gate can still fail. |
| T-09.4-06 | Spoofing | user-facing copy naming a nonexistent screen | medium | mitigate | Copy that instructs the user to "open the Fixer" for a write they need points them at nothing — including from the `gitid health` CLI, where the doctor engine's own SuggestedFix text carries the clause. Task 1 removes it at the producer, so all three consumers correct together; `make gate-copy-freeze` pins the one string that is frozen. |
| T-09.4-SC | Tampering | package-manager installs | low | accept | No `go get`, `npm`, `pip`, or `cargo` install occurs. RESEARCH.md's Package Legitimacy Audit records zero external packages for this phase; `go.mod` is not modified. |
</threat_model>

<verification>
The full closeout battery, in this order, all from the repository root. This
is the project's established discipline — every completed phase runs exactly
these four:

1. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...`
2. `make lint`
3. `make test-e2e`
4. `make gate-visual-regression`

Plus this plan's own two:

5. `make gate-copy-freeze` (also reached via `make test`)
6. `go list -deps ./internal/tuikit | grep -c 'castocolina/gitid/internal/doctor'` returns 0

Every one of these must be run by the executor and its real output recorded
in the SUMMARY — a PASS claim without quoted output does not close a gate in
this project.
</verification>

<success_criteria>
- No user-visible string names a tab the product no longer has, and the one
  frozen such string moved in lockstep with its `Makefile` gate entry.
- The merged Doctor status line is UI-SPEC's DRAFT copy, replacing both old
  screens' now-false lines.
- `make gate-visual-regression` passes against a one-screen registry, and has
  been demonstrated able to fail.
- All eight real-PTY behaviors run on the merged tab, including the
  injected-write-failure rollback with a positive text assertion.
- `REQUIREMENTS.md` and `ROADMAP.md` state the shipped 6-to-5 tab arithmetic.
- All six verification commands are green with quoted output.
</success_criteria>

<output>
Create `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-02-SUMMARY.md` when done.
Record: the full grep result for remaining Health/Fixer hits with a
justification line each; the visual gate's RED-then-GREEN fail-path evidence;
the eight PTY behaviors mapped to their new function names; and quoted output
for all six verification commands.
</output>
