---
phase: 09.4-tui-ux-consistency-doctor-fixer-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tuikit/frame.go
  - internal/tuikit/app.go
  - internal/tuikit/doctor_screen.go
  - internal/tuikit/health_screen.go
  - internal/tuikit/fixer_screen.go
  - internal/tuikit/doctor_screen_test.go
  - internal/tuikit/health_screen_test.go
  - internal/tuikit/fixer_screen_test.go
  - internal/tuikit/doctor_test.go
  - internal/tuikit/app_test.go
  - internal/tuikit/frame_test.go
  - internal/tuikit/mouse_test.go
  - internal/tuikit/identities_test.go
  - internal/tuikit/gitignore_test.go
  - internal/tuikit/globalgit_test.go
  - cmd/gitid/wiring_test.go
  - cmd/gitid/wiring_storage_test.go
  - e2e/health_fixer_pty_e2e_test.go
  - e2e/ui_pty_e2e_test.go
  - e2e/dummy_demo_e2e_test.go
  - e2e/global_git_pty_e2e_test.go
  - e2e/git_configuration_pty_e2e_test.go
  - e2e/identity_manager_pty_e2e_test.go
  - e2e/create_flow_pty_e2e_test.go
  - .planning/design/health-fixer/visual-divergence-allowlist.txt
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
autonomous: false
cross_ai: true
requirements: [UXP-05]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Exactly ONE top-level tab reports doctor findings; its list length equals len(orderedFindings(s)) for the same scan, so no second screen can report a contradicting number (UXP-05, D-06)."
    - "That one tab lists non-fixable (info-only) findings AND fixable findings together; selecting a fixable one offers the inline f action, selecting a non-fixable one offers no fix action at all (UXP-05, D-06)."
    - "Pressing f or F on a selection whose Fixable bit is false never opens a fix ceremony (D-02 scope fence carried into the merge; Phase 8 CR-01 regression guard)."
    - "The main menu shows 5 tabs reachable by keys 1..5; key 6 is inert and cannot index past the screens array (UI-SPEC Known Divergence #1)."
    - "A failed fix inside a Fix-all batch still halts the walk and renders the D-16 rollback message — checkFixBatchHalt is not a silent no-op after the model type changed (RESEARCH Pitfall 2)."
    - "The Doctor tab's first activation still runs the scan tick and renders the scanning state before findings appear (UI-SPEC UI Considerations: loading)."
    - "A scanned machine with zero findings still renders the shared two-line all-green state (UI-SPEC UI Considerations: empty)."
    - "The identity manager's h key and the header health chip both open the merged Doctor tab, and the h key still carries its per-identity findings filter (RESEARCH Pitfall 3)."
    - "This plan closes its wave GREEN on every gate the project runs at wave close — `make test`, `make lint`, `make test-e2e`, and `make gate-visual-regression` — with no gate knowingly deferred to a later plan (ONESHOT.md Non-Negotiable Rule 6; REVIEWS.md cycle-1 Plan 01 HIGH)."
    - "That wave-close green is EARNED, not vacuous: no e2e region comparison silently drops out of its gate because a header extractor stopped finding its anchor label. Every header-status extractor that anchors on a nav-tab label fails LOUDLY when its anchor is absent, so a post-merge anchor miss is a red build, not a skipped region (REVIEWS.md cycle-2 HIGH #1)."
  artifacts:
    - internal/tuikit/doctor_screen.go
    - internal/tuikit/doctor_screen_test.go
    - "internal/tuikit/frame.go: TabDoctor member of the 5-value TabID enum"
    - "internal/tuikit/app.go: App.screens [5]screenModel"
  key_links:
    - "checkFixBatchHalt's type assertion <-> the merged screen model's concrete type — a stale assertion fails to ok=false SILENTLY, disabling the D-16 rollback with no compile error (RESEARCH Pitfall 2)"
    - "tabNavLabels' member set <-> the e2e header-status extractors' anchor literal <-> the real-versus-dummy region comparison's both-sides-empty skip branch — when the anchor disappears both sides extract the empty string, the comparison SKIPS the region as 'not applicable', and the gate reports green having compared nothing (REVIEWS.md cycle-2 HIGH #1)"
    - "keyResult.doctorIdentity <-> the merged model's identityName field <-> a.setTab(TabDoctor) at all three routing sites (keyboard, mouse, header chip)"
    - "the merged list's data scope (orderedFindings, unfiltered) <-> the f/F handlers' per-row sel.Fixable gate — the list widens, the action gate does NOT"
    - "TabID enum arity <-> App.screens array length <-> the number-key switch's case set <-> the right-arrow clamp — Go arrays encode length in the type, and TabID(int(key[0]-'1')) indexes that array directly"
  planner_assumptions:
    - "UI-SPEC's UI Considerations row 'D-06's reversibility gate' is marked unresolved and explicitly out of scope for the design contract. Planner resolution: it is discharged as this plan's Task 1 checkpoint:decision, per CONTEXT.md D-06's own instruction to the planner. No design artifact is owed for it."
    - "Open Question 1 (where the merged model lives) is resolved by the planner as a NEW file internal/tuikit/doctor_screen.go, with both health_screen.go and fixer_screen.go deleted — see Task 2's action for the full rationale. RESEARCH.md's default (absorb into health_screen.go) is deliberately NOT taken."
    - "Open Question 2 (rename keyResult.healthIdentity) is resolved by the planner as YES, rename to doctorIdentity in this same task — the compiler catches every site, so the cost is near zero and leaving it contradicts the phase's own consistency bar."
    - "The CLI surface is deliberately NOT renamed: gitid health stays the primary verb, gitid doctor stays its hidden compatibility alias, and the gitid.health/v1 JSON envelope keeps its name. UXP-05's text scopes the rename to the TUI main menu; the JSON envelope is a published output contract and renaming it would be a one-way break with no requirement behind it."
    - "UI-SPEC sign-off state (REVIEWS.md HIGH, project-level): 09.4-UI-SPEC.md's 'Checker Sign-Off' block was left with empty checkboxes and 'Approval: pending' even though the gsd-ui-checker ran and returned 6/6 PASS, which is what STATE.md's 'Phase 09.4 UI-SPEC approved' records. The block was a stale TEMPLATE artifact, not a real gap. This planning pass mechanically backfilled the six checkboxes and the Approval line in 09.4-UI-SPEC.md, marking the backfill and its provenance inline. Executors and verifiers should treat the UI-SPEC as approved; no re-run of the UI checker is owed."
---

<objective>
Reverse Phase 8's FIX-02 split: collapse the standalone Health and Fixer tabs
into a single merged **Doctor** tab that lists every finding (Health's
unfiltered data scope) and carries the inline fix ceremony (Fixer's write
machinery, ported unchanged), so the two screens can no longer report
contradicting counts for the same scan.

This plan lands the compile-atomic core — the `TabID` enum, the App shell's
tab plumbing, the merged screen model, and every call site the Go compiler
forces to move with them — PLUS the minimum navigation retarget of the two
runtime-only surfaces (the real-PTY e2e suite and the screenshot capture
driver) needed to close this wave with every gate green. Renaming those
surfaces' symbols, consolidating their registries, re-deriving their
allowlists, strengthening their assertions, and the user-visible copy sweep
all remain Plan 02's scope.

Purpose: UXP-05's count-parity bug is not a copy bug — it is two
independently-scoped views over one findings source. A single source of truth
for "here are the findings, here is what I can fix" is the only fix that
cannot drift again (D-06).
Output: `internal/tuikit/doctor_screen.go` (the merged `doctorModel`), a
5-value `TabID` enum, and a GREEN full wave-close battery — `make test`,
`make lint`, `make test-e2e`, and `make gate-visual-regression`.
</objective>

<review_response>
## Cycle 1 (Codex `gpt-5.6-sol`, REQUEST CHANGES)

Cross-AI review (`09.4-REVIEWS.md`) raised
three findings against this plan plus two project-level ones. Their disposition,
recorded here so the executor and the next reviewer can see it without reading
REVIEWS.md:

| Finding | Severity | Disposition |
|---------|----------|-------------|
| Wave 1 deliberately left red — `make test-e2e` and `make gate-visual-regression` expected to FAIL at wave close, contrary to ONESHOT.md Rule 6 | HIGH | **INCORPORATED.** Task 4 (NEW) does the minimum navigation retarget of the PTY suite and the screenshot capture driver so this wave closes with all four gates green. `<verification>` no longer defers any gate. Codex's own first suggestion. |
| Tracer test checks only list membership/count — Health already does that; the merge property only emerges combined with the `f` assertion | MEDIUM | **INCORPORATED.** Task 2's `<behavior>` Test 1 is now ONE test proving all three properties on the same tab (all findings listed + fixable opens ceremony + non-fixable does not). Tests 2/3 remain as focused follow-ups but no longer carry the tracer property alone. |
| Source-reversion proof for the type-assertion guard would produce a COMPILE error, not the runtime `ok == false` path it claims to prove | MEDIUM | **INCORPORATED.** Task 3's action now names a wrong-but-compiling concrete type (`globalGitModel`) as the reversion target, so the RED state travels the real silent-failure path at `app.go`'s assertion site. |
| All four plans omit the mandatory `cross_ai: true` frontmatter field (ONESHOT.md Rule 3 / Rule 12) | HIGH | **INCORPORATED.** Added to all four plans' frontmatter. |
| `.planning/STATE.md` says the UI-SPEC is approved; `09.4-UI-SPEC.md` shows empty checkboxes and `Approval: pending` | HIGH | **RESOLVED as a stale template artifact** — see this plan's `planner_assumptions` entry. The UI checker ran and returned 6/6 PASS; the sign-off block was never mechanically updated. This pass backfilled it in `09.4-UI-SPEC.md` with inline provenance. No re-run is owed. |

One review claim needed a correction that the executor must know: Codex's Plan 02
HIGH stated that screenshot-tagged code "is never compiled". `make lint` depends on
`lint-tagged`, which runs `go vet -tags screenshot ./...` and
`golangci-lint run --build-tags screenshot ./internal/screenshot/...`
— so a renamed symbol with a stale caller DOES fail `make lint`.
(The follow-on claim originally recorded here — that the screenshot-tagged tests'
BODIES are never run — was itself wrong and has been corrected in cycle 2; see
Plan 02's `<review_response>` cycle-2 entry. `make test`'s second `go test` line
already runs them.)

## Cycle 2 (opencode-plan-review `local-llm-env/my-plan-review`, source-grounded)

Cycle 2 spot-checked all eight of this plan's cycle-1 fixes against the repo and
found every one of them real. It then raised one MEDIUM and one LOW against this
plan, plus a NEW HIGH whose blast radius lands squarely on this plan's Task 4.
All three claims were re-verified against the CURRENT source during this pass
before being acted on, and two of them needed correcting:

| Finding | Severity | Disposition |
|---------|----------|-------------|
| NEW: four e2e files still parse or assert the removed tab labels and are in no plan's `files_modified`; two of them (`git_configuration_pty_e2e_test.go`, `identity_manager_pty_e2e_test.go`) extract the header status via a `LastIndex` on a label the merge removes and `return ""` on no-match, so their region comparisons degrade to a VACUOUS pass rather than failing | HIGH | **INCORPORATED into THIS plan, and the finding was extended.** Re-verified at source: all four sites are real. Two corrections to the finding: (1) it names four files; there are **six** — `e2e/global_git_pty_e2e_test.go` also asserts a removed label, and `e2e/create_flow_pty_e2e_test.go` is a THIRD consumer of the identity-manager region extractor, so it inherits the same vacuous pass. (2) The finding assigns these to Plan 02; they belong HERE, because the three loud-failure files break `make test-e2e` the moment Task 2's merge lands, which would break exactly the Wave-1 green guarantee Task 4 exists to provide — and a wave that closes green with two silently-skipped gate regions is the unearned green this phase exists to eliminate. Task 4 now owns all six files; its `<action>` gains sections C and D; six files added to `files_modified`. |
| Task 4's "STOP if the gate demands an allowlist change" is likely to trigger for the merge itself, because the allowlist pins a breadcrumb needle naming the removed tab | MEDIUM | **INCORPORATED.** Re-verified at source: the allowlist's last entry is a `fixer-ceremony-preview:breadcrumb` disposition whose predicate needle is the removed tab's name joined to the ceremony crumb. Post-merge that predicate cannot hold on either side, so the gate goes RED for a NEEDLE reason while the divergence SET is genuinely unchanged — and the blanket STOP would block Task 4 from delivering its own green close. Task 4's action now carries a narrow, explicitly-bounded exception permitting exactly that one needle correction, with everything else still under the hard STOP. |
| The `paletteEntries` count criterion may over-count if a Help entry shares the slice literal with a tab field | LOW | **INCORPORATED, though the stated risk does not currently exist.** Re-verified at source: today's Help entry carries `help: true` and no tab field, so the criterion is accurate as written. Tightened anyway — the grep now anchors on the typed enum value and excludes the help line, so it stays correct if a future Help entry gains a tab target. |

## Cycle 3 (Codex `gpt-5.6-sol` fallback, after two same-lane opencode timeouts; source-grounded)

Cycle 3 re-verified all cycle-2 fixes as holding, then raised one NEW HIGH and
one MEDIUM against this plan (a second MEDIUM landed on Plan 02, see that
plan's own cycle-3 entry). Both were independently re-verified at source
before acting; this was the max-cycles (3) boundary, so per the user's
explicit direction these were closed with a targeted fix pass rather than a
fourth full review round.

| Finding | Severity | Disposition |
|---------|----------|-------------|
| Task 4's `<files>` omits `internal/screenshot/createflow_regions.go`, whose `extractHeader`/`extractHeaderStatus` (lines 541-556, 800-816) also anchor on `strings.LastIndex(plain, "Fixer ")` and degrade silently on a miss — a FOURTH instance of the same vacuous-pass shape cycle 2 fixed in two `e2e/` extractors, feeding the Doctor visual-gate registry's identity-count disposition | HIGH | **INCORPORATED.** Re-verified at source: both functions exist at the cited lines with the cited anchor. `internal/screenshot/createflow_regions.go` added to Task 4's `files`; section D extended to cover all FOUR extractors (not two); the fix mechanism differs by context — the two `e2e/` extractors use a hard test failure, the two library-code extractors (no `*testing.T` available) use `panic()`, since this package is build/test tooling never shipped in `gitid`'s runtime paths. Acceptance criteria and the loud-path proof requirement extended to all four sites. |
| Doctor's ported `identityName` per-identity filter (Plan 01 Task 2, section C) has no reset path outside the `h`-deep-link — `activate()` never clears it and every ordinary tab-entry route (numeric key, palette, header-chip click, arrow adjacency) shares the same `setTab` choke point the deep-link route pre-seeds before calling, so a filter set once silently survives every later plain tab switch | MEDIUM | **INCORPORATED.** Re-verified at source: confirmed `activate()` is a no-op on `identityName`, confirmed both deep-link call sites (`app.go:456-461,557-560`) pre-set the field before calling generic `setTab`, and confirmed six other call sites route through the same `setTab` with no clear. Task 2's action now specifies a `setTabFiltered` sibling method: the deep-link sites call it (set-then-enter), while plain `setTab` unconditionally clears the filter before activating — so every ordinary route lands clean. New acceptance criterion requires a test proving the filter resets on ordinary re-entry after a deep-link visit. |
</review_response>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@recipes/README.md

@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-CONTEXT.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md

@internal/tuikit/doctor.go
@internal/tuikit/health_screen.go
@internal/tuikit/fixer_screen.go
</context>

<artifacts_this_phase_produces>
Symbols this PLAN creates, renames, or deletes (Plan 02 adds the copy and
harness symbols; Plans 03/04 add the option-row and sub-tab symbols):

| Symbol | File | Kind |
|--------|------|------|
| `TabDoctor` | `internal/tuikit/frame.go` | NEW enum member (replaces `TabHealth` + `TabFixer`) |
| `TabHealth`, `TabFixer` | `internal/tuikit/frame.go` | DELETED enum members |
| `doctorModel` | `internal/tuikit/doctor_screen.go` | NEW type (merges `healthModel` + `fixerModel`) |
| `newDoctorModel(b Backend)` | `internal/tuikit/doctor_screen.go` | NEW constructor |
| `healthModel`, `newHealthModel`, `fixerModel`, `newFixerModel` | (deleted files) | DELETED |
| `keyResult.doctorIdentity` | `internal/tuikit/app.go` | RENAMED from `healthIdentity` |
| `doctorApp(t)` | `internal/tuikit/doctor_test.go` | RETARGETED to the merged tab |
| `fixerApp(t)` | `internal/tuikit/doctor_test.go` | DELETED (no successor) |
| `docModel(t, a)` | `internal/tuikit/doctor_test.go` | RETARGETED to return `doctorModel` |
| `TestDoctorMergedListShowsEveryFinding` | `internal/tuikit/doctor_screen_test.go` | NEW (tracer's red test) |
| `TestDoctorFixGateRespectsFixableBit` | `internal/tuikit/doctor_screen_test.go` | NEW (T-09.4-02 guard) |
| `TestDoctorBatchHaltSurvivesModelRename` | `internal/tuikit/doctor_screen_test.go` | NEW (T-09.4-01 / Pitfall 2 guard) |
| `TestDoctorCountIsTheOnlyFindingCount` | `internal/tuikit/doctor_screen_test.go` | NEW (UXP-05 count-parity guard) |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: One-way gate — confirm the Doctor merge with its now-quantified blast radius</name>
  <read_first>
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-CONTEXT.md` — D-06 in full, including its Reversibility note
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md` — Common Pitfalls 1-7
    - `.planning/REQUIREMENTS.md` §L — FIX-02's reopening note
    - `.planning/phases/08-health-fixer/08-01-PLAN.md` Task 2 — the original split's stated rationale
  </read_first>
  <reversibility rating="one-way">Un-completes a previously shipped and gate-verified requirement (FIX-02, marked Complete in REQUIREMENTS.md), deletes two shipped screen models, and changes the main menu's tab arity. Reverting after landing means re-splitting a screen and re-writing eight real-PTY e2e tests a second time.</reversibility>
  <decision>Proceed with reversing Phase 8's FIX-02 split — merging the standalone Fixer tab into Health to produce a single combined Doctor tab (D-06) — now that planning has measured a blast radius materially larger than the one CONTEXT.md estimated when the decision was locked.</decision>
  <context>
CONTEXT.md's D-06 Reversibility note named six source files plus "every test
file covering those symbols". Research and planning found four further layers
it did not name:

  1. **App tab plumbing is arity-typed.** `App.screens` is a fixed-size
     `[6]screenModel` array (`app.go:147`) and `newScreens` returns the same
     type (`app.go:726`) — Go encodes array length in the type, so this is a
     hard compile break, not a cleanup. The number-key handler
     (`app.go:474`) computes `TabID(int(key[0]-'1'))` and indexes that array
     directly, so leaving key `6` in the case set would index past a 5-slot
     array at runtime.
  2. **`checkFixBatchHalt` type-asserts to `fixerModel`** (`app.go:315,319`).
     A stale assertion does NOT fail to compile — it fails to `ok=false` and
     silently disables the D-16 batch rollback. This is the project's own
     recurring injected-seam wiring blindspot class.
  3. **A third registry hard-codes the two-screen shape**: the
     visual-regression `ScreenSpec` inventory (`internal/screenshot/createflow.go`'s
     `healthFixerSpecs()`, its three `Region*` constants, its
     `CaptureHealthFixerScreens` driver) and the 100-line
     `.planning/design/health-fixer/visual-divergence-allowlist.txt` it is
     cross-checked against. `go build` and `go test` both stay green while
     this drifts; only `make gate-visual-regression` catches it, and that
     target's own `-run` regex names the old screens literally.
  4. **User-visible copy naming Health/Fixer reaches further than the TUI**,
     including one string frozen by `make gate-copy-freeze`
     (`Makefile:465` paired with `design.go:411`), the real doctor engine's
     own output (`internal/doctor/checks/coherence.go:432` emits a
     SuggestedFix pointing at the Fixer screen, which the `gitid health` CLI
     also prints), the help overlay's key map (`app.go:83`), and the identity
     manager's findings header (`identities.go:4597`).

Total: roughly 30 source files and 13 test files across four packages, one
Makefile gate regex, one frozen-copy entry, and a 379-line eight-function
real-PTY suite that encodes the two-tab navigation as literal keystrokes.

What does NOT change, so the decision is not read as broader than it is: the
write-confirmation ceremony (preview + typed-confirm + backup, FIX-01,
CLAUDE.md's write-safety rule) is ported byte-for-byte; `doctor.go`'s shared
helpers are reused verbatim with zero edits; `internal/doctor`'s check engine
and `cmd/gitid/wiring.go`'s `Persist`/`FixPlanFor` seams are untouched; and
the CLI surface (`gitid health`, the hidden `gitid doctor` alias, the
`gitid.health/v1` JSON envelope) is deliberately left alone.

The one concern the ORIGINAL split actually carried, so the merge answers it
rather than ignoring it: `08-01-PLAN.md` Task 2's stated rationale was FIX-02's
requirement text plus one isolation property — that Health's rendered text for
a fixable finding carried no fix/confirm/backup/apply marker anywhere, grep-
checked in a test. That isolation property is exactly what UXP-05 knowingly
removes; nothing else motivated the split.
  </context>
  <options>
    <option id="merge">
      <name>Proceed with the merge as planned (recommended)</name>
      <pros>Delivers UXP-05's actual ask — one source of truth for "here are the findings, here is what I can fix", so no two screens can report different counts for one scan. Matches the original approved mockup's shape. The user already chose this in discussion with the trade-off named in full.</pros>
      <cons>The largest single change in the phase, and the only one-way one: it un-completes a shipped requirement and rewrites the eight-function real-PTY suite. The measured blast radius is larger than the estimate the decision was originally made against.</cons>
    </option>
    <option id="banner">
      <name>Narrow to the cheaper cross-reference banner</name>
      <pros>A fraction of the work, fully reversible, touches no shipped requirement. The banner pattern already exists elsewhere in the product.</pros>
      <cons>Treats the symptom: two tabs can still report different numbers for the same scan, which is the thing the user's audit actually objected to. Explicitly rejected during discussion; offered here only so the decision is re-made with full information.</cons>
    </option>
    <option id="split">
      <name>Split Phase 9.4 — ship UXP-01..04 now, re-scope the merge into its own phase</name>
      <pros>Four of five requirements ship immediately with no one-way step. The merge gets its own phase, its own budget, and its own review cycle proportional to the measured radius.</pros>
      <cons>UXP-05 — the inconsistency the user called out most sharply — ships later. Plans 01 and 02 are discarded and re-planned.</cons>
    </option>
  </options>
  <resume-signal>Select: merge, banner, or split. If `merge`, Task 2 proceeds. If `banner` or `split`, STOP — Plans 01 and 02 are void and the orchestrator re-plans; Plans 03 and 04 stand unchanged either way. Record the choice and its date in this plan's SUMMARY.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end merged Doctor tab — one findings list, one inline fix path</name>
  <files>internal/tuikit/frame.go, internal/tuikit/app.go, internal/tuikit/doctor_screen.go, internal/tuikit/health_screen.go, internal/tuikit/fixer_screen.go, internal/tuikit/doctor_screen_test.go, internal/tuikit/health_screen_test.go, internal/tuikit/fixer_screen_test.go, internal/tuikit/doctor_test.go, internal/tuikit/app_test.go, internal/tuikit/frame_test.go, internal/tuikit/mouse_test.go, internal/tuikit/identities_test.go, internal/tuikit/gitignore_test.go, internal/tuikit/globalgit_test.go, cmd/gitid/wiring_test.go, cmd/gitid/wiring_storage_test.go</files>
  <read_first>
    - `internal/tuikit/health_screen.go` (all 221 lines) — the unfiltered `findings()` scope, `parseErrorFinding`/`parseErrorScreenView`, the informational-only detail branch, the all-green branch
    - `internal/tuikit/fixer_screen.go` (all 389 lines) — `haltBatch`, `fixableState`, the `f`/`F` cases, `handleClick`'s ceremony routing, the batch-progress line
    - `internal/tuikit/doctor.go` (all 124 lines) — the five shared helpers this task must call and must NOT edit
    - `internal/tuikit/app.go` lines 40-60, 118-180, 300-340, 440-500, 500-565, 715-738 — `keyResult`, `paletteEntries`, `App`, `checkFixBatchHalt`, the keyboard/mouse routing, `newScreens`
    - `internal/tuikit/frame.go` lines 53-85 — the `TabID` enum and both label arrays
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md` — Known Divergence #1's exact before/after enum, and the "Approved Base States" rows for Doctor
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md` — Pitfalls 1, 2, 3, 6 (this task's four failure modes)
  </read_first>
  <behavior>
    - Test 1 (the tracer's RED test, `TestDoctorMergedListShowsEveryFinding`): ONE test, ONE App, ONE tab, asserting all THREE merge properties in sequence — because no one of them alone distinguishes the merged screen from a screen that already exists. Drive an App to the Doctor tab with a fixture carrying at least one fixable and at least one non-fixable finding, then assert, without leaving that tab: (a) the rendered list contains BOTH findings' titles and the status-line count equals `len(orderedFindings(s))`; (b) with the FIXABLE row selected, pressing `f` opens the fix ceremony in the detail pane; (c) with the NON-FIXABLE row selected, pressing `f` leaves the model in browse mode — no ceremony, no `pendingFixID`. Property (a) alone is NOT a merge proof: today's Health screen already renders every finding and already reports the all-findings count (`health_screen.go`), so a test asserting only (a) would pass against the unmerged Health tab and prove nothing. The merge property is exactly the CONJUNCTION — one tab that shows everything AND fixes only what is fixable — which today no single tab can satisfy: Health shows both rows but has no fix path, Fixer has the fix path but hides the non-fixable row. Write the test so its failure message names WHICH of (a)/(b)/(c) failed.
    - Test 2 (`TestDoctorFixGateRespectsFixableBit`, deepened in Task 3): the fixable/non-fixable gate under `F` (Fix-all) as well as `f`, on a fixture whose findings are ALL non-fixable.
    - Test 3: `newScreens` returns exactly 5 screens, and the model at index `TabDoctor` is a `doctorModel`.
  </behavior>
  <action>
Land the merge as ONE commit. It cannot be split: Go arrays encode their
length in the type and every in-package test file references the removed
symbols, so a partial change compiles neither the package nor its test
binary. Work in this order (RESEARCH.md's Primary recommendation, and
UI-SPEC's Focal Points: land the enum FIRST, correctly, then port).

**A. `internal/tuikit/frame.go` — the enum (per D-06, using UI-SPEC Known
Divergence #1's binding 6-to-5 math, NOT REQUIREMENTS.md's stale 5-to-4).**
Replace the `TabHealth` and `TabFixer` members with a single `TabDoctor` in
`TabHealth`'s old slot (index 3, key `4`); `TabGitIgnore` shifts to index 4,
key `5`. Update `tabLabels` and `tabNavLabels` to the 5-element arrays
UI-SPEC prints verbatim. Rewrite the `TabID` doc comment: it currently
describes the six views and cites `08-01-PLAN.md` Task 1's split as the
reason Health and Fixer are separate — that rationale is now reversed, so
say so and cite this phase.

**B. `internal/tuikit/app.go` — the shell.** Every one of these is a real
break; the fourth and fifth are the silent ones:
  - `App.screens` becomes `[5]screenModel` and `newScreens` returns
    `[5]screenModel` with `newDoctorModel(b)` in slot 3 and the GitIgnore
    model in slot 4 (Pitfall 1).
  - The number-key case set drops `"6"`, becoming `"1"` through `"5"`. It
    must not stay at six: `TabID(int(key[0]-'1'))` indexes the screens array
    directly, so a stale `"6"` case is an out-of-range index at runtime.
  - `paletteEntries` loses the Fixer entry and renumbers: slot 4 becomes the
    Doctor entry, slot 5 the Global Git Ignore entry (its label's leading
    digit changes from 6 to 5). The Help entry is unchanged.
  - `checkFixBatchHalt`'s TWO type assertions (`a.screens[a.tab].(fixerModel)`
    and `prevScreen.(fixerModel)`) become the merged type. Per Pitfall 2 this
    is the highest-risk edit in the plan: a missed assertion compiles fine
    and silently turns the D-16 rollback into a no-op. Task 3 adds the guard
    that would catch it; do not rely on the compiler here.
  - `keyResult.healthIdentity` is renamed to `doctorIdentity` (planner
    resolution of RESEARCH Open Question 2), and ALL THREE routing sites go
    to `TabDoctor`: the keyboard path (~L453-467), the mouse path (~L554-561),
    and the header health-chip click (~L520). The chip site has no
    identity field to grep for, so grep for the removed enum name across the
    whole file rather than for the field (Pitfall 3).
  - The right-arrow clamp `if a.tab < TabGitIgnore` still reads correctly
    against the new enum — confirm it, do not assume it.
  - The help key map at ~L83 (`{"1-6", "Switch view: Identities / SSH / Git /
    Health / Fixer / Ignore"}`) is user-visible: update its key range and
    label list to the 5-tab shell.
  - `identities.go`'s `keyResult` producer for the `h` key must set the
    renamed field; the compiler finds it.

**C. `internal/tuikit/doctor_screen.go` — the merged model (NEW FILE).**
Planner resolution of RESEARCH Open Question 1: create a new
`doctor_screen.go` and DELETE both `health_screen.go` and `fixer_screen.go`.
RESEARCH's default was "absorb into `health_screen.go`" as the smaller diff;
that is deliberately not taken, because a file named after a concept the
phase is removing would reintroduce exactly the name-drift class UXP-05
exists to eliminate, and because `doctor.go` (shared helpers) already exists
— `doctor_screen.go` sits beside it with an obvious helpers-vs-screen
relationship. The diff size argument is weak either way since git detects the
content move as a rename.

`doctorModel` = `fixerModel`'s complete field set PLUS `healthModel`'s
`identityName` (the per-identity filter; if it is not carried over, the
identity manager's `h` deep-link silently loses its filter — Pitfall 3).
Compose the model from the two existing halves, changing behavior in exactly
one place:
  - **Data scope comes from Health, unfiltered.** Port `healthModel.findings`
    verbatim (`orderedFindings(s)`, narrowed by `FindingsFor` only when
    `identityName` is set). `fixableState` is DELETED, not ported — its
    `fixableFindings(orderedFindings(s))` narrowing IS the count-parity bug
    UXP-05 exists to fix, and every handler in the ported Fixer code
    currently opens with `s := fixableState(rawState)`. Every one of those
    call sites must read the unfiltered scope instead.
  - **Ceremony and batch machinery come from Fixer, verbatim.** `haltBatch`,
    `fixCeremonyFor`, `doctorBatch`, the `f`/`F` cases, the batch-progress
    line, the ceremony click routing, `dimPane` during a fix, and
    `capturesKeys: m.fixing` all port unchanged (CONTEXT.md's Claude's
    Discretion asks explicitly for reuse-as-is; re-deriving risks losing
    D-16's queue-halt correctness, which cost a dedicated real-`chflags`
    failure-injection PTY test to prove).
  - **The `f`/`F` gate does NOT widen with the list.** `f` keeps
    `if ok && sel.Fixable`, and `F` keeps building its queue from
    `fixableFindings(ordered)`. The list widens; the action gate does not.
    This is the whole point: visibility without a false action.
  - **The detail pane branches on `sel.Fixable`.** Fixable renders Fixer's
    existing "Suggested fix" + fix-action line; non-fixable renders Health's
    existing informational-only line. Both branches already exist verbatim in
    the two files being merged — port, do not rewrite.
  - **The list row's sub-line uses Health's `fixNote`** (`fixable` /
    `info only`), not Fixer's unconditional `fixable` literal, since the list
    now carries both kinds.
  - Port `parseErrorFinding`/`parseErrorScreenView` and the scan-tick
    `activate`/`handleMsg` pair once (both files hold identical copies).
    Keep the parse-error check reading the RAW state's ordered findings, as
    `fixerModel.view` already does.
  - Keep the shared all-green branch as-is (both files already render the
    identical `FixerNothingToFixSSH`/`FixerNothingToFixGit` pair).
  - Call `doctor.go`'s helpers; do not inline a second sort, group, or
    filter pass anywhere. CONTEXT.md names this as the traceability
    guarantee: a re-introduced second filtered copy resurrects the bug.
  - Leave the status-line strings on Fixer's existing wording for now and
    let Plan 02 Task 1 replace them with UI-SPEC's merged copy — this task's
    scope is structural, and splitting the copy out keeps this already-large
    commit reviewable.
  - Do NOT edit `internal/tuikit/doctor.go`. Zero lines change there.

**D. Test call sites (same commit — the test binary will not compile
otherwise).** Merge `health_screen_test.go` and `fixer_screen_test.go` into a
new `doctor_screen_test.go`, preserving every existing assertion's intent;
delete the two old files. In `doctor_test.go`, retarget `doctorApp` at the
merged tab and DELETE `fixerApp` outright, re-pointing its callers at
`doctorApp` (Pitfall 6: `fixerApp` presses the literal key `"5"`, which now
belongs to Global Git Ignore — preserving it as an alias would silently open
the wrong screen and fail with assertions about unrelated content, whereas
deleting it makes the compiler find every caller). Retarget `docModel` to
return the merged type. Then fix the remaining referencing tests:
`app_test.go` (the `TabID` inventory list and `newScreens`' exhaustive
type table), `frame_test.go` (the label map and the two `len(tabNavLabels)`
loops), `mouse_test.go`, `identities_test.go` (the `h`-key deep-link's
per-identity assertion — keep it asserting the filter still applies),
`gitignore_test.go` (its key-`6` and right-arrow-from-Fixer navigation
assertions both move down one slot), `globalgit_test.go`,
`cmd/gitid/wiring_test.go` and `cmd/gitid/wiring_storage_test.go` (both
enumerate the tab set).

Do not touch `internal/screenshot`, `e2e/`, `Makefile`, `internal/dummytui`,
`internal/doctor`, or any user-visible copy beyond the four `app.go`/`frame.go`
label sites named above — those are Plan 02's scope, and this commit is
already at the buildable-boundary limit CLAUDE.md's commit-granularity rule
allows.

**`identityName`'s reset path (cross-AI review, cycle 3).** Porting
`healthModel.identityName` verbatim (per section C above) also ports a real
bug: `activate()` never clears it, and `setTab` is the single choke point
every entry route passes through — the two `h`-deep-link sites (`app.go:456,
557`, both PRE-set `a.screens[TabHealth].identityName` before calling
`a.setTab(TabHealth)`) AND every ordinary entry route (the numeric-key switch
at `app.go:475`, the palette match at `app.go:434`, the header-chip click at
`app.go:517`, and the left/right-arrow adjacency at `app.go:484,490`) all
call the SAME `setTab`. Today nothing clears `identityName` on the ordinary
routes, so a deep-link filter set once silently survives every later plain
tab switch back into Doctor — a stale, invisible filter that makes the
findings list quietly incomplete with no error and no indicator.

Fix by giving `setTab` a filter-aware sibling rather than special-casing
`TabDoctor` inline: add `setTabFiltered(t TabID, identityName string) (App,
tea.Cmd)` that sets the identity on the target screen (when it implements an
`identityFiltered` interface — or, if you judge a type assertion against
`doctorModel` directly is simpler given there is exactly one screen with this
field, do that instead and say which you chose in the SUMMARY) and then calls
the existing `setTab`. Have the two deep-link sites (`app.go:456-461,
557-560`) call `setTabFiltered(TabDoctor, res.healthIdentity)` instead of
manually mutating `a.screens[...]` before `setTab`. Change plain `setTab`
itself to unconditionally clear the identity filter on the model it is
activating (when that model carries one) BEFORE calling `activate` — so
every ordinary route (which never goes through `setTabFiltered`) always
lands with a clean filter, and the deep-link route's filter survives exactly
because it takes the sibling path that sets it AFTER the clear.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... ./cmd/gitid/... && go build ./... && make lint</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 and `make lint` reports 0 issues.
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... ./cmd/gitid/...` passes with no `t.Skip` added anywhere.
    - The RED test was observed failing BEFORE the implementation and its failure output is quoted in the SUMMARY (TDD mode; project precedent CR-17 requires red/green claims be evidenced, not asserted). RED and GREEN are two states of ONE task's internal work — the commit that lands is GREEN. Do not commit the red state (CLAUDE.md: every commit must compile and pass the pre-commit hooks; TDD drives authoring order, not commit history).
    - `TestDoctorMergedListShowsEveryFinding` asserts all three merge properties (all findings listed, fixable opens the ceremony, non-fixable does not) inside a SINGLE test function against a single tab; the SUMMARY quotes the assertion block showing the conjunction. A version asserting only list membership/count is rejected — Health already satisfies that today.
    - `internal/tuikit/health_screen.go` and `internal/tuikit/fixer_screen.go` no longer exist; `internal/tuikit/doctor_screen.go` does.
    - `git diff --stat internal/tuikit/doctor.go` is empty — the shared helpers file is untouched.
    - `rg -c 'TabHealth|TabFixer|healthModel|fixerModel|healthIdentity|fixableState' internal/tuikit cmd/gitid` returns no matches outside comment prose that Plan 02 will rewrite.
    - `sed -n '/^var paletteEntries/,/^}/p' internal/tuikit/app.go | grep -v 'help:' | grep -c 'tab: Tab'` returns 5. (The `help:` exclusion and the typed-value anchor are deliberate: a bare `grep -c` on the field name alone would over-count if a future Help entry ever gained a tab target in the same slice literal — REVIEWS.md cycle-2 LOW. Today's Help entry carries no tab field, so both forms agree at 5; the tightened form is the one that stays correct.)
    - `sed -n '/^func newScreens/,/^}/p' internal/tuikit/app.go | grep -c 'new.*Model('` returns 5.
    - `sed -n '/func (a \*App) checkFixBatchHalt/,/^}/p' internal/tuikit/app.go | grep -v '^\s*//' | grep -c 'doctorModel'` returns 2 — both assertion sites moved together (Pitfall 2).
    - `go doc ./internal/tuikit TabDoctor` resolves, and the test asserting `newScreens` returns exactly 5 screens passes.
    - The identity-manager `h`-key test still asserts the merged model's `identityName` is set to the selected identity.
    - **New test proves the filter resets on ordinary re-entry (cross-AI review, cycle 3):** deep-link into Doctor via `h` on one identity (filter set), leave Doctor for any other tab, re-enter Doctor via an ORDINARY route (a numeric key press or the header-chip click — not another `h` deep-link), and assert the findings list is once again unfiltered (shows every identity's findings, not just the previously-selected one). Name this test `TestDoctorIdentityFilterResetsOnOrdinaryReentry` or equivalent and quote its pass output in the SUMMARY.
  </acceptance_criteria>
  <done>One top-level tab renders every finding with the inline fix path; the package and its whole test suite compile and pass under `-race`; the five silent-failure sites Pitfalls 1/2/3/6 name are all confirmed moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression guards for the three failure modes the compiler cannot see</name>
  <files>internal/tuikit/doctor_screen_test.go</files>
  <read_first>
    - `internal/tuikit/doctor_screen.go` (as written by Task 2)
    - `internal/tuikit/app.go`'s `checkFixBatchHalt` (as written by Task 2)
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md` — Security Domain's two Known Threat Patterns, and Pitfall 2's "Warning signs" paragraph
    - `internal/tuikit/doctor_test.go` — the existing `stubBackend`/`press`/`appView` helpers these guards build on
  </read_first>
  <behavior>
    - `TestDoctorBatchHaltSurvivesModelRename` (T-09.4-01): drive a Fix-all batch, force `Backend.PersistError()` to return non-empty on the second fix, and assert the rendered detail pane CONTAINS the D-16 halt message naming the failed fix's ordinal and the rollback. Assert positively on the message text, never merely "no panic" — Pitfall 2's stated warning sign is a test that passes for the wrong reason because the type assertion early-returned.
    - `TestDoctorFixGateRespectsFixableBit` (T-09.4-02): with a non-fixable finding selected, `f` leaves `fixing` false and `pendingFixID` empty; `F` on a fixture whose findings are ALL non-fixable starts no batch. The rendered list still contains the non-fixable finding's title in both cases — the guard proves the list widened while the action gate did not.
    - `TestDoctorCountIsTheOnlyFindingCount` (UXP-05's literal ask): the number the Doctor status line reports equals `len(orderedFindings(state))`, and is strictly greater than `len(fixableFindings(orderedFindings(state)))` for a fixture containing at least one info-only finding — the two numbers that used to disagree across two tabs now come from one source and one is provably the superset.
  </behavior>
  <action>
Add the three guards as a separate commit from Task 2 (they are purely
additive and compile against the merged code, so the buildable boundary
permits the split and it keeps Task 2's large diff reviewable).

For each guard, prove RED by source reversion before claiming green, and
record the reverted line plus the observed failure in the SUMMARY. Do the
reversion, observe the failure, restore, and commit only the GREEN state —
the reversion is scratch work inside this task, never a committed state
(CLAUDE.md's buildable-commit rule).

`TestDoctorBatchHaltSurvivesModelRename` in particular must be shown to FAIL
through the SILENT path the threat actually takes. **Revert
`checkFixBatchHalt`'s assertion to a wrong-but-COMPILING concrete type — use
`globalGitModel`, which is a real `screenModel` implementation in this same
package — not to `fixerModel`.** `fixerModel` no longer exists after Task 2,
so asserting to it produces a COMPILE error, and a compile error proves
nothing about this guard: the whole hazard (RESEARCH Pitfall 2) is that a
stale assertion compiles fine and fails to `ok == false` at RUNTIME, silently
early-returning and turning the D-16 rollback into a no-op. Asserting to
`globalGitModel` compiles, is never the model on the Doctor tab, and
therefore drives exactly that `ok == false` branch. If the test still passes
under that reversion, the test is asserting the wrong thing — fix the test,
not the reversion.

Reuse the existing `stubBackend` seam for the `PersistError` injection rather
than adding a new backend implementation; extend it if it does not already
support a per-call failure, and keep the extension behind a zero-value
default so no existing test changes behavior.

Wire each guard's threat identifier into its doc comment so
`/gsd-secure-phase` can trace it later.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run 'TestDoctor'</automated>
  </verify>
  <acceptance_criteria>
    - All three named test functions exist in `internal/tuikit/doctor_screen_test.go` and pass under `-race`.
    - Each guard's RED state was produced by reverting a specific named line of production source; the SUMMARY quotes the reverted line and the resulting failure message for all three. Each reversion COMPILED — the SUMMARY states this explicitly for `TestDoctorBatchHaltSurvivesModelRename`, whose reversion target is `globalGitModel` (a wrong-but-compiling concrete type), so the observed failure is the runtime `ok == false` path and not a build break.
    - `TestDoctorBatchHaltSurvivesModelRename` asserts on the halt message's TEXT (a positive `strings.Contains` on the rendered pane), not on the absence of a panic.
    - Each test's doc comment names its threat ID (`T-09.4-01`, `T-09.4-02`) or the requirement (`UXP-05`).
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` is green across the whole module.
  </acceptance_criteria>
  <done>The two Tampering threats and the count-parity requirement each have an automated guard that has been demonstrated to fail when its mitigation is removed.</done>
</task>

<task type="auto">
  <name>Task 4: Close this wave green — retarget every runtime-only surface's navigation and labels to the merged tab</name>
  <files>e2e/health_fixer_pty_e2e_test.go, e2e/ui_pty_e2e_test.go, e2e/dummy_demo_e2e_test.go, e2e/global_git_pty_e2e_test.go, e2e/git_configuration_pty_e2e_test.go, e2e/identity_manager_pty_e2e_test.go, e2e/create_flow_pty_e2e_test.go, internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, .planning/design/health-fixer/visual-divergence-allowlist.txt</files>
  <read_first>
    - `e2e/health_fixer_pty_e2e_test.go` — the two navigation helpers around lines 105-140 that send the literal tab keys, and every assertion in the eight test functions that names a removed screen by its rendered title, breadcrumb, or status line (note `:112` and `:118` both assert a removed screen's status wording)
    - `e2e/health_fixer_pty_e2e_test.go` lines 310-325 — the TWO existing `t.Skipf` calls (chflags is macOS/BSD-only; chflags not in PATH). These are legitimate portability skips and MUST be preserved verbatim.
    - `e2e/ui_pty_e2e_test.go` around lines 435-445 — the nav-tab label slice the real-shell smoke test iterates, and the comment above it citing `08-01-PLAN.md` Task 1's split as the reason the labels are what they are
    - `e2e/dummy_demo_e2e_test.go` around lines 205-245 — the launch header-label assertions, the per-tab keystroke walk for keys 4/5/6 with its breadcrumb assertions, and the tab-4 body-specific status assertion; and around lines 330-375 — the real SGR mouse click on the header tab, which searches the decoded frame for the bracketed label literal and `t.Fatalf`s if it never renders
    - `e2e/global_git_pty_e2e_test.go` around line 236 — the right-arrow-leaves-Global-Git assertion, which names the tab to the right of Global Git by its short nav label
    - `e2e/git_configuration_pty_e2e_test.go` lines 595-606 (the header-status extractor) and 890-900 (`compareGitScreenCheckpoint`'s per-region loop, in particular the both-sides-empty `continue`)
    - `e2e/identity_manager_pty_e2e_test.go` lines 1457-1468 (the header-status extractor), 1802-1806 (`errorRecorder`, which already exposes `Fatalf`), and 1829-1840 (the per-region loop)
    - `e2e/create_flow_pty_e2e_test.go` lines 665-680 and 765-775 — the THIRD caller of the identity-manager region extractor, which inherits the same failure and which the cross-AI review did not name
    - `internal/tuikit/frame.go` as written by Task 2 — `tabNavLabels`' new member set and `headerTabText`'s ` [N] Label ` format, so every label/keystroke retarget below uses the SHIPPED values rather than assumed ones
    - `internal/screenshot/createflow.go` lines 2072-2135 — `CaptureHealthFixerScreens`, its per-frame navigation, and its positive "the ceremony frame differs from the list frame" assertion
    - `internal/screenshot/createflow_regions.go` lines 541-556 and 800-816 — `extractHeader` and `extractHeaderStatus` each anchor on `strings.LastIndex(plain, "Fixer ")` and degrade to an empty/truncated region on a miss. This is the SAME vacuous-pass shape section D already fixes in the two e2e header extractors, in a third file the cross-AI review did not name. The Doctor visual-gate registry's identity-count disposition (`internal/screenshot/createflow.go:2189`) consumes `RegionHeader`/`RegionHeaderStatus`, so an un-fixed miss here means the wave-1 visual gate can report green having compared nothing for that region.
    - `.planning/design/health-fixer/visual-divergence-allowlist.txt` line 100 — the ceremony breadcrumb disposition, whose predicate needle names the removed tab (this is the ONE allowlist edit this task is permitted to make; see the action's exception clause)
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md` — Pitfall 7 (raw keystrokes are bytes, not symbols; no rename tool and no compiler reaches them)
  </read_first>
  <precondition>`make build` succeeds and produces a `gitid` binary — the PTY suite drives the COMPILED binary, so Task 2's package-level green does not imply this suite can run.</precondition>
  <action>
This task exists because of a cross-AI review finding: the previous draft of
this plan closed Wave 1 with `make test-e2e` and `make gate-visual-regression`
knowingly RED and handed them to Plan 02. ONESHOT.md Rule 6 requires every
wave to close with those gates green, so the minimum work to get there moves
HERE. Codex's own recommendation was "move the E2E navigation rewrite and
minimum screenshot-registry correction into Plan 01".

Neither surface is reachable by the compiler: the PTY suite sends raw
keystroke BYTES to the compiled binary, and the capture driver navigates by
key string. Both go green through `go build`, `go vet`, and `go test ./...`
while being semantically broken. Only running them catches it.

**Scope fence — this task is NAVIGATION, LABEL LITERALS, AND EXTRACTOR
ANCHORS ONLY.** Do NOT rename the file, the test functions,
`CaptureHealthFixerScreens`, `healthFixerSpecs`, the `Region*` constants, the
`ScreenID` values, or the gate test; do NOT strengthen or add behaviors; do
NOT edit any allowlist file EXCEPT under section E's single named exception.
All of that is Plan 02's scope and doing it here would produce a commit far
past the buildable-boundary limit CLAUDE.md's granularity rule allows. If a
change is not required to make one of the two gates go green — genuinely
green, not vacuously green — it does not belong in this task.

**A. `e2e/health_fixer_pty_e2e_test.go` — retarget the navigation.** Today
the suite presses one key number to reach the read-only list and a DIFFERENT
key number to reach the fix screen. After the merge there is one key and the
fix keys act IN PLACE. Update the two navigation helpers so every test
reaches the merged tab with one key press and drives `f`/`F` without a second
tab switch. Then fix the assertions that name a removed screen by its
rendered title, breadcrumb, or status text — grep the file for the removed
names rather than working only from the list above. All EIGHT behaviors keep
their current function names and their current strength; Plan 02 renames the
file and strengthens behaviors 7 and 8.

Preserve both existing `t.Skipf` calls exactly as they are. They are genuine
platform-portability guards for the `chflags uchg` write-failure injection
(macOS/BSD-only), and the unit-level `TestBatchWalkHalt` they name covers the
same property on every OS. Adding a skip is forbidden; removing these two is
also forbidden.

**B. `internal/screenshot/createflow.go` — retarget the capture driver's
navigation.** `CaptureHealthFixerScreens` boots one key number for the
read-only frames and another for the fix frame. Point every frame at the
merged tab's single key and reach the ceremony by pressing the fix key in
place. Keep the existing second-row selection choice and its rationale
comment (the dummy fixture's first row wraps a timestamped backup path across
two lines, which the timestamp normalizer cannot handle). Keep the positive
assertion that the ceremony frame differs from the list frame — it is what
catches a fixture bug that leaves the ceremony unopened.

Do not edit the specs' `ScreenID`s, `Interaction` strings, or region
dispositions. For the allowlist, see section E — it is not a blanket "do not
touch" any more.

**C. The four OTHER e2e files that assert the removed labels — loud
failures, and they break `make test-e2e` the moment Task 2 lands.** A
cross-AI review found these outside every plan's declared file set. They are
this task's problem, not Plan 02's, because each one goes RED at THIS wave and
this task's entire purpose is that this wave closes green. In every case, take
the replacement label and key number from `tabNavLabels`/`headerTabText` as
Task 2 actually shipped them, not from this plan's prose:

  - `e2e/ui_pty_e2e_test.go` — the real-shell smoke test iterates a literal
    slice of every nav-tab label and `t.Fatalf`s on any that never renders.
    Drop the two removed labels, add the merged one, and rewrite the comment
    above it: it currently cites Phase 8's split as the standing rationale for
    the label set, which this phase reverses.
  - `e2e/dummy_demo_e2e_test.go` — four distinct classes of breakage in one
    file. (1) The launch header assertions name a removed label with its
    bracketed key number, and separately assert the bracketed key `6` renders
    — key 6 no longer exists. (2) The per-tab keystroke walk presses 4, 5 and
    6 and asserts a breadcrumb for each; after the merge, key 5 reaches Git
    Ignore and key 6 is inert, so the 5-expects-the-old-fix-screen and
    6-expects-Git-Ignore pair collapses into one assertion on key 5, and the
    key-6 press must be removed rather than re-pointed. (3) The tab-4
    body-specific assertion deliberately avoids the always-present header
    label by asserting the screen's post-scan STATUS line instead — and that
    status line is one of the two Task 2 collapses, so re-point it at whatever
    the merged screen actually renders AT THIS WAVE (Task 2 keeps the ported
    ceremony screen's wording; Plan 02 Task 1 later replaces it with UI-SPEC's
    merged copy, at which point this assertion moves a second time — note that
    hand-off explicitly in the SUMMARY so Plan 02 does not have to rediscover
    it). (4) The real SGR mouse-click test searches the decoded frame for the
    bracketed header label literal to derive its click column, then asserts on
    the resulting body; both the search literal and the body assertion carry
    the removed name.
  - `e2e/global_git_pty_e2e_test.go` — one assertion proves a plain right
    arrow leaves a failed Global Git screen by naming the tab immediately to
    its right. That tab is now the merged one. This file is otherwise Plan
    03's; touch ONLY this assertion here and say so in the SUMMARY, so Plan 03
    Task 4's additions compose with it rather than collide.

Grep the whole `e2e/` tree for the removed names before editing and work that
result list — the four files above are what planning found, not a guarantee of
completeness. `e2e/health_fix_cli_e2e_test.go`'s hits are the CLI's own verb
and helper names, which are deliberately NOT renamed (see this plan's
`planner_assumptions`); leave them alone and list them in the SUMMARY as
justified survivors.

**D. The FOUR header-status extractors that fail SILENTLY — the real
test-integrity bug in this set.** `e2e/git_configuration_pty_e2e_test.go` and
`e2e/identity_manager_pty_e2e_test.go` each define a header-status region
extractor that locates the header line's trailing status chip by searching for
the LAST nav-tab label and slicing after it, returning the empty string when
that search finds nothing. After the merge the searched-for label is gone from
`tabNavLabels`, so BOTH extractors return empty on BOTH the real and the dummy
frame. Their callers (`compareGitScreenCheckpoint`,
`compareIdentManagerCheckpointSkipping`, and — a third caller the review did
not name — the identity-manager region comparison inside
`e2e/create_flow_pty_e2e_test.go`) all open their per-region loop with a
"both sides empty, region not applicable to this checkpoint" `continue`. So
the header-status region silently drops out of three gates, never increments
their `comparable` counter, and every one of them reports green having
compared nothing. That is an unearned green of exactly the class this phase
exists to eliminate, and it is invisible to `go build`, `go vet`, and the
test's own PASS line.

A cross-AI review found a FOURTH instance of the identical shape in
`internal/screenshot/createflow_regions.go`'s `extractHeader` (lines
541-556) and `extractHeaderStatus` (lines 800-816) — both anchor on
`strings.LastIndex(plain, "Fixer ")` and degrade to an empty or truncated
region on a miss, feeding the Doctor visual-gate registry's identity-count
disposition. Fix this pair on the SAME two axes as the e2e pair below, in the
same task, since it is the identical bug class in a file already in scope
here via `internal/screenshot/createflow.go`'s neighborhood.

Fix ALL FOUR extractors (the two e2e ones plus these two) on two axes; neither
axis alone is sufficient:

  1. **Re-anchor so the extracted content keeps the same shape.** Point each
     extractor's search at the merged tab's SHORT nav label. Chosen
     deliberately over the alternative of anchoring on the genuinely-last nav
     label: both extractors today anchor on the second-to-last label (the
     Git Ignore tab was appended after they were written), so the region they
     actually return is "one trailing nav segment plus the chip". Anchoring on
     the merged tab's label preserves that exact shape — one trailing nav
     segment plus the chip — so the `contains`-style predicates in the
     git-configuration and identity-manager allowlists continue to hold and
     NO allowlist edit is owed on either of those two surfaces. Anchoring on
     the last label instead would narrow the region to the bare chip and churn
     both allowlists for no benefit. Verify the preserved shape by reading a
     captured frame, not by reasoning about it.
  2. **Make the miss path loud.** Replace each extractor's empty-string
     miss-return with a hard failure naming the region, the missing anchor,
     and the header line it searched. For the two `e2e/` extractors this is a
     hard TEST failure: thread the test handle into the extractor and its
     dispatch function; the identity-manager side already has an
     `errorRecorder` interface exposing `Fatalf`, and the git-configuration
     side's comparison already takes `*testing.T` — so both are a signature
     change, not a new mechanism. Update all three dispatch call sites. For
     `internal/screenshot/createflow_regions.go`'s `extractHeader` and
     `extractHeaderStatus` — LIBRARY code with no `*testing.T` available,
     called from `ExtractRegion` and, transitively, the visual-gate registry
     — the loud equivalent is `panic()` naming the same three facts (region,
     missing anchor, header line searched), not a silent return. This package
     is build/test tooling only (never shipped in the `gitid` binary's
     runtime paths, per this project's Technology Stack table), so a panic on
     a malformed capture is the correct fail-loud behavior, not a production
     crash risk. After this change, a future rename that strands any of the
     four anchors is a red build or a panic, never a skipped region.

Then PROVE the loud path works, because a guard that has never been observed
failing is indistinguishable from no guard: temporarily point one extractor's
anchor at a label that does not exist, run that file's gate test (for the two
e2e extractors) or `make gate-visual-regression` (for the two
`createflow_regions.go` extractors), observe the hard failure, restore, and
quote both states in the SUMMARY. This is scratch work inside the task —
commit only the restored state.

**E. The allowlist: one narrowly-bounded exception to the hard STOP.** The
merge changes what BOTH the real and the dummy backend render through the same
shared `tuikit` code, so the real-versus-dummy divergence SET the allowlist
records genuinely should not move, and the hard STOP still governs the file.
There is exactly ONE known exception, and a cross-AI review was right that the
previous blanket STOP would have fired on it and blocked this task from
delivering the very green close it exists to provide: the allowlist's final
entry is the fix ceremony's BREADCRUMB disposition, and its predicate needle
is the removed tab's name joined to the ceremony crumb. The merged screen's
breadcrumb cannot contain it, so the predicate fails on both sides and the
gate goes RED for a NEEDLE reason while the divergence set is unchanged.

You are permitted to correct THAT ONE NEEDLE — replacing only the removed tab
name inside it with the merged tab's name, leaving the entry's ID, screen ID,
region, classification, severity, and rationale text byte-identical. Confirm
against the rendered frame that the corrected needle actually holds on both
sides. Everything else in the file remains under the hard STOP: if the gate
demands any other allowlist change — a different entry, a screen ID, a
disposition, an added or removed line — STOP and record it in the SUMMARY
rather than editing it here. That is Plan 02 Task 2's
re-derive-do-not-hand-patch procedure and it must not be pre-empted by a hand
patch. Run the gate FIRST and record its pre-edit output, so the SUMMARY shows
the needle correction was demanded by an observed failure rather than
pre-emptively applied.
  </action>
  <verify>
    <automated>make build &amp;&amp; make test-e2e &amp;&amp; make gate-visual-regression</automated>
  </verify>
  <acceptance_criteria>
    - `make test-e2e` passes. All eight test functions in `e2e/health_fixer_pty_e2e_test.go` run and pass; the SUMMARY quotes the eight `--- PASS` lines.
    - `make gate-visual-regression` passes, and its `-v` output shows the Health/Fixer gate tests actually executing (quote the run lines).
    - No new skip: `rg -c 't\.Skip' e2e/health_fixer_pty_e2e_test.go` returns exactly 2, and both are the pre-existing `chflags` portability skips quoted verbatim in the SUMMARY.
    - The scope fence held: `rg -c 'CaptureHealthFixerScreens|healthFixerSpecs|RegionHealthBody|RegionFixerBody|RegionFixerCeremony' internal/screenshot cmd/gitid` is UNCHANGED from its pre-task value (record both numbers).
    - `git diff --stat e2e/` touches ONLY the seven `e2e/` files this task declares in `<files>`, and every one of the seven is accounted for in the SUMMARY with the reason it changed. (The previous draft required this diff to touch a single file; a cross-AI review proved that impossible — six further e2e files depend on the removed labels, three of them loudly.) Separately, `git diff --stat internal/screenshot/` touches only `createflow.go` and `createflow_regions.go` — the second cross-AI review found a fourth silently-degrading extractor pair living there, outside every prior plan's declared file set.
    - No live surface still asserts a removed nav-tab label: `rg -c 'TabHealth|TabFixer' e2e/` returns 0, and every surviving hit of the two bare words in `e2e/` is listed in the SUMMARY with a one-line justification (the expected survivors are `e2e/health_fix_cli_e2e_test.go`'s CLI verb and helper names, which this phase deliberately does not rename, and dated Phase-8 historical citations in comments).
    - **None of the four header-status extractors can degrade to a vacuous pass.** `rg -c 'LastIndex\(.*"Fixer"\)' e2e/ internal/screenshot/` returns 0. In `e2e/git_configuration_pty_e2e_test.go` and `e2e/identity_manager_pty_e2e_test.go`, the header-status extractor's anchor-miss branch calls a hard-failure method rather than returning the empty string. In `internal/screenshot/createflow_regions.go`, `extractHeader` and `extractHeaderStatus`'s anchor-miss branches `panic()` rather than returning `""`/a truncated region. Quote all four branches in the SUMMARY.
    - **The loud path was demonstrated for both mechanisms, not assumed.** One `e2e/` extractor's anchor and one `createflow_regions.go` extractor's anchor were each temporarily pointed at a nonexistent label; the resulting test failure and the resulting panic were both observed, and all four states (2 failures + 2 restorations) are quoted in the SUMMARY. Only the restored state is committed.
    - **The extracted region's shape was preserved, so no third-party allowlist churn was needed:** `git diff --stat .planning/design/git-screen/visual-divergence-allowlist.txt .planning/design/identity-manager/visual-divergence-allowlist.txt .planning/design/create-flow/visual-divergence-allowlist.txt` is EMPTY, and the SUMMARY quotes the header line from a captured frame showing the re-anchored extraction still yields one trailing nav segment plus the chip. A non-empty diff on any of these three means the re-anchor narrowed or widened the region — STOP and re-read the action's section D, item 1.
    - **The health-fixer allowlist changed by AT MOST the one permitted needle:** `git diff --numstat .planning/design/health-fixer/visual-divergence-allowlist.txt` shows either `0 0` (no change) or exactly `1 1` (the single ceremony-breadcrumb needle correction). Any other shape is a scope-fence violation and must STOP per the action's section E. If the file changed, the SUMMARY quotes the gate's PRE-edit RED output that demanded it, plus the before/after line, showing the entry's ID, screen ID, region, classification and rationale are byte-identical.
    - `wc -l .planning/design/health-fixer/visual-divergence-allowlist.txt` is unchanged from its pre-task value of 100 — the permitted exception is a needle correction, never an added or removed entry.
    - The full wave-close battery is green and quoted in the SUMMARY: `make test`, `make lint`, `make test-e2e`, `make gate-visual-regression`.
  </acceptance_criteria>
  <done>Every gate this project runs at wave close is green with the merged tab shipped, and that green is EARNED — no e2e region comparison silently skipped itself, and the two extractors that could have degraded to a vacuous pass now fail loudly and have been observed doing so. Nothing is knowingly handed to Plan 02 in a failing state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI keypress -> fix ceremony -> `Backend.Persist` | the only boundary in this plan where a user action reaches a real write to `~/.ssh/config` / `~/.gitconfig` / fragments |
| `DemoState.Findings` (produced by `internal/doctor`) -> the merged screen model | untrusted-in-shape data crosses here; the screen decides which rows offer a write action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.4-01 | Tampering | `App.checkFixBatchHalt` (`app.go`) | high | mitigate | The merged model's concrete type replaces `fixerModel` at BOTH assertion sites in the same commit as the rename (Task 2), and Task 3's `TestDoctorBatchHaltSurvivesModelRename` asserts positively on the D-16 halt message text after an injected `PersistError`. Without this, a failed mid-batch fix stops halting and the user is told nothing was rolled back when it was — a silent no-op with no compile error. |
| T-09.4-02 | Tampering | the merged model's `f`/`F` handlers (`doctor_screen.go`) | high | mitigate | The list scope widens to `orderedFindings` but the action gate stays `sel.Fixable` / `fixableFindings(ordered)`, exactly as `fixer_screen.go` gates it today. Task 3's `TestDoctorFixGateRespectsFixableBit` proves a non-fixable selection opens no ceremony. Without this, Phase 8's CR-01 regresses: a report-only finding renders a full preview/confirm/backup ceremony and a fabricated backup path for a fix that never runs. |
| T-09.4-03 | Tampering | the ported fix ceremony (`doctor_screen.go`) | high | mitigate | `haltBatch`, `fixCeremonyFor`, `doctorBatch`, and the ceremony key/click routing port VERBATIM from `fixer_screen.go` — Task 2 forbids re-deriving them. D-06's explicit "what this does NOT remove" is the preview + typed-confirm + backup discipline (FIX-01, CLAUDE.md's global write-safety rule); a re-implementation could drop the typed confirm while still looking correct. Plan 02's real-PTY suite is the end-to-end proof. |
| T-09.4-04 | Information Disclosure | the merged model's `identityName` filter | low | accept | If the per-identity filter is dropped, the `h` deep-link shows ALL findings instead of one identity's. Everything shown is already this user's own local machine state on a single-user CLI tool, so the impact is a UX regression, not a disclosure. Guarded anyway by the existing `identities_test.go` assertion Task 2 preserves. |
| T-09.4-07 | Repudiation | the e2e header-status region extractors (`git_configuration_pty_e2e_test.go`, `identity_manager_pty_e2e_test.go`, consumed by three comparison sites incl. `create_flow_pty_e2e_test.go`) | high | mitigate | Each extractor locates the status chip by searching the header for a nav-tab label the merge removes, and returns the empty string when the search fails. Both sides then extract empty, the comparison loop's "not applicable on either side" branch skips the region without incrementing its `comparable` counter, and three gates report green having compared nothing — an unearned green record for the phase, invisible to `go build`, `go vet`, and the tests' own PASS lines. Task 4 section D re-anchors both extractors on a label the merge keeps (preserving the extracted region's shape so no third-party allowlist churns) AND replaces the empty-string miss-return with a hard failure, then DEMONSTRATES the loud path with a temporary bad anchor. |
| T-09.4-SC | Tampering | package-manager installs | low | accept | No `go get`, `npm`, `pip`, or `cargo` install occurs in this plan. RESEARCH.md's Package Legitimacy Audit records zero external packages for this phase; `go.mod` is not modified. Nothing to audit. |
</threat_model>

<verification>
Run in this order after Task 4, all from the repository root. **Every one of
these must be GREEN before this wave closes — no gate is deferred to a later
plan** (ONESHOT.md Non-Negotiable Rule 6; this replaces the previous draft's
knowingly-red handoff, which the cross-AI review flagged HIGH):

1. `go build ./...` — exit 0.
2. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` — green, and the
   passing-test count is not LOWER than the pre-plan baseline except for
   tests genuinely subsumed by the merge; account for any drop explicitly in
   the SUMMARY (project precedent: the Wave-2 Phase-3 test-count audit that
   caught a wholesale-archived test file).
3. `make test` — green (this is the target that also reaches
   `gate-copy-freeze`; this plan does not edit any frozen string, so a
   failure here means an unintended copy edit crept in).
4. `make lint` — 0 issues. Note that `lint` depends on `lint-tagged`, which
   runs `go vet -tags screenshot ./...` and
   `golangci-lint run --build-tags screenshot ./internal/screenshot/...`, so
   this is the gate that catches a screenshot-tagged compile break.
5. `make test-e2e` — green. Long (roughly 950 seconds under `-race` per the
   project's measured baseline); budget for it, do not shortcut to a `-run`
   filter for the closing run.
6. `make gate-visual-regression` — green.
7. `go list -deps ./internal/tuikit | grep -c 'castocolina/gitid/internal/doctor'` — 0.
   The no-backend boundary must survive the merge: the merged screen is pure
   render, exactly as both halves were.
8. `git diff --stat internal/tuikit/doctor.go` — empty.

Record real output for each; this project does not accept a PASS claim
without it.
</verification>

<success_criteria>
- The user has recorded a decision on Task 1's one-way gate.
- One tab, `TabDoctor`, lists every finding and carries the inline fix path.
- The `TabID` enum has 5 members; `App.screens` is a 5-element array; keys
  `1` through `5` reach the five tabs and key `6` is inert.
- `internal/tuikit/doctor.go` has zero changed lines.
- Three automated guards exist for T-09.4-01, T-09.4-02, and the UXP-05
  count-parity requirement, each demonstrated RED by a source reversion that
  COMPILED, so the batch-halt guard's RED travelled the runtime `ok == false`
  path rather than a build break.
- The wave closes GREEN on all four project gates: `make test`, `make lint`,
  `make test-e2e`, `make gate-visual-regression`.
- That green is EARNED: no e2e region comparison silently skipped itself. Both
  header-status extractors fail loudly on a missing anchor, and one of them was
  observed doing so.
- The health-fixer allowlist changed by at most the single permitted
  ceremony-breadcrumb needle correction, demanded by an observed gate failure
  and recorded with its before/after; its entry count is unchanged at 100.
- No commit in this plan is a red-test commit; RED is observed and recorded
  inside a task, and only the green state is committed.
</success_criteria>

<output>
Create `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-01-SUMMARY.md` when done.
Record: the Task 1 decision and its date; the RED-then-GREEN evidence for the
tracer test and all three guards (quoting the reverted lines, and stating that
each reversion compiled); the `go test` count before and after with any drop
accounted for; the eight PTY `--- PASS` lines and the visual gate's `-v` run
lines from Task 4; the Task 4 scope-fence counts before and after; and quoted
output for all eight verification commands.

Task 4 additionally records: each of the seven touched e2e files with the reason
it changed; the surviving `Health`/`Fixer` word hits in `e2e/` each with a
one-line justification; both re-anchored extractors' hard-failure branches
quoted; the observed loud-failure output from the temporary bad-anchor probe and
its restoration; the captured header line proving the re-anchored region kept its
shape; and the allowlist outcome — `git diff --numstat` on the health-fixer file
showing `0 0` or exactly `1 1`, with the gate's pre-edit RED output that demanded
the needle correction if it changed. Also record explicitly, as a hand-off to
Plan 02 Task 1, WHICH status-line assertions in `e2e/dummy_demo_e2e_test.go` and
`e2e/health_fixer_pty_e2e_test.go` were pointed at the ported ceremony screen's
interim wording and will therefore move a second time when UI-SPEC's merged copy
lands.
</output>
