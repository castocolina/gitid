---
phase: 09.4-tui-ux-consistency-doctor-fixer-parity
plan: 03
type: execute
wave: 3
depends_on: ["09.4-02"]
files_modified:
  - internal/tuikit/theme.go
  - internal/tuikit/globalssh.go
  - internal/tuikit/globalgit.go
  - internal/tuikit/design.go
  - internal/tuikit/globalssh_test.go
  - internal/tuikit/globalgit_test.go
  - internal/tuikit/batch3_test.go
  - internal/tuikit/upload_copy_test.go
  - Makefile
  - e2e/global_ssh_pty_e2e_test.go
  - e2e/global_git_pty_e2e_test.go
  - .planning/design/global-ssh/visual-divergence-allowlist.txt
  - .planning/design/global-git/visual-divergence-allowlist.txt
autonomous: true
cross_ai: true
requirements: [UXP-01, UXP-02, UXP-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Entering the Global SSH Options screen highlights the FIRST option row, every time, including on re-entry after visiting another tab — no row is special-cased (UXP-01, D-01)."
    - "Global SSH and Global Git decide initial row focus by the same rule, derived from the freshly fetched option list rather than a hardcoded key literal on either screen (UXP-01)."
    - "Every option row on both screens reserves the identical checkbox-column display width, whether the row is selectable, non-selectable, or already applied (UXP-02, D-03)."
    - "A non-selectable option row renders a faint dot placeholder in that column on BOTH screens — Global SSH no longer leaves it blank (UXP-02, D-03)."
    - "A selectable row's on/off state reads as a bracket toggle: bold green when on, faint when off, and legible under NO_COLOR because the bracket shape carries the meaning (UXP-03, D-03, UI-SPEC Known Divergence #2)."
    - "Clicking a row's toggle cell still toggles that row without moving the detail selection, on both screens — the click hit-test needle matches the glyph actually drawn (D8 checkpoint-2 contract)."
    - "A set-but-differs row states on its existing second line why applying gitid's recommendation there would be a no-op, on both screens; no third line is added (UXP-02, D-04)."
    - "The checkbox and radio glyphs used everywhere else in the app, notably throughout the identity manager, are unchanged (D-02 scope fence, UI-SPEC Known Divergence #2's explicit boundary)."
    - "UXP-01, UXP-02 and UXP-03 are each proven in the COMPILED binary through a real PTY session — first-row focus on re-entry, the dot placeholder, both bracket states, and a real SGR mouse click that toggles a row — not only through in-package render tests (ONESHOT.md Rule 8; docs/agent-instructions/product-ui.md)."
    - "Global Git's view renders a zero-row empty state instead of indexing options[selIdx] out of range when a Backend legitimately returns zero option rows with no error (WR-17 class; Global SSH's renderOptions already has this branch)."
  artifacts:
    - "internal/tuikit/theme.go: glyphToggleOn and glyphToggleOff constants with their scoping doc comment"
    - "internal/tuikit/theme.go: the shared option-row checkbox-column width constant"
    - "internal/tuikit/design.go: the two strengthened set-but-differs sentences"
  key_links:
    - "the rendered toggle glyph <-> the click hit-test needle, on BOTH screens — a needle/glyph mismatch silently kills click-to-toggle with no compile error and no failing render test"
    - "Selectable() as the SINGLE predicate gating the toggle key, the checkbox render, and the click hit-test — the merge of a second, independently-computed selectability check is the D-02 fence's failure mode"
    - "the checkbox-column width constant <-> all four render variants on both screens <-> truncLine's remaining budget for the option key name"
    - "design.go's differs sentences <-> Makefile's gate-copy-freeze entries — the gate uses substring containment, so a prefix entry would pass even if the new clause were dropped"
---

<objective>
Close UXP-01, UXP-02 and UXP-03: make Global SSH's Options screen focus its
first row on every activation, give both option screens one identical
checkbox column with a visible placeholder for non-selectable rows, replace
the faint checkbox glyph with a legible bracket toggle, and strengthen the
set-but-differs row's existing explanation so the user can see WHY that row
is locked.

Purpose: three of the five inconsistencies the user's audit found are on
these two screens, and all three are the same underlying fault — Global Git
got a fix that Global SSH never received, so the two screens teach the user
contradictory rules. This plan makes the rule shared and explicit rather than
duplicated and drifted.
Output: one focus rule, one column width, one toggle glyph pair, one
differs-state explanation — each covered by a unit test on both screens AND
proven in the compiled binary through a real PTY session.
</objective>

<review_response>
Cross-AI review (`09.4-REVIEWS.md`, Codex `gpt-5.6-sol`, REQUEST CHANGES) raised
three findings against this plan. Their disposition, recorded here so the executor
and the next reviewer can see it without reading REVIEWS.md:

| Finding | Severity | Disposition |
|---------|----------|-------------|
| UXP-01/02/03 lack compiled-binary real-PTY proof — the existing Global SSH/Git PTY tests assert names and navigation, never `[ ]`, `[✓]`, the dot placeholder, or reset-on-reentry | HIGH | **INCORPORATED.** Task 4 (NEW) adds real-PTY coverage on both screens for first-row focus after re-entry, the dot placeholder, both bracket states, NO_COLOR legibility, and a REAL SGR mouse click that toggles a row. Both e2e files added to `files_modified`. Without this the phase could pass while the actual terminal affordances stayed wrong. |
| Zero-row Global Git behavior has no implementation instruction — `view` indexes `options[selIdx]` unconditionally while `handleKey` guards `len(options)==0` | MEDIUM | **INCORPORATED.** Task 1 now carries an explicit instruction to add a zero-options branch to Global Git's `view`, modeled on Global SSH's existing WR-17 branch in `renderOptions` (`globalssh.go`), plus a behavior asserting the RENDERED empty state rather than activation alone. Verified against source during this pass: `globalgit.go`'s `handleKey` returns early on `len(options)==0`, but `view` does not, and reaches `detail := options[selIdx]`. |
| Visual-regression allowlist ownership unresolved — the verification section expects allowlist re-derivation and possible E2E assertion changes, but neither appears in `files_modified` | LOW | **INCORPORATED.** Both surface allowlists and both PTY files are now in `files_modified`, and the `<verification>` section states the ownership rule: run the gate FIRST and edit an allowlist only if the real-versus-dummy divergence SET or its predicates actually changed. The glyph and width changes live in shared `tuikit` code and render identically on both the real and dummy sides, so the expected outcome is NO allowlist change. |
</review_response>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-CONTEXT.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-RESEARCH.md
@.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md

@internal/tuikit/theme.go
</context>

<artifacts_this_phase_produces>
| Symbol | File | Kind |
|--------|------|------|
| `glyphToggleOn`, `glyphToggleOff` | `internal/tuikit/theme.go` | NEW constants, scoped by doc comment to the two option-row sites only |
| `optionBoxWidth` (or equivalent single width constant) | `internal/tuikit/theme.go` | NEW constant — the one checkbox-column width both screens pad every variant to |
| `GlobalSSHWordDiffersUser`, `GlobalSSHWordDiffersOutside` | `internal/tuikit/design.go` | EXTENDED with the no-op clause (shared by both screens; Global Git already reuses them) |
| `TestOptionRowCheckboxColumnWidthIsUniform` | `internal/tuikit/globalssh_test.go` | NEW — the D-03 alignment guard covering all variants on both screens |
| `TestGlobalSSHActivateResetsFocusToFirstRow` | `internal/tuikit/globalssh_test.go` | NEW — the UXP-01 guard |
| Global Git `view`'s zero-options branch | `internal/tuikit/globalgit.go` | NEW render-path guard (WR-17 class; `view` currently indexes `options[selIdx]` unconditionally) |
| Real-PTY coverage for focus-on-reentry, the dot placeholder, both bracket states, NO_COLOR legibility, and a raw SGR mouse toggle | `e2e/global_ssh_pty_e2e_test.go`, `e2e/global_git_pty_e2e_test.go` | NEW / EXTENDED (Task 4) |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One initial-focus rule, derived from the option list, on both screens</name>
  <files>internal/tuikit/globalssh.go, internal/tuikit/globalgit.go, internal/tuikit/globalssh_test.go, internal/tuikit/globalgit_test.go</files>
  <read_first>
    - `internal/tuikit/globalssh.go` lines 116-172 — `newGlobalSSHModel` (the construction-time default), `activate` (which never resets it), and `refetchStoragePlan`
    - `internal/tuikit/globalssh.go`'s `detailIndex` helper — note it already falls back to index 0 on a miss
    - `internal/tuikit/globalgit.go` lines 110-135 — `activate`'s existing reset, and its `gitDetailIndex` helper
    - `internal/tuikit/globalgit.go`'s `handleKey` around line 541 — its `if len(options) == 0 { return keyResult{model: m, handled: true} }` early return, which is the KEY path's guard
    - `internal/tuikit/globalgit.go`'s `view` (starts ~line 851) down through its per-row loop and `detail := options[selIdx]` (~line 968) — note there is NO zero-length guard on this path
    - `internal/tuikit/globalssh.go`'s `renderOptions` (~lines 904-916) — the WR-17 comment and the `if len(options) == 0 { return m.subTabStrip() + ... }` branch that is the precedent to model on
    - `internal/globalssh/policy.go` lines 30-40 and `internal/globalgit/policy.go` lines 120-190 — the two policy tables' declared order, which is what "the first row" resolves to
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-CONTEXT.md` — D-01 in full, including the root-cause paragraph tracing the hardcoded default to the original HTML mockup
  </read_first>
  <behavior>
    - Test 1: a freshly constructed Global SSH model, activated once, has its detail selection on the FIRST row of the fetched option list. Today it lands on a middle row.
    - Test 2: navigate the selection down two rows, leave the screen, re-activate — the selection is back on the first row. This is the half `activate()` never did, and the half a construction-time-only fix would not cover.
    - Test 3: the same two assertions hold for Global Git, whose observable outcome is unchanged (its policy table's first row is the key it currently hardcodes) but whose RULE is now the shared derived one.
    - Test 4: activating against a backend that returns zero option rows leaves the model in a renderable state and does not panic — on BOTH screens.
    - Test 5: against that same zero-row backend, calling `view` and RENDERING the body produces a non-empty empty-state message on BOTH screens. Asserting only that `activate` survived is not enough: today Global Git's key path guards `len(options)==0` but its render path does not, so the panic lives in `view`, not in `activate`, and a test that never renders would pass over the bug.
  </behavior>
  <action>
Per D-01: remove `newGlobalSSHModel`'s construction-time detail-key default
entirely — the field starts zero-valued — and make `activate()` set the
selection from the option list it has just fetched.

Derive it from data, do not substitute one hardcoded key literal for
another. D-01's binding wording is that no row is special-cased in the focus
logic; a literal naming the current first policy row would move the bug
rather than remove it, and would silently re-break the moment the policy
table is reordered. Set it from the first element of the freshly fetched
slice, guarded for the empty case (a `Backend` may legitimately return zero
rows with no error — this codebase already has a recorded incident, WR-17,
of exactly that shape panicking a renderer).

Order matters inside `activate()`: the reset must happen AFTER the fetch and
must use the fetched slice, and on a fetch error the model must not be left
pointing at a row that does not exist. Both `detailIndex` helpers already
degrade to index 0 on a miss, which is the desired fallback — rely on it
rather than adding a second guard.

Apply the same derived rule to Global Git's `activate()`, replacing its
hardcoded key literal. UXP-01's requirement text asks for "a single,
consistent rule across screens"; one screen deriving and the other hard-coding
is two rules that happen to agree today. Verify against the policy table that
the key currently hardcoded IS the first row, so the change is provably
behavior-preserving for Global Git, and record that verification in the
SUMMARY.

**Add the missing zero-row render branch to Global Git.** The empty-case guard
above protects `activate`, but Global Git's RENDER path is unguarded and is
where the panic actually lives: `handleKey` returns early on
`len(options) == 0`, while `view` walks straight through to
`detail := options[selIdx]` — and `gitDetailIndex` returns 0 on no match, which
is out of range for an empty slice. This is the same WR-17 shape Global SSH
already fixed. Add an explicit zero-options branch to Global Git's `view`,
modeled on Global SSH's `renderOptions` branch: render a short faint
empty-state line instead of the master-detail body, keeping whatever chrome
that screen already prefixes. Copy the WR-17 rationale comment's shape so the
next reader sees why the branch exists — a `Backend` implementation may
legitimately return `(nil, nil)`, zero rows with no error, which is a different
condition from the `optionsErr` fetch-failure branch that already exists.

Do NOT reach for this by making `gitDetailIndex` return -1 or by adding a
second guard inside the row loop — one explicit early branch at the top of the
render path, matching the existing precedent, is the whole fix.

Leave every other part of `activate()` alone — the storage-choice sync, the
selection-set reset (D-15), the storage plan refetch, and the fallback-author
fetch all keep their current behavior and ordering.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run 'TestGlobalSSH|TestGlobalGit'</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^func newGlobalSSHModel/,/^}/p' internal/tuikit/globalssh.go | grep -c 'detailKey'` returns 0.
    - `sed -n '/globalSSHModel) activate/,/^}/p' internal/tuikit/globalssh.go | grep -v '^\s*//' | grep -c 'detailKey'` returns 1.
    - Neither screen's `activate` contains a quoted option-key literal for the focus reset; both index the fetched slice. Verified by reading the two functions and stated in the SUMMARY.
    - All five behaviors above have a passing test, and the re-activation test (Test 2) was observed RED before the fix with its failure output quoted in the SUMMARY. RED is observed inside this task and only the GREEN state is committed (CLAUDE.md's buildable-commit rule).
    - The zero-row test both activates AND renders on both screens without a panic, and asserts a non-empty empty-state message in the rendered body.
    - The zero-row branch lives on the RENDER path, not only the key path, and carries its rationale comment: `sed -n '/func (m globalGitModel) view/,/detail := options\[selIdx\]/p' internal/tuikit/globalgit.go | grep -c 'WR-17'` returns at least 1. The SUMMARY quotes the branch as written.
    - The SUMMARY records the policy-table check proving Global Git's outcome is unchanged.
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...` and `make lint` are green.
  </acceptance_criteria>
  <done>Both option screens reset focus to the first fetched row on every activation, by one shared derived rule with no key literal on either side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One checkbox column — uniform width, dot placeholder, bracket toggle, matching click needles</name>
  <files>internal/tuikit/theme.go, internal/tuikit/globalssh.go, internal/tuikit/globalgit.go, internal/tuikit/globalssh_test.go, internal/tuikit/globalgit_test.go, internal/tuikit/batch3_test.go, internal/tuikit/upload_copy_test.go</files>
  <read_first>
    - `internal/tuikit/theme.go` lines 64-76 — the four frozen glyph constants and the D3 checkpoint-2 contract comment the new pair sits beside
    - `internal/tuikit/globalssh.go`'s `optionRow` and `clickOnCheckbox` — the three box variants (selectable, applied, neither) and the paired hit-test needle
    - `internal/tuikit/globalgit.go`'s per-row render loop around lines 920-965 and its checkbox click branch around lines 835-850 — the two box variants and the paired needle
    - `internal/tuikit/globalssh.go`'s `handleClick` checkbox branch — note it already gates on `options[row].Selectable()` BEFORE consulting the needle
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md` — Known Divergence #2 in full (including the exact doc comment it prescribes), plus the Spacing/Layout row for the checkbox column and the Typography/Color rows for the on/off styling
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-CONTEXT.md` — D-02's scope fence and D-03
    - `internal/tuikit/batch3_test.go` around line 235 and line 251 — the two mouse tests that click the checkbox cell; the first passes a RAW glyph literal, not the constant
    - `internal/tuikit/globalssh_test.go` around line 569 — the not-applicable row's negative glyph assertion
    - `internal/tuikit/upload_copy_test.go` lines 78-98 — the test asserting other screens' labels must COMPOSE the shared glyph constants rather than embed them; confirm the new pair does not disturb it
  </read_first>
  <behavior>
    - Test 1: for every combination of screen (Global SSH, Global Git) and row state (selectable-off, selectable-on, non-selectable, already-applied), the checkbox column occupies an identical display width, measured ANSI-aware. This is the D-03 requirement stated literally, and it does not hold today on EITHER screen.
    - Test 2: a non-selectable row on Global SSH renders the faint dot placeholder, matching Global Git.
    - Test 3: a selectable row renders the bracket toggle in both states, and the on state is styled with the healthy/green role while the off state is faint.
    - Test 4: with color disabled, both toggle states remain distinguishable from each other and from the placeholder — the bracket shape, not the color, carries the meaning.
    - Test 5: clicking the toggle cell toggles that row and does NOT move the detail selection, on both screens (the existing mouse tests, re-pointed).
    - Test 6: the identity manager's own checkbox and radio glyphs are unchanged — a rendered create-flow/upload frame still draws the original glyph pair.
  </behavior>
  <action>
Three coupled edits. They must land in ONE commit: a glyph swap without its
paired click-needle swap silently kills click-to-toggle, and a width change
without the padding change breaks the column the swap is supposed to align.

**A. `theme.go` — two new constants and one width constant.** Add the
bracket toggle pair exactly as UI-SPEC Known Divergence #2 prescribes,
including its doc comment recording WHY it diverges from the app's one frozen
checkbox glyph pair and WHERE it may be used. The existing four glyph
constants stay untouched and keep their 30-plus call sites; this is a scoped,
documented exception, not a replacement.

Also add ONE exported-within-package width constant for the option-row
checkbox column, with a doc comment naming D-03 as its reason. Both screens
pad every box variant to this width.

**B. The two row renderers — uniform column, all variants.** Today's four
variants have three different widths across the two screens, so D-03's
"same width regardless of selectability" is not satisfied by copying either
screen's current code — this is a genuine finding beyond what UI-SPEC's
Spacing table describes, and it means Global Git is the reference for the
PLACEHOLDER PATTERN only, not for the width. Concretely, today: Global SSH
renders three raw spaces for a non-selectable row (the alignment bug), a
narrower cell for a selectable row, and a different narrower cell again for
an already-applied row; Global Git renders a wider cell for non-selectable
than for selectable. Rewrite both so every variant is padded to the single
width constant:
  - selectable, unchosen: the off bracket
  - selectable, chosen: the on bracket, bold plus the healthy/green role
  - non-selectable: the faint dot placeholder (Global SSH gains this; Global
    Git already has the glyph but must be re-padded)
  - already applied (Global SSH only): its existing check mark, re-padded

Pad with the ANSI-aware width helper this codebase already uses for
rendering, never with `len()` on a styled string — a styled cell's byte
length is not its display width, and getting this wrong is invisible until a
capture diff.

Because the column grows, the option key name has fewer cells before
`truncLine` clips it. Check the widest key in each policy table against the
master-list width at the fixed capture geometry and confirm nothing newly
truncates; if something does, resolve it within the existing frame (this
project's standing rule — never widen the frame) and record the resolution.

**C. The click hit-test needles — same commit, both screens.** Each screen's
checkbox click helper searches the rendered body for the glyph it expects to
find. Both needles must move to the new pair. A needle/glyph mismatch
produces no compile error and no failing render test — only the mouse tests
catch it, which is why Test 5 is mandatory here rather than optional.

Keep `Selectable()` as the SINGLE predicate gating the toggle key, the
checkbox render, and the click path. Global SSH's click handler already
checks `Selectable()` before consulting the needle and Global Git's already
wraps its needle check in the same predicate; preserve both. Do not introduce
a second, independently-computed selectability test anywhere — that is
T-09.4-07's exact failure mode and D-02's scope fence depends on the single
predicate.

**D. The referencing tests.** Re-point every test that names the old glyphs
for these two screens. Note that one of the mouse tests passes a RAW glyph
literal rather than the constant, so a search for the constant name alone
will miss it — grep for the literal too. Update the not-applicable row's
negative assertion so it asserts against the NEW toggle pair and, positively,
that the placeholder IS present. Leave the test that pins other screens'
label composition alone except to confirm it still passes.

This task changes rendering and interaction only. The classifier that decides
which rows are selectable is NOT touched (D-02's scope fence): no change in
`internal/globalssh/classify.go` or `internal/globalgit/classify.go`, and no
new capability to set an arbitrary value.
  </action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run 'TestOptionRow|TestGlobalSSH|TestGlobalGit|TestMouse|TestUpload'</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/func optionRow/,/^}/p' internal/tuikit/globalssh.go | grep -v '^\s*//' | grep -c glyphToggle` returns 2.
    - `sed -n '/globalSSHModel) clickOnCheckbox/,/^}/p' internal/tuikit/globalssh.go | grep -v '^\s*//' | grep -c glyphToggle` returns 2.
    - `rg -c 'glyphToggle' internal/tuikit/globalgit.go` returns 4 — the two render sites and the two needle sites moved together.
    - `rg -c 'glyphCheckOn|glyphCheckOff' internal/tuikit/globalssh.go internal/tuikit/globalgit.go` returns 0 for both files.
    - `rg -c 'glyphCheckOn|glyphCheckOff' internal/tuikit/identities.go` is unchanged from its pre-task value — the scope fence held (record both numbers in the SUMMARY).
    - The uniform-width test covers all four variants on both screens and passes; its RED state before the padding fix is quoted in the SUMMARY.
    - Both mouse click-to-toggle tests pass, and the SUMMARY names the raw-literal site that a constant-only search would have missed.
    - The widest key in each policy table was measured against the master-list width; the SUMMARY states whether anything newly truncates and, if so, how it was resolved inside the existing frame.
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...`, `make lint`, and `make gate-visual-regression` are green.
  </acceptance_criteria>
  <done>Both option screens draw one checkbox column of one width with a visible placeholder for non-selectable rows and a bracket toggle for selectable ones; every click needle matches the glyph actually drawn; every other screen's glyphs are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The set-but-differs row explains its own no-op, on both screens</name>
  <files>internal/tuikit/design.go, internal/tuikit/globalssh_test.go, internal/tuikit/globalgit_test.go, Makefile</files>
  <read_first>
    - `internal/tuikit/design.go` lines 236-248 — the two differs sentences, and the comment recording which decision froze them
    - `internal/tuikit/globalssh.go`'s `optionRowLine2` — how the sentences are appended to the now/recommended text
    - `internal/tuikit/globalgit.go`'s row line-2 helper around lines 276-295 — it REUSES the same two constants, so one edit reaches both screens
    - `Makefile` lines 376-500 — the `gate-copy-freeze` frozen list, specifically the two differs entries, and the `grep -rqF` containment mechanism
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-UI-SPEC.md` — the Copywriting Contract's two D-04 rows (the DRAFT wording) and the Spacing row confirming no new line is added
    - `internal/tuikit/globalssh_test.go` around lines 480-510 and `internal/tuikit/globalgit_test.go` around lines 925-940 — the existing assertions on both sentences
  </read_first>
  <behavior>
    - Test 1: a Global SSH row in the set-but-differs state, attributed to the user, renders a second line that states both the current-versus-recommended values AND why applying gitid's recommendation there would change nothing.
    - Test 2: the same row attributed OUTSIDE gitid's own files renders its own distinct variant of that explanation.
    - Test 3: both assertions hold for Global Git, which reuses the same two constants.
    - Test 4: neither row grows a third line, and neither second line wraps at the fixed capture width.
  </behavior>
  <action>
Per D-04: strengthen the EXISTING second line's wording. Do not add a third
line, do not add a new constant, and do not touch the already-set,
safe-by-default, or not-applicable sentences.

Extend the two shared differs sentences in `design.go` with the no-op clause
UI-SPEC's Copywriting Contract drafts. Because Global Git already reuses
these two constants rather than declaring its own, one edit reaches both
screens — verify that reuse before editing, and record it in the SUMMARY as
the reason no Global Git source change is needed.

Keep each sentence on one rendered line at the fixed capture width. CONTEXT's
Claude's Discretion note asks for short; the row already carries the
now/recommended prefix, so measure the FULL composed line (prefix plus
sentence) rather than the sentence alone, and shorten the clause if the
composed line would wrap.

Then update the copy-freeze gate. The gate greps for fixed substrings, so the
two existing entries are PREFIXES of the new sentences and would keep passing
even if the no-op clause were dropped — a gate that cannot detect the
regression it is supposed to pin. Replace each prefix entry with the full new
sentence so the clause itself is pinned. Run the gate to confirm, do not
infer it from the diff.
  </action>
  <verify>
    <automated>make gate-copy-freeze &amp;&amp; TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./internal/tuikit/... -run 'TestGlobalSSH|TestGlobalGit'</automated>
  </verify>
  <acceptance_criteria>
    - `make gate-copy-freeze` passes, and its two differs entries are the FULL new sentences rather than the old prefixes (quote both entries in the SUMMARY).
    - All four behaviors have a passing test, on both screens.
    - `grep -c 'globalGitRowLine2' internal/tuikit/design.go` returns 0 and the SUMMARY states that Global Git needed no source change because it reuses the shared constants.
    - The composed second line for the longest differs case was measured at the fixed capture width and does not wrap; the measured width is stated in the SUMMARY.
    - No third line was added to either row renderer — the row line count is unchanged (confirmed by the existing per-row line-count constant still being correct).
    - `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...`, `make lint`, and `make gate-visual-regression` are green.
  </acceptance_criteria>
  <done>A locked, set-but-differs row tells the user on its own existing line why nothing would happen if they could act on it, identically on both screens, with the explanation pinned by the copy-freeze gate.</done>
</task>

<task type="auto">
  <name>Task 4: Prove UXP-01, UXP-02 and UXP-03 in the compiled binary through a real PTY, including a real mouse toggle</name>
  <files>e2e/global_ssh_pty_e2e_test.go, e2e/global_git_pty_e2e_test.go</files>
  <read_first>
    - `e2e/global_ssh_pty_e2e_test.go` (all of it, ~10.6K) — the existing browse coverage, `seedGlobalSSHHome`, `startGlobalSSHPTY`, and every assertion that names a glyph or a focused row
    - `e2e/global_git_pty_e2e_test.go` (all of it, ~16.6K) — the same for Global Git
    - `e2e/create_flow_pty_e2e_test.go` lines 123-146 — `clickLabelRow(t, s, label)`, the existing raw SGR mouse press/release helper. It is package-level in `package e2e`, so it is directly callable from both files above. It locates the label in the CURRENT decoded frame and derives the column/row from that frame rather than hardcoding coordinates — reuse this technique; never hardcode a cell.
    - `e2e/harness_test.go` — `ptySession`, `waitFor`, `sendKey`, `keystrokeDelay`, and the fixture-HOME setup convention
    - `internal/tuikit/theme.go` as written by Task 2 — the exact `glyphToggleOn` / `glyphToggleOff` / placeholder runes the PTY assertions must look for
    - `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-VALIDATION.md` — the Nyquist strategy's real-PTY tier
  </read_first>
  <precondition>`make build` succeeds and produces a `gitid` binary — this suite drives the COMPILED binary through a real PTY, so a green `go test ./internal/tuikit/...` does not imply it can run.</precondition>
  <action>
This task exists because of a cross-AI review HIGH: Tasks 1-3 prove UXP-01,
UXP-02 and UXP-03 only through in-package render tests, while this project's
own policy (ONESHOT.md Non-Negotiable Rule 8) requires a TUI-surface change to
be exercised through the compiled binary in a real PTY with raw keystrokes and
mouse sequences. The existing Global SSH and Global Git PTY tests assert option
NAMES and NAVIGATION; none of them asserts a focused row after re-entry, the
placeholder glyph, either bracket state, or a mouse toggle. Without this task
the phase can pass every listed gate while the actual terminal affordances stay
wrong — which is precisely the doctor-injected-seam blindspot class this
project has already hit twice.

Extend the two existing PTY files rather than creating new ones; put each
assertion in the file that owns the screen it drives. Reuse every existing
session helper and fixture-HOME setup. Cover, in the compiled binary at the
project's fixed geometry:

**A. UXP-01 — first-row focus, including on RE-ENTRY (both screens).** Reach
the screen, assert the FIRST option row is the focused one in the raw rendered
frame. Then navigate the selection down two rows, switch to another main tab,
switch back, and assert the focus is on the first row again. The re-entry half
is the half a construction-time-only fix would not cover and is the half no
existing PTY test touches.

**B. UXP-02 — the dot placeholder and the uniform column (both screens).**
Assert a NON-SELECTABLE row's rendered line carries the faint dot placeholder
in the checkbox column, on BOTH screens — Global SSH is the one that renders
blank today. Assert the checkbox column's cells line up: take the rendered
option-list lines from the decoded frame and assert the option key names all
start at the same column, which is the observable consequence of the single
width constant and is what a user actually sees.

**C. UXP-03 — both bracket states, and NO_COLOR (Global SSH at minimum).**
Assert the off bracket is present on an unchosen selectable row and that after
toggling, the on bracket is present on that row. Then run one session with
`NO_COLOR=1` in the child environment and assert both bracket states and the
placeholder are still mutually distinguishable in the raw frame — the bracket
SHAPE carries the meaning, which is exactly the property that cannot be
verified from a styled in-package render.

**D. A REAL mouse toggle, not a keyboard one.** Call `clickLabelRow`-style raw
SGR press/release on a selectable row's checkbox cell and assert (i) that row's
bracket flipped and (ii) the detail selection did NOT move. Derive the
coordinates from the decoded frame the way `clickLabelRow` does; never hardcode
a column. This is the assertion that catches a click hit-test needle left
pointing at the old glyph — Task 2's highest-risk silent failure, which
produces no compile error and no failing render test.

Do not add a `t.Skip` to either file. Do not change any production source in
this task; if a PTY assertion fails, the defect belongs to Task 1, 2 or 3 and
is fixed there, then re-verified here.
  </action>
  <verify>
    <automated>make build &amp;&amp; make test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `make test-e2e` passes, and the SUMMARY quotes the `--- PASS` lines for every new or extended test function in both files.
    - Both screens have a PTY assertion for first-row focus AFTER re-entry (navigate away and back), not only on first activation.
    - Both screens have a PTY assertion that a non-selectable row renders the dot placeholder, and that the option key names in the rendered list all begin at the same column.
    - Global SSH has a PTY assertion for BOTH bracket states, plus one `NO_COLOR=1` session asserting the two states and the placeholder remain mutually distinguishable; the captured frame text is quoted in the SUMMARY.
    - At least one assertion uses a REAL raw SGR mouse press/release (the `clickLabelRow` technique, coordinates derived from the decoded frame) to toggle a row, and asserts both that the bracket flipped and that the detail selection did not move. A keyboard-only toggle does not satisfy this criterion.
    - `rg -c 't\.Skip' e2e/global_ssh_pty_e2e_test.go e2e/global_git_pty_e2e_test.go` returns 0 for both files.
    - `git diff --name-only` for this task's commit touches only the two `e2e/` files — no production source changed here.
  </acceptance_criteria>
  <done>UXP-01, UXP-02 and UXP-03 are each proven in the compiled binary through a real terminal, including a real mouse click that toggles a row, so the phase cannot pass with the terminal affordances broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mouse click / keypress -> option selection set -> the apply ceremony -> `Backend.Persist` | the boundary where a rendering change can cause a DIFFERENT option than the user intended to enter the write set |
| `OptionState` from the classifier -> the row renderer's `Selectable()` gate | the classifier decides what may be written; the renderer must not widen that decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.4-07 | Tampering | the option-row render and click path on both screens | high | mitigate | `Selectable()` stays the SINGLE predicate gating the toggle key, the checkbox glyph, and the click hit-test. Task 2 forbids introducing a second, independently-computed selectability check and preserves both screens' existing pre-needle `Selectable()` guard. Without this, a row the classifier marked non-writable (already-set, differs, or not-applicable) could enter the apply set — the exact regression D-02's scope fence exists to prevent. |
| T-09.4-08 | Tampering | the click hit-test needle versus the drawn glyph | medium | mitigate | Both needles move in the SAME commit as both render sites; Task 2's Test 5 exercises the click path in-package on both screens; and Task 4 drives a REAL raw SGR mouse press/release against the compiled binary through a PTY, asserting the bracket flipped and the detail selection did not move. A desynced needle does not fail to compile and does not fail any render test, so the compiled-binary mouse assertion is the load-bearing proof here (cross-AI review HIGH). The apply ceremony's preview plus typed confirm remains a second backstop. |
| T-09.4-09 | Spoofing | the differs-state explanation | low | mitigate | The strengthened sentence asserts a factual claim about gitid's write semantics (a managed block sits below a hand-written value, so applying it there changes nothing). Task 3 pins the full sentence in the copy-freeze gate rather than a prefix, so the claim cannot be silently truncated into something misleading. |
| T-09.4-10 | Tampering | glyph-swap scope creep | low | mitigate | The new toggle pair is scoped by doc comment and by an acceptance criterion that the identity manager's glyph-constant reference count is unchanged. A shared-helper leak would change checkbox rendering on unrelated write ceremonies. |
| T-09.4-SC | Tampering | package-manager installs | low | accept | No `go get`, `npm`, `pip`, or `cargo` install occurs. RESEARCH.md's Package Legitimacy Audit records zero external packages for this phase; `go.mod` is not modified. |
</threat_model>

<verification>
1. `TERM=dumb SSH_AUTH_SOCK= go test -count=1 -race ./...`
2. `make lint`
3. `make gate-copy-freeze`
4. `make gate-visual-regression` — **run it FIRST and read the result before
   touching any allowlist.** The option-row column width and glyph both change,
   so the Global SSH and Global Git captured FRAMES move. That is not by itself
   an allowlist change: the allowlist records SEMANTIC real-versus-dummy
   divergences, not stored golden frames, and both sides render through the
   same shared `tuikit` code — so a change made there appears identically on
   the real and the dummy side and the divergence SET should not move. Expected
   outcome: the gate stays green with `.planning/design/global-ssh/…` and
   `.planning/design/global-git/…` untouched.
   Ownership rule, stated so it is unambiguous: **this plan OWNS both surface
   allowlists** (they are in `files_modified`), but edits either one ONLY if the
   gate demonstrates the divergence set or its predicates actually changed. If
   it does, re-derive from the registry — never hand-patch the text file (Plan
   02 Task 2's rule). Record in the SUMMARY which of the two outcomes occurred,
   with the gate output that justifies it. `git diff --stat` on both allowlist
   files is expected to be EMPTY; a non-empty diff must be explained.
5. `make test-e2e` — the option screens have real-PTY coverage whose
   assertions may name the old glyph, and Task 4 extends both files. Both are
   in `files_modified`; re-point any existing assertion that names a glyph this
   plan replaced.
6. `NO_COLOR=1` render check: capture a Global SSH options frame with color
   disabled and confirm the toggle's on and off states and the placeholder
   are still mutually distinguishable. UI-SPEC's Inherited Non-Negotiables
   require this and no automated check covers it; record the captured frame
   text in the SUMMARY.
</verification>

<success_criteria>
- Global SSH focuses its first option row on every activation, by the same
  derived rule Global Git now uses, with no key literal on either screen.
- All four checkbox-column variants on both screens render to one identical
  display width, proven by a table test.
- Non-selectable rows show the faint dot placeholder on both screens.
- Selectable rows show the bracket toggle, green and bold when on, faint when
  off, and legible without color.
- Both click needles match the glyphs actually drawn; click-to-toggle works on
  both screens without moving the detail selection.
- The set-but-differs row explains its own no-op on its existing second line,
  on both screens, pinned by the copy-freeze gate as a full sentence.
- Global Git's render path has its own zero-row branch; a zero-option backend
  renders an empty state instead of panicking.
- All three requirements are proven in the compiled binary through a real PTY,
  including a real SGR mouse click that toggles a row without moving the detail
  selection, and one `NO_COLOR=1` legibility session.
- Both surface allowlists are accounted for: the gate was run first and the
  SUMMARY records whether the divergence set changed (expected: it did not).
- No classifier change; the identity manager's glyphs are untouched.
</success_criteria>

<output>
Create `.planning/phases/09.4-tui-ux-consistency-doctor-fixer-parity/09.4-03-SUMMARY.md` when done.
Record: the RED evidence for the re-activation test and the uniform-width
test; the policy-table check proving Global Git's focus outcome is unchanged;
the before/after glyph-constant reference count for the identity manager; the
truncation measurement for the widest key on each screen; the composed
differs-line width measurement; the two new copy-freeze entries verbatim; the
NO_COLOR frame captures (in-package and PTY); Task 4's PTY `--- PASS` lines
with the real SGR mouse-toggle assertion quoted; and the allowlist outcome for
both surfaces with the gate output that justifies it.
</output>
