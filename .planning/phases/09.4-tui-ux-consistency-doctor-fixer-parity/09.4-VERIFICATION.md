---
phase: 09.4-tui-ux-consistency-doctor-fixer-parity
verified: 2026-09-03T22:31:25Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 09.4: TUI UX Consistency & Doctor/Fixer Parity Verification Report

**Phase Goal:** Fix the focus, read-only-affordance, toggle-visibility,
submenu-discoverability, and Doctor/Fixer count-parity inconsistencies found
in a direct user audit of the Global SSH, Global Git, and Fixer screens.
**Verified:** 2026-09-03T22:31:25Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

All five come directly from ROADMAP.md's Phase 9.4 Success Criteria
(the roadmap contract) and were each independently re-derived from the
current codebase, not taken from SUMMARY.md or 09.4-REVIEW.md claims.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Global SSH's Options screen focuses a row on activation by the same rule Global Git's Options screen already uses (reset on every activation), with no construction-time hardcoded row surviving re-activation (UXP-01) | ✓ VERIFIED | `internal/tuikit/globalssh.go:145-151` — `newGlobalSSHModel` no longer constructs with a hardcoded `detailKey`. `globalssh.go:160-186` `activate()` sets `m.detailKey = m.options[0].Key` after every fetch. `globalgit.go:168-` `activate()` derives the same rule from the freshly fetched option list. Behaviorally proven in the compiled binary by `TestGlobalSSH_RealPTYOptionAffordancesAndMouseToggle` (`e2e/global_ssh_pty_e2e_test.go:187-221`), which navigates away, returns, and asserts focus resets to the first fetched row (`waitForFocusedOption(t, s, "StrictHostKeyChecking", "re-entering Global SSH resets focus to the first fetched row")`) — this test was executed as part of the full `make test-e2e` run and passed. |
| 2 | A non-selectable options row renders with the same visual pattern (checkbox-column placeholder) across Global SSH and Global Git, and the reason it is non-selectable is visible on the row (UXP-02) | ✓ VERIFIED | `internal/tuikit/theme.go:87-90` defines the single shared `optionBoxWidth` constant. `globalssh.go:1003-1010` and `globalgit.go:1167-` both render `padDisplay(styleFaint.Render("·"), optionBoxWidth)` for non-selectable rows. `design.go:241,243` defines `GlobalSSHWordDiffersUser`/`GlobalSSHWordDiffersOutside` ("set, differs — yours/external, would be a no-op here"), used identically by both screens (`globalssh.go:1046,1048`; `globalgit.go:472,474`, with `globalgit.go:461`'s comment confirming intentional reuse of "Phase 6's own frozen sentences"). Proven in the compiled binary: `TestGlobalSSH_RealPTYOptionAffordancesAndMouseToggle` asserts the `IdentitiesOnly` row carries a `·` placeholder with no bracket; `TestGlobalGit_RealPTYOptionFocusPlaceholderAndColumns` covers the Git side. |
| 3 | The selectable-row toggle indicator is visually more prominent than the current glyph (UXP-03) | ✓ VERIFIED | `theme.go:83-84` — `glyphToggleOn = "[✓]"`, `glyphToggleOff = "[ ]"` (bracket-style, bold green when on per `globalssh.go:1007`/`globalgit.go:1171`). Both screens' click hit-tests (`globalssh.go:943-944`, `globalgit.go:1049-1050`) use `hitNeedle(body, x, y, glyphToggleOn/Off)`, matching the actually-rendered glyph rather than a hardcoded offset. `TestGlobalSSH_RealPTYOptionAffordancesNoColor` (`e2e/global_ssh_pty_e2e_test.go:223-237`) proves `[✓]`, `[ ]`, and `·` remain mutually distinguishable with `NO_COLOR=1` — shape, not color, carries the signal. Ran and passed as part of `make test-e2e`. |
| 4 | A screen with local sub-tabs (Global SSH's Options/Storage & preview) is visually distinguishable from top-level main-menu navigation (UXP-04) | ✓ VERIFIED | `globalssh.go:764-790` — `gssSubTabStripRows()` is the single source of truth (3 rows: border/labels/border); `subTabStrip()` renders an accent-colored dashed box (`╭╌╌…╌╮` / `┊ … ┊` / `╰╌╌…╌╮`) around the Options/Storage labels, distinct from the flat main tab bar. `handleClick` (`globalssh.go:884-899`) derives its hit zones from the actual rendered strip via `hitNeedle`, not a hardcoded offset. Real raw SGR mouse click proven in the compiled binary by `TestGlobalSSH_RealPTYSubTabStripMouseClick` (`e2e/global_ssh_pty_e2e_test.go:415-465`), which clicks both labels via `clickLabelRow` and asserts the Storage/Options content actually switches — ran and passed as part of `make test-e2e`. `make gate-visual-regression` confirms no unclassified visual divergence was introduced. |
| 5 | Health and Fixer are merged back into a single "Doctor" tab that lists every finding and offers inline fix/fix-all, so no separate screen's finding count can read as contradicting another's; the write-confirmation ceremony is unchanged (UXP-05) | ✓ VERIFIED | `internal/tuikit/health_screen.go` and `fixer_screen.go` are deleted; `internal/tuikit/doctor_screen.go` is the sole findings screen. `frame.go:63-71` — `TabID` is a 5-value enum (`TabIdentities, TabGlobalSSH, TabGlobalGit, TabDoctor, TabGitIgnore`); `app.go:146` — `App.screens [5]screenModel`. `doctor_screen.go:94,96` list from unfiltered `orderedFindings(...)`, with `f`/`F` gated on `sel.Fixable` (lines 285,410,432,463) — non-fixable findings are listed but offer no fix action. `app.go:313-330` `checkFixBatchHalt` type-asserts `doctorModel` at both dispatch sites (lines 389,403), preserving the D-16 halt-on-failure ceremony. `identities.go:2556` and `app.go:471-472,579-580` route the `h` key and header health chip through `setTabFiltered(TabDoctor, ...)`. Grep for stray "Health"/"Fixer" tab-name strings in non-test `.go` files under `internal`, `cmd`, `e2e` returned zero hits outside justified categories (CLI verb `gitid health`, D-09 carve-out comment, `identity.IdentityHealth` domain type, `styleHealthy` color token, historical citations, `Fixer*` frozen constant *names* whose *values* render no tab-name text). `internal/tuikit/doctor_test.go` records zero changed lines — the shared finding helpers were reused, not rewritten. |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/tuikit/doctor_screen.go` | Merged findings + fix-ceremony screen model | ✓ VERIFIED | Exists, substantive, wired into `App.screens[TabDoctor]`; unfiltered `orderedFindings` scope + `Fixable`-gated `f`/`F` confirmed by direct grep/read. |
| `internal/tuikit/health_screen.go`, `fixer_screen.go` | Absent (absorbed into Doctor) | ✓ VERIFIED (absence confirmed) | Both files no longer exist on disk. |
| `internal/tuikit/frame.go` — 5-value `TabID` | UXP-05 tab count | ✓ VERIFIED | `TabIdentities, TabGlobalSSH, TabGlobalGit, TabDoctor, TabGitIgnore` — exactly 5. |
| `internal/tuikit/app.go` — `App.screens [5]screenModel` | Array arity matches TabID | ✓ VERIFIED | Confirmed at `app.go:146`. |
| `internal/tuikit/theme.go` — `glyphToggleOn`/`glyphToggleOff`/`optionBoxWidth` | Shared toggle glyphs and column width (UXP-02/03) | ✓ VERIFIED | Present with scoping doc comments; consumed identically by both `globalssh.go` and `globalgit.go`. |
| `internal/tuikit/globalssh.go` — `gssSubTabStripRows()` single source | UXP-04 centralized strip height | ✓ VERIFIED | One function, consumed by `renderOptions`, `renderStorage` (via `gssOptionsTopLines`), and `handleClick`. |
| `e2e/doctor_pty_e2e_test.go` | Real-PTY Doctor coverage | ✓ VERIFIED | Present; part of the passing `make test-e2e` run. |
| `.planning/design/health-fixer/visual-divergence-allowlist.txt` | Re-derived for merged registry | ✓ VERIFIED | `make gate-visual-regression` passed against it live (`TestGateVisualRegression` — "57 RequiredScreenSpecs frames checked as a classified real/dummy symmetric union"). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `checkFixBatchHalt` type assertion | `doctorModel` | `a.screens[a.tab].(doctorModel)` | ✓ WIRED | Confirmed at both dispatch sites (`app.go:389,403`); no stale `fixerModel`/`healthModel` assertion remains. |
| `keyResult.doctorIdentity` | `App.setTabFiltered(TabDoctor, ...)` | 3 routing sites (identity-manager `h` key, keyboard result, header health-chip click) | ✓ WIRED | `identities.go:2556`, `app.go:471-472`, `app.go:579-580` all confirmed. |
| Rendered toggle glyph | Click hit-test needle | `hitNeedle(body, x, y, glyphToggleOn/Off)` | ✓ WIRED | Both screens use the shared glyph constants as the literal click needle — no independent offset math. |
| `gssSubTabStripRows()` | `renderOptions`/`renderStorage`/`handleClick` row budgets | direct function call | ✓ WIRED | All three consumers call the same function; no literal row-count duplication found. |
| TabID enum arity | `App.screens` array length / number-key switch | Go array typing + `TabID(int(key[0]-'1'))` | ✓ WIRED | 5-value enum matches `[5]screenModel`; `tabLabels`/`tabNavLabels` are also 5-element arrays. |

### Behavioral Spot-Checks / Full Gate Runs

All commands below were run directly by the verifier in this session — not read from SUMMARY.md or 09.4-REVIEW.md.

| Command | Result | Status |
|---------|--------|--------|
| `go build ./...` | No output (success) | ✓ PASS |
| `go test -race -count=1 ./...` | 2587 tests passed across 22 packages, no failures | ✓ PASS |
| `make lint` (`golangci-lint run ./...` + screenshot-tagged run) | `0 issues.` (both invocations) | ✓ PASS |
| `make gate-copy-freeze` | Exit 0; all frozen-string checks report `ok`, including UXP-02/03's strengthened "differs" sentences | ✓ PASS |
| `make gate-visual-regression` | `TestGateVisualRegression`: "57 RequiredScreenSpecs frames checked as a classified real/dummy symmetric union" — PASS; all `TestNegativeControl_*` mutation-sensitivity guards PASS; `TestGlobalSSHAllowlistMatchesRegistry`, `TestSubTabStrip*`-adjacent gate tests PASS | ✓ PASS |
| `go test ./internal/tuikit/... -run 'TestDoctorBatchHaltSurvivesModelRename\|TestDoctorFixGateRespectsFixableBit\|TestDoctorCountIsTheOnlyFindingCount'` | 3 passed | ✓ PASS |
| `go test ./internal/tuikit/... -run 'TestSubTabStrip'` | 5 passed | ✓ PASS |
| `make test-e2e` (full real-PTY suite, `-race -timeout 2400s`) | `ok github.com/castocolina/gitid/e2e 979.486s` — includes `TestGlobalSSH_RealPTYOptionAffordancesAndMouseToggle` (focus reset + bracket toggle + click), `TestGlobalSSH_RealPTYOptionAffordancesNoColor` (NO_COLOR legibility), `TestGlobalSSH_RealPTYSubTabStripMouseClick` (real SGR mouse click switching sub-tabs), `e2e/doctor_pty_e2e_test.go`'s `TestDoctor_RealPTY*` suite, and `TestGlobalGit_RealPTYOptionFocusPlaceholderAndColumns` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| UXP-01 | 09.4-03 | Consistent initial row focus | ✓ SATISFIED | See Truth #1. |
| UXP-02 | 09.4-03 | Discoverable read-only rationale | ✓ SATISFIED | See Truth #2. |
| UXP-03 | 09.4-03 | Stronger toggle affordance | ✓ SATISFIED | See Truth #3. |
| UXP-04 | 09.4-04 | Submenu visual distinction | ✓ SATISFIED | See Truth #4. |
| UXP-05 | 09.4-01, 09.4-02 | Doctor/Fixer count parity — merge | ✓ SATISFIED | See Truth #5. |
| FIX-01 | (unchanged) | Confirmed, backed-up fixes | ✓ SATISFIED (unaffected) | Ceremony machinery ported verbatim into `doctor_screen.go`; no change to the preview/typed-confirm/backup flow. |

No orphaned requirements found for this phase in `.planning/REQUIREMENTS.md`'s §R (UXP) mapping.

### Anti-Patterns Found

Scanned every file in the union of the four plans' `files_modified` frontmatter (57 files) for `TBD|FIXME|XXX`, `TODO|HACK|PLACEHOLDER`, and stub-shaped empty returns.

- No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any Go file this phase touched.
- The only `TBD` hits were in `.planning/ROADMAP.md`, all pre-existing placeholders for *unrelated, not-yet-started future phases* (Phase 7 already complete, Phase 9.5/10 plan counts) — not artifacts of this phase's own deliverable.
- No blockers found.

### Human Verification Required

None. All five roadmap Success Criteria were independently verified against the codebase with both static evidence (grep/read of the actual render/click/activate logic) and dynamic evidence (a full, real-PTY `make test-e2e` run executed by the verifier, plus the project's own `make lint`, `make gate-copy-freeze`, and `make gate-visual-regression` gates — all run directly, not read from SUMMARY.md).

### Notable Non-Blocking Item (carried forward, not a phase-goal gap)

**09.4-REVIEW.md WR-01 (this round):** `internal/tuikit/globalgit.go:367-371` gates `m.currentName`/`m.currentEmail` behind `ceremonyOpen` after the CR-01 snapshot fix, introducing a narrow, self-healing UI-staleness window in the genuinely-abandoned (no-supersede) fallback-author-success case. The reviewer classified this as WARNING, not Critical — `App.state`'s `GitGlobalName`/`GitGlobalEmail` and the master-list's fallback-row display are unaffected (self-heals on the next screen re-entry). This is in the Global Git fallback-author ceremony machinery reused by the phase, not a UXP-01..05 requirement itself, and does not block phase closeout. Verified still present in current code; recorded here so it is not lost, per the reviewer's own recommendation, and left for a human/maintainer decision on whether to open a follow-up.

### Documentation Drift (non-blocking, informational)

- `.planning/REQUIREMENTS.md` §R still shows UXP-01 through UXP-05 as unchecked `[ ]` ("Pending" in the requirements-status table) despite the underlying code satisfying all five. This is standard: these checkboxes are updated by the ship/complete-phase workflow step that follows a passing verification, not by the verifier. No action needed beyond the normal phase-completion step.
- `.planning/REQUIREMENTS.md` §N's `SHELL-02` (marked `[x]` Complete, pre-dates this phase) still lists the view set as "4) Health, 5) Fixer" — stale since Phase 8/9.2/9.4 changed the tab set to `Identities / Global SSH / Global Git / Doctor / Global Git Ignore`. This requirement was not in any of the four plans' `files_modified` and is outside 09.4's declared scope; flagged here as informational drift, not a gap this phase is responsible for closing.

### Gaps Summary

None. All 5 roadmap Success Criteria (UXP-01 through UXP-05) are verified present, wired, and behaviorally proven through a real compiled-binary PTY session. `go build`, the full race-enabled test suite, `make lint`, `make gate-copy-freeze`, `make gate-visual-regression`, and the full `make test-e2e` real-PTY suite were all run directly by the verifier in this session and passed cleanly. The phase goal — one Doctor tab with a single source of truth for findings and fix actions, plus consistent focus/checkbox/toggle/submenu UX between Global SSH and Global Git — is genuinely achieved in the codebase, not merely claimed in SUMMARY.md.

---

_Verified: 2026-09-03T22:31:25Z_
_Verifier: Claude (gsd-verifier)_
