---
phase: 05-cli-surface-tui
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - cmd/gitid/main.go
  - cmd/gitid/copy.go
  - cmd/gitid/copy_test.go
  - cmd/gitid/completion_test.go
  - cmd/gitid/main_test.go
autonomous: true
requirements: [CLI-01, CLI-02]
user_setup: []

must_haves:
  truths:
    - "gitid (no args) on a TTY launches the TUI; non-TTY no-args prints a usage hint and exits 1"
    - "Top-level gitid rotate, gitid copy, and gitid host add are registered and reachable"
    - "identity copy <name> is registered under the identity group"
    - "gitid copy <name> reconstructs the identity, copies the public key to the clipboard, and prints upload instructions"
    - "gitid completion bash, zsh, and fish each produce a non-empty script containing gitid"
  artifacts:
    - path: "cmd/gitid/copy.go"
      provides: "newCopyCmd, newIdentityCopyCmd, newHostAddCmd, runCopy"
      contains: "func runCopy"
    - path: "cmd/gitid/main.go"
      provides: "no-args TTY→TUI branch + top-level aliases wired in newRootCmd"
      contains: "tui.Run()"
  key_links:
    - from: "cmd/gitid/main.go main()"
      to: "tui.Run()"
      via: "no-args + term.IsTerminal branch before Execute()"
      pattern: "term\\.IsTerminal"
    - from: "cmd/gitid/copy.go runCopy"
      to: "internal/clipboard.Copy + internal/upload.Instructions"
      via: "reconstruct → read .pub → clipboard.Copy → print upload.Instructions"
      pattern: "clipboard\\.Copy|upload\\.Instructions"
    - from: "newRootCmd"
      to: "newCopyCmd / newHostAddCmd / rotate alias"
      via: "root.AddCommand(...)"
      pattern: "AddCommand"
---

<objective>
Finalize the full `gitid` command surface (CLI-01) and verify shell completion (CLI-02). Add the no-args TTY→TUI launch branch in `main()`; register the three top-level aliases (`rotate`, `copy`, `host add`) and the `identity copy` subcommand in `newRootCmd()`; implement the new `copy <name>` command (clipboard + printed upload instructions, reusing the extracted `internal/upload.Instructions`); and add unit tests proving every SC-4 command is reachable and bash/zsh/fish completion is non-empty.

Purpose: Closes the SC-4 reachable-surface requirement and CLIP-02/UP-02 on-demand gaps via one `copy` command; makes `gitid` (no args) launch the TUI per TUI-01's entry contract.
Output: Modified `cmd/gitid/main.go`, new `cmd/gitid/copy.go`, command-registration + completion + no-args tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-surface-tui/05-RESEARCH.md
@.planning/phases/05-cli-surface-tui/05-PATTERNS.md
@.planning/phases/05-cli-surface-tui/05-UI-SPEC.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- New Cobra commands: top-level `rotate <name>` alias, top-level `copy <name>`, `host` group with `add` subcommand, `identity copy <name>` subcommand
- `cmd/gitid/copy.go` — `newCopyCmd()`, `newIdentityCopyCmd()`, `newHostAddCmd()`, `runCopy(out io.Writer, name string) error`
- `cmd/gitid/main.go` — no-args TTY→TUI branch in `main()`; alias registrations in `newRootCmd()`
- Tests: `cmd/gitid/copy_test.go`, `cmd/gitid/completion_test.go`, `cmd/gitid/main_test.go`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: copy command (copy + identity copy + host add) reusing internal/upload</name>
  <files>cmd/gitid/copy.go, cmd/gitid/copy_test.go</files>
  <read_first>
    - cmd/gitid/rotate.go (newRotateCmd shape, runIdentityRotate validation, sanitizeName/identityNameRe reuse — same package main)
    - cmd/gitid/add.go lines 176-262 (runAddAccount + fp() output helper) and lines 319-425 (buildDeps for host add wiring)
    - cmd/gitid/upload.go (now delegating to internal/upload.Instructions after 05-01)
    - internal/identity/loader.go lines 1-40 (Reconstruct signature) + internal/identity/identity.go lines 25-40 (Account fields: Name/Provider/KeyPath etc.)
    - internal/clipboard (Copy signature)
    - .planning/phases/05-cli-surface-tui/05-PATTERNS.md §"cmd/gitid/copy.go" + §"runCopy test pattern"
    - .planning/phases/05-cli-surface-tui/05-UI-SPEC.md §"CLI-only Command Copywriting" (Use/Short strings + copy output format)
  </read_first>
  <behavior>
    - Test: runCopy(out, "nonexistent") with HOME=t.TempDir() returns a non-nil error (identity not found), writes nothing fatal.
    - Test: runCopy rejects an invalid name (e.g. "Bad Name") via the name validation gate with a non-nil error.
    - Test: with a faked reconstructable identity + readable .pub, runCopy copies the pub line and the output contains "Copied public key" and the provider upload instructions.
  </behavior>
  <action>
    Create `cmd/gitid/copy.go` (package main). Implement `newCopyCmd()` and `newIdentityCopyCmd()` — both `Use: "copy <name>"`, `Args: cobra.ExactArgs(1)`, `Short` per UI-SPEC ("Copy the public key to the clipboard and print upload instructions"), RunE delegating to `runCopy(cmd.OutOrStdout(), args[0])`. Implement `newHostAddCmd()` — `Use: "add"`, `Short: "Add a host alias (SSH account) to an existing identity"`, RunE delegating to the existing `runAddAccount(...)` add-account flow with `buildDeps` (D-07). Implement `runCopy(out io.Writer, name string)`: validate the name via `identity.ValidateName(name)` (return wrapped error on failure); resolve `os.UserHomeDir()`; `os.ReadFile` `~/.ssh/config` and `~/.gitconfig` (annotate `//nolint:gosec // trusted gitid-managed path (G304)`); call `identity.Reconstruct(sshBytes, gcBytes, gitconfig.ReadFragment)`; find the account by name (return error "copy: identity %q not found" if absent); derive the `.pub` path from the account KeyPath, read it, `clipboard.Copy(pubLine)` (on copy error, print the key for manual copy and continue — do not hard-fail); then `fp(out, ...)` the success line ("Copied public key for %q to clipboard."), the key, and `upload.Instructions(account.Provider)`. Follow the exact output format in UI-SPEC §"gitid copy output format". Write `cmd/gitid/copy_test.go` per the behavior cases, HOME-isolated with `t.Setenv("HOME", t.TempDir())`.
  </action>
  <verify>
    <automated>go test ./cmd/gitid/... -count=1 -run 'TestRunCopy|TestCopyCmd|TestIdentityCopy|TestHostAdd'</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/gitid/... -run TestRunCopy` passes
    - `cmd/gitid/copy.go` contains `func runCopy`, `func newCopyCmd`, `func newIdentityCopyCmd`, `func newHostAddCmd`
    - runCopy references `clipboard.Copy` and `upload.Instructions`
    - runCopy validates via `identity.ValidateName`
  </acceptance_criteria>
  <done>copy/identity copy/host add commands implemented and tested; clipboard + upload instructions reused from extracted internal packages; name validation enforced (D-06/D-07).</done>
  <maps_to>VALIDATION: "copy reads pub file, copies to clipboard, prints upload instructions" (CLI-01/CLIP-02/UP-02), "identity copy registered" (CLI-01)</maps_to>
</task>

<task type="auto" tdd="true">
  <name>Task 2: no-args TUI branch + alias registration + completion/registration tests</name>
  <files>cmd/gitid/main.go, cmd/gitid/main_test.go, cmd/gitid/completion_test.go</files>
  <read_first>
    - cmd/gitid/main.go (current main() + Execute() + newRootCmd assembly point)
    - cmd/gitid/doctor.go lines 588-597 (isTerminalOutput / term.IsTerminal reuse pattern)
    - cmd/gitid/doctor_test.go (TestDoctorCmdRegistered pattern for registration tests)
    - .planning/phases/05-cli-surface-tui/05-PATTERNS.md §"cmd/gitid/main.go" (no-args branch + alias block) + §"completion_test.go"
    - .planning/phases/05-cli-surface-tui/05-RESEARCH.md Pattern 7, Pattern 8, Pattern 9
    - .planning/phases/05-cli-surface-tui/05-UI-SPEC.md §"Non-TTY / Piped Behavior Contract" (exact usage-hint string + exit 1)
  </read_first>
  <behavior>
    - Test: newRootCmd() has top-level commands "rotate <name>", "copy <name>", and a "host" group whose subcommands include "add"; the identity group includes "copy <name>".
    - Test: completion bash/zsh/fish each Execute() without error and produce output containing "gitid" (TestCompletionBash/Zsh/Fish).
    - Test: the no-args non-TTY path prints "gitid: no subcommand given. Run 'gitid --help' for usage." and signals exit 1 (test the extracted, injectable decision helper, not os.Exit directly).
  </behavior>
  <action>
    Modify `main()` so that when `len(os.Args) == 1`: if `term.IsTerminal(int(os.Stdout.Fd()))` call `tui.Run()` (on error print `gitid: tui: <err>` to stderr and exit 1), else print the non-TTY usage hint to stderr and exit 1; otherwise fall through to the existing `Execute()` path preserving the `doctorExitCode` tiered logic (RESEARCH Pattern 7). To keep this unit-testable without invoking the real TUI/os.Exit, factor the no-args decision into a small helper (e.g. `noArgsAction(isTTY bool, run func() error, out, errw io.Writer) int`) returning an exit code, and have `main()` call it; test the helper directly with `isTTY=false` asserting the hint + code 1, and `isTTY=true` with a fake `run` returning nil asserting code 0. Add imports `golang.org/x/term` and `github.com/castocolina/gitid/tui`. In `newRootCmd()`, after existing AddCommand calls, register: `root.AddCommand(newCopyCmd())`; the `identity` subcommand gets `identity.AddCommand(newIdentityCopyCmd())`; a top-level `rotate <name>` alias command whose RunE calls `runIdentityRotate(cmd.InOrStdin(), cmd.OutOrStdout(), args[0], false, buildDeps)` (per PATTERNS §main.go); and a `host` group (`&cobra.Command{Use: "host", Short: "Manage SSH host aliases"}`) with `host.AddCommand(newHostAddCmd())`. Write `cmd/gitid/main_test.go` (registration + no-args helper) and `cmd/gitid/completion_test.go` (bash/zsh/fish non-empty), HOME-isolated. Fish syntax validation is manual-only (fish absent locally — VALIDATION manual list); the unit test asserts non-empty output only.
  </action>
  <verify>
    <automated>go test ./cmd/gitid/... -count=1 -run 'TestNewRootCmd|TestCompletion|TestNoArgs|TestRotateAlias|TestHostGroup' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/gitid/... -run 'TestCompletionBash|TestCompletionZsh|TestCompletionFish'` passes
    - `go test ./cmd/gitid/... -run TestNewRootCmd` proves rotate/copy/host-add/identity-copy registered
    - `go test ./cmd/gitid/... -run TestNoArgsNonTTY` proves the usage hint + exit-1 decision
    - `cmd/gitid/main.go` references `tui.Run()` and `term.IsTerminal`
    - `go build ./...` succeeds
  </acceptance_criteria>
  <done>Full SC-4 command tree registered + reachable; completion verified for three shells; no-args TTY→TUI / non-TTY→hint branch wired and unit-tested.</done>
  <maps_to>VALIDATION: TestNewRootCmd (CLI-01), TestCompletionBash/Zsh/Fish (CLI-02), TestNoArgsNonTTY (TUI-01)</maps_to>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg (identity name) → core | `copy <name>` / `host add` user-supplied name reaches reconstruct + keygen paths |
| filesystem → runCopy | os.ReadFile of ~/.ssh/config, ~/.gitconfig, the identity .pub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | copy <name> identity argument | mitigate | `runCopy` calls `identity.ValidateName(name)` before any filesystem/reconstruct use (V5) |
| T-05-06 | Tampering | runCopy os.ReadFile (config + .pub) | mitigate | gitid-managed trusted paths; `//nolint:gosec // trusted gitid-managed path (G304)` annotation per cmd-layer precedent |
| T-05-07 | Information Disclosure | clipboard copy of pub line | accept | Public key is non-secret public data; clipboard exposure acceptable (RESEARCH Security Domain) |
| T-05-08 | Tampering | host add → runAddAccount write path | mitigate | Reuses existing add-account flow through identity.Deps → filewriter chokepoint (backup+atomic+confirm); no new write path |
| T-05-SC | Tampering | package installs | mitigate | No new package installs in this plan (deps added in 05-01) |
</threat_model>

<verification>
- `go test ./cmd/gitid/... -count=1` green (registration, copy, completion, no-args)
- `go build ./...` succeeds (tui.Run import resolves)
- `go test -race ./...` green at wave merge
</verification>

<success_criteria>
- Every SC-4 command (`doctor`, `identity add/list/test`, `host add`, `rotate`, `copy`) reachable as a gitid subcommand; `identity copy` present
- `gitid completion {bash,zsh,fish}` produce valid non-empty scripts (CLI-02)
- `gitid` no-args launches TUI on a TTY, prints a usage hint + exits 1 when piped (TUI-01 entry)
</success_criteria>

<output>
Create `.planning/phases/05-cli-surface-tui/05-02-SUMMARY.md` when done.
</output>
