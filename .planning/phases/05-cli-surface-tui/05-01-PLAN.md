---
phase: 05-cli-surface-tui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/upload/upload.go
  - internal/upload/upload_test.go
  - internal/identity/validate.go
  - internal/identity/validate_test.go
  - cmd/gitid/upload.go
  - tui/doc.go
  - tui/tui.go
  - tui/model.go
  - tui/messages.go
  - tui/keymap.go
  - tui/styles.go
  - tui/deps.go
  - tui/model_test.go
autonomous: true
requirements: [TUI-01, TUI-02, CLI-01]
user_setup: []

must_haves:
  truths:
    - "tui/ compiles against charm.land/bubbletea/v2, lipgloss/v2, bubbles/v2 at pinned versions"
    - "internal/upload.Instructions(provider) returns the GitHub/GitLab/default upload steps, importable by both cmd/gitid and tui"
    - "internal/identity.ValidateName(name) rejects names with spaces/uppercase/shell metacharacters and accepts [A-Za-z0-9._-]+"
    - "tui.Run() builds deps, constructs a root model, and runs a tea.Program (alt-screen) returning an error"
    - "rootModel implements tea.Model with View() tea.View (NOT string) and a []screenModel view-stack"
  artifacts:
    - path: "internal/upload/upload.go"
      provides: "Instructions(provider string) string extracted from cmd/gitid/upload.go"
      contains: "func Instructions"
    - path: "internal/identity/validate.go"
      provides: "ValidateName(name string) error domain-layer name validation"
      contains: "func ValidateName"
    - path: "tui/deps.go"
      provides: "buildTUIDeps() wiring identity.Deps + doctor.Deps from internal packages (no package main import)"
      contains: "func buildTUIDeps"
    - path: "tui/model.go"
      provides: "rootModel view-stack with v2-correct View() tea.View, push/pop handling"
      contains: "func (m rootModel) View() tea.View"
    - path: "tui/keymap.go"
      provides: "shared key.Binding keymap (D-13)"
      contains: "var keys"
    - path: "tui/styles.go"
      provides: "lipgloss v2 style tokens via lipgloss.NewStyle() (NO NewRenderer)"
      contains: "lipgloss.NewStyle()"
    - path: "tui/messages.go"
      provides: "custom tea.Msg types (familyResultMsg, pushScreenMsg, popScreenMsg, etc.)"
      contains: "type pushScreenMsg"
  key_links:
    - from: "tui/deps.go"
      to: "internal/doctor + internal/identity + internal/filewriter"
      via: "closure wiring mirroring cmd/gitid/add.go buildDeps + cmd/gitid/doctor.go buildDoctorDeps"
      pattern: "doctor\\.Deps|identity\\.Deps"
    - from: "cmd/gitid/upload.go"
      to: "internal/upload.Instructions"
      via: "uploadInstructions delegates to upload.Instructions"
      pattern: "upload\\.Instructions"
---

<objective>
Wave 0 foundation for Phase 5: add the three charm.land/*/v2 modules to go.mod, extract the two `package main` symbols the TUI cannot import (`uploadInstructions` → `internal/upload`, name validation → `internal/identity.ValidateName`), and stand up the TUI skeleton — `tui.Run()` entry, root view-stack model (v2-correct interface), shared keymap, lipgloss v2 style tokens, message types, and the TUI-side deps builder (`tui/deps.go`) that replicates the cmd-layer wiring without importing `package main`.

Purpose: Everything downstream (CLI aliases in 05-02, dashboard in 05-03, forms in 05-04) depends on these shared, import-cycle-free building blocks. Resolves RESEARCH Pitfalls 1, 2, 6, 8.
Output: go.mod with three new deps; `internal/upload` + `internal/identity.ValidateName` packages; compiling `tui/` skeleton with a runnable (if minimal) root model.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-surface-tui/05-RESEARCH.md
@.planning/phases/05-cli-surface-tui/05-PATTERNS.md
@.planning/phases/05-cli-surface-tui/05-UI-SPEC.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 0) creates the shared symbols later plans depend on:

**New packages / files:**
- `internal/upload` package — `Instructions(provider string) string`
- `internal/identity/validate.go` — `ValidateName(name string) error`
- `tui/tui.go` — `Run() error` (exported TUI entry point)
- `tui/model.go` — `rootModel` struct, `screenModel` interface, `newRootModel(...)`, `pushCmd`/`popCmd`
- `tui/messages.go` — `pushScreenMsg`, `popScreenMsg`, `familyResultMsg`, `identityListResultMsg`, `preWriteResultMsg`, `resolvedResultMsg`, `writeResultMsg`, `clipboardResultMsg`
- `tui/keymap.go` — `keyMap` struct + `keys` var (Up/Down/Left/Right/Select/Back/Quit/Help/Refresh/Add/Edit/Copy/Delete/Rotate/AddHost/Next/Prev/Submit/Confirm/Top/Bottom)
- `tui/styles.go` — `StyleTitle`/`StyleHeader`/`StyleSelected`/`StyleBody`/`StyleFaint`/`StyleLabel`/`StylePass`/`StyleFinding`/`StyleInputActive`/`StyleInputInactive`/`StyleHelpKey`/`StyleHelpDesc`/`StylePanel`/`StylePanelFocused` + `SeverityStyle(doctor.Severity) lipgloss.Style`
- `tui/deps.go` — `buildTUIDeps()`, `buildIdentityDeps()`, `buildTUIDoctorDeps(...)`, a `tuiDeps` aggregate struct
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add charm.land v2 deps + extract internal/upload and identity.ValidateName</name>
  <files>go.mod, go.sum, internal/upload/upload.go, internal/upload/upload_test.go, internal/identity/validate.go, internal/identity/validate_test.go, cmd/gitid/upload.go</files>
  <read_first>
    - cmd/gitid/upload.go (the exact source to extract verbatim into internal/upload)
    - cmd/gitid/rotate.go lines 1-30 (identityNameRe regex + sanitizeName — the validation logic to lift into identity.ValidateName)
    - .planning/phases/05-cli-surface-tui/05-PATTERNS.md §"internal/upload/upload.go" (exact extraction target)
    - .planning/phases/05-cli-surface-tui/05-RESEARCH.md §"New Installs Required" + Pitfall 6 + Pitfall 8
    - CLAUDE.md Technology Stack table (pinned charm.land versions)
  </read_first>
  <behavior>
    - Test (upload): upload.Instructions("github") contains "github.com/settings/ssh/new" and mentions two registrations; Instructions("gitlab") contains "user_settings/ssh_keys"; Instructions("bitbucket") is non-empty (default branch).
    - Test (validate): identity.ValidateName("personal") returns nil; ValidateName("work_2.x-y") returns nil; ValidateName("Work Bad") returns a non-nil error; ValidateName("a;rm -rf") returns error; ValidateName("") returns error; ValidateName("UPPER") returns error.
  </behavior>
  <action>
    Run `go get charm.land/bubbletea/v2@v2.0.7 charm.land/lipgloss/v2@v2.0.3 charm.land/bubbles/v2@v2.1.0` then `go mod tidy`. Create `internal/upload/upload.go` with package `upload` and exported `Instructions(provider string) string`, moving the body of `uploadInstructions` from `cmd/gitid/upload.go` verbatim (same GitHub/GitLab/default switch). Update `cmd/gitid/upload.go` so `uploadInstructions(provider)` delegates to `upload.Instructions(provider)` (keep the unexported wrapper so existing callers in add.go/rotate.go are untouched), importing `internal/upload`. Create `internal/identity/validate.go` with `ValidateName(name string) error`: trim whitespace, reject empty (error "identity name is required"), and reject anything not matching `^[A-Za-z0-9._-]+$` (error "invalid identity name %q"). Reuse the exact `identityNameRe` charset from rotate.go. Write table tests in both `_test.go` files using the behavior cases above. Do NOT yet change rotate.go's local identityNameRe (cmd-layer reuse is out of scope for this plan).
  </action>
  <verify>
    <automated>go test ./internal/upload/... ./internal/identity/... -count=1 -run 'TestInstructions|TestValidateName' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/upload/... -run TestInstructions` passes
    - `go test ./internal/identity/... -run TestValidateName` passes
    - `internal/upload/upload.go` contains `func Instructions(provider string) string`
    - `internal/identity/validate.go` contains `func ValidateName(name string) error`
    - `cmd/gitid/upload.go` references `upload.Instructions`
    - `go.mod` lists all three `charm.land/*/v2` modules at v2.0.7 / v2.0.3 / v2.1.0
    - `go build ./...` succeeds
  </acceptance_criteria>
  <done>charm.land v2 deps present; upload + ValidateName extracted to internal packages and importable by tui/; existing cmd callers unbroken; tests green.</done>
  <maps_to>VALIDATION behaviors: "copy reads pub file…upload instructions" (foundation), TUI form name validation (foundation for TUI-02)</maps_to>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TUI skeleton — keymap, styles, messages, deps builder, root view-stack model, Run()</name>
  <files>tui/doc.go, tui/keymap.go, tui/styles.go, tui/messages.go, tui/deps.go, tui/model.go, tui/tui.go, tui/model_test.go</files>
  <read_first>
    - tui/doc.go + tui/tui_stub_test.go (current stub package — replace stub)
    - cmd/gitid/add.go lines 319-425 (buildDeps closure wiring to copy into tui/deps.go buildIdentityDeps)
    - cmd/gitid/doctor.go lines 164-352 (buildDoctorDeps wiring to mirror in tui/deps.go buildTUIDoctorDeps)
    - internal/identity/identity.go lines 119-149 (Deps struct fields to wire)
    - internal/doctor/doctor.go lines 195-275 (Deps Check* fields, Family constants, Families())
    - .planning/phases/05-cli-surface-tui/05-PATTERNS.md §"tui/model.go", §"tui/keymap.go", §"tui/styles.go", §"tui/messages.go", §"tui/deps.go", §"tui/tui.go"
    - .planning/phases/05-cli-surface-tui/05-RESEARCH.md Patterns 1,2,4,6,7 + Pitfalls 1,2,3,6,9
    - .planning/phases/05-cli-surface-tui/05-UI-SPEC.md §"Keymap Contract", §"Lipgloss Style Token Reference" (NOTE: UI-SPEC uses renderer.NewStyle() — that is v1; use lipgloss.NewStyle())
  </read_first>
  <behavior>
    - Test: rootModel satisfies tea.Model (Init/Update/View compile); rootModel.View() returns a tea.View (not string) and does not panic on an empty stack.
    - Test: pushScreenMsg appends a screen to the stack; popScreenMsg pops one level but never empties below the home screen (len stays >= 1).
    - Test: a tea.WindowSizeMsg updates rootModel width/height.
    - Test: keys.Quit matches "q" and "ctrl+c"; keys.Back matches "esc" (via key.Matches on a synthesized tea.KeyPressMsg).
  </behavior>
  <action>
    Create `tui/keymap.go` with the `keyMap` struct + `keys` var exactly per UI-SPEC Keymap Contract (arrows + vim j/k/h/l + q/ctrl+c/esc/enter/?/r global, plus a/e/c/d/h/tab/shift+tab/g/G), using `charm.land/bubbles/v2/key`. Create `tui/styles.go` with all style tokens via `lipgloss.NewStyle()` (NEVER `renderer.NewStyle()` — RESEARCH Pitfall 1) and `SeverityStyle(doctor.Severity) lipgloss.Style` mapping critical/error→"1", warning→"3", info→"6", per UI-SPEC color tokens. Create `tui/messages.go` with `pushScreenMsg{next screenModel}`, `popScreenMsg{}`, `pushCmd`/`popCmd` helpers, plus `familyResultMsg{runID int; family doctor.Family; findings []doctor.Finding; err error}`, `identityListResultMsg`, `preWriteResultMsg`, `resolvedResultMsg`, `writeResultMsg`, `clipboardResultMsg` (signatures per PATTERNS §messages.go). Create `tui/deps.go` with a `tuiDeps` aggregate, `buildIdentityDeps() identity.Deps` (copy add.go buildDeps closures, drop the io.Writer param), `buildTUIDoctorDeps(home string, sshBytes, gcBytes []byte) doctor.Deps` (mirror doctor.go buildDoctorDeps, wiring all seven Check* fields to internal/doctor/checks), and `buildTUIDeps() (doctor.Deps, identity.Deps, error)`; annotate every gitid-managed `os.ReadFile` with `//nolint:gosec // trusted gitid-managed path (G304)` (RESEARCH Pitfall 9). Create `tui/model.go`: `screenModel` interface (`update(tea.Msg) (screenModel, tea.Cmd)`, `view() string`), `rootModel{stack []screenModel; width,height int; deps tuiDeps}`, `newRootModel(...)`, and v2-correct `Init() tea.Cmd`, `Update(tea.Msg) (tea.Model, tea.Cmd)` (handle WindowSizeMsg + push/pop + delegate to top of stack), `View() tea.View` returning `tea.NewView(top.view())` (RESEARCH Pitfall 2). Create `tui/tui.go` with exported `Run() error` that calls `buildTUIDeps()`, constructs `newRootModel`, creates `tea.NewProgram(m, tea.WithAltScreen())`, calls `p.Run()`, and wraps errors with `%w`. Replace the placeholder stub in tui/doc.go's package comment ("Implementation lands in a later phase") — keep the one-directional dependency note. For the home screen, push a minimal placeholder screenModel for now (a stub dashboard screen returning an empty view is acceptable; 05-03 replaces it). Write `tui/model_test.go` with the behavior cases above, following add_test.go fakeDeps idiom. RED stubs use `_`-param signatures returning interface-satisfying zero values (Init→nil, Update→m,nil, view→"") per the project RED-stub convention.
  </action>
  <verify>
    <automated>go test ./tui/... -count=1 && go vet ./tui/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./tui/... -run TestRootModel` passes (View returns tea.View, push/pop, WindowSizeMsg)
    - `tui/model.go` contains `func (m rootModel) View() tea.View`
    - `tui/styles.go` contains `lipgloss.NewStyle()` and NO `NewRenderer`
    - `tui/keymap.go` contains `var keys` with Quit/Back/Refresh/Select bindings
    - `tui/deps.go` contains `func buildTUIDeps` and `func buildTUIDoctorDeps`
    - `tui/tui.go` contains `func Run() error` calling `p.Run()`
    - `grep -rn "NewRenderer" tui/` returns nothing
    - `go build ./...` succeeds (tui importable by cmd/gitid)
  </acceptance_criteria>
  <done>tui/ compiles with v2 APIs; root view-stack model is v2-correct; Run() entry point exists; deps builder wires internal packages without importing package main.</done>
  <maps_to>VALIDATION behaviors: TestWindowSizePropagation (foundation), TestQuitFromAnyScreen (foundation); TUI-01/TUI-02 scaffolding</maps_to>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI/CLI form input → core | Identity names typed by the user cross into keygen/exec arg-slices |
| filesystem → tui/deps.go | os.ReadFile of ~/.ssh/config, ~/.gitconfig (gitid-managed paths) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | identity name input (TUI forms, copy cmd) | mitigate | Extract `identity.ValidateName` charset gate `^[A-Za-z0-9._-]+$`; all form/name consumers call it before any exec/keygen (V5 input validation) |
| T-05-02 | Tampering | tui/deps.go os.ReadFile | mitigate | Paths are gitid-managed trusted paths; annotate `//nolint:gosec // trusted gitid-managed path (G304)` consistent with cmd layer |
| T-05-03 | Tampering | go.mod dependency add | mitigate | charm.land/*/v2 verified via Go module proxy + locked by CLAUDE.md authority (RESEARCH Package Legitimacy Audit — all Approved [VERIFIED], no [ASSUMED]/[SUS]) |
| T-05-SC | Tampering | go get installs | mitigate | All three packages [VERIFIED] in RESEARCH audit; no [ASSUMED]/[SUS]/[SLOP] — no blocking human checkpoint required |
| T-05-04 | Elevation | write-free-core invariant | accept | tui/deps.go closures close over filewriter.Write; depguard D-01 scopes only internal/doctor/** — tui/ is correctly out of scope (RESEARCH Pitfall 10); no regression |
</threat_model>

<verification>
- `go build ./...` succeeds (proves tui importable, no import cycle, v2 deps resolve)
- `go test ./internal/upload/... ./internal/identity/... ./tui/... -count=1` green
- `grep -rn "NewRenderer\|View() string" tui/` returns nothing (v2 correctness)
- `go.mod` contains the three pinned charm.land modules
</verification>

<success_criteria>
- charm.land/bubbletea/v2 v2.0.7, lipgloss/v2 v2.0.3, bubbles/v2 v2.1.0 in go.mod; `go build ./...` green
- `internal/upload.Instructions` and `internal/identity.ValidateName` exist, tested, and importable by both cmd/gitid and tui
- `tui.Run()` + root view-stack model (v2-correct View() tea.View) + keymap + styles + messages + deps builder compile and pass unit tests
</success_criteria>

<output>
Create `.planning/phases/05-cli-surface-tui/05-01-SUMMARY.md` when done.
</output>
