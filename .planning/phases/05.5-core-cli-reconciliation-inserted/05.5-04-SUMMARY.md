---
phase: 05.5-core-cli-reconciliation-inserted
plan: "04"
subsystem: match-strategy-picker
tags: [tdd, match-strategy, flags, non-interactive, hasconfig, gitdir, cli]
dependency_graph:
  requires: [05.5-03]
  provides: [buildMatches, matchKinds, defaultURLPattern, promptMatchStrategy, addFlags, updateFlags]
  affects: [cmd/gitid/add.go, cmd/gitid/update.go, cmd/gitid/match.go]
tech_stack:
  added: []
  patterns:
    - "Match-strategy picker: 3-option menu (gitdir/url/both) replaces single gitdir prompt"
    - "buildMatches(choice, gitdirVal, urlVal) central factory; hasconfig stores remote.*.url: prefix"
    - "Flag-or-prompt: --gitdir/--url/--both presence skips picker; remaining prompts coexist"
    - "Update pre-fill: strategyNumFromKind(matchKinds(existing.Matches)) prevents Pitfall 6 gitdir collapse"
    - "variadic strategyDefault in promptMatchStrategy to decouple add (default=1) vs update (pre-filled)"
key_files:
  created:
    - cmd/gitid/match.go
    - cmd/gitid/match_test.go
  modified:
    - cmd/gitid/add.go
    - cmd/gitid/add_test.go
    - cmd/gitid/update.go
    - cmd/gitid/update_test.go
decisions:
  - "strategyNumFromKind added to match.go (NOT strategyNumber — original was unused and flagged); deferred until update.go actually called it"
  - "promptMatchStrategy uses variadic strategyDefault to cleanly handle add (default 1) vs update (pre-filled from existing.Matches) without overloading the signature"
  - "buildMatches stores Value as remote.*.url:<pattern> for MatchHasconfig (prepends prefix internally); caller passes bare URL pattern; confirmed round-trip via TestHasconfigRoundTrip"
  - "Task 2 RED+GREEN combined: signature changes (addFlags, updateFlags) required simultaneous test and implementation updates; cannot cleanly stage RED-compile-error-free without the stubs AND new tests in same commit"
  - "gatherAddAccount left unchanged (plan scope: gatherCreateInput only); add-account mode still uses single gitdir prompt"
metrics:
  duration: "45 minutes"
  completed: "2026-06-14"
  tasks_completed: 2
  files_modified: 6
---

# Phase 05.5 Plan 04: Match-Strategy Picker + Non-Interactive Flags Summary

Shared match-strategy picker (gitdir/URL/both), `buildMatches` factory, and
`defaultURLPattern` added. Wired into `identity add` and `identity update` with
`--name/--gitdir/--url/--provider` flags that skip prompts. Update pre-fills
the picker from existing match conditions (Pitfall 6 regression guard).

## Updated Prompt Order

### identity add → gatherCreateInput (create-new mode)

```
Create mode: [1/2/3]         → selectMode
Identity name:               → name flag-or-prompt (--name skips)
Git user.name:
Git user.email:
Provider (github/gitlab):    → provider flag-or-prompt (--provider skips)
Host alias [name.provider]:
Hostname [ssh.github.com]:
Port [443]:
Match strategy:              → picker (or buildMatches directly when flags present)
  1) folder (gitdir)         — default
  2) repo URL (hasconfig)
  3) both
Strategy [1]:                → --gitdir skips to choice "1"; --url skips to "2"; both flags → "3"
  (for 1): Match gitdir [~/git/name/]:
  (for 2): URL pattern [git@ssh.github.com:name/**]:
  (for 3): Match gitdir + URL pattern
Passphrase (empty for none):
--- key pair written to ~/.ssh here ---
--- .pub copied to clipboard + upload instructions ---
Pre-write connectivity test:
  [r] retry / [s] skip-&-write / [q] quit
--- persist on PASS or skip+confirm ---
```

### identity update → runIdentityUpdate

```
Git user.name [current]:
Git user.email [current]:
Host alias [current]:        → relabeled "Host alias" (D-10, already was)
Hostname [current]:
Port [current]:
Match strategy:              → pre-filled from matchKinds(existing.Matches)
  1/2/3                      → --gitdir/--url/--provider flags skip picker
Strategy [<current>]:        → "2" pre-filled for hasconfig, "3" for both (Pitfall 6)
  (value prompt for chosen strategy)
Enable commit signing? [y/n]:
--- preview + confirm ---
```

## What Was Built

### Task 1: Shared match-strategy picker + buildMatches (cmd/gitid/match.go)

- `buildMatches(choice, gitdirVal, urlVal string) []gitconfig.Match`:
  - "1"/"gitdir" → single `MatchGitdir{Value: gitdirVal}`
  - "2"/"url"/"hasconfig" → single `MatchHasconfig{Value: "remote.*.url:" + urlVal}`
  - "3"/"both" → `[MatchGitdir, MatchHasconfig]`
  - default (empty/"") → single `MatchGitdir` (safe default, D-07)
- `matchKinds([]Match) string` → "gitdir"/"hasconfig"/"both"/default "gitdir"
- `defaultURLPattern(hostname, name string) string` → `"git@<hostname>:<name>/**"`
- `strategyNumFromKind(kind string) string` → "1"/"2"/"3" for update pre-fill
- `promptMatchStrategy(r, out, gitdirDefault, urlDefault, ...strategyDefault)`:
  - 3-option menu; variadic strategyDefault for update pre-fill vs add default
  - Prompts only needed values based on choice
- `TestHasconfigRoundTrip`: proves `buildMatches("2", "", url)` → `RenderIncludeIf` →
  `conditionToMatch` round-trip stable (T-05.5-14 TOOL-04)

### Task 2: add and update wired with picker + flags

**cmd/gitid/add.go:**
- `addFlags` struct: `name`, `gitdir`, `url`, `provider` string fields
- `newAddCmd` registers `--name`, `--gitdir`, `--url`, `--provider` flags (D-09)
- `runIdentityAdd` and `runCreateNew` now accept `addFlags`
- `gatherCreateInput` signature: `addFlags` replaces deprecated `bool` param
  - `flags.name != ""` → skip name prompt
  - `flags.provider != ""` → skip provider prompt
  - Match strategy: flag combos map directly to `buildMatches`; no flags → `promptMatchStrategy`
  - Two flags (`--gitdir` + `--url`) → `buildMatches("3", ...)` for "both"
- `runReuse` passes `addFlags{}` (empty flags; reuse doesn't expose these flags yet)

**cmd/gitid/update.go:**
- `updateFlags` struct: `gitdir`, `url`, `provider` (NO `name` — positional per Q2)
- `newUpdateCmd` registers `--gitdir`, `--url`, `--provider` flags
- `runIdentityUpdate` accepts `updateFlags`
  - `flags.provider != ""` → `edited.Provider = flags.provider` (D-11/Q3; flag-only)
  - Without `--provider`: existing provider preserved (marker preserved on rewrite)
  - Match strategy: flag combos or `promptMatchStrategy` pre-filled with
    `strategyNumFromKind(matchKinds(existing.Matches))` — prevents Pitfall 6
  - Preview: `renderMatches(edited.Matches)` replaces removed `newMatchDir` var

**Tests (add_test.go):**
- `TestGatherCreateInput_URLStrategyWritesHasconfig` — strategy "2" → MatchHasconfig
- `TestGatherCreateInput_BothStrategyWritesTwoMatches` — strategy "3" → 2 matches
- `TestGatherCreateInput_NameFlag` — --name skips name prompt (9 remaining prompts)
- `TestGatherCreateInput_ProviderFlag` — --provider skips provider prompt
- `TestGatherCreateInput_GitdirFlag` — --gitdir skips picker (8 remaining prompts)
- `TestGatherCreateInput_URLFlag` — --url skips picker, builds MatchHasconfig
- `TestGatherCreateInput_HostAliasLabel` — prompt reads "Host alias" (D-10/F-5)
- All existing tests updated to new 10-prompt sequence (added strategy + gitdir prompts)

**Tests (update_test.go):**
- `TestRunIdentityUpdate_HasConfigPitfall6` — regression guard; hasconfig identity
  must produce MatchHasconfig after update (not collapse to gitdir)
- `TestRunIdentityUpdate_ProviderFlag` — --provider flows into host block
- `TestRunIdentityUpdate_GitdirFlag` — --gitdir skips picker, 6 prompts + confirm
- `TestRunIdentityUpdate_URLFlag` — --url skips picker, builds MatchHasconfig
- `TestNewUpdateCmd_NoNameFlag` — --name must NOT exist on update (Q2)
- `TestNewAddCmd_HasRequiredFlags` — verifies all 4 flags on add
- `TestNewUpdateCmd_HasMatchFlags` — verifies 3 flags on update
- All existing tests updated from 7 to 8 prompts (strategy + gitdir value split)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] strategyNumber flagged unused by golangci-lint**
- **Found during:** GREEN T1 commit (pre-commit hook)
- **Issue:** Added `strategyNumber` helper in match.go but update.go wasn't wired yet; lint rejected it as dead code
- **Fix:** Renamed to `strategyNumFromKind` and deferred it to the Task 2 GREEN commit where it is immediately used by update.go; Task 1 GREEN shipped without it
- **Files modified:** cmd/gitid/match.go

**2. [Rule 1 - Bug] promptMatchStrategy didn't accept strategy pre-fill for update**
- **Found during:** Pitfall 6 regression test failure
- **Issue:** `promptMatchStrategy` hardcoded `prompt(..., "Strategy", "1")` — update.go needed to pre-fill "2" or "3" from existing.Matches
- **Fix:** Changed signature to variadic `strategyDefault ...string`; update.go passes `strategyNumFromKind(matchKinds(existing.Matches))`; add.go passes nothing (defaults to "1")
- **Files modified:** cmd/gitid/match.go, cmd/gitid/update.go

**3. [Rule 2 - Missing] Task 2 RED+GREEN combined**
- **Found during:** Task 2 RED planning
- **Issue:** Changing `runIdentityAdd`/`runIdentityUpdate`/`gatherCreateInput` signatures (adding `addFlags`/`updateFlags`) required ALL existing test call sites to be updated simultaneously; could not compile with only the stub changes
- **Fix:** Updated all existing test call sites in the same commit as the implementation; tagged as "RED+GREEN combined" in commit message
- **Impact:** Audit trail shows 3 commits for T1 (RED/GREEN for match.go) and 1 combined commit for T2; TDD gate compliance slightly weaker for T2 but all tests pass

## Known Stubs

None. All new symbols are fully implemented:
- `buildMatches`: correct for all 3 choices
- `matchKinds`: correct for gitdir/hasconfig/both/empty
- `defaultURLPattern`: returns `git@<hostname>:<name>/**`
- `promptMatchStrategy`: full menu + conditional value prompts
- Flag plumbing in add.go and update.go: complete

## TDD Gate Compliance

| Gate | T1 (match.go) | T2 (add/update) |
|------|---------------|-----------------|
| RED commit (`test(...)`) | 3849ef5 | Combined with GREEN |
| GREEN commit (`feat(...)`) | 8471644 | 9b84c37 |
| Tests pass | Yes | Yes |

T2 RED gate is technically missing a standalone `test(...)` commit — the
signature updates required the test files AND implementation to change together.
Documented above as Deviation #3.

## Threat Flags

No new network endpoints, auth paths, file access patterns, or schema changes
beyond those planned. All `buildMatches` values flow into `RenderIncludeIf` (file
writes), never into `exec.Command` calls (T-05.5-12 gitdir/url injection: mitigated
by file-write-only path). Provider flag validated via existing `identityNameRe`
charset check in the create-new path; update path trusts the flag value as-is
(consistent with how the existing `edited.Alias` = `prompt(...)` works — the
marker render panics on newline per Plan 02 guard, T-05.5-13).

## Self-Check: PASSED

Files created/modified exist:
- cmd/gitid/match.go: func buildMatches, matchKinds, defaultURLPattern, promptMatchStrategy found
- cmd/gitid/match_test.go: round-trip test + full picker tests found
- cmd/gitid/add.go: type addFlags found; gatherCreateInput uses promptMatchStrategy
- cmd/gitid/update.go: type updateFlags found; runIdentityUpdate uses buildMatches/promptMatchStrategy

Commits:
- 3849ef5: test(05.5-04): RED T1 — match strategy picker stubs + failing tests
- 8471644: feat(05.5-04): GREEN T1 — shared match-strategy picker + buildMatches + defaultURLPattern
- 9b84c37: feat(05.5-04): GREEN T2 — picker + flags wired into add and update

go test -race ./cmd/gitid/ ./internal/gitconfig/ → both ok
make lint → 0 issues
