---
phase: 5.5
slug: core-cli-reconciliation-inserted
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-14
---

# Phase 5.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Three layers (CONTEXT.md D-18): TDD unit · deterministic hermetic E2E (fake ssh) ·
> agent-driven experience evaluation (advisory).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (Go 1.26) + golangci-lint v2/gosec |
| **Config file** | Makefile targets; `.golangci.yml` |
| **Quick run command** | `go test ./...` |
| **Full suite command** | `make test && make lint` |
| **E2E command** | `go test -tags e2e ./e2e/...` (hermetic; fake ssh on PATH, sandbox HOME) |
| **Estimated runtime** | ~30–60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `go test ./...`
- **After every plan wave:** Run `make test && make lint`
- **Before `/gsd-verify-work`:** Full suite + E2E must be green
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (planner fills) | — | — | FIX-CREATE-01 | — | un-authenticated key never persists artifacts | unit+e2e | `go test ./internal/identity/... && go test -tags e2e ./e2e/...` | ❌ W0 | ⬜ pending |
| (planner fills) | — | — | FIX-RECON-01 | — | provider marker round-trips; fallback for legacy | unit | `go test ./internal/identity/... ./internal/sshconfig/...` | ❌ W0 | ⬜ pending |
| (planner fills) | — | — | MATCH-URL-01 | — | gitdir/url/both written; flags skip prompts | unit+e2e | `go test ./cmd/gitid/... && go test -tags e2e ./e2e/...` | ❌ W0 | ⬜ pending |
| (planner fills) | — | — | DOC-08 | — | overlap warns; shared detector | unit | `go test ./internal/doctor/...` | ❌ W0 | ⬜ pending |
| (planner fills) | — | — | FIX-INSTALL-01 | — | path + PATH membership reported | unit | `go test ./...` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `e2e/` harness — fake-ssh stub installer (`os.WriteFile` 0o700, PATH prepend; never `sh -c`), sandbox-HOME helper, scripted-stdin driver, build-tag `e2e` (D-20)
- [ ] kevinburke/ssh_config comment-preservation stability test (Assumption A4) — must pass BEFORE relying on the provider marker round-trip (FIX-RECON-01)
- [ ] Provider hostname→provider fallback-map test fixtures

*Existing `internal/*` unit suites cover most logic; Wave 0 adds the E2E harness + A4 stability test.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Agent-driven experience evaluation of the changed CLI flows | FIX-CREATE-01, MATCH-URL-01, FIX-RECON-01, DOC-08, FIX-INSTALL-01 | Qualitative UX/workflow judgment; advisory (not a gate) — feeds Phase 5.6 (D-19) | An agent drives the built binary through create-flow loop, add/update match picker, list, doctor overlap, install; writes a structured findings report (works / confusing / fix). Scope = CLI flows only, NOT the TUI. |
| Real authenticated create against GitHub/GitLab | FIX-CREATE-01 | Needs real network + uploaded key | Follow VERIFICATION-PLAYBOOK create-flow with a real key; confirm persist-only-after-authenticated-PASS |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (E2E harness + A4 stability test)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
