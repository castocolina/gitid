---
phase: 05.5-core-cli-reconciliation-inserted
plan: 07
type: execute
wave: 7
depends_on: ["05.5-01", "05.5-02", "05.5-03", "05.5-04", "05.5-05", "05.5-06"]
files_modified:
  - e2e/create_e2e_test.go
  - e2e/match_e2e_test.go
  - e2e/overlap_e2e_test.go
  - e2e/install_e2e_test.go
  - .planning/phases/05.5-core-cli-reconciliation-inserted/05.5-EXPERIENCE-EVAL.md
autonomous: false
requirements: [FIX-CREATE-01, FIX-RECON-01, MATCH-URL-01, DOC-08, FIX-INSTALL-01]
user_setup: []

must_haves:
  truths:
    - "The full deterministic hermetic E2E suite (`make test-e2e`) is GREEN, proving create-flow gate, match picker, doctor overlap, and install feedback end-to-end (D-18 layer 2 — the completion gate)"
    - "An agent-driven experience evaluation of the five CLI flows this phase changed produces a structured findings report classified by severity (D-19 — advisory, feeds Phase 5.6)"
  artifacts:
    - path: "e2e/create_e2e_test.go"
      provides: "passing create-flow E2E (PASS-gate, quit-keeps-key, skip-confirm, denied-then-pass)"
      contains: "TestCreateFlow_PASSGateAutoPermits"
    - path: ".planning/phases/05.5-core-cli-reconciliation-inserted/05.5-EXPERIENCE-EVAL.md"
      provides: "structured UX findings for the five changed CLI flows, severity-classified"
      contains: "severity"
  key_links:
    - from: "make test-e2e"
      to: "the built gitid binary under sandbox HOME + fake ssh"
      via: "go test -tags e2e ./e2e/..."
      pattern: "tags e2e"
    - from: "05.5-EXPERIENCE-EVAL.md"
      to: "Phase 5.6"
      via: "advisory findings handoff"
      pattern: "Phase 5.6"
---

<objective>
Close the phase on its deterministic gate and capture the advisory experience signal. Turn the
RED E2E shells from 05.5-01 GREEN against the now-implemented behavior (D-18 layer 2 — the actual
completion gate), then run the agent-driven experience evaluation (D-19): drive the REAL built
binary through the FIVE CLI flows this phase changed (create-flow loop, identity add/update match
picker, identity list provider, doctor overlap, install feedback — NOT the TUI) and write a
structured findings report that feeds Phase 5.6.

Purpose: Prove the reconciliation holds end-to-end and harvest UX learnings without blocking on
them. Output: a green hermetic E2E suite + `05.5-EXPERIENCE-EVAL.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-CONTEXT.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-RESEARCH.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-VALIDATION.md
@.planning/phases/05-cli-surface-tui/VERIFICATION-PLAYBOOK.md
</context>

<artifacts_this_phase_produces>
New symbols introduced (excluded from drift verification):
- The E2E test bodies become assertions over real behavior (same test names as 05.5-01; now GREEN)
- `.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-EXPERIENCE-EVAL.md` (advisory report)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Turn the hermetic E2E suite GREEN across all five requirements</name>
  <files>e2e/create_e2e_test.go, e2e/match_e2e_test.go, e2e/overlap_e2e_test.go, e2e/install_e2e_test.go</files>
  <read_first>
    - e2e/harness_test.go (05.5-01 — SandboxHome, BuildBinary, FakeSSHDir, repoRoot)
    - e2e/*_test.go (05.5-01 — the RED shells whose prompt sequences must now match the IMPLEMENTED prompt order)
    - RESEARCH.md §`E2E hermetic scenario — create-flow` (lines 434-475), §Scripted stdin pattern (lines 477-496), Pitfall 1 (stdin exhaustion — keep the scripted sequence in lock-step with the real prompts incl. the new match picker + the loop prompt)
    - cmd/gitid/add.go (post-05.5-03/04 — the ACTUAL prompt order: mode, name, gitname, email, provider, host alias, hostname, port, match-strategy picker, value(s), passphrase, then the loop prompt) — read the implemented flow to fix stdin scripts
    - cmd/gitid/update.go, cmd/gitid/doctor.go (post-05.5-04/05 — overlap warn prompt; install info finding)
    - VALIDATION.md Per-Task Verification Map + Sampling Rate (E2E gate)
    - CONTEXT.md D-18 (layers 1-2 are the completion gate)
  </read_first>
  <action>
    Update each E2E test's scripted stdin to match the IMPLEMENTED prompt sequence (the match-strategy
    picker added in 05.5-04 and the retry/skip/quit loop added in 05.5-03 add lines the 05.5-01 RED
    shells could only approximate). For `create_e2e_test.go`: `TestCreateFlow_PASSGateAutoPermits` (fake
    ssh mode `pass`) asserts the four config artifacts + key pair exist and stdout lacks
    `Write all four artifacts now?`; `TestCreateFlow_QuitKeepsKey` (mode `denied`, loop choice `q`) asserts
    key pair exists but `.ssh/config`/`.gitconfig`/`.gitconfig.d/<id>`/`allowed_signers` do NOT;
    `TestCreateFlow_SkipRequiresConfirm` (mode `denied`, `s` then `y`) asserts artifacts written;
    `TestCreateFlow_DeniedThenPass` (toggle `GITID_FAKE_SSH_MODE` from denied→pass between iterations via
    re-`t.Setenv` then loop `r`) asserts eventual persist. For `match_e2e_test.go`: assert
    `hasconfig:remote.*.url:` in `.gitconfig` for the URL choice and two `[includeIf` conditions for
    `both`; also assert `identity list` shows `provider: github` for a markerless legacy-style block
    (FIX-RECON-01 end-to-end). For `overlap_e2e_test.go`: seed two managed gitconfig+ssh blocks with the
    same gitdir, run `gitid doctor`, assert an `Overlap` warning in stdout and a warning-tier exit.
    For `install_e2e_test.go`: assert `gitid doctor` (or `make install`) output contains the binary path
    + a PATH membership line. Run `make test-e2e` until fully green. Do NOT weaken assertions to pass —
    if a test reveals a real defect, fix the corresponding production code (small follow-up) rather than
    masking it.
  </action>
  <verify>
    <automated>make test-e2e 2>&1 | tee /dev/stderr | grep -qE '^ok' && go test -tags e2e ./e2e/... -count=1 >/dev/null 2>&1 && echo E2E_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags e2e -race ./e2e/...` exits 0 (all create/match/overlap/install E2E tests pass)
    - `TestCreateFlow_QuitKeepsKey` proves NO config artifacts after quit; `TestCreateFlow_PASSGateAutoPermits` proves all four after PASS
    - A match E2E proves `hasconfig:remote.*.url:` written and `identity list` shows `provider: github` for a markerless block
    - No assertion was weakened or `t.Skip`-ped to force green
  </acceptance_criteria>
  <done>The deterministic hermetic suite is green end-to-end, regression-proofing all five reconciliation fixes (D-18 completion gate).</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Agent-driven experience evaluation of the five changed CLI flows (advisory)</name>
  <files>.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-EXPERIENCE-EVAL.md</files>
  <read_first>
    - CONTEXT.md D-19 (agent-driven experience eval scope = ONLY the CLI flows 5.5 fixes; advisory, NOT a gate; feeds Phase 5.6) and D-18
    - VALIDATION.md §Manual-Only Verifications (the experience-eval row) — concrete instructions + scope
    - VERIFICATION-PLAYBOOK.md FEEDBACK ZONE (F-1, F-2, F-3, F-5, F-6, F-7) — the originating complaints to confirm are addressed (or note residual UX friction)
    - cmd/gitid/add.go, update.go, list.go, doctor.go (the changed flows to drive)
  </read_first>
  <action>
    Build the real binary (`make build`). An agent (or the user) drives `./bin/gitid` through the FIVE
    flows THIS PHASE CHANGED ONLY — (1) create-flow loop (generate → upload prompt → test loop →
    retry/skip/quit → persist-after-PASS), (2) `identity add` AND `identity update` match-strategy picker
    (gitdir/url/both + flags), (3) `identity list` provider correctness (incl. a legacy markerless
    identity showing `github`), (4) `gitid doctor` overlap warning, (5) install path/PATH feedback. Use a
    sandbox HOME and the fake-ssh-on-PATH trick (or a throwaway real identity) so no real key upload is
    needed. Write `05.5-EXPERIENCE-EVAL.md` with one section per flow, each finding classified
    works / confusing / fix-suggestion AND a severity (info / minor / major), explicitly noting whether
    the originating F-1/F-2/F-3/F-5/F-6/F-7 complaint is resolved. End with a "Handoff to Phase 5.6"
    summary. This is ADVISORY: findings feed Phase 5.6 and do NOT block phase completion — the
    deterministic layers (Plans 01-06 unit + Task 1 E2E) are the gate. Because `human_verify_mode` is
    end-of-phase and this is an agent/human-run qualitative pass, it is marked non-autonomous.
  </action>
  <verify>
    <human-check>05.5-EXPERIENCE-EVAL.md exists, covers all five changed flows (create loop, add/update match picker, list provider, doctor overlap, install feedback), and each finding carries a works/confusing/fix-suggestion classification plus a severity, with the F-1/F-2/F-3/F-5/F-6/F-7 status noted and a Phase 5.6 handoff section.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-EXPERIENCE-EVAL.md` exists
    - It has one section per the five changed flows
    - Every finding is classified (works / confusing / fix-suggestion) AND severity-tagged (info / minor / major)
    - It explicitly states whether each of F-1, F-2, F-3, F-5, F-6, F-7 is resolved
    - It includes a "Handoff to Phase 5.6" section
  </acceptance_criteria>
  <done>A structured, severity-classified UX report for the five changed CLI flows exists and is ready to inform Phase 5.6; it does not gate this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E child process → sandbox HOME + fake ssh | tests drive the real binary hermetically (no network, no real key) |
| agent eval session → sandbox/throwaway HOME | the advisory pass must not touch the user's real ~/.ssh without explicit care |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-21 | Tampering | E2E test mutating the developer's real ~/.ssh/~/.gitconfig | mitigate | All E2E tests use `SandboxHome(t)` (`t.TempDir()` HOME) + fake ssh on PATH; never the real HOME (05.5-01 harness) |
| T-05.5-22 | Tampering | experience-eval pass writing to the real config | mitigate | Eval instructions mandate a sandbox HOME or a throwaway identity with the fake-ssh trick; the create-flow itself only persists after PASS / explicit skip-confirm (Plan 03) |
| T-05.5-23 | Tampering | E2E green achieved by weakening assertions | mitigate | Acceptance criteria forbid `t.Skip`/weakened assertions; real defects must be fixed in production code |
| T-05.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; existing deps only |
</threat_model>

<verification>
- `make test-e2e` (and `go test -tags e2e -race ./e2e/...`) exits 0
- `make test && make lint` exits 0 (full unit suite + lint still green)
- `05.5-EXPERIENCE-EVAL.md` exists and meets the acceptance criteria
</verification>

<success_criteria>
- The deterministic hermetic E2E suite is green, proving all five fixes end-to-end (D-18 gate)
- The advisory agent-driven experience report exists, covers the five changed flows, is severity-classified, and hands off to Phase 5.6 (D-19)
</success_criteria>

<output>
Create `.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-07-SUMMARY.md` when done
</output>
