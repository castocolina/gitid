---
phase: 05.5-core-cli-reconciliation-inserted
verified: 2026-06-14T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 5.5: Core & CLI Reconciliation — Verification Report

**Phase Goal:** The defects the guided UAT surfaced in the CLI/core are fixed so the engine is trustworthy before the TUI is rebuilt on top of it. Create no longer persists artifacts before an authenticated PASS; provider reconstruction is correct (marker + hostname fallback); install reports its path + PATH membership; matching supports per-URL hasconfig alongside gitdir; doctor detects overlapping/ambiguous matches. CLI/core only — no TUI cosmetics.

**Verified:** 2026-06-14
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Deterministic Gate Results (run during verification)

All three gates passed on the live codebase:

```
go test -race ./...                          → 14/14 packages ok
go test -tags e2e -race -timeout 60s ./e2e/ → ok (8.786s)
make lint                                    → 0 issues
```

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Creating with un-uploaded key does NOT persist any artifact; after PASS it persists; loop offers retry/skip/quit; skip requires explicit confirm | VERIFIED | `runCreateLoop` in `cmd/gitid/add.go:424-450`: PASS auto-persists (line 429-431), quit keeps key with no config write (line 443-446), skip requires `confirm()` gate (line 437-439). `TestCreateFlow_PASSGateAutoPermits`, `TestCreateFlow_QuitKeepsKey`, `TestCreateFlow_SkipRequiresConfirm`, `TestCreateFlow_DeniedThenPass` all PASS. |
| 2 | `identity add` prompts for a match strategy — gitdir / hasconfig / both — and writes the chosen condition(s); labels the field "Host alias" | VERIFIED | `promptMatchStrategy` in `cmd/gitid/match.go:87-112`: emits 3-option menu (lines 89-91), dispatches `buildMatches` (line 111). "Host alias" prompt in `add.go:357`. `--gitdir`/`--url`/`--provider` flags in `addFlags`. `TestMatchStrategy_Gitdir`, `TestMatchStrategy_HasConfig`, `TestMatchStrategy_Both` all PASS. |
| 3 | `identity list` reconstructs and displays the correct `provider` (e.g. `github`) even when the host-alias does not follow `name.provider` | VERIFIED | `hostnameToProvider` map in `internal/identity/loader.go:16-21` maps `ssh.github.com` → `github`. `Reconstruct` at lines 58-62: prefers marker, falls back to map. Old `TrimPrefix` derivation is gone. `TestListProvider_MarkerlessFallback` PASSES: seeds a markerless block with `Hostname ssh.github.com`, asserts `identity list` output contains `provider: github`. |
| 4 | `make install` reports the install path and whether it is on PATH, with a hint when not | VERIFIED | `Makefile` lines 39,113-117: `ORIGINAL_PATH := $(PATH)` captured before `export PATH :=`; install target echo uses `grep -qxF "$(GOPATH_BIN)"` against `ORIGINAL_PATH`. `gitid doctor` surfaces `installInfoFinding()` (`cmd/gitid/doctor.go:249-278`) from `platform.BinaryInstallInfo` (`internal/platform/install.go`). `TestInstall_PathFeedback` and `TestInstall_MakeInstallOutput` PASS. Commit `166afc4` fixed the false-positive PATH check. |
| 5 | `gitid doctor` reports an error/warning when two identities' match conditions overlap, with guidance; detection shared so `add`/`update` warn at write time | VERIFIED | `internal/doctor/checks/overlap.go`: `DetectOverlaps` (lines 24-71) detects identical-gitdir, nested-gitdir, hasconfig-url; `CheckOverlap` wraps as `SeverityWarning` findings. `buildDoctorDeps` wires `CheckOverlap: checks.CheckOverlap` (doctor.go line 481). `warnOverlapAndConfirm` in `add.go:459-484` and `update.go:207-222` share the detector at write time. TUI: `makeFamilyCmd` in `tui/dashboard.go:109-110` dispatches `FamilyOverlap`. `TestDoctor_OverlapWarning_IdenticalGitdir` and `TestDoctor_OverlapWarning_NestedGitdir` PASS. |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `cmd/gitid/add.go` | `runCreateLoop` auth-gated loop + `PersistAll` after PASS | VERIFIED | Lines 417-450: loop, PASS auto-persist, skip+confirm, quit-keeps-key. `buildDeps.Generate` writes key to `~/.ssh` directly (D-01) with `filewriter.EnsureDir` guard (line 516). |
| `cmd/gitid/match.go` | `buildMatches` + `promptMatchStrategy` + `defaultURLPattern` | VERIFIED | Exists, substantive (113 lines), wired into `add.go:388` and `update.go:177`. |
| `internal/sshconfig/reader.go` | `extractProviderMarker` reading `# gitid: provider=` | VERIFIED | Lines 28-37: scans body for prefix, returns trimmed value. Wired in `ParseManagedHosts` line 57. |
| `internal/sshconfig/renderer.go` | `RenderHostBlock` emits `# gitid: provider=` marker | VERIFIED | Lines 42-44: conditionally appends marker when provider non-empty. Panics on `\n`/`\r` (panic guard; CR-01 fix makes valid values guaranteed clean). |
| `internal/identity/loader.go` | `hostnameToProvider` map + marker-prefers-fallback logic | VERIFIED | Lines 16-21 (map), 57-63 (prefer marker, fall back to map). Old `TrimPrefix` derivation absent. |
| `internal/identity/validate.go` | `ValidateProvider` charset validation (CR-01 fix) | VERIFIED | Lines 40-51: rejects whitespace and non-`[A-Za-z0-9._-]`; applied at `add.go:292,364` and `update.go:142`. |
| `internal/doctor/checks/overlap.go` | `DetectOverlaps` / `CheckOverlap` overlap detection | VERIFIED | 169 lines; detects all three overlap kinds (identical-gitdir, nested-gitdir, hasconfig-url). `SeverityWarning` (line 82). |
| `internal/platform/install.go` | `BinaryInstallInfo` + `binaryOnPath` | VERIFIED | 43 lines; `os.Executable()` + `filepath.EvalSymlinks` + PATH scan. Wired in `installInfoFinding()` (doctor.go:250). |
| `tui/dashboard.go` | `FamilyOverlap` dispatch in `makeFamilyCmd` (CR-02 fix) | VERIFIED | Lines 109-110: `case doctor.FamilyOverlap: fn = d.CheckOverlap`. Nil-fn guard (lines 130-135) surfaces false-pass as an error instead of silent green. |
| `e2e/harness_test.go` | Hermetic E2E harness: `SandboxHome`, `BuildBinary`, `FakeSSHDir` | VERIFIED | 160 lines; `-Q` case handles `ProbeKeyTypes`; `sync.Once` build avoids module-cache cleanup errors; `os.WriteFile(..., 0o700)` static literal; no `sh -c`. |
| `e2e/create_e2e_test.go` | 4 create-flow E2E tests (PASS/quit/skip/denied-then-pass) | VERIFIED | All 4 `TestCreateFlow_*` tests PASS in `go test -tags e2e`. |
| `e2e/match_e2e_test.go` | 3 match-strategy E2E tests + `TestListProvider_MarkerlessFallback` | VERIFIED | All 4 tests PASS. |
| `e2e/overlap_e2e_test.go` | 2 doctor overlap E2E tests | VERIFIED | Both `TestDoctor_OverlapWarning_*` PASS. |
| `e2e/install_e2e_test.go` | 2 install-feedback E2E tests | VERIFIED | Both `TestInstall_*` PASS. |
| `internal/sshconfig/marker_roundtrip_test.go` | A4 stability test `TestProviderMarker_RoundTripStable` | VERIFIED | PASS — proves `# gitid: provider=` comment survives `ssh_config.Decode` → `cfg.String()` → `Decode` round-trip with byte-identical field values. |
| `Makefile` | `test-e2e` target + `ORIGINAL_PATH` PATH-check fix | VERIFIED | `test-e2e: build` at line 126 running `go test -tags e2e -race -timeout 60s ./e2e/...`. `ORIGINAL_PATH := $(PATH)` at line 39 (before `export`). |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `runCreateNew` (add.go) | `runCreateLoop` | direct call line 166 | VERIFIED | Returns `persist` bool; on false returns without writing config |
| `runCreateLoop` | `tester.PreWrite` (via `deps.PreWrite`) | `deps.PreWrite` closure line 572-574 | VERIFIED | Checks `pre.Outcome == tester.PASS` (line 428) |
| `identity.PersistAll` | all four artifact writers | called line 188 | VERIFIED | Only reached after `persist==true` from `runCreateLoop` |
| `promptMatchStrategy` (match.go) | `buildMatches` | line 111 | VERIFIED | Returns `buildMatches(choice, gitdirVal, urlVal)` |
| `add.go gatherCreateInput` | `promptMatchStrategy` | lines 381-388 | VERIFIED | Flag-or-prompt dispatch; flags set `buildMatches` directly |
| `update.go` | `promptMatchStrategy` / `buildMatches` | lines 158-177 | VERIFIED | Pre-fills from `existing.Matches` |
| `extractProviderMarker` (reader.go) | `ParseManagedHosts` result | line 57 | VERIFIED | `info.Provider = extractProviderMarker(b.Body)` |
| `Reconstruct` (loader.go) | `hostnameToProvider` fallback | lines 57-63 | VERIFIED | Marker preferred; map used when `ssh.Provider == ""` |
| `DetectOverlaps` (overlap.go) | `warnOverlapAndConfirm` (add.go + update.go) | add.go:461, update.go:219 | VERIFIED | Both write paths call `warnOverlapAndConfirm` before persisting |
| `CheckOverlap` (overlap.go) | `buildDoctorDeps` (doctor.go) | line 481 | VERIFIED | `CheckOverlap: checks.CheckOverlap` in deps struct |
| `FamilyOverlap` (doctor.go) | `makeFamilyCmd` (tui/dashboard.go) | lines 109-110 | VERIFIED | `case doctor.FamilyOverlap: fn = d.CheckOverlap` |
| `BinaryInstallInfo` (platform/install.go) | `installInfoFinding` (doctor.go) | line 250 | VERIFIED | Called; result rendered as SeverityInfo finding under FamilyDeps |
| `ORIGINAL_PATH` (Makefile) | install PATH check | lines 39, 115-117 | VERIFIED | `printf '%s' "$(ORIGINAL_PATH)"` used instead of augmented PATH |
| `make test-e2e` | `go test -tags e2e ./e2e/...` | Makefile line 127 | VERIFIED | Target exists, runs with race and 60s timeout |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `identity list` output (list.go) | `accounts []Account` | `identity.Reconstruct(sshBytes, gcBytes, ...)` reading real disk files | Yes — parses `~/.ssh/config` and `~/.gitconfig` managed blocks | FLOWING |
| `doctor` install finding | `binPath, onPATH` | `platform.BinaryInstallInfo()` → `os.Executable()` + `filepath.EvalSymlinks` | Yes — real binary path | FLOWING |
| overlap E2E assertions | doctor stdout | `CheckOverlap(deps)` → `DetectOverlaps(deps.Identities)` | Yes — `deps.Identities` populated in `buildDoctorDeps` via `identity.Reconstruct` | FLOWING |
| create E2E artifacts | disk files in sandbox HOME | binary writes via `filewriter.Write` after PASS | Yes — real file writes under `t.TempDir()` sandbox | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full unit suite | `go test -race ./...` | 14/14 packages ok | PASS |
| Hermetic E2E suite | `make test-e2e` | `ok github.com/castocolina/gitid/e2e 8.786s` | PASS |
| Lint gate | `make lint` | 0 issues | PASS |
| A4 marker stability | `go test ./internal/sshconfig/ -run TestProviderMarker_RoundTripStable` | PASS | PASS |
| All 12 E2E tests (verbose) | `go test -tags e2e -v ./e2e/...` | 12/12 PASS | PASS |

---

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes exist for this phase. The deterministic completion gate is the E2E suite (`make test-e2e`) and unit suite (`go test -race ./...`), both run directly above.

---

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| FIX-CREATE-01 | 05.5-01 through 05.5-07 | Auth-gated create-flow: generate key first; loop until authenticated PASS; config artifacts written only after PASS or explicit skip-confirm | SATISFIED | `runCreateLoop` + `PersistAll` ordering in `add.go`; 4 create E2E tests pass |
| FIX-RECON-01 | 05.5-02, 05.5-07 | Provider reconstruction via marker comment + hostname-map fallback; old `TrimPrefix` derivation removed | SATISFIED | `extractProviderMarker` + `hostnameToProvider` in `loader.go`; `TestListProvider_MarkerlessFallback` passes |
| FIX-INSTALL-01 | 05.5-06, 05.5-07 | Install reports path + PATH membership from doctor and `make install`; `make install` uses `ORIGINAL_PATH` (not augmented PATH) | SATISFIED | `BinaryInstallInfo` + `installInfoFinding`; `ORIGINAL_PATH` in Makefile; 2 install E2E tests pass |
| MATCH-URL-01 | 05.5-04, 05.5-07 | Match-strategy picker (gitdir/url/both) on `add` and `update`; `--gitdir`/`--url`/`--provider` non-interactive flags; "Host alias" label | SATISFIED | `promptMatchStrategy` + `buildMatches` in `match.go`; flags in `addFlags`/`updateFlags`; 3 match E2E tests pass |
| DOC-08 | 05.5-05, 05.5-07 | Doctor detects overlapping/ambiguous match conditions; shared so `add`/`update` warn at write time; `FamilyOverlap` in TUI | SATISFIED | `DetectOverlaps`/`CheckOverlap` in `overlap.go`; wired in `buildDoctorDeps`; `warnOverlapAndConfirm` in `add.go`+`update.go`; `FamilyOverlap` dispatch in TUI; 2 overlap E2E tests pass |

All five phase requirements from PLAN frontmatter are satisfied. No orphaned requirements (the five IDs cover the phase's complete ROADMAP.md requirement set).

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `cmd/gitid/add.go` | 488-496 | `loadExistingAccounts` silently swallows Reconstruct errors | WARNING (WR-02 from review) | Overlap check may compare against empty slice if config is malformed; deferred to Phase 5.6 |
| `cmd/gitid/add.go` | 159-161 | `CopyPub` error swallowed; user always told "copied to clipboard" | WARNING (WR-03 from review) | User may believe key is on clipboard when it is not; deferred to Phase 5.6 |
| `cmd/gitid/doctor.go` | 96-109 | `runDoctor` reads `os.Stdin`/`os.Stdout` directly instead of injected seams | WARNING (WR-06 — recurring blindspot) | Interactive fix-convergence path untestable via `runDoctor` directly; deferred to Phase 5.6 |

No `TBD`, `FIXME`, or `XXX` debt markers in any Phase 5.5 modified files.

All three warnings are open code-review findings from the code review (05.5-REVIEW.md) that were explicitly deferred to Phase 5.6 as advisory input. They do not block the phase goal. The two critical findings from the review (CR-01: unvalidated provider; CR-02: TUI overlap family not dispatched) were both fixed within the phase (commits `d0d86ab` and `8aa5fef` respectively).

---

### Human Verification Required

None. The phase is CLI/core-only. The experience evaluation (D-19 — advisory) was completed by a dedicated evaluation agent (`05.5-EXPERIENCE-EVAL.md`): all six originating complaints (F-1, F-2, F-3, F-5, F-6, F-7) were confirmed RESOLVED. The F-1 make install false-positive (the one `major` finding) was fixed in commit `166afc4` during the phase. A real-network authenticated create against GitHub/GitLab is documented in `05.5-VALIDATION.md` as a manual-only check; the hermetic E2E suite (fake-ssh on PATH) satisfies the automated gate as designed (D-18/D-19).

---

## Gaps Summary

No gaps. All five roadmap success criteria are observably true in the live codebase, confirmed by passing deterministic test suites and direct code inspection.

---

_Verified: 2026-06-14_
_Verifier: Claude (gsd-verifier)_
