---
phase: 05.5-core-cli-reconciliation-inserted
plan: 05
type: tdd
wave: 5
depends_on: ["05.5-04"]
files_modified:
  - internal/doctor/checks/overlap.go
  - internal/doctor/checks/overlap_test.go
  - internal/doctor/doctor.go
  - cmd/gitid/doctor.go
  - cmd/gitid/add.go
  - cmd/gitid/update.go
  - cmd/gitid/add_test.go
autonomous: true
requirements: [DOC-08]
user_setup: []

must_haves:
  truths:
    - "`gitid doctor` reports a warning when two identities' match conditions overlap (identical gitdir, nested gitdir, or overlapping hasconfig URL globs), naming which identity currently wins and how to disambiguate (D-14, D-15)"
    - "The detection is a shared function so `add`/`update` warn at write time and require an explicit y/N to proceed (D-16)"
    - "Reserved/global blocks (baseline-include, _global) are excluded from overlap detection (Pitfall 4)"
  artifacts:
    - path: "internal/doctor/checks/overlap.go"
      provides: "DetectOverlaps + CheckOverlap shared detector"
      contains: "func DetectOverlaps"
    - path: "internal/doctor/doctor.go"
      provides: "FamilyOverlap + CheckOverlap CheckFn field wired into Run/Families"
      contains: "FamilyOverlap"
  key_links:
    - from: "cmd/gitid/add.go persist path"
      to: "checks.DetectOverlaps"
      via: "warn + y/N before PersistAll"
      pattern: "DetectOverlaps"
    - from: "cmd/gitid/doctor.go buildDoctorDeps"
      to: "checks.CheckOverlap"
      via: "CheckOverlap CheckFn field"
      pattern: "CheckOverlap"
---

<objective>
Add a shared overlap/ambiguity detector (DOC-08 / F-7): `DetectOverlaps([]identity.Account)
[]OverlapPair` flags identical gitdir, nested/prefix gitdir, and overlapping `hasconfig` URL
globs (D-14, conservative heuristic OK), excluding reserved/`_global` blocks (Pitfall 4).
`CheckOverlap(deps) []doctor.Finding` wraps it at warning severity (D-15), wired as a new
`FamilyOverlap` in doctor. The SAME `DetectOverlaps` is called by `add`/`update` before persisting:
on overlap, warn (naming the last-wins winner) and require an explicit y/N to proceed (D-16).

Purpose: Two identities matching the same `gitdir:~/git/personal/` is the F-7 "who wins?" ambiguity
the UAT flagged — doctor surfaces it and write paths prevent it silently. Output: one detector,
two consumers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-CONTEXT.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-RESEARCH.md
@.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols introduced (excluded from drift verification):
- `internal/doctor/checks/overlap.go` (new) with: `type OverlapPair struct{ A, B, Kind, Detail string }`,
  `func DetectOverlaps(accounts []identity.Account) []OverlapPair`, `func CheckOverlap(deps doctor.Deps) []doctor.Finding`,
  unexported `gitdirOverlaps(a, b string) bool`, `hasconfigOverlaps(a, b string) bool`
- `doctor.FamilyOverlap Family = "Overlap"` constant + entry in `Families()`
- `doctor.Deps.CheckOverlap CheckFn` field + its call in `doctor.Run`
- `cmd/gitid/doctor.go`: `CheckOverlap: checks.CheckOverlap` wiring in buildDoctorDeps
- `cmd/gitid`: a shared `warnOverlapAndConfirm(r, out, prospective, existing) bool` helper called by add + update persist paths
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: DetectOverlaps + CheckOverlap shared detector (RED→GREEN→REFACTOR)</name>
  <files>internal/doctor/checks/overlap.go, internal/doctor/checks/overlap_test.go</files>
  <read_first>
    - internal/doctor/checks/orphans.go:1-9 (imports), :40-77 (CheckOrphans structure to mirror), :82-88 (reserved-block carve-out to copy), :151-158 (sliceToSet)
    - PATTERNS.md §`internal/doctor/checks/overlap.go (new — shared overlap detector)` (lines 30-127) — exact OverlapPair struct, function signatures, the `gitdirOverlaps` heuristic, reserved-block filter
    - RESEARCH.md Pattern 4 (lines 275-299) — three overlap kinds, conservative hasconfig heuristic, reserved exclusion, last-wins semantics for the warning text; Code Examples gitdir prefix check (lines 600-613); Pitfall 4 (exclude `_global`)
    - internal/doctor/doctor.go:55-65 (Family constants), :96-101 (CheckFn type), :179-233 (Deps.Identities + the Run aggregation), :284-293 (Families())
    - internal/gitconfig/renderer.go Match/MatchKind (gitdir vs hasconfig), internal/gitconfig/reader.go:39-40 (IsReservedBlockName)
    - identity.Account.Matches + Account.Name (internal/identity/identity.go:25-50)
    - CONTEXT.md D-14, D-15
  </read_first>
  <behavior>
    - Test: two accounts with identical gitdir (after trailing-slash normalization) → one OverlapPair Kind `identical-gitdir`
    - Test: nested gitdir (`~/git/` vs `~/git/personal/`) → OverlapPair Kind `nested-gitdir`
    - Test: two accounts with `hasconfig` patterns sharing the same `git@<host>:` prefix / one prefix of the other → OverlapPair Kind `hasconfig-url`
    - Test: non-overlapping gitdirs (`~/git/a/` vs `~/git/b/`) → no pair
    - Test: a reserved block name (`baseline-include`) or `_global` among accounts is filtered → no spurious pair (Pitfall 4)
    - Test: `CheckOverlap` returns `doctor.Finding` entries with `Family == FamilyOverlap`, `Severity == SeverityWarning`, `Fix == nil`, and an Explanation naming both identities + the last-wins winner
    - Test: single account (no pair possible) → empty result
  </behavior>
  <action>
    Create `internal/doctor/checks/overlap.go` (package `checks`). Define `OverlapPair{ A, B, Kind, Detail
    string }`. Implement `DetectOverlaps(accounts []identity.Account) []OverlapPair`: filter out accounts
    where `gitconfig.IsReservedBlockName(a.Name) || a.Name == "_global"` (mirror orphans.go carve-out),
    then for each unordered pair compare their `[]gitconfig.Match`: `gitdirOverlaps(a,b)` (normalize both
    to trailing-slash, return identical / prefix-nested per RESEARCH heuristic) tagging `identical-gitdir`
    or `nested-gitdir`; `hasconfigOverlaps(a,b)` (conservative: equal, or one a prefix of the other, or
    same `git@<host>:` prefix with a subsuming wildcard) tagging `hasconfig-url`. Build a human Detail
    string noting which condition overlaps. Implement `CheckOverlap(deps doctor.Deps) []doctor.Finding`
    calling `DetectOverlaps(deps.Identities)` and wrapping each pair as `doctor.Finding{Family:
    doctor.FamilyOverlap, Severity: doctor.SeverityWarning, Title:..., Explanation: <names both ids + the
    last-written-wins winner>, SuggestedFix: <disambiguate by narrowing one match>, Fix: nil}`. RED-stub
    -under-strict-lint for the RED commit.
  </action>
  <verify>
    <automated>go test -race ./internal/doctor/checks/ -count=1 2>&1 | tee /dev/stderr | grep -qE '^ok' && grep -q 'func DetectOverlaps' internal/doctor/checks/overlap.go && grep -q 'IsReservedBlockName' internal/doctor/checks/overlap.go</automated>
  </verify>
  <acceptance_criteria>
    - `internal/doctor/checks/overlap.go` declares `type OverlapPair`, `func DetectOverlaps(accounts []identity.Account) []OverlapPair`, `func CheckOverlap(deps doctor.Deps) []doctor.Finding`
    - DetectOverlaps detects identical gitdir, nested gitdir, and overlapping hasconfig URLs, and excludes `baseline-include`/`_global` (proven by tests incl. the Pitfall 4 reserved case)
    - CheckOverlap findings are `FamilyOverlap` + `SeverityWarning` + `Fix: nil`
    - `go test -race ./internal/doctor/checks/` exits 0
    - RED then GREEN commits present
  </acceptance_criteria>
  <done>A shared detector flags all three overlap kinds, excludes reserved blocks, and exposes both a raw-pair API for write paths and a doctor.Finding API for the report.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FamilyOverlap into doctor + warn-and-confirm at add/update write time</name>
  <files>internal/doctor/doctor.go, cmd/gitid/doctor.go, cmd/gitid/add.go, cmd/gitid/update.go, cmd/gitid/add_test.go</files>
  <read_first>
    - internal/doctor/doctor.go:55-65 (Family const block — add FamilyOverlap), :211-233 (Deps check fields + Run aggregation — add CheckOverlap field + call), :284-293 (Families() — append FamilyOverlap in a sensible order, e.g. after FamilyOrphans)
    - cmd/gitid/doctor.go:245-441 (buildDoctorDeps — wire `CheckOverlap: checks.CheckOverlap`; `Identities: accounts` already populated at :323), :433-440 (the existing Check* field wiring block to extend)
    - cmd/gitid/add.go runCreateNew persist branch (Plan 03 — call DetectOverlaps before identity.PersistAll), the existing `confirm` helper (:512)
    - cmd/gitid/update.go:170-179 (the confirm + identity.Update call site — add the overlap warn before it)
    - PATTERNS.md §`cmd/gitid/add.go` Overlap warn at write time (lines 363-374), §`cmd/gitid/doctor.go (modify — wire overlap check)` (lines 419-428), §`confirm` shared pattern (lines 597-601)
    - RESEARCH.md Pattern 4 last-wins text + D-16 (warn + explicit y/N; deliberate overlaps stay possible)
    - identity.Reconstruct (loader.go) — how add/update load existing accounts from disk to compare against the prospective one
  </read_first>
  <action>
    In `internal/doctor/doctor.go`: add `FamilyOverlap Family = "Overlap"` to the const block, add a
    `CheckOverlap CheckFn` field to `Deps`, call it in `Run` alongside the other checks, and append
    `FamilyOverlap` to `Families()` (after `FamilyOrphans`). In `cmd/gitid/doctor.go` buildDoctorDeps, add
    `CheckOverlap: checks.CheckOverlap` to the Check* wiring block (Identities is already populated). Add a
    shared `cmd/gitid` helper `warnOverlapAndConfirm(r *bufio.Reader, out io.Writer, prospective
    identity.Account, existing []identity.Account) bool`: build `DetectOverlaps(append(existing,
    prospective))`, filter to pairs involving `prospective.Name`; if none, return true; else print the
    overlap warning (name the conflicting identity + the last-wins note) and return
    `confirm(r, out, "Proceed with overlapping match conditions?")` (D-16). In add.go runCreateNew, before
    calling `identity.PersistAll`, reconstruct existing accounts from disk and call
    `warnOverlapAndConfirm`; on false, abort without writing. In update.go, before the
    `confirm(... "Apply these changes now?")`/Update call, call `warnOverlapAndConfirm` with the edited
    account against the other existing accounts; on false, abort. This is glue wiring an already-tested
    detector — covered by add_test scripted-stdin tests (no new domain logic). Add a `cmd/gitid` test:
    seed two managed blocks with the same gitdir, drive `add` with stdin declining the overlap prompt,
    assert no write occurred.
  </action>
  <verify>
    <automated>go test -race ./internal/doctor/... ./cmd/gitid/ -count=1 2>&1 | tee /dev/stderr | grep -qE '^ok' && grep -q 'FamilyOverlap' internal/doctor/doctor.go && grep -q 'checks.CheckOverlap' cmd/gitid/doctor.go && grep -q 'DetectOverlaps\|warnOverlapAndConfirm' cmd/gitid/add.go</automated>
  </verify>
  <acceptance_criteria>
    - `internal/doctor/doctor.go` declares `FamilyOverlap`, a `CheckOverlap CheckFn` Deps field, calls it in `Run`, and lists it in `Families()`
    - `cmd/gitid/doctor.go` wires `CheckOverlap: checks.CheckOverlap` in buildDoctorDeps
    - `cmd/gitid/add.go` and `cmd/gitid/update.go` call the shared overlap warn+confirm before persisting; declining aborts the write
    - `gitid doctor` renders an `=== Overlap ===` family (proven by a doctor test or the e2e overlap test in 05.5-07)
    - `go test -race ./internal/doctor/... ./cmd/gitid/` exits 0
  </acceptance_criteria>
  <done>doctor reports overlaps as a warning family and add/update warn + require y/N at write time using the one shared detector.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconstructed accounts → overlap comparison | match conditions read from disk drive the warning |
| user y/N at write time → persist decision | overlap confirm gates whether an ambiguous config is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-15 | Tampering | overlap check fed reserved/_global blocks → spurious or destructive findings | mitigate | DetectOverlaps filters `IsReservedBlockName`/`_global` (Pitfall 4); CheckOverlap sets `Fix: nil` so no auto-removal can fight the baseline restore (the prior infinite-loop bug class) |
| T-05.5-16 | Repudiation | silent ambiguous write (user unaware which identity wins) | mitigate | Write paths warn + require explicit y/N naming the last-wins winner (D-16); doctor surfaces it persistently at warning severity |
| T-05.5-17 | DoS | O(n^2) pair scan on many identities | accept | Identity counts are tiny (single-digit); pairwise scan is bounded and runs only at doctor/write time |
| T-05.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; existing deps only |
</threat_model>

<verification>
- `go test -race ./internal/doctor/... ./cmd/gitid/` exits 0
- `make lint` exits 0
- `gitid doctor` exit code stays in the warning tier (1) for overlap-only findings (D-15) — verified by a doctor test asserting `doctor.ExitCode` over an overlap-only finding set
</verification>

<success_criteria>
- doctor warns on identical/nested gitdir + overlapping hasconfig URLs, naming the winner (DOC-08, SC-5)
- add/update warn + require y/N before persisting an overlapping config (D-16)
- reserved/global blocks excluded; severity is warning (exit tier 1)
</success_criteria>

<output>
Create `.planning/phases/05.5-core-cli-reconciliation-inserted/05.5-05-SUMMARY.md` when done
</output>
