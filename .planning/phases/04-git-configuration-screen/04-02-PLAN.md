---
phase: 04-git-configuration-screen
plan: 02
type: tdd
wave: 2
depends_on: [04-01]
files_modified:
  - internal/gitconfig/renderer.go
  - internal/gitconfig/renderer_test.go
  - internal/gitconfig/reader.go
  - internal/gitconfig/reader_test.go
  - internal/doctor/checks/reserved_test.go
  - internal/identity/loader_test.go
autonomous: true
requirements: [GITUI-02, GITUI-03, GITUI-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "gitdir, SSH-only hasconfig, and both strategies render and parse without loss; both means OR via two includeIf sections (D-02, D-03)."
    - "The optional provider rewrite is one idempotent reserved block per provider and opting out never deletes an existing provider block (D-05, D-06, D-11)."
    - "Changing an identity email replaces its signer block while preserving all other managed and foreign content (D-04)."
  artifacts:
    - path: "internal/gitconfig/renderer.go"
      provides: "Canonical includeIf and provider rewrite render/write APIs"
    - path: "internal/gitconfig/reader.go"
      provides: "Round-trip reconstruction and reserved rewrite recognition"
    - path: "internal/doctor/checks/reserved_test.go"
      provides: "Doctor non-destruction regression for provider rewrite blocks"
  key_links:
    - from: "internal/gitconfig/renderer.go"
      to: "internal/filewriter/block.go"
      via: "provider-keyed ReplaceBlock writes"
      pattern: "ReplaceBlock"
    - from: "internal/doctor/checks/orphans.go"
      to: "internal/gitconfig/reader.go"
      via: "IsReservedBlockName excludes non-identity blocks"
      pattern: "IsReservedBlockName"
---

<objective>
Make every locked Git matching and provider-rewrite rule a round-trip-safe UI-free core contract.

Purpose: Give the TUI one canonical renderer/parser for D-02 through D-06 and close the doctor reserved-block loop required by D-11.
Output: Tested strategy/rewrite renderers, reconstruction, and destructive-fix regression coverage.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@recipes/gitconfig.recipe
@.planning/phases/04-git-configuration-screen/04-CONTEXT.md
@internal/gitconfig/renderer.go
@internal/gitconfig/reader.go
@internal/filewriter/block.go
@internal/doctor/checks/orphans.go
@internal/doctor/checks/reserved_test.go

<authority>Every bare D-NN citation means `04-CONTEXT.md`; draft UI-SPEC IDs are never used as unscoped authorities.</authority>
</context>

<feature>
  <name>Conditional include and provider rewrite contracts</name>
  <files>internal/gitconfig/renderer.go, internal/gitconfig/renderer_test.go, internal/gitconfig/reader.go, internal/gitconfig/reader_test.go, internal/doctor/checks/reserved_test.go</files>
  <behavior>
    - `gitdir` defaults to `~/git/&lt;identity&gt;/`, retains a user-edited trailing slash, and round-trips through ParseManagedIncludeIf (D-02).
    - `hasconfig` renders only `hasconfig:remote.*.url:git@&lt;ssh-host&gt;:*/**`; no HTTPS include condition is generated (D-03).
    - `both` renders exactly the gitdir and hasconfig sections pointing to the same fragment, with OR semantics (D-03).
    - A provider rewrite block renders `[url "git@&lt;provider&gt;:"]` with `insteadOf = https://&lt;provider&gt;/`, is keyed by provider rather than identity, and a second identical write is byte-stable (D-05).
    - Declining the rewrite is a no-op; it does not remove an existing provider rewrite (D-06).
    - The rewrite block is excluded from identity reconstruction and survives applying every doctor orphan fix, with a real orphan control proving the test is non-vacuous (D-11).
  </behavior>
  <implementation>RED: add table-driven renderer/parser tests for all strategy cases, provider-sharing, opt-out, and newline/control rejection; add the doctor destructive-control test. GREEN: add explicit provider-rewrite block naming/render/write helpers and extend `IsReservedBlockName` to recognize that bounded name family. Keep identity names and provider names validated before sentinel construction. Reuse `filewriter.ReplaceBlock` and preserve foreign bytes. REFACTOR: centralize condition and reserved-name parsing only if it reduces duplication without obscuring the exact recipe strings.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green the three includeIf strategies</name>
  <files>internal/gitconfig/renderer.go, internal/gitconfig/renderer_test.go, internal/gitconfig/reader.go, internal/gitconfig/reader_test.go</files>
  <behavior>
    - D-02 default/editable gitdir paths retain terminal slash.
    - D-03 hasconfig is SSH-only and both emits two OR alternatives.
    - Render then parse returns the same match kinds, values, and fragment path.
  </behavior>
  <action>Write failing table tests first, including custom SSH aliases and nested git directories. Make `Match.condition`, `RenderIncludeIf`, and parser reconstruction produce the exact locked forms. Validate user-derived values instead of relying on panics at an interactive boundary; keep any internal panic-only helper behind a validated public entry. Do not add the deferred HTTPS hasconfig variant.</action>
  <verify><automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/gitconfig -run 'Test(IncludeIfStrategies|RenderParseRoundTrip|IncludeIfRejectsUnsafeInput)'</automated></verify>
  <done>All three strategy forms are exact, round-trip stable, and reject syntax-breaking input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green the provider-level insteadOf block</name>
  <files>internal/gitconfig/renderer.go, internal/gitconfig/renderer_test.go, internal/gitconfig/reader.go, internal/gitconfig/reader_test.go</files>
  <behavior>
    - Two identities on github.com resolve to one provider-keyed block per D-05.
    - Repeating an enabled write is byte-identical.
    - A disabled request leaves a pre-existing block byte-identical per D-06.
    - Foreign URL and hand-written Git content remain byte-identical.
  </behavior>
  <action>Define a deterministic reserved block name derived from a strictly validated provider hostname. Render the recipe-shaped URL subsection and write through the same atomic backup/idempotent managed-block chokepoint as includeIf. Explicitly do not reuse the Phase-7 baseline `url-rewrites` aggregate block or `RemoveURLRewritesBlock`: those APIs replace/remove the whole shared block and cannot satisfy D-05/D-06 provider-level ownership and opt-out no-op semantics. Keep the new API explicit about enabled versus disabled so opt-out cannot be mistaken for deletion. Commit failing behavior tests before implementation.</action>
  <verify><automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/gitconfig -run 'TestProviderRewrite'</automated></verify>
  <done>The rewrite is provider-scoped, idempotent, opt-out-safe, and preserves all content outside its block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove doctor cannot orphan or reconstruct the rewrite</name>
  <files>internal/gitconfig/reader.go, internal/gitconfig/reader_test.go, internal/doctor/checks/reserved_test.go, internal/identity/loader_test.go</files>
  <behavior>
    - ParseManagedIncludeIf excludes the rewrite block but still returns neighboring identity blocks.
    - CheckOrphans reports neither the rewrite block nor its provider as an identity.
    - Applying every offered fix preserves rewrite bytes; an unreserved control is removed.
    - Identity reconstruction ignores a neighboring provider rewrite while still loading the identity includeIf.
  </behavior>
  <action>Extend the existing L4 reserved-artifact regression rather than creating an unrelated doctor fixture. Seed an identity includeIf, provider rewrite, foreign content, and a genuine orphan; apply every fix and compare the reserved bytes before/after. Cite D-11 in the test documentation. Keep doctor free of `internal/filewriter` imports.</action>
  <verify><automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/gitconfig ./internal/doctor/checks ./internal/identity ./cmd/gitid -run 'Test(ParseManagedIncludeIfExcludesProviderRewrite|OrphansReservedGitRewriteSurvivesFix|OrphansUnreservedGitBlockControl|Load.*ProviderRewrite)'</automated></verify>
  <done>The rewrite is registered as non-identity managed wiring and cannot enter the doctor delete loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Form values → Git section/sentinel names | Provider, path, and alias values shape managed config syntax. |
| Doctor fix engine → managed wiring | Orphan cleanup can delete blocks unless reserved classification is exact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-04 | Tampering | renderer.go | high | mitigate | Reject newline/control and invalid hostname inputs before section or sentinel interpolation. |
| T-04-05 | Denial of Service | hasconfig evaluation | medium | mitigate | Never place remote sections in included fragments and emit only the locked SSH remote condition. |
| T-04-06 | Tampering | doctor orphan fixes | high | mitigate | Reserved-name recognition plus destructive-control regression proves provider blocks survive every fix. |
</threat_model>

<verification>
Run focused RED/GREEN commands, then `make test` and `make lint`. Confirm no package install or external service is introduced.
</verification>

<success_criteria>
- D-02/D-03 strategy output round-trips exactly.
- D-05/D-06 provider rewrite behavior is idempotent and non-destructive.
- D-11 is protected by a non-vacuous doctor regression.
</success_criteria>

<output>
Create `.planning/phases/04-git-configuration-screen/04-02-SUMMARY.md` when done.
</output>
