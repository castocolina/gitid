---
phase: 04-git-configuration-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tuikit/identities.go
  - internal/tuikit/identities_test.go
  - internal/tuikit/reviewfix_test.go
  - internal/gitconfig/renderer.go
  - cmd/gitid/wiring.go
  - cmd/gitid/wiring_test.go
  - e2e/create_flow_pty_e2e_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_test.go
  - .planning/design/create-flow/visual-divergence-allowlist.txt
  - Makefile
autonomous: true
requirements: [GITUI-01, GITUI-02, GITUI-03, GITUI-04, GITUI-05, DLV-06]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "After successful SSH testing, a developer can complete the real Git step and review one combined SSH + Git write ceremony (D-01, D-08)."
    - "The default confirmed path writes a per-identity fragment, gitdir includeIf, default-on provider rewrite, and signer entry whose principal equals user.email byte-for-byte (D-01, D-02, D-04, D-05, D-06)."
    - "Canceling before confirmation leaves the isolated HOME unchanged; Skip Git remains SSH-only (D-08, D-10)."
  artifacts:
    - path: "internal/tuikit/identities.go"
      provides: "Enabled post-SSH Git flow and combined read-only review"
    - path: "cmd/gitid/wiring.go"
      provides: "Real default Git artifact transaction behind CommitCreate"
    - path: "e2e/create_flow_pty_e2e_test.go"
      provides: "Compiled-real-TUI default-path tracer proof"
  key_links:
    - from: "internal/tuikit/identities.go"
      to: "cmd/gitid/wiring.go"
      via: "GitSpec carried into Backend.CommitCreate"
      pattern: "CommitCreate|GitSpec"
    - from: "cmd/gitid/wiring.go"
      to: "internal/gitconfig and internal/keygen"
      via: "confirmed transaction invokes existing safe artifact writers"
      pattern: "WriteFragment|WriteIncludeIf|WriteAllowedSigners"
---

<objective>
Prove one production-quality default Git-configuration path end to end through the compiled real TUI.

Purpose: Replace Phase 3's inert Git step with a test-first tracer that carries real per-identity Git data into the existing confirmed create transaction without weakening Skip Git or pre-confirm safety (D-01, D-02, D-04, D-08, D-10).
Output: Enabled default-gitdir create path, combined read-only review, real Git writes, and a raw-PTY tracer.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@recipes/README.md
@recipes/gitconfig.recipe
@recipes/ssh-config.recipe
@.planning/phases/04-git-configuration-screen/04-CONTEXT.md
@.planning/phases/04-git-configuration-screen/04-UI-SPEC.md
@.planning/design/git-screen/FIELDS.md
@internal/tuikit/backend.go
@internal/tuikit/views.go
@internal/tuikit/identities.go
@cmd/gitid/wiring.go
@e2e/create_flow_pty_e2e_test.go

<interfaces>
Existing contracts to extend, not bypass: `Backend.CommitCreate(DemoIdentity) tea.Cmd`; `GitSpec`; `WizardCommitMsg`; `gitForm.spec`; `wizardModel.reviewCeremony`; `realBackend.commitCreateTransaction`; `gitconfig.WriteFragment`; `gitconfig.WriteIncludeIf`; `keygen.AllowedSignersLine`; `keygen.WriteAllowedSigners`.
</interfaces>

<authority>Every bare D-NN citation in this plan means `04-CONTEXT.md`. The draft `04-UI-SPEC.md` is a canonical implementation aid only where it agrees with those locked decisions and the approved live dummy/FIELDS contract; its colliding IDs are written as UI-D-NN and never used as unscoped allowlist authority.</authority>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive the default Git path through the compiled real TUI</name>
  <files>e2e/create_flow_pty_e2e_test.go, internal/tuikit/identities.go, internal/gitconfig/renderer.go, cmd/gitid/wiring.go, cmd/gitid/wiring_test.go</files>
  <behavior>
    - RED: a 100x30 raw-PTY test reaches the Git step after the real two-stage SSH proof, sees no Phase-3 capability-disabled reason, reviews fragment + gitdir includeIf + allowed_signers with the SSH Host block, confirms once, and observes all artifacts under SandboxHome.
    - The fragment contains exactly user.name, user.email, gpg.format=ssh, user.signingkey as the public-key path, and commit.gpgsign=true at ~/.gitconfig.d/&lt;identity&gt; per D-01; the provider rewrite is checked and written by default per D-05/D-06.
    - The default include condition retains `gitdir:~/git/&lt;identity&gt;/` including the trailing slash per D-02.
    - The signer principal is exactly the entered email bytes and the line carries namespaces="git" plus the staged public key per D-04.
    - Esc before confirmation and the existing Skip Git route create none of the Git artifacts per D-08/D-10, each through a named negative-control test.
  </behavior>
  <action>Start with failing compiled-real-binary PTY happy/skip/cancel tests. Wire one default path end to end using the existing `GitSpec` shape: carry default gitdir and default-checked provider rewrite from `gitForm` through `finishIdentity` into `realBackend.CommitCreate` without crossing backend types into tuikit. Add the production per-provider renderer/writer on that path; Plan 04-02 expands its cases and doctor integration. In the same GREEN commit, change the real compatibility `GitStepDisabledReason` behavior and its direct `wiring_test.go` assertions. Retain the retired copy literal temporarily in a clearly marked cleanup constant/comment so the current copy-freeze hook stays green until Task 2 removes the literal and gate together; do not retain disabled behavior. Extend `reviewCeremony` with Host block, fragment, includeIf, optional rewrite, allowed_signers, targets, and backup promise. Add every Git file and created gitdir to the existing `commitCreateTransaction` operation journal/`createdDirs` before mutation, so the tracer already satisfies D-10 rollback; Plan 04-03 expands failure injection. Keep Skip Git unable to invoke Git writers. Resolve gitdir beneath isolated HOME and reject traversal/symlink escape. Commit RED before GREEN, then run the direct unit tests and `make test` to prove the GREEN commit is hook-safe.</action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./cmd/gitid -run 'TestRealBackendGitStep|TestCommitCreateGitRollback' && TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 ./e2e -run '^TestCreateFlow_Git(ConfigurationDefaultTracer|SkipCreatesNoGit|CancelCreatesNoGit)$' && TERM=dumb SSH_AUTH_SOCK= make test</automated>
  </verify>
  <done>The compiled real TUI completes one default-gitdir SSH+Git create, the on-disk artifacts match D-01/D-02/D-04, cancel and Skip Git remain no-Git-write paths, and the TDD RED and GREEN commits are recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire Phase 3 disabled-copy assertions without breaking test hooks</name>
  <files>Makefile, cmd/gitid/wiring_test.go, internal/tuikit/identities_test.go, internal/tuikit/reviewfix_test.go, e2e/create_flow_pty_e2e_test.go</files>
  <behavior>
    - `make test` no longer requires or expects the Phase 3 disabled Git copy after the real form is enabled.
    - Existing invalid-form tests still require the normal user.name/email validity reason; valid forms can advance.
    - The compiled create-flow tests retain a negative control proving Skip Git and pre-confirm paths create no Git files.
  </behavior>
  <action>Remove only the retired D-19 literal from the copy-freeze gate and update every unit/wiring/e2e assertion in these files that expected the real backend to disable Git. Preserve the frozen normal validity reason and its negative-grep hygiene. Add focused assertions that the review is read-only, shows public paths rather than private material, names only real backup targets, waits for `WizardCommitMsg`, and a second default run is byte-stable. Keep preview and persistence on shared renderers. This task closes the hard-hook blast radius before invoking `make test`; do not delete the compatibility interface method in this phase.</action>
  <verify>
    <automated>TERM=dumb SSH_AUTH_SOCK= go test -race -count=1 ./internal/tuikit ./cmd/gitid && TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 ./e2e -run '^TestCreateFlow_Git(ConfigurationDefaultTracer|ReviewIsReadOnly)$' && TERM=dumb SSH_AUTH_SOCK= make test</automated>
  </verify>
  <done>The real Git step is enabled, all retired disabled-copy assertions are gone from these gate owners, normal invalid-form coverage remains, and the combined review/idempotency invariants pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove stale D-19 visual dispositions</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_test.go, .planning/design/create-flow/visual-divergence-allowlist.txt</files>
  <behavior>
    - Create-flow required-screen metadata no longer classifies the removed disabled-Git copy as an intentional real/dummy divergence.
    - Region extraction continues to detect the normal invalid-form reason and has a mutation negative control.
    - The existing Phase 3 create-flow visual gate remains green after the real Git step becomes available.
  </behavior>
  <action>Remove the stale D-19 region disposition, special-case region matcher, and exact D-19 entry in the create-flow divergence allowlist while preserving every unrelated required screen, protected region, and classification. Update focused metadata tests. This completes the visual-gate half of disabled-state retirement before later Phase 4 states are registered and ensures stale-entry rejection is green.</action>
  <verify><automated>TERM=dumb SSH_AUTH_SOCK= go test -tags screenshot -race -count=1 ./internal/screenshot ./cmd/gitid -run 'Test(GateVisualRegression|NegativeControls|RequiredScreenSpecs)'</automated></verify>
  <done>No stale D-19 classification survives and the existing create-flow visual gate passes against the newly enabled real flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| TUI fields → config renderers | User-controlled name/email/path data enters Git syntax and filesystem targets. |
| Confirmation → user files | A single keystroke authorizes coordinated SSH and Git mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | Git field rendering | high | mitigate | Route values through existing validation and argument-slice Git writers; reject line injection before any write. |
| T-04-02 | Information Disclosure | review preview | high | mitigate | Render only the `.pub` path and public signer line; never read or display private key bytes. |
| T-04-03 | Repudiation | confirmation ceremony | medium | mitigate | Preserve exact target/backup preview and return actual backup paths only after transaction success. |
| T-04-14 | Elevation of Privilege | default gitdir creation | high | mitigate | Resolve beneath isolated HOME and reject traversal/symlink escape before confirmed directory creation. |
</threat_model>

<verification>
Run the focused tests above, then `make test`, `make lint`, and `make test-e2e`. Record each command and result. Confirm the git-screen path is tested only against temporary HOME directories.
</verification>

<success_criteria>
- A real user can advance from SSH proof through a valid Git form into one combined review and confirmed write.
- Default fragment/includeIf/allowed_signers output is recipe-shaped and idempotent.
- No pre-confirm or Skip-Git path writes Git artifacts.
</success_criteria>

<output>
Create `.planning/phases/04-git-configuration-screen/04-01-SUMMARY.md` when done.
</output>
