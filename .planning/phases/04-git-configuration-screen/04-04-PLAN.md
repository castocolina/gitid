---
phase: 04-git-configuration-screen
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - e2e/git_configuration_pty_e2e_test.go
  - internal/screenshot/createflow.go
  - internal/screenshot/createflow_regions.go
  - internal/screenshot/createflow_test.go
  - cmd/gitid/gate_visual_regression_test.go
  - Makefile
  - .planning/design/git-screen/visual-divergence-allowlist.txt
autonomous: true
requirements: [GITUI-01, GITUI-02, GITUI-03, GITUI-04, GITUI-05, DLV-04, DLV-06]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Raw PTY tests drive every approved Git-screen state in the compiled real binary at 100x30, including keyboard, mouse, create, edit, success, and rollback failure (D-09, D-10, D-12)."
    - "A compiled-real-TUI versus live compiled `cmd/gitid-dummy` PTY workflow compares fields, labels, controls, defaults, state order, and usable layout; every difference is classified as ux-improvement or defect (D-12)."
    - "No HTML, MUI, browser, Chromium, pixel, PNG-byte, or historical-artifact parity participates in Phase 4 acceptance (D-12)."
  artifacts:
    - path: "e2e/git_configuration_pty_e2e_test.go"
      provides: "Real and dummy compiled PTY workflow coverage"
    - path: ".planning/design/git-screen/visual-divergence-allowlist.txt"
      provides: "Named, decision-cited semantic classifications"
    - path: "Makefile"
      provides: "Repeatable Phase 4 TUI-only visual gate"
  key_links:
    - from: "e2e/git_configuration_pty_e2e_test.go"
      to: "cmd/gitid and cmd/gitid-dummy"
      via: "BuildBinary/BuildDummyBinary PTY sessions at 100x30"
      pattern: "BuildBinary|BuildDummyBinary|startPTYAt"
    - from: "cmd/gitid/gate_visual_regression_test.go"
      to: ".planning/design/git-screen/visual-divergence-allowlist.txt"
      via: "fails on unclassified or defect differences"
      pattern: "classification|defect|ux-improvement"
---

<objective>
Close Phase 4 with TUI-only, compiled-binary evidence against the live Bubble Tea dummy.

Purpose: Prove every Git-screen workflow and safety state through real PTYs and make unclassified or defective real/dummy differences fail automatically per D-12.
Output: Per-state real PTY suite, paired live-dummy PTY comparison, divergence registry, and repeatable gate.
</objective>

<execution_context>
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/workflows/execute-plan.md
@/Users/ramon/git/personal/ssh-git-config/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/agent-instructions/product-ui.md
@.planning/ONESHOT.md
@.planning/phases/04-git-configuration-screen/04-CONTEXT.md
@.planning/phases/04-git-configuration-screen/04-UI-SPEC.md
@.planning/design/git-screen/FIELDS.md
@e2e/harness_test.go
@e2e/dummy_demo_e2e_test.go
@e2e/create_flow_pty_e2e_test.go
@cmd/gitid/gate_visual_regression_test.go
@internal/screenshot/createflow.go
@Makefile

<authority>Every bare D-NN citation means `04-CONTEXT.md`. The live dummy, approved FIELDS/APPROVAL artifacts, and CONTEXT D-12 decide acceptance. The draft UI-SPEC supplies scoped implementation details only where consistent; its colliding IDs are represented as UI-D-NN. Phase 4 visual registries use scoped `CTX-D-NN`/`UI-D-NN` references so mechanical validation is unambiguous.</authority>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive all Git states in the compiled real TUI</name>
  <files>e2e/git_configuration_pty_e2e_test.go</files>
  <behavior>
    - Raw keyboard PTY paths reach git-form-empty, git-form-filled, match-strategy-select, review-readonly, confirm-write, backup notice/receipt, result-success, and result-failure at 100x30.
    - Raw SGR mouse clicks focus name, email, rewrite toggle, gitdir path, each strategy row, and ceremony controls.
    - Create, SSH-only completion, and complete edit paths are covered; edit changes email and proves stale signer removal.
    - Failure injection proves exact target/restoration copy and byte-identical prior HOME.
  </behavior>
  <action>Create a dedicated e2e file using the existing PTY harness, `SandboxHome`, fake SSH, and decoded-frame coordinate helpers. Build the real binary, drive raw bytes, and inspect the sandbox files after confirmation. Cover every named state and Phase 4 result-failure sibling without replacing state coverage with unit tests. Assert no Git files before confirmation and preserve the fixed 100x30 geometry. Do not start or invoke any browser tool.</action>
  <verify><automated>TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 180s ./e2e -run '^TestGitConfiguration_'</automated></verify>
  <done>Every state and entry mode is reachable through the compiled real TUI, with raw keyboard/mouse and filesystem evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compare compiled real and live dummy PTY workflows</name>
  <files>e2e/git_configuration_pty_e2e_test.go, .planning/design/git-screen/visual-divergence-allowlist.txt</files>
  <behavior>
    - The same semantic checkpoint script drives `BuildBinary` and `BuildDummyBinary` at 100x30.
    - The comparison inventories field order, labels, controls, defaults, navigation/state order, ceremony beats, and required visible content.
    - Every unequal or one-sided checkpoint has a decision-cited `ux-improvement` or `defect` classification; any missing classification fails, and any defect fails.
    - Scoped D-01..D-12 changes are classified against the live dummy; no web artifact is read.
  </behavior>
  <action>Pair two real PTY sessions, one for `cmd/gitid` and one for `cmd/gitid-dummy`, and capture normalized semantic checkpoints rather than comparing terminal escape bytes. Define a strict classification file whose entries name checkpoint, region/affordance, classification, scoped authority (`CTX-D-NN` or `UI-D-NN`), and rationale. Require all differences to consume exactly one entry; reject stale entries and fail on `defect`. Classify improvements for final milestone review. Treat the live dummy behavior as authoritative when no locked Phase 4 divergence applies. Do not use existing HTML routes, MUI sources, Chromium captures, or PNG pixel comparison.</action>
  <verify><automated>TERM=dumb SSH_AUTH_SOCK= go test -tags e2e -race -count=1 -timeout 180s ./e2e -run '^TestGitConfiguration_CompiledRealVsLiveDummyPTY$'</automated></verify>
  <done>The paired compiled PTY workflow passes with zero defects and zero unclassified differences.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register the Phase 4 workflow in the repeatable visual gate</name>
  <files>internal/screenshot/createflow.go, internal/screenshot/createflow_regions.go, internal/screenshot/createflow_test.go, cmd/gitid/gate_visual_regression_test.go, Makefile</files>
  <behavior>
    - `make gate-visual-regression` includes the Phase 4 Git workflow and remains read-only outside temporary directories.
    - Required state inventory is symmetric and fails closed on missing/empty real or applicable dummy checkpoints.
    - The gate rejects stale, broad, unclassified, and defect entries and has a negative control proving one mutation is detected.
    - `make test-e2e` runs the compiled paired-PTY workflow.
  </behavior>
  <action>Extend `RequiredScreenSpecs`, `RegionName`/`AllRegionNames`, capture helpers, and the gate to register all Phase 4 states and consume both the existing create-flow and new git-screen registries. Generalize decision-ref validation to accept existing D-NN/T-NN entries plus scoped CTX-D-NN/UI-D-NN entries; require scoped references for the Phase 4 registry to resolve the CONTEXT/UI-SPEC namespace collision. State exactly in the Makefile target and comments that `gate-visual-regression` invokes the Phase 4 semantic gate while `test-e2e` invokes the paired compiled PTY workflow. Preserve Phase 3 coverage, deterministic read-only routine gates, and a separate content-addressed publication target. Add negative controls for missing states, stale/removed classifications, and cross-registry leakage. Keep all Phase 4 paths independent of browser tooling.</action>
  <verify><automated>TERM=dumb SSH_AUTH_SOCK= make gate-visual-regression && TERM=dumb SSH_AUTH_SOCK= make test-e2e</automated></verify>
  <done>The normal visual and e2e gates exercise Phase 4, fail closed, and leave tracked evidence paths unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test harness → compiled binaries | PTY evidence must exercise production wiring, not fixture-only models. |
| Difference classifier → acceptance gate | Broad or stale allowlisting could hide a real interaction defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-11 | Spoofing | PTY tests | high | mitigate | Build and identify both real binaries explicitly; assert real HOME artifacts only on cmd/gitid path. |
| T-04-12 | Tampering | divergence registry | high | mitigate | Exact checkpoint/region matching, decision citations, stale-entry rejection, defect hard failure, and negative controls. |
| T-04-13 | Information Disclosure | evidence capture | medium | mitigate | Use synthetic keys and temporary HOME; normalize disposable paths and never capture private key bytes. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Post-SSH Git screen reviews and confirms fragment + includeIf + allowed_signers | 04-01, 04-03 | COVERED | Default tracer plus complete reusable flow.
REQ | GITUI-01 | Separate post-SSH per-identity screen | 04-01, 04-03, 04-04 | COVERED | Real flow and PTY proof.
REQ | GITUI-02 | Five fragment fields at ~/.gitconfig.d/&lt;identity&gt; | 04-01, 04-03 | COVERED | Exact fragment contract.
REQ | GITUI-03 | gitdir/hasconfig/both and live preview | 04-02, 04-03 | COVERED | Core round trip and UI controls.
REQ | GITUI-04 | Email-identical allowed_signers | 04-01, 04-03 | COVERED | Create and edit replacement paths.
REQ | GITUI-05 | Read-only review, confirm, backup, idempotent write | 04-01, 04-03, 04-04 | COVERED | Transaction plus PTY failure evidence.
REQ | DLV-04 | Live real TUI versus approved Bubble Tea mockup; classify every difference | 04-04 | COVERED | Paired compiled PTY semantic gate; no browser lane.
REQ | DLV-06 | At least one real PTY e2e path per Git screen/state | 04-04 | COVERED | Every named state plus result-failure driven at 100x30.
RESEARCH | — | No RESEARCH.md exists; Level 0 discovery uses established Go/config/PTY patterns and no new dependency | 04-01..04-04 | COVERED | Existing source patterns verified.
CONTEXT | D-01..D-04 | Fragment, strategies, SSH-only hasconfig, signer replacement | 04-01..04-03 | COVERED | Cited in task behaviors/actions.
CONTEXT | D-05..D-07 | Provider rewrite, opt-out, reusable create/edit flow | 04-02, 04-03 | COVERED | Provider core and shared UI flow.
CONTEXT | D-08..D-11 | Combined ceremony, seven states/diff, rollback, doctor reserved | 04-01..04-04 | COVERED | Transaction, UI, doctor, PTY.
CONTEXT | D-12 | Dummy-only TUI reference and compiled PTY classification | 04-04 | COVERED | Explicit browser-free paired PTY gate.
</source_audit>

<verification>
Run `make test`, `make lint`, `make test-e2e`, and `make gate-visual-regression`. Inspect the test command graph to confirm no HTML/MUI/browser/Chromium target is invoked. Require the Phase 4 review report to show zero defects and zero unclassified differences.
</verification>

<success_criteria>
- Every Git-screen state is proven through a compiled-real 100x30 PTY.
- Compiled real versus live dummy PTY workflow has zero defects and zero unclassified differences.
- Routine gates are deterministic/read-only and no browser lane is used.
</success_criteria>

<output>
Create `.planning/phases/04-git-configuration-screen/04-04-SUMMARY.md` when done.
</output>
